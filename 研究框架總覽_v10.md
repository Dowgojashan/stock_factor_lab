# 研究框架總覽（v10）

> # 🔴 2026-09-02 老師會議：路線變更，本文件第 5 節與 §1.1 已部分過時
>
> **方向 C（總經 → 選群）已由老師裁示終止**——「看起來都跟總經沒關係⋯不會有答案，
> 不會有好答案」。本文件 **§5（第四章·總經→選群）的產物全部保留但不再是論文主體**，
> 轉為第二階段 AI Agent 的基礎建設；**§1.1 的五章骨架第四章待重新定位**。
>
> **老師認定 HRP 這條線已足以畢業**，畢業必考題只剩三項：
> ①anchored walk-forward 多窗次 ②精選比例掃描 ③HRP 為什麼會贏的質性解釋。
>
> **2026-09-04 更新：①②已完成**（`walkforward_matrix.py`，8,370 格），
> 並額外做了**五項穩健性補強**（群數 k 的前視偏誤、統計檢定、共同窗穩健性、
> L3 的 OOS 驗證、交易成本）——完整結果見
> **`_analysis_outputs_robustness/H26_H27_walkforward矩陣結果.md`**。
> 核心結論：A_hrp 對全宇宙基準的 Calmar 勝率 92.3~94.3%，13/13 方案二項檢定顯著，
> 最保守的單一數字 p = 6.6×10⁻⁴。**③H-28 尚未動工。**
>
> **五項補強產出的新事實**（詳見結果文件「第零部分」）：
> ①跨市場早期窗的 k=3 是**退化解**（完整資料看合法、當下看違規）
> ②**MDD 的統計證據弱於報酬**（8/13 vs 13/13）——論文須分清楚是對哪個對手
> ③共同窗換掉後群結構 **ARI 0.732**，不是窗口選擇的產物
> ④**2018-2020 跨市場互補完全消失**——分散化在全球性衝擊期間失效，
>   「免費午餐」提供的是平滑報酬路徑的價值，**不是危機時保本的價值**
> ⑤**買 251 檔打敗買 2,116 檔**；且周轉率**不隨策略數放大**（持股重疊使買賣互相抵銷）
>
> 🔴 **完整的會議裁示與新路線見 `開發待辦追蹤.md` 第 11 節**，衝突處以該節為準。
> 本文件的 §2~§4、§6、§7（資料、管線、HRP 量化、群的解釋、驗證、抗幻覺）**全部仍然有效**。
>
> ---
>
> **文件性質**：這是目前**唯一**描述完整實驗框架與故事線的現行文件，寫於 2026-09-01。
> 所有數字都在寫作當下重新查證過實際產出檔案，不引用舊文件裡的過期數值。
>
> **取代關係**：
> | 舊文件 | 狀態 |
> |---|---|
> | `研究部完整流程_v9.md` | 🔶 **降級為歷史紀錄**。核心管線骨架仍正確，但 v9 之後歷經 H 系列（H-01~H-24）與 S 系列（S-01~S-08）大幅改動：L2 層級已移除、群數改用輪廓係數決定、新增 IS/OOS 驗證分支、新增總經決策層、總經訊號改滾動窗。**遇到衝突以本文件為準。** |
> | `實戰部架構_v8.md` | ❌ **廢止**。理由見 §9。 |
> | `GateC_標記清單_v1.md` | 🔶 仍有效，但只是階段1標記欄位的規格附錄，非架構文件。 |
> | `系統設計文件_v1.md`／`落差處理方案_v1.md` | 🔶 歷史紀錄，描述的是更早期的系統設計，已被實作超越。 |
> | `開發待辦追蹤.md` | ✅ **仍是現行文件**，與本文件分工：本文件講「框架與結果」，待辦追蹤講「每一項的開發歷程、bug 修正史、待辦狀態」。 |

---

## 1. 一句話故事線

> **上萬個看似不同的量化選股策略，其實只有 3~6 個真正獨立的賭注。**
> 我們用 HRP 階層分群把這件事量化出來、用 LLM 把每一群「是什麼」講清楚，
> 再讓總經狀態去決定「這個時候該用哪一群」，最後用 in-sample/out-of-sample
> 與四組對照實驗驗證這套方法是不是真的比「狂灑」和「挑歷史贏家」好。

### 1.1 定案的五章骨架

```
第一章 · 問題    上萬個策略的多樣性假象
第二章 · 量化    HRP 診斷 → 免費午餐只在跨市場 → 有效獨立賭注數
第三章 · 解釋    每一群的身份是什麼（定量特徵 + 視覺化 + LLM 解釋）
第四章 · 應用    總經狀態 → 選群 → 投組（權重用等權）
第五章 · 驗證    IS/OOS 切在投組配置層 + 四組對照實驗
方法論貢獻       抗幻覺架構，貫穿三、四章（不獨立成章）
```

### 1.2 四個方向的取捨（2026-08-29 定案）

| 方向 | 內容 | 決策 |
|---|---|---|
| **A** | 純量化 + LLM 僅做解釋 | ✅ **地基**，第二、三章 |
| **B** | 複製學長路線（LLM 直接選個股） | ❌ **不做**。我們的 LLM 讀不到新聞/財報等非結構化資訊，讓它在群之間選只是把我們自己算的數字重讀一遍，加值有限且是幻覺溫床 |
| **C** | 總經 → 選群 | ✅ **主軸**，第四章。總經環境是我們版本的「非結構化資訊」，LLM 在這裡才有真正的推理空間 |
| **D** | 抗幻覺架構方法論 | ✅ **輔助貢獻**，貫穿三、四章，單獨撐不起一章 |

---

## 2. 資料與策略宇宙

| 項目 | 台股 | 美股 |
|---|---|---|
| 宇宙 | **台灣上市＋上櫃**（1,809 家）⚠️ 見下方訂正 | Russell 3000（2026-07-08 快照，2,972 檔） |
| 資料期間 | 2000-01 ~ 2025-12 | 2000-01 ~ 2025-12 |
| HRP 共同窗（DD-03） | 2007-01 起 | 2002-01 起 |
| 自建宇宙基準 CAGR | **8.4256%** | **11.0556%** |
| 候選策略數（openSec 變體） | **7,128** | **8,682** |

**候選池合計 15,810 個策略**（`_frozen/stage0/candidate_index.parquet`）。

> 🔴 **2026-09-05（M-06）幣別揭露：台股報酬以 TWD 計、美股以 USD 計，全流程未做任何匯率調整。**
>
> 查證結果：`_frozen/stage2/macro/macro_raw.parquet` 的 `indicator_key` 只有五種
> ——`growth`／`growth_alt`／`inflation`／`rate_level`／`rate_direction`，
> **整個 `_frozen/` 沒有任何匯率序列**。
>
> **影響範圍**（只有跨市場的部分受影響，TW/US 各自的樹不受影響）：
> - **XM 樹的相關矩陣**：台美策略的月報酬以不同幣別直接算相關。
>   真實台灣投資人配置美股時，美股部位的 TWD 報酬 = USD 報酬 + 匯率變動，
>   匯率會**同時影響全部美股策略**，等於多加一個共同因子。
> - **M-01 的市場基準相關 0.5512**：同樣是未調整幣別的數字。
> - **跨市場組合的 CAGR / MDD**：把 TWD 與 USD 報酬直接等權平均，
>   隱含假設匯率不變。
>
> **方向不確定**：匯率可能放大也可能抵消跨市場相關，取決於 TWD/USD 與兩市場報酬的
> 共變關係。**本研究無法量化**——需要 collector 提供匯率序列才能做。
> 這是論文 limitations 必寫的一條。

> ⚠️ **這組數字取代舊文件裡的 7,162／6,916**——那是更早的變體版本，現行採用的是
> `_analysis_outputs_phase4/{TW,US}_L4_openSec_final_candidates.csv`。

> 🔴 **2026-09-02 訂正：台股宇宙其實是「上市＋上櫃」，不是只有上市。**
> 資料庫 `exchange_name = 'TWSE'` 這個標籤實際涵蓋整個台灣市場。證據：公司代號首碼分布
> 1xxx 228／2xxx 340／3xxx 309／4xxx 204／5xxx 149／6xxx 358／8xxx 166／9xxx 53，合計 **1,809 家**
> ——證交所（上市）約 1,000 家、櫃買（上櫃）約 800 家，加起來才對得上。
> 舊文件寫「TWSE 全部上市公司 1,775 家」不精確，**論文須寫成「台灣上市及上櫃公司」**。

> 🔴 **2026-09-02 查證：台股窗從 2007 起算的真正原因是「公司還沒掛牌」，不是資料缺漏。**
> 2000 年台灣市場只有 **581 家**公司在交易，2005 年才累積到 1,022 家。首次股價日期散在
> 475 個不同的個別日子、橫跨全部 12 個月（2882 國泰金 2001-12-31、2885 元大金 2002-02-04
> 都對得上真實掛牌日），是 IPO 的形狀而非批次匯入。掛牌前就有財報是正常的——TEJ 收的是
> **公開發行公司**，不是只有已掛牌公司。
> ⚠️ 同時**推翻舊文件的說法**：`落差處理方案_v1.md` 稱「台股 2000-2004 財報幾乎全空、約 90% 是假 0」，
> 實查資料庫**假 0 比例現在是 0.0%**、2000 年也有 1,164 家公司有財報——那批資料已回補。
> 故「捨棄 2000-2006 是淨化不是損失」這個理由**已不成立**，現在只剩「保住策略池完整性」這一個理由
> （2006-01 起算會少 522 個策略，2007-01 只少 1 個）。

---

## 3. 完整管線（前後串接）

```
階段 −1  Phase 1~4 SOP（老師的四階段方法論）
           ↓  final_candidates.csv（openSec 變體）
階段 0   格式整備 → candidate_index.parquet（15,810 列，複合主鍵）
           ↓
階段 1   策略標記 → strategy_scan / strategy_marks / returns_monthly / MKTCAP
           ↓
階段 2a  regime 切割（zigzag，價格驅動）  ┐
階段 2b  總經投資時鐘（指標驅動）          ├→ 兩條獨立軸線，都保留
階段 2c  regime 一致性檢查                ┘
           ↓
階段 3   HRP 階層分群（六棵樹）→ 群定義凍結
           ├─ 主線 stage3       全時間窗，唯一正式群定義
           └─ 分支 stage3_isoos IS 窗建樹、OOS 只驗證（第五章）
           ↓
階段 4   彙整成 strategy_map（15,810 列 × 全欄位）
           ↓
產出 A   20 年情境 × 策略表現對照表（216 格）
決策層   總經狀態 → 選群（三組對照 A_rule / B_llm / C_all）
```

### 3.1 階段 −1 · Phase 1~4 候選策略產生

老師的 SOP，一次只開一個維度：

```
F（體質因子，選什麼樣的公司）× C（動態條件，時間點對不對）× V（估值濾網，貴不貴）

Phase 1  單因子健檢（9桶線性/單調性檢定，Spearman ρ）
Phase 2  F1×F2 不對稱配對（primary 嚴格、secondary 寬鬆）
Phase 3  加動態條件 C（20 種，衍生自 ROE/EPS/FCF_P）
Phase 4  加估值濾網 V（PE 相對估值）
```

**採用 openSec 變體**：primary 用 Phase 1 過關的因子嚴格篩，secondary 全部開放。
理由：Phase 1 檢定的是「因子自己能不能單調預測」，但 secondary 的功能是「提供不同構面
資訊」，兩件事不一樣。台美股都獨立驗證過這個設計（strict/openSec/relaxed/all 四變體對照）。

**已完成的穩健性檢定**：D1（9桶算術合併 vs 3桶實跑）、C1（primary 門檻敏感度）、
B3（規模集中度）、D3（子期間穩健性），台美股都做過。

### 3.2 階段 0~1 · 格式整備與策略標記

- **複合主鍵**：`{market}::{F1}__{F2}__{C}__{V}`——裸 strategy 字串在台美股會碰撞，這是踩過的雷。
- **`passes_alpha_gate`**（H-01）：`CAGR >= BENCHMARK_CAGR[market]`。
  🔴 **實跑 15,810 個策略 100% 通過**——不是門檻沒用，是這關早在 Phase 4 的候選池
  篩選就已經做過，研究部拿到的母體本來就全部贏大盤。這個欄位的價值是把隱含事實
  變成資料契約裡明確可查的欄位。
- **`is_usable`**：TW 6,680/7,128、US 8,361/8,682（合計 15,041/15,810 = 95.1%）。
- **`data_glitch`**：資料異常只標記不淘汰（W-08 定案）。

### 3.3 階段 2a · Regime 切割（軸線一，價格驅動）

zigzag 連續區間切法，四個標籤：牛／熊／危機／盤整。

| 市場 | 總段數 | 牛 | 熊 | 危機 |
|---|---|---|---|---|
| TW | 43 | 22 | 16 | 5 |
| US | 29 | 15 | 11 | 3 |

**危機標籤的上帝視角問題（H-17 軸線一）**：危機要等跌幅完整走完才能確認，本質上是
事後標籤。**2026-09-01 查證定案：現行架構已經正確處理，不需新開發**——
危機標籤目前**沒有任何存活的決策驅動用途**（`ops/tools.py` T1 三處危機格的 method
已在 H-15 改回 `filter`；`output_a._pick_cluster_diversified_crisis()` 程式碼保留但
預設路徑不觸發），只當描述性揭露用。

### 3.4 階段 2b · 總經投資時鐘（軸線二，指標驅動）

四條軸線 `growth / inflation / rate_level / rate_direction`，
用 growth × inflation 的 z-score 切出投資時鐘四格：**復甦／過熱／停滯性通膨／衰退**。

> 🔴🔴 **2026-09-01 發現並修復的真實方法論缺口（H-18②）**
>
> 原本 `stage2b_macro.py` 的 z-score 平均/標準差、四格中位數邊界，是**拿 2000-2025
> 全樣本算一次凍結**的。程式註解自己寫「防 look-ahead」，但那只防住了「IS/OOS 用不同
> 參數導致標準不一致」，**沒防住「2005 年那個月的 z-score 其實用到了 2006-2025 年的
> 資料」這件事**。
>
> **定案：改用 5 年（60 個月）滾動窗**，`min_periods=60` 嚴格要求滿窗才分類。
> 新增 `research/macro_rolling_window.py`，獨立存放於 `_frozen/stage2/macro_rolling/`。
>
> **實測差距比預期嚴重很多**（194 個可比較月份）：
> | 市場 | 分類相同 | 分類不同 |
> |---|---|---|
> | TW | 101 (52.1%) | **93 (47.9%)** |
> | US | 94 (48.5%) | **100 (51.5%)** |
>
> 接近一半的月份分類會變。開發時先用合成資料驗證滾動窗 z-score 真的沒有偷看未來
> （前後段統計性質劇烈不同的序列，前段 z-score 不受後段影響），排除程式邏輯錯誤——
> 這個高差異率反映的是 clock_cell 分類本身對標準化基準相當敏感。
>
> **2026-09-01 已完成全部下游重跑**：階段4 → 產出A → `cluster_macro_conditional`
> → S-05 → S-07，S-02 起的整條決策層鏈路現在都跑在無 look-ahead 的滾動窗訊號上。
> 重跑後最明顯的變化：**US 的 `macro_best_cell` 從原本偏「過熱」變成 98.1%（8,518/8,682）
> 落在「復甦」**，方向跟「凍結版在近期系統性偏判過熱」的發現一致。

**🔴 這兩條軸線是獨立的，不能混為一談**：regime 是價格驅動的事後切割，總經是指標驅動的
即時分類。兩個都要算、都要記錄，當作不同的參考判斷依據。

### 3.5 階段 3 · HRP 階層分群（第二章主體）

**六棵樹**：TW/US/XM × normal/crisis。XM = 跨市場合併樹。

**群數改用輪廓係數客觀決定**（H-03，取代原本寫死的 `L1_TARGET=8`）：

| 樹 | k | 說明 |
|---|---|---|
| TW_normal | **6** | 落在原定義的「5-10 群」範圍內 |
| US_normal | **7** | 實測跟老師舉例方向相反——US 反而比 TW 需要更多群 |
| XM_normal | **3** | k=3 輪廓係數大幅領先（0.279 vs 次高 0.230），研判是台美市場邊界本身主導了低 k 分群，非策略層級的細緻分群。使用者定案「照資料走」 |

**L2 層級已完全移除**（H-04），現行只有 L1 與 L3。

#### 🔴 第二章的三個核心量化結果

**① 有效獨立賭注數（ENB）——多樣性假象的直接證據**

| 樹 | 策略數 N | ENB | 冗餘比 | k/ENB | 群代表獨立性 |
|---|---|---|---|---|---|
| TW_normal | 6,679 | **3.27** | 2,046 | 183.8% | 32.7% |
| US_normal | 8,360 | **4.36** | 1,916 | 160.4% | 21.3% |
| XM_normal | 15,040 | **5.77** | 2,605 | 52.0% | 59.7% |

平均每個真正獨立的訊號背後，藏了約 2,000 個高度重複的策略。

> ⚠️ **誠實揭露**：TW/US 的 HRP 群數（6/7）**超過**了各自的 ENB 理論上限，
> 群代表彼此的獨立性只有 32.7%/21.3%。這代表**不能簡單宣稱「HRP 找到了 k 個獨立群」**
> ——現有 k 個群即使已經分開，彼此仍有相當殘餘冗餘。論文措辭必須精確。

> 🔴🔴 **2026-09-05（M-05）：3.27 這個數字終於有尺度了——而且答案指向 beta。**
>
> | 樹 | **實測 ENB** | 純雜訊虛無 | **市場因子虛無** | 實測/雜訊 | **實測/市場虛無** |
> |---|---|---|---|---|---|
> | TW | 3.27 | 223.19 ± 0.03 | 3.98 ± 0.32 | **1.46%** | **82%** |
> | US | 4.36 | 282.13 ± 0.03 | 5.73 ± 0.51 | **1.55%** | **76%** |
> | XM | 5.77 | 225.30 ± 0.02 | 6.86 ± 0.59（雙因子）| **2.56%** | **84%** |
>
> **① 對純雜訊：實測只有 1.5~2.6%** ——這是「多樣性假象」最直接的量化。
>
> ⚠️ **純雜訊的上界不是 N 是 T**：N=6,679~15,040 而 T=228~288，相關矩陣的秩最多
> **T−1**，有 N−T+1 個特徵值恆為 0。所以純雜訊的 ENB 是 **223~282**，不是上萬。
> （稽核清單原本假設可以拿 N 當上界，那是錯的。）
>
> **② 對市場因子虛無：實測已經是它的 76~84%**（甚至略**低於**虛無 2~3σ）
> ⇒ **那 3.27/4.36/5.77 幾乎完全是市場 beta 造成的，不是策略選擇的結果**，
> 與 §3.5② 的 M-01 結論完全一致。實測略低於虛無代表真實資料的共同結構
> 比單純的市場因子**還要更集中**一點。
>
> ⚠️ **跨市場樹必須用雙因子虛無**（台股大盤 + 美股大盤，相關 0.5512）。
> 開發初版用單因子，XM 得到虛無 4.49 < 實測 5.77，看似「比虛無更分散 +3.1σ」
> ——**那是虛無設定錯，不是發現**。換成正確的雙因子虛無 6.86 後，實測其實低於虛無。
>
> 詳見 `_analysis_outputs_robustness/enb_null.csv`（`python -m research.enb_null`）。

**② 免費午餐只在跨市場（群間相關，用現行群數重算）**

| 樹 | 同市場配對 | 平均相關 | 跨市場配對 | 平均相關 |
|---|---|---|---|---|
| TW_normal | 15 對 | 0.779 | — | — |
| US_normal | 21 對 | 0.907 | — | — |
| XM_normal | 1 對 | 0.938 | **2 對** | **0.542** |

同市場內的群相關 0.78~0.94，跨市場配對降到 0.54。

> 🔴🔴 **2026-09-05 定案（M-01）：這裡的「分散來源是市場邊界」必須理解為
> 字面意義的「就是台美兩個市場之間的相關」，策略選擇沒有提供任何額外貢獻。**
>
> 實測 **台股全池等權 vs 美股全池等權的相關係數 = 0.5512**（228 個月）。
> 進一步用純 beta 模型 `corr = ρ(market) × √(R²_i·R²_j)` 對照實測：
>
> | 層級 | 群大小中位 | 群 R² 中位 | 實測跨市場 | **beta 模型預測** | 超額 | 低於預測% |
> |---|---|---|---|---|---|---|
> | **L1** | 6,464 | 0.9920 | **0.5418** | **0.5404** | +0.0014 | 50.0% |
> | **L3** | 23 | 0.8470 | **0.4580** | **0.4627** | −0.0047 | 55.7% |
>
> **兩個層級的實測值都跟純 beta 模型吻合到小數點後第二位**（49,496 個 L3 配對中
> 只有 55.7% 低於預測，純隨機是 50%）。扣掉各自市場的 beta 後，殘差相關落在
> 機械下限 −1/(k−1)，**沒有任何系統性共動**；XM 樹在 L1 的 ARI(原始, 殘差) 只有
> **0.2302**——「一個台股群 + 兩個美股群」的結構扣掉 beta 就消失了。
>
> ⚠️ **殘差相關看起來是負的（TW L1 −0.133、XM L1 same −0.671），但那是機械下限**：
> `mkt` 是同一批策略的等權平均，殘差被強制橫斷面加總 ≈ 0。
> **不可寫成「扣除 beta 後群呈負相關」。**
>
> 這跟 §3.4 的 **ENB 只有 3.27/4.36/5.77** 完全一致——真正獨立的賭注只有
> 「台股 beta」與「美股 beta」，其餘是同一件事的重複包裝。
> **這是「多樣性假象」最強的證據版本。**
>
> 詳見 `_analysis_outputs_robustness/M01_beta_adjusted_correlation.md`。
> ⚠️ 限制：beta 代理用 `B_all`（候選池等權），候選池是全期間贏家故非中性代理，
> 會略微高估 beta；乾淨版需 M-07 的市場指數。但 L1 的 0.5418 vs 0.5512 是
> **兩條基準之間**的比較，不受此偏誤影響。

**⚠️ 但「L1 沒有任何高互補配對（相關 < 0.5）」這句話會誤導——那是聚合效應（H-25）**

判定門檻是 `contracts.COMPLEMENTARITY_CUTS = {高: 0.5, 中: 0.8}`。
L1 層級 39 對配對確實沒有一對達到高互補，但這**不代表策略沒有互補性、也不是門檻該改**：

| | 對數 | 相關中位 | 高互補（<0.5） |
|---|---|---|---|
| XM **L1** 跨市場 | 2 | 0.542 | **0%** |
| XM **L3** 跨市場（453 群，成員中位 23） | 49,496 | 0.458 | **80.5%** |
| XM **L3** 同市場（對照組） | 52,882 | 0.834 | 1.1% |
| XM L3 跨市場（只算成員 ≥ 20 的群） | 18,256 | 0.487 | **62.6%** |
| XM L3 同市場（只算成員 ≥ 20 的群） | 19,419 | 0.880 | 0.3% |
| US L3 同市場（只算成員 ≥ 20 的群） | 12,720 | 0.857 | **0%** |

**機制**：群代表＝成員報酬簡單平均，平均愈多檔、特異成分抵消愈徹底，
代表序列愈趨近該市場大盤。舊 k=8 的 5 對高互補全部來自同一個 305 檔小群
（＝現行 TW_normal 群1），k=3 後被併進 6,679 檔的全台股群。
直接實測：XM群1(6,679檔) vs 美股 = 0.484/0.502，但 TW細群1(305檔) = **0.392/0.408**、
TW細群5(75檔) = **0.325/0.353**。

**同市場配對是天然對照組**（群大小/月份數同量級），排除了「小群估計雜訊」的替代解釋。

**定案：門檻不改。** 0.5 在 L3 層級把跨市場/同市場切得極乾淨（80.5% vs 1.1%），
證明門檻是對的；放寬到 0.55 會讓同市場的 TW群2×群5（0.539）也變高互補，
抹掉核心結論。

> 🔴 **2026-09-05 修正（M-01）：粒度效應的成因不是「小群比較特化」。**
>
> 上面那段「機制」講對了一半（平均愈多檔、代表序列愈趨近大盤），但當時的敘事寫成
> 「免費午餐藏在**小而特化**的群」——**「特化」是詮釋，資料只支持「小」**。
>
> | XM L3 群大小分位 | 群大小中位 | 群對自己市場的 R² 中位 |
> |---|---|---|
> | 最小 25% | 13 | **0.7624** |
> | 次小 | 20 | 0.8302 |
> | 次大 | 29 | 0.8610 |
> | 最大 25% | 62 | **0.9239** |
>
> **corr(群大小, R²) = 0.5115（Spearman 0.7458）**。群越小 → 平均掉的特異變異越少
> → R² 越低 → **beta 驅動的相關被稀釋越多** → 「看起來」越互補。
> L1 的 R² 0.9920 → L3 的 0.8470，掉的 0.145 正好把跨市場相關從 0.5418 拉到 0.4580
> （beta 模型預測 0.5404 → 0.4627）。**粒度效應是特異變異稀釋，不是因子層面的專門化。**
>
> **實務意義仍然成立**：要拿到帳面上較低的組合相關，確實該挑「小群 × 另一個市場」
> ——但要知道那買到的是**特異雜訊的稀釋**，不是新的報酬來源。
> 這正是 §3.5③ 代表策略挑選規則要處理的問題。

產出：`_analysis_outputs_robustness/complementarity_granularity_{summary,pairs}.csv`
（`python -m research.complementarity_granularity`）

**③ 群內代表策略挑選規則（H-10）**

品質分數 = Calmar 比率（CAGR/|MDD|，對應老師原話「績效去除以 MDD」）
+ 貪婪多樣性選擇（新代表對已選代表的相關不可超過**該群自己的 avg_intra_corr**，
門檻用群自己的水準而非武斷全域常數）。

**證實了老師講的陷阱是真實存在的**：TW 群5（75 成員）純品質選法彼此相關高達
**0.95**（幾乎是同一策略的複製品），多樣性選擇後降到 **0.83**；US 群1 從 0.96 降到 0.66。
m=3 時 16 群有 14 群、m=5 時 16 群全部能滿足多樣性門檻。

> ⚠️ `m` 的正式數字尚未拍板，3/5 是驗證規則穩健用的示範值。

> 🔴🔴 **2026-09-05（M-03）：這套規則買到的是「分散」，不是「報酬」也不是「Calmar」。**
>
> 把分群依據換成**隨機分群**（群大小 profile 與 HRP 完全相同）、其餘全部釘死，
> 200 次抽樣後對照：
>
> | 樹 | OOS CAGR | OOS MDD | OOS Calmar | **OOS ENB** |
> |---|---|---|---|---|
> | TW | **−2.93σ** ❌ | +0.18σ ➖ | **−1.94σ** ❌ | **+1.71σ** ✅ |
> | US | **−2.65σ** ❌ | **−3.18σ** ❌ | **−3.13σ** ❌ | −0.25σ ➖ |
> | XM | **−3.15σ** ❌ | **+2.50σ** ✅ | −1.02σ ➖ | **+3.91σ** ✅ |
>
> **三棵樹的 OOS 報酬全部輸給隨機分群。** 用 H-26/H-27 的 2,700 格矩陣獨立驗證，
> A_hrp 在 Calmar 上贏過 E_top_calmar 的比例只有 **TW 16.8%／US 8.0%／XM 34.0%**，
> 方向一致——**不是單一窗口的意外**。
>
> ⚠️ **不可拿「最大單群佔比」當 HRP 分散的證據**：A_hrp 每群固定挑 m 檔，
> 該值**恆等於 1/k**（套套邏輯，已寫成測試斷言）。
> **ENB 才是證據**（從 OOS 報酬共變異算，與分群定義無關）。
>
> **機制**：隨機子群是全宇宙的代表性樣本，每群的前 5 名 Calmar 都來自同一個地方
> ——A2_random 平均把 **91.2%** 的持股押在單一 HRP 群（A_hrp 是 33.3%）。
> HRP 群則是報酬結構上真的不同的群，「每群 5 檔」的配額才真的換到分散。
>
> **另一個對照 A1_fcombo（因子構成分群）**：XM 市場純度只有 **0.577**，
> 隨機基準 0.556、A_hrp 1.000——**因子構成幾乎完全無法還原市場邊界**，
> 與 §3.5② 的 M-01 結論互相印證。
>
> **論文必須放棄**：「HRP 讓我們選到更會賺的策略」「HRP 提供更好的風險調整後報酬」。
> **站得住**：HRP 的作用是**強制分散**，代價是報酬——這是取捨，不是「HRP 比較準」。
> 詳見 `_analysis_outputs_robustness/M03_partition_control.md`。

> ✅ **但「挑選」本身確實有技術（M-02，引用 H-12 既有結果）。**
>
> M-03 換掉的是**分群依據**；H-12 的 `C_random` 換掉的是**挑選機制**
> ——同樣檔數、從全宇宙**純隨機抽**（200 次獨立抽樣），不做品質排序也不做多樣性檢查：
>
> | 樹 | 指標 | A_hrp | C_random（200 抽樣） | σ |
> |---|---|---|---|---|
> | TW | OOS CAGR | 19.55% | 17.56% ± 0.88% | **+2.27σ** |
> | TW | OOS MDD | −18.55% | −19.95% ± 0.73% | **+1.91σ** |
> | TW | OOS ENB | 2.774 | 2.270 ± 0.179 | **+2.82σ** |
> | US | OOS CAGR | 32.48% | 25.33% ± 1.71% | **+4.19σ** |
> | US | OOS Sharpe | 1.244 | 1.002 ± 0.042 | **+5.73σ** |
> | US | OOS ENB | 3.872 | 2.561 ± 0.188 | **+6.97σ** |
> | XM | OOS CAGR | 28.25% | 22.36% ± 2.32% | **+2.54σ** |
> | XM | OOS Sharpe | 1.389 | 1.123 ± 0.075 | **+3.57σ** |
> | XM | OOS ENB | 5.051 | 3.264 ± 0.282 | **+6.33σ** |
>
> **全部指標、全部三棵樹都是正向，沒有一個例外。**
>
> 🔴 **2026-09-06（M-02b）：上表只是單一窗、legacy 比例、fixed k。已擴到整個矩陣重跑。**
>
> 現行矩陣是 45 窗 × 5 比例 × 2 分配 × 2 k_mode，組合檔數從 **15 檔到 1,504 檔**——
> H-12 的單點結果完全沒有覆蓋這個空間。用 **2,700 個 A_hrp 格子各自對應的
> C_random 抽樣分布**（每組 200 次，去重後 414 組）重算 z 分數：
>
> | 樹 | 指標 | 格數 | **z 平均** | z 中位 | **A 勝率** | z>2 比例 |
> |---|---|---|---|---|---|---|
> | TW | OOS CAGR | 900 | **+4.60** | +4.39 | **94.9%** | 88.7% |
> | US | OOS CAGR | 900 | **+5.21** | +4.48 | **85.6%** | 72.7% |
> | XM | OOS CAGR | 900 | **+5.41** | +4.30 | **92.2%** | 72.9% |
> | TW | OOS Sharpe | 900 | +4.73 | +4.45 | 95.4% | 85.0% |
> | US | OOS Sharpe | 900 | +5.94 | +5.18 | 88.0% | 77.3% |
> | XM | OOS Sharpe | 900 | +5.48 | +4.77 | 91.9% | 75.8% |
> | TW | **OOS ENB** | 720 | **+8.69** | +7.94 | **100.0%** | 95.0% |
> | US | **OOS ENB** | 540 | **+12.61** | +11.04 | **100.0%** | **100.0%** |
> | XM | **OOS ENB** | 360 | **+9.19** | +7.87 | **100.0%** | 96.1% |
> | TW | OOS MDD | 900 | +1.32 | +1.33 | 67.4% | 38.6% |
> | US | OOS MDD | 900 | +4.15 | +3.40 | 86.7% | 64.8% |
> | XM | OOS MDD | 900 | +2.85 | +2.52 | 86.2% | 58.1% |
>
> **結論在整個矩陣上完全成立，而且比單點更強**：
> ENB **三棵樹 100% 的格子都贏**（美股連 z>2 都是 100%），
> CAGR / Sharpe 勝率 85.6~95.4%。**MDD 又是最弱的一項**（台股 z 平均只有 +1.32），
> 與 M-09 的發現一致——A_hrp 的 MDD 優勢真實但較小。
>
> ⚠️ ENB 只在組合 ≤400 檔時計算（與矩陣同一條規則），故格數少於 900。
> ⚠️ 檔數對齊 A_hrp 的**實際 `n_members`**（非 target_total），因 backfill 會使兩者不同。
>
> 產出：`walkforward_random.csv` / `walkforward_random_compare.csv`
> （`python -m research.walkforward_random`）
>
> 🔴 **把 M-02 與 M-03 併起來讀，才是完整而精確的結論**：
>
> | 換掉什麼 | 對照組 | 涵蓋範圍 | 結果 |
> |---|---|---|---|
> | **挑選機制**（品質排序 + 多樣性限制） | `C_random` 純隨機抽 | **2,700 格全矩陣**（M-02b）| **A 大勝且全比例一致**：CAGR 勝率 79~100%、**ENB 勝率每一格都是 100%** ✅ |
> | **分群依據**（HRP vs 隨機分群） | `A2_random` 同 k、同群大小 | **2,700 格全矩陣**（M-03b）| **視比例與市場而定**：報酬多數落敗、XM 回撤全比例勝出、ENB 只在 legacy 勝 |
>
> 🔴 **兩者最乾淨的對比**：`C_random` 的 ENB 勝率是 **每一棵樹、每一個比例都 100%**
> （效果量 +0.68~+2.43），而 `A2_random` 的 ENB 只有 legacy 那一檔勝。
> ⇒ **分散度主要來自「多樣性限制」這條挑選規則，不是 HRP 的群邊界**；
> 群邊界只在配額極小（5 檔／群）時才額外有用。
>
> 🔴🔴 **2026-09-06（M-03b）：分群依據的對照也跑完全矩陣了（2,700 格 × 30 抽樣、6.9 小時）。
> 結果推翻我先前對 M-03 的兩個外推。**
>
> **① 「HRP 買到分散（ENB）」只在 legacy 這一檔成立，1% 以上全部反轉。**
>
> | 樹 | legacy | 1% | 3% | 5% |
> |---|---|---|---|---|
> | TW | **76%（+0.091）** ✅ | 53%（+0.014）➖ | 18%（−0.098）❌ | 17%（−0.162）❌ |
> | US | 38%（−0.076）❌ | 44%（−0.109）❌ | 6%（−0.312）❌ | — |
> | XM | **68%（+0.210）** ✅ | 13%（−0.470）❌ | — | — |
>
> （括號為效果量 A−隨機；ENB 僅在組合 ≤400 檔時計算，故高比例無列。）
>
> M-03 用的正是 legacy，所以它看到的 ENB 優勢是**真的，但只存在於最小的組合規模**。
> **機制**：配額只有 5 檔／群時，HRP 的群邊界會強迫你買到結構真的不同的東西；
> 配額一大，群邊界就不再有作用，而隨機分群的每一群本來就是全宇宙的代表性樣本，
> 分散度反而更高。
>
> **② 「A_hrp 在報酬上落敗」也不是全比例成立——台股會翻正。**
>
> | 樹 | ALL（聚合）| legacy | 1% | 3% | 5% | 10% |
> |---|---|---|---|---|---|---|
> | TW | 50.4% ➖ | **21.7%** ❌ | 29.4% ❌ | 62.8% ✅ | **75.6%** ✅ | 62.8% ✅ |
> | US | 26.4% ❌ | **1.1%** ❌❌ | 2.2% ❌❌ | 25.6% ❌ | 46.7% ➖ | 56.7% ✅ |
> | XM | 22.2% ❌ | 22.2% ❌ | 39.4% ❌ | 15.0% ❌ | 13.9% ❌ | 20.6% ❌ |
>
> ⚠️ **台股的聚合列 50.4% 是假平手**——legacy 只有 21.7%、5% 卻有 75.6%，
> 兩個方向相反的結構被平均掉了。**論文一律逐比例報，不可只報聚合。**
>
> **③ 唯一穩定成立的正面結果：跨市場的 MDD。**
>
> | XM OOS MDD | legacy | 1% | 3% | 5% | 10% |
> |---|---|---|---|---|---|
> | A 勝率 | **87%** | **89%** | 59% | 76% | **86%** |
> | 效果量 | +2.1pp | +1.2pp | +0.8pp | +1.2pp | +1.6pp |
>
> **跨市場樹的 HRP 分群在每一個比例上都讓回撤更淺**（台股/美股則沒有）。
> 這與 M-01 一致：XM 的 L1 分群就是把兩個市場分開，而市場分離確實能減少回撤。
>
> **→ 修正後的正確表述**：
> > HRP 分群的可證實貢獻是**跨市場的回撤控制**（XM MDD 全比例勝率 59~89%），
> > 以及**在最小組合規模（legacy）下的分散度**（TW 76%、XM 68%，1% 以上即反轉）。
> > **它不提供報酬優勢**，跨市場更是全比例落敗（勝率 13.9~39.4%）。
>
> ⇒ **有用的是「按品質排序 + 要求彼此夠不像」這套挑選規則，不是 HRP 這個特定的分群。**
> HRP 唯一可證實的獨立貢獻是**分散**（ENB），而那個分散的內容又是市場 beta（§3.5②、§3.4）。

### 3.6 階段 4 · strategy_map

彙整層，不重算。真正新算的只有三類：`regime_fit`、`macro_best_cell`/`macro_performance`、
`v1_beneficial`。輸出 15,810 列的主表 + 兩張附屬表。

- `regime_fit` 非空比例 10.9%（危機抗跌 1,373、熊市抗跌 433）
- `macro_best_cell` 覆蓋率 100%（**已用滾動窗訊號重算**）
- `v1_beneficial`：13,028 列有對照組可比，23.7% 的子樹 v1（有估值濾網）優於 v0

### 3.7 產出 A · 20 年情境 × 策略表現對照表

72 個 regime 段 × 3 種投資人類型 = **216 格**，其中 **131 格可評估**，
**80/131（61.1%）贏大盤**（等權判定，見 S-04）。

---

## 4. 第三章 · 群的身份是什麼（解釋層）

三層產出，逐層加深：

### 4.1 定量特徵（H-06，純程式無 LLM）

`cluster_profile_quant` + 逐年/逐季**複利**報酬表。群代表序列 = 成員報酬簡單平均。
只做 L1、只做 normal 樹（crisis 樹 17-26 個月不到兩年，做不出年度型態分析）。

**實測發現**：16 群全部「最佳年=2009、最差年=2008」（GFC 崩盤與反彈，符合常識）；
但 `n_years_positive` 跨群確實有意義地不同（TW 群5 高達 16/19 年賺錢、群1 只有 13/19），
對應老師要的「某幾年賺賠不同」。

> 🔴 `top1_factor_type` 查出**全部群都是「混合型」，沒有區分度**——查證後確認是真實
> 現象（15,810 策略裡 73% 本來就是混合型），非 bug。改看 `top1_F1`/`top1_C_source`/`pct_v1`
> 才有意義（TW 群1 的 v1 佔比 99.67% vs 群4 只有 0.83%，差異巨大）。

### 4.2 視覺化（H-07）

四張圖 × 三棵 normal 樹 = 12 張，存 `_analysis_outputs_hrp_clusters/{tree_id}/`：
①群×年度報酬熱力圖 ②群累積報酬疊圖 ③群間相關熱力圖 ④群因子組成堆疊圖。

### 4.3 LLM 解釋（兩個節點）

| 節點 | 產出 | 規模 | 模型 |
|---|---|---|---|
| `cluster_story` | 群**間**互補性的因果解釋 | 39 對 | gpt-5 |
| `cluster_identity` | 單**群**身份描述 | 16 群 | gpt-5 |

**16 群的 identity_label**（實際產出）：

| 樹 | 群 | 身份 |
|---|---|---|
| TW_normal | 1~6 | 估值結合獲利品質／EV_S+動能混合／動能與估值／動能+估值／**P_IC 主導**／估值+獲利混合 |
| US_normal | 1~7 | 估值主導偏小型／混合偏小型／混合偏小型／小型成長估值／EPS+ROE 偏好／混合偏小盤／估值主導混合 |
| XM_normal | 1~3 | 台股混合與估值大型群／美股小型股高占比／美股混合估值為主 |

**XM 樹的三群清楚呈現「一台股群 + 兩美股群」的結構**，跟 §3.5 的跨市場相關數字互相印證。

---

## 5. 第四章 · 總經 → 選群（應用層，方向 C 主軸）

### 5.1 資訊流設計（S-01 → S-02 → S-05）

```
H-06 定量特徵 ┐
H-08 群身份   ┴→ S-01 cluster_macro_interface（純程式組裝，無 LLM）
                    ↓ 排除帶日曆年份欄位、只收 identity_label
階段4 macro_performance → S-02 cluster_macro_conditional（群×四格條件式績效）
                    ↓
              macro_state_snapshot()  +  group_decision_context()
                    ↓
              S-05 三組對照：A_rule / B_llm / C_all
```

### 5.2 🔴 三個關鍵設計決策（每個都是「架構排除，不靠信任」）

**① 決策層看得到「條件式」績效，看不到「無條件」績效（S-02）**

若決策層直接看到「群X 的 CAGR 中位數 23.5%」，幾乎必然導向「總是選 CAGR 最高的群」
——這正是 H-10/H-12 已經證實的陷阱在總經層的翻版。所以決策層拿到的是
「群X 在『過熱』時條件式平均報酬多少」，不是「群X 整體 CAGR 多少」。
`CAGR_median`/`MDD_median`/`annual_ret_mean` 等 9 個無條件績效欄位一律排除。

**② 決策層不給日期（S-03）**

我們無法像學長那樣把驗證期切在 LLM 訓練截止日之後（2026 年當下，模型訓練資料極可能
已涵蓋我們全部 2002-2025 的歷史窗）。改用**「讓 LLM 沒有機會用到記憶，即使它有記憶」**：
prompt 不含日期、不含任何能認出「這是哪一段歷史」的線索。
`macro_state_snapshot(market, month)` 的 `month` 只是查表參數，**絕不出現在回傳值裡**。

**③ 只收 `identity_label`，不收其他三個 LLM 文字欄位（S-01）**

`cluster_identity` 的四個輸出欄位是**同一次呼叫、共用含真實年份的完整上下文**產生的。
即使實測 16 群裡只有兩個欄位明確外洩年份，那也只是這次運氣好、不是架構保證。

### 5.3 三組對照的實際結果（2026-09-01 用滾動窗訊號重跑）

| 情境 | clock_cell | A_rule（純規則） | B_llm（LLM 決策） | A⊆B |
|---|---|---|---|---|
| TW_normal / TW / 2025-12 | 復甦 | [5] | [5, 6] | ✅ |
| US_normal / US / 2025-12 | 復甦 | [1] | [1, 3, 7] | ✅ |
| TW_normal / TW / 2025-05 | 衰退 | [5] | [5, 1, 3] | ✅ |

### 5.4 🔴 重複執行穩定度（S-07，方向 C 可信度 + 方向 D 抗幻覺的共同實證）

同一快照重複呼叫 5 次真實 API（非快取重播）：

| 情境 | 完全一致率 | 平均 Jaccard | 穩定核心 | 不穩定邊緣 | **rule_in_llm_rate** |
|---|---|---|---|---|---|
| XM_normal / 復甦 | 100% | 1.000 | [2,3] | — | **100%** |
| US_normal / 復甦 | 60% | 0.650 | [1] | [3,7] | **100%** |
| TW_normal / 衰退 | 100% | 1.000 | [1,3,5] | — | **100%** |

**關鍵發現：三個情境的 `rule_in_llm_rate` 全部 100%**——A_rule（純規則基準選出的最佳群）
在全部 15 次 LLM 呼叫裡沒有一次被漏掉。代表 **LLM 決策穩定錨定在客觀條件式績效排名上**，
不穩定的只是「要不要多加一個邊際群」，不是整個決策亂跳。

### 5.5 決策頻率（H-19）

**定案：季度、半年兩個都測。**

2026-08-31 實測 `clock_cell` 切換頻率：TW/US 逐月平均只維持 4-5 個月。
🔴 **意外發現：半年取樣並沒有比季度取樣穩定**（TW 季度 36% 換手 → 半年 49%；
US 季度 56% → 59%，皆不減反增，因為 regime 自然存活時間比半年還短）。
這個發現本身值得在論文提一句，說明顆粒度是先看資料才選的、不是憑感覺。

---

## 6. 第五章 · 驗證（老師指定）

### 6.1 IS/OOS 切在投組配置層，不是策略參數層（H-11）

**老師的關鍵指引**：策略池（F×C×V 定義）完全不動，切點在投組配置層。

> 🔴 **原提案已撤銷**：原本用「OOS 要涵蓋 COVID 崩盤/2022 熊市」當選窗標準，
> 這是**先看歷史再回頭挑窗口**，等於用上帝視角選 OOS，跟「論文不可以假設有上帝在」
> 自相矛盾。使用者點出「台美股切不同年份這樣公平嗎」，查證後確認原提案理由站不住腳。

**v2 定案：兩市場 OOS 用同一段絕對日曆時間**

| 市場 | 共同窗 | IS | OOS |
|---|---|---|---|
| TW/XM | 2007-2025 | 2007-01~2018-12（144 月，63.2%） | **2019-01~2025-12**（84 月） |
| US | 2002-2025 | 2002-01~2018-12（204 月，70.8%） | **2019-01~2025-12**（84 月） |

IS:OOS 比例不同是窗口長度天生不同的自然結果，不是選擇性挑出來的。
實作上**完全獨立於主線**（`stage3_hrp_isoos.py` / `_frozen/stage3_isoos/`），
用檔案系統證據驗證過主線產物完全未被改動。

**🔴 結果：同市場不穩定，跨市場穩定**

| 樹 | 互補判定 IS/OOS 一致 | 一致率 |
|---|---|---|
| TW_normal | 8/15 | 53.3% |
| US_normal | 17/21 | 81.0% |
| XM_normal | 3/3 | **100%** |

跨市場的互補性判定在 OOS 完全維持，同市場（尤其台股）則大幅不穩定
——**再次指向「免費午餐的來源是市場邊界」**這個核心結論。
（這同時就是 H-13「互補性穩定度 KPI」要的答案，兩者是同一份產出的兩個編號。）

### 6.2 四組對照實驗（H-12）——精選 vs 狂灑

挑選依據**只用 IS 窗資訊**，五組皆等權：

| 組 | 定義 |
|---|---|
| A_hrp | H-10 規則（每群 5 支），TW 30/US 35/XM 15 檔 |
| D_top_cagr | 純 IS 期間 CAGR 前 N 名 |
| E_top_calmar | 純 Calmar 前 N 名（拆開「品質指標」與「多樣性限制」兩個因素） |
| B_all | 全宇宙 |
| C_random | 200 次獨立抽樣（seed=42）取均值±標準差 |

**🔴 結果分兩層，不能只看老師的假設有沒有成立**

**① 核心問題（30 多支 vs 狂灑）結論清楚且三市場一致：**

| 市場 | A_hrp OOS CAGR | B_all OOS CAGR | C_random OOS CAGR |
|---|---|---|---|
| TW | **19.55%** | 17.59% | 17.56% ± 0.88% |
| US | **32.48%** | 25.31% | 25.33% ± 1.71% |
| XM | **28.25%** | 22.52% | 22.36% ± 2.32% |

回答老師的問題：**不一樣，精選 30 多支確實比狂灑好。**

**② 次要假設（純 CAGR 選股 OOS 會雪崩）只在台股成立，美股/跨市場是相反方向：**

TW 的 D_top_cagr 確實衰退最多（IS→OOS 掉 10.7pp，比 A_hrp 的 6.6pp 更多，符合過擬合假設）；
但 US/XM 的 D_top_cagr 在 OOS 反而**比 IS 更好**，且 OOS CAGR 大幅超越 A_hrp
（US 53.15% vs 32.48%、XM 51.79% vs 28.25%）。

**🔴 已查明根因並修復診斷缺口**：查了 D 組實際選出的策略，發現**不是分散的贏家，
是集中賭注**——**XM 的 D 組 15 檔 100% 集中在單一 HRP 群**、US 的 D 組 54.3% 集中在單一群。
表面上的「D 組 OOS 更好」其實是「剛好押中一個在 2019-2025 表現超好的群」，
是**單一次歷史實現的運氣**，不是「純挑 CAGR 是穩健策略」的證據。

新增的 E_top_calmar 組把兩個因素拆開：**XM 的 E 組（純 Calmar）一樣 100% 集中在同一群**
——問題根源是「沒有多樣性限制」本身，跟用 CAGR 還是 Calmar 選標的無關。

已新增 `n_clusters_covered` / `max_cluster_share` 兩個集中度診斷欄位永久寫進 schema，
以後不需要手動 debug 就能直接看出任何一組是不是集中賭注。

> **這是需要誠實揭露的結果，不是失敗。** 核心結論（精選贏過狂灑）三市場都成立且穩健；
> 「歷史贏家過擬合」只在台股成立，美股/跨市場的反例已定位到根因是集中度而非市場本身
> 的因子持續力。但單一段 84 個月的 OOS 得出的結論仍需謹慎，不宜寫成強因果論述。

---

## 7. 方法論貢獻 · 抗幻覺架構（方向 D）

完整方法論文件：`_analysis_outputs_robustness/S08_抗幻覺架構方法論.md`

### 7.1 三道鐵則 + 一個總原則

| | 內容 |
|---|---|
| 鐵則 1 | **程式的判決不可被 LLM 推翻**——LLM 只能解釋程式已經算出的結論 |
| 鐵則 2 | **結構化輸出**——一律走 JSON Schema strict 模式，不接受自由文字 |
| 鐵則 3 | **只能引用提供的數字**——禁止杜撰未提供的事件、趨勢、消息 |
| **總原則** | **架構排除，不靠信任**——不給 LLM 有機會誤用的資訊，而不是信任它不誤用 |

### 7.2 四個 LLM 節點的稽核狀態

| 節點 | 規模 | 稽核結果 |
|---|---|---|
| T10 `return_story` | ⚠️ **0 筆真實呼叫**，架構已備但尚未上線 | 無真實稽核資料 |
| `cluster_story` | 39 對 | 31 對「低互補」**100% 都有明確揭露「不會帶來分散效果」**，無一遺漏 |
| `cluster_identity` | 16 群 | 自動掃描「升息/降息/通膨環境/景氣循環/適合在」等總經推論字眼，**0 命中** |
| `decision_layer_arms` | 15 次（S-07） | `rule_in_llm_rate` **100%**，決策穩定錨定客觀數字 |

### 7.3 LLM 成本監督（H-23）

`utils/openai_quota.py`：分兩個獨立免費池（標準池 1M tok/日、小模型池 10M tok/日），
呼叫前煞車（預留 5% 餘裕）、持久化帳本 `code/_catalog/llm_usage.jsonl`。
**全部工作至今都在免費額度內完成**，成本可在論文直接引用。

模型配置：`cluster_story` / `cluster_identity` / 決策層 = **gpt-5**；T10 = **gpt-5-mini**。

### 7.4 資料一致性防線（DD-08 凍結鏈）

每個階段執行前 `freeze.verify_inputs(目錄)` 驗上游雜湊、產出後 `freeze.write_manifest(...)`。
「附加」產物（`cluster_temporal_profile` / `cluster_macro_interface` /
`cluster_macro_conditional` / `macro_rolling`）各自有**獨立子目錄的 manifest**，
不污染主線 manifest，但呼叫端必須驗證對應的子目錄。

**測試套件：106/106 全數通過**（`python -m research.tests`）。

---

## 8. 誠實揭露的已知限制

| 限制 | 說明 |
|---|---|
| 美股倖存者偏誤 | Russell 3000 用的是 2026-07-08 單一快照。**2026-09-02 實查：2,990 家全部（100%）到現在仍在交易，一家停止交易的都沒有** |
| **台股倖存者偏誤** 🆕 | **2026-09-02 查出台股有同樣問題**：1,809 家裡 1,776 家（98.2%）至今仍在市，26 年間曾停止交易的只有 33 家——下市公司幾乎沒被收進來。<br>**影響方向**：倒閉下市的通常表現差，排除後基準（台股 8.43%）與策略報酬都被高估；相對比較受影響較小，絕對數字偏高。<br>**老師 2026-09-02 裁示不重跑**：「我們沒有要寫期刊論文⋯因為我們對所有人都一樣，你的 A 組 B 組 C 組都是一樣玩⋯所以這是公平的」——論文限制章節要寫清楚 |
| ENB < k | TW/US 的 HRP 群數超過各自 ENB 理論上限，不能宣稱「找到 k 個獨立群」 |
| **ENB 的低不是「策略太像」的獨立發現** 🆕 | M-05 虛無分布：實測 ENB 是**市場因子虛無的 76~84%**（TW 3.27 vs 3.98、US 4.36 vs 5.73、XM 5.77 vs 6.86）。**ENB 這麼低幾乎完全由市場 beta 解釋**，與 M-01 一致。論文不可把「ENB 只有 3.27」寫成獨立於 beta 的新發現 |
| **walk-forward 的窗次不完全獨立** 🆕 | M-10 實測：anchored 相鄰窗次選出的策略 **Jaccard 0.372、重疊係數 0.541**，且 **35.7% 的組合是每一窗都選中的同一批策略**（方案 K/L 達 50%）。OOS 區間確實不重疊（二項檢定的形式前提成立），但**策略層面部分是同一賭注重複下注，有效樣本數低於名目 n**。rolling 對照只有 0.138/0.034，證實重疊來自 anchored 的巢狀 IS |
| **HRP 不提供報酬優勢** 🆕 | M-03/M-03b 實測（2,700 格全矩陣）：把分群依據換成隨機分群，**A_hrp 在報酬上多數落敗**（XM 全比例勝率 13.9~39.4%、US legacy 僅 1.1%）。可證實的貢獻只有**跨市場回撤**（XM MDD 全比例勝率 59~89%）與**最小組合規模下的分散度**（legacy：TW 76%、XM 68%，1% 以上即反轉）。⚠️ **必須逐比例報**——台股聚合勝率 50.4% 是 legacy 21.7% 與 5% 75.6% 平均出來的假平手 |
| **相關結構未做 beta 調整** 🆕 | `stage3_hrp` 直接對原始月報酬算相關，**沒有扣除市場 beta**。M-01 證明整個相關結構幾乎完全由 beta 決定（策略 R² 中位 0.80~0.88），**跨市場分散＝台美市場相關 0.5512 的重現**，策略選擇零額外貢獻。這不是 bug 而是**發現**（見 §3.5②），但論文必須明講「HRP 分的是 beta 曝險結構，不是 alpha 來源」 |
| **beta 代理的循環性** 🆕 | M-01 的市場代理用 `B_all`（候選池等權），而候選池是**全期間贏過基準**的策略，非中性代理，會略微高估 beta、低估殘差。乾淨版需 M-07 重建等權全市場序列。L1 的「0.5418 ≈ 0.5512」是兩條基準之間的比較，不受此偏誤影響 |
| **無匯率調整** 🆕 | 台股報酬以 TWD 計、美股以 USD 計，跨市場相關與 XM 組合報酬**未做貨幣統一**。查證確認 `macro_raw` 只有 growth／growth_alt／inflation／rate_level／rate_direction 五個指標，`_frozen/` 內**沒有任何匯率序列**，無法量化。真實台灣投資人配置美股會承擔 TWD/USD 波動，且匯率會**同時影響全部美股策略**（等於多一個共同因子）。方向不確定，須向 collector 要資料才能做（M-06，細節見 §2）|
| **組合報酬隱含「每月拉回等權」** 🆕 | `_portfolio_series` 是 `wide.loc[members].mean(axis=0)`——每個月把各策略權重**重設回等權**，不是買進後放著。這是隱含假設，也是隱含成本（每月要在策略之間搬錢）。**實測影響**（OOS 窗、B_all）：每月再平衡的 CAGR **低於** buy-and-hold **0.18pp（TW）／1.69pp（US）／0.88pp（XM）**，MDD 幾乎相同（差 < 0.04pp）。⚠️ 方向是**扣分不是加分**（贏家繼續贏，再平衡等於一直賣掉贏家）。因四組對照全部採同一慣例，**相對比較不受影響**；受影響的是絕對數字（M-08）|
| 單一段 OOS | 84 個月只有一次歷史實現，四組對照的結論不宜寫成強因果 |
| 危機樹樣本量 | crisis 樹只有 17-26 個月，**刻意不做 IS/OOS**（切了兩段都不可靠），只當描述性揭露 |
| T10 未上線 | 抗幻覺稽核缺一個節點的真實資料 |
| 稽核方法 | 靠自動化關鍵字掃描 + 抽樣人工核對，非逐字全文審閱 |
| 決策層樣本 | 只測 3 個情境 15 次呼叫，且只涵蓋 gpt-5 一顆模型 |
| 決策層無回測 | S-05 明確保留範圍邊界——只做靜態單一快照比較，**沒有做無前視偏誤的歷史回測**（見 §10 H-18③）|
| GDP 版本對齊 | 總經資料的版本修正問題屬論文 limitations，未做即時版本還原 |
| MOM 因子（台股） | `factor_value` 幾乎全為 NULL，舊報告的「MOM 台股墊底」結論無效（是資料缺失不是因子沒用）；美股 MOM 正常 |

---

## 9. ❌ 實戰部為什麼廢止

`實戰部架構_v8.md` 描述的是「Agent0/Agent1/Agent2 + LangGraph 編排 + 三種投資人類型
的線上決策流程」。**這條線不做了**，理由：

1. **故事線定案走方向 C 之後，論文的價值主張變了**——從「做一個能跑的多 Agent 交易系統」
   變成「量化多樣性假象 + 總經選群的方法論」。實戰部那套三 Agent 編排是方向 B 的產物，
   而方向 B 已明確不做。
2. **程式從來沒有寫出來**——查證 `code/ops/` 確認：Agent0/Agent1/Agent2 的本體、
   LangGraph 編排、Agent 間資料傳遞格式，repo 裡完全沒有對應程式檔。廢止的是一份
   從未實作的規劃，不是砍掉已完成的工作。
3. **決策層的角色已經由 S 系列取代**——`decision_layer_arms.py` 的三組對照
   （A_rule/B_llm/C_all）就是實戰部「Agent 決策」在論文框架下的正確形式：
   有對照組、有可重複性量化、有抗幻覺稽核，而不是一個無法驗證的線上系統。

> ⚠️ **但工具層 `code/ops/tools.py`（T1~T13）仍在使用中，不可刪除。**
> `research/output_a.py` 實際依賴其中 5 個工具（`t1_get_recommended_criteria`、
> `t2_filter_pool`、`t5_get_complements`、`t9_compute_weights`、`t13_get_cluster_info`），
> 測試套件也有大量 T1~T13 的測試。廢止的是**架構文件描述的 Agent 編排層**，
> 不是底下的資料存取工具層。

---

## 10. 尚未完成的項目

| ID | 內容 | 卡在哪 |
|---|---|---|
| **H-18③** | 軸線三·決策品質：把 A_rule/B_llm 改成真正的 walk-forward（每個決策點只用當時能拿到的資料），跟「事後諸葛選當期實際最好的群」比較差距 | 做法已定（規則基準+事後諸葛跑完整 walk-forward，LLM 只抽樣跑代表性時間點），但**還有兩個參數要決定**：①walk-forward 評估期範圍 ②LLM 抽樣哪些時間點。H-19 的季度/半年兩種頻率要在這個迴圈裡一併支援 |
| **H-20** | 總經因子可泛化性：「不只 GDP，換別的因子也能用同一套方法論跑出來」 | 完全未動工，需要老師指定要加哪個新總經因子 |
| **H-21** | 多維度實驗矩陣彙整：老師對論文本體的定義「一個維度是時間顆粒度、一個維度是總經選哪些因子，每個空間去看 IS/OOS 表現」 | 依賴 H-18③/H-19/H-20 完成 |
| **H-22** | 「HRL」名詞澄清 | 老師從頭到尾講的是 HRL，判斷可能是記錯名詞，建議下次開會直接確認 |
| **S-06** | Prompt 風格當實驗變數 | ⏸ **定案暫緩，不照學長原案做**。①我們的群是統計分群不是投資哲學分群（73% 是混合型），硬套成長/價值標籤是自己編劇本 ②現階段沒有評估手段，多測幾種風格只是多幾份看不出優劣的結果 |
| 決策層產出型態 | LLM 是「直接選群」還是「給排序建議、程式做最終選擇」？後者更符合方向 D 的鐵則 1 但也較弱 | 未定 |
| 群內代表數 `m` | 正式數字未拍板 | 需要 H-12 之外的配置實驗依據 |
| XM 樹在方向 C 的角色 | 只有 3 群且研判是市場邊界主導——要納入決策層還是只當跨市場分散的證據？ | 未定 |
| 總經因子清單 | W-02b 抓取規格已交給 collector，**資料還沒到** | 外部依賴 |

---

## 11. 附錄 · 檔案與執行

### 11.1 凍結產物位置

```
_frozen/
├── stage0/            candidate_index.parquet（15,810 列）
├── stage1/            strategy_scan / strategy_marks / returns_monthly / mktcap
├── stage2/
│   ├── regime/        regime_table_{TW,US}.parquet
│   ├── macro/         macro_raw / macro_history_{TW,US}.parquet（全樣本凍結版，保留供對照）
│   ├── macro_rolling/ macro_history_rolling_{TW,US} + macro_clock_comparison（🆕 現行採用）
│   └── consistency/
├── stage3/            六棵樹（cluster_assign/meta/corr_matrix/linkage）+ 群描述系列
│   ├── _temporal_profile/   H-06 附加 manifest
│   ├── _macro_interface/    S-01 附加 manifest
│   └── _macro_conditional/  S-02 附加 manifest
├── stage3_isoos/      H-11 IS/OOS 驗證分支（完全獨立）
├── stage4/            strategy_map / regime_performance / macro_performance
└── output_a/          scenario_table.parquet（216 格）
```

### 11.2 分析輸出位置

```
_analysis_outputs_phase1~4/      階段 −1 的 Phase 1~4 分析
_analysis_outputs_atlas/         第四章 18 張圖鑑
_analysis_outputs_hrp_clusters/  H-07 群視覺化（12 張）+ 群特性分析.md
_analysis_outputs_robustness/    H09-H12 免費午餐量化 + S08 抗幻覺方法論 + 各項 CSV
_analysis_outputs_variants/      四變體對照（strict/openSec/relaxed/all）
_analysis_outputs_ccorr/ sizecontrol/ dataquality/
```

### 11.3 重跑指令

```bash
cd code    # ⚠️ 一定要在 code/ 目錄下跑，config.ini 用相對路徑讀

# 主線
python -m research.stage0_index
python -m research.stage1_scan  && python -m research.stage1_marks
python -m research.stage2a_regime && python -m research.stage2b_macro && python -m research.stage2c_consistency
python -m research.macro_rolling_window       # H-18② 滾動窗總經（stage4 的實際輸入）
python -m research.stage3_hrp                 # ~548 秒
python -m research.stage4_strategy_map        # ~12 秒
python -m research.output_a

# 群描述與決策層
python -m research.cluster_temporal_profile   # H-06
python -m research.cluster_visualizations     # H-07
python -m research.cluster_identity           # H-08（花錢，有 --dry-run/--resume）
python -m research.cluster_macro_interface    # S-01
python -m research.macro_decision_input       # S-02
python -m research.decision_layer_arms        # S-05（花錢，有 --dry-run）
python -m research.decision_repeatability     # S-07（花錢，15 次呼叫）

# 驗證
python -m research.stage3_hrp_isoos           # H-11，~279 秒
python -m research.four_group_control         # H-12
python -m research.effective_bets             # H-09
python -m research.complementarity_granularity # H-25 互補性粒度效應
python -m research.tests                      # 106/106
```

> ⚠️ Windows 中文輸出：終端機預設 cp950 會讓 `✓` 之類的字元炸掉 `UnicodeEncodeError`，
> 執行時加 `PYTHONIOENCODING=utf-8`。

### 11.4 路徑陷阱

- 根目錄有 `database.py`/`get_data.py`/`combinations.py`，但工作腳本在 `code/` 底下。
  自己寫 ad-hoc 腳本時要先 `import fcv_core`（它會把 ROOT 加進 `sys.path`），
  否則 `ModuleNotFoundError: No module named 'database'`。
- `Database(market)` 只是開連線，**不會自動依市場過濾**——`stock` 表沒有市場欄位，
  要 JOIN `company` 表用 `db._exchange_in_clause()`。美股還要加
  `db._universe_clause('c.company_symbol')` 才會篩進 Russell 3000。
