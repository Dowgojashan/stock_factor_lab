# 美股資料接入開發計畫（FMP → FCV 系統）

> 用途：追蹤「把 FCV 系統從純台股擴充成台美雙市場」的進度與方向。
> 每完成一項就更新左側勾選框與「狀態」。方向若有變更，改「決策紀錄」並註記日期。
> 相關文件：主軸與背景見 `研究總統整_換對話框用.md` §3；系統結構見 `handover_new.md`。

- **建立日期**：2026-07-02
- **負責人**：Dowgojashan
- **資料來源**：Financial Modeling Prep（FMP）API
- **FMP 訂閱方案**：Starter
- **目前整體狀態**：🟡 準備進入 B1（先串接 FMP、確認能抓到想要的美股資料）

---

## 0. 目標與範圍

把 FCV 因子回測系統擴充成**同一套系統 + market 參數**，同時支援台股與美股，作為後續 Agent 化與跨市場研究的資料/工具基礎。

- ✅ **納入範圍**：美股行情、財報、因子資料的抓取與入庫；讀取層（database/format_data/get_data）支援 market；回測費率/大盤基準分流；美股前瞻防護。
- ❌ **不在此文件範圍**：Agent 化（第三層）、Fund House 組合調度（第四層）、A3 配方產生器（另案，但與本案強耦合，見 §8）。

**核心原則**：所有台美差異**收斂在資料層**，`get()` 以上（`condition_factory`、`combinations`、`report_analysis`）完全共用。

---

## 1. 架構決策（已定）

**接法 A：FMP → 寫進 MySQL（沿用現有表）→ FCV 照常從 MySQL 讀。**

```
FMP API ──(美股 Collector：格式轉換/單位對齊/filing_date)──▶ MySQL `lab`
                                                                  │
                                                    database.py（依 market 查詢）
                                                                  │
                                                    format_data.py（前瞻防護分流）
                                                                  │
                                                    get_data.py  get(market=...)
                                                                  │
                                        ▼ 以上完全共用（不因市場改動） ▼
                            condition_factory / combinations / report_analysis
```

**為何用 A（而非在 get() 直接打 FMP）**：Fund House 要做跨市場組合，需輸出格式一致；批次回測需離線、可重現；資料庫成為台美的統一交會點。

---

## 2. 系統資料契約（美股最終要塞成的格式）

無論來源如何，最後都要變成這三種 `data.get()` 輸出：

| 呼叫 | 輸出 | 來源 DB 表 |
|------|------|-----------|
| `get("price:close")` 等 | index=日期、columns=股票代號 寬表 | `company` ⋈ `stock` |
| `get("report:ROE")` 等 | 同上寬表，值=因子值 | `factor` ⋈ `factorvalue` ⋈ `company` |
| `get("taiex:close")` | 大盤基準（美股需換 S&P500） | `taiex` 表 |

`factorvalue` 的長格式契約：`date / company_symbol / factor_name / factor_value`（＋本案新增 `filing_date`）。

---

## 3. FMP → 系統因子 對應表（含單位陷阱）

| 系統因子 | JSON 門檻 | FMP 端點 / 欄位 | FMP 單位 | 換算 |
|----------|----------|----------------|----------|------|
| ROE | 5/10/15/20（百分比） | `ratios` → `returnOnEquity` | 比例 0.15 | **×100** |
| DEBTRATIO | 0.35/0.55（比例） | `ratios` → `debtRatio` | 比例 | 免換 |
| FCF_P | 0/0.05（比例） | `key-metrics` → `freeCashFlowYield` | 比例 | 大致一致 |
| PE | — | `ratios` → `priceEarningsRatio` | 倍數 | 免換 |
| REVENUE | 動態(YoY) | `income-statement` → `revenue` | 絕對(USD) | 只做 YoY，可容忍 |
| EPS | 0/2（絕對值） | `income-statement` → `epsdiluted` | 絕對(USD) | ⚠️ **不可換算**（見 §8） |

---

## 4. 技術難點與解法

| # | 難點 | 現況/風險 | 解法 | 對應程式 |
|---|------|-----------|------|----------|
| 1 | **前瞻防護 / 公告日**（紅線） | `adjust_index_of_report` 用台股會計月份推公告日；美股會計年度不一（如 Apple 9 月底），會把年報誤判成 Q3。DB 美股 `date` 是期末日、無 filing date | 入庫記 FMP `fillingDate`（兩個 l），`factorvalue` 加 `filing_date` 欄；讀取時美股用 filing_date 當索引、跳過月份規則 | `format_data.py`、`database.py` |
| 2 | **單位/尺度** | 比例型因子尺度不同；絕對值因子跨幣別不可比 | 比例型入庫做單位對齊；絕對值因子改分位數門檻（併 A3） | Collector、`fcv_experiment_spec.json` |
| 3 | **代號語意** | 台股數字(2330) vs 美股英文(AAPL)；舊查詢有 `company_symbol>8700` 數值比較 | 代號一律當字串處理，全面檢查數值比較路徑 | `database.py`、`get_data.py` |
| 4 | **費率/稅 + 大盤基準** | `sim()` 寫死 `tax_ratio=3/1000`（證交稅）、`fee_ratio=1.425/1000`；`get("taiex")` 是台股加權 | 美股 `tax_ratio=0` + 美股手續費；基準換 S&P500 | `backtest.py`、`get_data.py`、`report.py`(待查) |

---

## 5. 改動地圖

### 要新增
- [ ] **FMP 美股 Collector**（工作量最大）：company / price / 財報 / 因子；來源 FMP、欄位對應（用 `singleindicator` 的 `us_indicator`）、單位對齊、寫 `filing_date`、含下市公司。從台股版 `TWCollectorFactory` 改寫。
- [ ] `config.ini` 的 US DB 區塊或 market 設定（`utils/config.py` 目前只讀單一 `[database]`）。

### 要修改（資料層）
- [ ] `database.py`：三查詢 `WHERE exchange_name='TWSE'` → 依 market 切換（美股 NASDAQ/NYSE/AMEX）；report 查詢多帶 `filing_date`。
- [ ] `format_data.py`：`adjust_index_of_report` 加美股分流（走 filing_date、不套月份規則）。
- [ ] `get_data.py`：`Data.__init__` / `get()` / `indicator()` 加 `market` 參數；taiex 基準可選 US。
- [ ] `backtest.py`（或呼叫端）：美股 `tax_ratio=0` + 對應手續費。
- [ ] `report.py`：確認/分流大盤基準用途（**待查**）。

### 不動（共用層）
- `condition_factory.py`、`combinations.py`、`report_analysis.ipynb`。
- `fcv_experiment_spec.json`（理想不動；絕對值因子問題見 §8）。

---

## 6. 里程碑與進度（B1–B5）

| 階段 | 內容 | 驗收標準 | 預估 | 狀態 |
|------|------|----------|------|------|
| **B1** | 串接 FMP + 單檔驗證（先抓幾檔如 AAPL 能動就好） | `fillingDate` 有回、欄位齊、單位確認（ROE 為 0.x）；**並確認 Starter 方案限制**（見下） | 1–2 天 | 🟡 進行中 |

> **B1 額外要驗證的 Starter 方案限制**（實測 API 回傳確認，勿憑猜測）：
> - [ ] 歷史深度：Starter 能抓到幾年前的財報/股價？（若只有近 5 年，會限制美股回測起點，需與台股 2018 起對照調整）
> - [ ] `delisted-companies` 端點是否可用？（關乎倖存者偏誤紅線；若 Starter 不給，MVP 需先接受偏誤或評估升級）
> - [ ] `historical-market-capitalization` 是否可用？（關乎市值欄位）
> - [ ] 呼叫速率上限（calls/min）與每日上限？（影響全市場抓取策略）
> - [ ] 是否有 bulk/batch 端點，或只能逐檔抓？
| **B2** | DB 加 `filing_date` 欄 | 台股留空、美股可填 | 0.5 天 | ⬜ 未開始 |
| **B3** | 寫美股 Collector（先 S&P500） | 入庫成功、單位對齊、含 filing_date 與下市公司 | 1.5–3 週 | ⬜ 未開始 |
| **B4** | 讀取層支援 market | database/format_data/get_data/費率分流；**台股回歸測試通過** | 3–5 天 | ⬜ 未開始 |
| **B5** | 美股單因子 MVP（ROE） | 抽樣核對「訊號日 ≥ fillingDate」、與台股量級對照合理 | 2–4 天 | ⬜ 未開始 |

> 進度勾選：⬜ 未開始 / 🟡 進行中 / ✅ 完成 / ⛔ 卡關。

---

## 7. 品質紅線（每階段務必守住）

- [ ] **前瞻偏誤**：B5 一定要抽樣手動核對「訊號產生日 ≥ fillingDate」。
- [ ] **台股回歸**：每次改讀取層後跑一次台股回測，確保沒弄壞這條乾淨對照組。
- [ ] **量級對照**：同因子（如 ROE）美股數值量級應與台股可比，差很多＝單位沒對齊。
- [ ] **倖存者偏誤**：確認 FMP 清單含已下市公司（用 `delisted-companies`）。
- [ ] **每次資料更新後抽樣驗證**：日期、數值、前瞻防護。

---

## 8. 與 A3（配方產生器）的耦合（重要）

**EPS 這類絕對值因子跨幣別不可比**（NT$2 ≠ US$2），無法靠單位換算沿用同一份固定門檻 JSON。
→ 這逼使 A3 對跨市場採用**分位數門檻**而非固定門檻。
→ 因此「美股銜接」與「A3 配方產生器」應**一起規劃**：共用一套產生邏輯，各市場產出各自的 JSON。

---

## 9. FMP 實務注意事項

- `fillingDate`（兩個 l）+ `acceptedDate`，別用一般拼法 `filingDate`。
- 財報端點帶 `period=quarter`，別混到 annual。
- 決定用 `adjClose`（還原價）或原始價，並與台股口徑一致。
- 歷史市值用 `historical-market-capitalization`（`profile` 只有即時）。
- 倖存者偏誤：用 `delisted-companies` 補下市公司。
- API 額度/速率：~9400 檔 × 多報表 × 多季，抓取要分批、快取、可續傳。

---

## 10. 待決策事項（動工前確認方向）

| # | 決策 | 選項 | 狀態 |
|---|------|------|------|
| D1 | 市場範圍 | S&P500 / 全美股 exchange / country='US' | 🟡 待問老師（MVP 先抓幾檔能動即可） |
| D2 | 美股資料是否重抓 | 沿用 DB 現有 / **用 FMP 重抓含 filing_date** | ✅ 用 FMP 重抓 |
| D3 | Collector 放哪 | **選項3：自有全新 repo `stock_factor_collector`**（種子=TW collector：factory US 插槽 + Database 寫入層 + TW 範本；台美統一定位、保留 country 分派） | ✅ 已建本地 repo，待推 GitHub |
| D4 | FMP 方案等級 | Free / **Starter** / 更高 | ✅ Starter（限制待 B1 實測） |
| D5 | 絕對值因子處理 | **併 A3 改分位數** / 美股改比例型替代因子 | ✅ 併 A3 改分位數 |

### 新增待釐清（2026-07-03）
| # | 問題 | 備註 | 狀態 |
|---|------|------|------|
| D6 | 美股回測起點年份？ | **以 Starter 能測的近 5 年為主** | ✅ 近 5 年 |
| D7 | MVP 先做哪些因子？ | **先只做 ROE 單因子**，通了再擴充到台股 6 個 | ✅ ROE 單因子起步 |
| D8 | 美股寫進同一個 `lab` DB？ | **維持同一 `lab`，用 exchange_name 區分**（分析見 §13） | ✅ 同一 DB（傾向，可再確認） |
| D9 | 倖存者偏誤處理 | **確定納入 delisted 下市公司清單**以避免偏誤；Starter 能否取得待 B1 實測 | ✅ 方向：納入 delisted（可用性待 B1） |
| D10 | 股價用還原價或原始價？ | **實測 DB 定案：台股存「還原價」（後復權，含股利）**。證據：2330/2412 近期價=實際成交價、越早期越低，且高息股(中華電)還原幅度更大。→ 美股須用 FMP `adjClose`（且 B1 確認含「分割+股利」，非只分割） | ✅ 台股=還原價；美股對齊用 adjClose |
| D11 | 數值幣別 | 維持 USD 原樣、不換算台幣 | ✅ USD 原樣 |

---

## 12. D3 分析：US Collector 該放哪

**選項**
- **A｜同 repo**：US collector 放進 `stock_factor_lab`。
- **B｜獨立 US collector repo**：自成一包，鏡像台股的 `TW_stock_factor_collector`。
- **C｜統一 collector repo**：自己擁有一包，同時含 TW（改寫）+ US collector，與 `stock_factor_lab` 分開。

**四個面向分析**
| 面向 | 同 repo (A) | 分開 (B/C) |
|------|------------|------------|
| 沿用現有架構 | ✗ 與台股「collector 獨立」慣例相悖 | ✓ 台股本來就分開，對稱一致 |
| 乾淨程度 | ✗ 把 API key、HTTP/ETL 依賴、原始 JSON 混進「純讀取/回測」repo | ✓ FCV repo 維持乾淨的 DB 消費者 |
| 開發難易度 | ✓ 起步略快（共用 config/DB code） | ~ 多一次 repo 建置；DB 連線樣板約 30 行、重寫成本低 |
| 分開的麻煩 | — | ~ 兩包要維護；schema 變更需跨 repo 協調（但 schema 本就是唯一契約、變動少） |

**關鍵判斷**：collector 與 FCV 的**程式耦合極低**——它們只透過「DB schema」這個契約溝通，資料單向流動（collector 寫 DB、FCV 讀 DB），彼此**沒有函式呼叫**。唯一共用的是 pymysql 連線樣板（可容忍重複）。因此技術上沒有「必須放同一 repo」的拉力，而分開能換得明顯的乾淨度。

**建議**：**與 `stock_factor_lab` 分開**。
- **B1（現在）不需要決定 repo**：先用一個 scratch script / notebook 打 FMP、看回傳即可。
- **到 B3（寫正式 collector）再建一個獨立 US collector repo（選項 B）**，最簡單、鏡像台股。
- **未來**若同時維護「台股 collector（別人的）＋ US collector（你的）」變麻煩，再考慮合併成選項 C（自己擁有、含台美）。現階段別急著重寫台股 collector。
- 任一方案：FMP API key 與 DB 帳密都放 gitignored 的 config（比照 `config.ini`）。

---

## 13. D8 分析：美股是否寫進同一個 `lab` DB

前提：`lab` 現況**已台美並存**（美股約 9451 家在庫），用 `exchange_name` 區分市場。

| | 同一 `lab`（台美並存） | 另開 DB（如 `lab_us`） |
|---|----------------------|------------------------|
| 讀取層改動 | 小（多一個 market 的 WHERE） | 大（依 market 切換連線、兩份 config） |
| 跨市場組合（Fund House） | ✓ 直接 union/join，未來輕鬆 | ✗ 跨 DB 查詢或程式端 merge，摩擦大 |
| 維度表共用 | ✓ company/factor 共用、schema 一致 | ✗ 重複維護 |
| 保護台股 | ~ 需靠紀律（market 圈定 + 回歸測試） | ✓ 物理隔離 |
| 與現況一致 | ✓ 延續台美並存 | ✗ 需遷移/重做 |

**建議：維持同一個 `lab`**，因論文終點是 Fund House 跨市場組合，單一 DB 讓那步輕鬆很多；符合接法 A 單一交會點。
**風險控制**：(1) 美股寫入/刪除一律帶 `exchange_name IN (美股)` 條件，只碰美股列；(2) 動完 DB / 讀取層後跑台股回歸測試；(3) `filing_date` 為可為空新增欄，對台股無害。
**重抓操作**：D2 選重抓 → 第一次「依 market 刪舊美股列 → 插入 FMP 新資料」，刪除務必帶美股條件，勿誤刪台股。

---

## 11. 決策紀錄（Changelog）

| 日期 | 項目 | 內容 |
|------|------|------|
| 2026-07-02 | 架構 | 確定接法 A（FMP→MySQL→get 共用）。 |
| 2026-07-02 | 文件 | 建立本開發計畫文件。 |
| 2026-07-03 | 決策 | D2=用 FMP 重抓；D4=Starter；D5=併 A3 分位數；D1 待問老師（MVP 先抓幾檔）。 |
| 2026-07-03 | 分析 | D3 完成分析，傾向與 FCV repo 分開（B1 先用 scratch，B3 再建獨立 collector repo）。 |
| 2026-07-03 | 決策 | D6=近5年；D7=先做 ROE 單因子；D8=同一 `lab`（分析見 §13）；D11=USD 原樣。 |
| 2026-07-03 | 決策 | D9=確定納入 delisted 下市清單（避免倖存者偏誤），Starter 可用性待 B1。 |
| 2026-07-03 | 查證 | D10：FCV 端不還原，但**實測 DB 定案台股 stock.close = 還原價（後復權含股利）**——2330/2412 近期價=實際成交、越早越低、高息股還原更多。→ 現有台股回測無除權息失真；美股須用 FMP adjClose（B1 確認含分割+股利）。 |
| 2026-07-03 | 行動 | 進入 B1：先串接 FMP、確認能抓到想要的美股資料。 |
| 2026-07-03 | 行動 | 建立 collector repo：以 TW collector 為種子 seed 出自有的 `stock_factor_collector`（乾淨歷史、config.ini 已 gitignore 並清除實驗室帳密、README 加 attribution、TEJ 產物保留當參考）。位於 `d:\研究所\stock_factor_collector`，已推上 https://github.com/Dowgojashan/stock_factor_collector 。發現：factory 已 import `USCollectorFactory/` 但該資料夾不存在→ 這正是要用 FMP 補的美股實作。 |
| 2026-07-03 | 決策 | **舊有的美股抓取碼一律不沿用**（學長姊 TW collector repo 內殘留的舊 US 相關內容已過時）→ 美股一律以「我們新開發的 FMP 版 `USCollectorFactory/`」為準，不去修復/接續舊 US 碼。 |
