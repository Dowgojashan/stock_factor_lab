# 總經資料收集需求（給 collector）

> **提出方**：研究部　**日期**：2026-08-20
> **一句話**：研究部階段 2b 需要台美各 4~5 個總經指標的月／季頻歷史序列，**資料庫目前一項都沒有**。
> **不需要動用 FMP 額度** —— 美股用 FRED（免費）、台股用官方公開統計。
> **交付前請先跑自我檢查**：`cd code && python -m research.validate_macro_raw <你的檔案>`

---

## 一、背景：這批資料要做什麼

指導教授 2026-07 指示「總經進研究部」，建立「**總經環境 → 哪些策略賺／賠**」的對應模型。做法是：

```
歷史總經（本次需求）→ 標準化 → 投資時鐘四格 → 對照每個策略在各格的表現
                                                    ↓ 凍結進 strategy_map
實戰部：拿「當前總經」去查這張凍結的表 → 找出歷史上類似環境下表現好的策略
```

所以這批資料的用途是**建立歷史對應關係**，不是即時交易訊號。

**兩種方法並用**（研究部 v9 階段 2b 已定案）：
- 方法一 · k-NN 相似歷史檢索：找當前總經最像的歷史 K 個月，看那幾個月誰賺
- 方法二 · 投資時鐘四格：成長 × 通膨 兩軸切四格（復甦／過熱／停滯性通膨／衰退）

---

## 二、需要哪些指標

台美**對齊「概念軸」而非同一個指標**——各市場用各自最合適、最可得的代表指標即可。

### 2.1 美股（建議來源：FRED，免費且無額度限制）

| `indicator_key` | 概念軸 | 指標 | FRED series | 頻率 | 必要性 |
|---|---|---|---|---|---|
| `growth` | 成長 | 實質 GDP 年化季增率 | `A191RL1Q225SBEA` | 季 | **必要** |
| `growth_alt` | 成長（月頻備援）| 失業率 | `UNRATE` | 月 | 建議 |
| `inflation` | 通膨 | CPI（全項目，季調）| `CPIAUCSL` | 月 | **必要** |
| `rate_level` | 利率水位 | 聯邦基金有效利率 | `FEDFUNDS` | 月 | **必要** |
| `rate_direction` | 利率方向 | 10Y − 2Y 公債利差 | `T10Y2Y` | 日 → 取月底 | **必要** |

> `T10Y2Y` FRED **直接提供利差本身**，不需要抓 DGS10 和 DGS2 自己相減。
> `rate_level` 若偏好用 10 年期殖利率，改抓 `DGS10` 亦可，請在交付說明註明用哪個。

FRED API：`https://api.stlouisfed.org/fred/series/observations?series_id=<ID>&api_key=<KEY>&file_type=json`
（API key 免費申請）

### 2.2 台股（建議來源：官方公開統計）

| `indicator_key` | 概念軸 | 指標 | 來源機構 | 頻率 | 必要性 |
|---|---|---|---|---|---|
| `growth` | 成長 | 實質 GDP 年增率 | 主計總處 國民所得統計資料庫 | 季 | **必要** |
| `growth_alt` | 成長（月頻備援）| 工業生產指數年增率 | 經濟部統計處 | 月 | 建議 |
| `inflation` | 通膨 | CPI 年增率 | 主計總處 物價統計資料庫 | 月 | **必要** |
| `rate_level` | 利率水位 | 央行重貼現率 | 中央銀行 | 事件驅動 → 取月底值 | **必要** |
| `rate_direction` | 景氣方向 | **景氣對策信號綜合判斷分數** | 國發會 | 月 | **必要** |

> 台股的第四軸用「景氣對策信號分數」取代美股的殖利率曲線——兩者概念相同（**領先性的景氣方向指標**），
> 但台灣公債市場深度不足，殖利率曲線的訊號品質遠不如景氣訊號。此為研究部 v9 定案。

### 2.3 第五軸（風險）

原本規劃的第五個特徵（美股 VIX／台股對應指標）**已定案留白，不需要交付**。

---

## 三、交付格式

### 3.1 檔案

- **路徑**：`_frozen/stage2/macro/macro_raw.parquet`（`.csv` 亦可，但需 UTF-8）
- **格式**：**long 格式**（一列一個「市場 × 指標 × 期間」）

刻意不用寬表：指標數少、頻率不一（月／季／日），寬表會有大量 NaN 且新增指標時要改結構。

### 3.2 欄位

| 欄位 | 型別 | 必填 | 說明 |
|---|---|---|---|
| `market` | str | ✅ | `TW` 或 `US` |
| `indicator_key` | str | ✅ | 上表的 key：`growth`／`growth_alt`／`inflation`／`rate_level`／`rate_direction` |
| `period` | str | ✅ | **資料所描述的期間**，月頻用 `YYYY-MM`、季頻用 `YYYYQn`（如 `2024-03`、`2024Q1`）|
| `value` | float | ✅ | 數值 |
| `unit` | str | ✅ | `index`／`percent`／`percent_yoy`／`percent_annualized`／`score` |
| `release_date` | str | ⭐ 強烈建議 | **該筆資料的實際發布日**（`YYYY-MM-DD`）。見 §四 |

**範例**：

```
market  indicator_key   period    value  unit               release_date
US      inflation       2024-03   3.48   percent_yoy        2024-04-10
US      growth          2024Q1    1.60   percent_annualized 2024-04-25
US      rate_direction  2024-03   -0.39  percent            2024-03-31
TW      inflation       2024-03   2.14   percent_yoy        2024-04-08
TW      rate_direction  2024-03   32.00  score              2024-04-27
```

### 3.3 期間範圍

**1998-01 起，至少涵蓋到 2025-12。**

為什麼要從 1998 而不是 2000（樣本內起點）：
- 年增率需要前 12 個月做基期
- 3 個月平滑需要暖機
- 從 1998 起可讓 2000-01 就有完整可用的特徵值

### 3.4 單位：請務必明寫，這是最容易靜默出錯的地方

原始值或年增率都可以，但 **`unit` 必須誠實填寫**。

原因：下游會做 z-score 標準化，而 **z-score 對尺度不敏感**——把 2.1% 寫成 0.021，標準化之後看起來一模一樣，**錯誤不會在任何圖表上顯現**，卻會污染四格邊界與 k-NN 距離。這是最典型的「不會報錯的錯誤」。

自我檢查工具會偵測這種情形（若宣告 `percent*` 但 95% 的值絕對值 < 1 就報錯）。

### 3.5 缺值處理

> ⚠️ **缺值請留 `NaN`，絕對不要填 0。**

本專案已經因為「假 0」付出過實質代價——台股 2000–2004 的財報約 90% 是缺值填 0，導致早期分位桶產生大量假訊號，整條線後來必須重驗。總經資料如果重蹈覆轍，會直接污染四格分類。

---

## 四、⭐ `release_date`：為什麼強烈建議提供

這是本需求中**唯一一個「有沒有」會造成方法論等級差異**的欄位。

### 問題

總經指標**不是在它所描述的期間結束當下就知道的**。2024 年 3 月底做決策時，你不可能知道 3 月的 CPI——那要等 4 月 10 日才公布。若直接用「3 月的 CPI」建模，就是用了當時不存在的資訊 = **前視偏誤**。這與財報的 `filingDate` 是完全相同的原則。

### 兩種解法

| | 作法 | 品質 |
|---|---|---|
| **A（有 `release_date`）** | 直接用實際發布日判斷「該時點看得到什麼」 | ✅ **實測**，精確、可稽核 |
| B（無 `release_date`）| 下游用各指標的公告時程**推估**滯後量 | ⚠️ 推估，遇到延遲發布、假日順延、時程變更就會失準 |

研究部已經備妥 B 的推估規格（`code/research/macro_spec.py`，每個指標都查過官方時程並附來源連結），所以**沒有 `release_date` 也能跑**。但若拿得到，品質差一個等級。

### 各來源的可得性

- **FRED**：有姊妹服務 **ALFRED**，用 `realtime_start` / `realtime_end` 參數可取得任一時點的資料版本。若能一併取，請填 `release_date`。
- **台灣官方統計**：多數僅提供最新值，`release_date` 可能取不到 → 留空即可，下游會自動退回推估模式。

### ⭐ 額外的好處（值得評估）

若美股能經由 ALFRED 取得 **vintage（歷史版本）**，還能一併解決另一個已知限制：

> 目前我們能做到「**時點對齊**」（不使用該時點尚未發布的期間），
> 但做不到「**版本對齊**」（拿不到該時點當下看到的**原始數值**）。
> GDP 與失業率事後都會被修正，我們拿到的是修正後的值。
>
> 這個殘留偏誤原本要寫進論文 limitations。**ALFRED 能讓美股這一半消失。**

台灣沒有對應的 vintage 服務，這個限制對台股會保留，但仍值得在論文裡說明「美股已處理、台股受限於資料源」。

---

## 五、⚠️ 需要你回答的問題

| # | 問題 | 為什麼重要 |
|---|---|---|
| **Q1** | **台股 GDP 取的是「概估統計」還是「初步統計」？** | 主計總處分兩階段發布：**概估統計**約季末後 1 個月、**初步統計**約季末後 2 個月（國民所得統計評審會審議）。這決定下游的滯後量該設 1 個月還是 2 個月。設錯就是前視偏誤。 |
| **Q2** | 台股 `rate_level` 用重貼現率還是貨幣市場利率？ | 重貼現率變動次數少（階梯狀），貨幣市場利率連續但雜訊多。兩者都可，請告知選了哪個以便下游說明。 |
| **Q3** | 美股 `rate_level` 用 `FEDFUNDS` 還是 `DGS10`？ | 前者是政策利率、後者是市場長率，經濟意義不同。 |
| **Q4** | ALFRED vintage 是否可行、成本如何？ | 決定能否解除 §四 的限制。若成本高可以先不做，之後補。 |
| **Q5** | 這批資料是否能做成**可重複執行的抓取器**？ | 實戰部 out 期需要每季抓「當前總經」去查凍結模型（研究部建表、實戰部查表）。若只是一次性 dump，之後每季都要人工重跑。 |

---

## 六、交付前自我檢查

研究部提供了驗證工具，**請在交付前跑過**，可以避免來回：

```bash
cd code
python -m research.validate_macro_raw <你的檔案.parquet 或 .csv>
```

會檢查 6 類問題，分「❌ 錯誤（不可交付）」與「⚠️ 警告（可交付但請確認）」：

| # | 檢查項 | 抓什麼 |
|---|---|---|
| 1 | 契約驗證 | 欄位齊全、型別正確、值域合法、主鍵唯一 |
| 2 | 指標齊全度 | 四個必要軸台美都有嗎 |
| 3 | 期間格式與涵蓋範圍 | `period` 可解析、起點夠早、終點涵蓋 2025-12 |
| 4 | 缺值與假 0 | 0 值比例過高會提醒確認 |
| 5 | **值域與單位** | 超出合理範圍、或宣告百分比卻全是小數 |
| 6 | `release_date` | 覆蓋率、可解析性、**發布日不得早於資料期間** |

輸出範例：

```
❌ 不可交付：2 個錯誤
   • TW 缺少必要指標 ['rate_direction']
   • TW.inflation 宣告 unit=percent_yoy 但 95% 的值絕對值 < 1（max=0.021）——疑似把百分比寫成小數

⚠️ 7 個警告（可交付但建議確認）：
   • TW.growth 起點 2005-01 晚於 1998-01
   • 未提供 release_date
```

---

## 七、下游已經準備好的部分（供你了解銜接點）

研究部這邊**不等資料就先把消費端做完了**，資料一到即可建置：

| 項目 | 位置 | 狀態 |
|---|---|---|
| 輸入契約 | `contracts.MACRO_RAW` | ✅ 已定義（即 §三 的欄位）|
| 產出契約 | `contracts.MACRO_HISTORY` | ✅ 已定義 |
| 發布滯後規格 | `research/macro_spec.py` | ✅ 10 個指標皆已查證官方時程並附來源 |
| 處理鏈 | `research/stage2b_macro.py` | ✅ 滯後對齊 → 月頻攤平 → z-score 凍結 → 3月平滑 → 投資時鐘四格 |
| 驗證 | `--selftest` | ✅ 合成資料測過，**內建前視稽核 312 個月 × 2 市場、違規 0 筆** |

所以你交付後，下游只需要跑一支指令就能產出 `macro_history`。

---

## 八、優先度

| 項目 | 優先度 | 說明 |
|---|---|---|
| 四個必要軸 × 台美 | 🔴 高 | 缺任一軸，階段 2b 無法建置 |
| `release_date`（美股）| 🟡 中高 | 影響方法論品質，但不阻塞 |
| `growth_alt`（月頻備援）| 🟢 低 | 季頻 GDP 粒度太粗時的替代方案 |
| ALFRED vintage | 🟢 低 | 加分項，可後補 |

> **與價格資料修復的關係**：這兩件事**互相獨立、可並行**。總經資料不受價格接縫問題影響，也不影響 Phase 1 重跑的判斷。若價格修復正在進行中，總經可以排在其後、不必搶資源。

---

*研究部 · 2026-08-20。相關文件：`系統設計文件_v1.md`（SDD）、`開發待辦追蹤.md`（W-02b 項）。*
