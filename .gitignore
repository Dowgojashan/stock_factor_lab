# Created by https://www.toptal.com/developers/gitignore/api/python
# Edit at https://www.toptal.com/developers/gitignore?templates=python

### Python ###
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/#use-with-ide
.pdm.toml

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

### Python Patch ###
# Poetry local configuration file - https://python-poetry.org/docs/configuration/#local-configuration
poetry.toml

# ruff
.ruff_cache/

# LSP config files
pyrightconfig.json

### MicrosoftOffice ###
*.tmp

# Word temporary
~$*.doc*

# Word Auto Backup File
Backup of *.doc*

# Excel temporary
~$*.xls*
.csv

# Excel Backup File
*.xlk


# PowerPoint temporary
~$*.ppt*

# Visio autosave temporary files
*.~vsd*

# End of https://www.toptal.com/developers/gitignore/api/python

### VSCode ###
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets

# Local History for Visual Studio Code
.history/

# Built Visual Studio Code Extensions
*.vsix

### Others ###

# output plot files
*.html
/OutputFile

# template from finlab doc files
/OfficialPage

# test file
/tests

# experiment ipynb folder
/experiment_file_lee
/experiment_file_hao

# trash can
/UselessFile

# local DB credentials (use config.ini.example as template)
config.ini




__pycache__/database.cpython-38.pyc
__pycache__/Backtest.cpython-38.pyc
__pycache__/get_data.cpython-38.pyc

### Project ###
# ─────────────────────────────────────────────────────────────
# 分界線：回測「原始產物」不上傳，「分析過的結果」要上傳
#
#   🚫 不上傳：每個策略的逐日明細（trades / position / stock_data /
#              return_table / stats.parquet）。這些是跑出來的中間產物，
#              數十萬個檔案、合計約 490 GB，重跑就有。
#
#   ✅ 要上傳：_analysis_outputs*/ 底下的 CSV / PNG / MD，
#              也就是已經彙整分析過、論文會直接引用的結果（約 90 MB）。
#
#   若要重現原始產物：依 _analysis_outputs_*/PhaseN_結果分析.md 開頭
#   記載的「執行腳本」重跑即可，各目錄內的 _MANIFEST.json 有完整參數。
# ─────────────────────────────────────────────────────────────

# ⚠️ .gitignore 不支援行尾註解（# 會被當成 pattern 的一部分），
#    所有說明都必須自成一行。

# 回測原始產物（巨大）
#   code/results_artifacts/  58.5 GB
#   /_archive/              160.9 GB
results_pickle/
results_artifacts/
code/timing_logs/
/_archive/

# 封存的舊回測，合計約 270 GB（各目錄自帶 README 說明封存原因）
# 2026-08-20 整理：code/_ARCHIVE_*/ 各資料夾已合併搬進 code/_archive/ 底下
# （去掉 _ARCHIVE_ 前綴），舊的 _ARCHIVE_*/ 規則已對不上，改為以下規則：
#   code/_archive/results_artifacts_thru2026-07/  257.7 GB
#   code/_archive/L3L4_strict_oldbench203/         11.9 GB
#   code/_archive/phase1_run1_16factors/            0.4 GB
#   code/_archive/phase1_run2_withV/                0.6 GB
_ARCHIVE_*/
code/_archive/

# 巨大的逐行 log（純 tqdm 進度條輸出，跑完即無用，重跑會重新產生）
code/_catalog/_archive_detail_logs/

# 舊版逐批 20 張圖圖鑑（56 MB / 413 檔）
#   結論已被 2026-08 的 SOP 重跑取代，且含已知無效的台股 MOM 結果。
#   2026-08-20：本體已搬進 /_archive/（root 的 /_archive/ 規則已涵蓋，這行留著防止改名後漏網）。
#   若要一併上傳，把下面這行註解掉即可。
_analysis_outputs/
_analysis_outputs_*舊版*/

# Cython 編譯產物（原始碼 core/backtest_core.pyx / .cpp / setup.py 有版控，
# 編譯結果與平台綁定，各自 `python core/setup.py build_ext --inplace` 產生）
core/build/
*.pyd
*.obj
*.exp

# 會議逐字稿／錄音整理（含指導教授談話內容，不對外公開）
*逐字稿*
*咪挺*


# Manual backup files
*.bak


# Environment variable variants
.env.*

# OS-generated files
.DS_Store
Thumbs.db
desktop.ini

# Files that may contain credentials
db_config.json
credentials.json
secrets.json

# Unpublished thesis reference material — must never be committed
/參考資料/
# 研究部凍結產物（衍生資料，可由 pipeline 重建）
# 例外：保留 MANIFEST.json 作為可複現性佐證（小檔，含 git SHA 與輸入指紋）
_frozen/**
!_frozen/
!_frozen/**/
!_frozen/**/MANIFEST.json
