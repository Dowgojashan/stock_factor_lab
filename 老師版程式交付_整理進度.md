# 老師版程式交付：整理進度追蹤

> **背景**：老師本週要求「整理之前可以跑策略的程式，協助裝在老師電腦上跑起來，他要親自對一次整體正確性」。
> **範圍原則（使用者2026-08-23明講）**：
>   1. 只給「跑策略的程式」（Phase 1-4 SOP 因子回測引擎），**不給老師看研究部/實戰部的流程文件**（`研究部完整流程_v9.md`、`實戰部架構_v8.md`、`GateC_標記清單_v1.md`、`開發待辦追蹤.md`、`code/research/`、`code/ops/` 等——這些是還沒給老師審過的後續設計，不在這次交付範圍）。
>   2. 不要多餘的程式——只整理「能跑」的那組，不是整個repo。
>   3. 先處理**環境**這一塊，其他（篩選程式碼、實際打包）先等。
> **本文件用途**：追蹤整個整理過程的階段與進度，每完成一步就更新狀態。

---

## 狀態圖例

`☐ 未開始`　`◐ 進行中`　`☑ 完成`　`🔒 待你確認才能繼續`

---

## 整體階段規劃

| # | 階段 | 說明 | 狀態 |
|---|---|---|---|
| 1 | 環境安裝文件盤點與重寫 | 對照現況重寫`安裝說明_SETUP.md` | ☑ 完成——已補上「B. 建立資料庫」（原本完全沒有）+「E. 跑正式的Phase1-4 SOP」（原本只有舊版example.ipynb demo）|
| 2 | 界定「跑策略程式」的範圍 | 哪些檔案算「核心可跑」、哪些算「多餘」| ☑ 已確認：核心引擎+四階段SOP腳本+穩健性檢定/報告分析腳本全部一起給 |
| 3 | 篩選並打包程式碼 | 整理成一份乾淨的交付資料夾（不含研究部/實戰部/內部文件），並**在原始碼位置外實際跑一次`phase1_linearity.py --dry-run`驗證過**，import鏈與DB連線都正常 | ☑ 完成，見下方「產物」|
| 4 | 資料庫匯出方案 | 匯出+驗證+寫進SETUP.md的匯入步驟 | ☑ 完成 |
| 5 | 乾淨環境實測 | 用打包好的程式（非原始repo路徑）實際跑一次dry-run，確認不依賴被排除的`research/`/`ops/` | ☑ 完成，見下 |
| 6 | 老師端驗收 | 老師自己裝、自己跑，你在旁邊視需要支援 | ☐ 未開始（等你交付）|

---

## 產物（2026-08-23 完成，2026-08-23二次修正）

暫存位置（`teacher_delivery/` 資料夾）：
```
...\3fd4a1f8-550a-4aaa-9ae6-af2477cf6702\scratchpad\teacher_delivery\
├── stock_factor_lab_程式.zip   （5.6 MB，已測試unzip -t完整）
└── lab_full_dump.sql.gz        （953 MB，已測試gzip -t完整、12張表都在）
```

🔴 **第一版（6.4MB）沒清乾淨，已修正**：驗證階段在打包資料夾裡實際跑了`phase1_linearity.py --dry-run`，這個動作本身會自動產生`code/_catalog/phase1_run_log.txt`，收尾時忘記清掉就打包了；`core/build/`底下也混進編譯中間產物（`.obj`/`.exp`/`.lib`+重複的`.pyd`）。兩者都已移除、重新打包驗證過，是使用者發現不對勁才抓到的，記錄在此提醒下次打包前要檢查「驗證動作本身有沒有留下副作用檔案」。

**是否附既有結果（`_analysis_outputs_*`）：決定不附**——同一份程式+同一份資料庫，Phase1-4 SOP是確定性流程，老師自己跑出來的結果應該要跟`策略成果報告_2026-08-17.md`一致，讓他自己跑更符合「親自驗證正確性」的目的，也避免包更亂更大。⚠️ 待確認：全因子全市場下去跑耗時可能是「一整夜」等級，是否要在SETUP.md提醒老師「先跑小範圍/dry-run」當第一步。

**兩個檔案刻意分開，不合併成一個大zip**——程式碼壓縮後只有6.4MB，這麼小可以直接用LINE/email附件傳，不用跟953MB的資料庫檔綁在一起增加傳輸門檻；只有資料庫那個大檔需要走雲端/email。

**範圍排除的細節**（比原先規劃更精確，經實測確認）：
- 根目錄的 `quantile_regression.py`／`oo_factor_analysis.py`——查過沒有任何程式引用（0處），確認是沒在用的舊檔，排除。
- `code/`底下4支session專屬的重跑腳本（`_rerun_after_a1.sh`等）——是這次對話過程中用來「從中斷處繼續跑」的暫時性腳本，帶著特定日期/狀態的假設，對老師沒有意義，排除。
- 舊版 `README.md`（提到conda+python3.8，跟現在的venv+python3.10流程矛盾）——排除，避免跟新版SETUP.md互相打架。
- 確認`code/`底下所有.py都沒有`import research`／`import ops`，排除研究部/實戰部不會弄斷任何程式。

**驗證方式**：把打包好的程式複製到暫存資料夾（不是原本的repo路徑），從那裡執行 `python phase1_linearity.py --dry-run --market TW`，成功連上資料庫、正確跑出180個預期策略數——證明檔案清單是完整的，不是漏東西的空想清單。

---

## 下一步

打包跟驗證都完成了。剩下就是你把這兩個檔案（zip + db dump）傳給老師，附上`安裝說明_SETUP.md`（已經在zip裡）。之後有需要再回來看「6. 老師端驗收」有沒有卡住的地方。

---

## 階段1：環境盤點結果（2026-08-23）

查了目前這台機器的實際環境，跟現有 `安裝說明_SETUP.md` 對照：

| 項目 | 現況 | 跟文件是否一致 |
|---|---|---|
| Python版本 | `.venv` 實際是 3.10.11 | ✓ 一致（文件要求3.10）|
| TA-Lib | 已裝 `ta_lib-0.6.3-cp310-cp310-win_amd64.whl` | ✓ 一致 |
| numpy | 1.24.4（鎖定版本）| ✓ 一致 |
| `requirements_clean.txt` | 存在，跟目前`.venv`裝的套件吻合 | ✓ 一致，不用大改 |
| `core/backtest_core` | 已編譯出 `backtest_core.cp310-win_amd64.pyd` | ✓ 已編譯成功，C++ Build Tools這關這台機器是通的 |
| `config.ini.example` | 存在，範本正確（host/port/db/user/password）| ✓ 可直接沿用 |

**結論：環境安裝（Python/TA-Lib/C++ Build Tools/套件版本）這幾塊文件本身寫得沒錯，不需要大改。**

### 發現的真正缺口：資料庫從頭到尾沒被涵蓋

現有文件在「MySQL資料庫」那條只寫了一句「你已用xampp建好並匯入SQL，這部分免處理」——**這句話對你成立、對老師不成立**。老師的電腦上完全沒有這批資料庫，現有文件完全沒有教怎麼從零建這個資料庫，這是目前最大的缺口，不補這塊老師裝完也跑不起來。

查了一下實際資料量（`information_schema.tables`）：

| 表 | 大約列數 | 大小 |
|---|---|---|
| stock（每日股價）| 3,892萬列 | 3.85 GB |
| factorvalue（因子值）| 1,417萬列 | 2.64 GB |
| financialreportindicatorvalue（財報指標）| 1,143萬列 | 1.41 GB |
| company / taiex / sp500 / sp500_tr / russell3000 / taiex_tr / period / factor / singleindicator | 各數千列以下 | 合計約4.5 MB |
| **總計** | — | **約7.9 GB（未壓縮）** |

7.9GB算大，但用`mysqldump`匯出+壓縮應該可以顯著縮小（數值型資料重複性高，壓縮率通常不差）。

### 資料庫瘦身評估（2026-08-23，你提議的「清掉多餘美股」）

實際查證（用 `company_id` 精確join，不是估計）：**8,413**家美股公司（非台股、也不在Russell3000名單）確實存在，佔用：

| 表 | 「多餘」佔比 | 換算可省空間 |
|---|---|---|
| stock（股價）| 47.5%（1,913萬列）| 約1.83 GB |
| factorvalue（因子值）| 27.0%（380萬列）| 約0.71 GB |
| financialreportindicatorvalue（財報）| 42.5%（488萬列）| 約0.60 GB |
| **合計** | | **約3.1 GB（7.9GB→約4.8GB，省~40%）** |

**建議：不要直接在正式資料庫上刪除**——這個資料庫還是整個研究部管線的正式來源，安全刪除的前提也是要先完整備份，既然都要備份，不如直接把備份（篩選過的版本）當交付檔，正式庫完全不用動，零風險。查證過程中也真的踩到一個SQL陷阱（子查詢欄位名打錯，MySQL沒報錯、默默解讀成別的意思、算出假的「0筆」結果），提醒了要對這種資料庫操作保持謹慎。

**已測試（2026-08-23）**：`mysqldump --single-transaction --quick` 直接壓縮（gzip -6），結果：

| 項目 | 數值 |
|---|---|
| 原始大小 | 7.9 GB |
| 壓縮後大小 | **953 MB** |
| 壓縮率 | 約8.3倍 |
| 完整性驗證 | gzip -t 通過、12張表CREATE TABLE都在、檔尾有正常的`Dump completed`標記 |

**結論：不需要做篩選瘦身這一步**——單純壓縮就已經把7.9GB壓到953MB（<1GB），足夠用隨身碟/雲端空間/校內網路任何管道傳輸，篩選省下的3.1GB在壓縮後的世界裡已經不是問題（篩選後估計也才剩約600~700MB，差距不大，不值得為了這點差異去承擔動資料庫的風險）。**正式資料庫全程沒有被觸碰**。

---

## 已確認的決定（定案，見開頭「範圍原則」與上方「產物」）

1. **範圍**：核心引擎+四階段SOP腳本+所有穩健性檢定/報告分析腳本一起給；不給研究部/實戰部/內部文件。
2. **資料庫交付**：不篩選，直接壓縮匯出（953MB）。
3. **傳輸管道**：老師端用 LINE／email／雲端空間，看實際傳得動哪個就用哪個，不強求單一管道。
