# 系統設計文件（SDD）v1

> **定位**：架構文件（`研究部完整流程_v9.md` / `實戰部架構_v8.md` / `GateC_標記清單_v1.md`）回答 **WHAT / WHY**；本文件回答 **HOW**——資料契約、模組介面、演算法規格、效能預算、執行計畫、驗收標準。
> **原則**：本文件**不推翻**任何已定案的架構決策。凡與架構文件衝突處，一律標示 🔴 並說明實查依據。
> **撰寫日**：2026-08-20　**基準 commit**：`cd2c48a`（branch `FMP_4`）
> **狀態標記**：✅ 已定案　⏳ 待實測　🔴 與架構文件不符（須修正架構文件）　🆕 本文件新增
>
> **v1.1 修訂（2026-08-20，全量實測後）**：`落差處理方案_v1.md` 對 14,078 個策略做了全量掃描（非抽樣），五項落差的處理方案**已全部經使用者確認定案**。本文件據此更新：DD-03 原建議的「P95 分位窗」**已撤銷**（實測證明不划算），改採每棵樹各自共同窗；新增 DD-11（主鍵碰撞，量測時發現的第五項落差）。所有 ⏳ 待實測項均已補上實測數字。

---

# 第一部分 · 現況實查

> 設計文件不能建立在架構文件的描述上，必須建立在磁碟上真實存在的東西上。以下每一項都經實際讀取驗證。

## 1.1 階段 −1 產物實況

| 項目 | 實查結果 | 架構文件的假設 | 落差 |
|---|---|---|---|
| 候選檔路徑 | `_analysis_outputs_phase4/{TW,US}_L4_openSec_final_candidates.csv` | 同 | ✅ 一致 |
| 檔案編碼 | **UTF-8 with BOM**，須 `encoding='utf-8-sig'` | 未指定 | 🆕 需寫進契約 |
| 台股列數 | 7,162（v0 **4,495** / v1 **2,667**）| 7,162 | ✅ |
| 美股列數 | 6,916（v0 **3,860** / v1 **3,056**）| 6,916 | ✅ |
| **實際欄位** | 僅 **8 欄**：`strategy`, `F組合`, `C`, `CAGR`, `max_drawdown`, `win_ratio`, `持股數`, `V` | — | 🔴 見 1.2 |
| 欄名語言 | `F組合` / `持股數` 為**中文欄名** | — | 🆕 階段0 須正規化為 ASCII |

## 1.2 🔴 落差一：`sharpe_ann` / `daily_sharpe` / `artifacts_dir` 不在候選 CSV 裡

研究部 v9 第五部分欄位表把這三個欄位標記為「**階段 −1 既有**」，實查**不成立**——候選 CSV 只有上述 8 欄。

**實際位置**：

| 欄位 | 真實來源 | 說明 |
|---|---|---|
| `sharpe_ann`, `daily_sharpe`, `avg_drawdown` | `results_artifacts/{job}/stats.parquet` | 每個 job 一份，欄位為 `CAGR, daily_sharpe, max_drawdown, avg_drawdown, win_ratio, ytd, strategy, sharpe_ann` |
| `artifacts_dir` | **不存在，須由階段0 計算** | 解析規則見 DD-01 |

**影響**：階段 0 從「合併兩份 CSV + 拆字串」升級為「**合併 + 拆字串 + join stats.parquet + 解析 artifacts_dir**」。這是純加法，不影響下游設計，但必須寫進階段0 規格，否則階段1 掃描器找不到原料。

**✅ 全量實測（2026-08-20）**：`stats.parquet` join 命中率**台美皆 100.00%**（7,162/7,162、6,916/6,916），`sharpe_ann`/`daily_sharpe`/`avg_drawdown` 非空率皆 100%；**CAGR 對帳 max diff = 9.71e-17**（float64 精度極限）——證明候選 CSV 與 `stats.parquet` 是同一次計算的兩個輸出，非兩份獨立計算。故方案採「join + 對帳斷言」，見 DD-01。

## 1.3 🔴 落差二：候選策略的產物散在兩個 job 目錄

| job 目錄 | 目錄數 | 內容 |
|---|---|---|
| `TW_L3_openSec_M` | 4,599 | **全部 `__v0`** |
| `TW_L4_openSec_M` | 4,599 | **全部 `__v1`** |
| `US_L3_openSec_M` | 3,948 | 全部 `__v0` |
| `US_L4_openSec_M` | 3,948 | 全部 `__v1` |

L4 是 v0/v1 對照實驗，**只跑 v1**；v0 的產物留在 L3。候選池 = 兩者的聯集中通過門檻者（TW 4,495 v0 + 2,667 v1）。

**覆蓋率實查**：對全部 14,078 個候選逐一檢查對應目錄是否存在 → **缺失 0 個**。✅ HRP 的原料完整可用。

## 1.4 每策略產物的實際內容（這是階段1 所有新欄位的原料）

| 檔案 | 大小 | 形狀 | 內容 | 供應哪些欄位 |
|---|---|---|---|---|
| `return_table.parquet` | 12 KB | 年×13 | index=年(str)，欄=`1..12`+`YTD`，值=**月報酬** | `annual_returns`, `worst_year`, `neg_year_count`, `return_shape`, `max_consec_loss_months`, **HRP 月報酬矩陣** |
| `stock_data.parquet` | 155 KB | 日×3 | `portfolio_returns`(淨值,基期100), `cum_returns`, `company_count`(僅換股日有值) | `ann_vol`, `holdings_median`, `holdings_p10`, `coverage_ratio` |
| `trades.parquet` | 167 KB | 交易×13 | `stock_id, entry_date, exit_date, position(權重), return, mdd...` | `effective_n`, `top1_share`, `rotation_score`, `turnover_ann`, `size_tilt_pct`, `smallcap_share` |
| `position.parquet` | **1,140 KB** | 換股日×全宇宙 | 持股權重寬表 | **設計上不使用**（見 DD-04）|
| **合計** | **1,474 KB / 策略** | | | |

## 1.5 🔴 落差三：台股策略起始期參差（本專案最大的技術風險）

**全量掃描 14,078 個候選策略**（非抽樣，2026-08-20，耗時 306 秒）：

| 市場 | 最早 | 中位 | P95 | 最晚 | 月數 min/中位/max |
|---|---|---|---|---|---|
| **台股** | 2000-06 | **2003-04** | 2006-09 | **2007-05** | 224 / 273 / 307 |
| 美股 | 2000-02 | 2000-12 | 2001-11 | 2002-03 | 286 / 301 / 311 |

台股起始年分布：`2000:3 / 2001:380 / 2002:3,079 / 2003:1,166 / 2004:735 / 2005:1,169 / 2006:628 / 2007:2`
所有策略的結束期一致為 **2025-12**。

研究部 v9 階段3 寫「報酬矩陣：**14,078 × 312 個月（2000-01 ~ 2025-12）**」——**此假設不成立**。台股歷史長度跨度達 7 年（與 auto memory 記錄的「台股 2000–2004 財報資料洞」一致），美股僅 2 年。

**後果（必須在設計層解決，不能留到實作）**：
1. 用 pairwise-complete 算相關 → 每一對用不同的重疊窗 → **相關矩陣不保證半正定（PSD）**；
2. 距離 `d=sqrt(0.5(1−ρ))` 隨之**可能違反三角不等式** → `scipy.linkage` 的階層樹在數學上失去意義；
3. 台美合併樹（N=14,078）的**共同窗由台股決定**，是跨市場分析的硬約束。

→ 處理方案見 **DD-03**。

## 1.6 🔴 落差四：頻率描述前後不一致

研究部 v9 內部有兩處衝突：階段3 正文說「**月頻**，與階段 2b 對齊」，改動 4 的補充 A 卻說「必須先把每個策略的**每日報酬**存下來」。

**實查結論**：`return_table.parquet` 已是**月報酬**（不需重算）；日報酬另存於 `stock_data.parquet`。兩者都在，不衝突——但契約要寫死誰是權威來源，見 **DD-02**。

## 1.7 可複用的既有程式（不重造輪子）

| 既有資產 | 位置 | 供應 |
|---|---|---|
| `stock_cum_contrib(trades)` | `code/analyze_batch.py:173` | 逐股累積貢獻（Top1/EffN 的基礎）|
| EffN = 1/HHI、`top1_share` 的實作 | `code/analyze_batch.py:184-195` | 關卡 A 直接複用，**演算法定義與第四章圖鑑一致** |
| `MKTCAP` 因子 | 資料庫（collector 於 2026-08-13 新增）| C-5 `size_tilt_pct` / `smallcap_share` |
| `universe_benchmark.py` | `code/` | 自建宇宙基準（台 8.67% / 美 12.35%）|
| 全庫掃描 + checkpoint 續跑範式 | `code/scan_stock_contributions.py` | 階段1 掃描器的骨架可沿用 |

> ⚠️ 依既定工作原則，`因子結合策略/`、`Quantile_AA/` 等資深研究員程式**不在複用範圍內，也不修改**。上述複用一律採「import 既有函式、不改既有檔案」。

---

# 第二部分 · 系統總覽

## 2.1 分層與資料流

```
┌─ 階段 −1（已完成，凍結）────────────────────────────────────┐
│  Phase 1–4 SOP → {TW,US}_L4_openSec_final_candidates.csv     │
│  + results_artifacts/{TW,US}_L{3,4}_openSec_M/<strategy>/    │
└───────────────────────────┬──────────────────────────────────┘
                            ▼
╔═══════════ 研究部（in-sample 2000–2025，一次性、產物凍結）═══════════╗
║  階段0  格式整備      → candidate_index.parquet                      ║
║  階段1  標記關卡A/B/C → strategy_marks.parquet + returns_monthly     ║
║            └ 尾端寬鬆硬篩 → is_usable → usable_pool                  ║
║  階段2a regime 切割 ─┐                                               ║
║  階段2b 總經↔策略   ─┴→ 階段2c 交叉佐證（階段2 統一出口）           ║
║  階段3  HRP 階層聚類（3 組樹 × 常態/危機）                           ║
║  階段4  merge → strategy_map（主表①+附屬②③④⑤）→ **凍結**          ║
╚═══════════════════════════╤══════════════════════════════════════════╝
                            ▼ 唯讀契約（第六部分）
╔═══════════ 實戰部（out 期 2026+，只讀不重算）═══════════════════════╗
║  Agent0（純程式）→ Agent1（agentic LLM 精挑）→ Agent2（解釋）        ║
║  工具層 T1–T13 = 查凍結檔的介面                                      ║
╚══════════════════════════════════════════════════════════════════════╝
```

## 2.2 模組佈局（新增程式碼的落點）

既有 `code/` 是扁平結構且正在使用中。新管線採**新增子套件、不動既有檔案**：

```
code/
├── research/                        🆕 研究部管線（本次開發主體）
│   ├── __init__.py
│   ├── contracts.py                 資料契約：schema 定義 + 驗證器（單一事實來源）
│   ├── freeze.py                    凍結/版本/指紋機制（DD-08）
│   ├── stage0_index.py              階段0 格式整備
│   ├── stage1_scan.py               階段1 掃描器（單趟掃全庫，checkpoint 續跑）
│   ├── stage1_marks.py              階段1 分位計算 + 等級判定 + is_usable
│   ├── stage2a_regime.py            regime dating
│   ├── stage2b_macro.py             總經↔策略對應（k-NN + 投資時鐘四格）
│   ├── stage2c_crosscheck.py        交叉佐證
│   ├── stage3_hrp.py                HRP 聚類 + 配權函式
│   ├── stage4_map.py                merge + 凍結 strategy_map
│   └── cli.py                       統一進入點：python -m research.cli <stage>
├── ops/                             🆕 實戰部（HRP 之後才開發，本文件只定介面）
│   └── tools/                       T1–T13
└── （既有檔案一律不動）

_frozen/                             🆕 研究部凍結產物（gitignore，走 manifest 管版本）
├── stage0/candidate_index.parquet
├── stage1/{strategy_marks.parquet, returns_monthly.parquet, scan_ckpt/}
├── stage2/{regime/, macro/}
├── stage3/{clusters/, hrp_tree/}
├── stage4/strategy_map/            ← 交給實戰部的唯一入口
└── MANIFEST.json                    每階段的輸入指紋、參數、git SHA、產出時間
```

**執行慣例（沿用專案既有陷阱）**：所有腳本一律 `cd code` 後執行；`research/` 任一模組於 import 時先 `import fcv_core` 以掛好 `sys.path`，否則 `from database import Database` 會失敗、且 `config.ini` 相對路徑讀不到 `[database]` 區段。

---

# 第三部分 · 資料契約（凍結介面）

> 契約是本設計文件的核心。凍結原則規定「改上游 = 連鎖重跑下游」，因此**欄位名稱、型別、值域必須先寫死**，且由 `contracts.py` 以程式強制檢查，不靠人工記憶。

## 3.0 通則

| 規則 | 內容 |
|---|---|
| 主鍵 | `strategy_uid = f"{market}::{strategy}"`。🆕 **不可只用 `strategy`**——台美策略字串可能碰撞（同名 F 組合），合併後會靜默錯 join |
| 欄名 | 一律 ASCII snake_case。階段0 把 `F組合`→`f_combo`、`持股數`→`avg_holdings` |
| 分位欄 | 所有 `*_pct` 一律 `groupby('market')` 計算，值域 0–100（✅ 架構已定案）|
| 等級欄 | 一律 `pd.Categorical` 且 ordered，禁止裸字串 |
| 缺值 | 禁止用 0 代表缺值（台股 2000–2004 假 0 已有前車之鑑）；一律 `NaN` + 對應的 `*_n_obs` 樣本數欄 |
| 檔案格式 | parquet + zstd；每份產物同目錄附 `.schema.json` |
| 時間 | 一律 `pd.Period('M')` 或 `YYYY-MM` 字串，禁止混用 datetime |

## 3.1 階段0 產出：`candidate_index.parquet`（14,078 列）

| 欄位 | 型別 | 來源 | 說明 |
|---|---|---|---|
| `strategy_uid` | str | 組合 | 主鍵 |
| `strategy` | str | CSV | 原始策略字串 |
| `market` | cat{TW,US} | 檔名 | 快篩必要鍵 |
| `f_combo` | str | CSV `F組合` | |
| `F1_factor`,`F1_band`,`F1_nbands` | str,int8,int8 | 字串拆解 | `EV_S_qb0of3` → (`EV_S`,0,3) |
| `F2_factor`,`F2_band`,`F2_nbands` | str?,int8?,int8? | 字串拆解 | F2 為空時 NaN |
| `F2_empty` | bool | 導出 | 台 384 / 美 652（老師「F1+C 就好」的分流依據）|
| `C_id`,`C_source`,`C_rule` | str?,cat?,str? | 拆 `C` 欄 | `C4_ROE_DYN_qmax8` → (`C4`,`ROE`,`qmax8`)|
| `V` | cat{v0,v1} | CSV | |
| `CAGR`,`max_drawdown`,`win_ratio`,`avg_holdings` | float | CSV | |
| `sharpe_ann`,`daily_sharpe`,`avg_drawdown` | float | 🆕 join `stats.parquet` | 見 1.2 |
| `artifacts_dir` | str | 🆕 DD-01 解析 | 絕對路徑，且**產出時驗證存在** |

**驗收**：列數 == 14,078；`artifacts_dir` 存在率 == 100%；`F2_empty` 計數 == 台 384 / 美 652（對得上即證明字串拆解正確）。

## 3.2 階段1 產出①：`returns_monthly.parquet`

| 欄位 | 型別 | 說明 |
|---|---|---|
| `strategy_uid` | str | |
| `month` | period[M] | |
| `ret` | float32 | 月報酬 |

長格式（long），非寬表——14,078 × 312 寬表有 66% 是 NaN（台股參差），long 格式省空間且明確表達「沒有」vs「是 0」。
**另存**：`returns_meta.parquet`（`strategy_uid`, `hist_start`, `hist_end`, `n_months`）——DD-03 的共同窗判定直接讀這份，不必重掃。

## 3.3 階段1 產出②：`strategy_marks.parquet`

> 這張表是 Agent1 快篩的索引。**12 格矩陣提到的每一個名詞都必須在這裡有對應欄位**，否則那一格快篩執行不了（見 6.2 自動化驗收）。

**A 組 · 可信度**（關卡A，原料 `trades.parquet`）

| 欄位 | 型別 | 定義 |
|---|---|---|
| `effective_n` | float | `1/HHI`，HHI 由逐股累積正貢獻算（複用 `analyze_batch.stock_cum_contrib`）|
| `top1_share` | float | 最大單股貢獻占比 |
| `rotation_score` | float | 🆕 **年度前三大貢獻股在相鄰年度的 Jaccard 相似度，全期平均**。低=有輪動 |
| `top20_cum_share` | float | 前20名累計貢獻占比 |
| `credibility_grade` | cat{高,中,低} | 由上述以**市場內分位**判級 |

**B 組 · 穩健度**（關卡B，僅 1 欄）

| 欄位 | 型別 | 定義 |
|---|---|---|
| `stability_grade` | cat{高原,中,陡峰} | 高原寬度分數：`bucket_F1/F2` 的 std + `deltaC_summary` 的 iqr |

**C 組 · 特性標記**

| 欄位 | 型別 | 定義 | 原料 |
|---|---|---|---|
| `factor_type` | cat{估值,體質,動能,規模,結構,混合} | F1/F2 同類取該類，異類=混合 | 階段0 |
| `factor_type_basis` | str | 判定依據（供 Agent2 引用）| 階段0 |
| `cagr_pct`,`mdd_pct` | float | 市場內分位 0–100 | 階段0 |
| `ann_vol` | float | 年化波動（日報酬 std × √252）| stock_data |
| `return_shape` | cat{穩定爬升,中等,大起大落} | 年度報酬 std 的市場內分位切三段取兩端 | return_table |
| `risk_shape` | cat{淺回撤,中等,深回撤} | `mdd_pct` 切三段 | 導出 |
| `worst_year`,`neg_year_count` | float,int | | return_table |
| `max_consec_loss_months` | int | 最長連續虧損月數 | return_table |
| `holdings_median`,`holdings_p10` | float | 由 `company_count`（換股日）算 | stock_data |
| `coverage_ratio` | float 0–1 | **有持股月份 ÷ 總月份** | stock_data |
| `turnover_ann` | float | 年換手率 | trades |
| `size_tilt_pct` | float 0–100 | 持股的 MKTCAP 分位中位數 | trades × MKTCAP |
| `smallcap_share` | float 0–1 | 持股落在最小市值 1/3 的比例 | trades × MKTCAP |
| `annual_returns` | struct/26欄 | 2000–2025 逐年報酬，**只存數字不貼牛熊標籤** | return_table |
| `is_usable` | bool | 尾端寬鬆硬篩結果 | 導出 |

> ⭐ **斷循環鐵則（不可違反）**：本表**不得出現任何需要 regime 的欄位**。`annual_returns` 只存數字，貼標籤是階段2 的事。這是階段1 不依賴階段2 的保證。

## 3.4 階段2 產出

`regime/regime_table_{TW,US}.parquet`（`regime_id`, `start`, `end`, `label∈{牛,熊,危機,盤整}`, `name_zh`）
🆕 `regime/regime_rule_{TW,US}.json`——**判定規則物件**（跌幅閾值等參數）。實戰部 Agent0 判當前 regime 必須 import 這份，兩邊共用同一份參數（實戰部 v8 補洞1）。
`macro/macro_history_{TW,US}.parquet`（月 × 5 feature，含近3月平滑版、已標四格）
`macro/zscore_params_{TW,US}.json` + `macro/clock_bounds_{TW,US}.json`——**凍結參數**，out 期沿用不重算
`macro/strategy_x_cell.parquet`（`strategy_uid` × 四格 → `mean_ret`, `win_rate`, `n_obs`, `confidence∈{高,中,低}`）
`regime/consistency_2c.parquet`（一致性標記 + 背離時段清單）

## 3.5 階段3 產出（HRP）

| 檔案 | 形狀 | 說明 |
|---|---|---|
| `clusters/cluster_assign.parquet` | per-策略 | `strategy_uid` × `tree_id` × `cluster_id_L1/L2/L3`。`tree_id ∈ {TW,US,XM} × {normal,crisis}` = **6 棵樹** |
| `clusters/cluster_meta.parquet` | per-群 | 成員數、群內平均相關、代表策略、各 regime 表現 |
| `clusters/cluster_corr_matrix_<tree>.parquet` | k×k | **給 LLM 讀的核心產物** |
| `clusters/co_fail_regimes.parquet` | per-群 | 危機樹中與本群合併的群 |
| `clusters/cluster_story.parquet` | per-群對 | LLM 點③（Opus）產出的互補因果解釋，**凍結** |
| `hrp_tree/linkage_<tree>.npy` + `<tree>_meta.json` | | linkage 矩陣 + 共同窗/參數，供 `T9 compute_weights` 遞迴二分 |

## 3.6 階段4 產出：`strategy_map/`（交給實戰部的唯一入口）

`main.parquet`（3.1+3.3+2b+3 全部 merge）、`returns/`、`clusters/`、`macro/`、`regime/`、`MANIFEST.json`。
🆕 額外欄位：`v1_beneficial`(bool，程式算，取代已刪的 `valuation_verdict`)、return_story 四道安檢**判決**（等級/布林，非文字）。

---

# 第四部分 · 關鍵設計決策

## DD-01　`artifacts_dir` 解析規則　✅

**問題**：候選 CSV 無此欄，但階段1 掃描器需要它才找得到原料。
**決定**：
```python
job = f"{market}_L3_openSec_M" if V == "v0" else f"{market}_L4_openSec_M"
artifacts_dir = ROOT / "code/results_artifacts" / job / strategy
```
**理由**：L4 是 v0/v1 對照實驗、只跑 v1，v0 產物留在 L3（1.3 實查）。
**防呆**：階段0 產出時**逐列 assert 目錄存在**，缺一即中止。已驗證現況缺失 0 個——這條 assert 是防止未來 `results_artifacts/` 被搬動而靜默產生錯誤結果。

## DD-02　報酬序列的權威來源與頻率　✅

| 用途 | 權威來源 | 理由 |
|---|---|---|
| **HRP 相關矩陣**、`annual_returns`、`worst_year`、`return_shape`、`max_consec_loss_months` | `return_table.parquet`（**月頻**）| 已是彙總好的月報酬，12 KB/策略，全庫僅 **169 MB**，且與階段2b 月頻總經天然對齊 |
| `ann_vol`、`coverage_ratio`、`holdings_*` | `stock_data.parquet`（**日頻**）| 需要日波動與換股日持股數 |
| MDD | **沿用階段 −1 既有值**，不重算 | 與已向老師報告的數字保持一致，避免同一策略兩個 MDD |

**✅ 一致性實測（台美 × L3/L4 各一策略，把日淨值複利成月報酬逐月比對）**：

| 樣本 | 共同月數 | max\|diff\| | >1e-6 的月數 |
|---|---|---|---|
| TW_L3 / TW_L4 | 262 | 2.220e-16 | 0 |
| US_L3 | 303 | 4.441e-16 | 0 |
| US_L4 | 302 | 2.220e-16 | 0 |

差異在 float64 精度極限——`return_table` 就是 `stock_data` 的月度彙總，**沒有任何獨立資訊**。I/O 差距 169 MB vs 2.2 GB（**13 倍**）。
（附帶驗證：日頻起點 2004-03-31 → 月表首個非零月 2004-04，吻合。這證明「首個非零月」是正確的起始期偵測法，DD-03 的全量掃描方法因此站得住。）

**化解 1.6 的文件矛盾**：階段3 用月頻（v9 正文正確）；改動 4 補充 A 說的「必須存日報酬」在本專案**早已滿足**（`stock_data.parquet` 一直都在，`return_table.parquet` 連月度彙總都做好了），**不需要任何額外工作**——此待辦應在架構文件標記為已完成，否則接手者會以為還有事沒做。

**🆕 配套**：階段3 完成後抽 200 個策略用日頻算一次相關，比對群結構是否翻盤，作為一句話的穩健性註記。

## DD-03　🔴 台股參差歷史的處理（最高風險項）　✅ 已定案（v1.1 修訂）

**問題**：見 1.5。pairwise-complete 會破壞相關矩陣的 PSD 性質，使 HRP 的距離度量失去數學意義——且 `scipy.linkage` **不會報錯，會照樣給出一棵無意義的樹**。這是最危險的失敗模式。

**⚠️ 本決策已於 v1.1 修訂**：原建議的「方案 C（P95 分位窗）」是基於 150 個抽樣，全量實測後**證明不划算，已撤銷**。

**全量實測的共同窗 trade-off**：

| 窗起點 | 台股保留 | 台股保留率 | 美股保留率 | 可用月數 |
|---|---|---|---|---|
| 2003-01 | 3,462 | 48.3% | 100% | 276 |
| 2005-01 | 5,363 | 74.9% | 100% | 252 |
| 2006-01 | 6,532 | 91.2% | 100% | 240 |
| 2006-09（P95，原建議）| ~6,800 | ~95% | 100% | 232 |
| **2007-01** | **7,160** | **99.97%** | 100% | **228** |

原方案 C 只多 4 個月，卻要犧牲約 630 個台股策略——**老師明確要「留一個軍團讓 AI 選」，砍策略比砍月份代價高得多**。

**✅ 定案：方案 B —— 每棵樹各自的共同窗**

| 樹 | 窗 | 月數 | 保留 |
|---|---|---|---|
| 台↔台 | 2007-01 ~ 2025-12 | 228 | 7,160 / 7,162 |
| 美↔美 | **2002-01** ~ 2025-12 | **288** | 6,915 / 6,916 |
| 台↔美（XM）| 2007-01 ~ 2025-12 | 228 | 14,076 / 14,078 |

**核心理由 —— 被捨棄的台股 2000–2006 本來就是不可信的那一段**：台股 2000–2004 財報幾乎全空、約 90% 是假 0（已知並記錄的資料問題，先前的乾淨期間穩健性檢定即是用 2005–2025 重驗）。故台股窗設在 2007-01 **不是「損失資料」而是「排除已知不可信的資料」**，資料品質反而提升，論文也好交代。

**其餘理由**：
1. **危機樣本零損失**：228 個月 = 2007-01~2025-12，涵蓋 2008 金融海嘯 / 2015 / 2018 / 2020 疫情崩 / 2022 熊市——所有關鍵危機段全在。這最重要，因為「危機時策略共同崩潰」是論文支柱之一。
2. **美股不必陪葬**：per-tree 窗讓美股樹保住 288 個月（24 年），比單一窗多 **26%** 樣本。
3. **跨樹不可比是可接受的**：v9 已定案三組樹是「三種平行的互補性工具」，本就不是拿來互比數值的。

**代價（須寫進論文方法論）**：三棵樹窗不同 → 群間相關數值**不可直接跨樹比較**。

**🆕 配套穩健性檢定**：主分析用上表，另用 **2003-01 窗**（台股保留 48.3%）再跑一次台股樹，比較兩窗下的群結構一致性（ARI）。一致性高 → 證明群結構非窗選擇的產物；低 → 寫進 limitations。成本僅階段3 多跑一次。

**實作要求**：`returns_meta.parquet` 記錄每策略 `hist_start`；每棵樹的窗寫進 `MANIFEST.json`；階段3 驗收必須通過 PSD 檢查（λmin ≥ −1e-8）。

## DD-04　跳過 `position.parquet`，掃描量降 76%　✅ 🆕

| 掃描策略 | 每策略讀取 | 全庫（14,078）|
|---|---|---|
| 讀全部 4 個檔 | 1,474 KB | **19.8 GB** |
| **跳過 `position.parquet`**（建議）| 334 KB | **4.7 GB** |
| 只讀 `return_table` | 12 KB | 169 MB |

**理由**：`position.parquet` 佔 77% 的位元組，但它能提供的資訊 `trades.parquet`（持股+權重+日期）與 `stock_data.parquet`（`company_count`）都已涵蓋——3.3 表中**沒有任何欄位非它不可**。
**風險與防呆**：抽 20 個策略，用 `position.parquet` 獨立驗算 `holdings_median` / `smallcap_share`，與掃描器結果比對一致才採用此優化。

## DD-05　階段1 單趟掃描器　✅

**決定**：**一趟掃完、一次算齊所有 A/B/C 欄位**，不做「一個欄位一支腳本」。
**理由**：I/O 是瓶頸（4.7 GB 隨機小檔讀取），欄位各掃一次會把成本乘上欄位數。
**規格**：
- 進入點 `python -m research.stage1_scan --market TW|US [--resume]`
- **依 job 目錄分片 checkpoint**（沿用 `scan_stock_contributions.py` 的續跑範式），中斷可續、不重跑已完成分片
- 逐策略產 1 列，每 500 列落地一次
- 單策略失敗**不中止全局**：記入 `scan_errors.parquet`，最後彙總報告
- **可平行**：以 job 目錄為單位開 process pool（台美可同時跑）
- ⏳ 效能預估：待 M2 先跑 200 個策略測速，再外推排程

## DD-06　HRP 演算法規格　✅ / ⏳

| 項 | 決定 | 狀態 |
|---|---|---|
| 相關 | Pearson（老師：不用弄太細）| ✅ |
| 距離 | `d(i,j)=sqrt(0.5×(1−ρ))` | ✅ |
| linkage | `single`（HRP 原論文）與 `ward`（分群較平衡）**都跑，比樹的可讀性後定** | ⏳ |
| 分群層級 | L1 / L2 / L3（粗→細）| ✅ |
| **L3 目標群數** | 🆕 **建議錨定在 407**（台 219 + 美 188 個獨立 F 組合）附近 | ⏳ |
| L1 目標群數 | 5–10（給 LLM 讀）| ⏳ |
| 樹的數量 | 3 組 × 常態/危機 = **6 棵** | ✅ |

**🆕 L3 群數為何錨定 407**：架構文件把 L3 群數列為「待資料」，但**資料已經有了**——14,078 個策略只來自 407 個獨立 F 組合，同組合的 35–38 個變體選股池相同、報酬高度相關。L3 若切得比 407 粗，就會把不同 F 組合併在一起、配額失去意義；切得比 407 細，等於在切同一個選股池的雜訊。**以 407 為起點上下試（如 300/400/500），用「L3 群 vs F 組合的 ARI 一致性」當客觀選擇依據**——這比純看樹好看與否更能辯護。

**記憶體預算（實查推算）**：
- 全池 condensed 距離向量 = `14078×14077/2 × 8B` ≈ **793 MB**；`scipy.linkage` 內部會複製 → 峰值約 **1.6 GB**。單市場樹（N≈7,162）僅 205 MB，無壓力。
- 🆕 **前置去重優化**：同 F 組合的變體可能有**完全相同的報酬序列**（C 條件恆真時）。先做序列雜湊去重、對代表元建樹、再把權重攤回同群成員，可望顯著降低 N。M5 開工前先量測重複率。

## DD-07　`valuation` 的定義　✅（回答 GateC 文件 §五-1 待決）

**決定：採方案 (a)** ——`valuation` 即 C-1 的 `V` 欄位（v0/v1），**階段4 不另產 `valuation_verdict`**。
**理由**：老師 2026-08-19 已判定 V 構面「沒有什麼用」；方案 (b)（重算持股當下估值水準）需額外計算，且與 F 構面的估值因子高度重疊，投入產出不成比例。
**連帶**：12 格矩陣的保守型-牛市格改為不限 valuation（實戰部 v8 已改寫完成）；保留程式算的 `v1_beneficial` 布林欄位承接「條件反轉」現象。

## DD-08　凍結機制　✅ 🆕

**問題**：凍結原則靠人工紀律，一旦有人改了上游、下游沒重跑，會靜默產出錯誤且**無法察覺**——這對「凍結地圖不作弊」的論文主張是致命的。

**決定**：每階段產出旁附 `MANIFEST.json`：

```json
{
  "stage": "stage3_hrp",
  "produced_at": "2026-08-2xT..",
  "git_sha": "cd2c48a",
  "params": {"linkage": "ward", "L3_target_k": 407, "common_window": "2005-01..2025-12"},
  "inputs": [{"path": "_frozen/stage1/returns_monthly.parquet", "sha256": "...", "rows": 3812044}],
  "outputs": [{"path": "clusters/cluster_assign.parquet", "sha256": "...", "rows": 14078}]
}
```

`research/freeze.py` 提供 `verify_freeze()`：重算上游指紋、與 manifest 比對，**不符即拒絕執行下游**。
**額外效益**：這份 manifest 就是論文「可複現性」章節的直接證據，也是老師要親自跑程式（M6）時的環境對帳單。

## DD-09　🆕 `rotation_score` 的可計算性確認

架構文件定義為「年度前三大貢獻股在相鄰年度的 Jaccard 相似度平均」。實查 `trades.parquet` 有 `stock_id` + `entry_date` + `return` + `position` → 可依 `entry_date.year` 分組算逐年逐股貢獻 → 取每年 top3 → 相鄰年 Jaccard → 平均。**原料齊備，無阻礙。**
**邊界處理**：有效交易年數 < 3 的策略 → `rotation_score = NaN` 且 `credibility_grade` 不因此降級（避免誤殺短歷史策略），另記 `rotation_n_years`。

## DD-10　🔴 主鍵必須含 market（v1.1：已由實測確認為真實問題）　✅ 已定案

台美策略字串來自同一套因子命名規則（如 `PB_qb0of3__EPS_qb0of3__C11_EPS_DYN_qmax8__v0`），字串**不編碼市場**。

**✅ 全量實測**：台美候選池的策略字串交集 = **1,381 個**（市場內部則無重複：台股 0、美股 0）。這不是理論風險，是既成事實。

**若以 `strategy` 為鍵合併**：`merge` 產生笛卡兒積（憑空多 1,381 列），或 join 靜默取錯（台股策略拿到美股的 `effective_n`、被分到美股的 cluster）。**1,381 / 14,078 ≈ 9.8% 的資料會錯，且無任何錯誤訊息**，錯誤會一路傳到 strategy_map、Agent1 快篩、最終建議卡。

**決定**：全管線主鍵一律 `strategy_uid = market + "::" + strategy`（人眼可讀，debug 時一眼看出市場）；`contracts.py` 對每份產物 `assert df.strategy_uid.is_unique`。

> 1,381 個碰撞本身是**好消息**——代表台美有共通的有效因子組合。問題只在主鍵設計，不在資料。

---

# 第五部分 · 逐階段模組規格

> 格式一致：輸入 → 演算法 → 輸出 → 複用 → 驗收。

## 階段0 · 格式整備（`stage0_index.py`）

**輸入**：2 份候選 CSV（`utf-8-sig`）、8 份 `stats.parquet`（L3/L4 × openSec × 台美）
**演算法**：讀入並改中文欄名 → 拆 `f_combo`/`C` 字串 → 導出 `F2_empty`/`factor_type` 的原料 → 依 DD-01 解析 `artifacts_dir` 並 assert 存在 → join `stats.parquet` 補 `sharpe_ann`/`daily_sharpe`/`avg_drawdown` → 組 `strategy_uid`
**輸出**：`_frozen/stage0/candidate_index.parquet`（14,078 列）
**驗收**：列數 14,078｜主鍵唯一｜`artifacts_dir` 存在率 100%｜`F2_empty` 計數 == 台 384 / 美 652｜`CAGR` 與原 CSV 逐列相符

## 階段1 · 標記（`stage1_scan.py` + `stage1_marks.py`）

**輸入**：`candidate_index.parquet` + 每策略 3 個 parquet（DD-04）+ MKTCAP（DB）
**演算法**：
1. `stage1_scan`：單趟掃描（DD-05）→ 逐策略原始指標 + `returns_monthly`（long）+ `returns_meta`
2. `stage1_marks`：`groupby(market)` 算全部 `*_pct` → 判 `credibility_grade`/`stability_grade`/`return_shape`/`risk_shape` → 尾端**兩把刀**（低 `effective_n`＋低 `rotation_score` 的假貨、`coverage_ratio` 過低）→ `is_usable`

**輸出**：`strategy_marks.parquet`、`returns_monthly.parquet`、`returns_meta.parquet`、`scan_errors.parquet`
**複用**：`analyze_batch.stock_cum_contrib`、`scan_stock_contributions.py` 的 checkpoint 範式
**驗收**：
- 掃描覆蓋 14,078，`scan_errors` 為空（或逐筆有解釋）
- **回歸檢查**：掃描器算出的 `CAGR`/`max_drawdown` 與階段 −1 既有值誤差 < 1e-6（證明讀對了原料）
- ⭐ **`is_usable` 淘汰率 < 5%**——架構文件已預告此為**預期結果、不是篩選失效**。若淘汰率異常高，是掃描器有 bug，不是策略變爛了
- DD-04 的 20 策略 `position.parquet` 交叉驗算通過

## 階段2a · Regime Dating（`stage2a_regime.py`）

**輸入**：大盤基準序列（台/美各一，來源與 `universe_benchmark.py` 一致）
**演算法**：固定跌幅規則 bull-bear dating，切**連續區間**（cycle）非按年；**純價格、不摻總經**（做法甲）；LLM 點②（Haiku）只貼人話標籤、不做切分判斷
**輸出**：`regime_table_{TW,US}.parquet` + 🆕 `regime_rule_{TW,US}.json`（實戰部共用同一份參數）
**驗收**：區間連續無重疊無空隙；人工核對 2008/2015/2018/2020/2022 五個已知事件皆被標為熊或危機

## 階段2b · 總經↔策略（`stage2b_macro.py`）

**輸入**：FMP（美）/ TEJ（台）總經 5 欄 + `returns_monthly`（複用階段1，不重算）
**演算法**：抓總經 → **發布滯後對齊**（P6）→ 月向量 z-score（參數只用 in-sample 算並凍結）→ 近3月平滑版並存 → 投資時鐘四格（歷史中位數切）→ 對每策略×每格算 `mean_ret`/`win_rate`/`n_obs`/`confidence`
**輸出**：3.4 所列
**驗收**：z-score 參數與四格邊界已凍結成 JSON；每格 `n_obs` 皆有記錄（低樣本標低信心但**不留白**）
**⏳ 阻塞項**：各指標發布滯後量待查官方發布時程——這是本階段唯一的外部相依

## 階段2c · 交叉佐證（`stage2c_crosscheck.py`）

核對 2a 判熊時段是否多落在 2b 衰退/停滯格。**背離時段保留不修、僅標記**（做法甲：價格為主、總經為輔）。
**驗收**：產出一致率數字 + 背離時段清單（此清單是論文回答口委「發現背離為何不修 regime」的材料）

## 階段3 · HRP（`stage3_hrp.py`）

**輸入**：`returns_monthly` + `returns_meta`（階段1）、regime 窗（2c）
**演算法**：DD-03 定共同窗 → DD-06 去重 → Pearson → 距離 → linkage → 準對角化 → 遞迴二分 → 切 L1/L2/L3 → 6 棵樹各產 `cluster_*`
**輸出**：3.5 所列 + `hrp_weights()` 函式（供實戰部 `T9` 呼叫）
**驗收**：
- 相關矩陣**PSD 檢查通過**（最小特徵值 ≥ −1e-8）——DD-03 是否成功的硬指標
- 距離矩陣三角不等式抽檢通過
- `cluster_id_L3` 與 F 組合的 ARI 記錄在案（DD-06 的選擇依據）
- ⭐ **危機樹有效群數 < 常態樹**——這是論文支柱「危機時策略共同崩潰、需跨市場分散」的量化證據；若不成立，該論點必須修正而非硬套
- 🆕 XM 樹中台美策略是否分屬不同高層群 → 跨市場分散有效性的直接證據

## 階段4 · strategy_map（`stage4_map.py`）

在 `candidate_index` 骨架上 merge 階段1/2/3 全部欄位，**不從零建新表**。加 `v1_beneficial`（程式算）+ return_story 四道安檢**判決**（判決凍結、文字按需生成＝乙案）。
**驗收**：6.2 的**12 格矩陣完整性測試全綠**；`MANIFEST.json` 通過 `verify_freeze()`；欄位表與研究部 v9 第五部分逐欄對帳

---

# 第六部分 · 跨部門介面（整體考量）

> 研究部的產出只有一個目的：讓實戰部跑得動。以下三節把「跑不跑得動」變成**可自動檢查的條件**，而不是等實戰部開發時才發現缺欄位。

## 6.1 工具層 T1–T13 ↔ 欄位供應矩陣

| 工具 | 讀什麼 | 供應階段 | 現況 |
|---|---|---|---|
| `T1 get_recommended_criteria` | 12格矩陣（設定檔，非資料）| 實戰部 | 矩陣已於 v8 改寫完成 |
| `T2 filter_pool(條件, **cluster_quota**)` | `strategy_marks` 全部快篩欄 + `cluster_id_L3` | 階段1 + **階段3** | 🔴 **相依階段3**，見 6.3 |
| `T3 get_strategy_profile` | 主表① 完整 profile | 階段1/2/3/4 | |
| `T4 get_macro_fit` | `strategy_x_cell` + `macro_history` | 階段2b | |
| `T5 get_complements` | `cluster_corr_matrix` + `cluster_story` | 階段3 | v8 已改為 cluster-based |
| `T6 check_correlation` | `returns_monthly`（**即時算，不預存 N²**）| 階段1 | 上限約 50 檔 |
| `T7 get_return_story_verdict` | 四道安檢判決 | 階段4 | |
| `T8 compute_portfolio_risk` | `returns_monthly` + `cluster_assign` | 階段1+3 | 即時算 |
| `T9 compute_weights` | `linkage_<tree>.npy` + `returns_monthly` | 階段3 | HRP 遞迴二分 |
| `T10 generate_return_story_text` | 凍結判決 | 階段4 | 按需生成 |
| `T11 get_current_regime` | 🆕 `regime_rule_{TW,US}.json` | 階段2a | **共用同一份參數是關鍵** |
| `T12 query_macro_model` | 凍結 z-score/四格參數 | 階段2b | |
| `T13 get_cluster_info` | `cluster_meta` + `co_fail_regimes` | 階段3 | |

**結論**：13 個工具**沒有任何一個**需要研究部產出以外的東西——介面是封閉的。✅

## 6.2 🆕 12 格矩陣完整性 → 自動化測試

架構文件把「把 12 格逐格拆成 pandas 條件、寫不出來就是漏了欄位」定為驗收方法，但寫成**人工程序**。本設計文件把它升級為**程式測試** `tests/test_matrix_completeness.py`：

```
for 類型 in (保守, 積極, 全天候):
    for regime in (牛, 熊, 危機, 盤整):
        criteria = T1.get_recommended_criteria(類型, regime)
        for 欄位 in criteria.referenced_fields():
            assert 欄位 in strategy_map.schema      # 欄位存在
            assert strategy_map[欄位].notna().mean() > 0.95   # 且真的算得出來
```

**為什麼這是設計級決定**：矩陣會改（v8 已改過一次），欄位也會加。人工檢查在改第二次時就會漏。把它變成測試，**任何一次矩陣修改都會自動告訴我們研究部要補什麼欄位**——這正是跨部門接縫最容易爛掉的地方。

## 6.3 🔴 相依警示：`T2` 的 cluster 配額使實戰部相依階段3

「快篩多樣性假象」的對策是 `T2 filter_pool` 加 cluster 配額，這讓**實戰部最前面的一步（Step2 快篩）相依研究部最後面的階段（階段3 HRP）**。

**後果**：實戰部**無法在階段3 完成前做端到端測試**。
**對策 🆕**：階段3 交付前，先提供**降級版 `cluster_id_L3`**——直接用 `f_combo`（F 組合）當代理分群鍵（台 219 + 美 188 = 407 群，正是 L3 想逼近的粒度，見 DD-06）。如此實戰部可提前開工，等真 HRP 群出來再無痛換掉。
**驗證**：真 L3 出來後，計算它與 `f_combo` 分群的 ARI，同時驗證降級版的合理性與 DD-06 的群數選擇。

## 6.4 產出A / 產出B 一致性

產出A（研究部 20 年情境×策略對照回顧表）與產出B（實戰部即時建議）**必須讀同一份凍結 `strategy_map`**，否則會出現「回顧說選 X、即時卻選 Y」的矛盾。
**機制**：兩者都經 `verify_freeze()` 檢核同一組 manifest 指紋；不允許任一方讀 `_frozen/` 以外的中間產物。

## 6.5 look-ahead 防護的落點

| 防線 | 落在哪 | 機制 |
|---|---|---|
| in/out 切點 | 階段 −1 已切（`IN_SAMPLE_END=2025-12-31`）| 既有 |
| 參數凍結 | 階段2b z-score/四格邊界、階段3 HRP 樹 | 存成 JSON/npy，out 期只讀 |
| 總經發布滯後 | 階段2b P6 | 只用「該月當下確定已公布」的最近一筆 |
| 實戰部工具 | T11/T12 吃 `end_date` 參數 | 嚴禁抓截止日之後資料 |
| **系統級** | `verify_freeze()` | 下游拒絕在上游指紋不符時執行 |

---

# 第七部分 · 非功能需求

| 項目 | 規格 |
|---|---|
| **效能預算** | 階段1 掃描 4.7 GB / 14,078 策略（⏳ M2 測速後外推）；階段3 峰值記憶體 ~1.6 GB（DD-06）；階段0/2/4 皆分鐘級 |
| **續跑** | 階段1、階段3 為長工作，一律 checkpoint（分片粒度：階段1 依 job 目錄、階段3 依樹）。中斷續跑不重算已完成分片 |
| **錯誤處理** | 單策略失敗不中止全局，記入 `*_errors.parquet` 最後彙總；**schema 違規則立即中止**（錯的資料比沒資料危險）|
| **可複現** | manifest 記 git SHA + 輸入 sha256 + 參數；LLM 點固定 temperature/seed 並記錄 trace |
| **日誌** | 沿用既有 `_catalog/*_run_log.txt` 慣例，每階段一份 |
| **測試** | ① `contracts.py` schema 斷言（每份產物）② 20 策略黃金樣本人工驗算 ③ `test_matrix_completeness.py`（6.2）④ 冪等性：同輸入重跑產出 sha256 相同 |
| **平台** | Windows + XAMPP/MariaDB 10.4.32。長工作前先確認 MySQL 服務正常（曾因硬關機損壞 Aria 系統表）|

---

# 第八部分 · 執行計畫

## 8.1 里程碑與相依

```
M0 基礎設施（contracts.py / freeze.py / cli.py）
 └→ M1 階段0 candidate_index ────┐
     └→ M2 階段1 掃描器（最重）──┼→ M3 階段1 分位+硬篩 → usable_pool
                                  │
                                  ├→ M4a 階段2a regime ──┐
                                  ├→ M4b 階段2b 總經 ────┴→ M4c 階段2c
                                  │        （M4a/M4b 可平行）
                                  └→ M5 階段3 HRP ←（危機樹需 M4c 的 regime 窗）
                                          └→ M6 階段4 merge + 凍結
                                                  └→ M7 實戰部介面驗收（6.2 測試）
```

**可平行**：M4a‖M4b；台股‖美股（階段1 掃描、階段2、階段3 的單市場樹）
**關鍵路徑**：M0 → M1 → M2 → M5 → M6（M2 與 M5 是兩個長工作）
**可提前**：常態樹只需 M2 的報酬序列，**不必等 M4c**；只有危機樹需要 regime 窗。M5 可拆成 M5-normal（早做）與 M5-crisis（等 M4c）。

## 8.2 建議開工順序（含理由）

| 順序 | 工作 | 為何排這裡 |
|---|---|---|
| ~~0~~ | ~~**DD-03 實測**（全量 `hist_start` 分布）~~ | ✅ **已完成**（2026-08-20，306 秒掃完 14,078 個策略）。最高風險項已在寫任何程式之前解除 |
| 1 | M0 + M1 | 契約先立（含 DD-10 主鍵）；階段0 是分鐘級的 quick win，能立刻驗證落差 1/2/5 的修正是否正確 |
| 3 | M2 階段1 掃描器 | 最重的工作，且下游全部相依它 |
| 4 | M3 + M5-normal | M3 完成即得 `usable_pool`；常態樹可同時開跑 |
| 5 | M4a/M4b/M4c | 2b 有外部相依（發布滯後查證），越早開始查越好 |
| 6 | M5-crisis + M6 + M7 | |

> 🔜 **老師 M6 待辦**：老師要親自跑程式、下週在學校交付可執行版本。`research/cli.py` 的統一進入點 + `MANIFEST.json` 正是為此設計——老師拿到的應該是「一行指令跑一個階段」而非一堆散落腳本。

---

# 第九部分 · 風險與待決

## 9.1 風險

| # | 風險 | 影響 | 對策 | 狀態 |
|---|---|---|---|---|
| R1 | **台股參差歷史破壞 HRP 距離度量** | 階段3 整個不可信 | ✅ 已實測定案：DD-03 方案 B（每樹各自窗）+ 階段3 PSD 硬檢查 | ✅ **已解除**（2026-08-20 全量實測）|
| R2 | 階段1 掃描耗時超出預期 | 排程延誤 | checkpoint 續跑 + 台美平行 + DD-04 減 76% I/O | ⏳ M2 測速 |
| R3 | 實戰部相依階段3 無法提前測試 | 開發序列化 | 6.3 降級版 `cluster_id` | ✅ 有解 |
| R4 | 階段2b 總經發布滯後量查不齊 | 2b 阻塞 | 越早查越好（排序 5）；查不到的指標先標低信心 | ⏳ |
| R5 | 凍結被無意破壞 | 論文主張受損 | DD-08 `verify_freeze()` | ✅ 有解 |
| R6 | HRP 危機樹「有效群數下降」不成立 | 論文支柱之一失效 | 階段3 驗收即檢查；**若不成立就改論述、不硬套** | ⏳ |
| R7 | 主鍵碰撞靜默錯 join | 全盤錯誤且難察覺 | DD-10 `strategy_uid` + 唯一性斷言 | ✅ 有解 |

## 9.2 待決 → ✅ 已於 2026-08-20 全數定案

| # | 事項 | 定案 |
|---|---|---|
| P1 | DD-03 共同窗方案 | ✅ **方案 B 每棵樹各自窗**（台 2007-01 / 美 2002-01 / XM 2007-01）+ 2003-01 窗穩健性對照。原方案 C 已撤銷 |
| P2 | DD-06 L3 目標群數 | ✅ 錨定 407（獨立 F 組合數），上下試並用 ARI 選 |
| P3 | 新程式碼落點 | ✅ 新增 `code/research/` 子套件，既有檔案一律不動 |
| P4 | `valuation` 定義 | ✅ DD-07 方案 (a)：直接用 `V` 欄位 |
| P5 | 🆕 主鍵設計（DD-10）| ✅ `strategy_uid = market::strategy` + 唯一性斷言 |
| P6 | 🆕 補 sharpe 方式（DD-01）| ✅ join `stats.parquet` + CAGR/MDD/win_ratio 對帳斷言 |
| P7 | 🆕 產物路徑處理 | ✅ 規則解析物化成 `artifacts_dir` 欄位（不用 symlink：RDP/換機器會斷；不用複製：+19.8 GB）|

> 完整的方案比較與否決理由見 `落差處理方案_v1.md`。

## 9.3 承接自架構文件、仍待資料的門檻（不在本文件解決）

階段1：EffN 門檻、Top1 上限、`rotation_score` 切點、`coverage_ratio` 下限、`stability_grade` 分級
階段2：K 值（類比月數）、各指標發布滯後量、信心門檻 n、regime 跌幅 X%
階段3：linkage method（single vs ward）、三層群數、「群間相關多低算互補」
實戰部：快篩縮到多少、cluster 配額每群取幾個

> **共同框架（✅ 已定案）**：全部用**市場內分位數**表達，不用絕對數字。故這些門檻可以等資料跑出來再填，**不阻塞開發**——這是分位數框架最大的工程價值。

---

# 第十部分 · 驗收標準總表

| 階段 | 硬驗收（不過就是錯了）|
|---|---|
| 0 | 14,078 列｜主鍵唯一｜`artifacts_dir` 存在率 100%｜`F2_empty` == 台384/美652｜CAGR 逐列對得上原 CSV |
| 1 | 掃描覆蓋 14,078｜CAGR/MDD 回歸誤差 <1e-6｜**淘汰率 <5%**｜20 策略黃金樣本人工驗算通過｜無 regime 相關欄位（斷循環）|
| 2a | 區間連續無重疊｜2008/2015/2018/2020/2022 五事件皆標熊或危機｜`regime_rule.json` 已產出 |
| 2b | z-score/四格參數已凍結成 JSON｜每格皆有 `n_obs`｜發布滯後已對齊 |
| 2c | 一致率數字 + 背離清單產出 |
| 3 | **相關矩陣 PSD（λmin ≥ −1e-8）**｜三角不等式抽檢通過｜6 棵樹齊備｜**危機樹有效群數 < 常態樹**（或明確記錄不成立）｜L3 vs F組合 ARI 已記錄 |
| 4 | **12 格矩陣完整性測試全綠**｜`verify_freeze()` 通過｜欄位與 v9 第五部分逐欄對帳 |

---

# 附錄 A · 本文件相對架構文件的修正清單

> 以下 4 項是實查發現架構文件與磁碟現況不符處，**架構文件應據此更新**，以免下一個接手者被誤導。

| # | 架構文件位置 | 原述 | 實查 | 本文件處理 |
|---|---|---|---|---|
| 1 | 研究部 v9 第五部分欄位表 | `sharpe_ann`/`daily_sharpe`/`artifacts_dir` 為「階段 −1 既有」| 候選 CSV 僅 8 欄，三者皆無 | 階段0 補 join `stats.parquet` + DD-01 解析路徑 |
| 2 | 研究部 v9 階段3 | 「14,078 × **312 個月**（2000-01~2025-12）」| 全量實測：台股起始 2000-06~**2007-05**（跨 7 年），美股 2000-02~2002-03 | **DD-03**（最高風險項）|
| 3 | 研究部 v9 改動4 補充A | 「必須先把每個策略的**每日報酬**存下來」| 日報酬已在 `stock_data.parquet`；月報酬已在 `return_table.parquet`，且實測兩者一致到 2.2e-16 | DD-02 定權威來源，此待辦**實際早已滿足，應標記為已完成** |
| 4 | 全部文件 | 未提及候選產物分散在 L3/L4 兩個 job 目錄 | v0→L3、v1→L4，覆蓋率 100% | DD-01 |
| **5** | 🆕 全部文件 | 階段0「合併台美兩份 CSV」未提主鍵設計 | **台美策略字串交集 1,381 個**（市場內部無重複）| **DD-10**：不處理會有 9.8% 資料靜默錯配 |

**附帶澄清（非錯誤）**：候選 CSV 為 UTF-8-BOM 且含中文欄名 `F組合`/`持股數`，讀取須 `encoding='utf-8-sig'`、階段0 須改名為 ASCII。

---

*SDD v1 · 2026-08-20 · 對應 研究部 v9 / 實戰部 v8 / GateC v1。待 P1–P4 確認後進入 M0。*
