# 應用層開發追蹤（AI-enabled 決策支援系統）

> **建立於 2026-09-08**，起因是老師 9-8 會議的方向性要求：
> 「你有沒有想要把你這整個東西變成是可以使用性的」
> 「資管的同學現在最有競爭力的，就是你有論文能夠做成一個 AI enabled 的決策支援」
>
> **使用者定案（2026-09-08）**：老師說「可以只做設計就好」，但**決定實際做出來**，
> 而且要有 UI、要參考 fund house 的運作形式、要有 AI 解釋性。
>
> 相關文件：意見來源見 `文件/老師9-8意見待報告事項_下次會議10-01.md` §2.2/§2.3；
> 研究部的框架與結果見 `研究框架總覽_v10.md`；研究部的開發歷程見 `開發待辦追蹤.md`。
> ⚠️ 跟已廢止的 `實戰部架構_v8.md`（三 Agent 線上系統）**不是同一件事**——
> 那份的 agentic 設計已放棄，本文件是**範圍窄很多、非 agentic、決定論優先**的版本，
> 但**沿用該架構留下的工具層** `code/ops/tools.py`（T1~T13）。

---

## 0. 四個決定整個設計的前提（2026-09-08 盤點程式碼、2026-09-09 補第四點）

### 0.1 「不重跑策略」要拆成兩件事

| | 要不要跑 | 依據 |
|---|---|---|
| 15,810 個策略的全域掃描回測（Phase 1-4 sweep）| ❌ **不用** | 凍結在 `_frozen/stage0/candidate_index.parquet` |
| 被選中那 ~30 檔策略「**現在**會買哪些股票」| ✅ **必須跑** | 財報更新 → 同一策略定義選出的股票就變了 |

第二件事**目前沒有現成介面**，但底層機械齊全：
`fcv_core.MarketData.get_mask()` 能算出條件當下有哪些股票通過，
`candidate_index` 有 `F1_factor/F1_band/F2_*/C_id/C_source/C_rule/V` 可還原策略定義。
⇒ **要新寫的是一層薄包裝（策略 uid → 當期持股清單），不是重跑回測。**

✅ **2026-09-09 review 已查證確認不是漏洞**：候選池凍結在過去（2000-2025），
`live` 模式運作到未來會不會漏掉新上市公司？——查了 `candidate_index.parquet`
的策略定義是**規則性的**（例如 `F1_factor=MOM, F1_band=2`），不是綁死的股票
清單，套用在當期最新股票池上，新上市公司只要符合條件自然會被納入，不需要
額外處理。

### 0.2 Fund house 的「兩個時鐘」，我們的研究結果剛好對得上

```
快時鐘（每季 · 財報更新驅動）
  └→ 策略組合不變，重新執行選股 → 換股（rebalance）
      對應 fund house 的「投組再平衡」

慢時鐘（每 2 年 · OOS 窗到期驅動）
  └→ 重跑 HRP 分群 + 重選策略組合 → 換策略（re-selection）
      對應 fund house 的「策略檢視 / 經理人汰換」
```

🔴 **慢時鐘的 2 年不是隨便訂的**——正好是 H-26 實測出的最佳窗口設定
（IS72/OOS24，OOS 每 2 年一段）。**研究結果直接變成系統的運作參數**，
這一段本身就是論文素材。

### 0.3 AI 層必須沿用既有的抗幻覺鐵則

專案已有兩個做完的 LLM 解釋模組，都用同一套架構：
`code/research/cluster_story.py`（群間互補解釋）、
`code/research/cluster_identity.py`（單群身份解釋，H-08）。

> 鐵則（`cluster_story.py` docstring 原文）：**互補程度由程式判定，LLM 只為既定判決寫字**

⚠️ **新的 AI 層必須沿用**，否則會毀掉論文最重要的方法論貢獻（S-08 抗幻覺架構）。
實作上：程式先算出判決（這期選了什麼、跟上期差在哪、風險集中在哪、歷史類似情境）
→ LLM 只把**已算好的判決**寫成人話，禁止推論程式沒算的東西。

### 0.4 「今天」是第 46 個窗——live／replay 兩種模式，以及 LLM 訓練資料汙染風險

**2026-09-09 討論釐清**：這個系統部署運作的那一刻，結構上等同於在 H-26 已有的
45 個 walk-forward 窗次之外，**製造第 46 個窗**——IS＝到今天為止的全部歷史，
OOS＝還沒發生的未來。差別只有一件事：前 45 個窗的 OOS 早就結束、可以驗證；
「今天」這個窗的 OOS **還沒發生，沒有人知道，包括 AI**。

🔴 **但這不代表系統要每次重新驗證方法論**。要切開兩個問題：

| 問題 | 誰回答 | 何時有答案 |
|---|---|---|
| ①「這套規則長期而言會不會贏過亂灑？」| H-26／H-27 的 45 個歷史窗 | **已有答案**（13/13 顯著）|
| ②「今天選出來的這批，這一期會不會贏？」| 沒有人能回答 | 要等這期 OOS 走完才知道 |

系統回答的是已經驗證過的①，**不是每次執行都在重新回答②**。

**因此系統要支援兩種模式**：

```
mode = "live"    正式運作：即時查資料庫，算「今天」（第46個窗）該買什麼，
                  OOS 未知，無法驗證，也不需要驗證方法論本身
mode = "replay"  重放展示：選一個過去的錨點，IS/OOS 都用凍結的歷史資料，
                  不用新開發——45 個窗次本身就是現成的重放資料集
                  （`walkforward_matrix_detail.csv`）
```

`replay` 模式的用途：不用等兩年就能給老師/口委看「選了什麼→後來真的怎樣」的
完整故事，也用來測試 AI 說明管線本身寫得對不對（有已知答案可以核對）。

🔴 **`replay` 模式有一個 `live` 模式沒有的風險：LLM 訓練資料汙染**。
如果重放「假裝現在是 2022-01」，LLM 的訓練資料可能已經包含 2022 年之後真實
發生的事（例如台積電 AI 行情）——若讓 LLM 自己「預測」或「解釋」這個重放窗，
它有可能不是根據餵給它的數字在推論，是直接背訓練資料裡記得的真實歷史。
這種「偷看答案」被口委抓到，是嚴重的方法論破綻。

**`live` 模式天生免疫**（未來還沒發生，LLM 不可能背到），但 `replay` 模式要加
專用防線：

| 風險 | 解法 |
|---|---|
| 讓 LLM 自己「預測」重放窗會怎樣 | **禁止**——重放窗的 OOS 結果由程式從凍結資料算出來當既定事實餵給 LLM，LLM 只轉述，不准推論 |
| LLM 說明裡出現訓練資料背出來的具體史實（我們沒餵給它的細節）| **自動掃描**：比對 AI 輸出裡的具體事實，是否全部對應到 prompt 裡真的餵給它的結構化數字，抓到餵給它以外的具體事實描述就打回——跟 `cluster_identity.py` 「自動掃描總經推論字眼、0命中才算過」同一招 |
| 重放錨點離訓練截止日太近，汙染風險更高 | 選錨點時避開太接近訓練截止日的窗，且展示時老實揭露這個限制 |

---

## 1. 五層架構

```
┌─ L4 · UI 層（Streamlit）
│   設定與執行 / 本期持倉 / 風險儀表板 / AI 解讀 / 歷史版本比較
├─ L3 · AI 解釋層（沿用 §0.3 鐵則 + §0.4 洩漏掃描）
│   程式算判決 → LLM 寫成「投資委員會備忘錄（IC memo）」風格
│   replay 模式強制過洩漏掃描才能輸出
├─ L2 · 風控層 ← 🔴 直接回應老師 9-8 的 fund house 風控意見
│   單群上限 / 單標的上限 / 市場配置 / backfill 警示
│   + 校準監控：進行中窗次的累積表現 vs 歷史 45 窗分布，偏離過大時示警
├─ L1 · 執行引擎
│   讀設定 → 跑下游管線 → 當期持股 + 績效/風險數字 + 決策軌跡
└─ L0 · 設定層（RunConfig：可存、可重用、可版本比較）
     mode（live/replay）/ 市場 / 精選比例 / 分配方式 / 群數來源 /
     風控上限 / 兩個時鐘的頻率 / replay_anchor（僅 mode=replay 需要）
```

### 現有資產 vs 要新寫的

> ⚠️ **2026-09-09 第二輪 review（AL-01）更正**：本表原本完全沒引用 `code/ops/tools.py`
> 的 T1~T13，跟文件開頭「沿用該工具層」的說法矛盾。已查證 `ops/tools.py:544` 的
> T8（`compute_portfolio_risk`）、`:605` 的 T9（`compute_weights`）可以**直接搬**進
> L1/L2，T6（相關矩陣，上限 50 檔，剛好涵蓋我們 25~60 檔的組合規模）、T13
> （群結構查詢）也有用，**Phase B 的工作量因此下修**。

| 層 | 已有（可直接接）| 要新寫 |
|---|---|---|
| **L0** | `code/research/contracts.py` 的 schema 慣例 | RunConfig 定義與存檔（含 mode 欄位）|
| **L1** | `research.cli`（一行指令跑一階段）、`cluster_representatives.select_representatives`、`four_group_control` 的組合評估、`freeze` manifest 鏈；`ops.tools.t9_compute_weights`（對任意子集重跑 HRP，慢時鐘重建樹直接用這支，介面不用改）；`walkforward_matrix_detail.csv`（replay 模式資料源）| **策略 uid → 當期持股**包裝（live）、orchestration 膠水 |
| **L2** | `four_group_control` 已有 `max_cluster_share` / `n_clusters_covered` 診斷欄位；`ops.tools.t8_compute_portfolio_risk`（`portfolio_mdd`／`cluster_coverage[level]["weight_share"]`——C2/C3 風控門檻要比對的原始數字，直接算好給你）；`ops.tools.t6_check_correlation`（兩兩相關，backfill 警示可能用得到）| 風控門檻判定 + 違規攔截 + 校準監控（門檻數字見下方 C5）|
| **L3** | `cluster_story` / `cluster_identity` 的架構、`utils/openai_quota` 額度煞車 | 新的判決計算 + IC memo prompt + 洩漏掃描（replay 專用）|
| **L4** | 無（環境未安裝任何 UI 套件，已確認）| 整個 UI app |

⚠️ **T8/T9/T6 的共同限制**：底層讀 `_returns_monthly()`＝凍結的
`STAGE1/returns_monthly.parquet`，**不是即時資料庫**——算的是「這組策略的
歷史風險輪廓」，不能拿來算 A2 要的「當期即時持股」，這件事文件原本的判斷
是對的，不用改。**T13** 另有限制：它查的是既有六棵**凍結**樹的
`cluster_assign.parquet`，慢時鐘重建的新樹不在裡面，只能用在查舊樹／
`replay` 模式，不能查即時重建的樹。

---

## 2. 分期實作與狀態

| 階段 | 內容 | 產出 | 驗證方式（完成即跑，不可跳過）| 狀態 |
|---|---|---|---|---|
| **A** | 核心可跑：RunConfig + 執行引擎 + 本期持股清單（CLI 版）| 能按設定重跑、輸出當期持股 | ⚠️ 只驗證了 `replay`——本機資料庫在開發當下連不上（見 CLAUDE.md §5），`live` 跑不了，改用 `replay` 模式跑通全流程並人工核對持股清單、績效數字合理 | ✅ 已完成（replay），`live` 待 A2＋資料庫恢復 |
| **B** | 風控層 + 集中度報表 | 風控判定、違規標記 | 拿 A 的真實輸出跑風控判定，故意調低門檻製造一次違規、確認真的被攔下（C4）| ✅ 已完成。實跑驗證抓到一個真實 bug：群佔比不能複用 `ops.tools.t8` 的 `cluster_coverage`（對到凍結全域樹，跟 walk-forward 每窗重建的樹對不起來，差 2.8~3.6 倍），已改讀 `engine.Holdings.cluster_info` 修正 |
| **C** | UI 儀表板 | 可操作介面 | 用真實 RunConfig 走一次完整流程（設定→執行→看結果），確認畫面數字跟 CLI 版一致 | ✅ 已完成。`code/app/ui.py`（Streamlit），用 `AppTest` 跑過設定→執行→風控違規/覆核→AI備忘錄→歷史紀錄全流程，數字跟 CLI 版一致；E2 的「距下次重跑還有多久」需要 `live` 模式才有意義，UI 上先誠實標註未實作，不顯示假倒數 |
| **D** | AI 解讀（IC memo 產生器）| 本期決策的人話說明 | 人工核對 memo 內容跟結構化判決一致（抗幻覺鐵則）；`replay` 模式額外跑洩漏掃描確認能攔下人工植入的洩漏測試案例 | ✅ 已完成。已用真實 LLM 呼叫（借用 `cluster_story` 的 `gpt-5`，`app_memo_model` 這個 config.ini key 還沒設）驗證過一次，D2 洩漏掃描在真實輸出上通過；數字先在 `build_prompt` 統一格式化成百分比/小數字串再餵給 LLM，LLM 照抄不重算 |

### §7 重新設計的分期（P0~P3，2026-09-10 起）

| 階段 | 內容 | 驗證方式 | 狀態 |
|---|---|---|---|
| **P0** | 三態驗證標籤定案（G7）；確認 `_frozen/stage3/TW_normal` 可直接使用 | 實跑確認樹的窗／k／策略數與正式模式需求一致 | ✅ **已完成**（2026-09-10）。實測 `TW_normal`：IS 2007-01~2025-12（228 個月）、k=6、6,679 檔，與 `contracts.HRP_WINDOWS["TW"]` 完全吻合 |
| **P1** | 新增 `code/app/clustering.py`：①凍結主線樹載入 ②任意窗建樹（驗證模式用）③代表挑選 ④**IS-only 評估路徑**（R14）⑤G4 一致性驗收 | `python -m app.clustering verify --n 14` | ✅ **已完成**（2026-09-10）。**14/14 成員清單完全相同，`is_cagr` 最大差 5.55e-17**（G4 容差 1e-8）。覆蓋 `fixed` 8 格／`silhouette_is` 6 格、A_hrp/D/E 三組、五種比例、兩種分配 |
| **P2** | 正式模式（固定 IS，無日期選擇器）＋ 驗證模式（凍結窗次）雙軌，貫穿 L0~L4；併同修掉 R3/R4/R6/R8②/R11/R12/R13 | `AppTest` 兩種模式全流程 ＋ CLI 兩種模式 ＋ `research.tests` 回歸 | ✅ **已完成**（2026-09-10）。詳見 §2.1 |
| **P3** | AI 升級／樹狀圖視覺化／新面孔 vs 老面孔 | — | ⬜ 未開始 |
| ~~探索模式~~ | ~~任意 IS 區間、手動 k~~ | — | ❌ **決定不做**，理由見 §7.5 G5 |

#### 2.1 P2 改了哪些檔案（2026-09-10）

| 檔案 | 改動 |
|---|---|
| `app/config.py` | `KMode` 加 `mainline_h03`；`__post_init__` 強制 mode↔k_mode 配套（live 必須 `mainline_h03`、replay 不得用它），避免稽核紀錄講不清楚 k 從哪來；新增 `default_live()` |
| `app/engine.py` | 新增 `_run_live()`（用 `clustering.load_mainline_tree` + `select` **即時挑代表**，候選方案三組也現場算）；新增 `reference_oos_distribution()`（R13＋R8②）；`Holdings` 加 `tree_info`／`validation`／`reference_oos` 與 `has_oos` property；replay 路徑同步補齊這些欄位 |
| `app/calibration.py` | R3：`CalibrationResult` 加 `status`／`note`，`oos_*` 改 Optional；正式模式回 `tracking_started` 而**不是崩潰或假裝算得出來**。docstring 更正舊規劃（「從 audit_log 累積」目前資料上做不到，因為 `returns_monthly` 就到 IS 結束為止）|
| `app/audit.py` | R4：新增 `_input_versions()`（記 4 個輸入檔的 size+mtime）、`record()` 收 `stock_holdings`／`stock_as_of` 並存不重複股票清單；entry 加 `tree_info`／`validation`／`has_oos`／`reference_oos`。R11：`_IDENTITY_KEYS` 加 `mode`。R6：`diff_holdings()` 加 `is_chronological`／`direction_note` |
| `app/memo.py` | R13：`_fmt_perf()` 依 `has_oos` 分流並內嵌 `_is_caveat`／`_no_oos_reason`；R8②：`_fmt_reference()` 含基準與「不可說贏大盤」註記；R12：`_ALT_CONTEXT` 定錨句（用查證過的 16.8%／13.2%）；`_fmt_alt()` 保留 `is_*`/`oos_*` 原欄位名不混用；system prompt 加鐵則 8~11 |
| `app/ui.py` | 模式 radio（正式／驗證）；正式模式隱藏窗次選單並改用凍結主線樹；**R13：正式模式最醒目的 metric 位置放「同類設定歷史 OOS 分布」而非本次 `is_cagr`**；G7 標籤＋結構性限制；R8② 基準區塊；R12 定錨句；R3 校準狀態分流；R4 把股票持股帶進 `record()`；R6 逆序警示 |
| `app/cli.py` | `new-config --mode live/replay`；`run` 的校準輸出依 `evaluable` 分流 |

**驗證結果**（全部實跑，非單元測試）：

| 項目 | 結果 |
|---|---|
| `AppTest` 正式模式 | ✅ 無例外。metric 顯示「持股 30 檔／同類設定歷史 OOS 中位數 17.66%／歷史 OOS p10 10.54%／單檔權重 3.33%」——**本次 `is_cagr` 刻意不在 metric 位置**（R13）。G7 標籤、結構性限制、IS 警語、無 OOS 說明、基準、R12 定錨句、`tracking_started` **七項檢查全過** |
| `AppTest` 驗證模式回歸 | ✅ 無例外。metric 仍為「持股 50 檔／OOS CAGR 14.24%／OOS Sharpe 1.10」，與 P2 前一致 |
| CLI 兩種模式 | ✅ live：30 檔、群佔比 16.67%、校準 `tracking_started`；replay：50 檔、OOS CAGR 14.24%、校準正常判定 |
| `research.tests` | ✅ **129/131，與 P2 前相同**（那兩項是 `results_artifacts` 換機器問題，見 CLAUDE.md §2）|

🔴 **P1 的一致性測試證明的是「接線正確」不是「演算法正確」**（G4 已更正措辭）——
`app/clustering.py` 全部委派給研究部既有函式（`stage3_hrp`／`walkforward_matrix`／
`cluster_representatives`），演算法是同一份程式碼，測不出演算法對錯。它能抓到的是
參數傳錯、日期切錯、排序不一致、群標籤對錯位這類**靜默錯誤**——那正是本專案
最常踩的一類雷（`pitfalls.md` §6）。

**若時間緊**：A+D 是最小可展示組合（有數字、有 AI 解讀），C 可後補。**但驗證方式不能省**——這是專案一貫的要求（每階段開發完要實跑驗證），寧可少做一個階段，不要跳過某階段的驗證。

---

## 3. 決定事項（2026-09-09 全部拍板，同日完成方法論 review）

按架構層分組，共 20 項。除 C2/C3 有詳細討論過程（見下方）外，其餘採建議值。

> 🔴 **2026-09-09 兩輪方法論 review 共抓到 8 個原本沒定義清楚的地方，已補進對應項目**：
> **第一輪**——**B2**（慢時鐘 k_mode 沒定）、**B3×C5**（提早觸發跟校準監控的互動
> 沒定義）、**D2**（洩漏掃描的技術難度被過度簡化）、**D3**（新增用量沒算進額度
> 預算）。**第二輪（外部審查 AL-01~AL-04）**——**AL-01** 現有資產表漏列
> `ops/tools.py` 的 T6/T8/T9/T13，Phase B 工作量下修；**AL-02** C5 補上具體門檻
> （OOS CAGR p10=10.54%／Calmar p10=0.679）；**AL-03** B2 的 IS 窗長度定義補上
> 且**更正外部建議的 rolling 寫法**（H-26 已證實 rolling 比 anchored 差）；
> **AL-04** C4 補上 override 流程、F2 補上必記 override 事件。
> 另外查證確認候選池過時問題**不是漏洞**（見 §0.1）。

### A. 範圍與資料層

| # | 事項 | 決定 |
|---|---|---|
| ~~A1~~ | 部署市場範圍 | ~~**先只做台股**，架構驗證完再擴美股／跨市場~~<br>🔴 **2026-09-11 作廢，改為 TW／US／XM 三市場全開**（§9.5 H2）。理由：①研究唯一證實的 HRP 貢獻是**跨市場**回撤控制（M-03）②XM 樹 3 群完全按市場切開（投影片 2.4），**只有用 XM 樹才拿得到跨市場分散**（群1台股 對 群2/3美股 ＝ 0.536/0.548，全 39 對最低）③只做台股等於只展示三個市場裡表現最弱的那一個——對真實市場指數，台股勝率 71.1%，美股與跨市場都是 **100%**。<br>⚠️ 連帶要處理的事見 §9.7（XM 不是資料庫層的真實市場、C2/C3 門檻要各市場重訂、US 的 IS 是 288 個月不是 228）|
| A2 | 當期持股怎麼算 | **即時查資料庫**算最新財報條件（非預算快取）。⚠️ **2026-09-09 暫緩，範圍比預期大**：查過 `build_tree_for_window`/`run_one_window`（`research/walkforward_matrix.py:348-554`）才發現，`live` 模式要重建的樹不是只查「今天符不符合篩選條件」這種輕量查詢——HRP 建樹靠的是候選池 15,810 個策略的**歷史月報酬相關性**（`_frozen/stage1/returns_monthly.parquet`），而這份資料**只到 2025-12**（`IN_SAMPLE_END`）。要在真正的「今天」重建樹，得先把候選池從 2026-01 到現在這段報酬序列補齊，而 `returns_monthly.parquet` 當初是**掃描 `results_artifacts/`（各策略回測產物）彙整出來的**（見 `stage1_scan.py`），不是輕量 SQL 查詢——本質上要重跑一小段跟當初產生候選池歷史報酬同一等級的批次回測。工作量遠比原本估計的「orchestration 骨架＋DB查詢」大，且沒有 DB 也驗證不了補齊邏輯對不對，考量 9/20 要開始寫論文的時程，**決定先暫緩，不投入開發**，等之後有餘裕再重新評估值不值得做。<br>🔴 **2026-09-10 更正（§6 落差⑤）**：上面暫緩的只是「重建 HRP 樹」這一半（慢時鐘）。**「已凍結驗證的策略清單，現在實際持有哪些股票」（快時鐘）不需要重建樹**，資料庫這次也已修好（獨立版 MariaDB），這部分**已實作**——見 `resolve_strategy_holdings.py` 跟 `app/ui.py`「股票層級持股明細」的「今天」選項，實跑驗證過（50 檔策略 → 1,872 筆策略-股票對應、613 檔不重複股票）|
| A3 | 支援模式 | **`live` + `replay` 都做**（§0.4）；`replay` 直接讀 `walkforward_matrix_detail.csv`，不用新開發資料源 |
| A4 | `replay` 錨點選擇原則 | 避開太接近 LLM 訓練截止日的窗，展示時老實揭露汙染風險限制 |

### B. 執行層（兩個時鐘）

| # | 事項 | 決定 |
|---|---|---|
| B1 | 快時鐘頻率（換股）| 沿用財報更新驅動（季度）|
| **B2** | 慢時鐘頻率（換策略）| 沿用 H-26 驗證出的 **2 年**（IS72/OOS24）。**群數來源固定用 `silhouette_is`**（每次重選 k，不沿用舊 k）——H-26 §1.5 已驗證這比 `fixed` 勝率更高（94.3% vs 92.3%）且沒有前視偏誤。**IS 窗長度＝原始錨點（台股 2007-01）到觸發當下的全部歷史（anchored，隨時間增長）**——⚠️ **2026-09-09 第二輪 review（AL-03）更正**：外部審查一度建議「固定抓觸發當下往前 72 個月」，**這其實是 rolling 窗的做法，H-26 已實測證實 rolling 是 13 個方案裡表現最差的**（OOS CAGR 20.95% vs anchored 22.44%）——採用 rolling 會讓部署系統偷偷用一個已驗證較差的設計，故明確排除，維持 anchored |
| **B3** | 慢時鐘到期前能否手動提早觸發 | **允許**，但要有明顯的「非預定週期」警示。**觸發時 C5 校準監控的追蹤起點要跟著重置**（見 C5 說明），UI 上要標示「這是提早觸發後的新窗次」——2026-09-09 review 補上這個原本沒定義的互動 |

### C. 風控層

| # | 事項 | 決定 |
|---|---|---|
| C1 | 風控門檻來源 | **兩者都要**：預設值＝研究結果，允許使用者自訂上限 |
| **C2** | **單一股票權重上限** | **8%**（詳見下方參考依據）|
| **C3** | **單一 HRP 群佔比上限** | **等量分配 25%／比例分配 50%，隨分配方式自動切換**（詳見下方）|
| **C4** | 違規時的行為 | **直接攔下**，要求人工確認，不只標記警示。⚠️ **2026-09-09 review（AL-04）補充流程**：單一使用者系統，「誰確認」就是操作者本人——重點不在身份，在於**要有明確的 override 畫面**：風控攔截後，UI 顯示違規項目跟數字，操作者按「覆核通過」才能繼續，覆核當下要填一句話原因（自由文字，不驗證內容，只留痕）；**override 事件強制寫進 F2 稽核紀錄**，不能只記「這次執行完成」不記「這次執行是被人工推翻風控才完成的」|
| **C5** | 校準監控 | **要做**——進行中窗次的累積表現 vs 歷史 45 窗分布，偏離過大時示警。**追蹤起點＝上一次分群（含 B3 提早觸發）的時間點**，B3 觸發時起點重置、不沿用舊視窗的累積數字，避免拿「跨了兩次不同分群結構」的資料混在一起比。⚠️ **2026-09-09 review（AL-02）補上具體門檻**：用同一套「從 45 窗歷史反推」的方法，取部署預設組合（台股／legacy／等量）的 OOS 分布 **p10 分位**當警戒線——**OOS CAGR p10 = 10.54%、OOS Calmar p10 = 0.679**，進行中窗次的累積表現低於這兩個數字任一個就示警 |

#### C2／C3 的參考依據（2026-09-09 從實測資料反推，不是猜的數字）

**C2（單一股票）**：A_hrp 是等權組合，單檔權重 = `1/n_members`，可以從歷史
900 格資料直接反推：

| 台股 | 檔數範圍 | 單檔自然權重範圍 |
|---|---|---|
| legacy | 25~60 檔 | 1.67%~**4.00%**（歷史最大值）|
| 1%~10% | 67~668 檔 | 0.15%~1.49%（檔數一多自動被稀釋）|

只有 legacy 比例會產生較高單檔權重，且**歷史上從沒超過 4.00%**。
⇒ 這個上限的作用是「異常偵測安全網」，不是日常限制——正常運作（legacy+等量）
幾乎不會撞到。**取歷史最大值的 2 倍留緩衝 → 8%**。

**C3（單一群佔比）**：`max_cluster_share`，依市場×分配方式（台股實測）：

| 分配方式 | 平均 | 最小 | 最大（歷史從沒超過）|
|---|---|---|---|
| 等量 | 16.0% | 8.3% | **22.3%** |
| 比例 | 37.4% | 23.3% | **46.7%** |

因為預設用等量分配，正常區間是 8.3%~22.3%，取最大值加緩衝 → **等量 25%**。
比例分配天生集中度高得多（跟 B_all 全宇宙集中度一樣），若沿用 25% 幾乎每次
都觸發，故**上限隨分配方式自動切換：等量 25%／比例 50%**。

⚠️ **未來擴到跨市場樹（XM）時，這組數字要重新訂**——XM 只有 3 群，等量分配
的 max_cluster_share 平均就有 **31.0%**（範圍 25.0%~33.5%），因為 1/3 群天生
逼近 33%。這不是集中度失控，是市場邊界本身的結果（3 群大致對應「1個台股
大群+2個美股群」），但代表 C3 的台股門檻不能直接套用到 XM。

### D. AI 解釋層

| # | 事項 | 決定 |
|---|---|---|
| D1 | 解讀深度 | 先做**①本期選股說明**，②情境比對第二輪再加 |
| **D2** | 洩漏掃描 | **要做成強制關卡**，`replay` 模式強制開啟（§0.4）。⚠️ **2026-09-09 review 更正**：不能簡單類比成 `cluster_identity.py` 的關鍵詞掃描——那是封閉集合的字串比對（禁用總經字眼），洩漏掃描要抓的是「AI 講了沒被餵給它的具體史實」，是開放式的語意問題，換個說法就躲得過關鍵詞比對。**Phase D 開發時要另外設計檢測方法**（可能要用另一個 LLM 呼叫去比對「這句話有沒有超出給定資訊」），不是複用同一套機制 |
| **D3** | LLM 模型與額度 | 沿用 `cluster_story`/`cluster_identity` 現有設定（`utils/openai_quota.py`）。⚠️ **2026-09-09 review 補充**：只是沿用煞車機制，**還沒算過這套新系統會增加多少次呼叫**（季度 IC memo＋`replay` 展示說明）疊加在既有用量上會不會提前撞到每日額度——**Phase D 開發時要重新估算呼叫頻率，跟現有額度對一次**。✅ **2026-09-09 已用真實帳本（`code/_catalog/llm_usage.jsonl`）估算完畢，排除風險**：`app_memo` 跟 `cluster_story`／`cluster_identity`／`decision_layer_arm_b` 共用同一個 `gpt-5` 標準池（1,000,000 tokens/日）。單次 IC memo 實測 ~3,736 tokens（3 次真實呼叫：3,830/3,802/3,577）。歷史最重的一天（2026-08-30，cluster_story+cluster_identity 批次）用了 193,470 tokens（19.35%）；就算展示期間一天測 20 次 IC memo（~74,720 tokens）疊加在那天上，合計也才 26.8%，離每日上限還很遠——正式上線後 D1 只跟著 B1 快時鐘（季度）走，一年頂多 4 次，用量更是可忽略。額度風險正式排除，不用再另外設計節流機制 |

### E. UI 層

| # | 事項 | 決定 |
|---|---|---|
| E1 | UI 框架 | **Streamlit** |
| E2 | 排程方式 | **純手動觸發**＋顯示「距下次重跑還有多久」，不接自動排程 |

### F. 維運層

| # | 事項 | 決定 |
|---|---|---|
| F1 | 歷史版本保留策略 | **全部留**（資料量不像 `results_artifacts` 那種等級）|
| **F2** | 每次執行的稽核紀錄 | 沿用 `freeze` manifest 精神記軌跡，格式可簡化。⚠️ **2026-09-09 review（AL-04）補充**：**風控違規＋人工覆核結果（含填寫的原因）是必記項目**，不能只留正常執行的軌跡——這是整個系統裡最需要留痕的一種事件，之後回頭檢討「這次為什麼放行」全靠這筆紀錄 |

---

## 4. 時程約束

| 時間 | 事項 |
|---|---|
| **9/8~9/20** | 老師給的應用層時間（「我們可以串個兩個禮拜」）|
| **9/20** | 開始寫論文（老師明確給的日期）|
| **10/1 早上** | 下次會議 |
| 10 月底（最晚）| 交出第一版全文 |
| 12 月中 | 完成 |

⚠️ **這代表應用層的實作必須在 9/20 前告一段落**，之後要以寫論文為主。
分期設計（A→B→C→D）就是為了讓任何一個時間點停下來都有可展示的東西。

---

## 5. 設計原則備忘（開發時要守住的）

1. **抗幻覺鐵則不可破**（§0.3）——程式判定、LLM 只寫字
2. **不碰凍結鏈**——L1 的重跑是讀凍結產物 + 算當期持股，不改 `_frozen/`
3. **可追溯**——每次執行都要留決策軌跡（沿用 `freeze` manifest 的精神），
   對應 fund house 的 audit trail
4. **風控要能擋，不能只標記**——老師的台積電集中度問題是實務要求，不是報表欄位
5. **設定可重現**——同一份 RunConfig 重跑要得到同樣結果（除非資料更新了）
6. **`live` 模式不做預測、不驗證方法論**（§0.4）——系統回答的是已驗證過的規則，
   不是每次執行都在重新證明規則有效
7. **`replay` 模式一律過洩漏掃描才能輸出 AI 說明**（§0.4）——防 LLM 訓練資料汙染

---

## 6. UI／工作流程重新設計：法人治理參考研究（2026-09-10）

> **起因**：Phase A~D 做完、實跑驗證通過後，使用者提出「應用層現在的操作方式太不實際，
> 不像 fund house 會用的形式」——按鈕、下拉選單堆起來像研究工具，不像投資決策流程。
> 這節記錄查證過的參考材料跟找出的具體落差。**2026-09-10 同日：使用者看完落差表後
> 直接給出對應的 5 項具體執行方案，逐項查證程式碼確認可行後全部實作完成**，
> 見 §6.3 最終版（含每項的驗證結果）。

### 6.1 參考材料（三份，都已讀完/查證，非憑印象）

| 材料 | 性質 | 位置 |
|---|---|---|
| 《紀律長贏：揭開清大校務基金操盤人風險控管與穩定獲利的策略》 | 真實案例書（清華大學永續基金治理），掃描版無文字層 | `book3-part{1,2,3}.pdf`（根目錄，已 gitignore） |
| Ang, Azimbayev, Kim (2026), *The Self-Driving Portfolio: Agentic Architecture for Institutional Asset Management* | 學術論文，機構資產配置的 agentic 架構 | `Agentic Architecture for Institutional Asset Management.pdf`（根目錄，已 gitignore） |
| Xiao, Sun, Luo, Wang (2024/2025), *TradingAgents: Multi-Agents LLM Financial Trading Framework* (arXiv:2412.20138) | 學術論文，多 agent 交易台架構 | `TradingAgents MultiAgents LLM Financial Trading.pdf`（根目錄，已 gitignore） |
| AQR Capital Management 公開資料 | 用 WebSearch 查證，非憑印象 | [Systematic vs. Discretionary](https://www.aqr.com/Insights/Research/Alternative-Thinking/Systematic-vs-Discretionary)、[Portfolio Construction](https://www.aqr.com/Insights/Portfolio-Construction) |

⚠️ 三個 PDF 都是他人著作（書籍/論文），已加進 `.gitignore`（`book3-part*.pdf`、兩篇論文全名），不進版控。

### 6.2 各材料的關鍵發現

**清華永續基金（真實治理案例）**：
- 治理三權分立：**決策**（校務會議→校務基金管理委員會→投資管理小組）／**執行**（財務發展組／基金經理人）／**監督**（校務基金稽核→投資審議委員會）（原書圖4-1）
- 投審會實際開會流程（六段式，2023-12 真實會議紀錄為例）：①回顧上期績效 ②說明哪些判斷符合預期/哪裡有落差 ③市場變化對組合的影響 ④提出下一階段投資構想 ⑤交代風險報酬取捨 ⑥說明為何此時選擇這個曝險水位
- 分歧處理：不是多數決否決專業意見，保留充分討論空間；僵持不下才由主席拍板並承擔責任
- 第7章明講個人不需要真的開委員會，但要「建立能持續反思、檢視與修正的機制」：定期檢查配置、停損/單一標的上限/波動閾值、寫「投資守則」、留紀錄不能靠印象回想——**這段直接對應我們已經做的 RunConfig/C2-C3/C5/audit_log**

**Agentic Architecture 論文**：
- 全架構繞著一份人類寫的 **IPS（投資政策聲明）**運作，之後所有 agent 都在 IPS 邊界內執行，CRO-agent 顯式檢查每個候選組合的 IPS 合規性
- CIO agent 產出的 **board memo** 固定結構：建議配置＋跟基準比的預期表現＋總經理由＋最大部位＋**跟上次相比改了什麼**＋要盯的風險＋下次再平衡計畫與提前觸發條件＋IPS 合規聲明
- LLM-as-judge 有硬約束：最終估計值**必須落在候選方法的數值範圍內**，不能自己生一個沒有出處的數字——跟我們的抗幻覺鐵則（LLM 只能引用提供的數字）同一個精神
- 風險：automation surprise（人類對自動化系統失去情境警覺，只會蓋章不會真的檢視）——board memo 的設計目的就是逼出真正的人類參與，不是走過場

**TradingAgents 論文**：
- 交易台角色分工：分析師（各自獨立產報告）→ 研究員（多空辯論）→ 交易員（綜合下決策）→ 風控小組（激進/中立/保守三方辯論）→ 基金經理人核准
- 刻意用**結構化文件而非自由對話**傳遞資訊，避免多輪傳話後失真（"telephone effect"）

**AQR（查證後）**：
- 「系統化投資仍然需要判斷，但那個判斷用在設計策略跟風控規則的時候，不是拿來逐案否決系統跑出來的結果」——**幫我們的抗幻覺鐵則背書**，不是新要求
- 有明確 tracking error 目標；再平衡在「維持最新觀點」跟「交易成本」之間找平衡——對應我們 B1/B2 兩個時鐘的設計精神

### 6.3 落差①~⑤：全部已實作並實跑驗證通過（2026-09-10）

> 這節原本是初步落差表，使用者看完後直接給了對應的 5 項具體執行方案
> （含程式改動位置、理由），逐項確認「現有系統就能做，不需要新技術」後，
> 同一天全部做完並用真實資料庫/真實執行驗證過，取代原本的初步落差表。

| # | 內容 | 改了哪裡 | 驗證方式（真實執行，非單元測試）|
|---|---|---|---|
| **①** | 補「累積」的 Portfolio 狀態：`record()` 補存 `holdings.members`（原本只存 `n_members`），新增 `find_previous()`／`diff_holdings()` 算跟上一筆同身份（market/group/ratio/allocation/k_mode）紀錄的新增/剔除 | `app/audit.py` | 實跑 window_no 6→5：正確算出「新增10檔、剔除30檔、不變20檔」；舊格式（無 `members`）紀錄正確回報 `has_previous=False`，不假裝算得出來 |
| **②** | 接上 T8 本來就算好、先前被丟掉的 `factor_exposure_F1`／`market_share`／`regime_avg_ret` | `app/risk.py`（`RiskReport` 加三個欄位）、`app/audit.py`（稽核紀錄一併存）| 實跑確認三組數字正確進到 `RiskReport` 跟 memo prompt |
| **③** | memo 加「同窗多方法對照」（借用 Self-Driving Portfolio 論文 PC Strategy Review 精神）：`engine.py` 撈同一格設定（tree_key/scheme/window_no/k_mode/ratio/allocation）下 `D_top_cagr`／`E_top_calmar` 的 OOS 績效與會增減幾檔 | `app/engine.py`（`_load_alternative_groups()`，新增 `Holdings.alternative_groups`）、`app/memo.py`（新 schema 欄位 `alternative_note`，禁止建議優劣、只能陳述數字）| 實跑：這一窗 `E_top_calmar` OOS CAGR 14.84% 略贏目前選定的 `A_hrp`（14.24%）、MDD/Sharpe 也都較優——資料真實有意義，不是套版 |
| **④** | 補 IPS 等級文件：目標範圍、風險容忍度（C2/C3/C5）、決策治理原則（含 AQR 查證引用）、兩個時鐘、已知限制、合規聲明模板 | 新檔案 `文件/系統投資政策聲明_IPS.md` | 純文件，內容全部可回頭指到程式碼或研究部產出檔案的實際位置 |
| **⑤** | 重新拆解 A2「live 模式暫緩」：慢時鐘（重建 HRP 樹）仍暫緩，但快時鐘（凍結驗證過的策略清單，現在實際持有哪些股票）不需要重建樹，機制早就在 `resolve_strategy_holdings.py` 驗證過，只是被跟慢時鐘的問題混在一起一起暫緩 | `app/ui.py`（date selector 加「今天」選項）、`app/engine.py`／`app/ui.py` docstring 更正過期說法（原本寫「本機資料庫連不上」，這次已修好）| 真實 `AppTest` 點擊「今天」選項＋「解析持股」，50 檔策略跑出 1,872 筆策略-股票對應、613 檔不重複真實現存股票 |

💡 **③ 實質上是一種「互動式 what-if 對照」**（2026-09-10 第二輪審查的觀察，採納）：
它回答的是「同一窗如果改用另一個方法會怎樣」，跟「調整比例/分配方式會怎樣」是同一
類需求，只是換了維度。⚠️ 但**目前只是把凍結表已經算好的結果攤開，不是即時試算**——
真正的互動式 what-if（改參數就即時重算）要等 §7 的 P1 即時引擎做完才可能。
（審查者把這件事掛在「AL-08」編號下，但本 repo 文件只有 AL-01~AL-04，查無此項，
故不採用該編號，見 §8.5。）

⚠️ **③ 有一個尚未補上的呈現問題**：實測 900 格台股對照，A_hrp 在 Calmar 只贏
E_top_calmar **16.8%**（OOS CAGR 13.2%），⇒ 單一窗次「看起來選錯了」是**常態**而非
例外。memo 現在正確禁止 LLM 建議換方法，但沒有提供定錨句把讀者拉回聚合結論。
處理方式見 §8.2-R12（列入 P2）。

**新增的 memo 欄位 `change_note`／`alternative_note`（②③的敘事部分）遵守同一套抗幻覺鐵則**：
只能引用提供的 diff 數字/替代方案數字，禁止推測換股原因、禁止建議該用哪個方案——
那件事已經由 H-26/H-27/M-03 的統計檢定回答過，不是這份 memo 的工作。

🔴 **code review 過程中順手抓到一個真 bug**：`memo.generate()` 的 dry-run 模式原本手動
列了 4 個欄位的假資料，schema 改成 6 個欄位後那份手動清單沒有跟著更新，UI 端用固定
key 去讀 memo 字典會直接 `KeyError` 崩掉。已改成從 `_MEMO_SCHEMA["schema"]["required"]`
衍生欄位清單，未來再加欄位不會重演同樣的遺漏。

---

## 7. 架構重新設計：從「查表」換成「即時計算」（2026-09-10，討論階段，尚未實作）

> **起因**：§6 的 5 項改動全部做完後，使用者再次指出核心問題還沒解決——
> 「目前的操作還是很像回測系統」。要求先重讀 `逐字稿 (1).docx`／`9-8咪挺.pdf`
> 發想方向（明確指示「先給回答我就好，不要動任何東西」），接著要求先講清楚
> 「現在應用層完整的故事線」，再討論怎麼改，避免做錯白工。本節記錄這一輪
> 討論確定下來的方向、查證過的數字、跟拍板的決定——**截至本節記錄時，
> 以下全部是設計討論，`app/engine.py` 等程式碼尚未依此修改**。

### 7.1 診斷：現有故事線的問題出在哪一段

完整重新逐檔查證後畫出來的現狀故事線：

```
candidate_index.parquet（唯一寫死，F/C/V 定義）
        ↓
walkforward_matrix.py（研究部，一次性跑完，8,370 格，凍結）
        ↓
========== 這條線以下，app/engine.py 只有查表 ==========
「選了哪些策略、績效多少、群結構長怎樣」← _load_replay_row() 純 .loc[] 查表
「跟其他候選方案比較」（§6③新增）      ← 還是查表，只是多查兩列
========== 這條線以下才是即時計算 ==========
風控數字（T8）、校準門檻（quantile）、跟上次比較（diff）← 即時算，但材料仍是查表結果
AI 轉述                                              ← 純轉述，不計算
股票層級持股解析（resolve_strategy_holdings.py）      ← 真的即時查資料庫，唯一獨立於凍結表的路徑
```

**精確定位**：核心決策「選什麼策略」現在 100% 是 `.loc[]` 查表，不是即時算的。
§6 五項改動全部是在查表結果之上疊加即時計算（風控/校準/diff/AI），但沒有動到
查表這件事本身，所以使用者的感受是對的——外層修再多，核心機制沒變。

### 7.2 方向：把 `engine.py` 從查表換成即時建樹

> 使用者原話定調：「唯一要寫死的地方是策略產生，但是這些策略怎麼去應用，
> 怎麼分群、建樹，這些都是我們後續可以做發揮」——`candidate_index.parquet`
> （策略定義）維持寫死，`_load_replay_row()` 這一段換成現場計算。

```
① 選策略池      ratio + 市場（唯一還是從 candidate_index 寫死）
② 選建樹區間     IS 起訖日期（正式模式固定 2007-01~最新可用月；不再是「方案A第6窗」代號）
③ 選分群方式     群數 k：輪廓係數自動 / 手動 / 掃描曲線
④ 建樹           現場跑，30秒~2分鐘，出樹狀圖/群結構/ENB
⑤ 選代表規則     A_hrp/D_top_cagr/E_top_calmar，可現場切換不用重建樹
⑥ 組合權重       **等權**（每月拉回等權），跟凍結表驗證過的口徑完全一致
⑦ 風控/校準/持股解析/AI（§6 已即時，不用改）
```

🔴 **2026-09-10 methodology-review 更正（原 §7.2 步驟⑥寫錯）**：本節初稿寫的是
「用 `ops/tools.py` 的 T9（`t9_compute_weights`）現場跑 HRP 算出每個策略的實際權重
（不是等量）」——**這是錯的，會直接跳出已驗證空間**。查證 `walkforward_matrix.py:406`
的 `_evaluate()`（docstring 原文「一組成員在 IS/OOS 的**等權**組合績效」）與
`_portfolio_series`＝`wide.loc[members].mean(axis=0)`，確認 **8,370 格驗證的全部是
等權組合，HRP 只用來分群與配額，從來沒有拿來配權重**（v10 §8 另有「組合報酬隱含
每月拉回等權」一條佐證）。連帶更正：`allocation="equal"/"proportional"` 決定的是
**各群分到幾個代表名額**（`walkforward_matrix.py:539` 的 `allocate(sizes, tot, how)`），
不是投組權重。⇒ **正式模式必須維持等權**；T9 若要用，只能當「若改用 HRP 權重會
長怎樣」的並列對照顯示，不得當作實際權重（否則 C2 的 8% 上限也一併失去依據——
那個數字是從 `1/n_members` 反推的）。

**凍結表（`walkforward_matrix_detail.csv`）不刪，角色改變**：
1. **校準基準**——C5 的 p10 門檻繼續從它來（不變）
2. **驗證邊界標示**——使用者的設定若對得上凍結表某格，UI 標「✅已驗證」；
   對不上則標「⚠️未驗證，僅供探索」，AI 不得對未驗證設定下優劣判斷
3. **回歸測試基準**——新引擎跑凍結表裡任一格的設定，成員清單必須跟凍結表**100%一致**，
   這是驗收新引擎沒寫錯的唯一硬標準（見 §7.5 決定②）

**風險與防線**（對應 §0.3/§6 AQR 查證的「判斷屬於設計階段」原則）：開放日期/k
自由選等於開放未驗證參數空間，容易變成調到好看為止。三道防線：
未驗證設定強制標記、`audit_log.jsonl` 記完整參數（本身就抑制 p-hacking）、
建樹/挑代表只准用 IS 資料，OOS 只能事後顯示。

### 7.3 查證：anchored 撐得住「全歷史建樹」的正式模式嗎

> 🔴 **2026-09-10 第二輪外部審查後大幅改寫。本節初稿有一個無效分析已撤回**，
> 詳見 §8-R9。撤回的內容不刪除、改列在本節末尾留痕（專案慣例：錯誤要留痕，
> 不靜靜改掉）。

#### ✅ 有效的證據：anchored vs rolling（E vs R 對照）

E（anchored）與 R（rolling）規格相同（`min_is_months=96`／`oos_len_months=36`／
4 窗），且 **OOS 區間完全對齊**，是矩陣裡唯一乾淨的 anchored/rolling 對照：

| | OOS CAGR 中位數 | OOS Sharpe |
|---|---|---|
| E（anchored，IS 隨窗成長至 204 個月）| **21.47%** | **1.244** |
| R（rolling，IS 固定 96 個月）| 20.57% | 1.202 |

逐窗對照（第 1 窗兩者 IS 相同，不計）：

| 窗 | E 的 IS 長度 | R 的 IS 長度 | E OOS CAGR | R OOS CAGR | 誰贏 |
|---|---|---|---|---|---|
| 1 | 96 | 96 | 17.50% | 17.43% | （同窗，不計）|
| 2 | 132 | 96 | 24.69% | 22.95% | **E** |
| 3 | 168 | 96 | 22.34% | 22.73% | R（小輸 0.39pp）|
| 4 | 204 | 96 | 35.72% | 24.45% | **E** |

⇒ **三個可比窗，長 IS 贏兩個**。這是矩陣裡**唯一有效**的「IS 長度」比較，因為
E 與 R 在 OOS 對齊的前提下 IS 長度真的不同。⚠️ 但 **n=3，證據力很弱**，只能說
「沒有證據顯示保留全歷史比較差」，不能說「已證實比較好」。

這一點與 H-26 的整體結論一致（`pitfalls.md` §5：**rolling 是 13 個方案裡最差的**，
OOS CAGR 20.95% vs anchored 22.44%，且已明文「不要再提案改 rolling」）。

#### ✅ 錨點與資料範圍

`returns_monthly.parquet` 實測範圍 **2000-02~2025-12**（15,810 個策略）；但 H-26 全部
anchored 方案的錨點是 **2007-01**（`walkforward_matrix.py:115`
`ANCHOR_START = {"TW": "2007-01", "US": "2002-01", "XM": "2007-01"}`）。
**正式模式的 IS 必須沿用 2007-01 錨點，不是 2000-02**——用 2000-02 會偏離
「待在驗證過的空間內」這個立論基礎。

⇒ 正式模式的 IS ＝ **2007-01~2025-12 ＝ 228 個月**
（剛好等於 `walkforward_matrix.py:117` 的 `SCHEME_TOTAL_MONTHS = 228`，台股共同窗全長）。

#### ⚠️ 外插幅度：實測過最長 204 個月，正式模式 228 個月

> 🔴 **2026-09-10 再次修正，先讀 §7.4b**：本小節講的「外插」**只適用於「用這麼長的 IS
> 挑出來的組合，OOS 表現如何」，不適用於「建樹」本身**——228 個月的樹早就建好了
> （`_frozen/stage3/TW_normal`，k=6，6,679 檔，DD-08 凍結），而且是研究部主線的基礎。
> 下面 walk-forward 的 204 個月上限，指的是「有 OOS 可以驗證績效」的最長 IS。

🔴 **`min_is_months` 是「第一窗」的長度，不是每一列的實際 IS 長度**——anchored
的 IS 逐窗成長。實際 `n_is_months`（台股）：

| scheme | A | B | C | D | E | F | G | H | I | J | K | L |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 窗 1 | 72 | 72 | 72 | 96 | 96 | 96 | 120 | 120 | 120 | 144 | 144 | 144 |
| 窗 4 | 144 | 180 | — | 168 | **204** | — | 192 | — | — | — | — | — |
| 窗 6 | 192 | — | — | — | — | — | — | — | — | — | — | — |

全部格子的 `n_is_months` 取值：`[72, 96, 108, 120, 132, 144, 156, 168, 180, 192, 204]`
⇒ **實測過的最長 IS ＝ 204 個月**（方案 E 第 4 窗）。

正式模式 228 個月 ⇒ **外插幅度 204→228，約 12%**。

⚠️ 而且外插區唯一的資料點不好看：`n_is_months > 192` 的桶（62 格，即方案 E 第 4 窗）
OOS CAGR 中位數只有 **1.18%**。但它同時也是 **OOS 2024-01~2025-12**，正是 v10 §8
已知台股表現最差的期間（2021 年後起點的窗全輸大盤，台積電佔指數 40%）——
**兩者完全共線，無法歸因**。不能說「IS 太長導致表現差」，也不能說「跟 IS 長度無關」。

#### 🔴 已撤回：原「IS 長度對 OOS 無系統性影響」的分析（無效，留痕）

本節初稿寫過：

> 控制 OOS 期間後檢查「IS 長度是否為效能槓桿」：同一個 OOS 起始月份，
> IS=72/96/120/144 個月算出的 OOS CAGR 沒有單調關係（例：OOS 起始 2023-01，
> 四種 IS 長度算出完全相同的 34.6%）⋯結論：IS 長度本身對 OOS 無系統性影響。

**這個分析無效，結論撤回。** 原因：那張表用 `min_is_months` 當分組欄位，但它是
**第一窗**的長度，不是那一列的實際 IS 長度。而 anchored 的 `is_start` 恆為 2007-01，
所以 **給定一個 `oos_start`，`n_is_months` 就被完全決定了**——同一列裡四個「不同
IS 長度」的欄位，實際上是同一個 IS 長度。

`oos_start=2023-01` 四欄算出完全相同的 34.6%，當時被當成「IS 長度不影響 OOS」的
證據，**實際上只是因為它們本來就是同一個窗**。

用實際 `n_is_months` 分桶重跑並控制 `oos_start`，每一列都只有一個桶有值：

```
oos_start   <=96    97-144   145-192    >192
2019-01      -     0.3385      -         -
2021-01      -        -      0.1886      -
2023-01      -        -      0.1380      -
2024-01      -        -        -       0.0118
```

⇒ **anchored 設計把 IS 長度與 OOS 期間完全共線，這個資料集根本回答不了
「IS 長度有沒有影響」。** 不是弱證據，是無效分析。

**對 G3 的影響**：決定不變（仍用全歷史），但**理由必須換掉**——見 §7.5 G3。

### 7.4 查證：快時鐘現在踩在哪個資料截斷點上

**釐清 B1/B2 分工**（§0.2/§6.3⑤ 已定義，此處為使用者確認的複誦）：
慢時鐘（2年）決定「持有哪些策略」，快時鐘（季度）在策略清單不變的前提下
決定「這些策略現在買什麼股票」。**快時鐘不選策略、只選股**，策略清單頻繁變動
本身就是過度配適的訊號，這個分工是刻意設計，不是簡化。

**快時鐘需要的資料只有 4 樣**：`candidate_index.parquet`（策略定義，凍結）、
慢時鐘產出的策略清單、DB `factorvalue`（26個因子財報值）、DB `stock`（日收盤價）
+ `company`（名稱）。**不需要 `returns_monthly.parquet`、不需要 `results_artifacts/`、
不需要重跑回測**——只是把條件遮罩重新套一次。

**2026 年財報現況（實測 DB，2026-09-10）**：

| 期別 | 公司數 | 狀態 |
|---|---|---|
| 2026-06-30（Q2）| 637 / 1,739 | ⚠️ 陸續公告中，未完整 |
| 2026-03-31（Q1）| 1,731 | ✅ 完整 |

26 個因子（ROE/EPS/PE/FCF_P 等）DB 裡最新一致到 2026-06-30，但**不完整**。
股價最新交易日 2026-07-24。

**三層截斷鏈**（依序查證，結果是快時鐘目前用的是乾淨的橫斷面，非部分污染）：
1. `format_data.py:adjust_index_of_report()` 套台股法定公告期限：Q2(6/30)→8/31 才生效
   → 2026Q2 資料要等 2026-08-31 才會被計入
2. `fcv_core.py:196` 用 `price_index.max()`（2026-07-24）截斷財報 frame
   → 2026-08-31 > 2026-07-24，2026Q2 那 637 家不完整資料**自動被切掉，不會進計算**
3. `resolve_holdings()` 找 ≤ 指定日的最近交易日 → UI 選「今天」實際落在 2026-07-24

**結論**：快時鐘現在跑「今天」，用的是「**2026Q1 完整財報 + 2026-07-24 收盤價**」的
乾淨橫斷面，沒有橫斷面內部分公司新財報、部分舊財報的不一致問題。代價是落後現在
（2026-09-10）約 7 週，補齊只需要更新股價到最新交易日（財報本身要等 8/31 才有 Q2
可用），這是 collector 職責，不是應用層要處理的事。

### 7.4b 🔴 重大發現：正式模式要的那棵 228 個月的樹**早就建好了**（2026-09-10，使用者指出後查證）

使用者提醒「2007-2025 的樹應該已經建過了，而且是 6 群」——**查證屬實**，這大幅縮小 P1 的範圍。

**證據鏈**：

| 查證項目 | 結果 | 出處 |
|---|---|---|
| 主線樹的共同窗 | **TW ("2007-01", "2025-12")＝228 個月** | `research/contracts.py:56` `HRP_WINDOWS` |
| `normal` 樹是連續窗還是 regime 子集 | **連續完整共同窗**（`crisis` 樹才是危機月份子集）| `stage3_hrp.py:194-198`；docstring:33「normal 樹 **228**/288/228 個月」|
| 策略數 | **6,679** | `cluster_assign.parquet` 實跑，與 walkforward 的 `n_universe=6,679` 完全一致 |
| L1 群數 | **k=6** | `cluster_assign.parquet` 實跑；`MANIFEST.json` `"L1_target": {"TW": 6}` |
| k 怎麼選的 | **輪廓係數，H-03** | `MANIFEST.json` `"L1_target_method": "silhouette（H-03，見cluster_count_selection.py）"` |
| 凍結狀態 | ✅ DD-08 manifest 齊全，含 4 個上游輸入的 sha256 | `_frozen/stage3/MANIFEST.json` |

⇒ **`_frozen/stage3/` 的 `TW_normal` 就是正式模式要的那棵樹。**

#### 這帶來三個直接後果

**① P1 不需要「即時建樹」了（至少正式模式不需要）**
原本 P1 的核心工作是「抽 `app/clustering.py`、現場算 6,679×6,679 相關矩陣、建 linkage、跑
`silhouette_scan` 選 k」。現在這些**全部已經有凍結產物**。正式模式真正還缺的只有一步：
**在這棵樹上跑代表挑選（`_pick_a`）**——因為 walkforward 只在 45 個窗上跑過挑選，
**沒有任何一格是 228 個月的完整窗**，所以「228 個月樹 × 各比例 × 各分配」的成員清單
確實不存在於任何檔案，必須算。

**② R7（量測 `silhouette_scan` 耗時）對正式模式失效**
`k_mode="silhouette_is"` 的語意是「只用該窗 IS 資料選 k」；正式模式的 IS 就是完整 228 個月窗，
而 H-03 正是在這個窗上用輪廓係數選出 k=6。**⇒ 正式模式的 k 已經有答案：6，不需要現場掃描。**
R7 只有在做「探索模式（任意區間）」時才有意義——而那塊已建議不做（G5）。

**③ §7.3「外插」的說法要修正（但不是取消）**
原本寫「228 個月超出實測驗證範圍 204 個月」——這句話**對建樹本身不成立**：228 個月的樹
不但建過，而且是研究部主線的基礎，H-03（群數客觀化）／ENB／群身份／M 系列全部建立在它上面，
是整個專案**驗證最充分**的一棵樹。

**真正沒被驗證的是另一件事，而且結構上永遠無法驗證**：
> 「用 228 個月的樹挑出來的組合，OOS 表現如何？」——因為 IS 用掉全部資料，**依定義沒有 OOS 可留**。

這不是「還沒做」，是**邏輯上做不到**。要驗證就得留 OOS，留了 OOS 就不是 228 個月的窗。
⇒ 外插的對象從「樹的建構」修正為「**組合的績效**」，而且要標明這是結構性限制，不是待辦事項。

#### ⚠️ 順帶查到一個過期註解（不影響結論）

`contracts.py:57-59` 的 `HRP_WINDOWS` 註解寫「TW 保留 **7,160 / 7,162**」，但
`stage3_hrp.py:15-17` 已用修復後的新候選池重新驗證為「TW 保留 **7,125 / 7,128**」，
再經 `is_usable` 過濾後為 **6,679**。`contracts.py` 那行是**候選池修復前的舊數字**，
只是註解沒跟著更新（實際程式不讀它）。已知，不影響任何結果，但下次改 `contracts.py`
時可以順手更正。

### 7.5 已拍板的決定

| # | 事項 | 決定 |
|---|---|---|
| **G1** | 正式模式要不要保留 OOS | **不保留**——部署時用全樣本重新擬合是標準做法，OOS 換位置變成三種模式並存：正式（IS=全樣本、無OOS）／驗證（凍結窗次、有OOS，P1一致性測試＋C5門檻來源）／探索（任意區間、強制未驗證標記）|
| **G2** | anchored 矩陣（`walkforward_matrix_detail.csv`）留不留 | **留，角色降級**——不再是答案來源，改當校準基準＋驗證邊界標示的裁判，依據見 §7.3。<br>🔴 **2026-09-10 第二輪審查更正「驗證邊界」的比對層級**：原本寫「設定若對得上凍結表某格就標 ✅已驗證，對不上標 ⚠️未驗證」，**這個規則在 G1 的設計下會讓正式模式永遠顯示⚠️**——凍結表每一格的 `is_end` 後面都必須留一段 OOS，所以**沒有任何一格的 `is_end` 等於最新可用月**；正式模式的 `is_end` ＝最新可用月且無 OOS，**結構上永遠對不上任何一格**。使用者最常用的模式被標成「未驗證」，會直接跟 IPS §1「已用 H-26/H-27 驗證過」打架。<br>**改成三態**（不是二選一——只比對方法論參數的話，探索模式隨便挑區間也會被標✅，那個標籤就失去意義）：<br>　`✅ 方法已驗證`：方法論參數（ratio/allocation/k_mode/group）在驗證集合內 **＋** anchored **＋** 錨點 2007-01 **＋** IS 到最新可用月 ⇒ 正式模式的標準狀態<br>　`🔶 方法已驗證，設定為外插`：方法論參數一致，但 IS 區間或錨點偏離驗證範圍（含 §7.3 的 204→228 外插）<br>　`⚠️ 未驗證`：方法論參數本身就不在驗證過的集合裡（例如手動指定 k）<br>三態的文案都要能點開看到「憑什麼這樣標」的依據 |
| **G3** | 正式模式 IS 區間 | **2007-01（H-26錨點）~ 最新可用月**，全樣本吃到底，不特意留窗當「上線前體檢」樣本外。<br>🔴 **2026-09-10 methodology-review 更正理由**：原本寫的理由之一是「C5 已提供校準功能」，**這是錯的**——`calibration.py:76-77` 讀的是 `holdings.performance["oos_cagr"]`／`["oos_mdd"]`，正式模式沒有 OOS，這兩個 key 根本不存在，直接 `KeyError`；`calibration.py:14-19` 的 docstring 自己也已註明「本版是一次性比對，不是真正的累積監控⋯真正的累積邏輯留給 live 模式做」，而那套邏輯至今沒寫。<br>**結論不變但理由更換為**：①§7.3 實測 IS 長度對 OOS 無系統性影響，留窗是純粹丟掉資訊 ②留 12 個月的窗產生不了統計效力（單一期間、n=1），做不成「體檢」。<br>⚠️ **連帶把「C5 累積監控」列為 P2 的必要工作**（見 §8-R3），不能繼續掛著當已完成。<br><br>🔴 **2026-09-10 第二輪審查再次更正理由（見 §8-R9）**：上面理由①也不成立——§7.3 那個「IS 長度無系統性影響」的分析是無效的（用 `min_is_months` 當分組欄位，但 anchored 下 IS 長度被 `oos_start` 完全決定，四個欄位其實是同一個窗）。**決定仍然不變，但理由第三次更換為**：<br>　①**H-26 已證實 anchored 優於 rolling**（OOS CAGR 22.44% vs 20.95%），而「限制 IS 長度」在 anchored 錨點固定的前提下就是變回 rolling——`pitfalls.md` §5 明文「不要再提案改 rolling」<br>　②**E vs R 在 OOS 對齊下的三個可比窗，長 IS 贏兩個**（§7.3 表），這是矩陣裡唯一有效的 IS 長度比較，雖然 n=3 證據力弱，但方向上不支持縮短<br>　③留 12 個月的窗產生不了統計效力（單一期間、n=1），做不成「體檢」<br>⚠️ **同時必須誠實標註**：正式模式 IS ＝228 個月，實測過的最長 IS ＝204 個月，**外插約 12%**，且外插區唯一樣本（方案 E 第 4 窗）OOS CAGR 僅 1.18%（與最差期間共線，無法歸因）。已寫進 IPS §5 |
| **G4** | P1（即時引擎）驗收標準 | 成員清單對凍結表任一格**必須100%相同**，數值容差 1e-8；清單有任一檔不同視為寫錯，不接受「差不多」。<br>🔴 **2026-09-10 措辭更正**：原本寫「視為**演算法**寫錯」——若 `app/clustering.py` 直接重用 `stage3_hrp._build_tree`／`walkforward_matrix._pick_a`（**建議這樣做，重用優於重寫**），這個測試證明的是「**接線正確**」（參數傳對、日期切對、排序一致），**不是「演算法正確」**。仍然有價值（本專案最危險的錯誤正是這種靜默對錯位的錯，見 `walkforward_matrix.py:435` 的 linkage 葉節點數斷言），但不可對外宣稱成「驗證了演算法」|
| **G5** | 分期順序 | 🔴 **2026-09-10 兩度修正**。<br>~~原：P1→P2 正式→P3 探索→P4 AI~~<br>**現行**：<br>　**P0** 三態標籤定案（R10）＋確認 §7.4b 的凍結樹可直接使用<br>　**P1** 在既有 `TW_normal` 樹上跑代表挑選 ＋ IS-only 評估路徑 ＋ 一致性驗收（G4）<br>　**P2** 正式模式（**固定 IS，不給日期選擇器**）＋ 驗證模式（凍結窗次下拉選單）＋ R3/R4/R8②/R12/R13<br>　**P3** AI 升級／樹狀圖視覺化／新面孔 vs 老面孔（原 P4 提前）<br>　~~**探索模式（任意 IS 區間）：建議不做**~~——理由見下<br><br>**為什麼砍掉探索模式**（2026-09-10 使用者提出空間疑慮後重新評估）：①**它是唯一需要每次點擊都重跑 `silhouette_scan` 的功能**——驗證模式的 k 在 `k_stability.csv`、正式模式的 k 就是 H-03 的 6（§7.4b），只有任意區間要現算 ②風險最高（開放未驗證空間、邀請 p-hacking、需配套三態標籤才敢放）③9/20 要開始寫論文，它對論文故事線沒有貢獻。<br>⚠️ **砍掉它不會讓系統退回「像回測系統」**——核心修正是 engine 從查表變成即時計算、且算出凍結表裡沒有的東西（228 個月樹的成員清單 ＋ 今天的股票持股），跟日期能不能自由選無關。<br>💾 **附帶澄清：磁碟空間不是考量因素**——實測 `_frozen/stage3/` 六棵樹全部產物僅 **2.5 MB**（`cluster_assign.parquet` 295 KB），一棵樹的 linkage 約 214 KB。真正吃資源的是建樹當下的記憶體（6,679² × 8 bytes ≈ **340 MB**，用完釋放，不落地）|
| **G7** | 三態驗證標籤的最終定義（P0 定案，2026-09-10）| 原本規劃正式模式標 `🔶 方法已驗證，設定為外插`，**改為 `✅ 方法已驗證`**——因為 §7.4b 查證後確認正式模式用的就是 `_frozen/stage3/TW_normal`，那是研究部**驗證最充分**的一棵樹（H-03 群數客觀化／ENB／群身份／M 系列全部建立在它上面），把它標成「外插」會嚴重低估其可信度、也跟 IPS §1 打架。<br>**最終三態**：<br>　`✅ 方法已驗證`：方法論參數（ratio/allocation/group）在驗證集合內 ＋ 用凍結主線樹或凍結窗次樹 ⇒ **正式模式與驗證模式都在這一態**<br>　`🔶 方法已驗證，設定為外插`：方法論參數一致但樹建在非驗證窗上（保留給未來擴充，目前無功能會產生此態）<br>　`⚠️ 未驗證`：方法論參數本身不在驗證集合內（例如手動指定 k）<br>⚠️ **但 `✅` 旁邊必須常駐一句結構性限制**：「本模式的**組合績效**無 OOS 可驗證——IS 用掉全部資料，依定義沒有樣本外可留（§7.4b）。已驗證的是方法，不是這一期的績效。」|
| **G6** | A2「live 模式暫緩」是否解除 | **快時鐘部分解除**——不需要重建樹（慢時鐘那半仍待 P1/P2）也能做到「正式模式選股到今天」，因為策略清單頻率是2年一次，用 2025-12 前資料建的樹在 2026-09 使用屬於 B2 週期內的正常狀態，不是將就；持股解析本來就已經是即時的（§6.3⑤）|

**下一步（尚未開始）**：依 §7.5 分期，從 P1 開始——抽出 `app/clustering.py`
承接 `research/stage3_hrp.py` 的建樹邏輯（同演算法、換成任意日期區間/子集輸入），
`engine.py` 新增即時計算路徑但**不刪除現有查表路徑**，先跑 5~10 格凍結表設定
做一致性驗收，通過才進 P2。⚠️ 開工前先處理 §8 的 🔴 項目。

---

## 8. 方法論審視結果（2026-09-10，`methodology-review` skill）

> **範圍**：`code/app/*.py` 全部＋§7 新設計。逐項實跑查證，非憑印象。
> 查證對象：`research/walkforward_matrix.py:340-560`、`ops/tools.py:t9`、
> `research/hrp.py:silhouette_scan`、實跑 `strategy_marks.parquet`（15,810 筆）／
> `walkforward_matrix_detail.csv`（8,370 格）／DB 財報與股價期別。
> 分級依 `pitfalls.md`：🔴 會讓結論不成立｜🟡 會被挑戰但可辯護｜🟢 已確認無誤。

### 8.1 🔴 會讓結論不成立

| # | 問題 | 證據 | 影響與處理 |
|---|---|---|---|
| **R1** | **候選池有全樣本前視偏誤，而應用層用的全是絕對數字** | 實跑 `strategy_marks.parquet`：`passes_alpha_gate` **TW 7,128/7,128＝100%、US 8,682/8,682＝100%**（`BENCHMARK_CAGR` TW 8.4256%／US 11.0556%）。v10 §3.2 原文：「這關早在 Phase 4 的候選池篩選就已經做過，研究部拿到的母體本來就全部贏大盤」 | 候選池是用**2000-2025 全期間** CAGR ≥ 基準篩出來的 ⇒ walk-forward 在 IS 挑代表時，母體已經知道 OOS 之後的結果；IS 好但 OOS 崩掉、全期間輸基準的策略**一開始就不在池裡**，而那正是會拉低 OOS 的一批。**方向明確：OOS 絕對數字系統性高估。**<br>✅ **相對結論不受影響**（共同座標軸成立，A/C_random/D/E 都從同一個池抽）——H-27 的 13/13 顯著、M-03 勝率比較都還站得住，邏輯與老師 9-2 對倖存者偏誤的裁示相同（「對所有人都一樣⋯所以這是公平的」）。<br>❌ **絕對數字站不住**，而應用層偏偏全用絕對值：`calibration.py:68` 的 p10 門檻、UI 顯示的 `oos_cagr`／`oos_mdd`、§7 正式模式「待在驗證過空間內」的立論水準。<br>**處理**：①v10 §8 已補一條限制（2026-09-10）②IPS §5 已補（同日）③UI/memo 要明講「歷史 OOS 不可當預期報酬」④**10/1 主動向老師報告** |
| **R2** | §7.2 步驟⑥ 的 HRP 權重會跳出驗證空間 | `walkforward_matrix.py:406` `_portfolio_series`＝`mean(axis=0)`；`_evaluate()` docstring「**等權**組合績效」 | ✅ **已於本次更正**，見 §7.2 的紅字段落 |
| **R3** | C5 在正式模式下無法運作，卻被 G3 拿來當理由 | `calibration.py:76-77` 讀 `performance["oos_cagr"]`／`["oos_mdd"]`，正式模式無 OOS ⇒ `KeyError`；docstring `:14-19` 自承累積邏輯未實作 | ✅ **G3 理由已更正**（見 §7.5）。**C5 累積監控列入 P2 必做**：改成從 `audit_log.jsonl` 逐季加總進行中窗次的實際表現，並在 B3 提早觸發時重置起點 |
| **R4** | 稽核紀錄不記股票層級持股，也不記資料版本 | `audit.py:105-125` 的 `entry` 只有 `members`（策略 uid）、`window_info`、`performance`、`risk` | ①**`resolve_strategy_holdings.py` 算出的 613 檔實際股票從未寫進稽核紀錄**——對一個宣稱做 fund house 治理的系統，「那天實際持有什麼」是最該留痕的一筆 ②**沒有輸入資料版本**（凍結檔雜湊、DB 快照日期）。現在讀凍結檔還沒出事，但**快時鐘讀的是每天在變的資料庫**，同一份 RunConfig 隔週跑結果會不同而紀錄無法區分，違反 `pitfalls.md` §8 可重現性與 DD-08 精神。`audit.py:4-6`「不用完整 DD-08 機制」的理由在接上即時 DB 後不再成立。**列入 P2** |

### 8.1b 🔴 第二輪外部審查新增（2026-09-10 同日）

| # | 問題 | 證據 | 影響與處理 |
|---|---|---|---|
| **R9** | **§7.3 的「IS 長度對 OOS 無系統性影響」是無效分析——這是本文件自己的錯誤** | 該分析用 `min_is_months` 分組，但它是**第一窗**的長度；anchored 的 `is_start` 恆為 2007-01，⇒ 給定 `oos_start`，`n_is_months` 就被完全決定。實測用真正的 `n_is_months` 分桶並控制 `oos_start`，**每一列只有一個桶有值**（見 §7.3 的表）。當初被當成證據的「`oos_start=2023-01` 四種 IS 長度算出完全相同的 34.6%」，實際上只是因為**它們本來就是同一個窗** | **外部審查原本只指出「這是外插不是內插」，查證後發現問題更嚴重**：不是外插，是那個分析根本測不到 IS 長度。<br>⚠️ 順帶更正審查者的兩個數字：實測過的最長 IS 是 **204 個月**（方案 E 第 4 窗）不是 144（`min_is_months` 只是首窗長度）；正式模式是 **228 個月**（2007-01~2025-12）不是 236（`returns_monthly` 只到 2025-12）。⇒ **外插幅度是 12%，不是 60%**。<br>**處理**：✅ §7.3 已改寫（撤回並留痕）、✅ G3 理由第三次更換（改用 anchored>rolling ＋ E vs R 三窗證據）、✅ IPS §5 已補外插限制。**G3 的決定本身不變** |
| **R10** | G2 的「驗證邊界標示」規則會讓正式模式**永遠**顯示未驗證 | 凍結表每一格的 `is_end` 後面都要留一段 OOS ⇒ 沒有任何一格的 `is_end` 等於最新可用月；正式模式 `is_end`＝最新可用月且無 OOS ⇒ 結構上永遠對不上 | 使用者最常用的模式被標「⚠️未驗證」，直接跟 IPS §1「已用 H-26/H-27 驗證過」打架。✅ **G2 已改成三態**（`✅方法已驗證`／`🔶方法已驗證，設定為外插`／`⚠️未驗證`）。**P1 前必須定案，否則實作會跟預期不一致** |

### 8.1c 🔴 第三輪（開工前 review，2026-09-10）

| # | 問題 | 證據 | 影響與處理 |
|---|---|---|---|
| **R13** | **正式模式沒有 OOS，只剩 IS 數字可顯示——而 IS CAGR 膨脹 1.48 倍** | 實跑 900 格台股 A_hrp：`is_cagr` 中位數 **23.60%** vs `oos_cagr` **15.98%**（差 **7.62pp**，1.48 倍）；legacy 180 格為 26.34% vs 17.65%（差 8.69pp）。逐格 **74.2%** 的格子 `is_cagr > oos_cagr` | ⚠️ **不是「IS 全面比較好看」**：IS MDD（−42.17%）跟 IS Sharpe（1.060）反而**比 OOS 更差**（−14.55%／1.128），因為 IS 涵蓋 2008 海嘯而所有 OOS 窗都從 2013 之後開始。<br>⇒ **正式模式的 IS 數字與凍結表的 OOS 數字在任何方向上都不可比，並排顯示會直接誤導**。而且這是**疊在 R1 之上的第二層樂觀**（R1 說 OOS 本身已被候選池前視偏誤高估）。<br>**處理（列入 P2）**：①**不**把本次 IS CAGR 當預期報酬顯示；要顯示就標「樣本內配適值，歷史上平均比實際 OOS 高 7.6pp」②主顯示改為凍結表同類設定的 **OOS 分布**（中位數 15.98%／p10 10.54%），標明「歷史參照，非本期預測」③IS MDD 可顯示且相對誠實（涵蓋 2008，屬壓力情境）|
| **R14** | `_evaluate()` 需要 `wide_oos`，正式模式沒有 ⇒ P1 必須另寫 IS-only 評估路徑 | `walkforward_matrix.py:404` 簽名為 `_evaluate(members, wide_is, wide_oos, cluster_map)`，一次算 IS+OOS 兩組指標 | 不能直接重用。✅ **已完成**（2026-09-10，P1）：`app/clustering.py` 的 `evaluate_is_only()`，只算 IS 指標＋集中度診斷，docstring 內建 R13 的警告 |

### 8.2 🟡 會被挑戰但可辯護

| # | 問題 | 證據 | 建議 |
|---|---|---|---|
| **R5** | **C2 風控永遠不會觸發，且量的不是老師問的東西** | `risk.py:70,79`：`equal_weight = 1.0/n`，`if equal_weight > cfg.single_stock_cap`。`1/n > 0.08` 需 `n < 12.5`，但 legacy 實測 25~60 檔、其他比例 67~668 檔 | ⇒ 任何正常設定下都不可能觸發，且算的是**單一策略**權重。老師 9-8 講的台積電集中度是**股票層級**問題。`resolve_strategy_holdings.py` 已能算出股票層級持股，**但 `risk.py` 完全沒有消費它**——資料已有、風控沒接。<br>✅ **已完成**（2026-09-11，P3）：新增 `risk.assess_stock_level()`。🔴 **實測結果推翻了原本的問題設定，見 §8.6** |
| **R6** | `find_previous()` 用執行時間排序，不是窗次時間 | `audit.py:64` `max(matches, key=lambda e: e["recorded_at"])`，`_IDENTITY_KEYS` 刻意排除 `scheme`/`window_no` | 先跑 window 6 再跑 window 5，會回報「本期新增10檔／剔除30檔」——實際是**拿未來的窗比過去的窗**（§6.3① 的驗證紀錄正是這個順序）。memo 的 `change_note` 會把它敘述成本期異動，在治理文件裡是誤導。**建議**：diff 時檢查窗次時序，逆序明確標示「回溯比較，非本期異動」 |
| **R7** | 探索模式的 `k` 選不出來 | `walkforward_matrix.py:427-433`：`k_is = k_table.get((tree_key, is_start, is_end))`，查不到直接 `raise KeyError`。`_load_k_table` docstring 說明刻意讀凍結檔因為 `silhouette_scan` 很慢 | 使用者自選的任意 IS 區間**不在 `k_stability.csv` 裡**，必須現場跑 `silhouette_scan`（台股 7,128 檔 ⇒ 7,128² 距離矩陣 × 約 9 個 k）。§7.2 我寫的「30 秒~2 分鐘」**沒有實測依據**。**P1 前必須先量實際耗時**，再決定探索模式可不可行 |
| **R8** | 流程層面四個沒設計到的環節 | — | ①**C5 示警之後要做什麼沒定義**——IPS 有門檻沒有行動，fund house 的 IPS 一定會寫「觸發後幾天內檢討、可採取哪些行動」②**沒有基準對照**——UI/memo 只有絕對 CAGR/MDD，沒跟 `contracts.BENCHMARK_CAGR` 或大盤比，而 v10 §8 已知「台股沒贏過市值加權大盤」，不含基準的投資備忘錄不成立 ③**沒有換手成本**——diff 顯示新增/剔除幾檔但無成本模型，M-08 量過策略層每月再平衡拖累（TW 0.18pp），**快時鐘每季換股成本從未量過**。<br>🔴 **2026-09-10 實測後升級嚴重度**：慢時鐘的換手率遠比預期高。方案 A／legacy／等量／A_hrp 逐窗實測——窗1→2 留用 19/55（**換掉 65%**）、窗2→3 留用 21/35、窗3→4 留用 18/35、窗4→5 留用 21/30、窗5→6 留用 20/30，**Jaccard 0.268~0.538（平均約 0.39，與 M-10 報的 0.372 一致 ✓）**。⇒ **每 2 年的慢時鐘檢視平均換掉 30~65% 的策略**，而換策略等於底下股票全盤更動，再疊加快時鐘每季換股。**這使「換手成本未模型化」從『有空再做』升級為『論文 limitations 必須明確量化』**——2 年換掉一半持股，交易成本不是小數 ④**沒有流動性/容量檢查**——613 檔裡有小型股，B3 規模集中度是研究部做的，應用層沒檢查解析出的持股能否以規模買到 |
| **R11** | `_IDENTITY_KEYS` 沒有 `mode`，live 模式做出來那天會立刻踩雷 | `audit.py:39` `_IDENTITY_KEYS = ("market", "group", "ratio", "allocation", "k_mode")`——沒有 `mode` | live 模式開始寫紀錄後，`find_previous()` 會把一筆 **replay 展示紀錄**當成 live 執行的「上一期」拿去 diff，算出的「新增/剔除幾檔」是拿正式運作結果比一個歷史展示案例。跟 R6 同類型、只是觸發條件（live 存在）還沒成立。**建議跟 R6 一起改**（兩者都在 `find_previous`／`diff_holdings`，改一次比較省），列入 P2 |
| **R12** | `alternative_note` 呈現「這期選錯了」時，memo 沒有把讀者拉回聚合結論 | 實跑 900 格台股逐格對照：**A_hrp 在 Calmar 贏 E_top_calmar 只有 16.8%**、OOS CAGR 13.2%（vs D_top_cagr 則是 27.0%／23.1%）⇒ 單一窗次「看起來選錯」是**常態**，§6.3③ 舉的實例（E 14.84% 略贏 A 14.24%）就是一例 | memo 規則正確禁止 LLM 建議換方法，但沒給定錨句，非技術背景的讀者只看這一期會直覺問「那為什麼不換」。<br>🔴 **但外部審查建議的措辭「這在多年回測的變異範圍內屬於正常」是錯的、不可採用**——M-03 已證明這**不是隨機變異，是系統性結果**，那樣寫等於暗示「再等等就會回來」，是誤導。<br>**正確做法**：在 `facts` 加一個程式產生的固定欄位 `alternative_context` 讓 LLM 照抄，內容為「A_hrp 在報酬類指標上輸給 E_top_calmar 是**已知的系統性結果**，非本期特例——900 格台股歷史對照中 A_hrp 僅 16.8%（Calmar）／13.2%（OOS CAGR）勝出。選用 A_hrp 的理由不是報酬優勢（M-03 已證實 HRP 分群不提供報酬優勢），而是分散度與跨市場回撤控制。是否更換方法屬政策層決定，不在單期備忘錄權責內。」<br>⚠️ 引用**聚合列（900 格）**的數字，不用逐比例列——依 M-16 的證據分層，前者是驗證性、後者是探索性。列入 P2 |

### 8.3 🟢 查過確認無誤（列出來是為了讓下次不用重查）

- **k 的前視偏誤**：`k_mode="silhouette_is"` 只用該窗 IS 選 k，且是**重切同一棵 linkage**
  （`walkforward_matrix.py:425` `hrp.cut_clusters(tree["link"], k_is)`，不重建樹），未用到 OOS。
  正式模式沿用此設定不會新增前視。
- **凍結樹 vs 每窗重建樹**：`risk.py:8-14` 正確避開 T8 的 `cluster_coverage`，改讀
  `holdings.cluster_info`，docstring 也已警告 live 模式同樣適用。踩過的雷處理正確。
- **anchored 錨點**：`walkforward_matrix.py:115` `ANCHOR_START = {"TW": "2007-01", "US": "2002-01", "XM": "2007-01"}`，
  §7.3 對 2007-01 的更正正確。
- **OOS 資料完整性**：`walkforward_matrix.py:456-462` 有硬檢查，缺值直接 raise，
  不會靜默少平均（`mean(skipna=True)` 陷阱已被擋住）。
- **MDD=0 除零**：`calibration.py:65-66`（門檻端 `.where(mdd_abs != 0)`）與 `:78-82`
  （檢查端 `float("inf")`）兩處都處理過。
- **D/E 組公平性**：`walkforward_matrix.py:543` `n_eff = len(a_members)`，D/E 用 A 的
  **實際**檔數對齊，共同座標軸成立。
- **快時鐘資料截斷鏈**：見 §7.4，2026Q2 不完整資料被 `fcv_core.py:196` 自動切掉，
  橫斷面乾淨。

### 8.4 處理順序

| 優先 | 事項 | 理由 |
|---|---|---|
| ✅ **已完成（文件）** | R1 揭露（v10 §8＋IPS §5）、R2（撤 HRP 權重）、R9（撤回 §7.3 無效分析＋G3 理由三改）、R10（G2 三態標籤） | 錯誤留著會直接指導錯誤實作；R1 愈晚講愈難講 |
| ✅ **已完成（P0/P1，2026-09-10）** | R7 判定失效（正式模式 k 就是 H-03 的 6）、G7 三態定案、**R14 IS-only 評估路徑**、G4 一致性驗收 14/14 | 見 §2 分期表 |
| ✅ **已完成（P2，2026-09-10）** | **R3**（校準 `tracking_started`）、**R4**（稽核記股票持股＋資料版本）、**R6**（窗次逆序警示）、**R8②**（基準對照）、**R11**（`_IDENTITY_KEYS` 加 `mode`）、**R12**（定錨句）、**R13**（績效呈現規則）| 見 §2.1 改動與驗證表 |
| **10/1 會議** | R1 主動向老師報告 | 符合本專案 M 系列主動推翻自己故事線的慣例，且老師對倖存者偏誤已有可沿用的裁示邏輯 |
| ✅ **已完成（P3 首項，2026-09-11）** | **R5 股票層級集中度風控** | 老師 9-8 台積電問題的正解。**實測結果推翻了問題設定**，見 §8.6 |
| ✅ **已完成（P3，2026-09-11）** | **新面孔 vs 老面孔** | 見 §8.7，含逐字稿原話查證 |
| ✅ **已完成（S3，2026-09-11）** | **解釋 agent 本體**（10 欄位，`app/explain.py`＋`app/scenario.py`） | 只在 `mode="live"` 啟用，見 §9.9 |
| ⬜ **P3 其餘** | 樹狀圖視覺化 | — |
| **標為已知限制** | R8③ 換手成本（慢時鐘每 2 年換掉 30~65%）、R8④ 流動性容量、R8① C5 示警後的行動未定義 | 論文時程內做不完，誠實揭露（已寫進 IPS §5）|

### 8.6 🔴 R5 實測結果：集中度不是問題，**低配台積電才是**（2026-09-11）

> 這一項本來只是「補一個沒接上的風控檢查」，但實跑出來的數字**推翻了原本的
> 問題設定**——老師 9-8 擔心的是「會不會過度集中在台積電」，實際答案是相反的。

**實作**：`risk.assess_stock_level()`（`app/risk.py`）。權重定義

```
w(股票 s) = Σ_{持有 s 的策略 i} (1 / N策略) × (1 / n_i)      n_i＝策略 i 當下持有幾檔
```

即「策略等權、且每個策略內部對自己的持股等權」——跟研究鏈 `_portfolio_series`
的每月拉回等權是同一套慣例，不是這裡自己發明的。⇒ Σ_s w(s) = 1。
✅ 數學已用手算對照驗證（4 檔合成案例，誤差 < 1e-12，總和恰為 1.0）。

**實測（正式模式，台股，legacy＋等量，as of 2026-07-24）**：

| 項目 | 數字 |
|---|---|
| 策略數 → 不重複股票數 | 30 檔策略 → **516 檔股票**（1,185 筆策略-股票對應）|
| **最大單一股票權重** | **1.76%**（2850 新產，被 12/30 個策略選中）|
| C2 上限 | 8.00% ⇒ **違規 0 筆** |
| 前 10 大合計 | 10.87% |
| 前 50 大合計 | 34.50% |
| 🔴 **台積電（2330）** | **0.085%**，只被 **2/30** 個策略選中 |

**兩個結論**：

1. **集中度風險不存在，方向剛好相反**——516 檔股票、最大權重 1.76%，這是極度分散的
   組合。老師 9-8 的疑慮（過度集中在單一權值股）在這個組合上沒有發生。
2. 🔴 **真正的問題是對台積電的極端低配**：本組合 **0.085%** vs 台積電在台股指數約
   **40.23%**（v10 §8 M-17）⇒ **相差約 473 倍**。
   這**直接解釋了 M-17 的結論**——「台股 A_hrp 19.55% 輸給市值加權報酬指數 20.91%，
   但贏過等權市場 15.02%」。以前只知道「加權方式造成差異」，現在有了**當期實際持股
   的證據**：不是抽象的加權效應，是這個組合確實幾乎不持有那檔主宰指數的股票。

⚠️ **這不代表策略錯了**——等權分散本來就是設計意圖（H-10 多樣性規則、C2/C3 上限都是
為了避免集中）。但它把 M-17 那條限制從「統計現象」變成「可以指著持股清單講清楚的
機制」，**是論文第四章（機制解釋）可以直接用的素材**，也是 10/1 向老師報告
台積電問題時最好的答案：**不是我們沒防集中度，是我們防過頭到幾乎不碰台積電**。

⚠️ **舊的 `assess()` C2 判定保留不動**——它仍是「策略數異常少」的安全網，
跟股票層級檢查是不同層級，不是誰取代誰。

🔴 **2026-09-11 code review 抓到並修正一個潛伏 bug**：`assess_stock_level()` 原本的
分母 `n_strat` 用 `stock_detail.groupby("strategy_uid").size()`，即 stock_detail 裡
「實際出現」的策略數——但若某策略在 `as_of` 當天 F1/F2/C/V 條件交集剛好篩出 0 檔
股票（`resolve_holdings()` 對此不保證非空），該策略根本不會出現在 stock_detail 裡，
分母會悄悄縮小，使其餘股票的權重被系統性放大，且**不會報錯**。上面 8.6 的實測數字
（30 檔策略、n_strat=30）沒有踩到這個邊界，數字本身沒有錯，但這是設計上的潛伏缺陷。
✅ 已修正：分母固定用 `holdings.n_members`（投組真正的策略成員數），並新增
`n_empty_strategies` 欄位揭露「查無持股」的策略數（UI 會在非零時提示），
修正後 Σweight 可能 < 100%（視同該份額持有現金），這是正確語意，不是新 bug。

| 意見 | 查證結果 |
|---|---|
| 「AL-08（策略的各種調整）的推論二已部分實作，應在文件註明」 | ❌ **編號查無此項**——`grep "AL-0[5-9]\|AL-1[0-9]" 文件/*.md` 完全沒有結果，本專案文件只有 **AL-01~AL-04**（2026-09-09 第二輪外部審查項目）。**不採用該編號**——寫進去會製造新的死連結。但**實質觀察採納**：`alternative_groups` 確實帶有 what-if 對照的精神，已補寫在 §6.3③。<br>🟢 **2026-09-11 更正**：那三個方向（風控觸發後建議調整／互動式 what-if 試算／人工覆核剔除）**不是憑空冒出來的**——`.venv` python + zipfile 直接查證 `逐字稿 (1).docx` 原文找到對應段落：「然後再來是一些策略的各種調準」，老師確實提過這件事，只是這份逐字稿裡沒有給編號。之前判定「查無此項」是對的（編號不存在），但「這件事不存在」是錯的——只是還沒補查原始逐字稿 |
| 「A_hrp 對 E_top_calmar 的 Calmar 勝率 19.6%」 | ❌ **數字對不上**——實跑 900 格台股逐格對照得到 **16.8%**（Calmar）／**13.2%**（OOS CAGR）；legacy 180 格則是 23.9%／25.6%。19.6% 不對應任何一組。已改用查證後的數字（見 R12）|
| 「memo 定錨句可寫『這在多年回測的變異範圍內屬於正常，不構成調整依據』」 | ❌ **措辭不可採用**——M-03 已證明 A_hrp 在報酬上落後是**系統性結果不是隨機變異**，那樣寫等於暗示「再等等就會回來」，是誤導且違反抗幻覺鐵則的精神。已改寫（見 R12）|
| 「正式模式 IS 約 236 個月、驗證上限 144 個月、超出 60%」 | ❌ **兩個數字都要更正**——正式模式 **228** 個月（`returns_monthly` 只到 2025-12，不是到今天）、實測過最長 **204** 個月（`min_is_months` 只是首窗長度，anchored 的 IS 逐窗成長）⇒ 外插 **12%** 而非 60%。<br>✅ 但查證過程發現**比審查意見更嚴重的問題**——那個分析本身無效，見 R9 |

### 8.7 🟢 新面孔 vs 老面孔（2026-09-11，P3）

> **起因**：使用者要求繼續 P3，選擇先做「新面孔 vs 老面孔」。動工前先查證這件事
> 是不是真的老師要求的，不憑上一輪對話的摘要（那可能是我自己的意譯）。

**查證**：`.venv` python + `zipfile` 解 `逐字稿 (1).docx` 的 `word/document.xml`
（同 CLAUDE.md §7 的既有讀法），找到老師 9-8 原話：

> 「我到底應該有哪些策略，哪些因質很可能是現在這些股票最火的，還是說⋯
> 以前都還不錯都蠻賺錢的，同時有取到他有沒有這一類的東西」

老師問的是：選出來的股票，是「現在最火」的，還是「一直都在」的常客——系統
有沒有辦法呈現這個區別。這是「新面孔 vs 老面孔」的真正出處，不是我方杜撰。

**設計**：策略清單在慢時鐘週期內（B2，2 年）固定不變，股票持股隨快時鐘
（B1，季度財報）更新。⇒ 拿「策略選定當下（`is_end`）」與「現在」兩個時間點
的股票持股做差集：

```
老面孔（persistent）：兩個時點都持有 —— 對應「以前都還不錯都蠻賺錢」
新面孔（new）      ：只在現在持有   —— 對應「現在這些股票最火的」
淡出（exited）     ：只在之前持有
```

**實作**：新檔案 `app/new_faces.py`，核心函式 `compare_faces()`。**不重新回測**，
跟 `resolve_strategy_holdings.py` 同一套機制，只是對同一批策略呼叫
`resolve_holdings()` 兩次、換一個 `as_of`。

**效能查證**（實跑，非假設）：`get_mask()` 用 `(field, name)` 當 key 跨 spec
快取整個布林矩陣，換日期只是重新切一天，不用重算遮罩。實測 30 檔策略：
第一個日期 **41.1s**（冷快取），第二個日期 **2.9s**（快取命中，快 14 倍）——
整組比較的成本約等於單一日期解析＋一點點，不是兩倍。

**實測結果**（正式模式，台股，legacy＋等量，2025-12→2026-07-24）：

| 項目 | 數字 |
|---|---|
| 策略選定當下（2025-12-01）持股 | 575 檔 |
| 現在（2026-07-24）持股 | 516 檔 |
| **老面孔** | 214 檔（佔現在持股 **41.5%**）|
| **新面孔** | 302 檔（佔現在持股 **58.5%**）|
| 淡出 | 361 檔 |

**解讀**：跨越 2025Q4／2026Q1 兩次財報更新（約 7 個月），58.5% 的換手率是合理
量級——季度財報驅動的選股條件本來就會讓相當比例的成分股換手，這不是異常，
是快時鐘設計的預期行為。⚠️ UI 已標註這個提醒，避免使用者誤讀成策略不穩定。

**驗證**：`AppTest` 端到端跑通（執行→解析持股→比較新舊面孔），數字與獨立
腳本測試完全一致（214/302/361）；`research.tests` 129/131（與 P3 前相同，
非回歸）；用一檔老面孔股票驗證邏輯（兩個時點都真的有策略選中它）。

---

## 9. 解釋 Agent 重新設計（2026-09-11，設計定案，**尚未實作**）

> **起因**：使用者指出「現在這個 AI 太死板，只是統整一點用處都沒有」，要的是能
> **獨立分析判斷、解釋為什麼這樣操作**的 agent，並指出**研究部這兩週做的策略互補性、
> 分群、群特性、什麼時候賺賠，應用層完全沒用到，產出的大量圖表也沒利用**。
>
> 🔴 **先更正我方（Claude）先前的兩個錯誤**：
> ① 曾把「memo.py 現在只做轉譯」講成「這是使用者設下的原則、明確排斥獨立判斷的 AI」
> ——**使用者從未這樣要求過**，那是我方實作時的選擇，被誤述成對方的立場。
> ② 曾提案用 S-01~S-05 的總經基礎設施做「宏觀評論員 agent」——**老師 2026-09-02
> 已裁示方向 C（總經）不做**，CLAUDE.md 自己就寫著，卻仍提案復活它。

### 9.1 老師 9-8 逐字稿原話（實際解出 `逐字稿 (1).docx` 查證，非轉述）

用 `.venv` python + `zipfile` 解 `word/document.xml`（CLAUDE.md §7 的既有讀法）。
STT 有錯字，括號內為解讀：

| # | 原話 | 要求 |
|---|---|---|
| ① | 「我想看到底⋯**每個車略(策略)目前最 OK 的策略分出來是多少次**⋯**這些該怎麼樣用 AI 解釋解釋這一塊**」 | **每檔股票被幾個策略選中（跨策略重疊度），並指名要 AI 解釋** |
| ② | 「**你不能說我壓六個車別(策略)⋯就因我車(策略)裏都都買了幾電關臺電(台積電)**⋯意思就是說 **hose(fund house)他有他那個風控的另外一個眼鏡(角度)**」 | **股票層級集中度是 fund house 風控的另一個角度** |
| ③ | 「這個明明就不錯，然後**這些股票居然最慘**⋯**過去是不是有發生過?我們過去會怎麼樣?就這一類的情景**」 | **情境比對要做到股票層級**，不只群層級 |
| ④ | 「我到底應該有哪些策略、**哪些因子**?很可能是**現在這些股票最火**的，還是說⋯**以前都還不錯、都蠻賺錢**的」 | **「追火的 vs 一直賺的」要從因子性格回答** |
| ⑤ | 「因為是各種情境的策略的選擇、一個是解釋，然後再來是一些**策略的各種調準(調整)**」 | 三件事：情境選擇／解釋／策略調整（**調整這件事本輪不做**，見 9.6）|

⇒ **「持股名單要不要放進解釋」的答案是「要」，而且是老師直接點名的**，不是我方自行加碼。

### 9.2 資源盤點：`_frozen/stage3/` 16 個產物，應用層只用了 2 個

| 產物 | 內容（對應投影片） | 改動前 |
|---|---|---|
| `cluster_assign` / `cluster_meta` | 策略→群、群內相關、代表策略 | ✅ 已用（僅供建樹挑代表）|
| **`cluster_identity`** | 群身份名稱（「P_IC純化群」這類）| ❌ 沒用 |
| **`cluster_profile_quant`** | 定量特徵表（主力F1／C來源／v1佔比／CAGR中位／MDD中位／小型股／賺錢年數）＝投影片 2.2/2.3/2.4 | ❌ 沒用 |
| **`cluster_corr_matrix_×6`** | 群間相關矩陣 **normal + crisis 各三份** ＝投影片 1.2 | ❌ 沒用 |
| **`cluster_annual_returns`** | 各群逐年報酬＝投影片熱力圖 | ❌ 沒用 |
| **`cluster_quarterly_returns`** | 逐季 | ❌ 沒用 |
| **`co_fail_regimes`** | 危機期共跌 | ❌ 沒用 |
| **`cluster_story`** | 群間互補解釋 | ❌ 沒用 |
| `cluster_macro_*` ×2 | 總經介面 | ⛔ 方向 C 已終止，不得使用 |

另有 `_analysis_outputs_atlas/` 第四章 18 張圖鑑，應用層一張都沒碰。

### 9.3 🔴 框架更正：從「分散是假的」改成「為什麼這樣挑會賺錢」

初版設計把診斷寫成「這個投組的分散是假的」。**使用者要求改成正面說明為什麼可行、
可以賺錢——而查證後發現這不只是語氣問題，原本的因果就講錯了**：

「ENB 只有 3.27、39 對配對 0 高互補」**不是這套方法的失敗，是這套方法「發現」的事實**
（v10 列為核心發現），描述的是**台股策略母體的客觀性質**。而投影片 4.2 的
`C_random 17.56%±0.88% ≈ B_all 17.59%` 更證明：在同質母體裡「亂挑 30 支」等於「全買」
⇒ **挑選規則才是唯一能創造價值的地方，而實測證明它創造了價值。**

**三個賺錢機制（都有數字，全部正面）**：

**機制一｜保障稀有但真正不同的群不被淹沒**（超配倍數依市場現算，不可寫死）

| 市場 | 最稀有群 | 佔母體 | 隨機抽的期望數 | legacy 保障 | 超配倍數 |
|---|---|---|---|---|---|
| 台股 | 群5 P_IC純化（75檔）| 1.12% | 30×1.12% ＝ 0.34 支 | 5 支 | **約 15 倍** |
| 美股 | 群6（225檔）| 2.69% | 35×2.69% ＝ 0.94 支 | 5 支 | 約 5 倍 |
| 跨市場 | 群2（1,897檔）| 12.6% | 15×12.6% ＝ 1.89 支 | 5 支 | 約 2.6 倍 |

台股群5 同時是六群中 **CAGR 中位最高（23.5%）、MDD 中位最淺（−49.3%）、19 年有 16 年
賺錢（84%，最高）**，且跟其他五群相關僅 0.54~0.67（台股唯一達中互補的群）。
⇒ **分群的作用不是「創造分散」，是「不讓最好又最稀有的那群被母體淹沒」。**

**機制二｜穩定性**：A_hrp 對 B_all 的 OOS CAGR 超額，**13 個窗口方案全部落在
+2.79pp ~ +3.86pp，沒有一個翻負**，勝率 85%~100%（投影片 9/07）。

**機制三｜回撤品質**：全市場 OOS MDD **A_hrp −17.44%**（最佳）vs B_all −18.63%
vs D_top_cagr −19.997%。且投影片 4.2 揭穿 D 組：**跨市場 D 組 15 檔 100% 落在同一群**
——「OOS 表現好是因為剛好押中 2019-2025 特別強的一群，是單一次歷史實現的運氣」。
⇒ **同樣賺錢，來源品質不同**：A 的報酬來自分散，D 的來自單押。

### 9.4 解釋 Agent 的七段輸出結構（使用者已認可）

```
【一】本期持有什麼
  1-1 策略層：群足跡 + 群身份 + 定量特徵（identity / profile_quant）
  1-2 股票層：幾檔、最大權重、跨策略重疊度        ← 老師要求①
【二】為什麼這樣挑會賺錢：三機制（超配倍數依市場現算）
【三】這批的性格：追火的還是一直賺的               ← 老師要求④
  ・因子面：主力 F1 是 MOM（動能）還是 P_IC/ROE/PB（品質估值）
  ・持股面：新舊面孔（new_faces.py，實測 214 老／302 新／361 淡出）
【四】風險與結構
  4-1 股票層集中度 + 台積電對照                   ← 老師要求②
  4-2 🔴 策略層相關高 ≠ 股票層集中（見下方說明）
  4-3 平時 vs 危機期相關（corr_matrix _normal vs _crisis，兩份都沒用過）
  4-4 多樣性機制是否生效（n_backfilled；legacy 1.8%/0.1%/0.0% vs 10% 時 45~61%）
  4-5 群結構重疊（台股群3/4/6 相關 0.96~0.98，「同一個東西的三個版本」）
【五】情境比對（三種都給，先看效果再挑）           ← 老師要求③
  ①群報酬型態最像哪一年（錨點：2008 全崩／2009 全噴／2011 唯一分歧年，
    群5 +60% 而其他 −17~−22%／2022 溫和回檔）
  ②同設定歷史窗次的 is_end → oos 實際結果（45 窗）
  ③regime 標籤（牛/熊/危機/盤整）
  ④股票層：這批股票在歷史類似時期的表現
【六】跟上次相比 + 候選方案對照（沿用 diff_from_previous / alternative_groups）
【七】限制（含 n=1 歷史實現、禁止外推）
```

🔴 **4-2 是本設計最有價值的一句，也是對老師疑慮的正面回答**：
群間相關 0.78，但實際持股 **516 檔、最大權重 1.76%、台積電僅 0.085%**。
M-01 已證明相關結構幾乎全由市場 beta 決定（策略 R² 中位 0.80~0.88）
⇒ **策略之間「像」，是因為都暴露在同一個市場，不是因為都買同一批股票。**
老師擔心的「六個策略都買台積電」，實測答案是**沒有，而且差很遠**。

### 9.5 決定事項

| # | 事項 | 決定 |
|---|---|---|
| **H1** | 診斷語氣 | **正面機制解釋**，不寫成「分散是假的」。誠實的部分講成**設計前提**（母體同質是市場客觀性質 → 所以挑選規則才是價值來源），不是缺陷 |
| **H2** | 市場範圍 | 🔴 **A1「先只做台股」作廢**——改為 **TW／US／XM 三個市場都開放**。理由：研究唯一證實的 HRP 貢獻是跨市場回撤控制，且 XM 樹 3 群完全按市場切開（投影片 2.4），**只有用 XM 樹才拿得到跨市場分散**（群1台股 對 群2/3美股＝0.536/0.548，全 39 對最低） |
| **H3** | 情境比對方法 | **三種（+股票層）全部先做**，看效果再挑 |
| **H4** | 輸出形式 | **8~10 個欄位的結構化 JSON**（每段一欄，風險段拆 2~3 欄），不用單一長文字——方便 UI 分區塊顯示與 D2 掃描 |
| **H5** | LLM 的角色 | **判讀基準由程式提供**（互補分級門檻、ENB、三機制數字、歷史錨點），**判讀本身由 LLM 做**。這跟抗幻覺鐵則不衝突：鐵則禁止的是「杜撰沒被餵的數字」，不是「不准形成判斷」 |
| **H6** | 成本 | 現行 memo 單次 ~3,736 tokens，新版估 **15~25k tokens/次**。D3 已算過每日 100 萬額度，季度執行一年 4 次，可忽略 |
| **H7** | 基準對照怎麼算（含 XM）| **採「等權大盤」**（`_analysis_outputs_robustness/weighting_decomposition.csv` 的 `equal_weight_cagr`）。<br>**理由**：我們的組合本身是**等權**（44 檔左右、單檔約 2.26%），拿等權比等權才是同類比較。M-17 已實測證明台股「等權 vs 市值加權」差 **4.88pp**，而那 4.88pp 幾乎解釋了 A_hrp 輸給市值加權指數的全部落差（台積電佔總市值 40.23%）。<br>**數字**（2019-01~2025-12）：**TW 15.02%／US 19.49%**。M-17 原文：「**A_hrp 相對『等權市場』是 +4.53pp（實測，非估計）**，只是輸給市值加權指數 1.36pp」。<br>⚠️ **三個限制必須一起揭露**：①只涵蓋 **2019-01~2025-12 單一窗**，不是逐窗——OOS 不落在此區間的窗次要標「期間不對應，僅供參考」②**沒有 XM**，且該檔只有 CAGR 沒有月序列，**不可線性平均兩個 CAGR**（CAGR 不能這樣合成）⇒ XM **並列 TW／US 兩個數字，不合成** ③美股 `mcap_null_share = 98.78%`（市值幾乎全缺）故 `cap_weight_cagr` 是 NaN，但**等權不需要市值，`equal_weight_cagr` 不受影響** |
| **H8** | 群檔案適用範圍 | 🔴 `cluster_identity`／`cluster_profile_quant`／`cluster_corr_matrix` **只對主線六棵樹存在**。walk-forward 每窗重建的樹 k 不同（實測 window1=11群、window6=10群），群 id 對不上，沒有對應的群檔案。<br>⇒ **群層級分析一律以主線樹為描述框架**（「這批策略在主線樹裡落在哪幾群」對任何投組都算得出來）。**正式模式**用的就是主線樹，完全吻合；**驗證模式**要明確標注「這是用主線樹的群體知識描述，不是選股當下那棵樹」。<br>⚠️ 這跟 `risk.py` docstring 記載的 T8 陷阱（凍結全域樹 vs 每窗重建樹，群佔比差 2.8~3.6 倍）**是同一類**，不可重蹈 |

### 9.6 明確不做（本輪範圍外）

- **老師要求⑤「策略的各種調整」**：使用者指示「先專心做解釋 agent 就好」，本輪不做
- **總經相關任何功能**：方向 C 已由老師終止（2026-09-02）
- **新聞分析**：`database.py` 只有 `daily_stock`／`finance_report`／`taiex`，**沒有新聞表**，要做得接外部 API，本輪不做

### 9.7 影響分析：開放三市場 + 解釋 agent 之後，還有哪些要跟著改

> 2026-09-11 逐項查程式碼確認，非推測。

#### 🔴 會擋住實作的

**I-1｜XM 在資料庫層不是真實市場，股票層級解析對 XM 直接壞掉** ✅ **已完成（S1，2026-09-11）**

```
database.py  MARKET_EXCHANGES 只有 TW/US → Database("XM") 會 raise ValueError
             BENCHMARK_TABLE / MARKET_REPORT_START 同樣只有 TW/US
```

但 XM 樹的成員是混合的（實跑 `cluster_assign`）：

| XM 群 | 檔數 | 市場組成 |
|---|---|---|
| 群1 | 6,679 | **100% TW** |
| 群2 | 1,897 | **100% US** |
| 群3 | 6,464 | **100% US** |

⇒ XM legacy 15 檔 ＝ 5 檔台股策略 ＋ 10 檔美股策略。實作了 `resolve_strategy_holdings.py`
的 `markets_needed(idx, uids)`（依 `candidate_index` 的 **`market` 欄位**分派，
不解析 uid 字串前綴——欄位比字串解析更不容易因命名規則變動而壞掉）與
`resolve_holdings_multi(md_map, idx, uid, as_of)`。改動的呼叫端：
`ui._resolve_stock_holdings()`（改吃 `md_map` 字典，回傳值從單一字串改成
`{市場: 實際對到的交易日}`）、`new_faces.compare_faces()`（同理，`current_date`／
`previous_date` 改成 `current_dates`／`previous_dates` 兩個 dict）。
`risk.assess_stock_level()` **不需要改**——權重公式 `w(s)=Σ(1/N策略)×(1/n_i)`
純以 uid／股票代號計數，跟市場/幣別無關。

**實跑驗證**（真實資料庫，XM legacy）：
- `markets_needed()` 正確判斷 TW→`['TW']`、US→`['US']`、XM→`['TW','US']`（5TW+10US）
- 兩個 `MarketData` 共載入 277s（TW 110s + US 167s）
- **519 筆策略-股票對應，394 檔不重複股票**，TW 策略只解出 TW 股票、US 策略只解出
  US 股票（分派正確性用集合斷言驗證）
- 股票層權重總和 ＝ 1.0000000000（精確）
- 🆕 **意外發現**：**US 股價資料實際到 2026-08-19，比 TW 的 2026-07-24 更新約 26 天**
  ——先前文件講的「股價只到 2026-07-24」其實只是 TW 的日期，US 資料庫本身已經
  更新。⇒ XM 投組會有**兩個不同的「今天」**（新機制已支援：回傳 dict 而非單一字串）。
  這對 §9.8 前瞻驗證是好消息：**美股腳的可衡量期間比原本估計的長**。
- `new_faces.compare_faces()` 混合市場比較耗時僅 2.9s（快取機制對兩個市場分別生效）
- TW／US 回歸（`AppTest`）：數字與 S1 之前完全一致（最大單一股票權重 1.76%、
  2850新產、老面孔214／新面孔302），`research.tests` 129/131（與前相同，非回歸）

⚠️ XM 股票權重會混 TWD 與 USD。權重公式是「每部位等額」故幣別不影響權重本身，但**報酬解讀有 v10 §8 已知的「無匯率調整」限制**，要標注。

**I-2｜XM 沒有基準** → 已由 **H7** 解決（並列 TW／US 等權大盤，不合成）。

#### 🟡 要跟著改的

| # | 項目 | 內容 |
|---|---|---|
| **I-3** | **A1 決定作廢 + UI 寫死市場** | ✅ **已完成（S2，2026-09-11）**：`market` 改成三選一 selectbox，正式模式的 IS 長度／k 說明依市場動態顯示（US 288月/k=7，不再誤植 TW 的 228月/k=6）。**順手抓到一個真 bug**：風控上限的 3 個 `number_input` 原本 `value=8.0/25.0/50.0` 寫死 TW 數字，選了 US／XM 也不會變——S0 訂出三市場門檻後這個 bug 才浮現。改成從 `config.py` 的 `DEFAULT_*_CAP` 表讀取，並用 `key=f"...{market}"` 讓 Streamlit 換市場時重新以正確預設值渲染。實測三市場 `AppTest`：TW 8/25/50、US 10/30/55、XM 14/40/60，全部正確且與 S0 一致 |
| **I-4** | **C2/C3 門檻是台股反推的，三市場要各自訂** | ✅ **已完成（S0，2026-09-11）**：用同一套方法（C2＝歷史最大值×2，C3＝歷史最大值＋緩衝取整數）從 900 格 `walkforward_matrix_detail.csv` 反推。**發現 US 比例分配歷史最大值（50.00%）剛好等於 TW 舊上限**——沿用會讓門檻對 US 形同虛設，證實三市場真的不能共用同一組數字。<br>**結果**（已寫入 `config.py` 的 `_DEFAULT_*_CAP`，依 `market` 動態帶入，`RunConfig.__post_init__` 解析 `None` 為市場預設值，C1 明確覆寫仍生效）：<br>　C2：TW 8%（不變，歷史最大4.00%）／**US 10%**（歷史最大5.00%）／**XM 14%**（歷史最大6.67%，legacy k=3 僅15檔）<br>　C3等量：TW 25%（不變，歷史最大22.31%）／**US 30%**（歷史最大**25.12%**）／**XM 40%**（歷史最大33.48%）<br>　C3比例：TW 50%（不變，歷史最大46.67%）／**US 55%**（歷史最大**50.00%**）／**XM 60%**（歷史最大53.33%）<br>已驗證：三市場正確帶入、JSON 存讀往返正確、C1 覆寫生效、TW 回歸不受影響、`smoke.py` 全流程無回歸。**R5 股票層 cap 尚未反推**，留待 S1 後處理 |
| **I-5** | **正式模式 IS 長度三市場不同** | `contracts.HRP_WINDOWS`：TW／XM ＝ 2007-01~2025-12（**228 月**），**US ＝ 2002-01~2025-12（288 月**）。§7.3 的 228、G3、IPS §5 的外插說明**只對 TW／XM 成立**；「實測過最長 204 個月」也是台股數字，美股要另外算 |
| **I-6** | **C5 門檻數字是台股的** | `calibration.get_thresholds()` **程式已 per-market 篩選，不用改**；但文件／IPS 寫死的「p10 ＝ 10.54%／0.679」是台股 legacy 等量的，要標注適用範圍 |
| **I-7** | **memo schema 6 → 8~10 欄** | `_MEMO_SCHEMA` required 要改；dry-run stub 從 required 衍生（**§6.3 修過的 bug 剛好保護了我們，自動適應**）；但 `ui.py` 的 `labels` dict 寫死 6 個 key **要跟著改**。D2 `scan_for_leakage` 要比對的數字會暴增（群檔案＋相關矩陣＋19 年報酬），`tol=0.06` 的誤報率要重新評估 |
| **I-8** | **audit 的 `input_versions` 要補** | 目前只記 4 個檔；新用到的 `cluster_identity`／`cluster_profile_quant`／`cluster_corr_matrix×6`／`cluster_annual_returns`／`cluster_quarterly_returns`／`co_fail_regimes`／`cluster_story`／`weighting_decomposition` 都要進去，否則違反剛補上的 R4 可重現性 |

#### 🟢 順手可以補的（不是本次改動造成，但相關）

**I-9｜app 層完全沒做 DD-08 凍結驗證**
`grep verify_inputs code/app/*.py` 只有 `audit.py` docstring 提到「沿用精神」。但研究部的 `decision_layer_arms.py`／`market_benchmark.py` 都有 `freeze.verify_inputs()`。既然要大量消費 stage3，`clustering.py` 讀 `cluster_assign` 時應該補上。

**I-10｜`_load_company_names()` 少了美股的 `_universe_clause`**
它只用 `_exchange_in_clause()`，但 `database.get_daily_stock()` 是兩個都用；CLAUDE.md 明文警告過這個雷。
⚠️ **實際影響有限，不是持股錯誤**：`names` 只當查表用（`names.get(sym, "")`），多幾筆無害；股票本身來自 `get_mask()`，`MarketData` 的價格宇宙已套過 universe 篩選。是「同一個雷在另一支程式沒防」的不一致，值得順手補。

#### 🔴 開工前 review 新增（2026-09-11，R15~R17）

| # | 問題 | 證據 | 影響 |
|---|---|---|---|
| **R15** | **群檔案是全樣本算的 → 驗證模式會有「敘事層前視偏誤」** | 實跑：`cluster_profile_quant` 的 `window_end_year` ＝ **2025**；`cluster_annual_returns` 的 year ＝ **2002~2025**；`cluster_identity` 是基於全樣本樹的 LLM 產出 | 拿「群5 CAGR 中位 23.5%、19 年 16 年賺錢」解釋一個 **IS 結束在 2016** 的歷史窗，那些統計**含 2017-2025 未來資訊**。這是 §0.4 汙染風險的**另一個來源**——§0.4 防的是 LLM 訓練資料，這次是**我們自己餵的群檔案就含未來**，**D2 數字掃描完全抓不到** |
| **R16** | **等權大盤只涵蓋 62.2% 的窗次** | 實跑 135 窗次（樹×方案×窗）：OOS 完全落在 2019-01~2025-12 者 **84（62.2%）**、部分重疊 15、**完全無重疊 36（26.7%）**。三市場各 28/45，完全一致 | 驗證模式**超過四分之一的窗次沒有基準可對照** |
| **R17** | **讓 LLM 做判讀 vs S-08 抗幻覺鐵則的張力（論文要有說法）** | `cluster_story.py` 鐵則原文「互補程度由程式判定，LLM 只為既定判決寫字」，而 S-08 是論文方法論貢獻 | **建議的界線＝區分兩種判斷**：**事實推論**（「群3 與群4 相關 0.979 ⇒ 實質是同一個東西」——可逐句核對，投影片自己就這樣寫）**✅ 可以**；**價值判決**（「所以這投組不好」「應該改用 E 組」——無程式準則可核對）**❌ 不行**。<br>⚠️ 誠實記錄：**D2 抓不到「錯誤的推論」**，只驗證數字有無出處。對策見 §9.8 四層標準的第二層（人工核對） |

**✅ 同時查過沒問題的**：XM 在 walkforward 的涵蓋與 TW／US **完全對稱**（三樹各 2,790 格、
13 方案、6 窗、4 組、6 比例）⇒ `engine`／`calibration`／`reference_oos` 的 per-market
邏輯對 XM 直接成立，不需特別處理。

**🆕 另一個順手抓到的正確性 bug（S2 三市場開放後才浮現）**：
`memo.py` 的 `_ALT_CONTEXT`（R12 定錨句）跟 `ui.py` 的 M-17 段落，原本都是**寫死
TW 數字的靜態字串**，三市場開放前沒問題（因為只有 TW），開放後若原樣沿用會把
TW 專屬事實講成通用事實。已修：
- **R12 定錨句**：新增 `engine.alt_group_win_rates(market)`，從 900 格
  `walkforward_matrix_detail.csv` **依市場現算** A_hrp 對 D/E 組的勝率。實測三市場
  數字差異很大（不能互相沿用）：TW 贏 E_top_calmar 在 Calmar/CAGR 為 16.8%／13.2%；
  **US 只有 8.0%／4.2%**；**XM 反而較高 34.0%／17.1%**。`memo.py` 的 `_ALT_CONTEXT`
  改成 `_alt_context(market)` 函式，`ui.py` 的對應 caption 同步。三市場定錨句
  已驗證互不相同、且 `build_prompt()` 正確帶入。
- **M-17 段落**（「A_hrp 相對市值加權大盤是輸的⋯」）：那組數字（19.55%／20.91%／
  40.23%）是 TW 專屬的深入個案研究，US／XM 沒有對應計算 ⇒ **gate 成只在
  `market=="TW"` 顯示**，不是每個市場都硬套或都拿掉。

**🎯 處置：解釋 agent 只在正式模式啟用，驗證模式保留現有簡版 memo。**
R15／R16／H8 三個問題**全部集中在驗證模式，正式模式一個都沒有**：

| 問題 | 正式模式 | 驗證模式 |
|---|---|---|
| 群檔案前視（R15）| ✅ IS 就是全樣本 | ❌ 含未來 |
| 基準涵蓋（R16）| ✅ 當近期參照即可 | ❌ 26.7% 窗次無基準 |
| 群 id 對不上（H8）| ✅ 就是主線樹 | ❌ 每窗 k 不同 |

🟢 **而 §9.8 的前瞻驗證剛好完全落在正式模式的乾淨區**：它用主線樹（群 id 對得上）、
無 OOS（不需逐窗基準）、且群檔案止於 2025-12 而執行時點在 2026+（**相對執行時點是真過去，
無前視**）⇒ **R15／R16／H8 在前瞻驗證裡全部不存在。**

⚠️ **但前瞻驗證需要一個目前沒有的小功能**：正式模式現在的股票解析日期只有
「IS 結束日」與「今天」兩個選項，**要能指定任意日期**（2026-01-01／03-31／05-15）。
這只影響**快時鐘的解析日期**，策略選擇仍固定用主線樹 ⇒ **不會重開「探索模式」那個口子**，
底層 `resolve_holdings(md, row, as_of)` 本來就支援任意日期，只是 UI 沒開。

#### ⏸️ 已知但本輪不處理

**I-11｜R8② 的「比錯窗」問題**（使用者 2026-09-11 指示先不處理）
M-17 docstring 第一句就批評：「**比錯窗**：19.06% 是 OOS 窗（2019-2025），8.43%/11.06% 是**全期**自建基準——製造出假的 10pp 差距」。
而 `engine.reference_oos_distribution()` 現在正是拿 `oos_cagr_median`（OOS 窗）對 `BENCHMARK_CAGR`（全期自建宇宙基準）⇒ **會顯示一個假的約 9pp 領先**。
**本輪處置**：不做 M-17 那套逐窗真實市場指數的完整改造，改用 **H7 的等權大盤**（TW 15.02%／US 19.49%）。
⚠️ 但要知道：`BENCHMARK_CAGR` 的正當用途是 `passes_alpha_gate`（**策略層級**的入池門檻），不是投組績效對照——兩者用途不同，不該混用。
📌 **完整修法備查**：`_analysis_outputs_robustness/market_benchmark_compare.csv` 已有逐窗真實市場報酬指數對照（含 XM），且有 DD-08 manifest。實測 A_hrp legacy 對真實市場指數：**TW +2.60pp／勝率 71.1%、US +13.01pp／勝率 100%、XM +11.32pp／勝率 100%**（⚠️ 美股回撤較市場深：−19.78% vs −15.70%）。日後要做正式的市場對照時直接接這份。

### 9.8 🆕 前瞻驗證設計（2026-09-11 使用者提出並定案，**尚未執行**）

> **這個設計解決了 §0.4 記錄過、原本只能消極迴避的死結。** §0.4 原文：
> 「`replay` 模式有一個 `live` 模式沒有的風險：LLM 訓練資料汙染⋯它有可能不是根據
> 餵給它的數字在推論，是直接背訓練資料裡記得的真實歷史。這種『偷看答案』被口委
> 抓到，是嚴重的方法論破綻。」當時的對策只有消極的 A4「避開太接近訓練截止日的窗」。
> **前瞻驗證直接繞過整個問題**：結果發生在訓練截止日之後，LLM 不可能背到。

#### 設計

**資訊隔離前提**：使用者 2026-09-11 上網查證確認 **GPT-5 訓練資料到 2024-09-30**。
✅ 2026-09-11 使用者確認：`config.ini` 實際使用 `gpt-5`（非 mini）。

**IS（建樹）**：`contracts.HRP_WINDOWS` 現成主線樹，**不重跑**
——TW／XM 2007-01~2025-12（228 月）、US 2002-01~2025-12（288 月）。
🔴 **不可用「2000 起算」**：`contracts.py:54` 明載「台股窗捨棄 2000–2006 是**排除已知
不可信資料**（財報洞、90% 假 0）」；投影片 slide 2 佐證 2007-01 起策略保留率 99.99%、
2002-01 只有 5.7%。

**三個執行時點**（🔴 **樹用到 2025-12 的資料，故任何時點必須 ≥ 2026-01，否則前視**）：

| 次 | 執行時點 | 當時生效財報 | 可衡量到 2026-07-24 |
|---|---|---|---|
| 1 | **2026-01-01** | 2025Q3（2025-11-15 生效）| 6.8 個月 |
| 2 | **2026-03-31** | 2025Q4（2026-03-31 生效）| 3.8 個月 |
| 3 | **2026-05-15**（✅ 2026-09-11 使用者確認採用建議，原規劃 2026-07-01）| 2026Q1（2026-05-15 生效）| **2.3 個月**（原 0.8）|

改第三次日期的兩個理由：①量測期 0.8→2.3 個月，在不更新股價的前提下是免費的改善
②**更符合 B1「換股由財報更新驅動」的定義**——生效日換股才是設計本意。
⚠️ 不可用 2026Q2：生效日 2026-08-31，且 DB 只有 637/1,739 家（36%）不完整，
又會被 `fcv_core.py:196` 的 `price_index.max()`（2026-07-24）截掉。

**市場範圍**：TW／US／XM 三者都跑（⇒ S1 XM 混合市場分派進入關鍵路徑）。
**股價**：使用者決定不更新（維持到 2026-07-24）⇒ 實際是「**2 次有績效 + 3 次有解釋**」。

#### 驗證什麼：四層標準（不可用「預測準不準」，因為我們明文禁止 LLM 預測）

| 層 | 指標 | 建議門檻 | 依據／成本 |
|---|---|---|---|
| **一** | **可歸因率**：每句事實陳述能否追溯到 facts 欄位 | **≥ 95%**，且不可歸因者不得是關鍵判斷 | 理論依據＝Rashkin et al.(2023) **AIS** 框架（郭鎧菘論文「判斷準則」元素引用的同一份）。⚠️ D2 現在只掃數字，**非數字陳述完全沒檢查**，這層要擴充 |
| **二** | 數字錯誤 | **0** | D2 自動 |
| **二** | 推論錯誤（方向講反、因果倒置）| **0 個嚴重錯** | ⚠️ **D2 抓不到**，人工核對（一份約 30~50 句）。誠實寫成「抽樣人工核對」，與 v10 §8 既有限制一致 |
| **三** | 重複穩定度 | 核心判斷 5 次一致；邊緣措辭可浮動 | **直接沿用 S-07 方法論**（同快照重複 5 次真實 API、算完全一致率與 Jaccard）。S-07 的型態可參考：`rule_in_llm_rate` 100%，不穩的只是邊際項目 |
| **四** | 風險提示事後檢驗 | 🔴 **不設通過門檻**，只做描述性報告 | n=3 樣本量不足以談命中率，硬訂門檻會被質疑 |

**第四層的做法（使用者定案：不限定提示清單）**：
agent 可自由提出任何風險，**但每條必須自帶「事後要看什麼數字才算應驗」的判準**。
範例：「台積電權重 0.085% vs 指數 40.23% ⇒ 若市場由權值股帶動本組合會落後。
**檢驗：比較該期間市值加權大盤 vs 等權大盤，前者顯著較高則應驗**」。
判準由 agent 在**不知道結果時**自訂 ⇒ 排除事後挑對自己有利的指標。
記錄完整 2×2（命中／誤報／**漏報**／正確略過）——**漏報最重要**：若當期最大風險完全
沒被提到，即使提的都中，解釋仍是失敗的。

#### 🔴 執行紀律（缺一則驗證不可信）

1. **facts 必須截斷到 as_of**。好消息：凍結產物都到 2025-12，三個時點都在 2026+，
   ⇒ **每項事實相對執行時點都是真過去**，不需複雜截斷。只需守住三處：
   ①選股日期（`get_mask()` 已處理法定期限＋as-of 語意）②新舊面孔第二次要跟第一次比、
   不是跟今天比 ③情境比對只能搜到 as_of 為止。
2. **績效衡量絕不可進 prompt**——它是事後裁判，不是輸入。
3. **prompt 模板在跑第一次前定稿並 commit，三次同一版**；跑完不因結果回頭改，
   改了要誠實記錄改什麼、為什麼。
4. **先凍結解釋、再算績效**：三次解釋跑完立刻寫 `audit_log.jsonl` → **git commit
   留雜湊與時間戳** → **然後才**寫績效衡量程式。三次都在今天跑，時間戳無法自證
   先後，靠版控補這個缺口（符合 DD-08 留痕精神）。

#### ⚠️ 誠實界線

| | 知不知道 2026 發生什麼 |
|---|---|
| GPT-5（截止 2024-09-30）| ❌ 不知道 ⇒ 隔離成立 |
| 輸入的 facts（凍結資料到 2025-12）| ❌ 不含 ⇒ 隔離成立 |
| **操作者（使用者本人）** | ✅ 知道 ⇒ **隔離不成立** |

⇒ 真正的風險是**無意識地調 prompt 直到結果好看**，唯一防法是上面的紀律 3。

🔴 **另一個因截止日提前而浮現的限制**：模型對 2024-10 之後一無所知，
⇒ **若 facts 組裝有 bug（數字算錯、欄位對錯），模型不會察覺也不會反駁，只會照錯的講**。
**facts 正確性沒有任何 LLM 端的安全網，只能靠程式端把關。**

**統計效力**：n=3 且三次持股高度重疊（季度換股，老面孔通常過半）
⇒ **這是「前瞻案例驗證」，不是統計驗證**。統計證據研究部已有（13/13 方案顯著、8,370 格）。
若三次都輸，那是誠實結果，**不推翻研究部的統計證據**（單一實現 vs 45 窗分布）。

#### 📌 施工順序建議

```
S0  三市場 C2/C3 門檻反推（I-4）                     ← 純計算，不寫功能
S1  XM 混合市場的股票解析分派（I-1）                  ← 最大工作量，三市場都要跑故必做
S2  ✅ UI 開三市場（I-3）+ 群檔案載入層（含 I-9 凍結驗證）——**已完成（2026-09-11）**
    └ 新增 `app/cluster_kb.py`：`build_footprint(market, members)` 組出投組的群足跡
      + identity/profile_quant/corr_matrix(normal+crisis)/annual_returns/co_fail/story，
      一律以主線樹為框架（H8）；載入時已補 `freeze.verify_inputs(paths.STAGE3)`（I-9）
    └ 實跑驗證三市場，且**跟投影片查證數字交叉核對一致**：XM 群1(TW)對群2/3(US)
      相關 0.536／0.548、群2對群3(同市場)0.938；TW 群5(P_IC純化)CAGR中位23.5%／
      MDD -49.3%——皆與 §9 投影片查證的數字吻合
    └ 順手改：正式模式的股票解析日期尚未開放指定任意日期（前瞻驗證仍需要，
      留到 S3 前處理，見 §9.7 R15~R17 結尾）

**✅ S2 收尾（S3 前處理，2026-09-11）：正式模式股票解析日期開放指定任意日期**
——新增 `st.date_input`（`min_value`＝IS 結束後第一天，防止選到樹沒看過的
資料以外的日期造成前視），選定後併入 `_date_options` 供「解析持股」使用。
⚠️ **不是重開探索模式的口子**：策略選擇仍固定用主線樹，動的只是快時鐘的解析
日期，`resolve_holdings(md, row, as_of)` 本來就支援任意日期，只是 UI 沒開。
實跑驗證：預設行為不受影響（仍預設選「今天」）、成功用 **2026-01-01**（§9.8
第一次前瞻驗證的執行時點）解析持股，`research.tests` 129/131（非回歸）。
S3  ✅ 解釋 agent 本體：資料組裝 + prompt + schema（I-7）——**已完成（2026-09-11）**
    └ 只在 mode="live" 啟用；replay 走現有 6 欄位 memo
S4  ✅ audit 補檔（I-8）、文件同步（I-5 I-6）、I-10 順手修——**已完成（2026-09-11）**
────────── 以上完成後才進 §9.8 前瞻驗證 ──────────
S5  ✅ 績效衡量模組——**已完成（2026-09-12）**：`app/performance.py`
S5b ✅ 層二/層三工具補齊——**已完成（2026-09-12）**：`explain.py` 接上 D2 數字
    洩漏掃描（直接複用 `memo.scan_for_leakage()`，通用函式不用重寫）；新增
    `app/stability.py`（S-07 精神：同一份 facts 重複真呼叫 N 次，比對每欄位
    引用數字集合的 Jaccard／exact-match，量化核心判斷穩不穩）
S6  ✅ 跑三次 × 三市場 = 9 組——**已完成（2026-09-12）**，見 §9.12
S7  四層驗證標準的實際評分（可歸因率標注、重複穩定度 5×N 次）——**待績效凍結後開始**
```

⚠️ **時程風險**：今天 2026-09-11，9/20 要開始寫論文 ⇒ 只剩 9 天，而 S1~S7 是完整
一輪開發。**S1（XM 分派）是關鍵路徑**，若卡住整條就會延。建議每完成一階段就
確認一次是否要縮減範圍（例如前瞻驗證先只跑台股、三市場留後）。

### 9.9 S3 完成記錄（2026-09-11）：解釋 agent 本體

**新增檔案**：
- `app/scenario.py`：情境比對四法（H3）——
  ① `weighted_annual_returns(footprint)`：本次持股在主線樹各群的佔比，套進
  `cluster_annual_returns` 回推「這個群權重結構歷史每年會是什麼報酬」。
  ③ `regime_snapshot(market, as_of_map, regime_avg_ret)`：現在 regime 標籤
  （`ops.tools.t11_get_current_regime`）＋這批策略歷史上在該 regime 的平均報酬
  （`risk.RiskReport.regime_avg_ret`，T8 早算好，不重算）。XM 分開給 TW／US
  兩份，不硬併成一個。
  ④ `stock_level_analog(md_map, symbols, year)`：現在持股裡在指定年份就已存在
  的股票，那一年的實際報酬——直接用已載入的 `MarketData.data.get("price:close")`
  價格面板算，不重查資料庫。年份**不是寫死的錨點年**，是從①算出的
  `weighted_annual_returns` 裡挑本次實際持股組成表現最好/最差的兩年，
  逐市場自動生效，不是複製投影片上 TW 專屬的 2008/2009/2011/2022。
- `app/explain.py`：解釋 agent本體。10 欄位 schema（H4）＋新系統提示——延續
  `memo.py` 的抗幻幻覺鐵則，但依 §9.7-R17 重新畫界線：**事實推論可以（把
  提供的數字放在一起講出關聯，任何人拿同樣數字都能覆核），價值判決不行
  （建議換方案這種無程式準則可覆核的判斷）**。`_call_llm()` 網路/錯誤處理/
  額度記帳包裝直接複用 `memo.py`（改成接受 `system_prompt`/`schema` 參數，
  預設值＝ `memo.py` 自己的版本，向後相容）。

**`engine.py` 新增兩個函式**（②③需要）：
- `historical_same_setting_windows(cfg)`：跟 `reference_oos_distribution()`
  用完全相同的篩選條件（market/group/ratio/allocation），給逐格明細（is_end→
  oos_cagr/oos_mdd/oos_sharpe）而不是分位數彙總。
- `mechanism_consistency(market)`：機制二/三——A_hrp 對 B_all 的 OOS CAGR
  超額分布（勝率、min/max/median）＋ A_hrp/B_all/D_top_cagr 的 OOS MDD 中位數
  排名，依市場現算，不寫死投影片上的 TW 數字。
  ⚠️ **實作時抓到一個真陷阱**：`B_all` 在凍結表裡沒有 ratio/allocation 概念
  （固定 `ratio="all"`／`allocation="unallocated"`，因為它是「全買」基準，
  不分配代表名額）——第一版用含 ratio/allocation 的完整 key 去對齊，直接撞
  成 0 列。改用 `(scheme, window_no, k_mode)` 三欄對齊後才對得起來。
  **實測三市場**（900 格）：TW 對 B_all 勝率 95%（CAGR 超額 −3.5%~+7.7%，
  中位 +2.0pp）；US 86%（−2.5%~+21.4%，中位 +3.6pp）；XM 92%（−3.7%~+32.8%，
  中位 +2.5pp）——⚠️ 這是**全部 900 格**的聚合（不分 ratio/allocation），
  比投影片講的「13 個窗口方案」母體更寬、更雜訊，數字不必也不應該對得起來，
  但**方向一致**（A_hrp 系統性打贏 B_all，不是本期特例）。

**實跑驗證**（真實資料庫，TW legacy，正式模式，`AppTest` 端對端）：
- 10 個欄位全部正確產生並顯示（dry-run）
- 機制一（稀有群超配倍數）現算結果：群1（TW 母體佔 4.57%）超配 3.6 倍、
  群2（3.02%）超配 5.5 倍、群3（母體最大 24.76%）反而只有 0.7 倍——跟
  §9.3 文件描述的「稀有群被保障、大群被稀釋」型態一致，但**是這次真正選中
  members 現算出來的**，不是抄投影片數字
- 情境比對①：2009 年加權報酬 +206%、2008 年 −37.9%，符合台股歷史認知
- 情境比對④：2009 年（①挑出的最佳年）本次前 20 大持股裡有 11 檔當時已存在，
  平均報酬 +91.4%，方向與①一致（互相佐證，非同一份資料重複算）
- 情境比對③：`t11_get_current_regime("TW", as_of="2025-12-31")` 回傳「牛」
  （自 2025-04-09 起，累計 +66.5%），對照這批策略歷史牛市平均報酬 +3.19%／
  月（`regime_avg_ret`，T8 已算好）
- `python -m research.tests`：129/131（跟 S0~S2 之後相同，非回歸）

**範圍限制（已知，非遺漏）**：`cli.py` 的 `--memo` 目前仍是舊版 `memo.py`
（策略層級即可，不需要股票層級解析＋MarketData），live 模式下也一樣——
完整 10 欄位解釋需要股票層持股（連 DB、載入 `MarketData`）＋群知識庫，
這兩件事目前只有 `ui.py` 走過，CLI 尚未接。§9.8 前瞻驗證的執行路徑本來就是
UI（`st.date_input` 也只在 UI 開放），不影響時程。

### 9.10 S4 完成記錄（2026-09-11）：audit 補檔、文件同步、I-10 順手修

**I-8（audit 補檔）**：`audit.py` 的 `_input_versions()` 新增 `_CLUSTER_KB_FILES`
常數，把 `cluster_kb.build_footprint()` 實際會讀到的 11 個檔案（identity／
profile_quant／annual_returns／co_fail_regimes／story／corr_matrix×6）都納入
版本追蹤——⚠️ **刻意不放** `cluster_quarterly_returns`（存在但目前沒有程式碼
真的讀它，放進去會讓稽核紀錄宣稱讀了其實沒讀的檔）跟 `weighting_decomposition.csv`
（H7 的數字是靜態寫進程式的已驗證事實，不是這次執行讀檔算出來的）。

同時把 `app/explain.py` 的完整輸出（`{"prompt","facts","explanation","dry_run"}`）
接進 `record()` 新參數 `explanation`，`ui.py` 的「寫入稽核紀錄」按鈕在 live 模式
會自動帶上（若已產生過）——這是為 §9.8「解釋要先進 audit_log.jsonl 並 commit，
才能算績效」鋪路，不是本輪臨時想到，是 §9.8 設計時就講好的執行紀律。
⚠️ 因為 `explanation["facts"]` 有些欄位直接來自 parquet（numpy 純量型別），
`json.dumps()` 寫檔那行也補了 `default=str`，跟 `explain.build_prompt()`／
`ui.py` 顯示時同一個處理方式。實跑驗證：`audit_log.jsonl` 最後一筆確認帶有
完整 10 欄位 `explanation`，`input_versions` 確認 15 個檔案版本都記到。

**I-5/I-6（文件同步）**：`系統投資政策聲明_IPS.md` 補齊 S0 已經做完但文件沒跟上
的缺口——
- §2 新增三市場 C2/C3/C5 完整門檻表（取代舊的「只有 TW 數字」表格）：
  C5 的 US／XM p10 現算驗證：US OOS CAGR p10＝11.15%／Calmar p10＝0.708，
  XM＝15.29%／0.921（TW 10.54%／0.679 不變，三者都用 `legacy`／`equal`）
- §5 補一條：228／204 個月只對 TW／XM 成立，`contracts.HRP_WINDOWS["US"]`
  是 **288 個月**（2002-01~2025-12），不能照抄台股數字
- 版本紀錄表新增一筆，說明這是把文件補齊到跟程式一致，政策數字本身
  S0 那天已經定案

**I-10（順手修）**：`resolve_strategy_holdings.py` 的 `_load_company_names()`
補上 `db._universe_clause('company_symbol')`，跟 `database.get_daily_stock()`／
`get_finance_report()` 用同一條規則。**實測確認這是有意義的修正**（不是純粹
補齊一致性）：US 修正前查到 **11,187** 家公司（`company` 表裡所有美股交易所
掛牌公司），修正後**篩到 2,992 家**（Russell 3000 宇宙）——TW 不受影響（1,775
家不變，因為 `_universe_clause` 對台股回傳空字串）。⚠️ 影響僅止於 `names`
查表顯示，不影響持股正確性（股票本身來自 `get_mask()`，已套過 universe 篩選），
但公司名稱查表現在也對齊了同一個宇宙，不會有极小機率的 ticker 撞名風險。

**實跑驗證**（真實資料庫，`AppTest` 端對端，TW legacy 正式模式）：解析持股→
產生解釋（dry-run）→寫入稽核紀錄，全部成功；`audit_log.jsonl` 最後一筆確認
帶有 `explanation` 與擴充後的 `input_versions`；`python -m research.tests`
129/131（跟 S0~S3 之後相同，非回歸）。

### 9.11 S5/S5b 完成記錄（2026-09-12）：績效衡量、D2 掃描、重複穩定度工具

**S5｜`app/performance.py`**：§9.8 的事後裁判模組，只能在解釋定案並 commit
**之後**呼叫，絕不進 `explain.py` 的 prompt。直接用已解析的股票層級持股權重
（`StockConcentration.weights`）套進 `as_of` 到 `end`（預設用該市場價格資料
最後一天）之間的**實際股價變動**算已實現簡單報酬——不能用
`returns_monthly.parquet`（凍結到 2025-12，涵蓋不到前瞻驗證期間）。基準是
同一段期間、同一個市場全宇宙的等權報酬，現算（不是 H7 那組固定 2019-2025
窗的數字）；XM 兩個市場分開列，不合成一個數字（跟 H7 立場一致）。
**實測**（TW legacy，2026-01-01→2026-07-24）：投組已實現報酬 **+18.97%**
vs 同期等權大盤 **+15.77%**（+3.2pp），575 檔全部量到價格，權重涵蓋 100%。

**S5b｜層二（D2）／層三（重複穩定度）工具補齊**：S3 完成 `explain.py` 時漏了
接 D2——`memo.scan_for_leakage()` 本來就是通用函式（吃任意 `{欄位:文字}` dict
跟 prompt 字串，不綁六欄位 schema），直接複用，`generate()` 在非 dry-run 時
自動跑，抓到就攔下（不寫進稽核紀錄）。層三新增 `app/stability.py`：沿用
`research/decision_repeatability.py`（S-07）的方法論精神——同一份輸入重複
呼叫真實 API N 次量化穩不穩定；但 S-07 測的是離散的「選中哪些群」集合，
這裡是十個自由文字欄位，沒有天然集合可比、逐字比對又太嚴格。改用跟 D2
同一套技術：抽出每個欄位**引用了哪些數字**，把「引用數字集合」當可比較的
離散物件算 Jaccard／exact-match——量的是「核心判斷穩不穩」的可查證代理
指標，不是唯一標準（純敘述性判斷的穩定度仍需人工核對，跟 D2 已知限制同一類）。
**驗證**：用假的 `_call_llm`（不花真的 API 錢）餵 5 組人工設計的數字集合
（3 組相同、1 組小數點誤差、1 組明顯離群值）進真實 facts 組裝管線，手算
驗證 `exact_match_rate=0.6`、`stable_core=[5.0]`、`mean_pairwise_jaccard=0.62`
跟程式輸出完全吻合。⚠️ 這個工具每呼叫一次就是 `n_repeats` 次真實 API 呼叫，
要花錢，呼叫前要先決定對 9 組裡的哪幾組跑，不必全部都跑 5 次。

⚠️ **這兩項是在使用者 2026-09-11 commit「prompt 樣板凍結」之後才加的**——
但都不影響送給 LLM 的 `_SYSTEM_PROMPT`／`_EXPLAIN_SCHEMA`／`build_prompt()`
本身（D2 是回應之後的驗證關卡，穩定度工具是另一個獨立模組），**沒有違反
§9.8 執行紀律③「prompt 模板跑第一次前定稿」**。但為了讓 git 歷史乾淨對應
「這次 9 組結果是哪個版本的程式碼跑出來的」（DD-08 留痕精神），建議在正式
跑 9 組之前，把這批新增（`explain.py` 的 D2 接線、`stability.py`、
`performance.py`）也 commit 一次。

### 9.12 S6 執行記錄（2026-09-12）：9 組正式前瞻驗證全部完成

**執行方式**：使用者授權「直接開跑，之後再 commit」——先跑再補 commit，
不影響結果可信度的原因同上（不改 prompt/schema 本身）。canary（TW/2026-01-01）
先單獨跑一次驗證管線，過程中**真的抓到兩個會讓 D2 誤攔正確解釋的真 bug**，
不是隨機的 LLM 失誤，修完才跑剩下 8 組：

1. **`mechanism_consistency()`／`historical_same_setting_windows()`／
   `scenario.stock_level_analog()`／`scenario.regime_snapshot()`／
   `co_fail_regimes` 的 `crisis_dest_share`／`engine.alt_group_win_rates()`
   共 6 處欄位餵給 LLM 時是未格式化的原始小數**（例如 `-0.14554961969523622`），
   違反系統提示鐵則 6「數字已格式化，直接照抄」的前提。實測後果：同一份
   未格式化資料，LLM 有時原樣照抄小數、有時「好心」自己換算成百分比字串
   （比對不上，被 D2 誤判成洩漏數字而攔下一份其實正確的解釋）——canary 第一次
   真呼叫就踩到，攔下 5 處「洩漏」，逐一比對 diagnostic 呼叫的原始輸出後
   確認全部源自這個格式缺口，不是內容錯誤。全部改用 `_pct()`／`_ratio()`
   統一格式化（新增 `_fmt_mechanism_consistency()`／`_fmt_historical_windows()`／
   `_fmt_stock_analog()`／`_fmt_regime()`／`_fmt_alt_context()` 五個小函式）。
2. **`memo._NUMBER_RE` 正則把「範圍/配對用的連字號」誤判成負號**——
   「2007-2025」「群3-4」這類文字會被切成一個從沒出現過的負數（如
   `-2025`），被 D2 誤判成洩漏而攔下正確解釋。加上 `(?<!\d)` 負向後顧：
   連字號前一個字元是數字就不當負號看待。這個正則是 `memo.py`／
   `explain.py`／`stability.py` 三處共用的基礎設施，一次修好三處都受益。
3. **順手修**：90 秒 API 逾時對 gpt-5＋10 欄位 schema（實測 completion
   token 常達 7~8k，其中 reasoning_tokens 近 4.8k）偏緊，跑 9 組時遇到
   兩次逾時中斷（皆為網路層 timeout，非內容問題），調寬到 180 秒。

**修復後驗證**：`research.tests` 129/131（無回歸）；canary 重跑 `leakage_check=[]`
乾淨通過，才繼續跑剩下 8 組。

**9 組結果**（全部 `leakage_check=[]`，已寫入 `audit_log.jsonl`）：

| 市場 | 2026-01-01 | 2026-03-31 | 2026-05-15 |
|---|---|---|---|
| TW | 575 檔（實際對到 2025-12-31）| 506 檔 | 516 檔 |
| US | 795 檔（實際對到 2026-01-01）| 784 檔 | 745 檔 |
| XM | 462 檔（TW 2025-12-31／US 2026-01-01）| 411 檔 | 373 檔 |

新舊面孔比較依 §9.8 執行紀律①的鏈式規則：每次都跟**上一次驗證時點**比
（第一次跟 `is_end`＝2025-12 比），不是每次都跟「今天」比——TW 第一次
老/新/淡出＝575/0/0（剛好等於 is_end，尚未換股），US 第一次已有
665/130/141（因為美股財報生效日跟 IS 結束點之間本來就有落差，見 §9.7
I-1 意外發現：US 股價資料實際更新到 2026-08-19，比 TW 新約 26 天）。

**內容品質**（抽查 TW/2026-01-01，完整內容見 `results_TW_2026-01-01.json`）：
機制一（稀有群超配）現算群5 超配 14.8 倍、群2 超配 5.5 倍；機制二/三
（對 B_all 的穩定性與回撤品質）正確引用格式化後的數字；股票層集中度與
策略層相關的區分（4-2 那句「本設計最有價值的一句」）正確做出；情境比對
四法數字全部到位且互相佐證（① 2009 年加權報酬 +206.01% 對應 ④ 個股實測
+91.40%）。單次呼叫 token 用量：約 31.6k prompt + 7.6k completion
（含 4.8k reasoning）＝ 39k total，比 H6 原估 15~25k 略高但仍在可負擔範圍。

⚠️ **尚未執行**：`performance.measure()`（S5 已就緒但按紀律④尚未呼叫）——
必須先由使用者 commit 這 9 組結果與本輪程式修正，才能進入算績效那一步。
