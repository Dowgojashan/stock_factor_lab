# 實戰監控 Agent 系統 — 開發追蹤

> 建立於 2026-09-16。**進度追蹤用**，不重複設計內容。
>
> | 文件 | 分工 |
> |---|---|
> | `實戰監控Agent系統_設計文件.md` | **規格**（怎麼做、為什麼這樣做）——唯一權威 |
> | **本文件** | **進度**（做到哪、還缺什麼、決定過什麼）|
> | `投組監控與調整_領域筆記.md` | 領域知識（SPC、歸因、監控節奏）|
> | `老師9-15會議重點_論文方向.md` | 需求來源 |
>
> ⚠️ 兩份衝突時**以設計文件為準**；本文件只記狀態與歷程。

---

## 0. 現況快照（一眼看完）

| 項目 | 狀態 |
|---|---|
| 設計文件 | ✅ 定案（**v20**，經 8 輪方法論審視 ＋ 5 次改版，最新一次是 2026-09-18 W2c 前置驗證通過並加入動作空間，見 D50）|
| **前置計算（§11.6，A1-A10）** | ✅ **全部完成**（2026-09-17）——A1 的 M2／M5／結果層歷史分布仍待補，但不阻斷 k／X／校準測試 |
| 參數 k | ✅ **已凍結 = 1.78**（2026-09-17，即時系統版錨＋扣真實交易成本，見 D24-D26；code review 抓到日期 bug 後由 1.63 修正，見 D31）|
| 參數 X | ✅ **已凍結 = 1.86pp**（2026-09-17，書面凍結，見 D27）|
| 動作空間 | ✅ **定案為 A0／A2／A4／A5／W2c**（W1／W3 前置驗證失敗棄用，見 D29；**W2c 條件式市值傾斜 2026-09-18 前置驗證通過**，連過 C2合規／交易成本／重分配規則／季中漂移四輪查證，淨改善 +23.48pp（校準期）／+21.98pp（前瞻驗證），見 D50。目前僅到決策記錄層級，執行邏輯未做）|
| §11.2b 校準測試 | ✅ **通過**（僅 M1-D，見 D30）|
| Agent 架構 | ✅ **2026-09-18 改為單一 Agent-A，Agent-B 正式移除**（設計文件 v18→v19，D44；程式碼側 D45）——`agents.py`／`simulate.py` 已同步刪除 Agent-B／4.5 修訂相關程式碼，dry_run＋真實單季呼叫皆驗證通過 |
| 程式實作 | ✅ **主線邏輯完成，架構已跟上設計文件 v19**（`agents.py`／`simulate.py` 已同步移除 Agent-B／4.5 修訂，見 D45）；`memory`／互動式工具集（tool-calling）／L3 對話介面未做，見 §3 |
| 實驗執行 | ✅ **正式 control0／L2 兩臂 × 8 季已跑完並驗證**（D51 執行層版本，真實 API）——16 筆 checkpoint 全部落盤，M1-D 2024Q4 首次觸發並持續到 2025Q4，L2 每個觸發季度皆選 A5。陰性對照 A/B（D57/D58）、陽性合成對照（D59）、多時間尺度敘事（D60/D61）、品質面 rubric（D55）皆已完成。**L3（人機對話）介面已完成**（D62，`app/ui.py` 新分頁＋`app/l3_dialogue.py`），可對任一真實 L2 季度開對話；§8 僅剩非阻斷性技術債（第9~11項）與跟老師報告（第0項）|
| 產業集中資料 | ✅ **資料層已備妥並查證**（D47/D49）——`industry_raw.csv/.parquet` 覆蓋率 TW/US 皆 100%，`code/app/industry.py` 完成 TWSE/TPEx 命名正規化。**診斷機制本身尚未接進 `diagnose.py`**，見 §8 |
| 已燒 token | 09-17～18 累計真實呼叫已有實質用量（單元測試＋3季試跑＋正式8季實驗），精確數字見 `utils.openai_quota`／`code/_catalog/llm_usage.jsonl` 帳本 |
| 🔴 **與老師方向的落差**（09-18 方法論審視發現，**尚未跟老師報告**）| 老師 9-15 具體驗收標準是「把 2025 喬到賺錢，用同一套策略」——已找到並驗證通過一個回應這個標準的機制（W2c，見上一列），但**這整個落差與轉折過程都還沒跟老師報告過**，見 §8 事項0 |
| 動作執行層 | ✅ **2026-09-18 完成**（D51）——`simulate.ActiveConfig` 讓 A2（切換 allocation，持續性）／W2c（條件觸發，見 D51 設計理由）真的改變後續季度的 `resolve_weights()`。dry_run 回歸＋權重比對測試皆通過 |

**2026-09-17 一天內完成**：k、X 凍結（D24-D27）、A7 逐季串接（D28）、A4 洩題防線、
W2/W3 驗證失敗棄用（D29）、A8／校準測試通過（D30），以及**整條程式主線**
（monitor→triggers→diagnose→facts_lean→agents→simulate）從零到真實驗證通過。
**2026-09-18 完成**：Agent-B 移除（D44/D45）、code review 抓到 3 個真實 bug 並修正
（D46/D48）、正式 8 季實驗跑完（D49）、資安檢查（DB／Streamlit 皆僅監聽
loopback，無網路曝露風險）、跟老師方向的落差審視（發現「調到賺錢」目前做
不到）、產業分類資料補齊並查證（D47/D49）、提出「M1-D 觸發時才啟動」的
條件式 W2 新方向（用 2024-2025 校準、2026 前瞻驗證，避開測試集調參數問題）。
**下一步排序見 §8。**

---

## 1. 待決參數

| # | 參數 | 用途 | 何時能訂 | 狀態 |
|---|---|---|---|---|
| P1 | **k** | 規模曝險風險容忍度（願意用幾倍方法優勢換等權分散）| 前置計算完成後 | ✅ **已凍結 k=1.78**（2026-09-17，見 D24-D26／D31）|
| P2 | **X pp** | M1-D 指數端集中度偏離門檻 | 同上 | ✅ **已凍結 X=1.86pp**（2026-09-17，見 D27）|

🔴 **訂定規則**（設計文件 §11.7，不可違反）：

1. 只能用 **2007-2023** 的資料訂
2. 訂完**寫入 audit_log 凍結**，主實驗開跑後不可改
3. 校準測試的 2015-2016 那半失敗 → 可回頭調；**2024-2025 那半失敗 → 只能記錄**

---

## 2. 前置計算進度（設計文件 §11.6）

**性質**：純程式、無 agent、**不燒 token**、無後見之明污染。

| # | 工作 | 產出 | 狀態 |
|---|---|---|---|
| A1 | 2007-Q1~2025-Q4 三層指標逐季序列（TW）| 乾淨分布、對照季度、M1-D 判準 | 🔶 **M1-D、M3 完成**（見下），M2／M5／結果層歷史分布未做 |
| A2 | 2024-Q1~2025-Q4 每季持股解析與已實現績效 | **對照 0 的基準線** | ✅ 完成（見下）|
| A3 | 每季等權／市值加權大盤 ＋ B_all 對照 | 歸因與 M7 判準 | ✅ 完成（等權＝市值加權＝B_all，A2/A3/A9 已算出）|
| A4 | 資料載入器：**物理過濾 window 4 的矩陣列** | 洩題防線（§7.7）| ✅ 完成（見下）|
| A5 | W 系列前置驗證（W2 的 α 格子最優先）**須排除 window 4** | W 系列可否啟用 | ✅ 完成——**結論：驗證失敗，W1~W3 棄用**（見下）|
| A6 | `memo._fmt_reference` 寫死的 40.23% 改逐季實算 | 修前視風險（§1.3）| ✅ 完成（見下）|
| A7 | 結構性下限改**逐季串接**（現為期初權重的單期近似）| §3.1 嚴謹化 | ✅ 完成（見下）|
| A8 | 補算 **2015-2016 的敞口與集中度分位** | §11.2b 校準測試的判準 | ✅ 完成（見下，校準測試通過）|
| A9 | 用**股票層方法**重算 B_all（與兩個大盤同一量測系統）| §7.4 拆解嚴謹化 | ✅ 完成（見下，B_all＝等權大盤，A2/A3 已算出）|
| A10 | **估計觸發次數**（8 季裡預計幾季會觸發）| 判斷實驗有無鑑別力 | ✅ 完成（見下）|

🔴 **已查證**：§10.10 的 2024-03~2025-12 季度持股解析**沒有留存**——`audit_log.jsonl`
裡沒有 scheme E window 4 的任何紀錄（查過全部 25 筆），那次是 AppTest 的即時輸出，
只留下文字描述（檔數、最大權重），**沒有存成資料檔**。⇒ **A2／A10 都要重新解析**，
已併入 `code/app/_prelim_a10_trigger_estimate.py` 一次做完（真實 DB 查詢，非估計）。

### A10 實測結果（2026-09-16，真實持股解析，非估計）

腳本：`code/app/_prelim_a10_trigger_estimate.py`；資料：
`_analysis_outputs_applayer/a10_trigger_estimate.csv`

TW／scheme E／window 4／A_hrp／legacy／equal（30 檔策略），登記時點 2023-12-31
的敞口 `q1_gap = −67.26%`（445 檔股票）。8 季相對登記時的偏離：

| 季 | 股票數 | 相對登記時偏離 |
|---|---|---|
| 2024-03-31 | 445 | +0.33pp |
| 2024-06-30 | 421 | −1.31pp |
| 2024-09-30 | 474 | +0.37pp |
| 2024-12-31 | 396 | +3.26pp |
| 2025-03-31 | 466 | −1.25pp |
| 2025-06-30 | 387 | −1.12pp |
| **2025-09-30** | **315** | **+4.96pp** |
| 2025-12-31 | 502 | −0.16pp |

平均絕對偏離 1.59pp、標準差 2.29pp、最大單季偏離 4.96pp。

🔴 **重要發現，推翻了 §7 M1-D1／D2 拆分的一個隱含假設**：先前用「全市場等權代理」
算出「投組端完全不變、漂移 100% 來自指數端」，但那不是真持股。**用這 30 檔策略
實際解析出來的持股重算，投組端自己就有真實雜訊**（換股造成，2025-09-30 因財報
更新股票數從約 450 掉到 315，單季擺動達 4.96pp）——**跟市場端兩年累計的結構性
漂移（+4.34pp，§3.2）量級相當**。

**⇒ 若 X 門檻設不夠高，系統會幾乎每季都因為正常換股誤觸發，而非真的偵測到
市場結構改變。** 用一個貼著雜訊上限的門檻（X≈5pp）估算：**8 季裡約只有 0~1 季
會觸發，且觸發原因主要是換股雜訊，不是結構性偏移**——證實了 §7①「實驗可能
沒有足夠決策點」的疑慮。

**後續**：投組端雜訊需要歷史分布才能正確設 X（不能只看一次實驗期的樣本），
已啟動 A1 延伸計算——結果見下方「M1-D 改版」。

### 🔴🔴 M1-D 改版（2026-09-16，使用者定案）

**A1 延伸計算結果**（`code/app/_prelim_a1_portfolio_noise.py`，windows 1-3，
2015-2023，n=36，全部在實驗期之外）：投組相對登記時點的敞口偏離，平均絕對
偏離 **3.30pp**、標準差 2.43pp、**p90=6.39pp**。**且全部 36 筆同一方向**
（非零中心的隨機雜訊，是持續漂移）。

🔴 **但反查 window 4（實驗期）發現矛盾**：平均絕對偏離只有 **1.59pp**——
**比歷史雜訊基準（3.30pp）還小**，且方向正負混雜（不像 windows 1-3 那樣
同方向）。若用歷史 p90（6.39pp）當門檻，**2024-2025 這組真正的異常期反而
幾乎不觸發**——問題不是門檻沒調好，是**投組端敞口偏離這個量測本身選錯了**
（主要反映換股雜訊，不是市場結構訊號）。

**使用者定案**：改用**指數端集中度**（不依賴投組持股，乾淨、單調，
即 §3.2 已驗證過的量測）。

**改版後結果**（`code/app/_prelim_a1_index_concentration.py`，2007-2023，
「8 季後累計變動」，n=60）：

| 統計量 | 數值 |
|---|---|
| p10 | −1.88pp |
| 中位數 | +0.58pp |
| **p90** | **+1.86pp** |
| 歷史最大值 | +2.86pp |

**反查 2023-12-31→2025-12-31 實際變動 +4.34pp ⇒ 落在歷史分布第 100 百分位、
超過歷史最大值**——無論 X 訂在 p90／p95／最大值都會正確觸發。逐季試算：
**2024Q4 首次觸發、2025Q1 短暫回落（維持觀察不算誤報）、2025Q2 起持續且加劇**
——乾淨、單調、有鑑別力，證實改版方向正確。

**設計文件同步更新**：§7.4 M1-D1／D2 拆分取消，統一為單一 M1-D
（`實戰監控Agent系統_設計文件.md` §7.4a，v16）。

#### ✅ X 值凍結（2026-09-17）

**凍結前查證**（比照 k 值踩過的偽獨立樣本問題）：`chg_8q` 是**逐季滾動**算的
（每季位移 1 季），相鄰樣本重疊 7/8，n=60 不是 60 個獨立樣本。拆成真正不重疊
的窗（每 8 季取一次，2007-2023 只夠切出 8 段）：

| | 滾動版（n=60，偽重複）| 不重疊版（n=8，真獨立）|
|---|---|---|
| 平均 | +0.27pp | +0.44pp |
| p90 | +1.86pp | +1.56pp |
| 最大值 | +2.86pp | **+1.65pp** |

n=8 的 p90 統計量本身不穩定（等於在 8 個點內插），不適合單獨當門檻依據。但
**觸發結論對這個統計細節穩健**：不管用滾動版（p90=1.86／max=2.86）或不重疊版
（max=1.65），實際 2023-12-31→2025-12-31 變動 **+4.34pp** 都遠遠超過——比
最保守的不重疊版最大值還高 2.6 倍。

**採用**：X = **1.86pp**（滾動版 p90，資料利用率較高，且觸發結論不受此選擇
影響）。訂定僅用 2007-2023 資料，符合 §11.7。

⚠️ **凍結的實際落地方式**：`code/app/monitor.py`／`triggers.py` 等程式尚未
實作（見 §3），目前沒有真正在跑的 `audit_log.jsonl` 登記機制可寫入——
「凍結」現階段是**書面凍結**（本文件 ＋ 設計文件 §3.4/§7.4a/§11.7），
待階段 1（期初登記）程式做出來後，X=1.86pp 這個值要原樣寫進當次執行的
`audit_log.jsonl`，不可在那之前重新調整。

### A2／A3 完成紀錄（2026-09-16）：對照 0 真實已實現績效基準線

腳本：`code/app/_prelim_a2a3_baseline.py`；資料：
`_analysis_outputs_applayer/a2_baseline_control0.csv`

**做法**：TW／scheme E／window 4／A_hrp／legacy／equal，策略清單全程不變
（對照 0 的定義），股票持股依快時鐘每季重解（財報驅動的正常換股，非調整）。
逐季用真實權重公式 `w=(1/n_strat)/len(syms)`（比照 `risk.assess_stock_level()`）
＋ `performance.measure()`（S5，正式模式已驗證過的模組）串接算真實已實現報酬，
對照同期真實 TAIEX 報酬指數與真實等權大盤（**三個數字同一套即時 DB 量測系統**，
不混用凍結矩陣）。

**結果**：2023-12-31→2025-12-31 複利串接 8 季：

| | 累計報酬 | 對投組超額 |
|---|---|---|
| **投組** | **+7.22%** | — |
| 等權大盤 | +10.46% | 投組落後 −3.24pp |
| 市值加權大盤（TAIEX）| +70.56% | 投組落後 **−63.34pp** |

**拆解**（同一套量測系統，比 §1.1 更乾淨）：
```
選股效果（投組 vs 等權大盤） = −3.24pp   ← 小
加權效果（等權 vs 市值加權） = −60.10pp  ← 主導
合計                         = −63.34pp  ✓ 對得上
```
方向與 §1.1 一致（加權效果主導），數值不同是因為這次用**同一套系統**三個數字
都重算，不是混用凍結矩陣與即時查詢的組合數字。

逐季明細（CSV）：8 季裡投組多數時候貼近或小贏等權大盤（2024Q3 +0.60pp、
2024Q4 +2.86pp、2025Q2 +0.23pp），但**對市值加權大盤幾乎每季都大輸**，
尤以 2025Q2~Q4（−10.56pp／−12.43pp／−12.47pp）最明顯。

#### 🔴 新發現：兩套量測系統的實際差距，這次第一次真的量到

同樣是「scheme E window 4」的兩年表現：

| 系統 | 兩年累計報酬 |
|---|---|
| 凍結策略層（`returns_monthly.parquet`，OOS CAGR 0.24%/yr）| 約 **+0.48%** |
| **即時股票層**（`performance.measure()`，本次真跑）| **+7.22%** |

**差距超過 15 倍。** 這正是設計文件 §5.3 已標注過的「兩套量測系統不可混比」，
但這是第一次真的量到差距有多大，值得記下來當論文材料（不是缺陷，是刻意
的方法論選擇差異，說清楚差距量級能讓讀者判斷這個警告不是隨口說說）。

**待查證**（列入未決事項）：差距最可能來自**重新平衡頻率**——凍結矩陣是
**月頻**重新評估持股，這次即時版只在**季頻**檢查點重新解析（持股在檢查點
之間不變）。尚未驗證這個假設，也可能有其他因素（股利處理、交易成本假設
差異等）。**不可在未查證前就寫進論文當定論**。

### A9 確認完成（2026-09-16）：B_all 即等權大盤，A2/A3 已一併算出

查證 `code/research/walkforward_matrix.py:62`：**B_all＝「全宇宙等權」**，
跟 `performance.measure()` 的 `equal_weight_benchmark_return`（`universe = 全部
有價股票, 等權`）是同一件事。**A2/A3 已算出的「等權大盤」同時就是 B_all 的
即時股票層真實績效，不用另外算。**

**順帶交叉驗證 D22**：

| | 兩年累計 |
|---|---|
| 凍結矩陣 B_all（OOS CAGR 1.39%/yr）| ~2.80% |
| 即時股票層 B_all（＝等權大盤）| **10.46%** |
| 差距倍數 | **3.7x** |

第二個獨立數據點支持 D22——不只 A_hrp 有落差（15x），B_all 也有（3.7x），
代表這是量測系統本身的系統性差異，不是 A_hrp 這個投組特有的巧合。

### M3（選股廣度）歷史分布（2026-09-16，免費算出）

用既有的 `a1_portfolio_noise_windows123.csv`（windows 1-3 持股解析時已經
記過 `n_stocks`，不用新跑）：

| | n_unique_stocks |
|---|---|
| **windows 1-3 歷史**（n=36）| p10=346／中位=476／p90=590／全距 287~644 |
| **window 4 實驗期**（n=9）| min=315／中位=445／max=502 |

**window 4 完全落在歷史正常範圍內，甚至沒碰到 p90**——跟 M1-D 的結論一致：
2024-2025 的異常完全是市場端集中度的事，投組自己的選股廣度沒有任何異常。
**M3 在這次實驗不會觸發**，這是誠實記錄，不是缺陷。

### ✅ k 錨點驗證與凍結（2026-09-16~17，已完成）

**動機**：§3.4 的 k 值錨用 M-17（凍結矩陣算出 A_hrp 相對等權市場 +4.53pp/yr）。
但上面已發現凍結矩陣 vs 即時股票層系統差距達 3.7~15 倍——**這個錨本身可能
需要用即時系統重新驗證，否則 k 會建立在不一致的基準上**。

**做法**：對 windows 1-3 比照 A2/A3 window4 的方法（真實權重 ＋
`performance.measure()` 串接）算出即時系統版的歷史超額，跟 M-17 對照。

腳本：`code/app/_prelim_k_anchor_check.py`；資料：
`_analysis_outputs_applayer/k_anchor_check.csv`

**結果（季頻重新平衡，跟 A2/A3 window4 同方法）**：

| window | 年化超額（vs 等權大盤）|
|---|---|
| 1（2015-2017）| +12.25%/yr |
| 2（2018-2020）| +10.12%/yr |
| 3（2021-2023）| +11.90%/yr |
| **平均** | **+11.42%/yr** |
| M-17（凍結矩陣，45 窗）| +4.53%/yr |

**即時系統算出來的優勢是凍結矩陣的 2.5 倍。** 若直接代入 §3.4 的 k 公式：
`k = 14.1 / 11.42 ≈ 1.23`（原為 3.1）——差很多，直接影響「現況要不要調整」的結論。

#### 機制驗證：是不是重新平衡頻率造成的假訊號？

假設：即時版用季頻重新平衡、凍結矩陣用月頻，頻率不同可能系統性放大超額報酬。

**驗證**：對 windows 1-3 重跑一次**月頻**版（`code/app/_prelim_k_anchor_monthly.py`，
`_analysis_outputs_applayer/k_anchor_monthly.csv`，108 個月度檢查點）：

| 版本 | windows 1-3 平均年化超額 |
|---|---|
| 月頻即時版 | **+12.81%/yr** |
| 季頻即時版 | +11.42%/yr |
| 凍結矩陣版（M-17）| +4.53%/yr |

**月頻版沒有收斂，反而比季頻版離凍結矩陣版更遠**（差距 8.28pp vs 6.89pp）。

🔴 **⇒ 重新平衡頻率假設被排除。**

#### 排除假說①：M-17 的「45 窗」定義釐清

`研究框架總覽_v10.md:786` 的 +4.53pp 其實是 **2019-2025 單一固定期間**的數字，
不是 45 窗平均——真正的 45 窗平均是另一個數字：**+2.60pp/yr（勝率 71.1%）**。
兩個數字都要納入比較，不能只用 +4.53pp。

#### 排除假說②：樣本數／範圍不同——用全部 45 窗重新驗證，結論不變

M-17 的「45 窗」實際定義：`walkforward_members.parquet` 裡 TW／A_hrp／
silhouette_is／legacy／equal 的全部 scheme(A-L,R)×window_no 組合，共 45 筆
（`grep` 驗證：6+4+3+5+4+3+4+3+2+3+2+2+4=45）。其中 **13 窗的 oos_end 落在
2024-01 之後，觸及實驗期間，依 §11.7 不可用來訂參數**，排除後剩 **32 窗**；
再排除 scheme 之間邊界完全相同造成的重複窗（不同 scheme 字母但
`is_end`/`oos_start`/`oos_end` 相同 ⇒ 成員與結果逐位元相同），**真正獨立的只有
17 窗**。

腳本：`code/app/_prelim_k_anchor_full45.py`，資料：
`_analysis_outputs_applayer/k_anchor_full45.csv`。

**結果（17 個獨立窗，季頻，毛報酬，未扣成本）**：

⚠️ **下表為初版數字，事後 code review 抓到日期解析 bug 已修正**（見本節最下方
「code review 更正」），修正後平均為 **+9.97%/yr**——數字略有調整但結論不變，
保留初版數字供對照歷程。

| 統計量 | 數值（初版，含 bug）|
|---|---|
| 平均年化超額 | +10.74%/yr（修正後 +9.97%/yr）|
| 標準差 | 2.08% |
| 最小值／最大值 | +6.50%／+13.96% |
| 勝率 | 100%（17/17，修正後仍 17/17）|
| t 檢定 H0=4.53% | t=12.3, p<0.0001 |
| t 檢定 H0=2.60% | t=16.1, p<0.0001 |

**⇒ 樣本數／範圍假設也被排除。** 3 窗版（+11.42%）跟 17 窗獨立版（修正後 +9.97%）
仍高度一致，不是抽樣運氣；17 窗裡最低的一窗都還贏過 M-17 兩個候選值。

#### 排除假說③、確認真正根因：交易成本不對稱 + 兩套系統定義不同

查 `backtest.py:118-123`（凍結矩陣用的舊回測引擎）：組合報酬扣了台股
**手續費 1.425/1000 + 證交稅 3/1000**；但 `market_benchmark.py` 算的比較基準
「全市場等權」是**純價格報酬，沒扣成本**——凍結矩陣版是拿「扣過成本的組合」
去比「沒扣成本的基準」，本身比較就不公平。即時系統版 `performance.measure()`
兩邊都沒扣成本，是公平比較。

**用真實持股序列驗證量級**（`code/app/_prelim_k_anchor_turnover_cost.py` 先在
3 窗驗證，`_prelim_k_anchor_full45_netcost.py` 擴大到全部 17 個獨立窗）：

⚠️ **下表同樣是初版（含日期 bug）數字**，修正後見本節最下方；3 窗欄不受影響
（手寫完整日期，本來就沒有這個 bug）。

| | 3 窗（不受影響）| 17 個獨立窗（初版，含 bug）| 17 個獨立窗（修正後）|
|---|---|---|---|
| 平均每季換手率 | 75% | 72~79%（各窗）| 68~79%（各窗，微調）|
| 毛超額（無成本）| +11.42%/yr | +10.74%/yr | +9.97%/yr |
| **淨超額（扣真實成本）** | **+9.32%/yr** | +8.65%/yr | **+7.90%/yr**（sd=1.86%，17/17 全贏）|
| 成本拖累 | 2.11pp/yr | 2.09pp/yr | 2.07pp/yr |
| t 檢定 H0=4.53%／H0=2.60%（修正後）| — | — | t=7.48／t=11.76，皆 p<0.0001 |

**交易成本不對稱是真實、可量化的貢獻（~2.1pp/yr），但只解釋落差的一小部分**
（扣完成本後修正版仍有 +7.90% vs M-17 的 +4.53%／+2.60%，還差近一倍）。

**剩下的落差判定為結構性、非 bug**：凍結矩陣的策略報酬＝每個策略自己歷史上
**真實的訊號驅動換股執行軌跡**；即時系統＝每個 checkpoint 重新解析持股快照後
**靜態持有**到下一個 checkpoint。D23 已排除「頻率不同」這個子假設（月頻沒有
收斂，反而更遠）——代表這不是頻率問題，是**兩套系統對「投組報酬」的定義本來
就不同**，呼應設計文件 §9 早就寫的「兩套量測系統不可混比」，現在有實測量級佐證。

**⇒ 原則性解法**：k 的分母必須跟**實際運作會用的系統**自洽——本系統跑
`performance.measure()`，錨也必須用即時系統版自己的歷史優勢，不可跨系統借用
M-17 的數字，不管 M-17 用哪個版本（+4.53% 或 +2.60%）都一樣不適用。

#### ✅ k 值凍結（初版，2026-09-17）

```
方法優勢（即時系統版、17 個獨立窗、扣真實交易成本）= +8.65pp/yr
規模曝險年度 p10 情境代價 = −14.1pp（不變，見 §3.4）
k = 14.1 / 8.65 ≈ 1.63
```

**k = 1.63**（原估計 3.1 已棄用），已寫入設計文件 §3.4／§11.7。

#### 🔴 code review 更正（2026-09-17 當天稍晚）：k 修正為 1.78

事後對今天寫的所有腳本做 code review，抓到 `_prelim_k_anchor_full45.py`／
`_prelim_k_anchor_full45_netcost.py`（就是算出上面 +8.65pp/yr 的兩支腳本）
有一個日期解析 bug：`checkpoints = [str(is_end)] + quarter_ends(str(oos_start),
str(oos_end))` 直接把 `walkforward_members.parquet` 的 `oos_end`（格式是
「YYYY-MM」，例如 `"2017-12"`）傳給 `pd.date_range(..., end=oos_end, freq="Q")`。
`pd.Timestamp("2017-12")` 解析成**該月第一天**（2017-12-01），導致
`pd.date_range` 判定 2017-12-31 超過終點而**每個窗次都漏算最後一季**——跟
`actions.py` 那次月份邊界洩題同一類問題（見上）。

**驗證**：修正前 E-1 窗印出「11 季」，修正後印出「12 季」，跟手寫完整日期的
獨立驗證腳本 `_prelim_k_anchor_check.py`（同一窗次、逐季報酬完全一致）對得上，
確認修正正確。修正方式：加 `month_end()` 輔助函式，把「YYYY-MM」轉成該月
最後一天再傳給 `quarter_ends()`。

**修正前後對照**：

| | 修正前（bug）| 修正後 |
|---|---|---|
| 32 窗毛超額平均 | +11.09%/yr | +10.20%/yr |
| 17 獨立窗毛超額平均 | +10.74%/yr | **+9.97%/yr** |
| 17 獨立窗**淨超額**（扣成本，即最終錨）| +8.65%/yr | **+7.90%/yr**（sd=1.86%，17/17 全贏）|
| 平均換手成本拖累 | 2.09pp/yr | 2.07pp/yr |

```
方法優勢（即時系統版、17 個獨立窗、扣真實交易成本，修正後）= +7.90pp/yr
規模曝險年度 p10 情境代價 = −14.1pp（不變）
k = 14.1 / 7.90 ≈ 1.78
```

**✅ k 最終凍結 = 1.78**（取代初版 1.63），已同步更新設計文件 §3.4／§11.7。
換算敞口上限約 −39%（原 k=3.1 對應「現況不調整」的 −68%），現況仍遠超新容忍度，
結論方向不變，但確切數字以這次修正後為準。

### A4 完成紀錄（2026-09-17）：`get_action_reference()` 物理過濾，抓到一個真實的月份邊界洩題 bug

**實作**：`code/app/actions.py` 的 `get_action_reference(ratio, allocation, as_of, ...)`，
依 §7.7 規定只回傳 `oos_end < as_of` 的窗次，直接在資料載入器過濾
`walkforward_matrix_detail.csv`，不靠 prompt 約束。

🔴 **實測抓到真實 bug**：矩陣的 `oos_end` 是「YYYY-MM」月份格式，`pd.to_datetime`
會解析成**該月第一天**。用 `as_of='2025-12-31'` 測試 window 4（`oos_end='2025-12'`
→ 解析成 2025-12-**01**）時，`2025-12-01 < 2025-12-31` 判定為真 ⇒ **window 4 洩漏**——
第一版程式碼實測就抓到這個問題（先寫了一個 `assert` 驗證再跑，不是憑印象相信寫對了）。
修法：統一轉成**月底**時間戳（`pd.PeriodIndex(...,freq="M").to_timestamp(how="end")`）
再比較。

**驗證**（4 個 as_of 逐一測試）：

| as_of | 可見窗次 |
|---|---|
| 2023-12-31（登記時點）| [1, 2]（window 3 的 oos_end 剛好等於登記日，保守排除）|
| 2024-01-01（實驗第一天起）| [1, 2, 3] |
| 2024-03-31 | [1, 2, 3] |
| 2025-12-31（實驗最後一天）| [1, 2, 3]（window 4 全程不出現，已用 assert 驗證）|

同時實作 `list_available_actions(as_of)` 列出每個 (ratio, allocation) 動作在
給定 as_of 下能看到幾窗、哪幾窗、平均 oos_cagr，方便之後人工核對「這次真的
看不到 window 4」。

⚠️ **範圍**：目前只做了 §7.7 要求的洩題防線本體，`actions.py` 其餘內容
（動作實際執行邏輯、W 系列）仍待後續開發。

### A7 完成紀錄（2026-09-17）：結構性缺口逐季串接，推翻了原本的預期方向

**動機**：§3.1 的缺口公式原用**期初權重**（2023-12-31 台積電 24.78%）套用整個
兩年期間的報酬差，但台積電權重期間內漲到 40.66%，設計文件原本自己標注「量級
可能低估」——這個顧慮須用真實逐季資料驗證，不能只憑直覺猜方向。

**做法**：`code/app/_prelim_a7_structural_gap_chained.py`。scheme E／A_hrp／
window 4，8 個季度逐季算真實 `gap_t = (w_port,t − w_idx,t) × (r_stock,t − r_rest,t)`：
- `w_port,t`：真實 resolve_holdings 解出的投組台積電權重
- `w_idx,t`：真實 `stock.market_capital` 算出的台積電佔全市場市值權重
- `r_stock,t`／`r_index,t`：真實台積電還原價報酬／`taiex_tr` 報酬指數報酬
- `r_rest,t`：代數反推 `(r_index,t − w_idx,t·r_stock,t)/(1−w_idx,t)`

**結果**（`_analysis_outputs_applayer/a7_structural_gap_chained.csv`）：

| | 台積電對缺口的貢獻 |
|---|---|
| 單期近似（原數字）| −29.92pp |
| **逐季串接（真實 8 季）** | **−22.87pp** |
| 差異 | **+7.05pp——缺口比原估計小，方向跟原本的顧慮相反** |

🔴 **推翻設計文件自己先前的預期**：原本猜單期近似會「低估」缺口，實測是**高估**。
根因：2024Q4→2025Q1 台積電下跌 −14.95%，同期其餘市場只跌 −7.13%，這一季
台積電**反而輸大盤**，單季貢獻 +2.76pp（正的），部分抵銷了其他季度的負貢獻——
這種路徑細節在兩年一次算的單期近似裡完全看不到。

**副產品**：逐季真實解出的投組台積電權重，8 季裡 **7 季是 0.00%**（策略完全沒
選到台積電），只有 2025Q4 出現 0.21%——比原假設「0.06%」更極端。

**連帶更新**：§1.1 的分解表（台積電佔加權效果從「52%」修正為「逐季串接版
約 40%」），結論不變（問題是低配大型股，不是低配台積電），但精確數字已更正。

### ✅ A5 完成（2026-09-17）：W2 前置驗證失敗，W1~W3 正式棄用

**動機**：§7.6 規定 W 系列（規模曝險軸，真正能解決 M1 的動作）啟用前必須先在
window 1-3 補算成矩陣格子，且不可碰 window 4（否則是用答案驗證答案）。

**做法**：`code/app/_prelim_a5_w2_prevalidation.py`。W2 定義具體化為
`w(s) ∝ w0(s)^α × mktcap(s)^(1−α)`（α=1 還原現有等權基準，α=0 變成對已選中
股票子集的市值加權），對 α ∈ {1.0, 0.75, 0.5, 0.25, 0} 用真實逐季持股解析＋
真實市值資料，在 window 1-3（scheme E／A_hrp，2015-2023）逐季驗證。

**結果——三窗一致，方向明確**：

| α | 三窗平均超額（vs 等權大盤）|
|---|---|
| 1.00（現況）| **+11.42%/yr（最好）**|
| 0.75 | +8.89%/yr |
| 0.50 | +5.45%/yr |
| 0.25 | +1.64%/yr |
| 0.00（純市值加權）| **−1.34%/yr（最差）**|

超額隨 α 降低單調遞減，α=0 在 window 2、3 甚至跑輸大盤。**三窗一致顯示市值
傾斜是負報酬動作。**

🔴 **判定為結構性驗證困境，非程式錯誤**：W2 是為了應付 window 4（2024-2025
大型股極端集中）設計的，但前置驗證規則只能用「正常」市場的 window 1-3（X 的
1.86pp 門檻本來就是拿這幾年校準的，正常年份不會逼近極端）——**這是保險式
策略的典型困境：不可能只用「沒出事的年份」驗證一個「出事時才有用」的策略**。

**使用者決策**（提供 5 個方案供選擇：A. 依規則誠實移除／B. 重新定位為僅供
人工參考／C. 擴大歷史樣本找先例／D. 合成極端情境壓力測試／E. 改用法規對齊
當正當性來源）：**選擇方案 A**——嚴格執行 §7.5 G5「不可用未驗證格子調參數」，
**W1／W2／W3 全部不啟用，動作空間正式收斂為 A0／A2／A4／A5**。已寫入設計文件
§7.5／§7.6（v16→v17）。

**對實驗的影響**：當 M1-D 觸發時，系統誠實可用的回應只剩「維持現狀」「切換
allocation（不解決 M1）」「觀察名單」「升級人工覆核」——沒有通過驗證的投組
層級解方。這本身是要報告的結果：嚴謹的前置驗證閘門正確擋下了一個看似合理、
實際只在未見過的極端窗次才會表現好的動作。

### ✅ A8 完成（2026-09-17）：2015-2016 敞口與集中度分位，§11.2b 校準測試通過

**做法**：直接重用 A1 已抓過的 `a1_index_concentration_series.csv`（真實 Q1
指數集中度序列），不需再連資料庫。登記時點取 2014-12-31（scheme A window 2／
D window 1 的 is_end，兩者剛好是重複窗，同一個 is_end）。

**結果**（跟設計文件 §3.2 已發布的 2023/2025 數字比對，完全吻合，確認方法一致）：

| 期間 | 期初敞口 | 集中度累計變動（8季）| 歷史分布百分位 | 超過 X=1.86pp？| 期望行為 |
|---|---|---|---|---|---|
| **2015-2016** | −66.01% | +1.15pp | 第 70 百分位 | ❌ 未超過 | 弱（不觸發）|
| **2024-2025** | −68.27%（§3.2 已發布 −68.26% ✓）| +4.34pp | 第 100 百分位 | ✅ 超過 | 強（觸發）|

**⇒ §11.2b 校準測試通過**：兩段給出明確不同強度的反應（2015-2016 遠低於門檻、
2024-2025 遠超門檻），M1-D 的門檻有鑑別力，不是隨便設的。範圍限定：只驗證了
M1-D 這一個機制，M2/M3/M5/M7 尚未各自校準。

### A6 完成紀錄（2026-09-16）

**改動**：`code/app/memo.py` 的 `_fmt_reference()` 與新增 `_benchmark_note_text()`；
`explain.py`／`memo.py` 兩個呼叫點都補上 `market=` 參數。

**修法**：原本的 `_benchmark_note` 寫死 2025-12 當下量到的台股數字
（19.55% vs 20.91%、台積電佔指數 40.23%）——這是 M-17 在「2019-2025」這個
固定期間量到的結果，套用到別的 as_of（例如驗證模式回到 2024 年初，當時台積電
實際只佔約 24.78%）就是前視。改成**不寫死任何時間點的具體數字**，只給結構性
提醒；依市場（TW／非 TW）給不同措辭，真正當期數字交給本來就有 as_of 語意的
欄位（三層監控指標、`alternative_context`）。

⚠️ **範圍說明**：這不是「即時查資料庫算出當下的真實比較數字」（那需要在 memo.py
引入即時 DB 依賴，是更大的架構改動），而是移除「拿一個凍結期間的數字冒充任何
時候都成立的事實」這個真正的前視風險來源。

**驗證**：
- 語法檢查通過
- `python -m research.tests`：**128/131**，跟修正前相同、非回歸（3 個既有失敗
  `t_stage0_artifacts_exist`／`t_stage0_idempotent`／`t_decision_repeatability_math_mocked`
  皆與此次改動無關，換機器沒帶 `results_artifacts/` 造成的既有問題）
- 直接測試 `_fmt_reference(ref, market=...)` 對 TW／US／XM／None 四種輸入，
  確認輸出**不含 `40.23`／`19.55`** 字串，且依市場給不同文字

---

## 3. 程式實作進度

| 檔案 | 內容 | 狀態 |
|---|---|---|
| `code/app/monitor.py` | 三層指標、expanding window 分布 | 🔶 完成環境層／結果層／過程層大部分（`max_cluster_share` 未接，見下）——已用 A1／A2 真實數字逐位元驗證通過 |
| `code/app/triggers.py` | 失效條件登記、比對、持續性規則 | ✅ **完成（M1-D）**——8 季逐季狀態跟 §7.4a 已記錄的真實時程逐位元一致，含遲滯持續性規則驗證通過；M2~M7 屬於 `diagnose.py` 範圍不在這裡 |
| `code/app/diagnose.py` | M0~M8 證據計算（**含所有衍生數字**，D2 相容）| 🔶 完成 **M3/M4/M6/M0/M8，含 performance_below_p10 端到端接通**（M8 見 D56）；**M2/M5/M7 發現結構性測不到，見下**；M1-R（回顧基準鏈）尚未做 |
| `code/app/weights.py` | W2c（條件式市值傾斜）＋ cap-and-redistribute | ✅ **2026-09-18 重新建立**（D50/D51）——W1／W3／永遠開啟版W2 仍不需要（D29 驗證失敗棄用）；W2c 前置驗證通過後正式建檔，`tilt_weights()`／`cap_and_redistribute()`／`apply_w2c()` 已接進 `simulate.py` 的執行層並驗證 |
| `code/app/actions.py` | 動作空間、換手率、動作參照（含 W2c） | ✅ **完成**——`get_action_reference`／`list_available_actions`（A4）＋ `w2c_reference()`（D50）；動作空間定案為 A0/A2/A4/A5/W2c，執行邏輯已接進 `simulate.py`（D51）|
| `code/app/facts_lean.py` | 監控專用精簡 facts（表格 CSV、巢狀 compact JSON）| ✅ **完成**——3a/3b 組裝＋§7.7 物理隔離防線，真實資料端到端驗證通過（見下）|
| `code/app/agents.py` | Agent-A（3a/3b/決策/總結/**人機對話**）、memory、tool dispatch | ✅ **已跟上 D44／D45／D62**——單一 Agent-A 架構，`call_agent_b()`／`call_agent_a_revision()`／`build_agent_b_memory_update()` 等已刪除，`assemble_quarterly_report()` 簽名同步簡化。真實 API 呼叫驗證通過（見 D45）。新增 `call_agent_a_dialogue_turn()`（§10 階段6，D62）——單輪呼叫，迴圈控制在 `l3_dialogue.py`。互動式 tool dispatch（受限工具集）未做 |
| `code/app/l3_dialogue.py` | L3（人機對話）迴圈控制、session 管理、落盤 | ✅ **新增**（D62）——重用已真實跑完的 L2 checkpoint 當對話起點，不重跑 3a/3b/5；≤5 輪、只有真實呼叫落盤到獨立的 `_runs/l3_dialogue_log.jsonl` |
| `code/app/simulate.py` | 模擬主迴圈 ＋ **checkpoint 落盤／續跑** ＋ **動作執行層** | ✅ **已跟上 D44／D45／D51**——`run_quarter()` L2 臂流程 3a→3b→決策→總結；新增 `ActiveConfig` 讓 A2／W2c 決策真的改變後續季度持股（D51）。dry_run＋權重比對測試皆驗證通過；正式 8 季實驗已用新執行層跑完（`formal_8q_control0_L2_execlayer_v2`，D57~D61 皆引用這份真實資料）；L3（人機對話）刻意仍不在這支自動迴圈範圍內——改由 `l3_dialogue.py`／`app/ui.py` 新分頁處理（D62），因為階段6需要真人即時互動，不該也無法自動化 |

**沿用不改**：`explain.py`（D2 掃描）、`performance.py`、`audit.py`（擴充欄位）。

### monitor.py 實作筆記（2026-09-17）

**架構決定**：沒有走 `risk.assess()`／`engine.run()` 那條完整 `Holdings` 物件管線
（那是給 replay／live 模式的既有功能用的，`live` 模式本身還沒實作完整即時建樹
路徑），改用今天全部前置計算已經驗證過的**輕量管線**：`resolve_strategy_holdings.
resolve_holdings()` 直接解持股 ＋ 手動聚合權重 ＋ `performance.measure()`——
跟 `_prelim_*.py` 系列同一套，理由：①這套已經被今天大量計算驗證過，可信度高
②不需要重建 replay 管線的 `RunConfig`／`cluster_info` 等額外狀態。

**驗證**（`code/app/_test_monitor.py`，非正式測試檔，開發時驗證用）：
- 環境層 Q1 集中度：2023-12-31＝88.2716%、2025-12-31＝92.6147%，跟 A1 已發布數字一致
- 結果層：2023-12-31→2024-03-31 投組報酬 −0.3393%、對等權超額 −0.2514%、n_stocks=445，
  跟 `a2_baseline_control0.csv` 第一列**逐位元相同**
- 過程層（新算，無獨立對照但邏輯共用已驗證的底層資料）：factor_exposure_F1
  最大佔比（用 `candidate_index.parquet` 的 `F1_band` 直接算，不需要完整
  Holdings 管線）、市值加權中位數、持股延續率、市場廣度

**未接的部分**：`max_cluster_share` 需要 HRP 樹的分群指派，跟輕量管線不是同一套
資料來源，範圍較大，暫緩；診斷 M5（風格漂移）用得到的 `factor_exposure_f1`
已經有了，不受影響。

### triggers.py 實作筆記（2026-09-17）

**範圍界定**：只做 **M1-D**——這是設計裡唯一「前瞻區可驅動動作」的機制
（§7.4「M1 的三個角色」），也是唯一有完整、已凍結門檻的機制。M2~M7 是**診斷**
（回答「為什麼」），依檔案分工屬於 `diagnose.py`，不在 `triggers.py`。

**持續性規則**：採雙門檻遲滯（p75 進場觀察、p90 進場觸發、只有跌破 p75 才解除，
觸發後小幅回落到 p75~p90 之間維持觸發狀態不降級）——這是為了正確處理 §7.4a
記錄的真實案例「2025Q1 短暫回落，不算誤報，維持觀察而非解除」。

**驗證**（`code/app/_test_triggers.py`）：8 個季度（2024Q1~2025Q4）的累計偏離值
與狀態**全部**跟 §7.4a 表格已記錄的真實時程一致（2024Q4 首次觸發 +1.89pp、
2025Q1 回落至 +1.46pp 仍維持 TRIGGERED、2025Q2 起 +2.59~+4.34pp 持續觸發）。

### 🔴🔴 diagnose.py：實作時發現 M2／M5／M7 結構性測不到（2026-09-17）

寫 M5 之前先推導它的定義，發現一個**在真正跑歷史分布之前就能推論出來**的問題，
沒有浪費運算：

- **M5**（風格曝險漂移，判準「`factor_exposure_F1` 對登記時的 L1 距離 > 歷史 p90」）：
  查 `ops/tools.py:566`，`factor_exposure_F1` 是算在**策略清單**（`strategy_uids`）
  上的分布，而 scheme E 的 walk-forward 設計裡策略清單在整個窗次（8 季）內
  固定不變，只有窗次邊界才會換。指標算在不會變的東西上 ⇒ **窗次內 L1 距離
  永遠是 0**——不是資料不夠，是這個指標的定義方式在本系統裡本來就測不到漂移
- **M7**（候選池整體失效，判準「B_all 相對等權大盤的超額 < 歷史 p10」）：A9
  已確認 B_all 在即時系統裡**就是**等權大盤本身（`walkforward_matrix.py:62`）
  ⇒ 這個判準**數學上恆為 0**，結構性測不到
- **M2**（策略群集中度）：需要 HRP 樹分群指派，跟輕量持股解析管線不同源
  （跟 `monitor.py` 的 `max_cluster_share` 未接是同一個限制）

**這三個不是「還沒做完」，是「用目前的即時系統量測方法本來就測不到」**——
比單純的工作量問題更根本，會寫進論文的誠實限制揭露（呼應 §7.3 已有的診斷
涵蓋率限制，這次是同一類問題在實作階段的具體案例）。

**實際完成**：`diagnose_m3()`（選股廣度，用 `a1_portfolio_noise_windows123.csv`
真實歷史分布）、`diagnose_m4()`（相對 B_all 連續 2 期失效，B_all＝等權大盤）、
`diagnose_m6_m0()`（僅隨機波動／無法歸因的互斥出口）。**用 window 4 真實 8 季
資料測試**：M3 只在 2025Q4 觸發（n=315，剛好低於歷史 p10=346）；M4 在
2024Q1-Q2（兩期連續小負超額）與 2025Q3-Q4（兩期連續大負超額）各觸發一次，
結果合理。M1-R（回顧區基準鏈拆解）尚未實作。

#### ✅ 整合缺口補上（2026-09-17）：`performance_below_p10` 端到端接通

新增 `code/app/_prelim_m0_performance_history.py`——**重新真實計算** windows
1-3 的逐季 `excess_vs_ball`（沒有沿用今天稍早跑 `_prelim_k_anchor_check.py`
時終端機印過的數字，那次沒有落盤，重算一次才正式採用，不憑印象），存成
`m0_performance_history.csv`（n=36）。歷史分布：平均 +2.50%、標準差 2.29%、
**p10=−0.16%**、中位 +2.39%、p90=+5.20%。

`diagnose.run_diagnosis()` API 簡化：`performance_below_p10` 改成內部用
`excess_vs_ball_history[-1]` 自動算（`is_performance_below_p10()`），不再要求
呼叫端另外傳一個容易跟 M4 用的「本季值」兜不起來的布林值。

**端到端測試**（window 4 真實 8 季）：

| 季 | 超額 | 判定 |
|---|---|---|
| 2024Q1 | −0.25% | M0（異常，無法歸因）|
| 2024Q2 | −0.28% | M4（連續 2 期負超額，M0 正確被壓制）|
| 2024Q3 | +0.60% | M6（正常）|
| 2024Q4 | −0.58% | M0（異常，無法歸因）|
| 2025Q1 | +2.86% | M6（正常）|
| 2025Q2 | +0.23% | M6（正常）|
| 2025Q3 | −3.33% | M0（異常，無法歸因）|
| 2025Q4 | −2.59% | M3+M4 同時觸發，M0 正確被壓制 |

M0 只在「績效低於歷史 p10、但 M3/M4 都沒觸發」時才出現，一旦有其他機制能
解釋就正確讓位——符合 §7.4「M0 不可硬塞機制」的設計初衷。至此 `diagnose.py`
的 M3/M4/M6/M0 已完全打通並用真實資料端到端驗證。

### ✅ facts_lean.py 完成（2026-09-17）：3a/3b 組裝 ＋ §7.7 物理隔離防線

**實作**：`three_tier_state_csv`／`m1d_state_csv`／`available_actions_csv`
（3b 前瞻用）與 `outcome_csv`／`diagnosis_csv`／`memory_compact_json`（3a 回顧
用），CSV 表格欄位數 ≤8、每 10 列重複表頭（§12.3② 的防線）。

🔴 **`build_prospective_facts()` 有程式層的物理防洩題檢查**：組完 dict 後
立刻遞迴掃描 key／字串值有沒有踩到流量變數關鍵字（`excess_vs`／
`portfolio_return`／`attribution` 等），踩到直接 `raise`——不是文件裡寫規則
要求 agent 自律，是程式碼本身擋。跟 `actions.py` 的 `get_action_reference()`
用資料載入器物理過濾 window 4 同一個精神（§7.7）。

**驗證**（`code/app/_test_facts_lean.py`，用 2024Q4 真實資料）：
- `outcome_csv`：報酬 −4.84%、超額 −0.58%、n_stocks=474，跟 `a2_baseline_control0.csv`
  第 4 列完全一致
- `m1d_state_csv`：偏離 +1.89%，跟 §7.4a 已記錄的「2024Q4 首次觸發」數字一致
- **故意**把一個流量變數塞進前瞻 facts，確認防線真的會 `raise` 擋下來，
  不是裝飾用的斷言

### 🔶 agents.py：只做 Agent-A 的 3b（前瞻評估）（2026-09-17）

**範圍決定**（跟使用者確認過）：完整每季迴圈要 Agent-A 扮演 5 種角色（3a 回顧
診斷、3b 前瞻評估、4.5 修訂、5 決策、7 總結）加上 Agent-B（質疑），每種角色
都要獨立的 system prompt／schema，工作量很大。**先做 3b**——這是「唯一可
驅動動作的區塊」，也是安全風險最高的一環（§7.7 的物理隔離只保護到 facts
組裝，prompt／schema 沒設計對，物理隔離就白做）。

**實作**：複用 `memo._call_llm`／`memo.scan_for_leakage`（不重寫網路/錯誤
處理/額度記帳/D2 掃描）。System prompt 把 §5.1 鐵則、§7.6「不得自行計算
預期效果」具體化成 6 條明確規則；schema 4 個欄位（state_summary／
m1d_interpretation／distance_to_threshold／caveat），刻意不含任何「建議
動作」欄位（那是階段 5 的事）。

**驗證**：`dry_run=True` 測試通過（prompt 組裝正確、只吃
`facts_lean.build_prospective_facts()` 的輸出）。

**✅ 第一次真實 LLM 呼叫成功**（`code/app/_test_agents_real_call.py`，2024Q4
真實資料，M1-D 首次觸發那一季，經使用者同意才執行）：
- D2 洩漏掃描通過（`leakage_check=[]`）——沒有引用資料外的數字
- 用量 4,476 tokens（1,750 prompt + 2,726 completion，含 2,240 reasoning）
- 輸出正確解讀 M1-D 為 TRIGGERED、正確指出缺門檻資訊的指標無法判斷、
  **沒有**建議任何動作、**沒有**做「如果 xxx 則預期 yyy」的推算——§5.1／§7.6
  鐵則全部遵守
- 內容偏機械式（接近複述數字），這是正確行為（規則本來就禁止超出資料的
  詮釋），非缺點；之後若要提升洞察力可再調整 prompt，本次重點是驗證
  管線不洩漏、不違規

### 🔴🔴 D2 洩漏掃描在本系統的識別碼命名下有真實盲區，已深入修正（2026-09-17）

**發現**：做 3a（回顧診斷）第一次真實呼叫時，D2 誤攔了完全正確的一句話
（「diagnosis_csv 未包含 M6 的判定」），因為 `scan_for_leakage` 用的泛用數字
正則會把「M6」裡的「6」當成獨立數字，這裡剛好沒有巧合命中就被誤擋。

**追查後發現範圍更大、方向更危險**：欄位名稱本身（`q1_weight`、
`m1d_state_csv`）內含的雜散數字，會被 D2 當成「prompt 裡已提供的合法數字」，
如果 LLM 捏造一個剛好等於這些雜散數字的假數字（例如「1」），D2 完全抓不到
——這比「正確答案被誤攔」更嚴重，是「該擋的沒擋住」。用乾淨測試直接證實：

```
prompt 只有 q1_weight（無關的欄位名，含數字1）
LLM輸出：「亂編的假數字約為一（1）成」
scan_for_leakage() → []（完全沒抓到，因為 q1_weight 的 1 洗白了它）
```

**使用者決定深入修**（而非只修 M6 那一個案例）。修法：不動共用的
`memo.scan_for_leakage`／`_NUMBER_RE`（explain.py／memo.py 共用、已測試過的
核心機制），在 `agents.py` 做域特定前處理——任何「以字母開頭、內含 1~2 碼
數字」的識別碼（M0~M7、A0~A5、W1~W4、F1/F2、L1~L3、C2/C5、T8、
`q1_weight`、`m1d_state_csv`……），比對前一律把數字部分拿掉，只留字母骨架。

🔴 **修正過程中自己又踩到一個真 bug**：第一版正則（數字長度不設限）會把
JSON 跳脫換行符（字面上的 `\n`，反斜線加字母 n）緊貼日期數字時（例如
`...weight\n2024-09-30...`）誤判成「字母 n 開頭、含數字 2024 的識別碼」，
把日期「2024」整個吃掉，衍生出全新的誤攔。修法：限制識別碼的數字長度為
1~2 碼（`\d{1,2}(?!\d)`）——本系統真正的識別碼數字都只有 1~2 碼，真正的
資料數值（年份、金額這類 3 碼以上連續數字）不會被誤吃。

**驗證**（`code/app/_test_agents_leakage_fix.py`，三個情境都要過）：
1. M6 機制代號不再被誤攔 ✓
2. 捏造的數字「1」正確被抓到，不再被 `q1_weight` 的「1」洗白 ✓
3. 日期等多位數真實數值不再被誤判成識別碼而吃掉（修正過程自己的回歸測試）✓

**最終用真實 API 再跑一次 3a**（`call_agent_a_3a()` 正式路徑）：LLM 又寫了
「例如 M6 等其他機制本次無法評估」，這次**正確通過**（`leakage_check=[]`），
確認修正在真實流程裡有效，不只是離線重建測試。

⚠️ **範圍限定**：這個修正只套用在 `agents.py`（本系統新寫的模組）。
`explain.py` 的正式管線很可能有同一個盲區（它的 facts 也有大量同類識別碼，
如 `F1_factor`／`cluster_L1`），但那是已上線的既有系統，要不要動、怎麼驗證
不回歸，需要使用者另外決定，這裡沒有擅自去改。

### ✅ Agent-B（質疑）完成，第一次真實呼叫就抓到兩個真問題（2026-09-17）

**範圍縮減**：§8 的 Agent-B 設計有「受限工具集」（互動式查詢，agent 主動呼叫
`get_attribution`／`get_historical_distribution`／`find_similar_quarters`／
`get_action_reference`），這是跟 3a/3b 不同的互動模式（tool-calling）。這次
先做「單次結構化質疑」版本——把 3a／3b 的完整 facts＋輸出都給 B，一次性
輸出質疑；工具互動式版本留待後續。Schema 支援 `no_substantive_objection`
（誠實回報沒有實質質疑的選項，§8 明訂必須存在，避免異議退化成雜訊）。

**真實呼叫結果**（用今天已驗證過的 2024Q4 真實 3a／3b 輸出）：D2 洩漏掃描
通過（`leakage_check=[]`），用量 8,874 tokens。**Agent-B 抓到兩個真的問題**：

1. **`available_actions_csv` 給了 3b 但 3b 完全沒用到**——B 正確指出這造成
   前瞻評估跟可用動作脫節。查證後確實如此：3b 的角色定義是「只描述現況，
   不選動作」（選動作是階段 5 的事），給 3b 用不到的資料只會造成混淆。
   **已修正**：`facts_lean.build_prospective_facts()` 移除
   `available_actions_csv`，改留給尚未實作的階段 5 決策 facts 用。
2. **🔴 更嚴重：`three_tier_state_csv` 跟 `m1d_state_csv` 的 `q1_weight` 對
   不上**（0.8926 vs 0.9016）——B 正確指出同名指標兩個數值不一致卻沒有
   解釋。查證後發現真正原因：測試腳本把 env／proc（用季初 `as_of` 解的
   持股／市場資料）跟 m1d（用季末 `end` 解的）混在同一份「現況」facts 裡，
   兩個不同時間點的快照被當成同一份現況餵給 LLM。**已修正**：
   - `facts_lean.build_prospective_facts()` 新增物理防線——`env["as_of"]`
     跟 `m1d["as_of"]` 不一致就直接 `raise`，不是留給人工事後發現
   - 修正 `_test_facts_lean.py`／`_test_agents_real_call.py`：狀態層
     （env／proc／m1d，回答「現在長什麼樣」）統一用 **end**（季末，最新
     可得資料）；結果層（outcome，回答「這段期間發生了什麼」）維持用
     **as_of** 解的持股（測「季初那筆持股在這段期間表現如何」）——兩者
     本來就該是不同時間點的快照，不能共用同一份 `weights`
   - 這個時間點對齊規則也適用於未來的 `simulate.py`，已記錄在程式碼註解裡

第三條質疑（建議用 `state` 欄位而非重新推導門檻邏輯）較偏風格建議，非
關鍵問題，未特別處理。**這次真實驗證了 Agent-B 這個角色的設計價值**——
兩個真問題都是我自己組資料時的疏失，3a／3b 個別測試時看起來都正常，
要放在一起互相檢查才看得出來。

---

## 4. 實驗執行進度

三臂 × 8 季。**對照 0 不用 agent**（純程式，D46 修正後確認真的零 LLM 呼叫）。

| 季 | 對照 0 | L2（無對話）| L3（有對話）|
|---|---|---|---|
| 2024Q1 | ✅ A0 | ✅ A0 | ⬜ |
| 2024Q2 | ✅ A0 | ✅ A0 | ⬜ |
| 2024Q3 | ✅ A0 | ✅ A0 | ⬜ |
| 2024Q4 | ✅ A0（TRIGGERED）| ✅ A5（TRIGGERED）| ⬜ |
| 2025Q1 | ✅ A0（TRIGGERED）| ✅ A5（TRIGGERED）| ⬜ |
| 2025Q2 | ✅ A0（TRIGGERED）| ✅ A5（TRIGGERED）| ⬜ |
| 2025Q3 | ✅ A0（TRIGGERED）| ✅ A5（TRIGGERED）| ⬜ |
| 2025Q4 | ✅ A0（TRIGGERED）| ✅ A5（TRIGGERED）| ⬜ |

✅ **control0／L2 已於 2026-09-18 完整跑完並驗證（D51 執行層版本）**，
run_id=`formal_8q_control0_L2_execlayer_v2`（取代表格與下方沿用的舊
run_id=`formal_8q_control0_L2`，那是 D51 執行層補上前跑的舊版本，保留當
歷史紀錄不覆蓋）。
**L3 介面已完成（D62）**，上面表格的「有對話」欄仍是 ⬜——介面能對任一
真實季度開對話，但這 8 季**還沒有人實際跑過一段真實對話**（跟「介面完成」
是兩件事，見 §8 第8項）。
**尚未做的**：逐季決策理由的品質面分析（§11.3 b 的 rubric 逐項評分）、
盲評程序（去識別化＋混合順序）——目前只確認了流程跑得動、狀態/決策對不對，
還沒真的評分「理由品質好不好」。

**對照測試**（設計文件 §11.2／§11.2b）：

| 測試 | 期望 | 狀態 |
|---|---|---|
| 陰性 A（window 3，2021-2023）| 不觸發 | ⬜ 未跑（需另外解析該窗次策略清單）|
| 陰性 B（2015-2016）| 可觸發但反應**弱** → A0 | ⬜ 未跑（門檻計算已在 §11.2b 驗證過，但沒有跑完整 agent 流程）|
| 陽性（合成）| 正確觸發並診斷 | ⬜ 指標序列尚未構造 |
| **校準測試**（§11.2b，僅 M1-D）| 兩段反應強度**不同** | ✅ **已通過**（D30）|
| mini 品質對照（Agent-B）| — | 🔴 **Agent-B 已移除（D44），此項作廢** |

---

## 5. 額度與成本紀錄

**每日免費額度**（`utils/openai_quota.py`）：標準池 1,000,000／小模型池 10,000,000，
扣 `RESERVE_RATIO=0.2` 後**標準池實際可用 800,000/日**。

**預估**（設計文件 §12.3）：

| 情境 | tokens | 天數 |
|---|---|---|
| 沿用現況 facts（26,443/次 × 138）| 3.65M | 4.6 天 |
| **採精簡 facts ＋ CSV** | 1.45M | 1.8 天 |
| **再把 B 移到 mini 池** | 標準池 1.28M | 1.6 天 |

**實際用量紀錄**（每次執行後補）：

| 日期 | 階段 | 呼叫數 | standard tok | mini tok | 備註 |
|---|---|---|---|---|---|
| — | — | — | — | — | 尚未開始 |

查詢指令：`python -m utils.openai_quota --usage`

---

## 6. 決策紀錄（已定案，避免重複討論）

| # | 決定 | 日期 |
|---|---|---|
| D1 | 實驗期＝ **2024-Q1~2025-Q4 共 8 季**（非只有 2025），登記時點 2023-12-31 | 09-16 |
| D2 | 「策略清單不變」＝**候選池不變**（TW 6,679 檔），組合方式才是研究對象 | 09-16 |
| D3 | 自主等級做 **L2／L3**，唯一差異是**有無對話**；清單外選項改為對話的產物 | 09-16 |
| D4 | 對話上限 **5 輪**（暫訂）| 09-16 |
| D5 | 記憶深度：全歷史，舊期用**程式產生**的結構化摘要 | 09-16 |
| D6 | 後見之明污染：**誠實揭露**，定位為流程驗證非盲測 | 09-16 |
| D7 | 驗收標準：績效面＋品質面**兩者都要**，衝突時並列不合成 | 09-16 |
| D8 | **對照組要做**（對照 0／L2／L3 三臂）| 09-16 |
| D9 | **工具呼叫要做**（四個受限工具，A／B 對稱）| 09-16 |
| D10 | 多重診斷**不排序、兩者都報告**，附可修／不可修的幅度拆解 | 09-16 |
| D11 | M7 判準改為「**B_all 相對等權大盤的超額**」（原判準會把 M1 效應算進來）| 09-16 |
| D12 | 報告分**可歸因區／狀態警示區**；B 區**絕不可估算 pp 貢獻** | 09-16 |
| D13 | 🔴 **規模曝險改為期初政策**（敞口近乎常數＋報酬差不可預測）；M1 拆成政策／M1-R／M1-D | 09-16 |
| D14 | 風險容忍度採 **(b) 情境損益法**，錨＝方法預期超額的 k 倍；用途為**示警型** | 09-16 |
| D15 | 🔴 **3a／3b 分兩次獨立呼叫**——3b 的 prompt 物理上不含回顧資料 | 09-16 |
| D16 | 成本：**精簡 facts ＋ 表格改 CSV ＋ Agent-B 試 mini** | 09-16 |
| D17 | **斷點續跑列為必要功能**（煞車會中斷路徑依賴鏈）| 09-16 |
| D18 | 技術選型：純 Python ＋ OpenAI SDK，**不引入 LangChain／LangGraph** | 09-16 |
| D19 | 診斷清單新增 **M0 無法歸因**出口；**A1（調 ratio）改列期初政策** | 09-16 |
| D20 | 流量變數**不可驅動投組變更**（A1/A2/W1-W3），但**可驅動 A0/A4/A5** | 09-16 |
| D21 | 🔴 **M1-D 改用指數端集中度**（非投組端敞口偏離）——投組端是換股雜訊、方向不穩定，2024-2025 反而被稀釋成不觸發；指數端乾淨單調，2024Q4 即正確首次觸發 | 09-16 |
| D22 | 🔶 **兩套量測系統差距已實測**（凍結策略層 0.48% vs 即時股票層 7.22%，差 15 倍以上；B_all 也差 3.7 倍），成因待查證，不可未查證先當定論 | 09-16 |
| D23 | 🔴 **重新平衡頻率假設已排除**——月頻即時版（+12.81%/yr）沒有收斂到凍結矩陣版（+4.53%/yr），反而比季頻版（+11.42%/yr）更遠。k 錨點的落差原因仍未解，k=3.1 與 k≈1.23 兩個候選值並存，**需使用者決定如何處理**才能往下凍結 | 09-17 |
| D24 | 🔴 **樣本數／範圍假設已排除**——把 M-17 的「45 窗」母體（排除觸及實驗期間的 13 窗、排除重複窗後為 17 個獨立窗）全部用即時系統重算，平均超額 +10.74%/yr（🔴 後經 D31 修正為 +9.97%/yr），17/17 全贏，跟 3 窗版（+11.42%）高度一致；t 檢定對 M-17 兩個候選值皆 p<0.0001 | 09-17 |
| D25 | 🔴 **落差根因查證完成**：①交易成本不對稱（凍結矩陣組合報酬扣台股手續費+證交稅、比較基準沒扣）可解釋 ~2.1pp/yr，已用真實換手率驗證；②扣成本後仍有 ~5-6pp/yr 差距，判定為結構性——凍結矩陣＝策略歷史真實換股執行軌跡，即時系統＝checkpoint 快照後靜態持有，**兩套系統對「投組報酬」定義本來就不同，不是 bug** | 09-17 |
| D26 | ✅ **k 初版凍結 = 1.63**——原則：k 的分母必須跟實際運作系統自洽，不可跨系統借用 M-17 數字；改用即時系統版、17 個獨立窗、扣真實交易成本的方法優勢（+8.65pp/yr）當錨，`k = 14.1/8.65 ≈ 1.63`。已寫入設計文件 §3.4／§11.7。🔴 **同日稍晚被 D31 更正為 1.78**，此決定的「原則」部分仍然有效，「數字」部分已由 D31 取代 | 09-17 |
| D27 | ✅ **X 正式凍結 = 1.86pp**——凍結前查證發現滾動 8 季變動樣本（n=60）相鄰重疊 7/8，真正獨立只有 n=8（p90=1.56pp／max=1.65pp）；n=8 太小不適合單獨定門檻，但觸發結論對此穩健（實際變動 +4.34pp 遠超兩版本的任何分位數），採用滾動版 p90=1.86pp。目前是**書面凍結**（`monitor.py`/`triggers.py` 尚未實作，還沒有真正在跑的 `audit_log.jsonl`）| 09-17 |
| D28 | 🔴 **A7 逐季串接推翻原預期方向**——原本猜單期近似「低估」缺口，實測結果是「高估」：真實逐季串接的台積電缺口貢獻是 −22.87pp，比單期近似的 −29.92pp 小 7.05pp（根因：2024Q4→2025Q1 台積電走弱一季，正貢獻抵銷部分負貢獻，兩年單期近似看不到這種路徑細節）。§1.1 的「台積電佔加權效果 52%」已修正為約 40% | 09-17 |
| D29 | 🔴🔴 **W2/W3 前置驗證失敗，正式棄用**——真實逐季驗證（window 1-3）顯示市值傾斜（α 從 1 降到 0）超額報酬單調遞減、α=0 甚至跑輸大盤；判定為保險式策略「無法用非極端期資料驗證尾部效果」的結構性困境。提供 5 個方案（依規則移除／重新定位為人工參考／擴大歷史樣本／合成情境壓力測試／改用法規正當性），**使用者選方案 A**：嚴格執行 §7.5 G5，W1～W3 全部不啟用，**動作空間正式收斂為 A0／A2／A4／A5** | 09-17 |
| D30 | ✅ **§11.2b 校準測試通過**——2015-2016 集中度變動（+1.15pp，歷史第70百分位）遠低於 X=1.86pp 門檻不觸發，2024-2025（+4.34pp，第100百分位）遠超門檻觸發，兩段反應強度明確不同，M1-D 門檻證實有鑑別力。數字跟 §3.2 已發布的 2023/2025 敞口值吻合，確認方法一致 | 09-17 |
| D31 | 🔴🔴 **code review 抓到並修正 k 值計算的日期 bug——k 從 1.63 更正為 1.78**——`_prelim_k_anchor_full45.py`／`_prelim_k_anchor_full45_netcost.py` 把 parquet 的「YYYY-MM」月份字串直接傳給 `pd.date_range`，`pd.Timestamp("2017-12")` 解析成該月第一天，導致每個窗次都漏算最後一季（跟 `actions.py` 那次月份邊界洩題同一類問題）。修正後跟手寫完整日期的獨立驗證腳本逐季比對一致。17 獨立窗淨超額從 +8.65pp/yr 修正為 **+7.90pp/yr**，**k = 14.1/7.90 ≈ 1.78**（原 1.63 已由此取代），已同步更新設計文件與本文件所有引用處 | 09-17 |
| D32 | 🔴🔴 **M2／M5／M7 判定為結構性測不到，非工作量問題**——M5 的 `factor_exposure_F1` 算在固定不變的策略清單上，窗次內 L1 距離恆為 0；M7 的「B_all 相對等權大盤超額」因 B_all＝等權大盤（A9）數學上恆為 0；M2 需要跟輕量管線不同源的 HRP 分群資料。`diagnose.py` 範圍縮為 M3/M4/M6/M0，三者列為誠實揭露的限制 | 09-17 |
| D33 | 🔴🔴 **D2 洩漏掃描在本系統識別碼命名下有真實盲區，已深入修正**——3a 第一次真實呼叫誤攔正確句子（M6 裡的「6」無巧合命中），追查後發現更嚴重的反向問題：`q1_weight`／`m1d_state_csv` 等欄位名裡的雜散數字會洗白 LLM 捏造的同值假數字，D2 完全抓不到（已用乾淨測試證實）。使用者選擇深入修（非只修單一案例）：在 `agents.py` 做域特定前處理，把「字母開頭＋1~2碼數字」的識別碼數字中性化，不動共用的 `memo.scan_for_leakage`。修正過程自己又踩到一個回歸 bug（JSON 跳脫 `\n` 緊貼日期數字被誤判成識別碼，吃掉年份），修好後三個情境（誤攔／洗白／真數值誤吃）都用測試檔驗證通過，並用真實 API 呼叫二次確認。範圍限定在 `agents.py`；`explain.py` 正式管線可能有同一盲區，未動，留待使用者決定 | 09-17 |
| D34 | ✅ **Agent-B（質疑）完成，第一次真實呼叫抓到兩個真問題**——① `available_actions_csv` 給了 3b 但 3b 設計上不該用它，已移除；② `three_tier_state_csv` 跟 `m1d_state_csv` 的 `q1_weight` 因為測試腳本用了不同時間點（as_of vs end）解的持股而對不上，已修正腳本並在 `build_prospective_facts()` 加物理防線（as_of 不一致直接 raise）。驗證了 Agent-B 角色的設計價值——兩個問題單獨測 3a/3b 都看不出來 | 09-17 |
| D35 | ✅ **Agent-A 4.5（修訂）完成並真實驗證**——吃 3a／3b 草稿＋B 的質疑，逐條回應（採納/不採納皆附理由），修訂範圍正確依 target 分流（三條質疑都針對 3b，revised_3a 原樣照抄未被誤動）。內容品質高：對 q1_weight 不一致補充來源說明、對沒看到具體內容的 available_actions_csv 誠實承認未臆測。第一次呼叫曾被 D2 攔下（原始內容未留存，無法確定是真捕獲還是殘餘誤判），重跑後乾淨通過 | 09-17 |
| D36 | ✅ **Agent-A 5（決策）完成並真實驗證**——新增 `facts_lean.build_decision_facts()`（物理排除 3a／回顧區資料，只給 m1d 基準動作＋可用動作歷史條件分布＋3b 完整輸出）。真實呼叫（2024Q4，M1-D TRIGGERED）：選 A5（採用基準），且**主動說明為何不選 A2**（「僅切換 allocation 並不解決規模曝險集中問題」，呼應 §7.5 設計初衷），理由正確引用 prospective_assessment 具體項目，D2 通過 | 09-17 |
| D37 | ✅ **Agent-A 7（季度總結）完成並真實驗證，Agent-A 全部 5 個角色至此做齊**——只讓 LLM 生成②回顧③前瞻兩段敘事（整合既有內容，不重新推論），①程式事實／④B 異議原文（規定不可改寫，設計 `assemble_quarterly_report()` 完全不經過 LLM）／⑤人的裁決留給呼叫端組裝，不進 LLM 呼叫範圍。真實呼叫內容連貫、沒有新增未確認結論，D2 通過。順帶記錄：設計文件 §10 標題寫「五段式」，另外兩處（表格、§12.3①）寫「四段式」，三處不一致，未擅自更動設計文件 | 09-17 |
| D38 | 🔶 code review：`agents.py` 模組開頭 docstring 已過期（還寫「只做 3b」），已修正為反映實際完成狀態。另記錄一個潛在脆弱點（非立即需修）：`facts_lean._assert_no_flow_leakage` 的流量變數關鍵字比對不只查欄位名，也查任意字串內容（含 LLM 自己生成的自然語言文字）——`build_decision_facts()` 把 3b 完整輸出包進去再檢查一次，若 3b 未來剛好寫出含 `attribution` 等關鍵字子字串的英文句子會被誤判攔下。今天測試皆為中文敘述未踩到，記錄備查 | 09-17 |
| D39 | ✅ **`simulate.py` 完成，dry_run 端到端驗證通過**——把 monitor／triggers／diagnose／facts_lean／agents（3a/3b/Agent-B/4.5/5/7）全部串成 control0／L2 兩臂 × N 季主迴圈，含 checkpoint 落盤與續跑（§12.4）。測試抓到一個真 import 順序 bug：`from utils import openai_quota` 原本寫在模組頂層，違反 `memo.py:355` 既有的「函式內延遲載入」慣例（root 尚未進 sys.path 時會直接 crash），已修正移入函式內。dry_run 測試：2 臂×2 季共 4 筆 checkpoint 正確落盤，續跑正確跳過已完成的 (臂,季)，未重複運算。**尚未真實執行任何一季**（需要真實 LLM 呼叫，留待使用者授權）；L3（人機對話）不在這支自動批次腳本範圍內 | 09-17 |
| D40 | ✅ **`simulate.py` 真實單季測試成功，且抓到並修正一個新的 D2 誤判類型**——Agent-A 用「1) ⋯；2) ⋯；3) ⋯」列點回應質疑／說明理由，清單編號被 D2 誤判成捏造數字攔下（分別在決策與修訂階段各抓到一次）。根因跟先前的識別碼問題同一類（泛用數字正則抓到非資料性質的類數字符號），修法新增 `_LIST_MARKER_RE`（用負向後顧 `(?<![\d.])` 確保不誤傷算式裡的真實小數，如 "0.012 - 0.008"），補進回歸測試（情境4）。修正後重跑，完整單季測試通過，且真實命中 Agent-B **有**質疑的分支（`no_substantive_objection=False`），確認 3a→3b→Agent-B→4.5修訂→5決策→7總結整條鏈在真實情況下完整跑通 | 09-18 |
| D41 | ✅ **`memory` 正式化完成，抓到並修正一個循環參照 bug**——新增 `agents.build_agent_a_memory_update()`（最近一期全文＋更早期程式產生的結構化摘要，避免 telephone effect）與 `build_agent_b_memory_update()`（歷次質疑／是否被採納／事後是否成立，§8 表）。接進 `simulate.py` 時抓到真 bug：`recent_quarter_full` 直接參照 `result` 物件本身，後面又把記憶寫回同一個 `result`，變成自我循環參照，`json.dumps` 直接炸掉——用淺拷貝（`dict(result)`）切斷循環後修好。3 季 dry_run 驗證：記憶遞移正確（最近一期永遠全文、更早期正確降級成摘要），續跑精確還原不重新摘要 | 09-18 |
| D42 | ✅ code review 抓到並修正 `build_agent_b_memory_update()` 的配對邏輯 bug——原本用「質疑文字前20字元是否為子字串」配對質疑跟修訂回應，但 `objection_summary` 是 Agent-A 改寫過的摘要，不保證含 B 原文字串，容易配不到、靜默把 `accepted` 留成 `None`（不會報錯的錯）。改用位置對應（`objections[i]`↔`responses[i]`，符合「逐條回應」的 system prompt 設計），長度不一致時保守標記全部 `None`，不亂猜。用真實資料重建驗證配對正確。另記錄兩個非緊急發現：`simulate.py` 的 `already_done()` 是死代碼；`outcome_layer()` 支援算 `excess_vs_cap_weight` 但 `simulate.py` 沒接 TAIEX 序列，該欄位在模擬中恆為空（不影響核心機制） | 09-18 |
| D43 | ✅ 補上 `excess_vs_cap_weight` 缺口——`QuarterInputs` 新增 `cap_weight_series`（taiex_tr），`run_simulation()` 抓取後傳入 `outcome_layer()`。dry_run 驗證：算出 −0.1384，跟 `a2_baseline_control0.csv` 已記錄的 −0.13841（同一季 2023-12-31→2024-03-31）對得上，確認接線正確 | 09-18 |
| D44 | 🔴🔴 **Agent-B（質疑者）正式移除，改為單一 Agent-A 架構**——使用者提出疑問「老師當初是不是說過不需要做這種對抗」，查證後找到兩個真實佐證支持這個判斷：①舊「三 Agent 線上系統」（`實戰部架構_v8.md`）已因故事線改變而廢止，接手決策層角色的 `decision_layer_arms.py`（S-05）完全沒用對抗 agent，一樣站得住腳且已寫進論文框架；②今天三次真實測試 Agent-B 兩次無實質質疑，唯一一次抓到的兩個問題（D34）事後查證都是**測試資料本身組錯**（facts 誤給、時間點不一致），不是「分析有歧義、需要另一視角才能發現」的問題，不符合 §8 引用 Ang et al. 設下的「多 agent 需要有意義分歧」門檻。**使用者同意移除**。已更新設計文件（v18→v19）：§8 改為單一 Agent-A；§10 移除階段4（質疑）／4.5（修訂），階段7 從五段式變回四段式（順帶解決 D37 記錄的三處不一致）；screen 數由 3 減為 2；§12.3 每季每臂呼叫數 6→4，成本上限 138→106 次。**誠實揭露代價**：型二錯誤守門角色消失，只能靠 A5 人工覆核；已記錄進 §10「每季都要有解釋」表跟 §15 對照表 | 09-18 |
| D45 | ✅ **D44 程式碼側完成，`agents.py`／`simulate.py` 已跟上設計文件 v19**——依使用者指示「先更新文件」後「也請你修改程式碼」，兩步分開執行。`agents.py` 刪除 `_AGENT_B_SYSTEM_PROMPT`／`_AGENT_B_SCHEMA`／`build_agent_b_prompt()`／`call_agent_b()`／`_AGENT_A_REVISION_SYSTEM_PROMPT`／`_AGENT_A_REVISION_SCHEMA`／`build_revision_prompt()`／`call_agent_a_revision()`／`build_agent_b_memory_update()`，`assemble_quarterly_report()` 簽名拿掉 `agent_b_output`；`simulate.py` 的 `run_quarter()` L2 臂流程簡化為 3a→3b→決策→總結（不再呼叫 Agent-B／4.5 修訂），`run_simulation()` 拿掉 `agent_b_memory` 追蹤。驗證分兩層：① dry_run 3 季完整跑通（含續跑續存記憶正確遞移）；② **真實單季 API 呼叫**（control0＋L2）乾淨跑完，L2 臂 `decision` 欄位為 `['decision','decision_detail','reasoning']`、`summary` 欄位為 `['program_facts','retrospective_section','prospective_section','human_ruling']`，確認正是四段式（①③②④）、無 Agent-B／revision 殘留欄位。過程中抓到一個測試腳本本身的殘留 bug（`_test_simulate_real_call.py` 印報告那段還在讀已移除的 `agent_b_output` 欄位，導致第一次真實呼叫在核心邏輯成功後於收尾列印時 `KeyError`），已修正腳本、重跑確認乾淨。**至此 D44 決定在文件與程式碼兩側都已完成並驗證，無待辦殘留** | 09-18 |
| D46 | ✅ **文件互審後 code review，抓到並修正兩個真問題**——①**`control0` 臂其實有燒真實 token**：`run_quarter()` 原本把 3a／3b 兩次真實 LLM 呼叫寫在 `if arm == "control0"` 判斷**之前**，導致 control0 每季仍會真實呼叫 2 次（輸出從未被使用），跟緊貼的註解「這是純程式基準線，不燒 token」及 §11.1「對照0：完全不調整」的定義矛盾，也讓 §12.3 的成本上限公式（只算 L2＋L3）低估了實際呼叫數。修法：把 control0 的提早回傳移到呼叫任何 agent **之前**（env／proc／outcome／m1d／diagnosis 仍照算，只是不再進 3a/3b/決策/總結）。②**M4 診斷餵錯欄位**：`simulate.py` 兩處（`run_quarter()`、`run_simulation()` 主迴圈）都用 `outcome["excess_vs_equal_weight"]` 餵 `diagnose.run_diagnosis(excess_vs_ball_history=...)`，但 `monitor.outcome_layer()` 早就算好語意正確的 `excess_vs_ball` 欄位沒被讀——現在兩者數字剛好一樣（A9：B_all＝等權大盤），是巧合正確不是設計正確，已改用 `excess_vs_ball`。兩處修正後重跑 dry_run（3 季＋續跑）驗證結構完整無誤，未再跑真實呼叫驗證 token 用量差異（下次真實跑 control0 時可比對前後用量佐證修好） | 09-18 |
| D47 | 🔴🔴 **§7.3 #6「產業集中現有資料就測得到」的判斷是錯的，已查證推翻**——使用者決定先補上這項診斷再跑正式實驗，但實際查資料庫（`Database("TW")`）發現 `company` 表只有 `id/company_symbol/name/exchange_name/country`，全部 12 張表掃描 `industry/sector/類/業` 關鍵字零命中；`_frozen/`／現有 parquet 產物也沒有類似 `F1_band` 的現成產業欄位。**專案內已有三處程式碼明確記錄這是既有資料缺口**（`stage4_strategy_map.py`／`decision_layer_arms.py`／`ops/tools.py` 的「產業β」功能因同一原因回傳 `None`）——「產業集中」跟「產業β」根本是同一個資料缺口，不是「現有資料就測得到、只是沒寫」。`dataframe.py` 的 `industry_rank()` 依賴 finlab 雲端 API、非本專案資料庫資料，且全庫無呼叫端（死代碼）。**使用者決定**：這次不解決資料源問題，產業集中診斷直接跳過，先進行 2-3 季真實試跑。已更正設計文件 §7.3（表格＋兩處說明文字）、§11.5 限制7、§14 未決事項#7，把「產業集中」從「優先補」清單移到跟因子擁擠同一類的**資料盲區** | 09-18 |
| D48 | 🔴🔴 **3 季小規模試跑（control0＋L2，真實 API，使用者授權）抓到一個真的成本 bug，修好後重跑驗證省下約 4 成 token**——第一次試跑（`_pilot_simulate_3q.py`）D46 兩項修正皆驗證通過（control0 零呼叫、`excess_vs_ball` 正常），但意外發現 `monitor_3a` 用量從第1季 4,429 跳到第2季 11,899、第3季 13,320，不是正常的 memory 增長曲線。追查發現 `agents.build_agent_a_memory_update()` 把整個 `quarter_result`（含 `weights_end`——逐股權重字典，實測 400~500 檔、JSON 序列化 13~14k 字元）當「最近一期全文」存進記憶，每季白白多燒這張 LLM 根本用不到的原始數值表（§8「最近一期全文」原意是保留 agent 自己的敘事，不是原始數值）。修法：`run_simulation()` 存進記憶前濾掉 `weights_end`（checkpoint 本身仍完整保留，不影響稽核）。修好後重跑：`monitor_3a` 三季分別降到 2,889／6,642／7,724（各省 35%／44%／42%），且 D46 兩項驗證再次通過。8 季（L2 臂）真實成本粗估從原本擔心的 1.4~3.5 天大幅下修到約 **0.3~0.4 天標準池額度** | 09-18 |
| D49 | ✅ **正式 8 季實驗（control0＋L2，真實 API，使用者授權）跑完，且產業分類資料已補齊並完成品質查證**。①`_run_formal_8q.py` 完整跑完 16 筆 checkpoint（2臂×8季）：M1-D 在 2024Q4 首次觸發並持續到 2025Q4（跟 §7.4a 記錄一致），L2 臂每個觸發季度都選 A5（升級人工覆核），control0 全程 A0；細部品質分析尚未進行，見待辦。②§7.3 #6 產業集中的資料缺口（D47）已由 `stock_factor_collector`（sibling repo）用 `CollectorFactory/IndustryCollector.py` 補齊，產出 `industry_raw.csv/.parquet`。**品質查證**（不採信 collector 回報文字，逐項重新核對）：覆蓋率 TW 1775/1775、US 2994/2994 皆 100%；缺值僅 13 檔美股（已下市/破產個股，如 BBBY/SI，資料源本身限制非收集問題）；台股 `sector_name` 全空是 FinMind 單層分類的設計本身，非缺漏。**發現並查證 TWSE/TPEx 分類命名不一致**：請 collector 補回原本被丟棄的 `type`（twse/tpex/emerging）欄位並做交叉表，確認 6 組分類是「同一產業、兩交易所各用不同名稱」的乾淨二分（其他電子業/類、居家生活/類、數位雲端/類、綠能環保/類、運動休閒/類、金融保險/金融業），collector 回報裡一處敘述有誤（說「塑膠工業/橡膠工業兩邊都混合」，實際查 crosstab 橡膠工業是 TWSE-only 非混合，已自行更正，不影響最終判斷）。**分工決定**：合併/正規化屬於研究判斷，不該推給 collector——已在 `stock_factor_lab` 這邊新增 `code/app/industry.py`（`load_industry_map()`），把 6 組乾淨二分合併，其餘（化學生技醫療/生技醫療業、塑膠/橡膠工業、電子工業粗顆粒度）刻意不合併並記錄理由；資料檔複製進 `_analysis_outputs_applayer/`（`industry_raw.csv/.parquet`、`tw_type_industry_crosstab.csv`）。驗證：正規化後 43 種分類收斂為 37 種，三組合併後數字（74/30/43）逐一核對正確，零缺值。**尚未完成**：產業集中度診斷機制本身（`diagnose.py` 尚未新增對應機制）——這次只完成資料層準備，是否要接成正式診斷機制待後續決定 | 09-18 |
| D50 | 🔴🔴 **W2c（條件式市值傾斜）前置驗證通過，正式加入動作空間**——回應「跟老師方向對齊」審視發現的核心落差（老師具體驗收標準「把2025喬到賺錢」，原動作空間做不到）。**路徑A（查2000-2026台股歷史找獨立先例）查證失敗**：用跟M1-D完全一致的定義逐月算過全部歷史，唯一超過2024-2025幅度（+4.34pp）的兩段都是同一事件本身延伸，沒有獨立樣本（最接近的獨立事件是2000-2002電子股泡沫+3.02pp、2009-2011金融海嘯後+2.86pp，幅度明顯較小）。**改採使用者提議的路徑**：用2024-2025校準（觸發門檻X本身用2007-2023訂的，沒碰過）、2026 H1（資料到07-24，Q1+Q2皆完整）做真前瞻驗證。機制：M1-D未觸發時α=1不傾斜，觸發時`w∝mktcap`（α=0）。**連續四輪查證，每輪都先假設結果是假的**：①**C2=8%合規性**——未加上限的α=0在多數觸發季違反C2（最高61.36%，單一台積電），加cap-and-redistribute（7%目標、按比例重分配，MSCI 25/50上限型指數同一套邏輯）後7個觸發季全部守住（除③的殘留限制）；②**真實交易成本**（FEE_RATIO=1.425/1000、TAX_RATIO=3/1000，沿用D24-D26已查證的真實成本）——淨改善幾乎沒被侵蝕（校準期毛+23.92%→淨+24.15%），意外發現條件式傾斜換手率（43~60%）反而低於現況基準線（74~85%，這是本次才第一次量到現況真實換手率），因為傾斜後集中在少數大型股比現況每季隨財報重排幾百檔小型股更穩定；③**季中價格漂移**——8%上限下3/5個案例季底又超過（最高9.90%），改7%目標留緩衝後5/6守住，僅1個殘留微幅超過（8.68%），**誠實結論：季度制重新平衡無法100%保證任何時刻都不超過硬上限**；④**重分配規則敏感度**（proportional vs equal-split）——改善幅度仍同量級（+20.06%~+21.27%），結論不依賴這個設計細節。**最終版本**（7%目標＋proportional）：校準期淨改善+23.48pp、前瞻驗證期淨改善+21.98pp。**查證過程意外發現一個更大的既有缺口**：準備接進動作空間時發現`simulate.py`的`run_quarter()`不論agent選A0/A2/A4/A5哪一個，`resolve_weights()`永遠用同一組寫死的`ratio/allocation`——**現有全部動作都只有「決策記錄」，沒有「決策執行」**，不是W2c獨有的問題。使用者決定：這次W2c先跟現有動作維持同一個記錄層級，執行層另列待辦（§8）。已更新設計文件（v19→v20）：§7.5 動作空間表新增W2c、§14未決事項#1部分解決。分析腳本：`_conditional_w2.py`（校準+前瞻）、`_conditional_w2_scrutiny.py`／`_conditional_w2_capped.py`（C2合規性查證）、`_conditional_w2_netcost.py`（交易成本）、`_conditional_w2_sensitivity.py`（重分配規則+季中漂移）| 09-18 |
| D51 | ✅ **動作執行層完成——決策現在真的會改變後續投組建構**（回應 D50 發現的既有缺口）。新增 `simulate.ActiveConfig`（`allocation`＋`last_decision_was_w2c`）跟隨每個臂逐季演進：A2 切換 allocation（`equal↔proportional`，§7.5 持續性規則，會一直維持到再被明確換回去）；W2c **刻意不做成持續性旗標**（跟 A2 不同）——只有「上一季決策剛好是W2c」且「這一季開始時 M1-D 仍是TRIGGERED」兩者同時成立才套用，若下一季 agent 選別的（含A0），或 M1-D 回到 NONE，傾斜自動關閉，不需要額外選一個「關掉」的動作——理由是 W2c 的驗證範圍只涵蓋「逐季重新評估要不要套用」，不曾測過「選一次後放著不管」在正常年份會不會像永遠開啟版W2一樣失敗（D29 的教訓）。`QuarterInputs.uids`（單一名單）改成 `uids_by_allocation`（兩個 allocation 各自的真實名單，來自 `walkforward_members.parquet` 本來就有的兩列，先前沒被讀取）；新增 `resolve_portfolio_weights()` 統一處理「依 config 解權重＋視情況疊加 W2c 傾斜」。**驗證**：①dry_run 全流程回歸通過（含續跑正確從 checkpoint 還原 `config_after`）；②直接比較 equal／proportional／equal+W2c 三種設定算出的實際投組，確認三者持股數與最大單檔權重互不相同（315/336/306 檔，W2c 版本三檔並列頂到 7% 上限，重分配邏輯運作正常）；③config 轉換邏輯（A2切換、W2c gating）獨立跑過所有分支確認符合設計。**範圍界限**（跟使用者確認過）：§7.7「動作後重算登記條件」規則這次不實作——查證後 M1-D 的登記基準（`register_m1d`）本來就只吃指數端資料（`monitor.environment_layer`），不依賴投組持股（D21 已有這個設計決定），所以 A2/W2c 改變投組不會讓 M1-D 的門檻失真，§7.7 這條規則對 M1-D 而言目前是空的，未來若新增依賴投組狀態的登記條件才需要真正處理 | 09-18 |
| D52 | 🔴🔴 **正式重跑撞到第三種 D2 誤判（句點式清單編號），已查證修正**——用新執行層跑正式8季實驗（run_id=`formal_8q_control0_L2_execlayer`）到 L2 第3季（2024-09-30）時，季度總結階段被 D2 攔下「出現數字 3，找不到對應值」。**查證過程**：直接重試同一句話因 LLM 非決定性抓不到同一段文字（重跑一次乾淨過關），改用離線測試（不重打 API）直接驗證假設——`_strip_identifier_digits()` 對「1. 第一點。2. 第二點。3. 第三點。」這種**句點式**清單編號完全沒有中性化（跟 D40 修過的括號式「1) 2) 3)」是同一類問題的第三個變體，`_LIST_MARKER_RE` 原本只涵蓋括號式）。修法：`_LIST_MARKER_RE` 新增分支 `(?<![\d.])\d{1,2}\.(?!\d)`（數字前不能是數字/小數點、後面接句點、句點後不能立刻接數字），用離線測試驗證句點式清單編號正確剝除、且不誤傷真小數（3.14、0.5）與算式（0.012／0.008）。補進 `_test_agents_leakage_fix.py` 情境5（回歸測試），全部五情境通過。**額外發現**：這次真實 8 季重跑裡 L2 第2季（2024-06-30）選了 **A4**（進入觀察名單）——是執行層接上後第一次觀察到非 A0 的真實決策，執行層確實在運作 | 09-18 |
| D53 | 🔴🔴 **正式8季實驗跑完，但發現 W2c 從沒被選過，根因是自己留下的過期資訊**——完整跑完 16 筆 checkpoint（run_id=`formal_8q_control0_L2_execlayer`），L2 在連續 5 個 TRIGGERED 季度（2024Q4~2025Q4）全部選 A5（升級人工覆核），一次都沒選 W2c，導致這次 L2 跟 control0 的實際持股／績效**逐季逐位元完全相同**（已用 checkpoint 資料驗證確認）。**查根因**：重新檢查決策 prompt 發現 `actions.w2c_reference()` 的 caveats 跟決策 system prompt 都還寫著「目前只到決策記錄層級，選它不會真的改變後續持股計算」——這句話是 D50（執行層還沒做）當時寫的**真話**，但 D51 做完執行層後就變成**過期且錯誤**的資訊，忘記回頭修正。Agent 決策時親眼看到系統告訴它「選這個等於沒選」，理性選擇不選並不意外。**修法**：拿掉這條過期 caveat；決策 system prompt 改寫成「選它會真的改變下一季的投組建構（D51 已接上執行層），跟其他動作一樣必須認真評估，caveats 不是『不要選』的理由，是『選的話要在理由裡承認限制』」。**這不是為了引導 agent 選 W2c 而調整語氣，是修正一個真的過期的事實描述**——用新 run_id 重跑驗證這個修正是否真的改變 agent 行為 | 09-18 |
| D54 | ✅ **修正過期資訊後重跑（run_id `_v2`），agent 依然連續5季全選A5，證實保守傾向不只是過期資訊造成的；改用「L3簡化版」模擬人核准，乾淨驗證 W2c 的真實價值**。①重跑後（16/16 checkpoint，過程未再撞到D2）L2 決策序列跟修正前完全一樣（A0×3、A5×5），代表 D53 的修正雖然必要，但不是唯一原因，agent 對「升級人工覆核」的偏好比預期更根深蒂固。②**使用者決定不繼續調整 prompt 去影響自主決策**（避免變成「調到出現想要的行為」，污染 L2 決策記錄的誠實性），改用 `_l2_human_approved_w2c.py`：直接沿用 `_v2` 真實跑出的決策序列（不重打任何 LLM），只在「假設人每次 A5 觸發都核准 W2c」這個明確標註的假設性情境下，重算實際持股與績效，因果時序上核准要到下一季才生效（不能回頭改變已發生的那一季）。**結果**：真實 L2（=control0，8季累計 +7.22%）vs 人核准 W2c 情境（8季累計 +24.70%），**差異 +17.47pp**——比 D50 理想化校準情境（一觸發就立刻套用）保守，因為這裡多了「人要先看到才能核准」的一季時間差，這個保守是誠實的因果限制不是縮水。**這個設計乾淨分離了兩件事**：L2 自主決策的記錄（誠實揭露 agent 系統性選擇保守升級）與 W2c 機制本身的驗證價值（透過模擬的人工核准展示），不互相污染，且完整回應了老師「用同一套策略、把2025喬到賺錢」的驗收標準 | 09-18 |
| D55 | ✅ **§11.3(b) 品質面 rubric 完成稽核**（對 `formal_8q_control0_L2_execlayer_v2` 這次乾淨版本）。**範圍調整**：原設計的「盲評程序」是為了比較 L2 vs L3（避免評分者因為知道來源而偏袒），但 L3 沒有自動化跑（需人機對話介面，未開發），沒有 L3 輸出可比較，這次改成對 L2 唯一的真實決策記錄逐項套用 rubric 標準——是「品質稽核」不是「盲評比較」，範圍窄化並誠實記錄，非造假成盲評。**逐項結果**：①登記門檻選擇品質——8季分布 NONE=3/TRIGGERED=5，非極端，通過；②雜訊vs訊號判斷——逐季狀態轉換用真實 m1d 資料重算遲滯規則，完全符合定義，通過；③矛盾證據的權衡——8季裡 M3／M4 從未同時觸發，本輪沒有真實案例可測，判 N/A（誠實記錄非跳過）；④動作選擇理由——8季（含3季A0）皆有實質理由且抽查偏離數值與程式記錄一致，通過，**但額外發現**8季理由文字從未明確提及 W2c（即使它已在動作空間裡），是誠實揭露的品質缺口而非造假；⑤診斷機制正確性——用§7.4a遲滯規則（新觸發超過p90，或遲滯期維持不誤降級）重新檢查每個TRIGGERED季度，全部正確，通過；⑥陽性/陰性對照——尚未執行（§8待辦#5/6），判 N/A。**過程中自己抓到並修正稽核腳本本身的兩個假陰性**：項目4原本用字串比對偏離數值（"0.0080"對"0.008"，因LLM省略尾零而誤判沒引用），項目5原本用「dev>p90」判斷TRIGGERED是否正確、沒考慮遲滯規則（2025-03-31 dev介於p75~p90，遲滯維持TRIGGERED是正確行為卻被誤判不通過）——都是稽核邏輯太嚴格導致的假陰性，非系統真的有問題，已修正並重新驗證。結果存檔 `_analysis_outputs_applayer/quality_rubric_l2_execlayer_v2.txt` | 09-18 |
| D56 | ✅ **M8（產業集中度診斷）接進 `diagnose.py`，資料層（D47/D49）正式接成診斷機制**。範圍精確界定：M8 測的是**投組自身有沒有無意間集中在單一產業**（狀態警示區，跟M2/M3/M5同一類「我自己跑掉了嗎」），不是 §7.3 原本想像的「資金集中同一產業、反轉時同時被殺」那種外部市場現象（那需要資金流資料，本專案仍然沒有，跟#1因子擁擠同一類盲區）——**只解決了問題的一半，已在設計文件誠實標註**。**實作**：`code/app/industry.py` 新增 `industry_exposure()`／`max_industry_weight()`；新增 `_prelim_industry_concentration_history.py` 算 windows 1-3 歷史分布（n=39，p90=16.39%，跟M3同一方法論）；`diagnose.py` 新增 `diagnose_m8()`，`run_diagnosis()` 新增選填 `weights` 參數（不傳則誠實記錄「本次未提供」而非假裝機制不存在）；`facts_lean.diagnosis_csv()`／`simulate.py` 一併接線。**驗證**：用真實8季資料（`formal_8q_control0_L2_execlayer_v2`）算過，只有 2025-09-30（16.76%）超過p90門檻觸發，其餘7季不觸發（非極端分布，跟M3/M1-D同樣有鑑別力）；dry_run全流程回歸通過。**更新設計文件**：§7.3表格#6從❌盲區改🔶部分涵蓋、涵蓋率統計從「3/2/7」改「3/3/6」；§7.4診斷清單新增M8列。診斷涵蓋機制數：M3/M4/M6/M0/M8共5個 | 09-18 |
| D57 | 🔶 **陰性對照A/B開始執行，今日額度用盡暫停**——使用者確認「陰性A/B」純粹是兩個歷史對照期間的編號，跟Agent-A/Agent-B（已於D44移除）無關，決定兩個都跑完整agent流程（非文件原本留的降級退路）。**陰性A**＝scheme E window 3（OOS 2021-01~2023-12，12季，30檔成員）；**陰性B**＝scheme A window 2（OOS 2015-01~2016-12，8季，35檔成員，跟scheme D window 1日期相同、任選其一）。新增 `_negative_control.py`（參數化的窗次跑法，重用`simulate.py`共用元件，不動`run_simulation()`本身避免影響已驗證的正式實驗）與`_run_negative_controls.py`（launcher）。**過程中抓到自己的操作疏失**：第一次「真實」執行其實完全沒有真的呼叫API——忘記在跑真實版本前清掉dry_run驗證階段留下的checkpoint，續跑邏輯看到「已有紀錄」直接跳過整個run，額度用量跑前跑後完全沒變（708,109→708,109）當場抓到並不採信、清掉假紀錄重跑。**真實進度**：陰性A control0 12/12完成（免費）、L2真實跑完4/12季（2021Q1~Q4，全部NONE/A0，符合正常年份不該觸發的預期）；陰性B尚未開始。跑到L2第5季時額度用盡，`FreeTierExhaustedError`正確觸發並暫停，checkpoint已落盤，明天額度重置後可直接續跑 | 09-18 |
| D58 | ✅ **陰性對照A/B全部真實跑完，結果乾淨通過**（2026-09-19，額度於本機時間約08:00 UTC換日重置後接續）。**陰性A**（scheme E window 3，12季）：control0 12/12、L2 12/12全部完成，**全程NONE/A0，20季（A+B合計）沒有一次誤報**。**陰性B**（scheme A window 2，8季）：同樣全部NONE/A0。逐季核對 `cumulative_deviation` 數值：陰性B最大值僅0.0115（2016-12-31），仍低於p75=0.012門檻，跟A8/D30當初用M1-D門檻邏輯算出「2015-2016只到歷史第70百分位」的結論完全一致，不是巧合過關。合計真實花費171,014 tokens（陰性A剩餘8季＋陰性B全部8季）。**§11.3(b) rubric項目6「陰性對照通過率」現在可以正式判「通過」**（陽性合成對照仍待辦，見§8#6，該項仍是部分完成）。§8待辦第5項完成 | 09-19 |
| D59 | ✅ **陽性合成對照完成，正確觸發並診斷，§11.3(b)項目6全數通過**。設計：M1-D是純指數端量測（`environment_layer()`只吃`mcap_wide`，不依賴投組持股，D21已定案），可以**只合成指數集中度序列**、真實策略持股與報酬仍用window 4真實資料，兩者乾淨分離不用整條管線造假。人工構造100檔股票的市值面板（前20檔equal cap解代數方程、其餘80檔cap=1，可精確解出任意目標q1_weight），設計7季序列涵蓋狀態機全部轉換規則：NONE→NONE→OBSERVING（首次跨p75）→TRIGGERED（跨p90）→TRIGGERED（遲滯維持，dev回落到p75~p90但因prev已觸發不降級）→NONE（跌破p75正確解除）→TRIGGERED（從NONE直接重新觸發，不需先過OBSERVING）。**兩層驗證**：①純Python邏輯層（不連資料庫不燒token）——7季全部跟預期state一致；②真實agent完整管線（`_positive_control.py`＋`_run_positive_control.py`，real API）——L2決策序列 A0/A0/**A4**/A5/A5/A0/A5 完全對應狀態機（OBSERVING→A4、TRIGGERED→A5、NONE→A0皆正確），且**這是第一次真正驗證到OBSERVING→A4這條路徑**——真實2024-2025資料集中度單調上升太快，直接從NONE跳TRIGGERED從沒經過OBSERVING，只有合成資料才測得到這條真實資料覆蓋不到的路徑。抽查兩個關鍵轉折（首次OBSERVING、跌破解除回NONE）的決策理由文字，皆正確引用合成數值（0.865/0.015、0.858/0.008）並做出正確判斷，非矇對。**過程中記得昨天D57的教訓**：dry_run驗證完先清掉假checkpoint才跑真實版本，沒有重犯同一個疏失。§8待辦第6項完成，§11.2陽性/陰性對照全部收尾 | 09-19 |
| D60 | ✅ **多時間尺度解釋（老師9-15「當月→當季→半年→一年」）完成，方案B定案並實作**——使用者確認範圍：底層量測/觸發邏輯維持季度不變（已驗證），新增的是**敘事層級彙整**，不是重建monthly版的k/X凍結整套流程（那是方案A，工作量極大且月頻雜訊比例高，價值存疑，未採用）。**實作**：`monitor.monthly_breakdown()`沿用既有已驗證的`outcome_layer()`，只是切成月頻子區間算（不是新量測邏輯）；`facts_lean.py`新增`monthly_breakdown_csv()`／`multiscale_rollup_csv()`；`agents.py`新增`call_agent_a_multiscale_narrative()`，四個輸出欄位（月/半年/年三段敘事＋**跨尺度一致性欄位**，這是老師要的「一邊觀察哪邊對哪邊錯」的核心，明訂只能引用已提供數字比較、不可自行算新統計量）。**驗證**：monthly_breakdown()先用真實資料核對過（拿正確的季初權重算三個月複合報酬對到季報酬，第一次用錯權重版本查出來的差異已排除，不是bug）；套用到已完成的正式8季（`formal_8q_control0_L2_execlayer_v2`），8季全部真實跑完，半年資料在Q2/Q4/Q6/Q8出現、年度資料在Q4/Q8出現（跟日曆對齊的季度分組完全正確）。**內容品質**：兩個年度彙整季度的跨尺度一致性敘事都抓到具體、有意義的觀察（例如2025-12-31那季正確指出「季度層級未掩蓋月頻中超額轉負的事實」），真的做到老師要的跨尺度一致性檢查，不是空話。§8待辦第7項完成 | 09-19 |
### D65 後續延伸待辦（2026-09-22 使用者確認要做，依優先順序）

| # | 事項 | 狀態 | 為什麼排這裡 |
|---|---|---|---|
| 1 | 找出HRP代表挑選的具體排序規則，直接看機制（為什麼放大到零，不只是統計上像非巧合）| 🔶 **機制查到了，但假設沒證實，見D66** | 成本最低（純讀程式碼），解釋力最強，直接把D65「統計上不像巧合」升級成「這裡就是為什麼」|
| 2 | ✅ 查台積電選中的~219個策略集中在哪個HRP群（是否集中1-2群）| ✅ **完成，見D70，找到真正機制** | 直接證實/推翻「代表挑選系統性冷落特定群」的假設 |
| 3 | ✅ 換一檔權值股測（鴻海2317／聯發科2454），看排除模式是否普遍存在於大型股 | ✅ **完成，見D67，且修正了故事核心** | 測試發現的普遍性——只有台積電特例還是大型股通例 |
| 4 | ✅ 換其他window（1~3）測，不限定window4（2024-2025）| ✅ **完成，見D68** | 測試是長期結構特徵還是這段期間台積電評價特別極端造成的 |
| 5 | ✅ 換美股候選池測 | ✅ **完成，見D69，跟老師觀察吻合** | 跟老師9/15會議記錄「美股沒有這個問題」的既有觀察對照驗證 |

| D72 | ✅ **設計並測試「保底名額」新配額規則，機制本身有效但救不了台積電/聯發科——整條調查線的收尾證據**。設計原則（避免「為了塞台積電硬調」）：業界有正當先例（指數增強策略常見的「前N大成分股保底覆蓋率」追蹤誤差控制手法）；目標是前十大權值股整體覆蓋率，不是專為台積電量身訂做；不取代現有equal規則的5個名額，是額外疊加1個保底名額（該群Calmar品質最高、且選中前十大權值股任一檔的候選人，沒有候選人就不硬湊）。**前十大權值股**（2023-12-31排序）：2330台積電24.78%／2454聯發科2.61%／2317鴻海2.33%／2881富邦金1.52%／2412中華電1.50%／2382廣達1.39%／2308台達電1.31%／6505台塑化1.24%／2882國泰金1.19%／2303聯電1.06%。**結果**：現行規則(30檔)前十大整體覆蓋率6/30(20%)；**加保底名額後(36檔)覆蓋率翻倍到12/36(33%)**——機制本身確實有效，多選中了廣達、富邦金、國泰金；**但台積電/聯發科/中華電/聯電四檔，前後都是0，完全沒有改善**。**原因**：6個群的保底名額規則各自挑出候選人時，全部傾向選中廣達、金控股（相對便宜），沒有一個群挑到台積電或聯發科——**代表即使前十大權值股裡本來就有幾檔容易被價值因子選中，台積電/聯發科依然是這十檔裡評價最極端的兩個異類，連通用的「保底覆蓋大型股」規則都湊不到它們**。**這是整條D65延伸調查線最強的收尾證據**：不是配額規則設計不夠好，是問題核心確實在評價溢價的極端程度本身，配額規則的修正空間已經測到底——再往下真的只能動①因子設計本身，或走②core-satellite，配額規則這條路已經完整驗證過、沒有更多空間。分析腳本`_design_test_guaranteed_megacap_slot.py`，結果存檔`_analysis_outputs_applayer/guaranteed_megacap_slot_test.csv` | 09-22 |
| D71 | 🔴🔴 **測試D70建議的修法（配額改依群大小比例分配），發現系統早就有這個選項（`allocation="proportional"`），且已凍結算好，但實測沒有解決問題，反而略差**。查`walkforward_members.parquet`確認window4/legacy/TW已經有`allocation="proportional"`的凍結結果（跟`allocation="equal"`並存，同一總量30檔，只是6群間怎麼分配名額不同）。**各群名額對照**：equal全部固定5個；proportional依群大小給群3(2336檔)/群2(2258檔)各10個、群6(1497檔)7個、群1(240)/群4(75)/群5(273，密度最高的小群)各只剩1個。**結果**：proportional選中2/30台積電相關策略，equal選中3/30——proportional反而更差。**原因**：雖然把群3名額從5拉到10，但同時把群5（equal規則下密度最高、抓到2/5台積電策略的群）名額從5砍到1，一好一壞抵銷，淨效果是負的。**結論**：「依群大小比例分配」這個直覺解法**系統早就試過（凍結在既有的45窗矩陣裡），已驗證不足以解決問題**，不是沒想到的新點子。往下若要真的改善，需要設計一個全新的、專門考慮大型股代表性的配額規則（不是切換既有的equal/proportional選項），且這已經不是「修bug」，是設計新方法論——使用者決定：先誠實記錄這個負面結果，再往下試看看能不能設計出真正有效的新規則（見下一則） | 09-22 |
| D70 | 🔴🔴 **D65延伸item2（最後一項，補完機制）：重建window4的HRP樹，找到台積電代表被排除的真正機制——不是品質偏見（item1已排除），是「每群固定5名額」配額規則對大群結構性不利**。純本地運算（`research.walkforward_matrix._load_inputs()`＋`build_tree_for_window("TW","2007-01","2023-12",...)`，不需資料庫/MarketData，也不涉及LLM），重用45窗計算同一套函式，未另外重寫。「會選台積電的策略」清單直接讀`_check_tsmc_representative_quality.py`已存的920檔（不重新查DB）。**結果**：樹內6,679檔（經usable_pool/零變異數過濾），6個L1群裡台積電相關策略高度集中——**群3（2,336檔，最大群）裝了全部920檔裡的527檔（59.7%，近六成）**，群5（273檔）73檔（8.3%但該群內密度26.7%，全群最高）、群6（1,497檔）226檔（25.6%），群1／群4完全0檔。**legacy的5個名額分配結果**：群3（0.21%覆蓋率）0/5選中、群5（1.83%覆蓋率，覆蓋率高群3近9倍）2/5選中、群6 1/5選中，其餘0/5。**加總0+0+0+2+1+0=3，跟D65/D67查到的「legacy(30)=10.00%即3/30」完全交叉驗證吻合**，證實整條分析管線內部一致。**核心結論**：問題不是代表挑選對台積電策略有品質偏見（item1的Calmar比較已排除這個假設），是**「每群固定m=5名額、不論群多大」的legacy配額規則，讓裝了近六成台積電策略的巨大群（2336檔）代表覆蓋率只有0.21%，遠低於小群的1.83%——大群天生被配額規則稀釋**，這也解釋了為什麼D65放大到668檔（10%比例）改善依然有限（如果配額仍按群比例而非候選池絕對數量分配，大群還是相對吃虧）。**至此D65延伸的5項待辦全部完成**，機制、普遍性、歷史穩定性、跨市場對照、配額結構根因，五個角度都已交叉驗證且彼此吻合，是一條完整可寫進論文的故事線。分析腳本`_check_tsmc_cluster_concentration.py`，結果存檔`_analysis_outputs_applayer/tsmc_cluster_concentration.csv` | 09-22 |
| D69 | ✅ **D65延伸item5（最後一項）：換美股測NVDA，跟老師9/15會議記錄「美股不會發生這個問題」的觀察吻合，且有具體數字佐證**。同一套機制，8682檔美股候選池，查2024Q1~2025Q4（跟台股window4同期）NVDA選中率。**結果**：8季累計曾選過6.33%（550/8682）——**比台積電的12.91%更低**，但**逐季選中率從未真正掛零**（0.40%~4.91%範圍，每季都有）。**關鍵對照**：台積電window4是7/8季完全掛零的極端二元模式；NVDA即使平均選中率更低，也從未出現這種「整季完全歸零」的情況——跟台股window1-3的歷史模式（也是從未掛零）一致，反而是台股window4才是異常。**結論**：問題核心不是「美股vs台股」的市場差異，是**台股大盤集中度本身遠比美股極端**（台積電權重衝到38.38%，遠超過NVDA在美股候選池對應指數的權重量級）——市場集中度的絕對極端程度才是「季季掛零」這個極端模式出現與否的關鍵，不是選中率高低本身。**這把D65延伸的5項待辦全部完成**，只剩item2（重建window4 HRP樹查台積電策略集中在哪個群，成本較高，待後續決定是否投入）。分析腳本`_check_nvda_us_pool.py`，結果存檔`_analysis_outputs_applayer/nvda_us_pool_check.csv` | 09-22 |
| D68 | 🔴🔴 **D65延伸item4：換其他window(1~3)測，發現「結構性低配」長期存在，但window4(2024-2025)的「幾乎完全掛零」是異常極端值，不是常態**。用同一套7,128檔候選池、同一套F1/F2/C/V機制，逐季查window1(OOS2015-2017)/window2(OOS2018-2020)/window3(OOS2021-2023)。**全期累計「曾經選過」比例四個window驚人地穩定**：window1=12.02%、window2=12.89%、window3=10.87%、window4=12.91%——**跨越近10年、不同市場環境，這個結構性排除比例幾乎不變，證實這是長期存在的方法論特徵，不是2024-2025特有的**。**但逐季分布模式完全不同**：window1-3每一季都有非零選中（範圍0.48%~5.72%，**從未真正掛零**）；window4卻是7/8季**完全掛零**、唯一一季暴衝到9.95%的極端二元模式。**修正後結論**：底層的「結構性低配」（~11-13%累計比例）本來就存在、跨十年穩定，不是新發現的問題；但**window4「季季掛零」的極端程度，在過去10年裡從未出現過**，這跟台積電這段期間指數權重同步創新高（24.78%→38.38%，高於window2最高28.27%、window3範圍24-28%）方向一致，代表2024-2025的評價溢價確實達到歷史級的極端值，把本來就存在的低選中率壓到了歷史從未見過的谷底。**對論文的意義**：不能講「這是本方法論的普遍嚴重缺陷」（結構性低配一直存在但過去沒到過完全掛零的程度），要講「這是本來就存在的結構性特徵，在2024-2025台積電評價創歷史極端值的背景下，被放大到前所未見的嚴重程度」——更精確也更誠實，避免§7過度延伸推翻自己十年來都用的方法論。分析腳本`_check_tsmc_other_windows.py`，結果存檔`_analysis_outputs_applayer/tsmc_other_windows.csv` | 09-22 |
| D67 | 🔴🔴 **D65延伸item3：換其他權值股測，發現問題核心不是「大型股」，是「估值溢價」——修正了整條故事線的框架**。用同一套7,128檔候選池、同一套F1/F2/C/V機制、同一組8個真實as_of日期，換測鴻海(2317)、聯發科(2454)。**結果**：台積電8季累計曾選中12.91%（指數權重24.78%→38.38%）；**鴻海36.95%（反而是台積電的近3倍），指數權重只有2.33%→3.42%（遠小於台積電）**；聯發科9.68%（最低），指數權重2.39%→3.14%（跟鴻海相近但選中率差3.8倍）。**修正後的核心結論**：鴻海是傳產代工股，本益比/股價淨值比低，是真正的「便宜股」，價值型因子邏輯樂於選它進候選池；台積電、聯發科都是評價明顯偏貴的半導體/AI題材股——**不管市值權重大小，被同一套估值倍數篩選邏輯排除的，是「貴」不是「大」**。這比原本D64/D65「大型龍頭股被結構性排除」的講法更精確：正確講法應該是「**評價溢價股**被結構性排除，台積電剛好又貴又是最大權值股，兩個特性疊加讓它的個案最極端、對績效的衝擊也最大，但機制本身是評價驅動，不是規模驅動」。**這個修正對論文敘事很重要**——原本「規模曝險」的框架要跟「評價因子排斥高評價股」這個更精確的機制說法並列，不能只講規模。分析腳本`_check_other_megacaps_exclusion.py`，結果存檔`_analysis_outputs_applayer/other_megacaps_exclusion.csv` | 09-22 |
| D66 | 🔶 **D65延伸item1：找到代表挑選機制，但「品質較差所以被排除」假設未證實**。查程式碼確認機制：`research/walkforward_matrix.py::_pick_a()`呼叫`research/cluster_representatives.py::select_representatives()`——每群內部依Calmar品質分數（IS期CAGR/\|IS期MDD\|）由高到低貪婪納入代表，多樣性門檻只剔除跟已選入者過度相關的候選，不會把低品質但分散的候選拉進來；quality_is只用該窗IS資料，機制本身無前視偏誤。**經驗驗證（用candidate_index.parquet全樣本CAGR/MDD當代理指標，非window4專屬IS期，僅供方向性測試）**：會選台積電的策略（n=920）vs不會選的策略（n=6208），Calmar中位數**完全相同**（0.257 vs 0.257），平均值略低（0.265 vs 0.276）但Mann-Whitney U檢定p=0.0647**未達顯著**。**誠實記錄：「品質較差所以被排除」這個直覺假設沒有得到支持**，比較合理的下一步假設改成「台積電策略集中在特定1-2群，配額被同群其他策略拿走」（=item2，但驗證需要重建window4的HRP樹，成本較高，真正相關矩陣建置，先擱置，使用者決定先做item3-5較便宜的再回頭）。分析腳本`_check_tsmc_representative_quality.py`，結果存檔`_analysis_outputs_applayer/tsmc_representative_quality.csv` | 09-22 |
| D65 | ✅ **切開「是HRP分群的問題，還是整個候選池本身的問題」**（使用者主動提出的問題，D64後續追查）。**方法**：兩層對照，都是純離線查證（沿用同一套`condition_factory`機制），不燒token。①放寬window4的HRP代表池比例（legacy30→1%67→3%200→5%334→10%668，**這些名單`walkforward_members.parquet`早就凍結算好，不需要重跑HRP建樹**）；②直接對**全部7,128檔台股候選策略**（`candidate_index.parquet`裡`market=='TW'`全部列，完全不經過HRP分群/代表挑選）重跑同一套8季查證。**結果**：全部候選池每一季選中台積電的比例是1.67%~9.95%（**從未真正掛零**），HRP window4代表池不管放大到多少（30~668檔）**7/8季依然完全掛零**，只有一季（2025-09-30，全池選中率剛好是8季最高的9.95%）才勉強抓到。**用機率排除巧合**：以2023-12-31為例，全池選中率3.07%，若668檔代表池是接近隨機抽樣，期望值約20檔會選中台積電，實際觀察值是0——這種偏差在統計上幾乎不可能是隨機抽樣的巧合。**結論：兩層問題疊加，不是二選一**——①底層問題（D64已查）：整個候選池本身因為價值型因子邏輯，選中台積電的策略本來就是少數，這是選股方法論的結構性特性，就算完全跳過HRP也存在；②**新發現的第二層問題**：HRP分群＋代表挑選過程，把這個已經偏低的比例**進一步系統性放大到接近完全排除**（多數季度從「少數」變成「零」），代表代表挑選規則（可能是依群內某績效指標排序、只取排名前面者）本身也在避開會選台積電的策略，不只是抽樣運氣。**這開了一個新的、可能值得跟老師討論的方向**：分群代表挑選規則要不要額外考慮「因子多樣性」，不只看單一績效排序——這已經不是「調參數」層級，是分群後選代表這一層的規則設計問題，範圍比C2/W2c更大。分析腳本`_check_tsmc_in_wider_pool.py`／`_check_tsmc_full_candidate_pool.py`，結果存檔`_analysis_outputs_applayer/tsmc_wider_pool_check.csv`／`tsmc_full_pool_check.csv` | 09-22 |
| D64 | ✅ **台積電排除原因查證完成**（回應 D63 結尾提出的後續問題）。方法：window 4（TW/A_hrp/scheme E/legacy/equal，**實際 30 個策略，非先前誤記的50個**）逐一重建 F1/F2/C/V 條件遮罩（沿用 `resolve_strategy_holdings.py` 同一套 `condition_factory`機制），對台積電逐一檢查每個策略在哪一關被刷掉。🔴🔴 **2026-09-22 自我方法論審視抓到真問題並已修正**：原本只測三個時點（2023-12-31登記日／2024-12-31／2025-12-31），講「三個時點都是0/30，機制一致」——但A7早就查到8季裡有1季（2025-09-30）台積電權重0.21%，剛好不在這3個測試日期內，用「0/30」描述整個實驗期間**過度延伸了結論**（違反pitfalls.md第七類）。已改成跟A7同一組完整8個真實as_of日期全部重測，**修正後結果**：7/8季（2023-12-31~2025-06-30）確實0/30全排除；**唯一例外是2025-09-30，3/30策略選中**——且查到這個例外的機制：那一季V濾網（PE低於自己近4季均值）通過率從平常的0%跳到**100%（10/10）**，代表台積電那一季本益比相對自己近期歷史剛好降溫，讓卡在V這關的策略首次通過。這不是巧合，是估值濾網邏輯正確運作下的自然結果，且與A7的0.21%權重時點完全吻合，反而讓機制解釋更完整。**正確講法是「7/8季完全排除、1/8季有明確機制解釋的例外」，不是「0/30恆定排除」**。**兩個決定性、幾乎恆殺的關卡**：①**F1 價值型因子**——P_IC／EV_S／PS／PB 這幾個 band=0/3（=市場最便宜的三分之一）的條件，台積電身為市場龍頭溢價股，這些估值乘數排名必然落在昂貴的那一桶，不是便宜那一桶，F1整體通過率穩定在23.3%（7/30）；②**V 估值濾網（v1）更關鍵，三個時點全部 0/10 通過**——`fcv_core.get_v_mask()`定義是「本季PE低於自己近4季均值、但高於近4季最低值」，這是**相對自己歷史的擇時濾網**，台積電在2024-2025 AI行情裡本益比評價很可能整體上修（估值重估），PE持續高於自身近期均值，天生會被這個「等回檔」邏輯排除。**C動態條件不是關鍵瓶頸**——通過率隨時間反而上升（23%→67%→100%），不是恆定排除因子。**核心結論，直接解釋D63的意外發現**：window 4這30個策略本質是**價值型因子策略**（找便宜股），台積電作為市場最貴的龍頭溢價股，在這套選股邏輯下**被結構性排除是設計的必然結果，不是漏洞或巧合**——這代表**C2上限調多高都沒用**，因為問題根本不在「選中後配多少權重」，是「一開始就沒被選進候選池」，兩個問題彼此獨立、互不影響，完整解釋了D63「調高cap只多縮小4pp」的意外發現。分析腳本`_check_tsmc_exclusion_reason.py`，結果存檔`_analysis_outputs_applayer/tsmc_exclusion_reason.csv`。**這是一個有機制解釋、可以寫進論文的真實發現**——價值型因子在成長股重估的主升段結構性失靈，不是需要「修」的bug。**補充：window 4 實際使用的30個策略組成**（依F1主因子分類，`_analysis_outputs_applayer/tsmc_exclusion_reason.csv`可重建）：P_IC（股價/投入資本）8個、EV_S（企業價值/營收）8個、MOM（動量）4個、PB（股價淨值比）4個、OCF_E（營運現金流/盈餘）3個、PS（股價營收比）3個。**F1通過率23.3%（7/30）精確對應MOM(4)+OCF_E(3)=7**——台積電動量強、現金流品質不差，這兩類因子它其實通過，真正擋住它的是**估值倍數類**（P_IC/EV_S/PB/PS，取最便宜1/3的band=0條件）。**V濾網組成重新查證**：v0=20個（66.7%，多數）、v1=10個（33.3%，少數）——v1不是主力設計，是少數策略疊加的第二道防線；真正影響面最廣的仍是F1的估值倍數篩選 | 09-19 |
| D63 | ✅ **C2 單一股票上限調整驗證**（使用者發現 C2=8% 是本專案自訂值——900格歷史反推，**非法規要求**，真實法規遠寬鬆：台灣舊制10%、新制25%「台積電條款」2026-04-24生效——且 §3.2 已測出等權設計低配大型股是12年以上結構性常數，§3.3 結論「這是一次性政策決定」，故要求驗證調高的效果）。**方法**：沿用 D50 同一套 W2c 機制（α=0市值加權＋cap-and-redistribute）跟四輪查證紀律，唯一變數是cap值：7%（現行）／10%（舊法規）／23%（新法規25%留2pp緩衝）／25%（新法規無緩衝）。**新增比較**：對真實市值加權大盤（taiex_tr）的超額，這是上一輪對話使用者關注的「跟大盤走向比」，原始W2c驗證沒做，這次補上。純離線計算（不燒LLM token）。**結果**：baseline對大盤超額校準期−66.25%／前瞻期−39.05%（大盤同期累積+70.56%／+60.29%，遠超策略）；調高cap確實縮小超額缺口（7%時−42.53%/−16.86%，25%時−38.40%/−19.07%），但**幅度遠小於§3.1靜態算法的預期**（靜態算法算「台積電配到25%缺口幾乎消失+0.27%」，這裡真實機制算出7%→25%只多縮小約4pp）。**意外發現且更根本的問題**：瓶頸可能不是持股上限，是**選股邏輯本身幾乎完全排除台積電**（A7已查出8季有7季投組台積電配置0.00%）——市值加權是把「已選中的股票」按市值配重，若台積電根本沒被選進候選池，上限調多高都沒用。**前瞻期出現反直覺結果**：cap越高前瞻期改善反而略降（22.19pp→19.98pp，樣本僅2季，誠實揭露不確定性）。**季中漂移**：10%上限反而漂移違規最多（3次，且台灣舊制已於2026-04-24被新制取代，不再是現行依據）；**23%（留2pp緩衝）漂移0次**，改善幅度（+27.58pp）幾乎與無緩衝25%（+27.85pp）相同，是目前查證後最站得住腳的候選值，跟現行7%相對8%的留緩衝設計邏輯一致。分析腳本 `_c2_cap_raise_scrutiny.py`，結果存檔 `_analysis_outputs_applayer/c2_cap_raise_scrutiny.csv`。**後續**：已同步啟動「為什麼台積電幾乎沒被選中」的根因調查（`_check_tsmc_exclusion_reason.py`），這比單純調高持股上限更可能是槓桿更大的槓桿點，待結果出爐 | 09-19 |
| D62 | ✅ **L3 人機對話介面完成**（§10 階段6，§8待辦第8項）。使用者從三個實作方案（CLI互動腳本／擴建既有Streamlit UI／這個對話框本身扮演介面）裡選**擴建`app/ui.py`**。**設計取捨**：對話的階段5決策草案**重用已經真實跑完的L2 checkpoint**（`_runs/simulate_{run_id}.jsonl`），不重新呼叫3a/3b/5——那幾階段已被D50~D61完整驗證過，對話只從決策草案接續，不重跑也不污染那份真實紀錄；`decision_facts`用`facts_lean.build_decision_facts()`純函式從checkpoint的`m1d`／`prospective_output`＋一次性的`actions.w2c_reference()`精確重建，不需要額外存一份。**實作**：`agents.py`新增`call_agent_a_dialogue_turn()`（單輪呼叫，沿用`_check_leakage_or_raise`，D2照掃不重寫）＋schema（`response`／`still_recommends_stage5_decision`／`revised_recommendation`／`proposes_out_of_list_option`／`out_of_list_option_description`，明訂清單外選項必須聲明「尚未經程式回測驗證」，且對話不能片面推翻階段5決策，只能誠實表態、由人核准）；新模組`app/l3_dialogue.py`管迴圈控制（≤5輪、session管理、只有真實呼叫才落盤到獨立的`_runs/l3_dialogue_log.jsonl`，dry-run只顯示不落盤，避免測試灌水稽核檔）；`ui.py`新增「監控Agent·人機對話（L3）」分頁，用`st.chat_message`/`st.chat_input`，且刻意放在`holdings is None`的提早返回**之前**渲染——這是獨立子系統，不依賴左側選股引擎的任何設定。**驗證**：`streamlit.testing.v1.AppTest`headless跑過整支`ui.py`（不需要真的開瀏覽器），第一次執行就抓到一個真bug——`list_run_ids()`把`_run_multiscale_narrative.py`沿用同一個`simulate_*.jsonl`命名慣例但欄位完全不同（沒有`arm`/`decision`）的檔案也列進可選項，選到就KeyError崩潰；改用`.get()`容錯＋`list_run_ids()`先過濾只留「真的有L2臂資料」的run_id，修好後AppTest對話輪次（dry-run）完整跑通、chat_message正確渲染。額外把預設run_id指向主線`formal_8q_control0_L2_execlayer_v2`（不是隨機選到陰性/陽性對照組），跟側邊欄「持股規模」預設選legacy同一個`_default_index`慣例。**新增永久回歸測試**`_test_l3_dialogue.py`（3情境：非本模組格式檔案的相容性、decision_facts重建正確性、dry-run schema完整性），與既有`_test_agents_leakage_fix.py`（7情境）一起跑，全部通過。**範圍界線**：這次只接「單一季度的一段對話」，不含清單外選項的實際程式回測驗證（那是agent對話裡明確聲明「須經程式回測驗證」但本次不自動觸發的獨立工作，符合§6對L3的定義本身）|09-19|
| D61 | 🔴🔴 **多時間尺度敘事真實呼叫連續撞到兩個新的D2誤判模式，同一類問題的第四、五個案例，且再次確認「漏網」比「誤攔」更危險**。**案例四**：agent把年份跟季度代號直接連寫成「2024Q3」「2024Q4」（無分隔符）——`_IDENTIFIER_WITH_DIGIT_RE`要求`\b[A-Za-z]`開頭，但「4Q」是數字接字母，中間沒有單字邊界，規則完全比對不到；且「2024Q4」的「4」被攔下當下才發現，但「2024Q3」的「3」完全沒被攔下（被facts別處真實數字3洗白），是D33講的漏網方向的第三個真實再現。修法：新增`_YEAR_QUARTER_RE`用lookbehind精確定位「緊接4碼年份後面」的字母+1~2碼數字，只清掉季度代號部分，年份完全不動。**案例五**：修好案例四、重跑同一次呼叫，緊接著又撞到鏡像模式——agent反過來寫成「1Q、2Q、3Q、4Q」（數字在前字母在後），兩條新規則都比對不到，且同樣是「3Q」的3被洗白沒攔下、其餘被攔下。修法：新增`_DIGIT_QUARTER_RE`比對「1~2碼數字+單一字母」的鏡像形狀。**兩者都補進`_test_agents_leakage_fix.py`情境6/7永久回歸測試**，全部七情境通過。累計至此D2識別碼洗白盲區已修過五種變體（識別碼M6/q1_weight、括號清單1)、句點清單1.、年份+季度2024Q3、數字+季度1Q），每次都是真實呼叫才發現，且屢次證實「漏網」（洗白）比「誤攔」更該優先查——這次兩個案例都是先看到被攔下的那個數字，深查後才發現另一個同類但沒被攔下的數字 | 09-19 |

---

## 7. 🔴 動工前必須知道的三件事

**① 實驗可能沒有足夠的決策點**（設計文件 §11.5 限制 9）
把所有限制疊加後，8 季裡真正產生「投組變更決策」的可能只有 **2–3 季**。
未觸發季度三臂結果完全相同 ⇒ **~138 次呼叫可能只換到 2–3 個有效觀測**，
且 L2／L3 在有限選項下很可能做出相同選擇，**對話價值恐測不出來**。

🔴 **2026-09-17 更新，這個限制比原本想的更嚴重**：A5 前置驗證完成後，
**W1~W3（真正能解決 M1 的動作）已正式棄用**（見 D29）——就算 M1-D 真的觸發，
系統誠實可用的回應也只有「維持現狀／切換 allocation（不解決 M1）／觀察
名單／升級人工覆核」，**沒有通過驗證的投組層級解方**。這代表即使那 2-3 個
「有效觀測」季度真的觸發了，agent 能做的實質選擇也非常有限——不只是「決策
點少」，是「就算有決策點，可驗證的選項本來就窄」。

**最可能的結果是「三臂差異很小」**，屆時論文貢獻應落在**流程與方法論**
（可稽核決策鏈、誠實限制揭露、**驗證閘門正確擋下未經驗證動作**），而非
「agent 讓績效變好」。**這件事必須在動工前接受**，否則做完會失望。

**② 診斷涵蓋率只有 3／12**（設計文件 §7.3）
未涵蓋因子擁擠（**資料上根本測不到**）、因子衰減、產業集中、流動性成本、
波動體制轉換。**不可宣稱「系統能偵測策略失效」**，只能說「能偵測所列的這幾種」。
🔴 **2026-09-18 更正**：原本這裡寫「產業集中現有資料就測得到，最值得優先補」
是錯的判斷（見 D47）——本專案資料庫完全沒有產業分類資料，是跟因子擁擠同一類
的**資料盲區**，不是能快速補的項目。2026-09-18 已請 collector 補齊產業分類
資料並查證品質（D49），資料層已備妥（`code/app/industry.py`），但**診斷機制
本身仍未接進 `diagnose.py`**。

**③ 這是 n=1 的案例研究，不是統計驗證**
單一市場、單一窗次、單一 scheme；8 季是一條路徑上的 8 個檢查點（agent 有跨期記憶
造成路徑依賴），**不是 8 個獨立觀測**；L2／L3 皆不可重現。

---

## 8. 待辦（2026-09-18 全面重整，依建議順序）

> ⚠️ 這份清單取代舊版（舊版 1-17 項全部是前置計算/程式主線的歷程，已於
> 2026-09-17～18 全部完成，紀錄保留在 D1-D49，不重複列出）。以下是**現在
> 往前看**還沒做的事，依建議開發順序排列，附「為什麼排這裡」。

### 🔴 非開發，但應優先處理

| # | 事項 | 為什麼優先 |
|---|---|---|
| 0 | **跟老師報告「2025喬到賺錢」的落差與後續轉折** | 09-18 方法論審視發現老師 9-15 的具體驗收標準目前系統做不到，但**現在已經有解方了**（W2c，見 D50）——這件事完整的前後過程（發現落差→路徑A查證失敗→條件式W2驗證通過）老師都還沒聽過。**越早報告越好**，現在手上是一個完整、有說服力的故事，不是空手去講「做不到」 |

### 開發順序

| 順序 | 工作 | 狀態 | 為什麼排這裡 |
|---|---|---|---|
| 1 | ~~條件式 W2 驗證~~ | ✅ **完成**（D50，見設計文件 §7.5）——連過 C2合規／交易成本／重分配規則／季中漂移四輪查證，淨改善 +23.48pp（校準）／+21.98pp（前瞻）|
| 2 | ~~動作執行層~~ | ✅ **完成**（D51）——`ActiveConfig` 讓 A2／W2c 真的改變後續季度的持股計算，dry_run 回歸＋權重比對測試通過。§7.7「動作後重算登記條件」確認對 M1-D 目前是空規則（M1-D 只吃指數端資料，不依賴投組），未實作，留待未來有依賴投組狀態的登記條件時再處理 |
| 3 | ~~8 季正式實驗的品質面分析~~ | ✅ **完成**（D55）——4項可評分項目全數通過，2項因無真實案例/對照組未跑判N/A（誠實記錄）；盲評程序因無L3輸出無法執行，範圍縮小為品質稽核 |
| 4 | ~~產業集中度診斷機制接進 `diagnose.py`~~ | ✅ **完成**（D56）——M8 已接線並用真實8季資料驗證，只在1/8季觸發（非極端分布），設計文件§7.3/§7.4已同步更新 |
| 5 | ~~§11.2 對照季度：陰性 A、陰性 B 跑完整 agent 流程~~ | ✅ **完成**（D57/D58）——20季（12+8）全部真實跑完，全程 NONE/A0，沒有一次誤報，跟 A8/D30 的門檻校準結論一致 |
| 6 | ~~§11.2 陽性對照（合成指標序列）~~ | ✅ **完成**（D59）——7季合成序列涵蓋狀態機全部轉換規則，真實agent正確診斷，且首次驗證到真實資料沒覆蓋的OBSERVING→A4路徑 |
| 7 | ~~多時間尺度解釋~~ | ✅ **完成**（D60/D61）——方案B（敘事層級彙整，底層量測維持季度不變）已套用到正式8季真實資料，8季全部真實跑完，跨尺度一致性內容品質良好 |
| 8 | ~~L3 人機對話介面（§10 階段6，≤5輪）~~ | ✅ **完成**（D62）——使用者選定「擴建既有 Streamlit UI（`app/ui.py`）」，新增獨立分頁，對話對象是**已經真實跑完**的 L2 決策草案，不重跑 3a/3b/5 |
| 9 | M1-R 回顧區基準鏈拆解正式化（A7 已有一次性分析，未接成 `diagnose.py` 功能）| ⬜ | 技術債，不影響現有功能，但論文若要引用逐季回顧歸因會需要 |
| 10 | §11.6 item 10：用股票層方法重算 B_all，讓 §7.4 拆解跟等權/市值加權同一套量測系統 | ⬜ | 讓已有的歸因表更嚴謹，非阻斷性 |
| 11 | Agent-A 互動式 tool-calling（受限工具集，目前是單次結構化呼叫簡化版）| ⬜ | 效能/架構優化，非阻斷性，現行簡化版已驗證能正確運作 |
