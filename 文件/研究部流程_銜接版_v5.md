# 研究部流程 — Claude 銜接版（v5）

> 用途：給接手的 Claude 快速理解研究部怎麼跑、產出什麼、有哪些坑。執行導向，決策理由各保留一句。
> 人類完整版見「研究部完整流程與圖表運用_v5」，含論證、老師語錄、演進史。

---

## 0. 核心心智模型（不懂這個會全盤做錯）

**百科全書的本體是「資料」（master_index），不是圖。** 圖只在數值篩出候選後才按需調閱，LLM 不是一張張讀熱力圖。

兩層調閱：
1. **數值索引掃全庫（純程式）**：每個策略的 CAGR/MDD/std/EffN… 全進 master_index。這是目錄。
2. **圖像判讀只看少數（LLM/人）**：先用數值篩候選，才對候選調圖判「成片 vs 孤立」。

一句話：**數字掃全部建目錄，看圖只看被篩出的少數。**

**百科全書兩層版本**（解釋「同種圖為何很多張」）：
- 外層 job/spec：market{US,TW} × N{5,10} × 頻率{M,Q} × 窗，命名如 `US_f3_N5_c20_v2_M`。換 job → 整套圖換。
- 內層策略：job 內 F1×F2×C×V 全展開，一列一策略，名如 `F1__F2__C__V`。換策略 → 單張圖換。
- 百科全書 = 全部 job × 全部策略。

---

## 1. master_index：研究部的真正起點

`catalog_builder.py` 併所有 job 的 stats → `_catalog/master_index.parquet`，一列一策略，27 欄四組：

| 組別 | 欄位 | 用途 |
|---|---|---|
| 結構 | F1,F2,C,V,F1_factor,F2_factor,F1_band,F1_N,F2_band,F2_N,C_kind | 檢索鍵 |
| 設定標籤 | market,rebalance,job_label,universe_size,N_list,factors | 外層版本識別 |
| 指標 | CAGR,sharpe_ann,max_drawdown,avg_drawdown,win_ratio,ytd | 數值篩選 |
| 去重/路徑 | is_dup,is_cross_dup,artifacts_dir | 去重旗標＋明細/圖路徑 |

- `artifacts_dir` = 該策略明細（return_table/stock_data）與圖的實體路徑。
- ⚠️ **現況**：只跑過 SMOKE 測試，`master_index.parquet` 尚未正式產出（`_catalog/` 只有 log/manifest）。正式 sweep（估一週量級）跑完才生成。**這是目前最優先要手動跑的東西。**

---

## 2. 研究部 vs 實戰部：事前/事後兩層

- **研究部**：離線、一次性。讀 master_index，用 in-sample 加值，產出**凍結的 strategy_map**。
- **實戰部**：out 期間逐季。Agent0 判當年 regime → Agent1 撈策略 → Agent2 解釋。兩輸入在 Agent1 匯流：① 凍結的 strategy_map ② Agent0 的當年 regime。

**資料切點（已定案）**：in-sample 2000–2025 年底；out 2026 年初迄今（能做多少算多少，畢業前可再跑一次拉長）。strategy_map 在 out 期永久凍結，不得重建（避免 look-ahead）。

**全局鐵則**：實戰部 Agent **只查 strategy_map 現成欄位，不重算、不重看圖**。圖是研究部建圖時的判讀工具，結論已濃縮成欄位。

---

## 3. 研究部四階段

> 順序：0 → 1 → 2 → 3 → 4。階段1 必須先於階段2（理由：對未篩過、可能大量過度配適的策略做市場分類，分出的「這類熊市好」可能只是雜訊）。

### 階段 0 · 資料整備（純程式，不用圖）
- 讀 `master_index.parquet`（全庫索引，非單一 rank 檔）。
- 濾掉：`daily_sharpe`（bug 不採信，見坑#1）、`is_dup`/`is_cross_dup`（重複策略）、換股 <5 的策略。
- 切死 in-sample：return_table/日淨值只取到 2000–2025 年底。
- 產出：乾淨、去重、已切 in-sample 的全庫索引 DataFrame。

### 階段 1 · 策略特性與穩定性（三條需求路徑的共同前提）
三關卡 **A 可信度 → B 穩健度 → C 特性標記**。
（A→B 順序是本研究設計推論，非老師指示；理由：獲利若靠單一飆股，分析參數穩定性無意義。接手者勿當疏漏「修正」。）

**關卡 A · 可信度篩**（真本事還是運氣）

| 來源圖 | 看什麼 | 判準 |
|---|---|---|
| 4-10 Effective N | 獲利分散到幾檔 | 越高越可信（最好用的單一可信度數字）|
| 4-6 Top1 占比 | 最大單股吃掉多少 | 越低越可信 |
| 4-9 累積貢獻曲線 | 前20名累計占比 | 越平緩越可信 |
| 4-4 年度貢獻分布 | 主力個股是否年年換 | 換人＝動態選股，好 |

→ 產出 `credibility_grade`、`effective_n`、`top1_share`。

**關卡 B · 穩健度篩** — ⚠️ **拆 B1/B2，判的是兩種不同的穩健，不可互相取代（坑#2）**

| | B1 因子篩選 | B2 高原驗證 |
|---|---|---|
| 回答 | 這個**因子**強不強、換搭配還撐得住？ | 這個**具體策略組合**是高原還是孤立尖點？ |
| 層級 | 因子（哪個 F、哪個 C） | 策略（某個 F1×F2×C） |
| 用圖 | 4-16 折線（主入口） | 4-13/4-17 熱力圖 |
| CSV 可判？ | 可（線的平均高度） | **否，必須看 PNG**（成片vs孤立是二維鄰接） |
| 產出 | 強因子清單 | `stability_grade` |

- **B1（主入口，老師指定，可先出一版結果）**：4-16 折線。直看（固定 F，看8條C線高度）找「配任何C都好」的**強F**；橫看（追一條C線在所有F下的位置）找「換什麼F都能發揮」的**穩C**。
- **B2**：先數值掃全庫算高原寬度分數（`bucket_F1/F2` 的 std、`deltaC_summary` 的 iqr，小=穩）篩候選 → 只對候選調 4-13/4-17 判「成片(高原,穩)vs孤立(尖點,過度配適)」。
- ⚠️ **跑完 4-16 ≠ 做完關卡B**：4-16 只給強因子清單，`stability_grade` 要靠 B2 才能填。
- ⚠️ 美股系統目前無 4-16 型圖，需自行補做（見待辦 E）。

**關卡 C · 特性標記**（不篩掉東西，只貼標籤供分流）

| 維度 | 從哪來 | 標籤範例 |
|---|---|---|
| 因子類型 | F/C/V 設定 | 體質型/動能型/估值型 |
| 報酬形態 | 4-3 淨值曲線 | 穩定爬升/大起大落 |
| 風險形態 | 4-3 回撤、MDD | 淺回撤/深回撤 |
| 環境適配（初標） | 逐年報酬×regime | 牛強/熊抗跌/全天候（階段2精修）|

**階段1 總產出**：可信 + 穩健 + 已標記特性的策略池。

### 階段 2 · 市場分類（regime dating）
- 前提：對象是階段1已篩過的策略。
- ⚠️ **全域一套 regime（台美共用），不各市場獨立標**。這是**先求簡化的初步決策**，非方法論必然。代價：在地差異被強制對齊（如2022美股熊市/台股波段）。若階段3發現「共跌」判斷高度受在地狀態影響，再改各一套。（接手者勿當疏漏「修正」。）
- 程式：用大盤基準或階段1代表策略逐年報酬跑 bull-bear dating。
- 少量看 4-3 淨值曲線人工核對轉折點（2000/2008/2020/2022）。
- LLM 為每個 regime 命名+定義。
- 產出：單一 `regime_table`（台美共用）+ 各策略 `regime_fit`。

### 階段 3 · 策略互補性（跨市場，要自己補做）
- ⚠️ 第四章無現成跨市場圖，資料自己生（判讀邏輯沿用既有圖種）。
- 這是整個研究的支柱：單一市場（尤其台股）內策略在系統性危機時一起跌，分散化無意義，必須靠跨台美才能真互補。
- 程式：用 master_index 定位台美同 recipe 策略（台美共用 3 因子 ROE/EPS/FCF_P，是「同一份 recipe 各跑一次」），逐年報酬算跨市場相關矩陣 + Effective N。
- 少量：對候選互補對調 Top1 貢獻（4-5/6）確認非靠同批少數股。
- LLM：因果互補解釋 + 標系統性風險（2008 全球同跌→互補失效）。
- 分工界線：互補性「計算」屬研究部；「情境解釋」屬 Explainer Agent。
- 產出：`complement_partners` + `co_fail_regimes`。

### 階段 4 · 歸納成 strategy_map
- ⚠️ **不從零建新表**，在 master_index 骨架上補加值欄位。
- 新增欄位：`credibility_grade`、`effective_n`、`top1_share`、`stability_grade`、`regime_fit`、`valuation_verdict`、`complement_partners`、`co_fail_regimes`、`return_story`。
- `valuation_verdict`：把 4-21/22/23/24 的估值條件性判讀（「加估值划不划算高度依背景而變」）預算成單一欄位，讓 Agent 只查欄位。值域 `favorable`/`unfavorable`/`neutral` + 條件註記（如「僅 ROE5-10 背景成立」）。
- `return_story`（最危險）：內化強制競爭假說，每個歸因給一對立解釋，用 Top1/EffN 數字裁決。建議先人工做一版當 ground truth 再讓 LLM 複現。

---

## 4. strategy_map 欄位表（= master_index 骨架 + 研究部加值）

| 欄位 | 來源 | 對應圖 | 給誰 |
|---|---|---|---|
| strategy, F1/F2/C/V, market, N, rebalance | master_index 既有 | — | 檢索鍵 |
| CAGR, max_drawdown, win_ratio | master_index 既有 | 4-2 | Agent1 |
| artifacts_dir | master_index 既有 | — | 調閱明細/圖 |
| `factor_type` | 加值 階段4 | 4-12/4-16/4-21 | Agent1 |
| `regime_fit` | 加值 階段1C初標+階段2精修 | 4-3 | Agent1 |
| `stability_grade` | 加值 **階段1 關卡B2** | 4-13/4-17 | Agent1 |
| `credibility_grade` | 加值 **階段1 關卡A** | 4-5~4-10 | Agent1/2 |
| `effective_n` | 加值 **階段1 關卡A** | 4-10 | Agent1/2 |
| `top1_share` | 加值 **階段1 關卡A** | 4-6 | Agent2 |
| `valuation_verdict` | 加值 階段4 | 4-21/4-24 | Agent1/2 |
| `complement_partners` | 加值 階段3 | 自補 | Agent2 |
| `co_fail_regimes` | 加值 階段3 | 自補 | Agent2 |
| `return_story` | 加值 階段4 | 4-2/4-5~10 | Agent2 |

> ⚠️ 階段標註以此表為準。effective_n/top1_share/credibility_grade 來自**階段1關卡A**，stability_grade 來自**階段1關卡B2**。舊版曾沿用階段對調前的編號誤標為階段2/3。

---

## 5. 三個 Agent 讀什麼（實戰部，只查欄位）

- **Agent0 Macro**：讀當年總經 + `regime_table`。不讀圖。→ 當年 regime。
- **Agent1 撈策略（匯流點）**：讀 ①當年 regime ②strategy_map 的 regime_fit/factor_type/stability_grade/max_drawdown/effective_n。→ 候選策略組。
- **Agent2 Explainer**：讀 return_story/complement_partners/co_fail_regimes/top1_share/effective_n + regime。→ 因果說明 + 失效警示。

---

## 6. 三條需求路徑（實戰部 O2 的 pandas query 腳本）

> 起點：階段1 產出的策略池。O2 是工具節點，不是 Agent。論文核心做保守/積極兩種，全天候列延伸。

**6.1 保守型（風險優先）**
1. 下檔保護：查 `valuation_verdict=favorable` + `mdd` 上限。
2. 危機年表現：查 `regime_fit` 確認 2008/2020/2022 沒崩（整段 MDD 小可能只是運氣避開危機）。
3. 分散性加嚴：`effective_n` 門檻高於共同前提。
4. 估值條件確認：查 `valuation_verdict` 條件註記。
- ⚠️ Step1/4 是查欄位不是臨場看圖（4-24 條件反轉 Agent 臨場做不到）。

**6.2 積極型（報酬優先但防假）**
1. 報酬排序：`CAGR`。
2. 假 alpha 檢查：查 `top1_share`/`credibility_grade`，**排序越前段門檻越嚴**（4-5/6 的健康分散是「前25%」的結論，不能套到任一策略；4-7/8 證明越後段假 alpha 越多；高報酬正是假 alpha 最愛偽裝處）。
3. 高原確認：查 `stability_grade`。
4. 找強組合：查關卡B1 強因子清單（強F×穩C）。

**6.3 全天候（跨背景穩健優先，延伸）**
1. 跨背景穩健：4-16 橫看找「配任何 F 都好」的 C。
2. 跨 regime 表現：逐年報酬×`regime_fit`，各狀態都不掉後段。
3. 互補組合：跨市場相關 + `co_fail_regimes`（單一策略難全天候，靠組合達成）。

**同一張圖三種問法**（需求路徑分類的價值）：

| 圖 | 保守型 | 積極型 | 全天候 |
|---|---|---|---|
| 4-16 折線 | F/C 會不會一調就崩？ | 高報酬是高原還是尖點？ | 哪個C在所有F下都不錯？ |
| 4-10 EffN | 夠分散扛黑天鵝？ | 高報酬靠少數股？ | 分散性各環境穩定？ |
| 4-21 四象限 | 落報酬↓風險↓？**（核心）** | 落報酬↑？（次要） | 各背景都划算？ |
| 4-3 淨值/回撤 | 危機年撐得住？**（核心）** | 多頭跟得上？ | 各年不掉隊？ |

---

## 7. ⚠️ 已知的坑（不知道會做錯）

1. **daily_sharpe bug**：傳入價格序列而非報酬序列，數值異常偏高。已決定隔離不修，階段0 直接濾掉此欄，勿採信。
2. **4-16 ≠ 做完關卡B**：見階段1關卡B。4-16 只給強因子清單，stability_grade 要靠 B2 熱力圖。
3. **成片vs孤立 CSV 判不出**：是二維鄰接關係，B2 必須看 PNG。
4. **strategy_map 不從零建**：在 master_index 上加值。
5. **Agent 不看圖只查欄位**：所有圖判讀結論都要在階段4濃縮成欄位，否則 Agent 執行不了。
6. **美股倖存者偏誤**：目前 universe 有此問題，寫進 limitations。
7. **全域 regime 與 A→B 順序是刻意決策**，非疏漏，勿「好心改掉」（見階段1、階段2）。

---

## 8. ⚠️ 待決事項（尚未定案）

| # | 事項 | 建議 |
|---|---|---|
| A | 各 Step 門檻值（EffN 多高？MDD 上限？高原前X%？）| 用**分位數非絕對值**（絕對值在台美/不同N分位不通用）|
| B | 篩太乾淨剩0個 | 「先嚴後鬆」：記錄每關淘汰數，不足N個時放寬最不關鍵一關 |
| C | 需求類型數量 | 核心做保守/積極，全天候延伸（過多會路徑爆炸）|
| D | 關卡C 環境適配標籤時機 | 階段1粗標，階段2 regime 完成後精修 |
| E | **4-16 型圖尚未產出（美股）** | **最優先補做**——老師指定主入口 |
| F | Critic Agent 去留 | 待與指導教授確認（暫折衷融入 Explainer）|
