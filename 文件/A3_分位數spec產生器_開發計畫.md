# A3｜分位數 spec 產生器 開發計畫（v6，方案 A，資料窗定案）

> 範圍：**產生器 + 產出 spec + condition_factory 純新增 q_band**（IS/OOS 另立）
> 已定案：**方案 A — 橫斷面每期排名**；**宇宙＝全美股**（S&P500 待老師確認前不縮）
>
> **資料窗定案（2026-07）**：US **2000–2026**、TW 2000–2023。
> - collector 端 probe 證實：FMP 乾淨基本面深至 1994/1986、DB 已有（3654 家 2016 前有 filing_date），
>   「只到 2016」是讀取端常數，非資料/collector 問題（見 collector `UPDATE_US.md §0.5`）。
> - 已把 `database.py` 的 `MARKET_PRICE_START` US→2000、`MARKET_REPORT_START` US→1998（2年緩衝）；台股不受影響。
> - **P0-1 SQL bug（`date > 2018-01-01` 整數算術失效）已修**：改市場感知字串比較，US 限窗、TW 逐列不變。
>
> v6 要點：
> 1. **前瞻（跨公司）**：逐碼驗證安全；US `dropna(filing_date)` + collector 前瞻守衛（filing_date>期末）
>    雙保險，橫斷面只用乾淨值。加「實測斷言」guardrail。
> 2. **倖存者偏誤：DB 驗證『無』**——母體含數千已下市公司存活期資料（非現存 S&P500）。Q6 解決。
> 3. **深歷史對 A3 安全**：方案 A 是**每期橫斷面排名（非池化）**，各期只跟同期同儕比，
>    2000 與 2026 混在 frame 不會扭曲任一期的「前 20%」→ 放深窗不重啟 P0-1 的池化污染疑慮。
> 4. **宇宙＝全美股 ~6500 家**（非 498 S&P500）；效能估見 Q4，窗放 2000–2026 後回測再 ×~2.7。
> 5. **band 邊界小 n**：n≈6500≫5，可忽略；佔比驗證輸出有效 n。
> 6. **方向2 的代價**：發散條件型別＝**犧牲跨市場可比**，標紅讓老師知情再選。

---

## 目標

把 P1（F 構面）4 個体质因子（ROE/EPS/FCF_P/DEBTRATIO）的手寫固定門檻，
換成 **橫斷面分位 band**，讓「同一個桶」在台美代表 **同一相對位置**，
達成跨市場可比、且 **不新增前瞻偏誤**。

---

## 🧭 台美 spec：預設共用、結構可分（保留老師兩方向）

**一份 config → 產出 `spec_TW.json` / `spec_US.json`。不用單一實體檔。**

| 老師方向 | 意思 | 支援方式 | 跨市場可比？ |
|---|---|---|---|
| 方向1：同 recipe、各市場跑再比 | 台美相同因子定義，差別只在宇宙 | TW/US 指向同一 config | ✅ 因子定義層可比 |
| 方向2：各市場特性設不同條件再比 | 台美因子/條件/N 可不同 | 只改 `CONFIG_BY_MARKET["US"]` | ⚠️ 見下方警告 |

> **🔴 方向2 的代價（v4 補）**：一旦台美用**不同條件型別**（一邊 q_band 排名、一邊固定門檻），
> 「同一個桶＝同一相對位置」的**跨市場可比性即失效**——因為一邊是相對排名、一邊是絕對水準。
> 此時比較只剩「各市場各自最佳策略」的**結果層對照（報酬/Sharpe）**，不是因子定義層可比。
> 老師若選方向2，論文「可比」論點的口徑要跟著改。**方向2 不是零成本彈性。**

---

## 步驟

### 1. 擴充 `condition_factory.py`（純新增 `q_band`）
- `q_band_factory(k, N)`：**直接對日頻 ffill frame 做橫斷面分位**
  `pct = s.rank(axis=1, pct=True)`，回傳 `(pct > k/N) & (pct <= (k+1)/N)`。
- **時間軸＝換股日橫斷面（正確）**：已逐碼確認——
  US 以 `filing_date` 為索引（`dropna(filing_date)`）、TW 經 `adjust_index_of_report`
  位移到公告可得日，兩者 `get()` 只 `ffill` 向前 → 任一天每格都是「該公司該日已公告的最新值」，
  ffill 不會把未來值往回帶 → **跨公司排名無前瞻（by construction）**。
- **不使用 `_compress_report_updates`**（那是 P3 時序型 `_q` 用的，橫斷面用會錯位）。
- **效能（v5 上修）**：rank 作用在財報因子欄位（美股實測 **≈6500 家 full-US**，非先前估的 498）。
  **每因子只算一次 rank 並快取，5 band 共用**——否則 4 因子×5 band×~2000 日會重算 20 次。
  快取後 4 次 rank（各 6500×~2000）數十秒級可接受。
  **快取要用安全實作**：`WeakKeyDictionary` 以 frame 為 key（隨 frame 回收而清），
  或讓 5 個 band 由同一份 closure 共享預算好的 rank；**不可用會殘留的全域 `{id(s):…}`**（id 回收再用會取到舊 rank）。
- **⚠️ filter-before-rank（新，來自 code 檢查）**：現行 `fcv_us.py` 是「masks 建在 full ~6500 欄 → 之後才把
  position 對齊 `common`」。今日無子宇宙篩選故無誤；但**一旦宇宙 knob 加上流動性/市值/成分股篩選，
  必須先把財報 frame 限縮到該子宇宙、再讓 q_band 排名**，否則會變成「全 US 排名後再交集篩選」，
  桶不再是「篩選宇宙內的前 20%」。
- **小 n 邊界**：n≈6500≫N=5，桶0=(pct>0)&(pct≤1/N) 正常；僅當某日有效家數 n<N 才會失真
  → 於佔比驗證輸出有效 n 監看（見步驟3）。
- 註冊進 `CONDITION_FACTORY`，`auto_name` 加分支（如 `ROE_qb4of5`）。
- **隔離保證**：完全不碰 gt/lt/between/is_largest/rise。

### 2. `spec_generator.py`
- `CONFIG_BY_MARKET`：預設 TW/US 指向同一 config（方向1）。
- P1：對 4 因子各輸出 N=5 個 `q_band`（k=0..4）。P3 原樣帶過。
- 產出 `code/spec_TW.json`、`code/spec_US.json`（檔頭標 generated, do not edit）。

### 3. 驗證（v4 加嚴）
- **round-trip**：`build_conditions()` 讀回，每條建得出函式。
- **實測無前瞻斷言（新）**：任取一換股日 d，對每檔取其該格值的來源 `filing_date`
  （US）/位移公告日（TW），斷言全部 `≤ d`。這是「橫斷面無前瞻」的**充分必要驗證**，
  不只靠推論。
- **實際佔比 + 有效 n（新）**：band 條件套小樣本，確認每換股日選中比例 ≈ 1/N（≈20%），
  並輸出每日**有效家數 n**，標記 n 太小的日子。
- **台股回歸 guardrail**：加完 q_band 後，用**現有固定門檻 spec** 跑台股，
  結果須與改動前 **byte-identical**（證 q_band 純隔離）。

### 4. 描述用對照表（非回測）
每換股日取 `np.nanpercentile(該日跨公司值, k/N·100)` 再對時間彙總，
印「各市場×因子×band 的實現絕對門檻」（如 US ROE p80≈? vs TW≈?）。無前瞻、不進回測。

---

## ✅ 倖存者偏誤（Q6）——已用 DB 實測，結論『無偏誤』

- **v3 誤述**：「NaN 公司 rank 為 NaN → 天然處理倖存者」。**錯**——rank-NaN 只保證下市『後』不被選，
  是否無倖存者偏誤取決於**母體是否含下市公司存活期資料**（資料層性質）。
- **實測結果**（`code/check_surv.py` + `check_q6.py`，2026-07 DB 快照）：
  - US 財報公司 **8593 家**（非 498）；`get("report:roe")` 實得 **6522 欄**；filing_date≥2018 宇宙 **6515 家**。
  - **關鍵（母體偷換之疑的決定性回答）**：只看 `dropna(filing_date)` 後『實際進 rank』的宇宙，
    其中 **窗內退場(中途下市) 1464 家(2018–2023) + 439 家(2024–25H1) ≈ 1903 家**，仍存續 4612 家。
    範例：**ABMD**(Abiomed，2022 被 J&J 併)、**ACC**(2022 併)、**AERI**(2022 併)——
    **下市公司帶有 filing_date、落在『留下』那批**，存活期進排名、之後退場 → **留下的宇宙本身就無倖存者偏誤。**
- **前瞻交叉確認**：US factorvalue 共 632 萬列，其中 **404 萬列 filing_date=NULL**（疑為舊資料），
  US 路徑 `dropna(filing_date)` 已自動濾除；被濾掉的是 NULL-only 名單，**非那 1903 家下市公司**。

### ⚠️ 由實測帶出、需你/老師確認的兩點
1. **宇宙定義**：實際排名宇宙是 **full-US ~6500 家（含微型股）**，非「S&P500 500 家」。
   「前 20% ROE」會含大量不可交易微型股。是否加 **流動性/市值 篩選** 或限定成分股 → 老師的宇宙問題。
   **A3 的 q_band 與此解耦**（宇宙篩選在 runner 上游），可先建；宇宙當一個 knob。
2. **資料來源**：DB 有 16 因子、深歷史到 1986、404 萬列無 filing_date → 疑混入你交代忽略的舊學長姊美股資料。
   `dropna(filing_date)` 後留 6515 家——**請確認這批（有公告日者）全是新 collector 抓的、可信。**

---

## 開發前審查回應（8 題，v4 更新）

| # | 問題 | 處置 |
|---|---|---|
| 1 🔴 | q_band 純新增、台股隔離 | 純新增；台股回歸 byte-identical 證隔離 |
| 2 🔴 | 橫斷面前瞻 | **逐碼驗證安全**（filing_date/位移日 + 只 ffill 向前）＋實測斷言 guardrail |
| 3 | spec 表達 band | `{"type":"q_band","args":[k,N]}`；`build_conditions` 泛型零改；對照表另算 |
| 4 | 效能/宇宙 | **實測 ≈6500 家 full-US**（非 498）；務必快取 rank（否則重算 20 次）；數十秒級可接受 |
| 5 | EPS/FCF_P 的 0 錨 | 留 `PIN_ZERO` 開關，跑 sign-split 對照桶0 成分再定 |
| 6 ✅ | 倖存者偏誤 | **已實測『無』**：8593 家含數千下市公司存活期資料；非現存 S&P500 |
| 7 🟢 | yoy_gt 跨市場語意 | P3 原樣，文件記一筆 |
| 8 🟢 | `_compress_report_updates` | q_band **不重用**它（僅 P3 時序條件用） |
| +🔴 | 方向2 犧牲可比 | 標紅：發散條件型別＝失去因子定義層可比，論文口徑要改 |

---

## 設定參數（`spec_generator.py` 頂端）

| 參數 | 預設 | 說明 |
|---|---|---|
| `N` | `5` | 分桶數 |
| `P1_FACTORS` | `{ROE, EPS, FCF_P, DEBTRATIO}` | 體質因子→欄位 |
| `P3_TEMPLATE` | 複製現有 P3 | 動態 C 原樣 |
| `CONFIG_BY_MARKET` | TW/US 同一 config | 方向2 時讓 US 發散（注意可比代價） |
| `PIN_ZERO` | `False` | sign 有意義因子是否釘 0（對照用） |
| `US_START` | `"2018-01-01"` | 美股資料/回測起點 |

---

## 待辦 / 下一步

- [x] ~~DB 跑 `check_surv.py` 確認倖存者偏誤~~ → **已驗證『無偏誤』**（8593 家含下市）。
- [ ] **確認資料來源**：filing_date 非 NULL 的 6515 家是否全為新 collector 資料（排除舊學長姊美股）。
- [ ] **宇宙定義**（老師）：full-US ~6500 家 vs S&P500 vs 加流動性/市值篩選——A3 解耦，當 runner knob。
- [ ] 跑分位數版全量 FCV，與固定門檻版對照；PIN_ZERO 對照。
- [ ] IS/OOS 樣本內外切分（另立計畫）。
- [ ] （附帶發現）`get_finance_report` 的 `date > 2018-01-01` 是整數運算 bug（實際≈2016 且未生效），
      DB 才會回到 1986 資料；runner 另有 US_START 過濾故回測窗仍正確，但此濾條建議修。
