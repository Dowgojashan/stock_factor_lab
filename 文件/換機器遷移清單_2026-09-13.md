# 換機器遷移清單（2026-09-13）

> 這是**這次**換機器要看的清單。上一份 `換機器遷移清單_2026-09-09.md`（重灌那次）
> 內容大部分還適用（資料庫怎麼裝、機密文件清單、封存區數字），這份**不重複抄一遍**，
> 只列：①這次跟上次不一樣、必須額外處理的東西 ②快速核對用的完整清單（含引用上一份
> 沒變的部分），照這份走一次就好，不用兩份分開看。

---

## 一、🔴🔴 這次最容易漏掉、上次沒有的兩件事

### 1. `code/app/_runs/audit_log.jsonl`——已 gitignore，不會跟著 git clone 走

**這是全部要遷移的東西裡唯一「遺失了無法用同樣方式重造」的檔案。**

- 現況：23 筆紀錄（3MB），2026-09-13 查證
- 內容：§9.8 前瞻驗證的 9 組正式紀錄（2026-01-01／03-31／05-15 × TW/US/XM，含
  `risk_flags`）＋ 補 schema 前的舊 9 組（保留備查）＋ S7 層三重複穩定度測試的
  5 次呼叫
- **為什麼遺失了救不回來**：這批紀錄的價值在於「LLM 在完全不知道後續發展的情況下
  寫的解釋」——選這 3 個日期就是刻意挑在 GPT-5 訓練截止日（2024-09-30）之後，
  確保資訊隔離。重新呼叫 LLM 只能得到「現在才寫的」新解釋，**不再是同一批乾淨的
  前瞻驗證證據**，這條「預測先於結果」的稽核鏈條一斷就接不回去。
- **動作**：把整個檔案複製到新機器的同一個路徑
  （`code/app/_runs/audit_log.jsonl`）。沒有這個檔案，`app/audit.find_previous()`
  也會找不到「上一期」，`change_note`／`diff_from_previous` 會在新機器上全部
  顯示「沒有上一筆可比較」，不是壞掉，是資料沒帶過去。
- ⚠️ **順便決定一件事，不是這次必須解決但要有意識地擱置**：這類真實驗證紀錄
  只存在單一機器上、`.gitignore` 排除、沒有備份機制，長期是不是該考慮至少把
  §9.8 這種正式驗證批次的紀錄另外存一份到版控裡（例如另開一個不受 `_runs/`
  規則排除的子路徑）——`應用層開發追蹤.md` §9 有記這個待決事項，還沒有答案。

### 2. 6 個未 commit 的檔案——這次對話的全部產出

執行遷移前**必須先 commit（並建議 push）**，否則這些改動只存在這台機器的
工作目錄裡，`git clone` 到新機器不會帶走：

```
code/_catalog/llm_usage.jsonl
code/app/explain.py
code/app/memo.py
code/app/stability.py
code/app/ui.py
文件/應用層開發追蹤.md
```

內容摘要（commit message 可以參考）：解釋 agent 加第 11 欄 `risk_flags`（§9.8
層四）＋ D2 掃描修正兩個真 bug（facts 格式化不一致、正則誤判範圍記法為負數）＋
API timeout 調到 300s＋績效衡量模組（`performance.py`）接進 UI（新增「已實現
績效」區塊，含真實市值加權大盤對照）＋ S7 四層驗證標準全部執行完畢的記錄。

**確認方式**（新機器上跑，兩項都要對）：
```bash
git log --oneline -1               # 應該看得到剛才 commit 的內容
git status --short                 # 應該乾淨（沒有 M 開頭的檔案）
```

> ⚠️ 這份文件本身（新寫的 `換機器遷移清單_2026-09-13.md`）跟同一批更新的
> `現況銜接.md` 也還沒 commit——一併加進去。

---

## 二、快速核對清單（含上次沒變的部分，照順序走）

### 🔴 最關鍵——遺失了很難救回來

- [ ] **`code/app/_runs/audit_log.jsonl`**（見上方第一條，這次新增的關鍵項）
- [ ] **本機 MariaDB 資料庫本體**——如果新機器要重新灌資料庫，用 `mysqldump` 匯出
      （做法同上次，見 `_2026-09-09.md` 完整步驟）；如果新機器是直接搬整台電腦或
      磁碟（不是全新安裝），資料庫服務本身可能就在，只需要確認連得上
- [ ] **`config.ini`**（gitignored，真實 DB 連線資訊 + OpenAI API key）
      → 手動複製，內容不能貼給任何人看

### ✅ 不用管——git 會帶走

- [x] `_frozen/`（已進 git，92 檔／97MB）
- [x] 這次修改的 6 個檔案（**前提是已照上面第一節 commit 完**）
- [x] `code/_catalog/llm_usage.jsonl`（跟上次不同——**這個現在是 git 追蹤的檔案**，
      不是 gitignore，只要有 commit 就會跟著走）
- [x] 其他所有 git 追蹤的東西（`code/`、`_analysis_outputs_*`、`文件/` 等）

### 🟡 機密文件——gitignored，要手動複製

同上次，逐一確認新機器上有沒有：
- [ ] `9-8咪挺.pdf`（老師會議逐字稿，根目錄）
- [ ] `逐字稿 (1).docx`（根目錄）
- [ ] `郭鎧菘_結合量化交易與多代理人決策之金融交易框架初探.pdf`（不可散布/上傳）
- [ ] 學姊余姵穎的論文 PDF（不在 repo 內，找上一台機器實際存放的路徑）

### 🟢 選擇性——不是必須，但省得之後重算

這次對話在 Claude 的 scratchpad（機器/session 專屬暫存資料夾，**跟 git 無關，
新機器新對話框會是全新空白的**）留了不少分析腳本跟中間產物，例如
`measure_chained.py`／`performance_chained.json`／`stability_TW_2026-01-01.json`
等。**這些都不是必須帶走的東西**——關鍵發現與數字已經寫進
`應用層開發追蹤.md` §9.12~§9.16，之後要查數字看那份文件就好；真的要重算，
腳本邏輯不複雜、重寫也快。**如果不確定要不要留，不用特別處理，這次就讓它留在
舊機器，不影響新機器繼續工作。**

### 🟡 封存區──沿用上次的盤點，這次沒有重新實測容量

`code/results_artifacts/`（189GB）、`/_archive/`（158GB）等封存資料夾，
2026-09-09 已盤點過「不搬不影響現在/未來跑程式，只影響能不能重跑 stage0 或
查舊逐日交易明細」，這次沒有變動也沒有重新實測，**細節與影響評估直接看
`換機器遷移清單_2026-09-09.md` 第二節**，這裡不重複貼一次容易過期的數字。

### 🟢 不用管——新機器自己重建

- `.venv/`（`requirements_clean.txt` + `setup.bat`）
- `__pycache__/`
- `TA-LIB/`（已進 git）

---

## 三、新機器上的驗收順序

1. `git clone`（或如果是同一份工作目錄搬過去，跳過這步，改成 `git pull` 確認最新）
2. 確認 `git log --oneline -1` 看得到本次的最新 commit、`git status` 乾淨
3. 放回 `config.ini`、資料庫（見上）、機密 PDF
4. **手動複製 `code/app/_runs/audit_log.jsonl`**（不要漏，見第一節）
5. `setup.bat` 建 `.venv`
6. `cd code && python -m research.tests` → 要 131 項全過（若 129/131，且掛的是
   `t_stage0_artifacts_exist`／`t_stage0_idempotent`，代表沒帶 `results_artifacts/`，
   屬已知、不影響應用層的落差，見 `現況銜接.md` §5-11）
7. `streamlit run app/ui.py` 開起來，切到「股票層級持股明細」頁籤，解析一次持股、
   點「查詢已實現報酬」，確認能正常跑出數字（順便驗證 S5 這次接進 UI 的東西沒有
   因為換機器而壞掉）
8. 切到「AI 解讀」頁籤，用 dry-run 產生一次解釋，確認能看到第 11 欄「風險提示」
   （`risk_flags`）——如果看不到，代表 `explain.py` 沒有跟著更新過去
