# 研究部完整流程（v9）

> **本檔定位**：研究部的權威流程文件（人類版，含理由論證，供你與口試使用）。
> **狀態標記**：✅ 定案　⏳ 待資料　❓ 待老師　🆕 新增　🔄 v9 改動　🔴 重要接縫
> **v9 更新（2026-08-19 老師會議後 · 端到端 review）**：
> 1. **階段 −1 正式接入**——Phase 1–4 因子篩選 SOP 從「懸空的前置」變成正式章節，研究部起點改為 `{TW,US}_L4_openSec_final_candidates.csv`（14,078 列）。
> 2. **階段 0 大幅簡化**——去重/換股/切期已由 Phase 1–4 完成，本階段縮為「格式整備」。
> 3. **階段 1 定位變更：篩選 → 標記**——上游已篩掉該篩的，本階段改為產出 Agent1 快篩所需的索引。關卡 B 大幅縮編。
> 4. **階段 3 改寫為 HRP**——老師 2026-08-19 指示，兩兩 Pearson 改為階層聚類。
> 5. **LLM 點 6 → 3**——刪除所有參與篩選決策的 LLM 節點。
> 6. **valuation_verdict 刪除**——老師判定 V 無普遍價值。
>
> ## 🔴 v9.1 修訂（2026-08-20 · 實驗產物全量實測後）
> 架構規劃與 Phase 1–4 實驗是平行推進的，兩邊未即時同步，加上台美資料收集限制，產生五項落差。**已全量實測（14,078 策略全掃，非抽樣）並定案處理方式**，本文對應段落已就地更正：
> | # | 落差 | 更正位置 |
> |---|---|---|
> | 1 | `sharpe_ann`/`daily_sharpe`/`avg_drawdown` 不在候選 CSV | 階段0、第五部分欄位表 |
> | 2 | `artifacts_dir` 不存在；產物散在 L3(v0)/L4(v1) 兩個 job 目錄 | 階段0、第五部分欄位表 |
> | 3 | **台股歷史起始期跨 7 年**，「312 個月」不成立 | 階段3 演算法規格 |
> | 4 | 月頻/日頻描述矛盾 → 實測完全一致，補充A 待辦早已滿足 | 改動4 補充A |
> | 5 | **台美策略字串碰撞 1,381 個** → 主鍵必須含 market | 階段0、第五部分欄位表 |
> **完整方案比較見 `落差處理方案_v1.md`；實作規格見 `系統設計文件_v1.md`。**

---

## ⭐ v9 · 階段依賴圖與凍結原則（端到端骨架）⭐

> **為什麼重要**：本研究是階段式管線（pipeline），每階段的產出是下一階段的輸入。依賴一旦凍結就不可隨意改（改上游會連鎖動下游）。故**依賴要早鎖定、凍結後不可改**——這是可複製性與嚴謹性的根本。

**依賴鏈（→ 表示「產出餵給」）**：
```
階段 −1  Candidate Strategy 產生（Phase 1–4 SOP，老師 2026-08-05 方法論）
  └→ {TW,US}_L4_openSec_final_candidates.csv（台 7,162 + 美 6,916 = 14,078）
      └→ 階段0 格式整備：合併台美 + 拆結構欄位 → candidate_index
          └→ 階段1 標記關卡 A/B/C + 尾端寬鬆硬篩 → usable_pool（≈14,000）+ 標記欄位
              ├→ 階段2a 價格切 regime ┐（2a、2b 平行，皆只對 usable_pool 算）
              ├→ 階段2b 總經↔策略對應 ┘
              │    └→ 階段2c 交叉佐證（2a×2b 匯流，＝階段2 統一出口）
              │         └→ 階段3 HRP 階層聚類（三組樹 + 常態/危機兩套）
              │              └→ 階段4 merge 成 strategy_map（凍結）→ 實戰部
              └→（階段1 的報酬序列也直接餵階段3，算 HRP 的原料）
```

**關鍵依賴聲明**：
- **階段2、3 只對 usable_pool 算，不對全庫**。故**階段1（含尾端硬篩）必先於 2、3**。
- 階段2a、2b **平行**；2c 需 2a+2b 完成。**2c 是階段2 統一出口**，下游（階段3）從 2c 接。
- **階段3 兩個輸入**：階段1（報酬序列，算 HRP 的原料）+ 2c（regime 窗算危機樹、2b 總經供互補因果解釋）。
- **主鏈 2c→階段3→階段4**：階段3 攜帶上游往下給階段4。
- 階段4 是彙整層。

> 🔄 **v9 為什麼 HRP 放階段 3 而不是階段 2**（曾一度誤放，記錄備查）：
> 階段 2 處理的是「策略 ↔ **環境**」的關係；階段 3 處理的是「策略 ↔ **策略**」的關係，HRP 正屬後者。
> 且 HRP 的「常態樹 vs 危機樹」需要 regime 窗，**必須等 2c 完成**——放階段 2 會造成循環依賴。

**凍結原則**：
- 研究部全部產出**凍結**（in-sample 2000–2025 一次性建立），**out 期只讀不改**：z-score 參數、四格邊界、信心門檻、regime 切割、**HRP 樹與群定義**、strategy_map 全部凍結。
- 凍結後改上游 = 連鎖重跑下游。**故每階段的輸入/輸出/理由須 review 穩定、經老師確認後才凍結。**
- 加減欄位/工具很容易（鬆耦合）；改跨階段依賴/順序很貴（連鎖）。**加市場不在需求內**，不需市場參數化。

---

## ⭐ v9 · strategy_map 最終輸出格式（研究部→實戰部的交接物）⭐

> strategy_map 不是單一平表，而是**一個主表 + 附屬結構**的凍結檔案集合。

**① 主表 `strategy_map.parquet`（一列一策略）**——完整欄位見第五部分。

**② 報酬序列 `returns/`（逐月報酬，per-策略序列）**：給實戰部即時算組合層/相關，也是階段3 HRP 的原料。

**③ 🔄 v9 改寫 · HRP 結構 `clusters/`**：
- `cluster_assign`：per-策略的 `cluster_id_L1/L2/L3`（三組 × 常態/危機各一套）
- `cluster_meta`：per-群的成員數、群內平均相關、代表策略、各 regime 表現
- `cluster_corr_matrix`：**k×k 群間相關矩陣**（這是給 LLM 讀的核心產物）
- `cluster_story`：群間互補的通用因果解釋（LLM 點③ 產出，凍結）
- `co_fail_regimes`：per-群，危機樹中與本群合併的群
- ⚠️ **v9 刪除 `complement/`（策略×策略）**：被上述取代。

**④ 總經底稿 `macro/`（全域共用）**：macro_history（台美、含平滑版與滯後版）、四格邊界、z-score 參數。階段2b 產物。

**⑤ regime 定義 `regime/`（全域共用）**：regime_table（台美）、2c 一致性標記。

- **凍結**：①②③④⑤ 全部凍結，out 期只讀。
- **return_story 只凍結「判決」，文字按需生成**（乙案）——主表存判決、不存文字。

## ⭐ v8 · 研究部產出物二：20年情境×策略表現對照表（產出A）⭐

> **來源·老師逐字稿**：老師要「最後整理 20 年對照 + 明顯的模式趨勢（2022 熊、2015/2018/2020 下殺段…），可做成表格或 AI 對答」。此為**回顧型呈現物**，屬**研究部**（素材全在研究部產出裡，不需等實戰部）。

**定位釐清（產出A vs 產出B）**：
- **產出A（研究部）= 20年情境×策略表現對照回顧表**：把 in-sample 20年切成各 regime 情境，對照各情境下策略/組合表現與明顯模式。素材來自 `regime_table`（階段2a）+ 各策略×四格表現（階段2b）+ 互補矩陣（階段3）。研究部彙整即得。
- **產出B（實戰部）= 情境→怎麼選的即時應用**：當前情境進來→agentic 挑一組兵→解釋。屬實戰部。
- **A、B 分屬兩部門、各自獨立產出**（A 非 B 的副產品）。但**兩者須用同一套凍結 strategy_map**，避免「回顧說選X、即時卻選Y」的矛盾——一致性要守住。

**表格結構**：
- **行 = regime 時期**（各 regime 段：2022熊、2020崩、2015/2018 下殺、各牛市段…）
- **列 = 決策與結果（組合層為主）**：該情境下選了哪類兵、互補/免費午餐效果、組合報酬/回撤、贏大盤否；策略層當可展開細節。
- **組合層為主**（呼應老師「免費午餐」：三支不同時賠、合起來回撤遠小於各自加總）。

**待定/延伸**：
- **AI 對答 = 系統延伸、視進度做**（能問歷史特定情境就回答）；「任意假設情境即時跑」列未來工作。此保留讓實戰部主線架構可維持輕（單向流水線+有限回饋足夠，不需複雜三Agent協作）。
- **是否×3（三類投資人各一張）= 綁定待老師確認「是否按保守/積極/全天候三類跑 out」**。

---

## ⭐ v9 · 工具層概念（實戰部詳細規格見實戰部 v8）⭐

> strategy_map 各欄位是「實戰部 agentic 工具的資料後端」。工具 = 查這些凍結檔案的介面。按「查什麼」分類：
- **A 單策略情報**：`get_strategy_profile(id)` 查主表①完整 profile。
- **B 篩選/檢索**：`filter_pool(條件)`（快篩，🆕 **須含 cluster 級配額**）、`rank_by_macro_fit(當前總經)`。
- **C 關係**：🔄 v9 改寫——`get_cluster_info(id)`（查所屬群、群內相關、群間互補解釋）、`check_pair_correlation(id1,id2)`（**即時算，不預存 N²**）。
- **D 組合層即時計算**：`compute_portfolio_risk(策略清單)` 用②報酬序列即時算組合回撤/波動/曝險集中度/台美比重。**組合數爆炸、只能即時算。**
- **E 總經/regime**：`get_current_regime()`、`get_analog_months(當前總經)` 查④⑤。
- **F 解釋生成（按需）**：`generate_return_story(id)` 對候選策略把凍結判決串成文字。
- **G 配權**：🆕 v9 新增——`compute_weights(策略清單)` 用 **HRP 遞迴二分**配權。**此解掉原本待老師的 Q4「權重方法」。**

**工具層原則**：回傳「客觀數字+信心標註」不回傳判斷（判斷是 LLM 的事）；無狀態、可複現。

## ⭐⭐ v7 · 老師會議後重大更新（2026-07 報告完的最新指示）⭐⭐

> 這次跟老師報告後，老師給了明確建議，其中**好幾個推翻了 v6 的定案**。下一個對話請以本區為準。改動點如下，各節內文也已標 🆕。

### ✅ 改動 1（最大）：總經進研究部 + 「總經↔策略」對應【已定案，見階段2b】
- **老師要求**：strategy_map 含過去 20 年總經；建「總經環境→哪些策略賺/賠」對應；實戰部拿當前總經查、找類似環境下表現好的策略。
- **推翻了什麼**：v6「總經只在實戰部、研究部不碰總經」。現總經進研究部。
- **✅ N1 定案**：對應模型**建在研究部、離線建、凍結進 strategy_map；實戰部只查**。
- **✅ N3 張力化解**：老師要的是 **ML 檢索/對應器（相似度比對，非 LLM 敘事）**，與 regime dating 同屬「程式做的數字→數字對應」，**未真正踩到抗幻覺鐵則**。
- **✅ 落地方式**：總經在階段2 扮兩角色、刻意分開——(A) **2c** 當 regime 切割的總經佐證；(B) **2b** 建總經↔策略對應。詳見階段2。
- **✅ 主方法**：方法一 k-NN 相似歷史檢索（主）+ 方法二 投資時鐘四格（輔）並存；方法三迴歸不實作僅論述。月頻、台美各一套。
- **✅ N2**：Agent0 與研究部總經模型的接法 → 見「實戰部連鎖」，實戰部改寫時定。

### 🆕 改動 2：關卡 A、B 篩選都「放寬」，不再硬門檻嚴篩
- **老師要求**：除非策略「很爛」才砍掉，否則保留、當它的個性。判準放寬成「只要還賺錢、至少贏過大盤」就留。**A（可信度）和 B（穩定度）都一樣放寬**（不只 A）。
- **理由**：留越多越好，策略太少 AI 沒得選；要製造「一個軍團」讓 AI 從裡面選出好的，這才證明系統對。堪用即可、不用精選。
- **推翻了什麼**：v6 原本定「關卡 A 硬門檻 AND 嚴篩」。現在改成寬留、把選擇權交給後面。

### ✅ 改動 3：regime 用「cycle（連續區間）」切【已定案，見階段2a/2c】
- **老師要求**：用完整週期切連續時間區間，不按年切。
- **✅ 總經佐證落地（做法甲·先切後驗）**：2a **純價格**切 → 2b 總經分四格 → **2c 交叉佐證**（核對熊市時段是否落衰退/停滯格）。價格為主、總經為輔；背離時段保留不修僅標記。

### 🆕 改動 4：跨市場互補 → 其實是「三組互補」，且 Pearson 只跑一個
- **老師補充**：互補不只跨市場，還有**台股↔台股、美股↔美股、台股↔美股 三組**。台股內部也要算相關（有的台股策略賺、有的不賺，看內部相關多高）。
- **強化論點**：台股內部若互補不足、危機一起垮，更凸顯必須跨到美股。
- **Pearson 只跑一個就好**：相關有很多種，老師說不用弄太細，跑一個 Pearson 即可。
- ~~**實作前提（補充A）**：要算常態相關+危機相關，必須**先把每個策略的每日報酬存下來**，才能按牛熊 cycle 切分段算。~~
  ✅ **v9.1：此待辦早已滿足，無需任何工作**（2026-08-20 實測）。日報酬一直存在 `stock_data.parquet`，月報酬更已預算在 `return_table.parquet`；兩者實測一致到 **2.2e-16**（float64 精度極限），`return_table` 就是 `stock_data` 的月度彙總。階段3 用月頻（169 MB）即可，不需讀日頻（2.2 GB，13 倍 I/O）。

### 🆕 改動 5：三個架構級待決，老師都給了答案
- **walk-forward**：❌ 不用滾。因為參數是窮舉全跑的、本來就沒有 overfitting 問題，walk-forward 只是驗證「挑策略方法有沒有用」，時間考量不用做。
- **baseline**：✅ 跟**大盤**比就好。系統有 alpha，絕對比純大盤高。
- **LLM 幻覺指標**：❌ 先不用做。因為 LLM 不選策略（選股是程式做的），幻覺風險小。

### 🆕 改動 6：最終產出 = 「AI 對照表」，挑有趣段落回答
- 整理 20 年 + 標出幾個明顯熊市（2015、2020、2022 跌多慘）、幾個大牛市（賺多少）。
- **要挑「有趣的段落」回答**，例如「我預期今年第三季可能跟 2024 某段類似，因為 GDP 很像」，而非平淡回答整個 20 年。用 AI 對照表或 SOP 產出即可。

### 🆕 改動 7：執行順序與誠實度（記錄備查）
- **台股先跑完、美股接著補**（台股跑完若美股有缺可趕快補）。暑假先抓資料開跑。
- **out-sample 誠實度**：只跑到 2026（6月）的 out-sample，嚴格科學標準「算是作假」（2025 選的策略在 2026 必然很優）；但以做策略角度可交代得過去。老師結論：**做多少算多少**。論文要誠實面對此限制。
- **效率前緣**：跨市場互補的數學依據是「免費的午餐 / 效率前緣」，但不用算太緊（算太緊會崩潰），求數字即可。

---

## ⭐⭐⭐ v7 · 論文核心定位重校準（B架構 + agentic 精挑）⭐⭐⭐

> **這是本次最根本的釐清，影響全論文。** 起因：老師逐字稿反覆說「留一個軍團讓 AI 選」「AI 去挑出堪用的兵」「這樣選出棒的就表示你的 AI 是對的」。重讀確認：**老師要的是 LLM 從一大坨堪用策略裡「精挑」，而非程式規則挑、LLM 只解釋。** 原文件「Agent1 純規則不用 LLM」與此**衝突**（即「文件的錯誤」所在）。

### 定案架構：B（程式粗篩 + LLM 精挑），三層分工
1. **第一層 · 程式硬篩**（七萬→幾千）：純數字規則，淘汰虧錢/換手不足/可信穩健不過關。**絕對程式、不容 LLM**，守住「客觀事實由程式產生」。
2. **第二層 · 程式算情報/工具**（幾千個堪用池）：方法一、二、互補、風險等計算，產出**客觀評分與標籤**，封裝成工具。不做最終取捨。
3. **第三層 · agentic LLM 精挑**：LLM 看得到 regime/macro，**自主 plan → 呼叫工具取情報 → 執行挑兵並解釋**。這是 LLM 不可替代價值所在。

### 抗幻覺鐵則·重新表述（未推翻、是精確化）
- 舊：「LLM 不碰選股」。
- **新：「LLM 只在程式篩定的堪用池內做判斷式精挑，不能生成池外或虧錢的策略；權衡須引用客觀依據」。** 幻覺被關在「客觀事實」的籠子裡，但 LLM 判斷力被釋放。**論點更強**：LLM 價值在「程式無法做的判斷式精挑與矛盾權衡」。

### agentic 自主程度：半結構（guided agentic）✅
- 給大致階段骨架，**每步內** LLM 自主呼工具、作判斷；步驟大方向由我方定，不亂跳。
- 理由：真 agentic（有 plan/tool/判斷）+ 可複現/可評估/可辯護（碩論命脈）+ debug 可行。
- 完全自主留「未來工作」一句；工具式太保守（退回被動）。

### 🔴 連鎖影響清單（新定位外溢，後續要改，依此為地圖）
**重大（必改，集中在實戰部）**：
0. **組合層特性工具（實戰部待辦·審查隱憂1）**：agentic 挑「一組」兵時，除單策略情報+兩兩互補外，需「給定一組策略→即時算組合整體風險/波動/回撤/因子曝險集中度/台美比重/特定regime整體表現」的工具。**組合數爆炸、只能即時算，不設研究部階段**；研究部已備齊原料（單策略逐日報酬序列 + factor_type/regime_fit 等標籤）。實戰部工具層須實作此類組合層工具。
1. **實戰部 Agent1 整個改寫**：純規則檢索器 → agentic LLM 精挑（半結構）。〔未做〕
2. **抗幻覺鐵則重述**：如上新表述，全論文骨，要重寫。〔本檔已表述，實戰部文件待同步〕
3. **實戰部 agentic 骨架設計**：半結構步驟骨架、每步要哪些工具、衝突權衡+引用要求。〔未做〕
4. **「工具層」新產物**：2b、階段3 計算封裝成 LLM 可呼叫工具（非重做計算，是包裝）。〔未做，需列工具清單〕

**中等（定位/表述調整，計算不變）**：
5. **✅ 階段1 產出定位（已改）**：A/B 改「寬鬆硬篩（只砍明顯爛貨）+ LLM 情報」雙身份；基調寬鬆硬篩+大池（幾萬可接受），壓縮交實戰部快篩；C 特性標記定位改情報、暫不加碼。詳見階段1。
6. **✅ 階段3 互補定位（已改）**：三組（台↔台/美↔美/台↔美）**平權、各自有用**，為 agentic 挑兵的三種平行互補工具；跨市場賣點重定位（台↔台是揭示現象的工具、非反面教材）；三組各用對應危機窗；傾向離線全算+凍結、可行性等階段1筆數試跑決定；B3 分工定案（離線產通用解釋/實戰引用做情境判斷）。Pearson only。詳見階段3。
7. **✅ 階段4 strategy_map 定位（已改）**：= agentic 工具的資料後端（各欄位為工具查詢來源）；分層原則（離線凍結客觀身份證/實戰引用做情境判斷）；客觀特徵凍結、門檻映射交 agentic 即時判斷；**return_story/valuation 乙案：判決凍結+文字按需生成**（省幾萬筆 Opus）。詳見階段4。
8. **Agent1/Agent2 邊界**：Agent1 agentic 自帶「引用理由」後，與 Agent2 解釋職責是否重疊，須重釐清。〔待議〕

**論述層（論文要跟著調）**：
9. **核心論點句改寫**：→「LLM 在判斷式精挑與矛盾權衡上不可替代」。
10. **客觀衡量多代理優越性**（本就掛著、現更尖銳）：擬設 baseline——同一候選池，程式規則挑 vs agentic 挑，比績效。
11. **可複現性**：agentic trace 記錄、溫度/seed 固定，寫進方法論。

> **多代理人體現的口試備答**：純 B 第三層是單一 LLM 精挑，口委或問「多代理在哪」。答：多代理體現在**跨部門角色分工**（Agent0 總經/regime、Agent1 挑兵、Agent2 解釋），非單節點內辯論。此立場站得住。

---

> **v6 相對 v5 的演進**（保留備查）：
> 1. regime 全域→台美各一套。2. 互補改常態+危機相關、危機判兩套。3. 程式vsLLM分工+模型強度。4. 抗幻覺三防線。5. 關卡A集中型防誤殺。6. 關卡C初標純年度報酬斷循環。7. baseline三階梯+walk-forward待決。8. 各階段方法選型定案。
> ⚠️ 注意：上列 v6 第 7 點的 walk-forward、baseline 待決，已被 v7 改動 5 回答；v6「關卡A硬門檻」已被 v7 改動 2 推翻。

---


# 第一部分 · 下週要問老師的問題（會議議程）

> 排序原則：先談會**重構實驗規模**的架構級決定，再談方法級確認。前兩大題不定，後面的方法細節都懸空。

## A. 架構級（最優先——決定整個實驗規模）

### A1. 論文核心比較維度 + baseline 設計
- **核心命題確認**：本論文要證明的，不只是「有沒有用 LLM」，而是「**把 LLM 組織成分工的多 Agent 系統，是否優於 (a) 不用 LLM 的純量化、(b) 用單一 LLM 全包**」？
- **baseline 三階梯**：是否設計成三級對照——① 純量化（無 LLM）② 單一 Agent（用 LLM 但一個模型全包）③ 多 Agent（完整系統）？① 證明 LLM 有價值、② 證明「多 Agent 架構本身」有價值。
- **範圍可行性**：完整三階梯要跑三套系統，工作量是否超出碩論？能否只做關鍵一二級（如只做多 Agent vs 單一 Agent）？

### A2. 衡量指標——多 Agent 的「更好」好在哪個可測量處？（最難、最關鍵）
- **難題**：因為刻意讓 LLM 不碰選股，多 Agent 的優勢**不會反映在報酬（CAGR/夏普）上**——報酬由程式的選股決定。那該用什麼指標比較？
- **我方傾向**：比 **幻覺率 / 事實一致性 / 解釋可信度**，而非報酬。這與論文主軸（LLM 做因果評估與診斷、非幫賺錢）一致。
- **但「主觀品質難評分」是真難題**，擬用的繞法（請老師確認取捨）：
  - 主：**不評「好不好」（主觀），改評「有沒有與數字矛盾」（客觀）**——用程式回頭查 LLM 陳述是否與 EffN/Top1 等數字衝突，把「幻覺率」變成可數的硬指標。此法是抗幻覺設計的副產品，天然契合。
  - 輔：**一致性**（同策略多次跑，結論穩不穩）。
  - 背書：**小規模人工評分**（抽 ~20 個當黃金標準，校準自動指標）。
  - 待定：**LLM-as-a-judge**（用 Opus 盲評+評分細則），根本質疑「LLM 評 LLM 可信嗎」仍在，是否採用請老師定。

### A3. Walk-forward（滾動更新）要不要做？
- **命題（✅ 已由老師回答·不用滾）**：研究部地圖用 2000–2025 建**一張凍結地圖**，不做 walk-forward 滾動重建。
- **價值**：滾動更貼近 AQR 真實運作；驗證點從 1 個（2026，資料還少）變 25 個；天然無 look-ahead。
- **代價**：算力 ×N、LLM 成本 ×N、「凍結地圖」整套設計要重寫、論文範圍膨脹。
- **問老師的定位題**：「論文重點是『架構能運作』還是『架構的時間穩健性』？」前者固定分割就夠；後者才需要滾動。
- **折衷提案**：不做完整 25 年逐年滾，改切 **2–3 個時間段**（如 00–10 建圖驗 11–15、00–15 驗 16–20、00–20 驗 21–25），3 個驗證點、算力 ×3，是「1 次」與「25 次」的甜蜜點。

## B. 方法級（確認方向，不敲數字）

### B1. 階段0 換股門檻依據
- 原「換股 < 5」門檻，是老師定的還是學姊沿用的？依據為何？（決定要不要改成相對門檻）

### B2. 階段2 regime 基準
- 確認用**大盤基準**（而非階段1篩出的代表策略）判 regime，以避免「用策略表現定義市場、又用市場評估策略」的循環論證——這方向對嗎？

### B3. 階段3 分工界線
- 研究部的「離線通用互補解釋」與實戰部 Explainer 的「即時情境解釋」，界線劃在哪？兩者職責如何不重疊？

## C. 已被我們討論解決、不必再問（記錄備查）
- ~~跨市場危機窗如何統一~~ → 已解：**判兩套**（台股危機期、美股危機期各算），不需共同危機窗。整段相關用日曆共同時間軸即可。

---

# 第二部分 · 研究部完整流程

## 0. 核心心智模型：百科全書是「資料」，圖是「按需調閱的輔助」

百科全書的本體是**資料**（master_index：每個策略的 stats + 明細表），圖只是給人看的輔助。LLM 讀結構化資料遠比讀 PNG 準。正確心智模型是兩層：

- **第一層 · 數值索引（掃全庫，純程式）**：每個策略的數值特徵（CAGR/MDD/std/EffN…）全進 `master_index`。這是「目錄」。
- **第二層 · 圖像判讀（只看少數）**：先用數值篩候選，才對候選調圖做判讀。

一句話：**數字掃全部建目錄，看圖只看被篩出的少數。**

⚠️ **看圖 ≠ 用 LLM**：整個研究部只有兩處圖判讀動用 LLM（成片vs孤立、估值註記），其餘看圖是程式算或人輔看。詳見「第三部分 · 程式 vs LLM 分工」。

## 1. 百科全書的「兩層版本」結構

- **外層 · job/spec**：market{US,TW} × N{5,10} × 頻率{M,Q} × 窗，命名如 `US_f3_N5_c20_v2_M`。切 job → 整批圖換一套。
- **內層 · 策略**：job 內 F1×F2×C×V 全展開，一列一策略，名如 `F1__F2__C__V`。切策略 → 單張圖換。
- **百科全書 = 全部 job × 全部策略**。

## 2. 研究部的真正起點（🔄 v9 更新：從 master_index 改為 Phase 1–4 產物）

> 🔄 **v9 改動**：研究部起點不再是 `master_index.parquet`（舊的 10 批海選產物，方法論已作廢），而是**階段 −1 的 Phase 1–4 SOP 產出** `{TW,US}_L4_openSec_final_candidates.csv`（台 7,162 + 美 6,916 = 14,078 列）。以下 master_index 的欄位說明保留作為**欄位對照參考**（兩者結構欄位命名一致）。

`catalog_builder.py` 併所有 job 的 stats → `_catalog/master_index.parquet`，一列一策略，27 欄四組：

| 組別 | 欄位 | 用途 |
|---|---|---|
| 結構 | F1,F2,C,V,F1_factor,F2_factor,F1_band,F1_N,F2_band,F2_N,C_kind | 檢索鍵 |
| 設定標籤 | market,rebalance,job_label,universe_size,N_list,factors | 外層版本識別 |
| 指標 | CAGR,daily_sharpe,max_drawdown,avg_drawdown,win_ratio,ytd | 數值篩選 |

> 🆕 **v7 更正·daily_sharpe 立場**：**採用學姊論文的 daily_sharpe 計算，視為正確、直接使用，不當 bug、不重算、不修正**（學姊論文經指導教授檢查、口試通過）。此更正推翻本檔早期「daily_sharpe 是 bug、不採信、改用 sharpe_ann」的舊敘述——凡文件他處出現「daily_sharpe bug/不採信」字樣，一律以此更正為準。（註：daily_sharpe 最高者常是低交易、win_ratio≈0 的不賺不賠策略，屬正常數學現象；排序找「最穩策略」時須併看交易次數/win_ratio，但這不代表欄位本身錯。）
| 去重/路徑 | is_dup,is_cross_dup,artifacts_dir | 去重旗標＋明細/圖路徑 |

- `artifacts_dir` = 該策略明細（return_table/stock_data）與圖的實體路徑。
- ⚠️ **現況**：只跑過 SMOKE 測試，`master_index.parquet` 尚未正式產出。正式 sweep（估一週量級）跑完才生成。**目前最優先要手動跑的東西。**

## 3. 研究部 vs 實戰部：事前/事後兩層

```
【事前 · 離線 · 一次性】
研究 Agent（讀 master_index，用 2000–2025 年底做加值）
        ▼ 產出「凍結的 strategy_map」
─────────────────────────────────────────
【事後 · out 期（2026 年初迄今，建議季頻）】
當年總經 → Agent0(判regime) → Agent1(撈策略) → Agent2(解釋) → 輸出
                                    ▲ 兩輸入匯流：①凍結strategy_map ②當年regime
```

- **資料切點（✅）**：in 2000–2025 年底；out 2026 年初迄今（能做多少算多少，畢業前可再跑一次拉長）。
- **凍結鐵則（✅）**：strategy_map 在 out 期永久凍結、不重建（避免 look-ahead）。
- **實戰部定位澄清（✅）**：實戰部**只運作在 out 期（2026+）**，讀取（不重算）用 2000–2025 建好凍結的地圖。Agent0 判當年 regime 用當下資料；Agent1 讀凍結地圖欄位。**讓實戰部回頭重跑 in-sample = 作弊（look-ahead）**，這是樣本內外分割的全部意義。
- ✅ **註**：本研究採「固定分割單版」架構。老師已定案 **walk-forward 不用滾**（窮舉全跑、無 overfitting），故不需包時間迴圈，凍結定義維持「單一版本」。

## 3.5 圖表分類原則：以「需求路徑」為主軸

不按構面（F/C/V）分類，改按**需求路徑**：因為實務上沒人說「我要體質構面」，只會說「我要抗跌的」。結構為 **共同前提 + 三條分歧**：

```
【共同前提 · 研究部階段1】關卡A可信度 → 關卡B穩健度 → 關卡C特性標記
   ↓ 產出：可信 + 穩健 + 已標記特性的「策略池」
【需求分歧 · 實戰部 O2】保守型 / 積極型 / 全天候型（三條 pandas query 路徑）
```

分類的終點是 strategy_map 欄位——實戰部 Agent 不看圖、只查欄位。

---

## 4. 研究部四階段（順序 0→1→2→3→4）

> 階段1 必須先於階段2（老師定調：「策略穩定性要先做」「你找比較穩定的策略，才能去做市場分類」）。理由：對未篩過、可能大量過度配適的策略做市場分類，分出的「這類熊市好」可能只是雜訊。

### 階段 −1 · Candidate Strategy 產生（Phase 1–4 SOP）🆕 v9 正式接入
> **輸入←** 因子資料庫（TEJ / FMP）　**輸出→** `{TW,US}_L4_openSec_final_candidates.csv`（台 7,162 + 美 6,916）
> **依據**：老師 2026-08-05 指示「**自己決定分桶＝作弊**」，要求建立系統化因子篩選流程。
> **repo**：`Dowgojashan/stock_factor_lab` branch `FMP_4`，`重跑計畫_老師方法論SOP.md`
> **狀態**：✅ **台美均已跑完並向老師報告通過（2026-08-19）**

> 🔄 **v9 定位變更**：v8 時此 SOP 是「懸空的前置、待整合」。現已跑完並經老師驗收，正式成為研究部的第 −1 階段。
> ⚠️ **v9 刪除 v8 檔頭的「舊台股結論作廢」警語**（10 批海選、EV_S 冠軍 36.7%、MOM 墊底、固定三因子 ROE/EPS/FCF_P）——重跑已完成，該警語已無作用，留著只會誤導接手者。舊結果僅存作探索紀錄，不列入論文結論。

**四階段（一次只開一個構面）**：

| Phase | 開什麼 | 做什麼 | 結果 |
|---|---|---|---|
| **1** | F1 單因子 | 19 個因子各切 **9 桶**，算桶號 × CAGR 的 Spearman ρ 看單調性 | 台股 6 過關 / 美股 8 過關 |
| **2** | ＋F2 配對 | 3 桶兩兩配對（**F2 可為空集合**），四道門檻 | 1,596 → 台 219 / 美 188 晉升 |
| **3** | ＋C 條件 | 20 個動態條件，**固定 F 只換 C** 量增益 | 台 4,495 / 美 3,860 候選 |
| **4** | ＋V 濾網 | v0/v1 逐一配對比較 | 台 7,162 / 美 6,916 |

**Phase 1 判準（門檻在跑之前訂好）**：|ρ|≥0.85 過關（可當 primary）／0.60–0.85 邊際／<0.60 但單端顯著＝只取極端桶／<0.60 淘汰。

**Phase 2 四道門檻**：primary 贏基準 ≥2pp ／ secondary 輸基準 ≤1pp ／ 配對退步 ≤0.5pp ／ **平均持股 ≥10 檔**（老師的獨立約束）。

**採用變體 openSec**：primary 照 Phase 1 篩、secondary 全開。經 strict/relaxed/all 四變體對照決定（涵蓋度 +45.5%、中位僅降 0.20pp）。

**基準**：🔄 **v9 更正**——改用**自建宇宙基準**（同宇宙、同成本、含股利、等權）。台股 **8.67%**（v8 的 4.71% 是價格指數、不含股利，已作廢）、美股 **12.35%**。

**進入研究部階段0 的關鍵事實**（會影響後續階段的設計）：

| 事實 | 數值 | 對下游的影響 |
|---|---|---|
| 候選池總數 | 台 7,162 / 美 6,916 | 階段3 的 N² ≈ 9,900 萬對 → 促成 HRP |
| **獨立 F 組合數** | 台 **219** / 美 **188** | **有效多樣性遠低於總數** → 快篩需 cluster 配額 |
| 每個 F 組合的變體數 | 中位 35–38 | 同組變體選股池相同、報酬高度相關 |
| 低於基準的策略 | **0 個** | 階段1「砍虧錢的」那一刀空轉 → 刪除 |
| 持股數 < 10 的策略 | **0 個** | 階段1「持股下限」那一刀空轉 → 刪除 |
| F2 空集合策略 | 台 384 / 美 652 | 老師指示「F1+C 就好」→ 需 `F2_empty` 標記 |

**已知限制（寫入論文 limitations）**：
- **美股倖存者偏誤**：Russell 3000 成分股取自 2026-07 快照，未含歷史下市公司。老師 2026-08-19 判斷「要嚴格做得買資料庫，我自己也沒買」「68% 你再怎麼減 10% 還是很強，不會差那麼多」→ **決定不重跑，但誠實揭露**。
- **整條 SOP 在同一份 in-sample 上做選擇**：已做子期間對半切檢定（2000–2012 vs 2013–2025，台美 ρ 相關 0.678 / 0.677，不穩定者集中在原判「邊際」的一群）。2026 保留為樣本外。

### 階段 0 · 格式整備（純程式，無 LLM）✅ 🔄 v9 大幅簡化
> **輸入←** `{TW,US}_L4_openSec_final_candidates.csv`（階段 −1 產出，14,078 列）　**輸出→** 統一索引（給階段1）
> 🔄 **v9 改動**：階段 −1 的 Phase 1–4 SOP 已完成原階段 0 的絕大部分工作，本階段從「清洗關卡」縮成「**格式整備**」。

**v9 刪除的項目（附理由，勿當疏漏補回）**：

| 原項目 | 刪除理由 |
|---|---|
| 濾 `is_dup` / `is_cross_dup` | Phase 2 的 `_POOL_SHARE` 共用與去重機制已處理 |
| 濾「換股不足」 | 已被 Phase 2 的 `MIN_HOLDINGS = 10`（老師的獨立約束）取代，依據更明確 |
| ❓ B1 待問老師「換股<5 的原始依據」 | **連帶撤銷**，該門檻已不存在 |
| 切死 in-sample 到 2025 | Phase 1–4 全程 `IN_SAMPLE_END = 2025-12-31`，已切好 |

**v9 保留 / 新增**：
- 保留：**不把 `daily_sharpe` 當 bug 濾除**（見 §2 更正；相信學姊算法、直接使用、不重算）。
- 🆕 **新增**：合併台美兩份 CSV → 統一索引，並從策略字串拆出結構欄位：
  `market` / `F1_factor` / `F1_band` / `F2_factor` / `F2_band` / `F2_empty` / `C_id` / `C_source` / `C_rule` / `V`
  （即關卡 C 的 C-1 組，零成本、直接字串拆解）
- 🔴 **v9.1 新增三項（2026-08-20 實測發現，原 v9 未涵蓋）**：
  1. **主鍵必須含市場**：實測台美策略字串**交集 1,381 個**（同一套因子命名規則、字串不編碼市場；市場內部無重複）。若以 `strategy` 為鍵合併會產生笛卡兒積或靜默錯配，**約 9.8% 資料會錯且無錯誤訊息**。→ 全管線主鍵一律 `strategy_uid = market + "::" + strategy`，並斷言唯一。
  2. **`sharpe_ann`/`daily_sharpe`/`avg_drawdown` 不在候選 CSV**（CSV 僅 8 欄），須 join 各 job 的 `stats.parquet`（實測命中率 100%、CAGR 對帳 9.71e-17 證明同源）。加對帳斷言防不同步。
  3. **`artifacts_dir` 不存在，須由規則解析**：v0 的產物在 `{market}_L3_openSec_M`、v1 在 `{market}_L4_openSec_M`（L4 是 v0/v1 對照實驗、只跑 v1）。實測覆蓋率 100%，解析後物化成欄位並斷言目錄存在。
  - CSV 讀取須用 `encoding='utf-8-sig'`，且欄名含中文（`F組合`/`持股數`）須正規化為 ASCII。
- 產出：`candidate_index.parquet`（14,078 列，含結構欄位）。

### 階段 1 · 策略標記（三條需求路徑的共同前提）🔄 v9 定位變更
> **輸入←** 階段0 統一索引　**輸出→** usable_pool（≈14,000）+ 各策略 A/B/C 標記欄位（給階段2、3、4）

> ## 🔴 v9 定位變更 · 階段1 從「篩選關卡」改為「標記關卡」
>
> **變更理由**：階段 −1 的 Phase 1–4 SOP 已經把原階段1 打算篩的東西篩掉了。實測候選池中**低於基準的策略是 0 個**（Phase 3 已 gate）、**持股數 < 10 的是 0 個**（Phase 2 已 gate）。原設計的「三刀」有兩刀是空轉。
>
> **變更內容**：階段1 不再為了減量而篩，只為了**讓後面的階段與 Agent1 有東西可查**。
>
> **預期淘汰量 < 5%，`usable_pool ≈ 14,000`。** ⚠️ 接手者注意：這是預期結果，不是篩選失效。
>
> **真正的「壓到 LLM 吃得下」由實戰部 Agent1 Step2 快篩負責**（沿用 v7 定案），階段1 只管標記。
>
> **關卡數量與定位**：
> - **關卡 A（可信度）**：保留，但淘汰條件從三條縮成**一條**（只砍靠單一飆股的假貨）。
> - **關卡 B（穩健度）**：**大幅縮編**，B1 整節刪除、B2-2 LLM 視覺判讀刪除，只留一個程式指標。
> - **關卡 C（特性標記）**：**大幅擴充**，這是本階段的主體，因為它是 Agent1 快篩的索引。

#### 關卡 A · 可信度（真本事還是運氣）— 純程式、無 LLM

> 🆕 **v7 改動 2（老師指示）**：放寬篩選——除非「很爛」才砍，否則保留當個性，留一個「軍團」給 AI 選。
> 🔄 **v9 收斂**：既然「賺不賺錢」「持股夠不夠」上游已經 gate 過，本關卡**只剩一個淘汰理由**。

| 來源圖 | 看什麼 | 判準 |
|---|---|---|
| 4-10 Effective N | 獲利分散到幾檔 | 越高越可信（核心指標）|
| 4-6 Top1 占比 | 最大單股吃掉多少 | 越低越可信 |
| 4-9 累積貢獻曲線 | 前20名累計占比 | 越平緩越可信 |
| 4-4 年度貢獻分布 | 主力個股是否年年換 | 換人＝動態選股，好 |

> ⭐ **v6 定案·集中型策略防誤殺（保留）**：低 EffN **不直接淘汰**，要再看年度輪動——
> - 低 EffN ＋ **不輪動**（主力年年同一檔）＝ 靠運氣，**淘汰**
> - 低 EffN ＋ **有輪動**（主力隨邏輯換人）＝ 刻意集中設計，**保留並標記**
> 否則策略庫會結構性缺席一整類集中型策略，而你從「剩幾個」看不出來。

> ✅ **v9 定案 ·「輪動」的操作型定義**（原本一直沒寫死）：
> **`rotation_score` = 年度前三大貢獻股在相鄰年度之間的重疊程度**。每年取前三大貢獻股，計算相鄰年度的 Jaccard 相似度，全期取平均。分數低 = 主力年年換人 = 有輪動。

**v9 刪除（附理由）**：
- ❌ 「CAGR < 0 或 < 大盤」→ Phase 3 已 gate，候選池 **0 個**低於基準，此條空轉。
- ❌ 「持股數 < 10」→ Phase 2 已 gate。

- ⏳ EffN / Top1 的門檻：待資料（用市場內分位數框架）。
- 產出：`credibility_grade`、`effective_n`、`top1_share`、`rotation_score`。

#### 關卡 B · 穩健度 🔄 v9 大幅縮編

> 🔄 **v9 刪除 B1 整節（4-16 強因子清單）**
> **理由**：B1 要回答的是「這個**因子**強不強、換搭配還撐得住嗎」。這件事已經被**階段 −1 的 Phase 1 線性檢定（9 桶 Spearman ρ）＋ 四變體對照（strict/openSec/relaxed/all）**完整取代，而且做得更嚴謹、更有方法論依據（老師 2026-08-05 指定的 SOP）。留著 B1 是重複勞動。
> **連帶撤銷**：v8「⚠️ 美股系統目前無 4-16 圖，需自補（最優先）」——此待辦作廢。

> 🔄 **v9 刪除 B2-2（LLM 視覺判讀「成片 vs 孤立」）**
> **理由一（關鍵）**：這是**整個系統唯一讓 LLM 參與篩選決策的節點**，與抗幻覺第一道防線「**LLM 不碰篩選**」直接衝突。刪除後，全流程再無任何 LLM 節點參與淘汰決策。
> **理由二**：「策略之間像不像」這件事，**階段 3 的 HRP 階層聚類會用相關結構客觀地做掉**，比 LLM 看熱力圖判斷可靠得多，而且可複現。
> **連帶刪除**：LLM 點 ①（Sonnet 視覺辨識）。

**v9 保留的 B（只剩一個程式指標）**：
- **B2-1（程式）**：算高原寬度分數（`bucket_F1/F2` 的 std、`deltaC_summary` 的 iqr，小 = 穩），產出 `stability_grade`。
- 定位：**只標記、不淘汰**（沿用 v7 改動2 的放寬精神）。

**關卡 B 總產出**：`stability_grade`（單一程式欄位）。

#### 關卡 C · 特性標記（純規則，無 LLM）🔄 v9 大幅擴充

> 🔄 **v9 刪除 LLM 點⑥（混合型 Haiku 判讀）**
> **理由**：實測候選池只有 **F1 + F2 兩個因子**，台股 F1 僅 11 個不同的桶、美股 16 個。規則覆蓋率 **100%**，沒有需要 LLM 補判的邊緣案例。刪除後少一個幻覺面。
> v6「受約束選擇題 + 可數字反驗」的防幻覺設計因此不再需要（記錄備查）。

> 🔴 **關卡 C 是 Agent1 快篩的索引，不是可選項。**
> Agent1 Step2 的快篩是**純程式**（`T2 filter_pool`），12 格門檻矩陣裡出現的每一個名詞都必須是本關卡產出的欄位。**矩陣提到、但這裡沒產出的 → 那一格快篩就執行不了。**

**C-1 身份與結構**（階段0 已拆好，此處確認）
`strategy_id` / `market` / `F1_factor` / `F1_band` / `F2_factor` / `F2_band` / `F2_empty` / `C_id` / `C_source` / `C_rule` / `V`

> 🆕 `F2_empty` 的用途：老師 2026-08-19 指示「**通常是 F1 加 C 最好，F1 加 C 就好**」，理由是「F1 你去管他，那個池子尺寸會變」。實測台股 384 個、美股 652 個 F2 空集合策略，**CAGR 較低但持股數是三倍以上（台 231 vs 77、美 277 vs 81）而 MDD 幾乎相同**——本質是「保守/大容量」那一類的原料，不是次級品。此欄位供 Agent1 分流用。

**C-2 因子類型**（`factor_type`、`factor_type_basis`）
由 F1/F2 的因子分類規則導出，五分類：
```
估值型：PB PS P_IC EV_S EV_EBITDA FCF_P FCF_OI
體質型：ROE EPS ROIC CROIC OCF_E ACCRUAL
動能型：MOM VOL
規模型：REVENUE REV_G
結構型：DEBTRATIO NETDEBT_EBITDA
```
F1 與 F2 同類 → 該類型；不同類 → `混合型`，並在 `factor_type_basis` 記錄兩者各屬哪類（供 Agent2 引用）。

**C-3 報酬與風險形態**（整段統計量）
`cagr` / `cagr_pct` / `max_drawdown` / `mdd_pct` / `ann_vol` / `win_ratio` / `sharpe_ann` / `return_shape`（穩定爬升 vs 大起大落，用年度報酬標準差的市場內分位切）/ `risk_shape`（淺/中/深回撤）/ `worst_year` / `neg_year_count` / `max_consec_loss_months`

**C-4 可執行性** 🆕 v9 新增
`holdings_median` / `holdings_p10` / **`empty_ratio`** / `eval_month_ratio` / `turnover_ann` / `entries_per_year`

> 🔴 **v9.2 更正（2026-08-20 實測）**：原設計的 `coverage_ratio`（有持股月份 ÷ 總月份）**算不出來、已停用**。
> 權威來源 `backtest.py:344` 是 `company_count = (position != 0).sum(axis=1)`，只存在於換股日索引上，
> 「某月沒紀錄」語意曖昧（可能空手、也可能只是部位沒變動）。反證：一個**無 C 條件**、持股恆為 11 檔的策略
> 也只算出 0.625。且該比值是市場常數（台 0.622–0.629／美 0.710–0.714），測的是換股節奏而非策略特性。
> **改為兩個語意明確的欄位**：`empty_ratio`（`company_count == 0` 的比例，明確空手、無歧義，10.3% 策略 >0 有真實變異）
> 與 `eval_month_ratio`（換股頻率，僅供情報、不可當篩選刀）。詳見 `系統設計文件_v1.md` DD-12。

> **為什麼新增**：現行 `avg_holdings` 排除空手月份、且用平均值，會把「一半月份 22 檔、一半月份 2 檔」偽裝成正常。候選池持股數 p10 台股是 13 檔。老師的原話是「選出來的股票不能太少，**那就是策略不穩定**」——講的正是穩定性，而平均恰好把不穩定藏起來。

**C-5 規模依賴** 🆕 v9 新增
`size_tilt_pct`（持股市值分位中位數，用 MKTCAP）/ `smallcap_share`（持股落在最小市值三分之一的比例）

> **為什麼新增**：實測台美 Top 50 各有 **48% / 58%** 含最小規模桶，而全池佔比只有 **3.1% / 10.5%**——排行榜頂端明顯偏小型股。**保守型不應該拿到重倉微型股的策略**，但原 12 格矩陣沒有任何欄位能表達這個限制。這是實質的流動性與可部署性風險。

**C-6 逐年報酬序列**（`annual_returns`，2000–2025）

> ⭐ **v6 定案·斷循環（保留，不可違反）**：環境適配「初標」**只存逐年報酬數字、不貼牛熊標籤**；報酬/風險形態也**只能用不分 regime 的整段統計量**。任何需要分 regime 的判斷一律延到階段 2。如此階段1 完全不依賴階段2，斷開循環。

> ✅ **v9 定案 · 分位數一律以市場為單位計算**
> 所有 `*_pct` 欄位必須 `groupby(market)` 算。理由：台美 CAGR 分布差距大（中位 15.53% vs 23.61%），若用全池分位，美股策略會整批排在台股前面，快篩會失效。

#### 階段1 尾端 · 寬鬆硬篩（🔄 v9 從「三刀」縮為「一刀半」）

| 刀 | 判準 | 狀態 |
|---|---|---|
| 靠單一飆股的假貨 | 低 `effective_n` ＋ **高** `rotation_score` | 🔴 **v9.2 更正方向**（見下）|
| 空手期過長 | ~~`coverage_ratio` 過低~~ → **`empty_ratio` 過高** | 🔴 **v9.2 更正欄位**（見 C-4）|
| ~~虧錢的（CAGR<0 或 <大盤）~~ | — | ❌ 刪除，Phase 3 已 gate |
| ~~穩定度極差~~ | — | ❌ 刪除，改為只標記 `stability_grade` |

> 🔴 **v9.2 更正 · 第一刀的方向原本寫反了（2026-08-20）**
> 本節原寫「低 `effective_n` ＋ **低** `rotation_score`」，與本文件自己的定義矛盾：
> - 上方關卡 A 定義：**分數低 = 主力年年換人 = 有輪動**（好事，是刻意集中設計）
> - 上方淘汰規則：低 EffN ＋ **不輪動**（主力年年同一檔）＝ 靠運氣 → 淘汰
> - 不輪動 ＝ 前三大貢獻股年年重疊 ＝ Jaccard 高 ＝ **`rotation_score` 高**
>
> **故正確判準為「低 `effective_n` ＋ 高 `rotation_score`」。**
> 原敘述會**砍掉刻意集中但有輪動的好策略、留下真正靠單一飆股的假貨**，且不會報錯。
> （定義與實戰部 v8 積極型的「`rotation_score` ≤ 上限」原本就是對的，只有本表寫反。）

> ⚠️ **v9.2 實測補充 · 分位數框架在本欄位上會退化**
> 全量掃描 14,078 個策略的結果：**62.4% 的策略 `rotation_score` 恰好等於 0**，
> P50=0、P90=0.016、P95=0.017、P99=0.025、max=0.072。
> 因為超過六成的值並列於 0，**任何低於 P62 的分位切點都會退化成 0、無鑑別力**。
> → 本欄位的切點必須用 **P95／P99 這類高分位**，或直接用絕對值（如 `> 0.02`），
> 不能沿用其他欄位「切三段取兩端」的作法。
> 鑑別力雖弱但存在：低 EffN（後 10%）子集有 47.2% 的 `rotation_score > 0`，全池為 37.6%。

- 產出 `is_usable` 欄位。**預期淘汰 < 5%**。
- ⏳ 兩把刀的確切門檻：待資料（用市場內分位數框架，但 `rotation_score` 須依上述注意事項處理）。

### 階段 2 · 市場分類與總經對應（拆為 2a / 2b / 2c）✅
> **輸入←** usable_pool + 各策略報酬序列（階段1）、總經原始資料（FMP/TEJ）　**輸出→** regime_table、macro_history/四格表/各策略×格表現、2c 一致性標記（給階段4）
> 🆕 **v7 定案·作用對象**：階段2、階段3 **只對階段1 尾端篩出的 `usable_pool`（幾萬筆堪用池）計算，不對全庫**。理由：對會被硬篩掉的爛策略算 regime/總經/互補是浪費，且污染分布。**因此順序依賴明確：階段1（含尾端寬鬆硬篩）必須先於階段2、3。**
> 🆕 **v7 定案**：階段2 拆為三個子階段。**2a、2b 核心計算平行**（各自獨立、互不依賴），**2c 於兩者完成後匯流做交叉佐證**。順序 2a→2b→2c（或視 2a、2b 為平行皆可）。台美各一套。
> **總經進研究部（改動1）與 regime 佐證（改動3）在此落實**：總經以兩種角色進入——(A) 2c 當 regime 切割的總經佐證；(B) 2b 建「總經↔策略」對應。兩者刻意分開，不混在 regime dating 內。

---

#### 階段 2a · Regime Dating（牛熊時期切割）
> 🆕 **改動3**：regime 用 **cycle（連續區間）切**——牛/盤整/熊各是「某日→某日」的連續時段，**不按年切**。
- **方法**：固定跌幅規則跑 bull-bear dating（cycle 連續區間）、用**大盤基準**。少量人看 4-3 核對轉折。
- **原則·做法甲（先切後驗）**：2a **純用價格**切（先知式切割），**不在切割時混入總經**；總經佐證延到 2c。理由：保持 2a 邏輯單純，符合老師「先切、總經來洗清作弊嫌疑」的描述（價格為主、總經為輔）。
- **🤖 LLM：regime 命名 + 寫定義**（Haiku，貼人話標籤，不做切分判斷）。
- ⭐ **v6·台美各一套 regime**（推翻 v5 全域一套）：全域一套會用錯誤危機窗污染階段3「危機相關」（如 2022 美股熊、台股未必同步）。各一套後台股用台股危機窗、美股用美股危機窗。「台美危機不同步」本身即跨市場分散有效的證據。連帶：階段3 跨市場相關算法隨之改變。
- ⏳ 跌幅 X% 閾值：待資料。
- **📌 定位更新（B架構連鎖）**：`regime_fit` 從「程式撚策略依據」→「agentic LLM 可查的工具情報之一」。計算不變、定位變。
- 產出：**台股一套 + 美股一套** `regime_table`，各策略 `regime_fit`。

---

#### 階段 2b · 總經↔策略對應（總經進研究部·改動1 落實）
> 🆕 **N1 定案**：ML 對應模型**建在研究部、離線 train/建、凍結進 strategy_map；實戰部只查、不重算**。
> **性質不玄**：本質是「輸入總經數字→對應策略報酬」的檢索/對應問題，非黑箱 LLM。

- **主方法（並存·互為情報）**：
  - **方法一 · k-NN 相似歷史檢索**（主）：找當前總經最像的歷史 K 個月，看那幾個月誰賺。極可解釋、無 overfit、天然抗幻覺。
  - **方法二 · 投資時鐘四格分箱**（輔）：成長×通膨兩軸切四格（復甦/過熱/停滯性通膨/衰退），預算各策略每格歷史表現。命中老師「可對外講的模式」、且供 2c 當佐證。手法延續學姊圖4-21 四象限分類。
  - **方法三 · 監督式迴歸**：❌ 不實作，僅論文對照論述（辯護：240~300月小樣本對每策略 train 模型易 overfit、且與可凍結/抗幻覺不符）。
- **頻率**：**月頻**（總經本質月/季頻；改日頻只會產生假重複 pseudo-replication，無效樣本仍~300）。**範疇**：台美各一套。

**離線建置流程**：
1. **抓總經（5欄 feature）**——成長/通膨/利率水位/利率方向或景氣 各1、**風險先留白**。台美對齊「概念軸」、用各市場最佳指標：
   - 美股(FMP)：GDP年增或失業率 / CPI年增 / 聯邦基金利率或10Y殖利率 / **殖利率曲線斜率(10Y−2Y)** /（VIX留白）
   - 台股(TEJ)：GDP年增或工業生產指數年增 / CPI年增 / 重貼現率或貨幣市場利率 / **景氣對策信號分數** /（留白）
   - ⚠️ 台美指標不同、各自標準化，**兩套 macro_history 不可直接互比**（本就台美分開，無妨；記一筆免誤用）。
2. **前視防護 · 發布滯後對齊（P6）**：各指標按實際發布延遲，只用「該月當下確定已公布」的最近一筆（CPI推約半月、GDP推約一季、利率即時…）。與財報 `fillingDate` 同一「時點可得原則」。⏳ 各指標滯後量待查官方發布時程。
3. **組月向量 + z-score 標準化**：平均/標準差**只用樣本內(2000–2025)算並凍結**，供實戰部沿用同一組（防 look-ahead）。**並存「近3月平滑版」**（P4，見下）。
4. **方法二四格歸類**：成長、通膨各以**歷史中位數(P3)**切高/低→四格。利率方向當**附加標籤**（不參與切格）。每月歸一格。
5. **接策略逐月報酬**（複用階段3 已存的日報酬彙總，不重存）。
6. **預算每格表現**：每策略×四格算平均報酬+勝率，**一律附樣本數 n 與信心等級**（高/中/低；n 過低標低信心但仍可查，不留白——延伸抗幻覺/引用標註精神）。

**凍結產出（進 strategy_map，以「可被工具查詢的結構化形式」存放，不預排成給 LLM 的表格）**：
- `macro_history`（台美各一，月×5欄，含**近3月平滑版**，每月已標四格）
- 凍結 z-score 參數、四格切割邊界（台美各一組）
- 各策略×四格的 平均報酬/勝率/**樣本數 n/信心等級** 表

**實戰查詢（實戰部即時、不凍結）**：
- 方法一：當前總經（**近3月平滑**）→凍結參數標準化→對 `macro_history` 算**歐氏距離(P2)**→取最近 **K** 個類比月（K 保留待定 P1）→各策略在類比月平均報酬+勝率。
- 方法二：當前落哪格→查該格各策略預算表現。
- 兩者**封裝為 agentic LLM 可呼叫的工具**；回傳須讓 LLM 看到「哪個方法、各自信心多少」（兩法信心不可混成一個）。
- **衝突處理**：兩法推薦/信心矛盾時，**由 LLM 權衡、須引用客觀依據說明理由**（不設機械仲裁規則）——衝突從「bug」變成「展示 LLM 判斷力」的素材。
- ⚠️ **out 期極端格對策**：若當前所在格歷史樣本/信心過低（如 2026 落在罕見組合），系統須能退回依賴方法一、或明確標示「當前環境歷史罕見、整體信心偏低」。
- ⚠️ **樣本內外**：信心門檻、四格邊界、z-score 參數皆**樣本內定並凍結，out 期沿用不重算**（凍結地圖不作弊的一環）。

---

#### 階段 2c · 交叉佐證（2a × 2b 匯流）
> 🆕 **改動3 落實·回應「regime 要總經佐證才不算作弊」**。2a、2b 完成後做，**不改動 2a/2b 內部計算**，只事後核對。
- **方法**：核對 2a 判熊的時段是否多落在 2b 衰退/停滯性通膨格；判牛時段是否多落在復甦/過熱格。
- **一致** → 2a 價格切割獲總經獨立佐證，洗清 look-ahead 嫌疑。
- **不一致（背離時段）** → **保留不修、僅標記**（做法甲精神：價格為主、總經為輔，背離是「待解釋的現象」而非「regime 切錯」）。此立場須在論文明講，以答口委「發現背離為何不修 regime」。
- **前提**：2c 佐證用的總經與 2b 五欄**同源**。
- 產出：regime×總經格 一致性標記；背離時段清單。

---

**參數層總表（階段2）**：

| 參數 | 設定 | 狀態 |
|---|---|---|
| P1 K值（類比月數） | 保留待定 | ⏳等資料 |
| P2 距離公式 | 歐氏距離 | ✅ |
| P3 分箱邊界 | 歷史中位數 | ✅方向 |
| P4 當前窗口 | 近3月平滑（歷史同步存平滑版、平滑對平滑） | ✅ |
| P5 fit分數 | 平均報酬+勝率都存 | ✅ |
| P6 發布滯後量 | 待查各指標發布時程 | ⏳等資料 |
| 信心門檻 n | 待資料看分布定 | ⏳等資料 |

### 階段 3 · 策略互補性（HRP 階層聚類，三組平權）🔄 v9 改寫
> **輸入←** usable_pool + 各策略逐月報酬序列（階段1）、**2c 統一出口（regime 窗 + 總經）**　**輸出→** cluster 樹（三組）、群間相關矩陣、co_fail_regimes、HRP 權重函式 + 凍結的報酬序列（給階段4、實戰部組合工具）
> 🔄 **v9 最大改動（老師 2026-08-19 指示）**：原本的「三組兩兩 Pearson」改為 **HRP（Hierarchical Risk Parity，階層式風險平價）**。相關係數仍是 Pearson，改的是**輸出形狀**——從 N² 個係數改為「這一坨該分成幾類、每類相關性多少」。
> 🆕 **v7 改動 4 + 三組平權重定位（保留）**：互補是**三組平權、各自獨立有用**——台↔台、美↔美、台↔美。三組是給 agentic LLM 挑兵的**三種平行情報工具**。

#### 為什麼從 Pearson 兩兩改成 HRP

**老師的原話（2026-08-19）**：
> 「我如果有 10 個策略跟 10 個策略，然後我去跑相關性，**他的相關性如果拿一個錯的，那就會錯得很離譜**」
> 「一樣去跑相關性（皮爾森），可是他告訴你的不是這 10 個跟 10 個的相關性，**他不是跑出 90 個數字，他是跑出說：這一坨應該分成幾類、每一類的相關性多少**」
> 「這樣組出來的組合，獨立性就是要能夠吃到**免費午餐**——不會同時賠、會一起賺」
> 「這個對你這一次這樣很有效，因為**有些很像、有些獲利很像**，那你先把他分階層」

**本研究的具體理由（比老師講的更強）**：
1. **規模**：usable_pool ≈ 14,000（台 7,162 + 美 6,916），兩兩對數約 **9,900 萬**。矩陣本身算得動（約 0.8 GB），但**輸出無法給 LLM 用**——沒有任何 agent 能消化 9,900 萬個數字。HRP 的 k×k 群間矩陣（k 約 20–50）才是可讀的。
2. **冗餘結構**：14,078 個策略實際只來自 **407 個獨立 F 組合**（台 219 + 美 188），每個 F 組合底下掛 35–38 個變體（20 個 C ×（v0/v1））。**同一 F 組合的變體選股池相同、報酬高度相關**。兩兩相關矩陣會被這個區塊結構灌爆；HRP 則會自然把它們收成一群，這正是要的效果。
3. **一石二鳥**：HRP 同時是**分群方法**與**配權演算法**（López de Prado 2016）。分群供階段 3 用、權重供實戰部 `T9 compute_weights` 用，**直接解掉原本待老師的 Q4「權重方法」**。

> ✅ **v9 撤銷**：v8「N² 可行性等階段1 筆數出來後試跑決定」這個未知數已解除——不是跑不跑得動的問題，是輸出形狀的問題，HRP 直接繞過。

#### 演算法規格（純程式，無 LLM）

1. **報酬矩陣**：🔴 **v9.1 更正（2026-08-20 全量實測）**——原寫「14,078 × 312 個月（2000-01~2025-12）」**不成立**。實測台股策略起始期橫跨 2000-06 ~ 2007-05（7 年），美股 2000-02 ~ 2002-03；歷史參差會使 pairwise 相關矩陣**非半正定**、距離違反三角不等式，而 `scipy.linkage` 不會報錯、會給出無意義的樹。
   **✅ 定案：每棵樹各自共同窗**——台↔台 `2007-01~2025-12`（228 月，保留 7,160/7,162）、美↔美 `2002-01~2025-12`（288 月，保留 6,915/6,916）、台↔美 `2007-01~2025-12`（228 月，保留 14,076/14,078）。
   捨棄的台股 2000–2006 正是已知財報資料洞期間（90% 假 0），**是排除不可信資料而非損失資料**；228 月仍涵蓋 2008/2015/2018/2020/2022 全部危機段。代價：三棵樹窗不同 → 群間相關不可跨樹比較。配套：另用 2003-01 窗跑一次台股樹做 ARI 穩健性對照。
   詳見 `落差處理方案_v1.md` 落差3、`系統設計文件_v1.md` DD-03。用月頻，與階段 2b 對齊。
2. **相關**：Pearson（老師：不用弄太細）。
3. **距離**：`d(i,j) = sqrt(0.5 × (1 − ρ(i,j)))` —— 標準相關距離，滿足距離公理。
4. **階層聚類**：`scipy.cluster.hierarchy.linkage`，method 用 `single`（HRP 原論文）或 `ward`（分群較平衡）。⏳ 兩者都試、看樹的可讀性決定。
5. **準對角化（quasi-diagonalization）**：依樹狀圖重排相關矩陣，讓相似策略相鄰。
6. **遞迴二分（recursive bisection）**：由上而下切分、依逆變異數配權 → HRP 權重。
7. **分群層級**：切三個層級 `cluster_L1 / L2 / L3`（粗→細）。⏳ 各層目標群數待資料，原則是 L1 約 5–10 群（給 LLM 讀）、L3 約 50–100 群（給快篩配額用）。

#### 三組平權（保留，改成三棵樹）

| 組別 | 樹 | 用途 |
|---|---|---|
| 台↔台 | 台股樹（N=7,162） | 台股池內部分散 |
| 美↔美 | 美股樹（N=6,916） | 美股池內部分散 |
| 台↔美 | 台美合併樹（N=14,078） | 跨市場分散 |

**跨市場賣點·由 HRP 直接呈現**：在台美合併樹裡，若台股策略與美股策略**分屬不同的高層群**，就是跨市場分散有效的直接結構性證據——比原本比較兩個相關係數的大小更有說服力。

#### 常態 / 危機兩套（保留並升級）

- **常態樹**：整段共同時間軸。
- **危機樹**：三組各用對應危機窗（台↔台 用台股危機窗、美↔美 用美股危機窗、台↔美 判兩套）。
- 🆕 **升級點**：不只比相關係數，而是**比較兩棵樹的結構差異**。若危機期的樹「塌成少數幾群」（有效群數大幅下降），那就是「危機時策略共同崩潰」的**直接量化證據**，比原本的相關係數對比有力得多。這正是論文支柱之一「台股市場內部策略在危機時期共同崩潰、需跨市場分散」的量化形式。
- 🆕 **老師延伸建議**：「你甚至可以把它分成說我就分成幾個迴圈，例如說最近從疫情以後」→ **分 regime 段各做一次 HRP**，觀察階層結構隨環境的變化。此為選配、視進度做。

#### 產出

| 產出 | 形狀 | 說明 |
|---|---|---|
| `cluster_id_L1/L2/L3` | per-策略 | 進 strategy_map 主表①，供 T2 快篩配額用 |
| `cluster_meta` | per-群 | 群內成員數、群內平均相關、代表策略、該群在各 regime 的表現 |
| `cluster_corr_matrix` | k×k | **群間相關矩陣**，這是給 LLM 讀的核心產物 |
| `co_fail_regimes` | per-群 | 🔄 **定義改變**：從「兩兩危機相關高」改為「**危機樹中與本群合併的群**」（危機期塌在一起 = 共跌） |
| `hrp_weights(策略清單)` | 函式 | 遞迴二分配權，供 T9 即時呼叫 |
| 逐月報酬序列 | per-策略 | 凍結，供階段4 與實戰部 T6/T8 |

> ⚠️ **v9 刪除 `complement_partners`（策略×策略清單）**：被 `cluster_id` + `cluster_corr_matrix` 取代。理由：per-策略的夥伴清單在 N=14,078 下會產生 9,900 萬列，且資訊重複。**實戰部 T5 需連帶改寫**（見實戰部 v8）。

**🤖 LLM 點③ · 群間互補的因果解釋**（Opus）
- 🔄 **v9 對象改變**：從「解釋精華策略配對為何互補」改為「**解釋群與群之間為何互補**」。
- **好處有三**：① 群數只有幾十，Opus 成本從幾萬筆降到幾十筆；② 解釋層級更高（族群 vs 個別策略），更適合寫進論文；③ 一個群的解釋可被該群所有成員共用，Agent2 引用時涵蓋率 100%。
- **✅ B3 分工界線定案（保留）**：研究部離線 LLM 產「通用/結構性」互補解釋（凍結成客觀情報）；實戰部 agentic LLM 挑兵時**引用**、不重寫。
- 🆕 **v8 小接縫（保留）**：通用互補解釋文字**須凍結**且**工具層要查得到**（見實戰部 T5/T13）。

- ⏳ 待資料：分群層級的目標群數、linkage method、「相關多低算互補」改為「群間相關多低算互補」。
- **📌 定位**：三組 HRP 樹 = agentic 挑兵的**三種平行互補性工具**（三組平權、定位為 LLM 情報）。

### 階段 4 · 歸納成 strategy_map（本質是彙整，非分類）
> **輸入←** 階段1/2/3 全部產出　**輸出→** 凍結的 strategy_map（主表①+附屬②③④⑤，見開頭「輸出格式」）→ 實戰部
> ## 📌 v7 定案 · 階段4 定位（B架構+agentic 連鎖·項7）
> **strategy_map = agentic LLM 工具的資料後端。** 各欄位（factor_type、regime_fit、EffN、stability_grade、cluster_id、return_story 判決、總經對應…）**就是實戰部各工具的查詢來源**。
>
> **分層原則（與階段3 B3 同一原則）**：
> - **研究部離線凍結 = 策略的「客觀身份證」**：固有、不隨 regime 變的屬性。
> - **實戰部 agentic = 「當前情境下要不要用這張身份證」**：即時、隨 regime/macro 變的挑兵判斷，**引用**凍結身份證當依據、不重寫。

- **不從零建新表**，在階段0 的 `candidate_index` 骨架上 merge 加值欄位。這是「成績彙整處」——前三階段算好的等級/數字，按策略 ID join 進來。
- **只放客觀特徵，不放需求門檻**（✅定案）：客觀體質資料放這裡並凍結；「保守型=MDD<X」這種**帶門檻、會變**的需求映射**不預先寫死**——交給 agentic LLM 挑兵時按當前需求即時判斷。

> 🔄 **v9 刪除 `valuation_verdict` 欄位與 LLM 點⑤**
> **理由**：老師 2026-08-19 明確判定 V 構面「**是好玩的，沒有什麼用**」，實測也支持（雙輸象限台股 48.1%、美股 72.8%）。而 12 格矩陣原本引用的 `valuation=favorable`，其資訊等價於 C-1 已有的 `V` 欄位（v0/v1）。
> **決定**：`valuation` 直接用 `V` 欄位表達，階段4 不再另外產 `valuation_verdict`，也不需要 LLM 寫條件註記。
> **保留**：4-24 的「條件反轉」現象仍有價值（特定 F 子樹下 v1 優於 v0），改為**程式算的布林欄位 `v1_beneficial`**，不需 LLM。
> **連帶**：v9 保留 v1 策略在庫內（依使用者決定，不移除），僅在論文正文降級為「條件性工具」。

- **🤖 LLM 點·return_story（v7 定案·乙案：判決凍結 + 文字按需）**：
  - **四道安檢（是否靠少數股/產業β/規模效應/真alpha）由程式算出結論、凍結進 strategy_map**（便宜、客觀、抗幻覺）。
  - **「串成 story 文字」那步不對幾萬筆全預寫** → 改為 agentic 挑兵時、只對候選的少數策略即時生成。
  - 抗幻覺不變：文字是把**凍結的程式判決**串起來，LLM 不下判斷、不推翻數字。
  - ⚠️ 殘餘風險（疑慮四，暫掛）：LLM「串」時可能偷渡程式沒給的因果連接。方向：結構化條列輸出、禁止添加程式未提供的因果。
  - 🆕 v9：第三道安檢「規模效應」現在有 C-5 的 `size_tilt_pct` / `smallcap_share` 當客觀依據，判決更扎實。
- 建議 return_story 判決先人工做一版當 ground truth，再讓 LLM 複現文字。

---

# 第三部分 · 程式 vs LLM 分工 + 模型強度

> 原則：**數字→數字用程式；文字↔數字（判讀、解釋、命名）才用 LLM。LLM 不碰篩選決策。**

> 🔄 **v9 重大變更：LLM 點從 6 個減為 3 個。**
> 刪除 ①（B2-2 視覺判讀）、⑤（valuation 註記）、⑥（關卡C 混合型）。
> **刪除 ① 的意義最大：現在全流程再無任何 LLM 節點參與篩選/淘汰決策**，抗幻覺第一道防線從「原則」變成「結構上不可能違反」。

| LLM 點 | 階段 | 任務性質 | 建議模型 | 為何這個強度 | 省 token 關鍵 |
|---|---|---|---|---|---|
| ② regime 命名 | 2a | 貼人話標籤 | **Haiku** | 不做判斷、錯了好改，最輕 | 批次命名、只餵時期統計 |
| ③ **群間**互補因果解釋 | 3 | 因果經濟推理 | **Opus** | 核心賣點、進論文、真分析 | 🔄 v9：對象從「策略配對」改為「**群與群**」，群數僅數十，成本大降 |
| ④ return_story | 4 | 為程式判決寫 story（**判決凍結·文字按需**）| **Opus** | 差異化立論核心；安檢已歸程式故負擔輕 | 判決凍結、文字按需，只串候選策略 |

**v9 刪除的 LLM 點（記錄備查，勿當疏漏補回）**：

| 原編號 | 原任務 | 刪除理由 |
|---|---|---|
| ① 成片vs孤立（Sonnet）| 階段1 B2-2 視覺判讀 | 唯一參與篩選的 LLM 節點，與抗幻覺鐵則衝突；相似性改由階段3 HRP 客觀處理 |
| ⑤ valuation 註記（Sonnet）| 階段4 | `valuation_verdict` 欄位已刪除，改用 `V` 欄位 |
| ⑥ 關卡C 混合型（Haiku）| 階段1 C | 只有 F1+F2 兩個因子，規則覆蓋率 100%，無邊緣案例 |

**整體 token 策略**：3 個 LLM 點只有 ③④ 用 Opus（進論文的核心分析），② 用 Haiku（貼標籤）。最大省法不是選小模型，而是**用程式把送 LLM 的量壓到最少**。③④ 都在離線研究部一次性跑完、不在即時迴圈，成本可控。
> ✅ v9 清理：原「若採 walk-forward 則 ③④⑤ 要 ×N 遍」整句刪除——老師已定案 **walk-forward 不用滾**。

# 第四部分 · 抗幻覺三道防線

> 為何重要：策略特性若被 LLM 幻覺污染，實戰部會拿著錯地圖操作，後果嚴重。

1. **LLM 不碰篩選**：所有淘汰/篩選決策由程式數字定，LLM 只在解釋類節點出手。→ 不會幻覺篩掉好策略。
2. **判斷收回程式、LLM 只寫字**：return_story 安檢歸程式、valuation 主值歸程式、關卡C 要求數字佐證且可反驗。LLM 從「自由判斷」降為「為程式結論寫說明」。
3. **可數字反驗**：凡 LLM 輸出的判斷（關卡C型別、成片孤立、return_story 陳述），都要能被程式數字檢查一致性，不一致觸發人工複查。

> 這三道防線同時就是 A2「衡量指標」的基礎——「可數字反驗」機制轉個身就是「幻覺率」的客觀量測工具。防幻覺與評分是同一套機制。

---

# 第五部分 · strategy_map 欄位表（🔄 v9 全面更新）

| 欄位 | 來源 | 給誰 |
|---|---|---|
| 🔴 **`strategy_uid`（= `market::strategy`，主鍵）**, `strategy`, `market`, `F1_factor`, `F1_band`, `F2_factor`, `F2_band`, `F2_empty`, `C_id`, `C_source`, `C_rule`, `V` | **階段0**（結構拆解）| 檢索鍵 / Agent1 快篩 |
| `CAGR`, `max_drawdown`, `win_ratio`, `avg_holdings` | 階段 −1 既有（候選 CSV）| Agent1 |
| 🔴 `sharpe_ann`, `daily_sharpe`, `avg_drawdown` | **v9.1 更正：不在候選 CSV**，階段0 join `stats.parquet` 補 | Agent1 |
| 🔴 `artifacts_dir` | **v9.1 更正：不存在，階段0 依 v0→L3 / v1→L4 規則解析** | 調閱明細/圖 |
| `cagr_pct`, `mdd_pct`（市場內分位）| **階段1-C3** 🆕 | Agent1 快篩 |
| `ann_vol`, `return_shape`, `risk_shape`, `worst_year`, `neg_year_count`, `max_consec_loss_months` | **階段1-C3** 🆕 | Agent1/2 |
| `credibility_grade`, `effective_n`, `top1_share`, `rotation_score` | **階段1 關卡A** | Agent1/2 |
| `stability_grade` | **階段1 關卡B2-1** | Agent1 |
| `factor_type`, `factor_type_basis` | **階段1-C2** | Agent1/2 |
| `holdings_median`, `holdings_p10`, **`empty_ratio`**, `eval_month_ratio`, `turnover_ann`, `entries_per_year` | **階段1-C4** 🆕（v9.2 更正：`coverage_ratio` 已停用）| Agent1 |
| `size_tilt_pct`, `smallcap_share` | **階段1-C5** 🆕 | Agent1/2 |
| `annual_returns`（2000–2025）| **階段1-C6** | 階段2 用 |
| `is_usable` | 階段1 尾端 | Agent1 |
| `regime_fit`（台/美各一）| 階段2a | Agent1 |
| 四格×該策略 平均報酬/勝率/樣本數/信心 | 階段2b | Agent1 |
| `cluster_id_L1/L2/L3` | **階段3（HRP）** 🆕 | Agent1 快篩配額 / Agent2 |
| `co_fail_regimes`（🔄 定義改為「危機樹中與本群合併的群」）| 階段3 | Agent2 |
| `v1_beneficial` | 階段4 🆕 | Agent1 |
| return_story 四道安檢**判決**（等級/布林，非文字）| 階段4 | Agent2 |
| ~~`artifacts_dir` | 階段 −1 既有 | 調閱明細/圖~~ | （已移至上方並更正來源）| |

**v9 刪除的欄位（記錄備查，勿當疏漏補回）**：

| 欄位 | 刪除理由 |
|---|---|
| `valuation_verdict` | 老師判定 V 無普遍價值；資訊等價於 `V` 欄位 |
| `complement_partners` | 被 `cluster_id` + `cluster_corr_matrix` 取代；per-策略夥伴清單在 N=14,078 下會產生 9,900 萬列 |
| 強因子清單（B1）| B1 整節刪除，已被階段 −1 的 Phase 1 線性檢定取代 |

---

# 第六部分 · 交棒：三個 Agent 讀什麼（實戰部，只查欄位）

> 原則：Agent 只查 strategy_map 現成欄位，不重算、不重看圖。

- **Agent0（資料節點·純程式）**：讀當前總經 + `regime_table`（台美各一套）+ 階段2b 凍結對應表。產**一份** regime + macro_fit，分丟 Agent1/Agent2。
  - ⚠️ 接縫：台美各一套 regime，當台美處於不同 regime（台股牛、美股熊）時，Agent1 撈策略邏輯要能處理。
- **Agent1（agentic 精挑，匯流點）**：讀 ①當前 regime ②strategy_map 的 `regime_fit`/`factor_type`/`stability_grade`/`mdd_pct`/`effective_n`/`cluster_id`/`smallcap_share`/`empty_ratio`/`F2_empty`。→ 候選策略組。
  - 🆕 **v9 新增依賴 `cluster_id`**：見下方接縫警示。
- **Agent2（Explainer）**：讀 return_story 判決 / `cluster_id` + 群間互補解釋 / `co_fail_regimes` / `top1_share` / `effective_n` / `size_tilt_pct` + regime。→ 因果說明 + 失效警示。

> 🔴 **v9 新發現的接縫 · 快篩的「多樣性假象」**
> `usable_pool ≈ 14,000`，但實際只來自 **407 個獨立 F 組合**（台 219 + 美 188），每個 F 組合底下掛 35–38 個變體，**選股池相同、報酬高度相關**。
> **後果**：Agent1 Step2 快篩若篩到剩 300 個，這 300 個可能全部來自 10 個 F 組合——LLM 以為自己在 300 個裡挑，實際上只有 10 種真正不同的東西。
> **對策**：`T2 filter_pool` 必須加一條 **cluster 級配額**（每個 `cluster_id` 最多取 N 個），確保遞給 LLM 的候選有真實多樣性。這正好是階段3 HRP 的直接應用。**此為實戰部必改項，見實戰部 v8。**

---

# 第七部分 · 三條需求路徑（🔄 v9 更新）

> 起點：階段1 產出的 `usable_pool`。需求門檻即時算，不進凍結地圖。論文核心做保守/積極，全天候列延伸。

### 7.1 保守型（風險優先）
1. 下檔保護：`mdd_pct` 上限 + `risk_shape` = 淺回撤。
2. 危機年表現：查 `regime_fit` 確認 2008/2020/2022 沒崩（整段 MDD 小可能只是運氣避開危機）。
3. 分散性加嚴：`effective_n` 門檻高於共同前提。
4. 🆕 **規模與可執行性**：`smallcap_share` 上限 + **`empty_ratio` 上限** + `holdings_p10` 下限。（v9.2：原 `coverage_ratio` 下限已停用）
5. 🆕 **群分散**：候選須跨 `cluster_id_L1` 至少 N 群，且避開 `co_fail_regimes` 互相重疊者。
> 🔄 v9 刪除原 Step1/4 的 `valuation_verdict` 查詢（欄位已刪）。

### 7.2 積極型（報酬優先但防假）
1. 報酬排序：`cagr_pct`。
2. 假 alpha 檢查：查 `top1_share`/`credibility_grade`/`rotation_score`，**排序越前段門檻越嚴**（高報酬正是假 alpha 最愛偽裝處）。
3. 高原確認：查 `stability_grade`。
4. 🔄 **原「查 B1 強因子清單」改為**：查 `factor_type` + `F1_factor`（因子強弱已在階段 −1 的 Phase 1 判定過，直接用判定結果）。

### 7.3 全天候（跨背景穩健優先，延伸）
1. 🔄 **原「4-16 橫看找配任何 F 都好的 C」改為**：查階段 −1 的 Phase 3 C 排名（增益為正比例高者 = 不挑 F 組合）。
2. 跨 regime 表現：`annual_returns` × `regime_fit`，各狀態都不掉後段。
3. 🔄 **互補組合改用 HRP**：跨 `cluster_id_L1` 挑選 + 檢查 `co_fail_regimes` 不重疊（單一策略難全天候，靠組合達成）。

# 第八部分 · baseline 三階梯（新增，待老師定範圍）

> 對應第一部分 A1/A2。目前流程只描述「多 Agent 完整版」；baseline 是為了比較而需平行跑的對照，屬架構級增量，待老師確認做到哪一級。

| 階梯 | 內容 | 證明什麼 |
|---|---|---|
| ① 純量化（無 LLM）| 拿掉所有 LLM 節點、換簡單規則，產「無 LLM 版 strategy_map」| LLM 整體有無價值 |
| ② 單一 Agent | 用 LLM 但一個大模型全包（不分工）| **多 Agent 架構本身**有無價值 |
| ③ 多 Agent（完整）| 本研究系統 | 主張 |

**另需對照組**（與 baseline 不同）：保留「被篩掉的策略」，out 期一起追蹤，證明「篩留的 > 篩掉的」= 篩選有效。

**衡量指標**（見 A2）：因 LLM 不碰選股，優勢不在報酬。主用**幻覺率/事實一致性**（客觀、可數字反驗）+ 一致性 + 小規模人工校準；LLM-as-a-judge 待老師定。

---

# 第九部分 · 待決總表（🔄 v9 更新）

## ✅ 架構級（老師已回答）
| # | 事項 | 老師的答案 |
|---|---|---|
| A1 | baseline | ✅ 跟**大盤**比就好（系統有 alpha 必贏純大盤）|
| A2 | LLM 衡量指標/幻覺率 | ❌ **先不用做**（LLM 不選股、幻覺風險小）|
| A3 | walk-forward | ❌ **不用滾**（窮舉全跑、無 overfitting）|

## ✅ 老師 2026-08-19 會議新增定案
| # | 事項 | 定案 |
|---|---|---|
| **M1** | 整套 Phase 1–4 SOP | ✅ **驗收通過**（老師逐張看完曲線，「台股跟美股都有大概 6、7 個，差不多」）|
| **M2** | F2 的角色 | ✅ 「**通常是 F1 加 C 最好**」——理由是「F1 你去管他，那個池子尺寸會變」。→ 新增 `F2_empty` 標記供分流 |
| **M3** | V 構面 | ✅ 「**V 是好玩的，沒有什麼用**」→ 刪 `valuation_verdict`、刪 LLM 點⑤；v1 策略保留在庫但論文正文降級 |
| **M4** | 美股倖存者偏誤 | ✅ **不重跑**（「要嚴格做得買資料庫，我自己也沒買」「68% 減 10% 還是很強」），但誠實寫入 limitations |
| **M5** | 階段3 方法 | ✅ **改用 HRP**（Hierarchical Risk Parity）取代兩兩 Pearson |
| **M6** | 老師要親自跑程式 | 🔜 下週在學校交付可執行版本 |

## ✅ N 問題（全數定案）
| # | 事項 | 狀態 |
|---|---|---|
| N1 | 總經 ML 模型放哪 | ✅ 研究部離線建凍結、實戰部只查 |
| N2 | Agent0 與研究部總經模型如何接 | ✅ Agent0 產一份 regime+macro 分丟；一建一查不重疊 |
| N3 | 總經進研究部 vs 抗幻覺鐵則 | ✅ 張力化解（ML 檢索器非 LLM 敘事）|
| N4 | 三組互補的實作 | ✅ 三組平權，🔄 v9 改為三棵 HRP 樹 |
| N5 | regime cycle 切法 + 總經佐證 | ✅ 做法甲先切後驗、2c 交叉佐證 |

## ❌ v9 撤銷的待決事項（記錄備查，勿當疏漏補回）
| # | 原事項 | 撤銷理由 |
|---|---|---|
| **Q4** | 組合權重方法待老師確認 | **HRP 本身就是配權演算法**（遞迴二分），一併解決 |
| **B1** | 階段0「換股<5」的原始依據 | 該門檻已刪除，被 Phase 2 的 `MIN_HOLDINGS=10` 取代 |
| — | 美股無 4-16 圖需自補（最優先）| B1 整節刪除，此待辦作廢 |
| — | 階段3 N² 可行性待試跑決定 | 不是算不算得動的問題，是輸出形狀的問題，HRP 直接繞過 |

## ❓ 待老師（仍未問/未答）
| # | 事項 |
|---|---|
| B3 | 階段3 研究部離線解釋 vs 實戰部 Explainer 即時解釋的界線（已有暫定方案，見階段3）|

## ⏳ 待資料（用市場內分位數框架定）
- 階段1：EffN 門檻、Top1 上限、`rotation_score` 切點（⚠️ 須用 P95+，62.4% 的值並列於 0）、`empty_ratio` 上限、`stability_grade` 分級
- 階段2：K 值（類比月數）、各指標發布滯後量、信心門檻 n、regime 跌幅 X%
- 階段3：**HRP linkage method（single vs ward）**、三個分群層級的目標群數、「群間相關多低算互補」
- 實戰部：快篩縮到多少、**cluster 配額每群取幾個**

## 🔧 暫掛（已知、待處理）
| 項 | 說明 |
|---|---|
| return_story 殘餘幻覺 | LLM「串 story」可能偷渡未給的因果；未來改結構化條列輸出 |
| 美股倖存者偏誤 | ✅ 已定案不重跑，寫進 limitations |
| out-sample 只到 2026 的誠實度 | 論文誠實面對，做多少算多少 |
| ~~12 格矩陣改寫~~ | ✅ **已於實戰部 v8 完成**：三類危機格改用 cluster 分散、保守型牛市格移除 valuation、全格新增 `smallcap_share` |
| ~~快篩多樣性假象~~ | ✅ **已於實戰部 v8 完成**：`T2 filter_pool` 加 `cluster_quota` 參數 |
| HRP 實作細節 | ⏳ linkage method（single vs ward）、三層群數，待試跑決定 |

## ✅ 已定案（記錄備查，⚠️ 標示已被推翻者）
階段 −1 Phase 1–4 SOP（老師驗收通過）｜自建宇宙基準（台 8.67% / 美 12.35%）｜階段0 格式整備｜~~階段0 相對換股門檻~~（v9 刪除）｜~~關卡A 硬門檻~~（v7 改放寬）｜關卡A 集中型防誤殺｜`rotation_score` 定義（年度前三大貢獻股 Jaccard）｜~~關卡B 嚴篩~~（v7 改放寬）｜~~B1 強因子清單~~（v9 刪除）｜~~B2-2 LLM 視覺判讀~~（v9 刪除）｜關卡C 純規則（v9 刪 LLM 補邊緣）｜關卡C 初標純年度報酬斷循環｜`*_pct` 一律 groupby(market)｜階段2 cycle 切 + 總經佐證 + 台美各一套｜~~階段3 三組兩兩 Pearson~~（v9 改 HRP）｜階段3 三組平權（保留）｜階段4 客觀特徵放研究部 / 需求門檻放實戰部｜~~valuation_verdict~~（v9 刪除）｜return_story 安檢歸程式 + 乙案文字按需｜抗幻覺三防線（v9 強化：LLM 已完全退出篩選）｜baseline 大盤｜walk-forward 不滾｜不做幻覺指標｜最終產出 AI 對照表

---

*文件結束。v9 於 2026-08-19 老師會議後全面 review；圖表判讀見「第四章_24張圖_圖鑑_銜接版」；實戰部對接見「實戰部架構 v8」。*