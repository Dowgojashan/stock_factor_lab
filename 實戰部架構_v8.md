# 實戰部架構（v8）

> **本檔定位**：實戰部（Operations Department）的架構文件。與「**研究部完整流程 v9**」對接。
> **v8 更新（2026-08-19 老師會議後 · 研究部 v9 連鎖）**：
> 1. **12 格門檻矩陣全面改寫**——`complement_partners` 已刪、`co_fail_regimes` 定義改變、`valuation_verdict` 已刪，三類的危機格與保守型牛市格都要重寫；並新增 `smallcap_share` 維度。
> 2. **工具層 12 → 13 個**——新增 `T13 get_cluster_info`；`T5`/`T9` 改寫；`T2` 必須加 cluster 配額。
> 3. **配權方法改為 HRP**——同時撤銷待老師問題 Q4。
> 4. **🔴 新發現的接縫：快篩多樣性假象**——usable_pool 14,000 實際只來自 407 個獨立 F 組合，必須用 cluster 配額處理。

---

## ⭐⭐⭐ v7 · 實戰部整體層定案（B架構 + agentic）⭐⭐⭐

> 對應研究部 v9 的核心定位重校準。整體層決策已定，可據此逐一改寫各 Agent。

### 架構定位：B（程式粗篩 + agentic LLM 精挑）
- 舊定位「Agent1 純規則、不用 LLM」**已推翻**（見 §3 Agent1，待改寫）。新：**Agent1 = agentic LLM 精挑**。
- 三層分工（同研究部）：①程式硬篩（階段 −1 SOP + 研究部階段1 → usable_pool ≈14,000）→ ②程式算情報/工具（strategy_map + 工具層）→ ③**實戰部 agentic LLM 精挑**。
- **抗幻覺鐵則重述（沿用研究部 v9）**：LLM 只在程式篩定的堪用池內判斷式精挑、不能生成池外或虧錢策略、權衡須引用客觀依據。

### 第一件 · agentic 邊界：甲 + 有限回饋（暫定，待問老師）
- **甲**：只有 Agent1 是 agentic agent；Agent0（程式/查詢）、Agent2（解釋）維持相對固定角色。三段單向流水線 Agent0→Agent1→Agent2。
- **+有限回饋**：Agent2 的驗證可**觸發 Agent1 一次重選**（有限、不無限來回）。此接線屬跨 Agent1/Agent2，待兩者都討論完再定。
- 理由：老師逐字稿要的最終產出（20年對照表 + AI對答）屬呈現導向、不需複雜三Agent協作；輕架構可控、可複現、好辯護。多代理人靠「跨部門+跨Agent角色分工」體現，非來回協作。
- ❓ **待問老師**：最終表格要「純呈現」還是「互動問答」——決定實戰部複雜度（純呈現→純甲即足；互動→甲+回饋/延伸）。

### 第二件 · 12格門檻矩陣命運：丙（預設起點、可有理由偏離）
- 矩陣（類型×regime 撈取條件）**保留為預設**：Agent1 挑兵預設按矩陣條件，但可**有理由地偏離**（發現矩陣會漏好兵、或當前總經特殊），偏離須記錄理由、可被驗證。
- 完美符合「半結構 agentic」：骨架（矩陣）兜底、LLM 在骨架上發揮；矩陣心血保留；可複現中上；防 LLM 離譜（如危機時給保守型亂配）。
- 矩陣計算邏輯留用，定位從「程式決策步驟」→「agentic 的預設工具 `get_recommended_criteria(類型,regime)`」。

### 最終產出：產出A（研究部）+ 產出B（實戰部）
- **產出A = 研究部的 20年情境×策略表現對照回顧表**（已補進研究部 v9）。
- **產出B = 實戰部即時應用**：當前情境→agentic 挑一組兵→解釋。**組合層為主**（呼應老師「免費午餐」）。
- **A、B 須共用同一套凍結 strategy_map**，避免「回顧選X、即時選Y」矛盾。
- **AI 對答 = 系統延伸、視進度做**；任意假設情境即時跑列未來工作。
- **表格是否×3（三類投資人各一）= 綁定待老師確認「是否按三類跑 out」**。

### 跨 Agent 待辦（進 Agent 討論時處理）
- **✅ N2 已定案**：Agent0 產「一份 regime+macro、分丟 Agent1/Agent2」；不自己判斷總經（研究部階段2b 建表、Agent0 查表，一建一查不重疊）；Agent0 純程式。詳見 Agent0 節。
- **✅ 有限回饋機制已定案**：Agent2 抗幻覺核對發現「硬矛盾/客觀事實衝突」時，觸發 Agent1 重選一次（不無限來回、須帶依據、主觀不夠好不觸發）。詳見 Agent2 節。
- **✅ 工具層詳細規格已定案**：中粒度、原子化、無狀態、12個工具（T1-T12）對應骨架各步 + 新增工具檢查清單 + 擴充說明。詳見 §3.5 工具層。
- **✅ 組合層工具已納入**：`T8 compute_portfolio_risk`（見 §3.5），用凍結報酬序列即時算組合整體風險/曝險，不設研究部階段。
- 🔴 **v8 新增待辦 · 快篩多樣性假象**：`T2 filter_pool` 必須加 cluster 級配額。詳見「撈取數量處理」節。
- ✅ **v8 撤銷 Q4（權重方法選型）**：HRP 同時解決分群與配權，不需再問老師。

### 逐一改寫順序：Agent1 → Agent0 → Agent2
- 先 Agent1（agentic 精挑主角，定了它 Agent0/2 角色才好定）→ 再 Agent0（N2）→ 最後 Agent2（與 Agent1 邊界 + 有限回饋）。
- ⚠️ 以下 §3 各 Agent 仍多為 v6 舊定位，逐節改寫時更新。

---

## ⭐⭐ v6 · 老師會議後重大更新（與研究部 v7 同步）⭐⭐

> 這次跟老師報告後的指示，影響實戰部的部分如下。下一對話請以本區為準。

### 🆕 三個架構級待決，老師已給答案（原文中標「待老師」處以此為準）
- **walk-forward**：❌ **不用滾**。→ 本檔所有「若採 walk-forward 升級為建議C」的內容作廢，主流程就是 out 期逐季/一次跑（做多少算多少，out 只到 2026）。
- **baseline**：✅ 跟**大盤**比就好。→ 本檔「baseline 三階梯/分兩層」簡化為「跟大盤比」為主。
- **LLM 幻覺指標**：❌ **先不用做**（LLM 不選股、風險小）。→ 驗證階段的「幻覺率」指標移除；Agent2 抗幻覺設計仍保留（那是設計、非衡量指標）。

### 🆕 總經進研究部 → 連動實戰部（N1 已定案、N2 待 Agent0 改寫）
- 老師要總經進研究部、用 ML 建「總經→策略」對應模型。這會連動改實戰部 Agent0：
  - ✅ **N1 已定案**：ML 模型**放研究部（離線凍結）、實戰部只查**（見研究部 v9 階段2b）。
  - 🔜 **N2 待 Agent0 改寫**：原 Agent0 兩條線（技術面 regime + 總經報告）與研究部凍結總經模型如何接、會否重複？→ Agent0 逐一改寫時處理。

### 🆕 其他連動
- **Agent0 的三個待老師問題**（總經是否參與 regime、新聞、台美整合）→ 併入上面 N1/N2 一起在下一對話重新討論（因總經定位改變了）。老師已明確：**regime 切割要有總經佐證**（不能純價格切）。
- ✅ **權重方法（Q4）· v8 撤銷**：老師 2026-08-19 指示 HRP，HRP 本身即含配權（遞迴二分），不需再問。等權保留為 baseline 對照。
- **最終產出**：老師要「AI 對照表」——挑有趣段落回答（如「今年第三季可能像 2024 某段，因 GDP 像」），非平淡回答整段。實戰部輸出可朝此形式設計。

---

> **v5→v6 說明**：v5 是「三 Agent 資料流 + 主流程 + 驗證階段（閉環）」完整版。v6 主要是套用老師會議答案（上區），原文細節多數仍有效，僅 walk-forward/baseline/幻覺指標三處依上區調整。

---

> **本檔定位**：實戰部（Operations Department）的架構文件。與「研究部完整流程 v6」對接——實戰部讀取研究部產出的凍結 strategy_map。
> **目前範圍**：三個 Agent 資料流全部走完 + **主流程 + 驗證階段（v5 新增，實戰部從開環變閉環）**。
> **狀態**：三 Agent 資料流 + 主流程 + 驗證階段均已展開；剩 LangGraph 編排、Agent 間傳遞格式為實作細節；正式實驗規模待研究部 walk-forward/baseline 定案。
>
> **v5 相對 v4 的重大新增（補齊 review 找出的結構性缺口）**：
> 1. **主流程（§4.1）**：定義「實戰部跑一次」的完整輸入輸出與節奏。含 walk-forward 對應。
> 2. **驗證階段（§6）**：實戰部原本只有「產出建議」（開環），新增「驗證建議對不對」（閉環）。含需求達成度、風險警示事後準確性兩大差異化指標。
> 3. **對接與元件補洞（§4.2）**：regime 判定規則交接物、out期績效計算器、多類撈到、baseline 分層。

---

## 1. 定位：實戰部 vs 研究部

- 實戰部**只運作在 out 期（2026 年初迄今）**，讀取（不重算）研究部用 2000–2025 建好、**凍結的** strategy_map。
- 讓實戰部回頭重跑 in-sample = 作弊（look-ahead）。這是樣本內外分割的意義。
- ❓ 若研究部採 walk-forward（待老師），實戰部驗證方式要跟著改（不再是跑一次，而是每段都跑）——此部分待定。

---

## 2. 起點：固定三種投資人類型

- **不做需求理解**：實戰部不設「接收/判斷投資人需求」的角色。
- **直接跑三種固定類型**：保守型、積極型、全天候型。
- **三類都要跑，各出一份結果**（對應研究部第七部分的三條需求路徑）。
- 三類差異的價值：證明「不同需求 → 撈出不同策略」的媒合主張。

---

## 3. 三個 Agent 的骨架

> 命名統一為 **Agent0 / Agent1 / Agent2**，廢除舊的 O1/O2/O3 編號（舊編號差一號、易混淆）。
> Critic Agent：**已排除**，不設此角色。

### Agent0 · Macro（v7 改寫·資料流已展開）

**定位**：實戰部資料鏈起點，一季執行一次。**產出一份「當前 regime + macro」，分別丟給 Agent1（挑兵）與 Agent2（解釋）。純程式，無 LLM。**
**節奏**：一季一次。判完後，下游對保守/積極/全天候三類各跑一遍。

> ## ⭐ v7 改寫（N2 定案）：一份產出、分丟兩下游；Agent0 不自己判斷總經
> **推翻 v6「兩條線、兩種產出、線二用 LLM 產總經報告」。**
> **核心釐清（N2 其實不重疊）**：研究部階段2b 是**離線建好「總經→策略」對應並凍結**（建表）；Agent0 是**當前拿現在的總經去查那個凍結模型**（查表）。一建一查、天生不重複。
> **Agent0 = 針對「當前」總經情況做分析與查詢，產出一份 regime+macro**：
> - **regime**：當前市場在牛/熊/危機/盤整（技術面切）。
> - **macro**：抓當前總經 → 查研究部凍結的 2b 模型 → 得「當前這種總經下歷史上哪些策略適配」（macro_fit）。
> - **這一份分丟**：→ **Agent1** 決定策略（挑兵）；→ **Agent2** 解釋（為何這環境選這些）。
> - **Agent0 不自己判斷總經**：判斷是研究部凍結模型做的，Agent0 只「抓 + 查 + 整理成結構化」。敘事全交 Agent2。

#### 產出內容（一份，分丟兩下游）

**① regime（技術面切）**
- **方法**：程式，用研究部階段2a 的**同一套固定跌幅規則**（bull-bear dating）。
- **為何用同一套**：讓 Agent0 判的「當前 regime」與 strategy_map 的 regime_fit 是同一套語言，否則 Agent1 撈策略會錯配。守住研究部對齊接縫的關鍵。
- **輸入**：到當前季為止的近期大盤資料（台股指數、美股指數）。研究部 regime 台美各一套，這裡判**兩套**。
- **輸出**：當前季 台股 regime + 美股 regime（對齊 regime_table 語言：牛/熊/危機/盤整）。
- **不摻總經**：regime 標籤純技術面（總經佐證是研究部離線 2c 的事，實戰部 regime 純技術面對齊地圖）。

**② macro（抓當前總經 → 查 2b 凍結模型）**
- **抓當前總經**：對齊 2b 的 5 欄 feature（成長/通膨/利率水位/利率方向或景氣），台美各一份。**純資料抓取，不做 LLM 判斷。**
- **查 2b 凍結模型**：用當前總經（近3月平滑、凍結參數標準化）查方法一（類比月）+ 方法二（投資時鐘格）→ 得 macro_fit（當前總經下各策略/類型的歷史適配）。
- **輸出**：結構化的 macro_fit + 客觀總經數字 + 類比月/所在格。**台美各一份**（對齊 2b 台美各一套）。

**分丟兩下游**：
- **→ Agent1**：regime 標籤 + macro_fit，供 Step1 讀情境、Step2 快篩、Step3 查情報。
- **→ Agent2**：結構化的客觀總經數字 + 2b 查詢結果（類比月/所在格），當**解釋素材**。**Agent0 不寫敘事**，Agent2 自己據此寫。

#### 消掉的 v6 待老師懸念（N2 定案後）
- ✅ **Q1（兩線是否合併）**：不合併也不再是「兩條線」——是**一份產出（regime+macro）**，職責清楚。
- ✅ **Q3（台美整合/各一份）**：對齊 2b → 總經查詢**台美各一份**。
- 🔜 **Q2（總經新聞非結構化）**：**延後、列延伸**。2b 用結構化 5 欄，新聞不在其中；新聞若用只是「額外給 Agent2 的解釋色彩」，非必要。

#### 前視偏誤防護（保留，對齊 2b 發布滯後）
- 每個資料工具吃「當前季」截止參數，**嚴禁抓截止日之後的資料**。
- 總經硬指標用「當季當下發布的版本」、對齊 2b 的**發布滯後**（不用事後修正版）。
- 新聞（若用）用發布日鎖死。

#### 借鏡學長 InvestingAgents（kevin0629）
1. 前視防護做法：所有工具吃 end_date、鎖死截止日。
2. 結構化報告輸出（Pydantic，強制格式，下游好接）。
- ⚠️ **關鍵差異**：學長單市場、且 Agent0 直接給交易訊號（「總經參與判斷」）；你是跨市場、且 **Agent0 不自己判斷（判斷交研究部凍結 2b 模型）**、regime 對齊研究部純技術面。學長 repo 是「總經參與判斷」的反例對照。

### Agent1 · agentic LLM 精挑（v7 改寫·資料流已展開）

> ## ⭐⭐ v7 重大改寫：Agent1 = agentic LLM 精挑（半結構），非純規則
> **推翻 v6「Agent1 不用 LLM、純規則決策器」。** 起因：老師要「留一個軍團讓 AI 挑出堪用的兵」（見研究部 v9 定位重校準）。新定位下 Agent1 是實戰部的 agentic 精挑主角。
>
> **換引擎不換零件**：v6 的規則零件（12格矩陣、撈太多選互補、配權、決策脈絡）**全部保留**（🔄 v8：矩陣已改寫、配權改 HRP），只是從「程式查表一步到位」改成「**agentic LLM 多步驟呼叫這些零件**」。
>
> **抗幻覺（重述·沿用研究部 v9）**：LLM 只在**程式篩定的堪用池內**判斷式精挑，不能生成池外/虧錢策略；**不碰數字計算**（快篩、配權都純程式，見下）；權衡與偏離**須引用客觀依據、記錄理由**。幻覺被關在客觀事實籠子裡，LLM 判斷力被釋放。
>
> **自主程度·半結構（guided agentic）**：給定下方步驟骨架，**每步內** LLM 自主呼工具、作判斷；步驟大方向固定、不亂跳。

#### 半結構骨架（Agent1 內部步驟·v8 五步）
> 🆕 **v8 修正（圖已改·此處補文件）**：原六步的「Step3 查情報」**併進「精挑」**——查情報是 agentic LLM 精挑時**自己規劃、自主呼叫工具**的動作，不是外部固定的獨立步驟（拆開會把 agentic 降級成工具式）。故從六步→五步。
> 🆕 **v8 純甲**：Agent1 所有資料存取（**含數字**）統一走工具層，無例外（界面一致、可複現）。快篩用的 T1/T2 是「LLM 登場前的純程式前置」，不算 LLM 呼叫工具。
- **Step1 讀情境**：Agent0 給的兩套 regime 標籤（台/美）+ 當前總經 + 要跑的類型（保守/積極/全天候）。
- **Step2 快篩（純程式前置·點1a）**：程式查 12格矩陣拿該「類型×regime」的**預設條件** → filter 堪用池（幾萬）到候選（數百）。**LLM 尚未登場**，快篩是機械縮池。（可用門檻試算當快篩條件參考。）
- **Step3 精挑（agentic LLM·核心價值）**：LLM 從候選中精挑一組兵。**過程中自主呼叫工具查情報**（macro_fit / 互補 / 真alpha判決 / 穩健度…，回傳含信心標註）+ 判斷交織進行——查與判斷不分先後、由 LLM 自己規劃。**矩陣為預設起點、可有理由偏離**（丙）：發現預設會漏好兵、或當前總經特殊時可偏離，**偏離須記錄理由、可被驗證**。衝突由 LLM 權衡、引用依據。
- **Step4 配權（純程式·點2a）**：LLM 選定一組後，呼叫組合層工具算風險/配權（🔄 **v8 改為 HRP 遞迴二分**），**LLM 不碰數字權重**。若組合風險不滿意，可回 Step3 調整選擇。
- **Step5 輸出**：選定組合 + 完整決策脈絡（見下輸出）。

#### 「有理由偏離」規範（丙的配套）
- **預設**：無特殊理由時，照 12格矩陣條件。
- **合理偏離**：須滿足——(1) 引用具體客觀依據（strategy_map 欄位/工具回傳數字）；(2) 記錄「偏離什麼、為何偏離」；(3) 偏離可被事後驗證（不可憑空）。
- **不合理偏離**（禁止）：無依據、憑感覺、生成池外策略、違反該類型核心風險（如危機時給保守型選高風險）。

#### 輸入（讀什麼）
1. Agent0 的兩套 regime 標籤（台股、美股）+ 當前總經情報。
2. 當前要跑的類型（保守/積極/全天候，三選一輪流）。
3. strategy_map 可查詢欄位（透過工具層）。
- **矩陣門檻**：作預設起點，**可有理由偏離**（不再是 v6「只能選不能改」）。

#### 處理（做什麼）— 見上方半結構骨架 Step1–6
- **路徑隨 regime 變**：同類型在不同 regime 撈的預設條件不同（否則 Agent0 判的 regime 對撈策略沒作用）。
- **regime 共用、精挑跑三遍**：Agent0 判一次 regime，三類共用；「Step2 快篩 + Step3-4 精挑」對三類各跑一遍。
- **跨市場 regime 不一致 → 依類型權衡**：保守取保守方、積極取樂觀方、全天候要兩邊扛。⚠️ 具體權衡在 agentic 下**由 LLM 依類型核心風險判斷、引用依據**（不再須窮舉規則）。

#### 輸出（給 Agent2）
- 每類選定的策略組（策略ID + 關鍵欄位 + 權重）+ **決策脈絡（結構化欄位，agentic 強化版）**：regime、用了哪格預設條件、**是否偏離預設及理由**、呼叫了哪些工具、跨市場如何權衡、為何選這組（引用依據）。
- 理由：Agent2 要解釋「為何選這些」，需 Agent1 的完整決策脈絡（含偏離理由與引用），否則會重推、可能推錯。職責分明：Agent1 精挑並記錄脈絡，Agent2 展開成敘事。**（Agent1↔Agent2 邊界與有限回饋於 Agent2 節定。）**

---

#### 門檻矩陣（v7 定位：agentic 的「預設起點」工具，非程式決策步驟）🔄 v8 全面改寫

> **📌 v7·丙（保留）**：矩陣保留，角色從「程式查表決策」→ **agentic LLM 的預設起點**。封裝成工具 `get_recommended_criteria(類型, regime)`；LLM 可有理由地偏離，偏離須記錄理由、可被驗證。
- **為何仍需要**：條件要隨 regime 變、又須預先備好 → 每種「類型×regime」條件事先算好存表，當 LLM 精挑的專家預設。
- **結構**：列=類型(3) × 欄=regime(牛/熊/危機/盤整,4) = **12格**。台美各一套 regime → 同矩陣查兩次。
- **每格內容分兩層**：結構部分（關心哪些欄位、什麼邏輯）=**現在能定**；數值部分（門檻數字）=**待資料**（用**市場內**分位數）。

> 🔴 **v8 改寫的三個原因（研究部 v9 連鎖）**
> 1. **`complement_partners` 已刪除**——研究部階段3 改用 HRP，per-策略夥伴清單被 `cluster_id` + `cluster_corr_matrix` 取代。原矩陣全天候-危機格直接失效。
> 2. **`co_fail_regimes` 定義改變**——從「兩兩危機相關高」改為「**危機樹中與本群合併的群**」。三類的危機格條件都要重寫。
> 3. **`valuation_verdict` 已刪除**——老師判定 V 無普遍價值，改用 `V` 欄位（v0/v1）。保守型-牛市格要改。
> 🆕 **同時補上原矩陣缺的一個維度**：`smallcap_share`。實測台美 Top 50 各有 48% / 58% 含最小規模桶（全池僅 3.1% / 10.5%），**保守型不該拿到重倉微型股的策略**，但原矩陣沒有任何欄位能表達這個限制。

**12格結構（🔄 v8 版·數字待資料）**：

*保守型*（核心風險=虧損；regime惡化→收緊 MDD/EffN；全格套用規模與可執行性下限）

| regime | 邏輯與條件 |
|---|---|
| 牛 | `mdd_pct` 標準、`effective_n` 標準、`regime_fit` 不特別限　🔄（原 `valuation=favorable` 改為不限；如需可加 `V=v1` 但非必要）|
| 熊 | `mdd_pct` 更嚴、`effective_n` 更高、追加 `regime_fit` 含「熊市抗跌」|
| 危機 | `mdd_pct` 最嚴、`effective_n` 最高、追加 `regime_fit` 含「危機抗跌」+ 🔄 **候選須跨 `cluster_id_L1` ≥ N 群，且各群的 `co_fail_regimes` 不得互相重疊** |
| 盤整 | 標準、`return_shape` = 穩定爬升、`factor_type` ≠ 動能型（靠選股 alpha 不靠趨勢）|

> 🆕 **保守型全格共用下限**：`smallcap_share` ≤ 上限、`coverage_ratio` ≥ 下限、`holdings_p10` ≥ 下限。
> 🆕 **保守型可優先取 `F2_empty = True`**：老師 2026-08-19「F1 加 C 就好」，實測 F1+C 策略持股數是三倍以上（台 231 vs 77、美 277 vs 81）而 MDD 幾乎相同 → 本質是「大容量、可部署」那一類。

*積極型*（核心風險=假alpha；regime惡化→收緊防假 `top1_share`/`credibility_grade`/`rotation_score`）

| regime | 邏輯與條件 |
|---|---|
| 牛 | `cagr_pct` 前段、`stability_grade` = 高原、門檻最寬但防假不省 |
| 熊 | 防假收更嚴、`mdd_pct` 給上限（比保守寬）、`regime_fit` 偏「熊市仍有表現」|
| 危機 | 防假最嚴、`stability_grade` = 高原、🔄 **追加跨 `cluster_id_L1` 分散 + `co_fail_regimes` 檢查**、`regime_fit` 含「危機仍有表現」|
| 盤整 | 盤整期 `cagr_pct`、`stability_grade` = 高原、`factor_type` ≠ 動能型、`return_shape` = 穩定爬升 |

> 🆕 **積極型的防假三件組**：`top1_share` ≤ 上限、`credibility_grade` ≥ 等級、`rotation_score` ≤ 上限（低分=有輪動=非靠單一飆股）。**排序越前段門檻越嚴**——高報酬正是假 alpha 最愛偽裝處。

*全天候*（核心風險=某regime失效；不隨regime偏食、要求全regime穩）

| regime | 邏輯與條件 |
|---|---|
| 牛 | `regime_fit` 跨多 regime 及格（不偏食牛市）、`stability_grade` = 高原 |
| 熊 | 跨 regime 及格 + 熊市不太差、`effective_n` 分散 |
| 危機 | 跨 regime 及格 + 危機不崩、🔄 **核心手段改為：從 `cluster_corr_matrix` 中挑群間相關最低的 K 群，每群各取 1–2 個策略，並確認這 K 群在危機樹中不合併** |
| 盤整 | 跨 regime 及格 + 盤整不掉隊、`stability_grade` = 高原 |

> 🔄 **全天候-危機格是本次改寫幅度最大的一格**。原設計「`complement_partners`（靠組合）」在 N=14,078 下無法實作；改用 HRP 之後反而更直接——**群間相關矩陣就是為了這件事而生的**，挑低相關的群比挑低相關的策略對更穩健（單一策略對的相關係數容易是噪音，這正是老師說「拿一個錯的就會錯得很離譜」的問題）。

**三類本質差異（橫向對照·🔄 v8 更新）**：

| | 保守型 | 積極型 | 全天候 |
|---|---|---|---|
| regime惡化收緊 | `mdd_pct`、`effective_n`（守）| `top1_share`、`credibility_grade`（防假）| 不適用（本就要求全regime穩）|
| 核心風險 | 虧損 | 假alpha | 某regime失效 |
| 危機格特化 | 最嚴 MDD + 群分散 | 最嚴防假 + 群分散 | **最依賴群間互補** |
| 🆕 規模限制 | **最嚴**（`smallcap_share` 上限）| 寬（知情承受）| 中 |
| 跨市場互補角色 | 危機時防護 | 危機時知情承受 | **核心達成手段** |
| 對當前regime態度 | 適應（換條件）| 適應（換條件）| 無視（要求全面）|

> 觀察：**群分散（cluster）出現在三類的危機格**——跨市場研究在實戰部的集中落點。**全天候最依賴群間相關矩陣**。
> 實務建議：先把保守型一列做細當範本，其他兩類照模式展開。
> ⚠️ **盤整格是四格最弱一環**：盤整用殘差定義，是「雜物箱」、樣本可能薄。備案：品質太差則退化用「整段（不分regime）表現」撈。

> ✅ **v8 完整性驗收方法**：把 12 格逐格拆成 pandas 條件，**若某一格有任何條件寫不出對應欄位 → 那個欄位就是漏掉的，必須回研究部補**。三類 × 四 regime 全走一遍，這是最可靠的檢查。

---

#### 撈取數量處理（v7 定位：LLM 的行為準則，非程式規則）

> **📌 v7**：以下從「程式規則」→ **agentic LLM 的挑兵準則**（LLM 於精挑步驟遵循）。

**撈太少（0個或不足）**：LLM 以**誠實回報「當前 regime 下該類型無適配策略」**為主——「某regime某類型無策略」本身是有價值的發現（如「危機時積極型幾乎無安全策略」）。輔以逐步放寬非核心條件，但**核心風險條件不得放寬**，放寬須記錄。

**撈太多**：LLM **選「互補的一組」而非「最強的 N 個」**——🔄 v8 改為呼叫 `T13 get_cluster_info` / `cluster_corr_matrix`，**從不同群各取策略**。理由：直接體現「策略庫價值在互補、非單一最優」。

> 🔴 **v8 新增·快篩的「多樣性假象」（研究部 v9 發現的接縫，必須處理）**
> `usable_pool ≈ 14,000`，但實際只來自 **407 個獨立 F 組合**（台 219 + 美 188），每個 F 組合底下掛 35–38 個變體（20 個 C ×（v0/v1）），**選股池相同、報酬高度相關**。
> **後果**：Step2 快篩若篩到剩 300 個，這 300 個可能全部來自 10 個 F 組合——**LLM 以為自己在 300 個裡挑，實際上只有 10 種真正不同的東西**，而它從候選清單上看不出來。
> **對策（必做）**：`T2 filter_pool` 必須支援 **cluster 級配額**參數——每個 `cluster_id_L3` 最多取 N 個。⏳ N 值待資料。
> **副作用（好的）**：加了配額之後，遞給 LLM 的候選數可以更少（因為每一個都真的不同），Opus token 反而下降。

---

#### 權重分配（🔄 v8 改寫：HRP）

> **📌 v7·點2a（保留）**：配權由**組合層工具純程式計算**，LLM 選好一組後呼叫、不介入數字權重（守抗幻覺：LLM 不碰數字計算）。

- **保底：等權重**（1/N）——極簡、可複現、當 baseline。
- 🔄 **主方法改為 HRP（Hierarchical Risk Parity）**——老師 2026-08-19 指示的方法本身就內含配權：對選定的策略清單做階層聚類 → 準對角化 → **遞迴二分配權**。
- **為什麼比原本的風險平價好**：
  1. 傳統 risk parity 需要相關矩陣求逆，在策略高度相關時**數值不穩定**（正是老師說的「拿一個錯的就會錯得很離譜」）。HRP 不需求逆。
  2. HRP 用的是研究部階段3 **已凍結的同一棵樹**，實戰部只做遞迴二分，一致性有保證。
  3. **一個方法同時解決分群與配權**，不需要兩套邏輯。
- ✅ **v8 撤銷待老師問題 Q4（權重方法選型）**——HRP 已同時回答分群與配權，不需再問。
- （備選保留：可信度加權讓研究部品質欄位發揮作用；等權仍保留當 baseline 對照。）

### Agent2 · Explainer（LLM，v7 更新·資料流已展開）

> **定位：有紀律的分析師**——可分析、可解讀，但所有分析須建立在程式/前面 Agent 給的結構化事實上，不無中生有。這是實戰部展示「LLM 價值」最集中處，也是幻覺風險最高處。
> **v7 更新**：輸入來源對齊 Agent0（結構化 macro，非敘事報告）與 Agent1（agentic 決策脈絡）；新增 Agent1↔Agent2 邊界釐清與**有限回饋機制**。四要素骨架、引用標註抗幻覺、B3 界線**保留不變**。

#### 輸入（讀什麼，v7 對齊上游改動）
1. **Agent1 決策脈絡（agentic 強化版·結構化）**：選了哪些策略、用哪格預設條件、**是否偏離預設及理由**、呼叫了哪些工具、跨市場如何權衡、引用了什麼依據。
2. **策略的 strategy_map 欄位**：`return_story`（判決）、🔄 `cluster_id` + 群間互補解釋（原 `complement_partners`）、`co_fail_regimes`、`top1_share`、`effective_n`、`credibility_grade`、`regime_fit` 等。
3. **Agent0 的結構化 macro**（非敘事報告）：客觀總經數字 + 2b 查詢結果（類比月/所在格/macro_fit）。**Agent2 據此寫敘事，Agent0 不代寫。**

#### 與 return_story 的界線（= 研究部 B3 的具體答案，保留）
- **return_story（研究部離線）**：策略「本質上靠什麼賺錢」——通用、不隨時間變。
- **Agent2（實戰部即時）**：「在當前 regime、當前總經下，為何現在推薦這策略給這類投資人」——情境化、隨當前變。
- Agent2 **不重寫 return_story**，而是引用它（本質）+ 疊加當前情境（regime+總經）+ 對應投資人需求。

#### Agent1 ↔ Agent2 邊界（v7 釐清·agentic 化後）
- **不重複、是分工**：Agent1 記錄的是「**決策脈絡**」（選了什麼、偏離什麼、引用什麼，**給機器看的結構化**）；Agent2 把這脈絡**展開成給人看的敘事解釋**。Agent2 **不重做挑兵決策**，是「翻譯+展開」Agent1 的脈絡（含把偏離理由講給人聽）。

#### 處理：四個固定要素（可複現骨架，每塊標來源）
| 要素 | 內容 | 來源 |
|---|---|---|
| 1 為何選這組 | 為何被選、符合此類型什麼需求、**若偏離預設則說明理由** | Agent1 決策脈絡 + credibility/stability |
| 2 賺錢邏輯 | 靠什麼賺錢 | **引用** return_story（不重寫）|
| 3 當前環境意涵 | 當前總經/regime 對這組順風或逆風 | Agent0 結構化 macro（客觀數字+2b結果）+ regime_fit |
| 4 風險警示 | 何時失效、跨市場夥伴會否一起垮 | co_fail_regimes + top1_share |

> 設計邏輯：要素2（本質，研究部給）與要素3（情境，實戰部加）並列，體現 return_story 與 Agent2 的分工；要素1（回顧決策）、要素4（前瞻風險）一頭一尾。四塊固定 → 可複現、可溯源、可比較。
> **要素3 來源更新**：從「Agent0 敘事報告」→「Agent0 結構化 macro（客觀數字+2b查詢結果）」。Agent2 引用客觀數字寫敘事，**不自己另判總經**（判斷是研究部凍結 2b 做的）。Agent0 不判、Agent2 據客觀數字寫，分工乾淨。
> ⏳ 之後要加更多要素再加（如「權重理由」，目前權重純顯示數字不解釋）。
> ⏳ 要素3 是 LLM 價值最高、幻覺風險也最高處，是否額外加嚴（解讀須對應具體數據）——保留待定。

#### 有限回饋機制（v7 定案·甲+有限回饋的接線）
> **觸發條件（a·只在硬矛盾）**：Agent2 的抗幻覺核對（做法三）若發現 Agent1 選的組合與**客觀事實硬矛盾**（如某策略 co_fail 顯示危機會一起垮、卻被選進保守組），**觸發 Agent1 重選一次**。
- **限制**：只回饋**一次**（不無限來回）；回饋須帶「問題點 + 客觀依據」；Agent1 重選時針對該問題調整。
- **不觸發的情況**：Agent2 只是「主觀覺得不夠好」**不觸發**——回饋須有客觀事實衝突，Agent2 是「抓錯」不是「搶挑兵權」，不越權。
- 與 v6「驗證階段」呼應：把驗證明確接成「Agent2→Agent1 一次回饋」，使系統從開迴路→閉迴路。

#### 抗幻覺：強制溯源（做法二 + 做法三兜底，保留）
- **做法二 · 引用標註式（主）**：自由寫敘事，但每個關鍵論述標註來源（如「危機時仍能分散（來源:co_fail_regimes）」）。標註可被程式檢查。
- **做法三 · 事後核對式（兜底）**：寫完用程式核對有無與結構化事實矛盾，矛盾者標記——**此核對同時是有限回饋的觸發點**（見上）。
- ⭐ **一魚兩吃**：做法三核對機制同時是 A2「衡量指標」要的**幻覺率客觀量測工具**——抗幻覺、評分、回饋觸發是同一套機制。
- 不靠 prompt 拜託，靠結構強制。

---

## 3.5 工具層（v7 定案·agentic 的資料存取橋）

> **定位**：strategy_map（研究部凍結）與 agentic LLM 之間的橋。工具 = 查凍結檔 / 即時算 的介面。
> **粒度·中粒度**：按「挑兵時的提問」切（非按欄位、非大包），約 12 個，對應 Agent1/Agent0 骨架的各步。
> **設計原則**：原子化（各工具獨立、互不依賴）、無狀態（輸入→輸出、不記憶、不依賴呼叫順序）、查詢類回傳一律附**信心標註**。

### 工具清單（依骨架步驟組織）🔄 v8 更新

**Step2 快篩（純程式，LLM 只給條件）**
| 工具 | 輸入 | 回傳 | 查/算 |
|---|---|---|---|
| `T1 get_recommended_criteria(類型, regime)` | 投資人類型、當前regime | 該格預設撈取條件（門檻組合） | 12格矩陣（丙·預設起點，可偏離）🔄 **v8 矩陣已改寫** |
| `T2 filter_pool(條件, cluster_quota)` | 一組條件 + 🆕 **每群配額 N** | 符合的策略ID清單+數量 + 🆕 **涵蓋的群數** | strategy_map 主表（純程式縮池）🔴 **v8 必改：加 cluster 配額，防多樣性假象** |

**Step3 查情報（LLM 對候選查各面向，分開·按需·省token）**
| 工具 | 輸入 | 回傳 | 查/算 |
|---|---|---|---|
| `T3 get_strategy_profile(id清單)` | 策略ID（批次） | credibility / stability / factor_type / regime_fit / 🆕 `smallcap_share` / 🆕 `coverage_ratio` / 🆕 `F2_empty` / 🆕 `cluster_id` 等完整 profile　🔄 **v8 移除 `valuation_verdict`，改回傳 `V` 與 `v1_beneficial`** | 主表① |
| `T4 get_macro_fit(id清單, 當前總經)` | 策略ID、當前總經 | 方法一(類比月報酬/勝率)+方法二(所在格表現)+樣本數/信心，**兩法各自值不混** | 2b 凍結模型（不變）|
| `T5 get_complements(id, 組別)` | 策略ID、組別(TW-TW/US-US/TW-US) | 🔄 **v8 改寫**：回傳「與本策略所屬群**相關最低的 K 個群**」+ 各群代表策略 + 群間相關值 + 群間互補的凍結解釋文字 | HRP 群結構③ |
| `T6 check_correlation(id清單)` | 一組策略ID（**上限約 50**）| 兩兩相關矩陣(常態+危機) | 報酬序列②　🔄 **v8：改為即時算，不再預存 N²** |
| `T7 get_return_story_verdict(id)` | 策略ID | 四道安檢**判決**(靠少數股/產業β/規模/真alpha) | 主表(凍結判決)。🆕 第三道「規模效應」現有 `size_tilt_pct`/`smallcap_share` 當客觀依據 |
| 🆕 `T13 get_cluster_info(id 或 cluster_id)` | 策略ID 或 群ID | 所屬群、群內成員數、群內平均相關、群的代表策略、該群各 regime 表現、`co_fail_regimes`（危機樹中與本群合併的群）| HRP 群結構③ |

**Step4 配權/組合檢查（純程式即時算·組合層）**
| 工具 | 輸入 | 回傳 | 查/算 |
|---|---|---|---|
| `T8 compute_portfolio_risk(id清單)` | 一組策略ID | 組合整體回撤/波動/因子曝險集中度/台美比重/特定regime整體表現 + 🆕 **涵蓋群數與各群權重佔比** | 報酬序列②**即時算** |
| `T9 compute_weights(id清單)` | 一組策略ID | 🔄 **v8 改為 HRP 遞迴二分權重**（+等權 baseline 對照）| 凍結 HRP 樹③ + 報酬序列②（點2a·LLM不碰權重）|

**Step5 解釋（按需生成）**
| 工具 | 輸入 | 回傳 | 查/算 |
|---|---|---|---|
| `T10 generate_return_story_text(id)` | 選中的策略ID | 凍結判決串成的story文字 | 主表判決（乙案·只對候選少數呼叫）|

**Agent0 用（產 regime+macro）**
| 工具 | 輸入 | 回傳 | 查/算 |
|---|---|---|---|
| `T11 get_current_regime(市場, 截止日)` | 市場(台/美)、截止日(前視防護) | 當前regime標籤 | 2a 同一套規則跑當前資料 |
| `T12 query_macro_model(當前總經)` | 當前總經向量 | 類比月+所在格+macro_fit | 2b 凍結模型 |

> 🔄 **v8 工具層變動摘要（共 13 個工具）**
> | 變動 | 工具 | 原因 |
> |---|---|---|
> | 🆕 新增 | `T13 get_cluster_info` | 研究部階段3 改 HRP，群結構需要專屬查詢介面，否則 Agent2 引用不到群間互補解釋 |
> | 🔄 改寫 | `T5 get_complements` | `complement_partners` 欄位已刪，改為 cluster-based |
> | 🔄 改寫 | `T9 compute_weights` | 風險平價 → HRP 遞迴二分 |
> | 🔴 必改 | `T2 filter_pool` | 加 cluster 級配額，防多樣性假象 |
> | 🔄 微調 | `T3` / `T6` / `T7` / `T8` | 欄位增減與計算方式對齊 v9 |
> | ✅ 不變 | `T1` / `T4` / `T10` / `T11` / `T12` | 介面不變（T1 背後的矩陣內容有改，但簽章不變）|

### 工具分類與特性
- **查詢類（T1,T3-T7,T11-T12）**：讀凍結檔，快、可複現，回傳帶信心標註。
- **即時計算類（T8-T9）**：用凍結報酬序列即時算（組合層，原料來自研究部凍結②）。
- **生成類（T10）**：唯一「生成文字」，按需、只對候選少數。

### 擴充性（實作後缺工具可加）
> **可擴充、且好擴充**——原子化+無狀態的回報，**加工具是純加法**，不動現有工具、不改骨架（LLM 自然在需要時呼叫新工具）。

**新增工具檢查清單**（未來加工具照此，維持架構乾淨）：
1. 屬哪類？（查詢/即時計算/生成）
2. 要的資料研究部凍結檔**有嗎**？有→直接寫查詢工具；**沒有→先回研究部補產出再寫工具**（跨部門，唯一稍麻煩處）。
3. 查詢類回傳是否帶**信心標註**？
4. 是否**無狀態**（不依賴呼叫順序、不記憶）？
5. 對應 Agent1/Agent0 骨架**哪一步**（讓 LLM 知道何時呼叫）？

**長期提醒**：工具數若長到很多（30+），LLM 選對工具會變難，屆時需**分組**（給分類清單）。碩論規模（~12個）不受影響，現在不用管。

---



```
當前總經 + 大盤資料（截止當季，前視防護）
        ▼
   Agent0（純程式）：判台美兩套當前 regime（T11）+ 查 2b 凍結總經模型（T12）
        │  產一份「regime + macro_fit」，分丟兩下游 ↓
        ├─────────────────────────────┐
        ▼                             ▼
   Agent1（agentic LLM 精挑）      （macro 客觀數字也給 Agent2）
   Step2 快篩(純程式,T1預設+T2縮池)
   Step3 查情報(T3-T7)
   Step4 精挑(LLM判斷,矩陣可偏離)
   Step5 配權/組合檢查(純程式,T8-T9)
        ▼  選定組合 + 決策脈絡
   Agent2（Explainer, LLM）：四要素解釋 + 抗幻覺核對
        │  ⟳ 若核對發現硬矛盾 → 觸發 Agent1 重選一次（有限回饋）
        ▼
   輸出（三類各一份）
```

> **三種類型各跑一遍**：Agent0 判一次 regime + macro_fit，**三類共用**（regime 是市場狀態、與投資人類型無關）；只有 Step2-4「快篩+精挑」對保守/積極/全天候**各跑一遍**、各出一份。
> **有限回饋**：Agent2 核對發現硬矛盾才回饋 Agent1 重選一次；**若重選後仍矛盾 → 照常輸出但標記此矛盾**（誠實揭露，不再回饋，避免無限來回）。

---

## 4.1 主流程：實戰部怎麼跑完 out 期

> 上面的資料鏈是「跑一次」的內部。這裡定義「一次」的邊界，以及怎麼串成整個 out 期實驗。

**單次執行（= 一季）的邊界**
- **觸發/輸入**：一個決策時間點（某季）+「到該時間點為止已知的」所有資料（市場、總經）。嚴禁用該點之後的資料（前視防護）。
- **執行**：跑一次完整鏈（Agent0 判 regime + 查 2b 產 macro_fit → Agent1 對三類各 快篩+精挑+配權 → Agent2 對三類各解釋 + 抗幻覺核對、必要時觸發 Agent1 重選一次）。
- **產出**：該季的三類建議卡。

**整個 out 期怎麼跑（節奏，已定案：建議B 逐季滾動）**
- out 期逐季各跑一次，各產三類建議卡，季末驗證上季建議（見 §6 驗證階段）。
- ⚠️ **這是「固定分割」下的選擇**。若老師採 walk-forward（研究部 A3），此處升級為 **逐年滾動建議+驗證**：2000 建圖→2001 用、2001 結束→用 2000+2001 重建圖→2002 用……。屆時「建議B（逐季）」升級為「建議C（walk-forward 逐年）」，驗證樣本從 2026 兩三季暴增到多年。
- **開發階段最小版**：正式跑 B/C 前，可先用「單點快照」（只挑一季跑一次）驗證系統跑得通。

---

## 4.2 對接與元件補洞（review 找出）

**補洞1 · regime 判定規則交接物（研究部↔實戰部）**
- Agent0 判當前 regime 要用「研究部階段2 的同一套跌幅規則」。台美各一套後，兩市場的跌幅閾值參數不同。
- **要求**：研究部階段2 除了輸出 regime_table，還要輸出「**regime 判定規則物件**」（含台美各自的閾值參數）。Agent0 import 此物件判當前，兩邊共用同一份、不靠人工記憶。（此項也要回填研究部 v6 階段2 產出清單。）

**補洞2 · out 期績效計算器（新元件，是工具非 Agent）**
- 驗證階段需要「用 out 期資料跑策略、算實際表現」，但 strategy_map 是凍結的（只有 in-sample 數字）。
- **要求**：補一個獨立**程式工具**「out 期回測器」——輸入策略ID+時間範圍，用 out 期資料算實際績效。純數字計算、無判斷，符合「數字→數字用程式」。
- ⚠️ 時間界線：只在**驗證階段（事後）**用，不可在建議階段用（否則 look-ahead）。

**補洞3 · 同一策略被多類撈到**
- 保守型與全天候可能都撈到同一穩健策略——正常，不用特別處理。
- 在總覽表標記「此策略同時入選 X 類」，當「全能型」特徵呈現，而非問題。

**補洞4 · baseline 分兩層比（連動研究部 A1/A2）**
- 純量化版無 Agent2，輸出只有策略清單+數字、無敘事。故比較分兩層：
  - **策略表現層**（三版都有）：比報酬 / 需求達成度。→ 驗證「LLM 對選股結果有無加分」。
  - **解釋層**（僅多Agent、單Agent版有）：比解釋品質 / 幻覺率。→ 驗證「多Agent 對解釋有無加分」。
- 兩個 baseline 驗證不同東西，分層讓比較更清楚。

---

## 4.5 多 Agent 概念釐清（v7 改寫·Agent1 已 agentic）

> v7 後 Agent1 = agentic LLM 精挑，「多 Agent」立論比 v6 更強。以下釐清。

- **「多 Agent」是跨研究部+實戰部的整體**，不是只看實戰部三個。
- **「用了多個 LLM 節點」≠「多 Agent」**：依既有標準（純計算不應稱代理人），**有判斷/診斷性質的節點才算 Agent**。v7 後實戰部三個 Agent 的性質：
  - **Agent0**：純程式（判 regime + 查凍結模型），**是資料節點、非判斷 Agent**（不自己判斷，判斷交研究部凍結模型）。
  - **Agent1**：**agentic LLM 精挑 = 判斷 Agent**（在堪用池內做判斷式挑兵、可有理由偏離預設）。這是 v7 相對 v6 的關鍵升級——挑兵從程式變 LLM 判斷。
  - **Agent2**：LLM 情境化解釋 = 判斷 Agent。
- **v7 立論（比 v6 強）**：實戰部有 Agent1（挑兵判斷）+ Agent2（解釋判斷）兩個判斷 Agent，加研究部解釋類節點，多 Agent 主張紮實。**抗幻覺不靠「LLM 不碰選股」，而靠「LLM 只在程式篩定的堪用池內判斷、須引用依據」**（見整體層抗幻覺重述）。
- **多代理人體現**：靠**跨部門+跨Agent角色分工**（研究部/實戰部兩層、Agent0資料/Agent1挑兵/Agent2解釋），非單節點內辯論。
- ⚠️ **連動 baseline**：「多 Agent vs 單 Agent」比的是整個系統的 agent 分工，範圍待與 baseline 一起釐清。
- 📌 待辦：之後單獨盤點「研究部+實戰部所有 LLM 節點，各屬 Agent 還是工具」，定義論文「多 Agent」指哪些（口試好材料）。

---

## 5. 輸出格式：A + C 混合

**底層結構化（JSON）+ 上層渲染成卡片**：既給人看、又能被程式比較（baseline 對照、幻覺率核對都需結構化）。一份資料兩種用途。

**呈現方式：先總覽表 + 再逐卡**
- **總覽表**：三類（保守/積極/全天候）各撈到什麼、關鍵指標並排 → 一眼看出「不同需求→不同策略」（媒合主張的證據）。
- **逐卡**：每類一張詳細卡。

**每張建議卡的結構：**
- **頭部**：投資人類型、當前 regime（台股X/美股Y，因台美各一套）、決策季別。
- **主體·推薦組合（格式A）**：策略清單（ID + 權重〔HRP 算〕+ CAGR/MDD/EffN）；組合層級指標（整組預期報酬、整組MDD、策略數）。權重**純顯示數字、不特別解釋**。
- **解釋·四要素（格式C，每塊標來源）**：1為何選這組 / 2賺錢邏輯(引用return_story) / 3當前環境意涵 / 4風險警示。
- **尾部·溯源與檢查**：事後核對結果（做法三）——有無與結構化事實矛盾（此塊也是幻覺率資料來源）。

**接收者**：實驗中輸出給你與老師看，假想對象是三類投資人。
⭐ **設計要求**：能並排比較不同版本（多Agent vs 單Agent vs 純量化），讓解釋品質差異看得出來——輸出格式本身服務 baseline 比較。

---

## 6. 驗證階段（v5 新增：實戰部從開環變閉環）

> 原本實戰部只有「產出建議」（開環），這節補上「驗證建議對不對」（閉環）。這才是論文「系統 out-sample 有沒有用」的證據。
> **兩種時間**：建議在「決策時間」（某季當下），驗證在「驗證時間」（那季/那年過完）。
> ⚠️ **in-sample 數字不能當「預期」**：建議卡上的「預期報酬/MDD」是基於歷史的預期，要標明「非保證」——因為過去表現會否延續，正是 out 期要驗證的，不能拿還沒驗證的當結論。
>
> 🆕 **v8 釐清·系統有「兩個不同的閉環」，勿混淆**：
> 1. **有限回饋（執行內·跨 Agent）**：單次執行中，Agent2 抗幻覺核對發現硬矛盾 → 觸發 Agent1 **重選一次**。範圍小、即時、屬「這一季這次挑兵」內部的自我修正。
> 2. **驗證閉環（跨期·系統級）**：out 期各季跑完 → 事後驗證需求達成度 → 回饋修正系統。範圍大、跨時間、驗證「系統整體對不對」。
> 兩者是**不同層級的回饋**，架構圖與文件都要分開呈現（前者在 Agent1↔Agent2 之間，後者在 產出B→驗證→系統 之間）。

### 6.1 驗證策略表現
| 指標 | 內容 | 意義 |
|---|---|---|
| 絕對表現 | 建議組合在建議後實際報酬/MDD | 基本：實際賺賠 |
| 相對 baseline | vs 大盤 / 純量化 / 單Agent | 證明多Agent系統的建議較好（論文核心）|
| **需求達成度** ⭐ | 保守型是否真少賠、積極型是否真報酬高、全天候是否真各環境穩 | **驗證「媒合」成功——不是賺最多，是給對需求。保守型MDD小即成功，即使報酬不高。最貼合論文主張。** |

### 6.2 驗證解釋品質（LLM 價值的驗證）
| 指標 | 內容 | 意義 |
|---|---|---|
| 幻覺率 | Agent2 解釋有幾條與結構化事實矛盾（用做法三核對機制）| 客觀、多vs單Agent對比。設計Agent2時已埋好 |
| **風險警示事後準確性** ⭐ | Agent2 當初警示「X情況會失效」，事後 X 真發生時是否真失效 | **把「主觀解釋品質」變成「可事後查核的預測準確性」。驗證 LLM 前瞻解釋有預測力，很強** |

### 6.3 驗證時間結構
- 分層驗證：短期（季後）看積極型；完整（年後）看保守型/全天候（它們是長期抗跌，要更長時間才看得出）。

> **兩大差異化主打**：`需求達成度`（驗證媒合成功）+ `風險警示事後準確性`（驗證LLM解釋有預測力）。這兩個把論文最難驗證的「媒合」和「LLM解釋價值」變成可測量，是相對一般量化系統的差異化驗證。
> ⚠️ 驗證階段的完整設計深綁 walk-forward（決定驗證樣本多寡）與 baseline（決定跟誰比），故正式版待老師會議後定。

---

## 7. 已定案清單

- 起點：固定三類（保守/積極/全天候），不做需求理解，三類都跑。
- 命名：Agent0/1/2，廢 O 編號。
- Critic：排除。
- **Agent0 · 純程式（v7）**：產一份「regime + macro_fit」分丟兩下游——技術面跌幅規則（對齊研究部）判台美兩套 regime → Agent1；查 2b 凍結模型得 macro_fit + 客觀總經數字 → Agent1 挑兵、Agent2 解釋。不自己判斷總經。
- Agent0 報告不給 buy/sell 交易訊號（避免越權做 Agent1 的事）。
- 前視防護：所有資料工具吃當前季截止參數、嚴禁截止日後資料（借鏡學長）。
- **Agent1 · agentic LLM 精挑（v7）**：半結構骨架（快篩純程式→查情報→LLM精挑→配權純程式）；矩陣當預設起點、可有理由偏離；LLM 只在堪用池內判斷、不碰數字計算、須引用依據。
- Agent1 處理：路徑隨 regime 變、regime 共用撈取跑三遍、跨市場不一致依類型權衡。
- **門檻矩陣**：類型(3)×regime(4)=12格，結構已定、數字待資料；盤整用殘差定義、為最弱一格。
- **撈取數量**：太少誠實回報無適配（輔以先嚴後鬆）、太多選互補一組。
- **權重**：等權保底 → 🔄 **v8 改為 HRP 主方法**（原風險平價作廢）。
- Agent1 輸出：策略組（含權重）+ 結構化決策脈絡。
- 跨市場 regime 不一致：由 Agent1 判斷（具體權衡規則待定）。
- **多 Agent 釐清**：跨研究部+實戰部整體；有判斷性質才算 Agent；Agent1 不用 LLM 不影響主張、反強化抗幻覺。
- Agent2：因果解釋 + 失效警示。
- **Agent2 · Explainer（LLM）**：有紀律的分析師——可分析但須基於結構化事實、不無中生有。
- Agent2 四固定要素：為何選這組/賺錢邏輯(引用return_story)/當前環境意涵/風險警示，每塊標來源。
- Agent2 與 return_story 界線：return_story 講本質（通用）、Agent2 講情境（此regime此環境為何現在推薦）。
- Agent2 抗幻覺：做法二引用標註 + 做法三事後核對兜底；核對機制同時是幻覺率量測工具。
- 輸出：A+C 混合、總覽表+逐卡、底層JSON上層渲染、可並排比較。
- **主流程（§4.1）**：單次=一季，逐季滾動（建議B）；若老師採 walk-forward 升級為逐年滾動（建議C）；開發先用單點快照。
- **驗證階段（§6）**：實戰部閉環。策略表現（含需求達成度）+ 解釋品質（幻覺率、風險警示事後準確性）+ 時間結構。in-sample數字不可當「預期」。
- **對接補洞**：regime判定規則當交接物、補out期績效計算器（工具）、多類撈到當特徵、baseline分表現層/解釋層兩層比。

---

## 8. ❓ 待老師確認（實戰部）

| # | 事項 | 說明 |
|---|---|---|
| Q1 | **總經是否參與 regime 判斷**（做法X vs Y）| 現況做法X：總經只解釋、不參與 regime 標籤（純技術面，對齊地圖）。做法Y：總經參與判斷，更前瞻，但地圖 regime_fit 是純技術面建的、跟不上，會反過來要求改研究部階段2讓 regime 納入總經（連動大）。學長 InvestingAgents 是做法Y實例（單市場、直接給交易訊號），可當對照。 |
| Q2 | **總經新聞（非結構化）要不要納入** | 硬指標（利率/匯率/指數）客觀、好防前視；新聞更即時、敘事豐富，但前視偏誤更難防、品質參差。學長有用新聞（get_macro_news）。 |
| ~~Q3~~ | ~~總經報告台美整合 vs 各一份~~ | ✅ **已定案·台美各一份**（對齊 2b 台美各一套，見 Agent0 節）。 |
| ~~Q4~~ | ~~**權重分配方法選型**~~ | ✅ **v8 撤銷**：老師 2026-08-19 指示 HRP，已同時解決分群與配權。等權保留為 baseline 對照。 |
| Q5 | **實戰部是否跟研究部一起 walk-forward** | 你的想法：2000建圖→2001用、2001結束→用2000+2001重建→2002用……逐年滾動。這與研究部 A3 是同一決定的兩面（研究部滾動建圖、實戰部逐年驗證）。選建議B（固定分割逐季）或建議C（walk-forward逐年）取決於此。walk-forward 好處：驗證樣本從2026兩三季暴增到多年。 |

---

## 9. 待討論（尚未展開）

**已知擱置/待資料/實作**：
- Agent1 跨市場權衡規則的具體內容（連帶問題二，暫擱置）。
- Agent1 查詢 tool 的具體介面（開發時再定）。
- Agent1 門檻矩陣的數值（待資料，用分位數）。
- Agent2 要素3 是否額外加嚴（保留待定）。

**尚未展開**：
- **LangGraph 編排**：三 Agent 如何串接、狀態如何傳遞。
- **Agent 間資料傳遞格式**：Agent0→1、1→2 的具體資料結構。
- **驗證與比較設計**：跑幾次、怎麼比、什麼指標——**待研究部 walk-forward / baseline 決定**（研究部 v6 A1/A2/A3）。
- 📌 **多 Agent 節點盤點**：研究部+實戰部所有 LLM 節點各屬 Agent 還工具。

---

*實戰部 v5 · 三 Agent 資料流 + 主流程 + 驗證階段（閉環）。剩 LangGraph 編排、Agent 間傳遞格式（實作）；正式實驗規模待老師會議。*
