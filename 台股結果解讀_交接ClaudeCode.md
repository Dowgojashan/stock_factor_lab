# 台股 Sweep 結果解讀 — Claude Code 執行交接文件

> **給誰**：VS Code 裡的 Claude Code（在使用者本機、可直接讀 `results_artifacts/` 與 `_catalog/`）
> **目的**：對這次台股（TW）Sweep 跑出來的十一批策略結果做**健全性檢查 + 解讀**，產出一份**完整解讀報告**。這份報告的讀者是**使用者本人（理解用）+ 另一個 Claude 對話（用來製作本週向指導教授報告的進度 ppt）**——**不是直接給老師的簡報**。
> **repo / 分支**：`Dowgojashan/stock_factor_lab`，分支 **FMP_3**
> **重要**：這份是「分析既有結果」，**不是重跑 Sweep、不是產生器**。不要重跑回測、不要改 condition_factory、不要動 SQL。

---

## 0. 一分鐘背景（為什麼做這個分析）

這批策略是整條研究流程的**最源頭**——暴力展開 F(因子水準)×C(動態條件)×V(估值濾網) 組合、跑回測得到的原始庫，**還沒經過任何篩選**。

### ⚠️ 重要：這是「十一批獨立實驗」，不是一個七萬筆的統一母體
（詳見 repo 檔 `因子候選批次_F與C因子定義.md`）

- 每一批 = **固定三因子（ROE/EPS/FCF_P）+ 1 個候選因子**，各自跑一次完整 Sweep（~7,140 策略）。
- 十一個候選因子：**PE, EV_EBITDA, EV_S, CROIC, FCF_OI, ROIC, PB, PS, P_IC, OCF_E, MOM**。
- 目錄命名：`TW_batch_{候選}_M`（如 `TW_batch_PE_M`、`TW_batch_MOM_M`）。
- **十一批彼此獨立、不合併成一個大池來算分布**。本質是**因子海選**（對照學姊論文 3.6.1「發表用因子確定前測試多項候選因子」）——在測「這 11 個候選因子，加哪個有用」。
- 台股各批存活數：多數 6,800~7,100；**MOM 批因分桶樣本少僅 ~4,203**（正常，非 bug）。

### 這次分析的兩層結構（**務必照這個做**）
1. **批內各自分析**：母體分布/賺賠比例/冠軍，**一批一批各自算**（11 份）。**絕不把十一批混成一坨算「整體分布」**——那等於把 11 個不同實驗攪在一起，無意義且誤導。
2. **批間比較**：11 批擺一起比，回答**「哪個候選因子最有用」**——這是海選的核心產出、最該報告老師的東西。

### 堪用池 = 乙案（使用者已定案）
最終要餵給 LLM 精挑的堪用池，**橫跨所有批匯集**：不管哪批，只要策略本身堪用（賺錢/贏大盤）就進池。**不是只留贏家因子批**。所以堪用池估算 = 跨全部 11 批、逐批算堪用數再加總。

指導教授（2026-07）三層分工：產生器(電腦暴力跑) → 堪用池(留一個軍團) → LLM Agent 精挑(研究貢獻點)。**本次分析的核心任務**：
- (a) 確認各批結果**可信、沒壞**（健全性）
- (b) 認識**每批母體長什麼樣**（批內分布）+ **哪個候選因子有用**（批間比較）
- (c) 估算**匯集成堪用池大概剩幾千**，判斷夠不夠 AI 挑（對接下一步）
- (d) 挖 2–3 個**有趣模式**當報告 / 論文敘事彈藥

---

## 1. 資料在哪、什麼格式、什麼欄位（已確認）

### 1.1 目錄結構（在使用者本機，被 gitignore、不在 repo）
```
results_artifacts/
├─ TW_batch_PE_M/                  ★ 一批一包（固定三因子 + 候選 PE）
│   ├─ stats.parquet              ★ 一列一策略 × 指標（本次分析主檔）
│   └─ <每策略>/
│        ├─ return_table.parquet  逐期報酬（可攤平成逐月，用於淨值/相關/月頻）
│        ├─ position.parquet      持股
│        ├─ trades.parquet        交易
│        └─ stock_data.parquet    cum_returns 淨值曲線
├─ TW_batch_EV_EBITDA_M/
├─ TW_batch_ROIC_M/
├─ TW_batch_MOM_M/                 （此批策略數較少 ~4203，正常）
└─ ...（共 11 批：PE, EV_EBITDA, EV_S, CROIC, FCF_OI, ROIC, PB, PS, P_IC, OCF_E, MOM）
_catalog/
├─ master_index.parquet           所有 job 的 stats 併成一張，一列一策略（含 job_label 可辨批次）
└─ dedup_registry.parquet         去重後「獨立策略」清單
```
> ⚠️ **master_index 由 `code/catalog_builder.py` 手動產生，不會自動觸發**。若不存在或過舊，先跑 `python code/catalog_builder.py`。
> **辨別批次**：master_index 用 `job_label`（如 `TW_batch_PE_M`）分批；或直接逐批讀各自的 `stats.parquet`。**批內分析建議逐批讀 stats.parquet 最乾淨**；批間比較才用 master_index 彙整。

### 1.2 stats.parquet / master_index 的真實欄位（**用這些欄名，不要猜**）
一列 = 一個策略。核心欄位：

| 欄位 | 意義 |
|---|---|
| `strategy` | 策略全名（含 F1/F2/C/V 標籤，可解析出結構） |
| `F1`, `F2` | 第一/第二體質因子區間（如 `ROE_>=20`、`FCF_P_0_0.05`；`None`=未用第二因子） |
| `C` | 動態條件（如 `C1_ROE_SEQ_qmax4`；`None`=未用） |
| `V` | 估值濾網（`v0`/`v1`） |
| `CAGR` | 年化報酬率 |
| `daily_sharpe` | 夏普值（見 §1.3 重要註記） |
| `max_drawdown` | 最大回撤（負值，如 -0.46） |
| `win_ratio` | 勝率 |
| `ytd` | 年初至今報酬 |
| `market` | `TW`/`US`（master_index 才有） |
| `rebalance` | 換股頻率 M/Q（master_index 才有） |
| `artifacts_dir` | 指向該策略明細檔資料夾（master_index 才有） |

master_index 另有：`N`、`job_label`、`is_cross_dup`（跨 job 同名重複標記）等。

### 1.3 ⚠️ daily_sharpe 的重要指示（**務必遵守**）
- 這批 `daily_sharpe` 沿用**學姊論文的計算方式**。使用者已明確指示：**該計算當作正確、不要質疑、不要修正**（學姊論文經指導教授檢查、口試已通過）。
- repo 裡 `code/analyze_spec_us.py` 有一段 `add_sharpe_ann()`（從 return_table 月報酬重算年化 Sharpe）。**分析台股時，除非使用者另外要求，一律以 `daily_sharpe` 欄位為準來排序/呈現**，不要用重算版取代、也不要在報告裡宣稱 daily_sharpe「壞掉」。若要輔助參考可另列一欄標明「(另計)」，但主軸用 daily_sharpe。

---

## 2. 執行前置

```bash
# 1) 確認分支
git branch --show-current   # 應為 FMP_3

# 2) 若 master_index 不存在/過舊，先建（手動、不會自動）
python code/catalog_builder.py

# 3) 分析用 pandas 直接讀 parquet 即可；可參考 code/analyze_spec_us.py 的既有函式
#    （parse_name 解析策略名、_monthly_returns 攤平逐月報酬）
```
> **只分析台股（TW）**。若 master_index 同時含 US，先 `df = df[df.market=="TW"]` 篩出台股再分析。
> **去重**：報告母體數量時，注意 `is_cross_dup`——去重後的「獨立策略」數才是真數量（用 `dedup_registry.parquet` 或 `df[~df.is_cross_dup]`）。

---

## 3. 分析任務清單（**這是主體**，分「批內逐批」與「批間比較」兩層）

> 輸出建議：批內數字表存 `_analysis_outputs_TW/{batch}/`、批間比較存 `_analysis_outputs_TW/_cross_batch/`。圖存 PNG、數字存 CSV。最後彙整成 `台股結果解讀_report.md`。**數字表優先、圖為輔。**
> **執行方式**：A–E 的批內項，寫成一個能吃「批次名參數」的函式，**對 11 批各跑一次**，統一格式輸出（方便批間並排）。可參考 `code/analyze_batch.py` 既有邏輯。

---
### 【第一層 · 批內逐批分析】對 11 批各自產生（絕不混算）

#### A. 健全性檢查（每批各自）
1. **數量**：該批總策略數、去重後數。和預期 ~7,140 對不對得起來（MOM 批 ~4,203 屬正常）。異常暴量/缺漏要標記。
2. **缺失值**：CAGR/daily_sharpe/max_drawdown/win_ratio 各欄 NaN 比例。
3. **極端值健全性**：該批 CAGR 最高/最低各 10 筆、max_drawdown 最深 10 筆。**人工判斷是真策略還是疑似 bug**（CAGR 離譜高、回撤 0、勝率 1.0 標存疑）。**不修數字，只標記待使用者判斷。**
4. **重複**：`is_cross_dup` 比例。

#### B. 母體分布全貌（每批各自）
5. **報酬分布**：CAGR 的 min/P25/median/P75/max/mean/std + 直方圖。
6. **回撤分布**：max_drawdown 分布 + 直方圖。
7. **夏普分布**：daily_sharpe 分布 + 直方圖。
8. **勝率分布**：win_ratio 分布。
9. **風險–報酬散布圖**：x=|max_drawdown|、y=CAGR（參考 `analyze_spec_us.py::fig_risk_return`）。看有無分群。

#### C. 有沒有賺錢與冠軍（每批各自，**使用者特別要求**）
10. **普遍賺不賺**：該批 CAGR>0 比例、CAGR≤0 比例、CAGR>大盤（§4）比例。一句話結論該批偏賺/偏賠/兩極。
11. **最賺策略**：該批 CAGR 最高 Top 20，列 `strategy/F1/F2/C/V/CAGR/daily_sharpe/max_drawdown/win_ratio`。
12. **最穩策略**：該批 daily_sharpe 最高 Top 20 / max_drawdown 最淺 Top 20。

#### E. 有趣模式（每批各自）
16. **F1 因子分群**：以 F1 因子分組（含該批候選因子）算 CAGR 的 mean/median/std/count。哪個因子群最好/最差？**特別看該批的候選因子群表現如何**（這直接指示該候選因子有沒有用）。
17. **單 vs 雙因子**：F2=None vs F2≠None 的績效對比。疊加是否真的更好？
18. **C 條件效果**：有 C vs C=None 對比；哪些 C 平均最能加分（參考 `recommend_C`/`avoid_C` 產法）。

---
### 【第二層 · 批間比較】11 批擺一起（**海選核心產出**）

#### F. 候選因子海選比較表（最該報告老師的東西）
20. **主比較表**：一列一批（11 列），欄位含：策略總數、CAGR 中位數、CAGR>0 比例、CAGR>大盤比例、daily_sharpe 中位數、冠軍 CAGR、堪用數（見 G）。**依「CAGR>大盤比例」或「堪用數」排序，直接看哪個候選因子最有用。**
21. **候選因子淨貢獻**：每批比較「含該候選因子的策略」vs「該批 None/純固定三因子基準」的績效差——該候選因子加進去到底有沒有加值？做成排行。
22. **視覺化**：11 批 CAGR 分布並排 boxplot（一眼看哪批整體高）、11 批堪用數長條圖。

#### G. 堪用池估算（乙案：跨批匯集，**最關鍵**）
23. **逐批堪用數**：每批算「CAGR>0」與「CAGR>大盤」各剩幾筆。
24. **匯集總池**：11 批堪用策略**加總**（乙案）= 最終堪用池規模。去重後（`is_cross_dup`）的獨立堪用數。
    - **關鍵判斷**：匯集後落在「幾千」（老師甜蜜點）嗎？太少→生成不夠多樣/篩太嚴；太多（三四萬）→ LLM 挑不動、第二層要再加粗排。**給明確一句判斷。**
25. **逐月報酬齊備性**：抽查各批若干策略的 `return_table.parquet` 能否攤平成逐月（後續階段2b/3 要用）。標記缺 return_table 或長度異常的比例。

#### D-extra. 最賺股票（跨批冠軍，使用者要求）
26. **最賺錢的股票**：從**全部批的 CAGR 冠軍策略**的 `position/trades/stock_data.parquet` 明細，找出貢獻報酬最高的個股（哪些台股在這些冠軍策略裡賺最多）。若逐檔歸因太重，退為：列冠軍策略持股 + 各持股期間報酬，標出前幾檔。

---

## 4. 台股基準（大盤）註記
- 「贏過大盤」需要一個台股基準報酬。優先用 repo / DB 裡既有的台股大盤基準（如發行量加權股價報酬指數 / 0050 類）。
- 若 repo 內沒有現成基準序列，**先標記「基準待使用者提供」**，並先用「CAGR>0」當保守版堪用池門檻產出結果，不要自己硬湊一個基準數字。

---

## 5. 產出物（交回給使用者）
1. `_analysis_outputs_TW/{batch}/`：11 批各自的批內數字表 CSV 與圖 PNG（A/B/C/E 各項）。
2. `_analysis_outputs_TW/_cross_batch/`：批間比較表與圖（F/G/D-extra）——**海選核心**。
3. **`台股結果解讀_report.md`**：**讀者是「使用者本人 + 另一個 Claude 對話」，不是老師**。用途有二：
   - (a) 給**使用者**理解這次台股結果到底跑出什麼；
   - (b) 給**另一個 Claude**當素材，**用來製作本週要報告老師的進度 ppt**。
   → 因此這份報告要**資訊完整、數字齊全、結論明確**，不是簡報稿本身（簡報化留給 Claude 做）。寧可多給數字與脈絡，讓下游能自由取捨做投影片。結構建議：
   - (1) 十一批概況總表：每批總數、賺賠比例、冠軍 CAGR（一眼看全局）
   - (2) **候選因子海選結論**：哪幾個候選因子有用/沒用（排行 + 理由）← 最重要
   - (3) 冠軍榜：跨批最賺策略、最賺股票、最穩策略
   - (4) 堪用池估算（乙案匯集）+「夠不夠 AI 選」判斷
   - (5) 有趣模式 2–3 個
   - (6) 健全性備註：極端值/缺失/存疑項（誠實列，不隱藏；MOM 批策略少屬正常要註明）
   - (7) **關鍵數字速查區**：把最可能放上投影片的數字集中列一區（總策略數、堪用池規模、海選前3名因子、冠軍策略/股票、2-3 個亮點數字），方便下游 Claude 直接取用做 ppt。
4. **所有圖表 PNG 路徑清單**：報告末尾附一份「圖檔路徑 + 一句說明」清單，讓下游 Claude 知道有哪些圖可放進 ppt。
> ⚠️ 這份報告**不是**給老師的口頭稿、也**不是**簡報。它是「完整解讀 + ppt 素材庫」。真正的老師簡報由另一個 Claude 對話依此製作。

---

## 6. 絕對不要做
- ❌ 不要重跑 Sweep / 回測 / 改 condition_factory / 動 SQL。
- ❌ 不要「修正」daily_sharpe，或宣稱它壞掉（見 §1.3）。
- ❌ 不要修改任何極端值/疑似 bug 的數字——只**標記待使用者判斷**。
- ❌ 不要只產圖不產數字表（數字優先）。
- ❌ 台美不要混算，只分析 TW。

---

## 7. 和整體研究的銜接（給你脈絡，非本次任務）
這次解讀的產出會餵給後續：第一層硬篩（用堪用池估算定刀口）、階段1 特性標記（用因子分群）、階段2b 總經對應與階段3 危機相關（用逐月/逐日報酬）。所以 §3-D、§3-E 的產出對後續最關鍵——不只是「這次跑得好不好」，而是「這批東西能不能撐起後面的 LLM 精挑」。
