# 開發待辦追蹤

> **用途**：研究部開發的工作分配與進度追蹤。**這是活文件，做完就更新狀態欄。**
> **建立**：2026-08-20　**最後更新**：2026-09-03
>
> ---
> # 🔴🔴 2026-09-02 老師會議：路線重大變更 → 見第 11 節
>
> **老師把整條畢業路線一次講完，並明確終止總經方向。** 摘要：
> 1. ❌ **方向 C（總經→選群）確定不做**——「看起來都跟總經沒關係⋯**不會有答案，不會有好答案**」
> 2. ✅ **HRP 已證明有效，足以畢業**——「你已經可以證明說 HRP 是對的，所以這樣是可以的」
> 3. 🎯 **畢業必考題只剩三項**（anchored walk-forward、精選比例掃描、HRP 質性解釋），老師估「一兩禮拜就做完」
> 4. 💼 **第二階段＝就業加分題**（AI Agent 每季選股），做完必考題再說
>
> **本節以下的第 9 / 10 節內容有大量條目已被 9-02 會議推翻或轉用途，
> 閱讀時務必先看第 11 節，衝突處一律以第 11 節為準。**
> ---
>
> **2026-08-27 更新**：切到 `Research_3` 分支。2026-08-26 與老師開會（逐字稿 `8-26咪挺.pdf`），
> 老師針對已完成的 HRP 主線給出具體修正意見，**開啟下一輪迭代**（不是重做，是接續第1節已完成的主鏈）。
> 詳細任務見新增的 **第9節 · H系列**。核心修正：牛熊/危機切割是上帝視角、不能當主打；HRP分群母體
> 要先過alpha關卡；群要有定量描述+解釋性（論文兩大重點之一）；in-sample/out-sample要切在投組配置層，
> 不是策略參數層。老師時程：**9月底前實驗做完**，接著寫中文論文，目標11月底完稿。
>
> **本次更新摘要（2026-08-25，歷史記錄）**：先**逐項用程式碼/資料實測重新查證過整份文件**——R系列與B系列表格先前跟敘述矛盾（表格打☐/🔒但敘述說已完成），查證後確認 R-01~R-09、B-02/B-04/B-05/B-07 全部真的做完，狀態欄已訂正。同日接續開發完成 **W-06、W-08、D-03、D-04、N-03**，以及兩個 LLM 點：**T10 return_story**（實戰部工具層最後一個未實作的工具）與 **W-15 cluster_story**（研究部階段3附加，84對全跑完）。事後對這批變更做 **code review**，抓到並修好 **W-16**（stage1_scan/stage1_marks 共用manifest、四份產物脫離雜湊驗證的既存bug）。
>
> ✅ **研究部第一輪主鏈程式開發已完成**（主鏈階段0→4＋output_a＋資料品質防線＋兩個LLM點，全部用真實資料驗證過，測試 77/77）。**第二輪（H系列，依老師8-26意見的HRP精修）進行中，見第9節。**
>
> ⚠️ 但有兩件事不要誤讀：
> 1. **N-02 狀態不明**——那是 collector 端要刪的美股9檔ticker重用歷史，本repo無清單可驗證，**須向 collector 確認**。本文件第6節先前誤列為「已完成」，這次訂正。
> 2. **實戰部幾乎還沒開始**——只有工具層 T1~T13 完成，Agent0/1/2 本體與 LangGraph 編排完全沒寫。研究部完成 ≠ 專案完成，詳見第6節的紅字說明。
> **背景**：2026-08-20~22 的價格資料異常修復與總經資料到位已全部完成，資料修復期間並行開發的 W 系列主鏈（階段0~4 + output_a + 實戰部工具層）也已全部完成並用真實資料驗證。
> 相關文件：`系統設計文件_v1.md`（SDD）｜`_analysis_outputs_dataquality/重跑必要性評估.md`

## 狀態圖例

`☐ 未開始`　`◐ 進行中`　`☑ 完成`　`🔒 被阻塞`　`⏸ 暫緩`

---

## 0. 目前的阻塞源（總覽）

**2026-08-25 查證：以下四個阻塞源全部已解除。**

| 阻塞源 | 負責 | 狀態 | 備註 |
|---|---|---|---|
| 台股接縫修復（186 檔）| collector | ☑ 已解除 | R-01 完成 |
| 美股 26 檔修復 | collector | ☑ 已解除 | R-01 完成 |
| 9 個因子重算 | collector | ☑ 已解除 | R-02 完成 |
| 總經資料收集 | collector | ☑ 已解除 | W-02b 用真實資料驗證完成 |

> 🔴 **2026-08-24 事後補記的意外事故**：research department 的 `_frozen/` 底下 stage0~4／output_a 的實際計算產物（非 `MANIFEST.json`）曾在整理交付用複本時被誤刪——`_frozen/**` 整個被 `.gitignore` 排除（只有 `MANIFEST.json` 例外會被 git 追蹤），是**跨分支共用的磁碟狀態**，不受切換分支保護。查證後確認**沒有實質資料遺失**：唯一的外部原始輸入（collector 交付的 `macro_raw.parquet`）在 `stock_factor_collector` repo 裡還有一份，SHA256 核對一致；其餘全部 stage 都是確定性計算、輸入未變，可完整重跑復原。2026-08-25 已依序重建，逐階段用 `research.cli verify` 核對雜湊，詳見「4. R 系列」。

**W 系列與 D 系列全部可做，B 系列已在 2026-08-25 查證後解鎖（見第 3 節）。**

> 🆕 **2026-08-31 · 論文故事線已定案（方向 A+C+D），見第 10 節。**
> 主軸是「總經 → 選群」，因此第 9 節第六階段（H-17~H-21）從「暫緩」升格為論文第四章主體。
> **H 系列全數完成**（H-01~H-24），含 H-09/H-10/H-11/H-12/H-13（免費午餐的量化與驗證，
> 真實資料跑完，見 `_analysis_outputs_robustness/H09_H10_H11_免費午餐量化與驗證.md`）
> 跟 H-08（單群LLM身份解釋，16群全跑完）。**S-01（群→總經介面）／S-02（決策層
> 資訊源設計）也已完成**——`macro_decision_input.py` 的核心決策是決策層只看
> 「條件式」績效（投資時鐘四格）、不看「無條件」績效（CAGR中位數等），避免
> 重蹈H-10/H-12已證實的「純挑歷史贏家」陷阱。**目前上游待做項目**：S-05（決策層
> 對照組設計）／S-06（Prompt風格當實驗變數）。

---

## 1. W 系列 · 可立即進行（不受資料修復影響）

| ID | 任務 | 為何不受影響 | 預估 | 驗收條件 | 狀態 |
|---|---|---|---|---|---|
| **W-01** | **階段2b：查證各總經指標的發布滯後量** | 總經資料與股價完全無關 | 1–2 天 | ☑ **2026-08-20 完成** — `code/research/macro_spec.py`（單一事實來源，10 個指標 × 官方來源連結 + `available_period()` 可得性計算）＋ 匯出 `_frozen/stage2/macro/lag_spec.json`；4 項測試鎖住邏輯（含季頻邊界與「不得晚於決策月」鐵則）。<br>⚠️ 查出 **vintage 限制**（見 N-04）與 **TW GDP 待確認項**（見 N-05）| ☑ |
| **W-02a** | 階段2b：資料契約 + 處理鏈實作 | 邏輯與資料內容無關 | 1 天 | ☑ **2026-08-20 完成** — `contracts.MACRO_RAW`／`MACRO_HISTORY` 契約；`stage2b_macro.py` 完成滯後對齊→月頻攤平→z-score 凍結→3月平滑→投資時鐘四格；`--selftest` 用合成資料驗證，**內建前視稽核 312 個月 × 2 市場、違規 0 筆** | ☑ |
| **W-02b** | 階段2b：正式建置 macro_history | — | 0.5 天 | ☑ **2026-08-23 完成並用真實資料驗證** — `macro_history_{TW,US}.parquet` 各312列，契約通過，前視稽核0違規。<br>🔴 **修過一個資料品質bug**：US growth 一開始只有44%非空率（176/312月缺值）。查證發現 collector 交付的 `release_date`，US GDP **1998Q1~2014Q2（64季/16年）全部標記同一天2014-09-26**——那是 ALFRED 資料庫自己開始追蹤vintage的起點，不是BEA當年真正的發布日。原本無條件信任release_date的邏輯把2014-09之前的所有決策月都判定「這個指標還沒發布過」，且2014-09那個月一次冒出一堆同天可得的期間，`max()`又沒tie-break，錯選到最舊的1998Q1而非最新一期。<br>修法：加合理性判準（與macro_spec推估滯後相比，落差>180天視為不可信→退回推估值），並修tie-break（同一發布日下優先取期間較新者）。修復後US growth 100%非空、2014-09正確顯示`growth=2014Q2`。<br>**同時完成 N-05**：台股GDP滯後量依collector建議從1改為2個月（`macro_spec.py`已改），並重新匯出`lag_spec.json` | ☑ |
| **W-03** | 階段3：HRP 演算法本體 | 演算法與資料內容無關 | 2–3 天 | ☑ **2026-08-22 完成** — `code/research/hrp.py`：相關→距離→linkage→準對角化→遞迴二分配權，全部用合成資料驗證（24/24 測試通過）。<br>🔴 **修過一個嚴重 bug**：`quasi_diagonal_order` 原本借 `scipy.dendrogram()` 取葉序，內部遞迴實作在 N≈7,000 就 `RecursionError`（本專案最大到 15,807，必炸）。改用 López de Prado 原書的**迭代版**演算法，兩者在小樹上結果逐位元一致，且不受遞迴深度限制 | ☑ |
| **W-04** | 階段3：**真實資料建置**（openSec 修復後資料，非髒資料乾跑）| 資料已乾淨，直接建正式樹 | 1 天 | ☑ **2026-08-22 完成** — `stage3_hrp.py`：TW/US/XM 三棵 **normal** 樹全部建成（crisis 樹待階段2a regime 窗）。<br>🔴 **DD-06 linkage 方法決定，且推翻了「用 cophenetic 選」的原計畫**：single linkage 在三棵樹上都出現典型**鏈狀效應**（TW 95.5%/US 95.9%/XM 95.7% 的策略被塞進同一巨群，60~75%的「群」是singleton），但 cophenetic 分數反而比 ward 高（TW 0.76 vs 0.50）——cophenetic 衡量的是整棵樹保真度，不代表切出來的群有沒有用。**改用「L3切割時最大群佔比」選 method**，三棵樹全選中 ward，分群平衡（最大群佔比 1.9~2.7%、0 singleton）。<br>PSD 全通過（min_eig ~1e-12量級）、三角不等式抽檢 3000組0違規、ARI(L3 vs F組合)= TW 0.186／US 0.237／XM 0.203。<br>耗時：TW 39s／US 65s／XM 292s（15,807×15,807相關矩陣），記憶體峰值遠低於預估，未觸及機器41GB可用記憶體的瓶頸 | ☑ |
| **W-14** | 產出A：20年情境×策略表現對照回顧表 | 前提（研究部主鏈+W-05工具層）已完成 | 1天 | ☑ **2026-08-23 完成並用真實資料驗證** — 新增 `research/output_a.py`。使用者確認範圍：**3類投資人各跑一份**（文件原標「待老師確認」）。<br>行=全部72個regime段（TW43+US29）×3類型=216格；選兵用**純量化規則代替尚未建置的Agent1 Step3**（呼應研究部v9 baseline①純量化階，非簡化版agentic）：非危機格跨cluster_L1分散取5檔；危機格用T5找K=4個低相關群+排除co_fail重疊，每群取信度最高者。權重用T9(HRP)。<br>🔴 **開發中抓到一個符號反向的bug**：`free_lunch_mdd_gain`（免費午餐量化）原始寫成`avg_individual_mdd - portfolio_mdd`，但MDD是負數，這個方向代表「數值越正=portfolio越差」，跟docstring說的「正值=分散有效」剛好相反。自己寫的單元測試（用互補式合成資料手算驗證）抓到，改成`portfolio_mdd - avg_individual_mdd`後測試才過；修復前對2008/2022台股危機段的初步判讀是反的，已重新產出、未對外報告過錯誤方向。<br>**結果**：216格中133格可評估（83格無適配候選，多數落在保守型熊/危機格——查證是「台股能扛熊市的高信度策略剛好都是集中持股的動能策略，撞上保守型的持股廣度下限」，真實發現非bug）；可評估格中60.2%組合報酬贏過大盤同期基準；免費午餐效果133格中104格(78%)為正值、中位數+0.66個百分點MDD改善，三類投資人正值比例相近(76~79%)。<br>新增4項測試（含2項合成資料驗證免費午餐算法方向，抓到上述符號bug），全套件 62/62 通過 | ☑ |
| **W-05** | 實戰部工具層 T1–T13 | 前提（研究部主鏈0~4）已完成，改用真HRP群非降級版 | 3–5 天 | ☑ **2026-08-23 完成並用真實資料驗證** — 新增 `code/ops/tools.py`（全新套件，與`research/`平行，對應「實戰部」）。**12/13個工具已實作**（T3/T13查表、T2過濾+cluster配額、T1矩陣、T5互補、T6/T8/T9即時算、T11當前regime、T12/T4總經模型查詢，皆用真實資料smoke test過）。<br>**T1矩陣的門檻是本階段的具體化**：架構文件只寫「標準/更嚴/最嚴」等質化描述，門檻數值本身「待資料」——本模組在**呼叫當下**用`market`即時算市場內分位數（非寫死絕對值，因台美分布差異大，如smallcap_share中位數TW32.3%/US46.8%）。危機格（保守/積極/全天候三類皆同）回傳`method="cluster_diversify"`並附註須改呼叫T5/T13做群間分散，不是簡單filter可以表達的。<br>**T5/T13的「群間互補解釋文字」固定回傳None**（LLM點③cluster_story尚未觸發，只回傳客觀數字不編造文字）。<br>**T7的「產業β」固定回傳None**（資料不存在，同階段4的限制，真alpha判決僅基於前三道並在note註明）。<br>**T9對任意子集重新跑一次完整HRP**（非查表，因使用者選的子集通常不等於整棵樹全部成員），复用`hrp.build_tree()`。<br>**T11用2a同一套zigzag函式**（直接import不重寫）跑到當前（實測台股資料到2026-07-24、美股到2026-07-02，皆為牛市段，since 2025-04附近，標記provisional因zigzag是回顧式演算法）。<br>**T4/T12新建了「方法一k-NN類比月」**（研究部v9只建了方法二投資時鐘四格，方法一之前從未實作）：歐氏距離找當前總經z-score最像的歷史K個月，兩法各自值不混。<br>🔶 **T10（generate_return_story_text）2026-08-25 已寫程式本體，但尚未用真實API驗證**：把T7的凍結判決+T3支持數字餵給LLM、用JSON schema逼結構化條列輸出（每道安檢分開寫，防止「串story」時偷渡程式沒給的因果，呼應v9「暫掛」清單的return_story殘餘幻覺對策）、`temperature=0`求可複現。受新寫的`utils/openai_quota.py`額度偵測保護（額度用盡raise `QuotaExhaustedError`、不靜默失敗）。<br>✅ **2026-08-25 已用真實API驗證完畢**（21次呼叫，約$0.05）：5種判決組合（靠少數股/規模效應/真alpha的各種True/False排列）× luna/terra 各跑一輪，**兩個模型都零幻覺**、判決全部正確反映、產業β缺失都正確表達為「無法判定」而非臆測。<br>🔴 **實跑抓到兩個我自己寫錯的東西**：①寫死的 `temperature=0` 被 gpt-5.6 系列拒絕（400 unsupported_value），已改為預設不送——**連帶影響**：v9方法論「固定溫度求可複現」對這類新型推理模型不可用，可複現性只剩「凍結輸入」+「schema限制輸出」兩道，論文若要宣稱可複現須誠實註明。②原始float精度（`effective_n=28.417404303353255`）被模型一字不漏抄進輸出，已改為呈現層四捨五入（28.4／17.5%），判決仍用原值算不受影響；**這個改動是既有測試先失敗才發現的**。<br>**模型定案 `gpt-5.6-terra`**（實測輸出穩定226~256tok，luna為214~611且中文偶有不順；按需用途下價差可忽略，故選穩定度而非便宜）。<br>新增12項W-05原有測試 + 4項T10測試，全套件 74/74 通過。<br>⚠️ **但目前沒有任何東西會呼叫T10**——觸發它的 Agent1 尚未開發，它是備好待接的零件，不是已上線的功能 | ☑ |
| **W-06** | `test_matrix_completeness.py`（12 格矩陣完整性測試）| 純 schema 檢查 | 0.5 天 | ☑ **2026-08-25 完成** — 未另開獨立檔案，改以 `research/tests.py` 新增 `t_matrix_completeness_w06` 整合進既有測試套件（沿用專案唯一的 `python -m research.tests` 執行入口，不分裂成兩套測試跑法）。<br>做法照文件 §門檻矩陣「12格逐格拆pandas條件」的驗收法：3類型×4regime×2市場全部24組合，呼叫 `T1 get_recommended_criteria` 拿條件、餵給 `T2 filter_pool` 對真實 `strategy_map` 實際套用——`T2` 對不存在的欄位本來就會 `KeyError`，此測試把這個訊號收集成「哪一格、缺哪個欄位」的清單。<br>**實測 24/24 全過，0個欄位缺失**（`T1` 現有實作全部用真實欄位名，如 `cluster_L1`/`co_fail_peers` 而非文件口語的 `cluster_id_L1`/`co_fail_regimes`），全套件 63/63 通過 | ☑ |
| **W-07** | `stage1_marks.py` 程式碼（分位計算 + 等級判定 + 兩把刀）| 門檻是分位數參數化的，程式不因資料變而改 | 1 天 | ☑ **2026-08-23 完成並用真實15,810策略驗證** — 產出 `credibility_grade`/`stability_grade`/`return_shape`/`risk_shape`/`is_usable`/`drop_reason`，契約驗證通過。<br>驗收：淘汰率 4.86%（符合<5%預期；TW 6.29%／US 3.70%，TW偏高查證為 empty_ratio 門檻在該市場較嚴，非bug）；冠軍策略均 `is_usable=True`；rotation_score=0被淘汰的323個**全部**走empty_ratio刀、0個走rotation刀（方向確認正確）；`credibility=低`組平均CAGR(21.8%)高於`credibility=高`組(17.0%)，印證「高報酬是假alpha最愛偽裝處」的設計意圖非bug；22個`stability_grade=None`查證為無可比較兄弟策略的合理留白。<br>⚠️ `stability_grade`公式為本階段對GateC模糊敘述的解讀（bucket敏感度+deltaC敏感度），非精確重現架構文件公式，已在程式docstring註明 | ☑ |
| **W-08** | 資料品質防線常設化 | 新需求，與修復平行 | 1 天 | ☑ **2026-08-25 完成** — `contracts.py` 新增 `data_glitch` 欄（`STRATEGY_MARKS`/`STRATEGY_MAP` 都有）+ 兩個門檻常數；等級判定放在 `stage1_marks.py`（呼應 stage1_scan 原本的設計意圖：scan算原始數字、marks轉等級/旗標，跟 credibility_grade 等其他欄位同一套架構）；`diagnose_price_anomalies.py` 掛進 `research.cli dataquality` 子指令，成為一行指令可跑的常規驗收（原本只能用散落的 `python -m research.diagnose_price_anomalies`）。<br>🔴 **實測抓到自己設計的第一版有真實缺口**：只用「策略NAV單日跳動≥300%」當門檻時，`data_glitch` 對深掃驗出的「CAGR因資料異常灌水>1個百分點」283個策略只接住6個，漏了277個——異常股票在20-30檔分散組合裡通常只佔小權重，單日跳動被稀釋到組合層級遠低於300%，但累積到月報酬仍有實質影響。加了第二把刀（單月報酬≥100%，門檻用283個材質案例的`max_monthly_ret`反推校準，283/283全數≥100%）後，recall回歸測試100%通過，全池標記率2.56%（404/15,810），符合canary該有的低誤報率。<br>`cross_check_data_glitch()`：`cli dataquality`跑完自動核對，驗收基準改成只看材質案例（CAGR灌水>1pp）的recall，不要求涵蓋「碰過異常股但無實質影響」的案例（那類佔比49.7%、中位數CAGR灌水=0，硬要涵蓋只會逼出高誤報率的刀）。<br>已同步更新SDD DD-13、`research/tests.py`新增2項測試（方向+真實資料recall回歸），全套件65/65通過；stage1_marks→stage4已重跑並verify通過。<br>**下游確認**：`output_a`（唯一依賴stage4的下游）也已重跑（`data_glitch`未被選兵邏輯引用，重跑後216格結果與改動前逐項相同——133/216可評估、80/133贏大盤，數字未變，只是manifest指紋更新到新的strategy_map版本）。全鏈stage1→2a→2b→2c→3→4→output_a七個manifest 2026-08-25當天全部`verify`通過，無斷鏈 | ☑ |
| **W-09** | ☑ **2026-08-22 完成** — `code/research/` code review 9 項修復 | 8/20 code review 找出 13 個發現，暫緩後於本次補完 | 已完成 | 9 項全修復並逐一驗證（見下表）；`research.tests` 16/18 通過（餘 2 個失敗屬既知 B-01，非本次 regression）| ☑ |
| **W-10** | 階段2a：Regime Dating（zigzag牛熊切割，做法甲純價格）| 只用大盤基準價格，不受財報資料修復影響 | 1 天 | ☑ **完成並用真實資料驗證** — `stage2a_regime.py`：TW 43段（牛22/熊16/危機5）、US 29段（牛15/熊11/危機3）。<br>驗收（v9明確要求的人工核對）：5個已知事件（2008海嘯/2015中國股災/2018Q4股災/2020 COVID/2022熊市，依市場適用性）**TW 5/5、US 4/4（1個中國股災事件對美股不適用，非漏抓）全數命中**；`regime_table` 銜接測試確認區間連續無空隙無重疊。<br>門檻從20%調整到15%（bear/bull_thresh）：實測 TW/US 2018Q4分別跌15.5%/19.8%，20%門檻會漏抓，15%後兩者皆命中且未明顯增加雜訊段。<br>本次（2026-08-23）追加：`contracts.REGIME_TABLE` 契約補齊並接進 `stage2a_regime.run()`（原本沒驗證自己的輸出），重跑確認仍全數通過 | ☑ |
| **W-11** | 階段2c：交叉佐證（2a regime × 2b 總經四格 匯流）| 2a、2b 皆已完成 | 0.5 天 | ☑ **2026-08-23 完成並用真實資料驗證** — 新增 `stage2c_consistency.py` + `contracts.REGIME_CONSISTENCY`／`REGIME_EXPECTED_CELLS`：核對熊/危機段是否多落在總經「停滯性通膨/衰退」格、牛段是否多落在「復甦/過熱」格（>50%多數決），盤整不檢查。<br>**結果**：TW 43段可判定，20/43一致（47%，月數加權42%）；US 29段可判定，16/29一致（55%，月數加權59%）。<br>抽查多筆背離段確認非bug、是真實的「價格領先總經」現象——例如TW 2008-11~2010-01牛段(GFC後反彈)總經仍多落在「衰退」格（市場先落底、實體經濟晚半年才回升，教科書級現象）；TW 2022危機段10個月剛好5:5（過熱/停滯性通膨），卡在50%邊界判不一致，人工核對數字無誤。<br>依做法甲精神：**背離段保留不修regime，只標記**（`regime_divergence_{market}.csv`）。<br>產物目錄改放獨立的 `_frozen/stage2/consistency/`（非文件原描述的與2a同一個`regime/`資料夾）——因 `freeze.write_manifest` 每目錄只保留一份`MANIFEST.json`，共用會覆蓋2a自己的凍結紀錄，破壞DD-08凍結鏈，已在模組docstring註明此解讀理由。<br>`cli.py` 補上 `stage2a`/`stage2b`/`stage2c` 三個獨立進入點（原本只有一個含糊的`stage2`佔位、模組=None）。測試：新增6項單元測試（合成資料，含多數決/方向不寫反/盤整不檢查/無資料留白/tie-break確定性/真實資料契約），全數通過 | ☑ |
| **W-13** | 階段4：彙整成 strategy_map | 前提（階段0/1/3）已完成 | 1 天 | ☑ **2026-08-23 完成並用真實資料驗證** — 新增 `stage4_strategy_map.py` + `contracts.STRATEGY_MAP`／`REGIME_PERFORMANCE`／`MACRO_PERFORMANCE`。<br>**本質是彙整**：candidate_index(0)+strategy_scan(1原始指標)+strategy_marks(1等級) join 起來就佔了 GateC C-1~C-5 幾乎全部欄位（都已在階段1掃描時算好）。真正新算只有三件事：<br>① **regime_fit**（GateC「含『熊市抗跌』等」，無精確公式，本階段解讀：該regime月份平均報酬>=0且n_months>=3才貼標籤，只對熊/危機貼——牛市好不算防禦賣點）：TW 熊28/7128、危機2/7128；US 熊405/8682、危機1371/8682（TW危機段多是-30~-56%的急殺、只有2個策略扛得住；US較溫和+2020V型反彈更多策略沾到）。<br>② **macro_fit**（四格×策略 平均報酬/勝率/樣本數/信心）：查完發現TW策略在「衰退」格平均月報酬3.04%最高、「過熱」格-0.01%最低（幾乎打平）——已用clock_cell月數分布交叉驗證不是小樣本噪音（過熱格反而月數最多82個月），是真實的「TW因子策略在成長趨緩期表現最好、過熱期最差」現象。<br>③ **v1_beneficial**（market×f_combo×C_id子樹級事實，v0/v1兩列廣播相同值）：13,028/15,810列有對照組（跟先前手算的6,514組×2完全吻合），23.7%子樹v1優於v0。<br>**HRP投影**：cluster_L1/L2/L3/co_fail_peers 只投影「策略自己市場的常態樹」，完整六棵樹仍在附屬表 `cluster_assign.parquet`/`co_fail_regimes.parquet`。覆蓋率95.1%（DD-03窗外的少量策略無群id，正常）。<br>🔴 **重跑前查出上一個bug**：`_tree_universe()` 從沒過濾 is_usable，769個階段1淘汰策略全部漏進六棵HRP樹，已在銜接階段4前修好重建（見W-12更新）。<br>❌ **明確不做**（非遺漏，已在程式docstring說明）：`return_story`四道安檢判決——其中「產業β」需要持股逐月產業分類資料，目前資料庫/管線完全沒收集這項，硬做會是編造；`cluster_story`（LLM點③，Opus群間互補因果解釋）——要花錢呼叫LLM且v9定位它是離線一次性產物，留給使用者決定何時觸發。<br>新增4項測試（2項regime_fit方向/門檻合成資料+1項v1_beneficial廣播邏輯+1項真實資料契約），全套件 46/46 通過 | ☑ |
| **W-12** | 階段3：HRP **互補性**部分（crisis 樹 + co_fail_regimes，2a regime窗已就緒故解除阻塞）| 前提（2a）已完成 | 1 天 | ☑ **2026-08-23 完成並用真實資料驗證** — `stage3_hrp.py` 重構：抽出 `_build_tree()` 共用 normal/crisis 全流程；新增 `run_crisis_tree()`（同一批DD-03策略，相關只用危機窗月份算）、`build_co_fail_regimes()`、`contracts.CO_FAIL_REGIMES`。<br>**危機窗定義**（v9只寫「危機樹三組各用對應危機窗，台↔美判兩套」，未明講合併規則，本階段的解讀）：TW/US單市場樹各用自己的2a危機段（落在DD-03窗內）；**XM合併樹用聯集**（非交集）——實測台美危機交集僅6個月（2008-05~10 GFC同步高峰）、聯集26個月，交集會把「台股獨有2022危機、美股當時沒有」「美股獨有2020 COVID、台股當時已回穩」這些台美不同步月份全部丟掉，那正是論文要呈現的跨市場分散證據，已在docstring說明。<br>🔴 **重跑前發現並修復一個真實 bug**：`_tree_universe()` 從沒 join `strategy_marks.is_usable`，導致階段1尾端硬篩掉的769個策略全部漏回六棵樹（TW污染6.26%／US 3.70%／XM 4.85%，比例與階段1淘汰率吻合，證實非邊緣案例）。已在 `run()` 載入時補上過濾，重建六棵樹，污染歸零，並新增回歸測試 `t_stage3_universe_excludes_non_usable` 防止復發。<br>**修復後最終結果**：六棵樹全部建成（TW 6,679／US 8,360／XM 15,040 個可用策略，各一常態一危機），危機窗月數 TW 17／US 22／XM 26，PSD全通過（N≫T的秩不足不影響PSD，屬預期現象非bug）、0個零變異數策略被排除。<br>**co_fail_regimes**：TW 8個L1常態群中8個在危機期與其他群塌一起、US 5/8、XM 5/8。<br>**結構塌縮量化**（L1最大群佔比，normal→crisis）：TW 28.6%→35.4%（塌縮）、US 21.7%→25.6%（塌縮）、**XM 30.0%→25.7%（L1不塌縮，但L2/L3兩個更細層級都確認塌縮：0.075→0.088、0.013→0.017）**——已誠實記錄這個層級依賴的細節，不能只講L1好看的部分。<br>`cli.py`/`tests.py` 同步更新，新增6項測試（3項合成資料co_fail邏輯+2項真實資料契約+1項usable_pool回歸測試），全套件 42/42 通過 | ☑ |

| **W-15** | 🆕 階段3附加：`cluster_story`（研究部 v9 的 **LLM點③**，群間互補因果解釋）| 前提（階段3六棵樹+階段4 strategy_map）已完成 | 1 天 | ☑ **2026-08-25 完成並用真實資料實跑** — 新增 `research/cluster_story.py` + `contracts.CLUSTER_STORY`／`COMPLEMENTARITY_CUTS`。三棵 normal 樹 × L1 全部 28 對＝**84 對全跑完**，契約通過，凍結為 `_frozen/stage3/cluster_story.parquet`。<br>🔴 **開發前實測推翻了原本的天真設計**：先量了六棵樹的群間相關分布，發現**市場內幾乎沒有真正低相關的群對**（TW 0.539~0.981、US 0.847~0.979），跨市場才有（XM 0.372~0.976）。若照原訂「問LLM解釋這兩群為何互補」，對相關0.98的群對LLM幾乎一定會編一套聽起來合理的經濟故事——那正是論文最不能有的東西。**故互補程度改由程式判定**（`COMPLEMENTARITY_CUTS`：<0.5高／<0.8中／否則低），LLM只為既定判決寫說明，與T10同一套「判斷歸程式、LLM只寫字」原則。<br>**實跑結果**：低59對／中20對／高5對，且**高互補的5對全部是XM樹的「群1(台股)×某美股群」**（corr 0.372~0.407）；美股樹內28對全數低互補、台股樹內0對高互補。→ 量化證實**分散效果只存在於跨市場**，單一市場內部怎麼分群都高度同步。另：XM樹L1**完美依市場分裂**（群1-3全台股、群4-8全美股、0混合）。<br>**抗幻覺稽核**：掃過全部59對「低互補」文字，檢查有無偷宣稱「能分散/互補性強/適合搭配」→ **0件違規**；抽驗corr=0.979那對，LLM明確寫「放在一起不會帶來分散效果」，並主動說明co_fail=False不能推翻此判決。<br>**模型**：`gpt-5.6-sol`（比T10的terra強一級，因v9定位此點為「核心賣點、進論文」且任務是因果推理非單純轉譯）。成本：prompt 112,861／completion 43,046 tok ≈ **$1.31**。<br>**不寫進stage3的MANIFEST**：LLM輸出不保證位元可複現，納入雜湊驗證會讓stage3每次verify都失敗；改自帶 `cluster_story_meta.json` 側錄（同 stage2c 當初選獨立目錄的理由）。<br>連帶：T5 的 `explanation_text` 從固定None改為查此凍結產物，**查不到時回None但客觀數字照給**（新增測試鎖住這個「選配產物缺席也不能壞、更不能編造」的行為）。新增4項測試，全套件 74/74 通過 | ☑ |
| **W-16** | 🔴 code review 修復：`stage1_scan`/`stage1_marks` 共用同一份 MANIFEST.json | 2026-08-25 對 W-08/W-15 的變更做 code review 時發現的既存bug，非本次新引入 | 0.5 天 | ☑ **2026-08-25 完成並用真實資料驗證** — 兩階段都寫 `freeze.write_manifest(..., paths.STAGE1, ...)`，後寫的（stage1_marks）蓋掉先寫的（stage1_scan），導致 `strategy_scan.parquet`／`returns_monthly.parquet`／`annual_returns.parquet`／`returns_meta.parquet` **四份產物完全脫離雜湊驗證**、且沒有任何報錯——`research.cli verify stage1` 過去實際上只驗到 `strategy_marks.parquet` 一份。這正是 DD-08 凍結鏈要防的情境，與先前 `_frozen` 誤刪事故同一類風險（差別是這次是自己的code review抓到，非事後才發現）。<br>**修法**（比照 `stage1_mktcap.py` 已有的 `_mktcap/` 前例）：`stage1_marks.py` 的 manifest 改寫進獨立子目錄 `_marks/`，實際產物檔案位置不變。連帶修正 `stage3_hrp.py`／`stage4_strategy_map.py`——兩者都讀 `strategy_marks.parquet`，原本只驗 `paths.STAGE1` 那半邊，現在補上對 `_marks/` 的 `verify_inputs()`。`cli.py` 的 STAGES 拆成 `stage1`（=stage1_scan，順手訂正原本說明文字寫「→strategy_marks」但模組其實是scan的誤導）與新增的 `stage1_marks`，兩者可各自 `verify`。<br>**驗證**：重跑 stage1_scan（利用既有shard，547s即完成merge，非重新全量掃描）→ stage1_marks → stage3（547s，六棵樹重建，策略數與之前逐位元相同：TW 6,679／US 8,360／XM 15,040）→ stage4 → output_a，**全鏈216格結果與改動前完全相同**（133可評估/80贏大盤），確認只是修補雜湊保護、沒有動任何實際資料。新增回歸測試 `t_stage1_scan_and_marks_manifests_are_independent` 鎖住兩份manifest不再共用目錄，全套件 77/77 通過 | ☑ |

> ✅ **W-04 紀律更新**：原計畫「乾跑結果不得定案」是針對**髒資料**；現在資料已修復完成，
> 本次是在**正式資料**上跑，linkage 方法選擇（ward）**已可視為定案**（見上方 bug 記錄的分群平衡度證據）。
> **L3 群數仍維持錨定 F組合數的假設**（TW 218/US 235/XM 453），ARI 0.19~0.24 顯示分群與 F組合有
> 中等程度對應但非完全一致——這是預期中的結果（HRP 用相關結構分群，F組合是用因子定義分群，
> 兩者出發點不同，中度相關代表兩者互相佐證而非重複）。**B-03 正式解除**。

---

## 2. D 系列 · 文件修正（現在就能做）

| ID | 項目 | 嚴重性 | 說明 | 狀態 |
|---|---|---|---|---|
| **D-01** | 🔴 **`rotation_score` 方向矛盾** | **高 · 實質隱患** | ☑ **2026-08-20 完成** — 研究部 v9 自相矛盾：定義「分數低＝有輪動」、淘汰規則「低 EffN ＋**不**輪動＝淘汰」（不輪動＝分數**高**），但尾端硬篩表寫「低 EffN ＋**低** rotation_score」——**方向相反**。SDD 照抄跟著錯。**不修會砍錯策略且不報錯。** 已修：研究部 v9 尾端硬篩表（加 v9.2 更正區塊）、SDD DD-09（改寫並加實測分布）、SDD 階段1 規格、SDD 3.3 欄位表、GateC v1.2。<br>**另發現分位框架在此欄位會退化**：62.4% 的值恰為 0，切點須用 P95+，已一併寫入三份文件 | ☑ |
| **D-02** | `coverage_ratio` 定義失效 | 中 | ☑ **2026-08-20 完成** — 新增 SDD **DD-12** 完整記錄（含 `backtest.py:344` 權威定義、無 C 條件策略的決定性反證、市場常數證據）；回填研究部 v9 C-4／欄位表／需求路徑／待資料清單、GateC C-4（v1.2）| ☑ |
| **D-03** | 落差清單加第 6 項 | 中 | ☑ **2026-08-25 完成** — `系統設計文件_v1.md` 新增「1.7 落差六」（原1.7可複用既有程式改編號1.8），記錄NUTX案例、v1/v2判定方法論、186+26檔修復規模，並點出「這是六項落差中唯一導致重跑者」的定位 | ☑ |
| **D-04** | SDD 新增「資料品質防線」章節 | 中 | ☑ **2026-08-25 完成** — 新增 **DD-13**，記錄兩把刀（`max_daily_ret`≥300%單日／`max_monthly_ret`≥100%單月）的判準與校準過程、還原價下絕對價位不可當判準的教訓；順手訂正3.3表格裡一個既存的過期殘留（`coverage_ratio`早被DD-12拆掉，該表當時漏改，這次一併補上`empty_ratio`/`eval_month_ratio`/`drop_reason`/`data_glitch`）、6.2訂正實作位置與拿掉不可行的非空率門檻 | ☑ |
| **D-05** | 研究部 v9 記錄本次事件 | 中 | 資料異常的發現、collector 查證結論、重跑決議，寫進 v9.2 修訂區塊 | ☐ |

---

## 3. B 系列 · 資料修復前不得定案（2026-08-25 查證：已全部解鎖）

> **這一節原本是為了防止「用髒資料做的決定被誤當定案」。2026-08-25 逐項回去看程式碼實際內容，
> 確認 B-02/B-04/B-05/B-07 的決定其實都已經在 W-03/W-04/W-07/W-13 完成時一併做掉了，
> 只是這裡的狀態欄沒有跟著翻，特此訂正。**

| ID | 項目 | 現有（暫定）值 | 為何必須重新推導 | 狀態 |
|---|---|---|---|---|
| **B-01** | `contracts.py` 的 `EXPECTED_*` 驗收常數 | ☑ **2026-08-22 完成** — 新值：總列數 15,810／TW 7,128・US 8,682／`F2_empty` 384・**786**（美股翻倍，因基準修正使門檻放寬）／F組合 218・235／v0v1 拆分／基準 CAGR TW 8.4256%・US 11.0556% | 全部改用修復後 openSec 數字，並修正 `research.tests` 裡硬編碼的碰撞數斷言（1381→改為 `>0` 的結構性檢查，不再釘死快照數字）| ☑ |
| **B-02** | DD-03 HRP 共同窗 | ☑ **確認**（見 `stage3_hrp.py` 檔頭註解）— **台 2007-01(228月)／美 2002-01(288月)／XM 2007-01，用修復後新候選池的 hist_start 分布重新驗證過，數字不變** | ~~決策邏輯不變，數字要重算~~ 已重新驗證完成 | ☑ |
| **B-03** | DD-06 L3 目標群數 + linkage方法 | ☑ **2026-08-22 完成** — linkage 方法：**ward**（實測 single 有鏈狀效應，見 W-04）。L3 群數沿用 F組合數錨定：TW 218／US 235／XM 453（新候選池數字）。ARI(L3 vs F組合)：TW 0.186／US 0.237／XM 0.203 | ☑ |
| **B-04** | 階段3 正式執行 | ☑ 即 W-03/W-04/W-12 | HRP 六棵樹（TW/US/XM，各一常態一危機）已在修復後資料上正式建成並驗證，見 W-04/W-12 | ☑ |
| **B-05** | strategy_map 凍結 | ☑ 即 W-13 | `stage4_strategy_map.py` 已完成並凍結，見 W-13 | ☑ |
| **B-06** | 向老師更正美股數字 | ~~68.9% → 55.5%~~ **已作廢**，用正確清單重算為 68.94% → **68.94%（不變）** | 數字本身已確認不需更正（清理前後一致）。**「要不要跟老師報告這段查證過程」是溝通動作，非資料/程式問題，留給使用者決定** | ☐ 數字已確認／溝通動作待你決定 |
| **B-07** | 階段1 兩把刀的門檻值 | ☑ 即 W-07 | `stage1_marks.py` 已用真實15,810策略跑出`credibility_grade`/`stability_grade`等分位門檻並驗證，見 W-07 | ☑ |

---

## 4. R 系列 · 資料修復完成後的重跑順序（2026-08-25 查證：全部完成）

> ✅ **2026-08-22 全部完成**：collector 修復（台股接縫186檔/美股確認26檔）→ 因子重算 →
> 基準重算（TW 8.67%→8.43%／US 12.35%→11.06%，2026-08-25 直接讀`results_artifacts/TW_UNIVERSE_M/stats.parquet`
> 複驗 CAGR=8.4256%，確認是修復後乾淨值）→ Phase1（primary清單：TW ROE出OCF_E進／US不變）
> → Phase2順序bug修正 → Phase2-4全鏈（台先跑完、美接續）→ 18張圖鑑重建 → 孤兒清理(2,646目錄/5.10GB)
> → **B-01 常數更新** → **階段0/1 全套重跑**（15,810策略，0失敗）→ **測試 18/18 通過**。
>
> ### ✅ 2026-08-24 三變體重跑全部完成（原記錄的斷點已補完）
> **`all`**：L2回測(8/22 04:55)→P2分析→P3(7,833策略,8.49h)→P3分析→
> 白名單驗證373/373遺漏0→P4(2.66h)→P4分析。
>
> **`strict`**：原本卡在 Phase3 中途被中斷（無`_DONE`），2026-08-24 發現後重跑完成——
> Phase3(3,129策略,1.32h)→Phase4(3,128策略,0.49h)→兩個analyze，全部真正重新展開＋回測，
> 非沿用舊產物（曾發生過`is_done()`只看`_DONE`存在與否、誤把8/17舊資料當成已完成而跳過的問題，
> 已刪掉對應`results_artifacts`子目錄強制重跑修正）。
>
> **`relaxed`**：原本Phase3/4底層資料是8/17的舊回測（analyze雖然在8/22重跑過，但只是重新彙整舊資料，
> 不是真的重新backtest），2026-08-24同樣強制重跑——Phase3(4,473策略,3.02h)→Phase4(4,471策略,1.56h)→
> 兩個analyze。
>
> 三變體現在 `results_artifacts` 底層與 `_analysis_outputs_phase3/4` 的 `_DONE`／CSV 全部一致更新，
> 且已提交進本分支（`git show HEAD:...`核對過 strict 3,043列／relaxed 4,340列／
> Phase4 strict 4,962列／relaxed 7,071列，與重跑後實測完全吻合）。

> **關鍵設計：R-04 是決策點，只花 18 分鐘就能決定要不要花 20 小時。**（回顧：此次判定為「primary清單有變」，故走了 R-05 全鏈重跑）

| 順序 | ID | 動作 | 實測耗時 | 產出／判準 | 狀態 |
|---|---|---|---|---|---|
| 1 | R-01 | collector 修復價格資料（台股接縫 + 美股 26 檔）| — | 全序列對獨立源比值無階梯 | ☑ |
| 2 | R-02 | 重算受影響的 9 個因子 | 1.5–2 h | — | ☑ |
| 3 | R-03 | 重算 `universe_benchmark` | 待估 | 見 N-01——基準已重算為乾淨值，2026-08-25 複驗 TW=8.4256% | ☑ |
| 4 | **R-04** | **只重跑 Phase 1** | **8.7 分/市場 → 台美 18 分** | **決策點**：primary 清單有變 → 判定走 R-05 | ☑ |
| 5a | R-05 | primary 清單**有變** → 重跑 Phase 2–4 | Ph2 42.5分／Ph3 5.3h／Ph4 3.9h，台美共約 **20 h**（可過夜）| 新候選池（含三變體，見上方2026-08-24補記）| ☑ |
| 5b | R-06 | （不適用，R-04判定走R-05）| — | — | ☑ |
| 6 | R-07 | 重跑階段0 `candidate_index` | 0.5 秒 | 契約驗證通過，2026-08-25 因`_frozen`誤刪事故重跑過一次，15,810列雜湊核對一致 | ☑ |
| 7 | R-08 | 重跑階段1（mktcap + scan）| mktcap 6 分／scan 6 分 | 4 份產物落地 | ☑ |
| 8 | R-09 | **更新 B 系列全部常數與決策** | 0.5 天 | B-01~B-07 逐項填入新值並解除 🔒，見第3節（2026-08-25 訂正） | ☑ |

**我的估算（供 R-04 對照，非定案）**：台股 4 個因子判定會變（ROE primary→邊際、ROIC 邊際→淘汰、OCF_E 與 FCF_P 升級）、美股 3 個（EPS primary→邊際、FCF_OI 與 NETDEBT_EBITDA 升級）。**這是近似計算，ρ 變動約 0.10 剛好落在 0.85 門檻附近，估計誤差可能影響判定——所以要實跑驗證。**

---

## 5. N 系列 · 新發現待確認

| ID | 發現 | 說明 | 建議 | 狀態 |
|---|---|---|---|---|
| **N-01** | ⚠️ **自建宇宙基準本身可能被污染** | 台股基準 8.67% 是「同宇宙、等權、含股利」＝等權持有 1,775 檔，**那 186 檔異常股就在裡面**。粗算 2024-01-02：`77 檔 × (1/1775) × 400% ≈ +17%` 單日。三個群聚日加總，基準可能被高估**接近 1pp**。<br>**嚴重性**：Phase 2／Phase 3 的所有門檻都是「贏過基準」→ 基準灌水 → 門檻墊高 → **篩掉了本該過關的策略**（方向是過度嚴格，非放水）| ☑ **2026-08-25 複驗完成** — 已隨R-03一起重算，直接讀`results_artifacts/TW_UNIVERSE_M/stats.parquet`確認CAGR=8.4256%，即修復後乾淨值（非污染前的8.67%），問題已解除 | ☑ |
| **N-02** | 美股 NO_HISTORY 9 檔的影響被低估 | 那 9 檔是 ticker 重用，**整段早期歷史都是別家公司**，不只是跳動那一天。目前的影響估算只扣掉單日，屬保守低估 | 修復時整段刪除（collector 已規劃）| ⚠️ **無法從本專案驗證，需向 collector 確認**（2026-08-25 查證）——這是 collector 端的動作（見 `_analysis_outputs_dataquality/重跑必要性評估.md` 第136行「NO_HISTORY 9 檔刪除該段歷史」），不是我們這邊的程式；且本 repo 沒有那 9 檔的 ticker 清單，無從檢查是否真的刪乾淨。⚠️ **注意：本文件第6節先前把 N-02 列在「已完成」是錯的**，與本列的 ☐ 互相矛盾，該處已一併訂正 |
| **N-04** | ⚠️ **總經資料只能「時點對齊」，做不到「版本對齊」** | 我們能確保只用「該時點已發布」的期間資料，但拿不到「該時點當下看到的原始數值」。GDP／失業率事後會被修正，而 TEJ／FMP 存的是**修正後的最新值**；美國有 ALFRED 保存歷史版本，台灣無對應服務。<br>→ 消除了「用到未來期間」的偏誤，殘留「用到未來修正值」的偏誤（影響小得多，但存在）| **寫進論文 limitations**；CPI／利率幾乎不修正故影響最小，GDP 與景氣領先指標最需註明 | ☐ |
| **N-05** | TW GDP 存的是概估數還是初步統計數？ | 主計總處分兩階段發布：**概估統計**季末後約 1 個月、**初步統計**季末後約 2 個月（國民所得統計評審會）。目前 `macro_spec` 暫定 `lag_months=1`（概估）。**若 TEJ 存的是初步統計數，實際可得時點應為 2 個月** | ☑ **已解決**（見 W-02b 2026-08-23 記錄）— collector 確認後 `macro_spec.py` 的 TW `growth` 已改為 `lag_months=2`，程式內留註解「原值lag_months=1已作廢，見N-05」，2026-08-25 複查原始碼確認 | ☑ |
| **N-03** | collector 驗證的限制 | 判定只針對「每檔最大的那次跳動」，不排除同檔其他日期還有較小接縫（TNXP、RCAT、QXO、BMNR 事件數 2–80）| ☑ **2026-08-25 完成重掃**（`python -m research.cli dataquality`，隨W-08一起做）——結果：TW 3檔/3次事件、US 204檔/475次事件仍判定為資料異常，較R-01/R-02修復前的186+26檔規模大幅下降；已對映射到的7,850個受影響策略算CAGR灌水量化，283個>1pp的材質案例已被新版`data_glitch`100% recall涵蓋。⚠️ W-09 code review第9/10項（判準用全期最高價、基準價與pct_change對齊）**未在本次重新獨立稽核**，維持先前「非阻塞、留待有疑慮再查」的決定，因結果已用獨立的CAGR灌水量化交叉驗證過、數字合理 | ☑ |

| **N-06** | 🆕 **建議總經資料改用免費官方源，不吃 FMP 額度** | 美股用 **FRED API**（免費、涵蓋 BEA/BLS/Fed/Treasury 全部所需序列）；台股用主計總處／國發會／央行公開統計。<br>⭐ **額外好處**：FRED 的 **ALFRED** 提供 vintage（歷史版本）→ **可望解決 N-04 對美股的限制**，讓美股做到真正的 point-in-time | 見第 8 節規格 | ☐ |

---

## 7.5 W-09 · code review 修復明細

| # | 檔案 | 問題 | 驗證方式 |
|---|---|---|---|
| 1 | stage1_scan.py | `--limit` 測速分片與正式分片同名，會靜默污染正式產物 | 實跑確認 probe 落在獨立 `scan_probe/`，正式 `scan/` 未被動 |
| 2 | stage2b_macro.py | `np.select` 對 NaN 誤判，缺值月份被貼假的四格標籤 | 真實資料驗證，誤標數 = 0 |
| 3 | stage2b_macro.py | `--selftest` 缺 `unit` 欄位，文件教的指令跑不動 | 重跑 `--selftest` 全過 |
| 4 | validate_macro_raw.py | 混合月/季頻 `PeriodIndex` crash | ☑ 已於查證總經資料交付時修過 |
| 5 | contracts.py / validate_macro_raw.py | 契約禁止 NaN 但提示訊息教人填 NaN，自相矛盾 | 改寫對齊：缺值 = 不寫那一列 |
| 6 | contracts.py | `RETURNS_META` 卡死列數，任何策略失敗會讓全部產物一個都生不出來 | 移除硬性 `expected_rows`，改記警告 |
| 7 | stage2b_macro.py | （原疑慮：只建到 in-sample 末端）| **重新判斷非 bug**——是設計如此，實戰部即時查詢是未開發的另一支工具 |
| 8 | stage2b_macro.py | 同指標裡部分期間缺 `release_date` 會被整個排除 | 加入逐期間 fallback，不再靜默丟資料 |
| 9 | diagnose_price_anomalies.py | 判準只用已標記列、用全期最高價 | 維持先前決定，留給 N-03 重掃時處理 |
| 10 | diagnose_price_anomalies.py | 基準價與 pct_change 對不齊 | 同上，留給 N-03 |
| 11 | stage2b_macro.py | ffill 延續的月份，provenance 欄位顯示「無來源」，稽核工具本身不可靠 | 真實資料驗證：ffill 月份 `src_periods` 正確顯示來源期間 |
| 12 | stage1_scan.py | 上次失敗的錯誤檔，這次乾淨重跑不會被清掉 | 合成測試：確認過期檔被刪除 |
| 13 | stage1_mktcap.py | 只在同次呼叫兩市場都跑才寫 manifest，分開跑永遠寫不出 | 只重跑一個市場，manifest 仍正確補齊 |

---

## 8. 給 collector 的總經資料抓取規格（W-02b 的前置）

**交付格式**：`_frozen/stage2/macro/macro_raw.parquet`，long 格式，欄位依 `contracts.MACRO_RAW`：

| 欄位 | 說明 |
|---|---|
| `market` | TW / US |
| `indicator_key` | `growth`／`growth_alt`／`inflation`／`rate_level`／`rate_direction` |
| `period` | 資料所描述的期間，`YYYY-MM` 或 `YYYYQn` |
| `value` | 數值 |
| `release_date` | ⭐ **該筆的實際發布日**（有就填）——有這欄就能做真 point-in-time，不必倚賴推估滯後量 |

**期間範圍**：1999-01 起（z-score 用 2000-01~2025-12，前一年供平滑暖機）

**美股 · FRED（免費，無額度限制）**

| `indicator_key` | FRED series | 說明 |
|---|---|---|
| `growth` | `A191RL1Q225SBEA` | 實質 GDP 年化季增率（季）|
| `growth_alt` | `UNRATE` | 失業率（月）|
| `inflation` | `CPIAUCSL` | CPI（月）→ 需自行算年增率 |
| `rate_level` | `FEDFUNDS` 或 `DGS10` | 聯邦基金利率／10Y 殖利率 |
| `rate_direction` | `T10Y2Y` | 10Y−2Y 利差，**FRED 直接提供，不必自己相減** |

> ⭐ 若能一併用 **ALFRED**（`realtime_start`/`realtime_end` 參數）取 vintage，請填 `release_date`，
> 這會讓美股從「時點對齊」升級為「版本對齊」，直接改善 N-04。

**台股 · 官方公開統計**

| `indicator_key` | 指標 | 來源 |
|---|---|---|
| `growth` | 實質 GDP 年增率 | 主計總處 國民所得統計資料庫 |
| `growth_alt` | 工業生產指數年增率 | 經濟部統計處 |
| `inflation` | CPI 年增率 | 主計總處 物價統計資料庫 |
| `rate_level` | 央行重貼現率 | 中央銀行 |
| `rate_direction` | 景氣對策信號綜合判斷分數 | 國發會 |

**❓ 需 collector 回答**：TEJ／主計總處的 GDP 欄位存的是**概估數**（季末+1月）還是**初步統計數**（季末+2月）？這決定 `macro_spec` 的 `lag_months` 該設 1 還是 2（見 N-05）。

**風險軸（第 5 欄）**：v9 定案**留白**，不需交付。

---

## 9. H 系列 · 老師 2026-08-26 意見的後續開發（Research_3 分支）

> **背景**：2026-08-26 開會逐字稿 `8-26咪挺.pdf`。老師對照已完成的 HRP 主線給出具體修正，
> 這裡記錄跟 Claude 討論後定案的開發項目，接續第1節已完成的主鏈，屬於**下一輪迭代**。
>
> **順序邏輯**：老師原話「我是你就先跑1，然後在你現在這個3，我會有2」——alpha關卡(1)先過，
> 才跑HRP(3)，總經(2)最後且不急，因為2要預測的對象就是3產出的群，群沒描述清楚，2無從預測起。

### 第一階段 · alpha 出賽關卡

| ID | 任務 | 決策內容 | 狀態 |
|---|---|---|---|
| H-01 | 新增 alpha 出賽門檻 | ☑ **2026-08-28 完成** — `contracts.py`（STRATEGY_MARKS/STRATEGY_MAP 都加）+ `stage1_marks.py` 新增 `passes_alpha_gate`（`CAGR >= BENCHMARK_CAGR[market]`），獨立欄位不覆寫`is_usable`。**實跑發現關鍵事實**：15,810個策略 **100% 通過**，不是門檻沒用——查證後確認`CAGR>基準`這關早在`phase4_analyze.py`的「Candidate Strategy池」（`best["CAGR"] > bench`）就做過了，`stage0_index.py`直接讀那份`final_candidates.csv`，研究部pipeline拿到的母體本來就已經全部贏大盤。`passes_alpha_gate`的價值不是篩選，是把這個隱含在Phase4 CSV裡的事實變成研究部資料契約裡明確可查的欄位。使用者確認「只要確認都有贏過大盤即可」，不需額外加寬度，**H-01到此定案**。stage4_strategy_map.py靠schema驅動的merge自動接住新欄位，不需改code | ☑ |
| H-02 | 重跑 stage3 六棵樹（過濾後池＋僅IS窗） | ☑ **2026-08-30 查證定案：不需要，判定為已解決（moot）** — 原本綁了兩件事：①alpha過濾後池：H-01已查明100%已贏大盤，過濾後池＝現有池，零變化，不必重跑。②僅IS窗：H-11拍板後查證發現，H-11實際採用的做法是**另開一條完全獨立的驗證分支**（`stage3_hrp_isoos.py`／`_frozen/stage3_isoos/`），主線六棵樹（`_frozen/stage3/`，全時間窗）**原封不動保留當唯一的正式群定義**，供H-06/H-07/H-08/S-01等後續所有「描述群、餵給總經決策層」的工作使用；IS/OOS只用來**驗證**投組配置層的穩健度（H-12/H-13），不取代主線的分群結果。**這個設計本身也呼應論文章節骨架**（第10.2節）：第二三章的量化/解釋用全資料，IS/OOS驗證是獨立的第五章，不是要把前面的分析全部换成IS窗重做一次。故H-02原本設想的「重跑主線六棵樹」不需要發生，主線從H-03起沿用至今的六棵樹即為最終版本 | ☑ 已解決/不需執行 |

### 第二階段 · HRP 分群本身的調整

| ID | 任務 | 決策內容 | 狀態 |
|---|---|---|---|
| H-03 | 群數改用客觀準則決定，取代寫死的 `L1_TARGET=8` | ☑ **2026-08-28 完成** — 新增 `hrp.silhouette_score`/`silhouette_scan`（不依賴sklearn，環境沒裝，手刻向量化版，用合成資料驗證方向正確：分得好的分群≈0.98、隨機打散≈-0.09）+ `research/cluster_count_selection.py`（對凍結linkage重算距離矩陣、k=3~20掃描，不重跑相關矩陣/PSD/linkage本身）。**實測結果跟老師舉例方向相反**：US(k=7)反而比TW(k=6)需要更多群；TW/US剛好都落在v9文件原定義的L1「5-10群、給LLM讀」範圍內。XM在k=3輪廓係數大幅領先（0.279 vs 次高0.230），研判是台美市場邊界本身主導了低k分群、非策略層級細緻分群，跟5-10範圍不合；使用者2026-08-28確認「照資料走」，XM就用k=3。`L1_TARGET`改為`{"TW":6,"US":7,"XM":3}` | ☑ |
| H-04 | 移除 L2 層級 | ☑ **2026-08-28 完成** — 拿掉 `stage3_hrp.py` 的 `L2_TARGET`/`cluster_L2`計算；`contracts.py`（CLUSTER_ASSIGN/CLUSTER_META/CO_FAIL_REGIMES/CLUSTER_STORY/STRATEGY_MAP，共5處schema的`level`允許值或`cluster_L2`欄位）；`stage4_strategy_map.py`的HRP投影；`ops/tools.py`（T3的`_PROFILE_COLUMNS`、T8的`cluster_coverage`迴圈）全部同步移除。`py_compile`全過，全域搜尋確認無殘留的`cluster_L2`/`L2_TARGET`程式碼引用（只留下說明性註解）| ☑ |
| H-03/H-04 下游重跑 | 六棵樹＋stage4＋output_a全部重跑並驗證 | ☑ **2026-08-28 完成** — 依序重跑 `stage3_hrp`（六棵樹全部重建，PSD/契約全過，耗時548s；TW L1實際群數6/US 7/XM 3皆與目標吻合；結構塌縮診斷改用新群數後三市場皆「未塌縮」，跟舊L1=8時代的結論不可比，是全新基準）→`stage4_strategy_map`（15,810列，契約通過，`passes_alpha_gate`/新cluster_L1成功接上）→`output_a`（131/216可評估、59.5%贏大盤，對照重跑前133/216、60.2%，合理小幅漂移）。<br>修掉2個因硬編碼L1=8而失敗的既有測試（`t_stage3_co_fail_regimes_real_data`改用`S3.L1_TARGET[m]`動態比對；`t_ops_t5_degrades_gracefully_without_cluster_story`的XM情境k=3→k=2，因XM現在只有3群、扣自己剩2群可比）。**全套件回到78/78通過**。<br>🔴 **重跑過程中額外抓到一個資料一致性風險**：`_frozen/stage3/cluster_story.parquet`（LLM點③，8/25用舊L1=8群編號跑的84對）群編號範圍到7/8，但新樹群編號只到6/7/3——`ops/tools.py`的`_lookup_story()`純用整數ID比對、無版本/雜湊檢查，若不處理會**靜默配對到錯誤的群**（查群2其實會拿到舊群2的解釋文字，但那已經是完全不同的一批策略）。已移到`_frozen/stage3/_stale_pre_H03/`隔離（保留備查、未刪除），並實測確認T5現在正確降級回傳`explanation=None`。<br>☑ **2026-08-30 隔離解除**：cluster_story已針對新群數（TW6/US7/XM3）完整重跑，見H-24條目。舊隔離資料仍保留在`_stale_pre_H03/`供查證，不影響現況 | ☑ |

### 第三階段 · 群的定量描述（老師點名的論文兩大重點之一）

| ID | 任務 | 決策內容 | 狀態 |
|---|---|---|---|
| H-06 | 群的定量特徵表 | ☑ **2026-08-28 完成** — 新增 `research/cluster_temporal_profile.py` + 3張契約表（`CLUSTER_ANNUAL_RETURNS`／`CLUSTER_QUARTERLY_RETURNS`／`CLUSTER_PROFILE_QUANT`，已註冊進`ALL_SCHEMAS`）。純程式算、無LLM。群代表序列＝成員報酬簡單平均（同`stage3_hrp._cluster_meta_and_corr`口徑），逐年/逐季**複利**（非簡單加總，已用合成資料鎖住方向＋跨年邊界不滿12個月的月數計算）。只做L1、只做normal樹（crisis樹17-26個月不到2年，做不出年度型態分析）。<br>**實測發現**：TW6群/US7群/XM3群全部「最佳年=2009、最差年=2008」（GFC後反彈/GFC崩盤，符合常識，沒有異常）；但`n_years_positive`（賺錢年數）跟`annual_ret_std`（年度波動）確實跨群有意義地不同（如TW群5高達16/19年賺錢、群1只有13/19），對應老師要的「某幾年賺賠不同」。<br>🔴 **`top1_factor_type`欄位查出全部群都是「混合型」，沒有區分度**——查證後確認是真實現象（15,810策略裡73%本來就是混合型，非bug），已在彙總表裡改看`top1_F1`/`top1_C_source`/`pct_v1`才有意義（例如TW群1的v1佔比99.67% vs 群4只有0.83%，差異巨大，呼應`cluster_story.py`早先發現的「分群高度受V與F1家族支配」）。<br>新增2項測試（合成資料複利方向+跨年邊界、真實資料三表契約+跨表一致性），全套件78/80通過（2個預期內失敗見下方cluster_story隔離說明，訊息已註明非回歸）| ☑ |
| H-07 | 群的視覺化 | ☑ **2026-08-28 完成** — 新增 `research/cluster_visualizations.py`，四張圖×三棵normal樹＝12張，存 `_analysis_outputs_hrp_clusters/{tree_id}/`。①群×年度報酬熱力圖（用H-06的cluster_annual_returns）②群累積報酬疊圖（即時重算月度代表序列複利畫線）③群間相關熱力圖（沿用stage3既有的cluster_corr_matrix）④群因子組成堆疊圖（F1前4大+其他）。<br>🔴 開發中抓到一個真實bug：④原本把`strategy_map`（自帶`cluster_L1`但只投影「策略自己市場」的常態樹）直接跟`cluster_assign`（依`tree_id`篩、XM樹群編號完全不同）合併，pandas因欄位撞名自動加`_x`/`_y`尾碼，導致`df["cluster_L1"]`直接KeyError；修法：合併前只取`strategy_map`的`F1_factor`欄位，不帶自己的`cluster_L1`。<br>目視確認12張圖全部正常渲染，內容跟已知結論吻合（US群1報酬遙遙領先、TW群5幾乎100%為P_IC因子、XM群1×2/3相關0.54-0.55明顯低於群2×3的0.94）| ☑ |
| H-08 | 單群身份的 LLM 解釋（新產出，非既有 cluster_story） | ☑ **2026-08-30 完成** — 新增 `research/cluster_identity.py`，跟`cluster_story.py`同一套架構（H-23額度煞車、--dry-run/--limit/--resume全都有）。輸入=H-06`cluster_profile_quant`（時間型態）+ `cluster_story._cluster_profiles`（橫斷面成分）+ `co_fail_regimes`（危機期關聯，僅供揭露）。**額外多一條H-08專屬鐵則**：禁止推論「這群適合什麼總經環境」——那是S-01/S-02決策層的工作，這裡沒給任何總經資訊，推論了就是編造，會污染後面決策層要用的素材。<br>16群（TW6/US7/XM3）全部完成，模型gpt-5（跟cluster_story同一顆，因為同樣是要進論文的解釋性任務）。品質檢查：抽驗3群（TW/US/XM各1）人工核對四鐵則全過，特別是「依avg_intra_corr高低調整同質性描述語氣、不是套模板」；全16群自動化掃描「升息/降息/通膨環境/景氣循環/適合在」等總經推論字眼，**0命中**，鐵則3有效。<br>今日累計使用標準池193,470/1,000,000 tokens(19.35%)，仍在免費額度內。<br>新增3項測試（mock契約、resume、真實資料含總經字眼自動掃描），**套件回到92/92全過，無任何已知失敗殘留**。<br>**輸出`cluster_identity.parquet`是S-01（群→總經介面）的直接素材來源** | ☑ |

### 第三.五階段 · LLM 成本監督（2026-08-29 新增，非老師意見，是使用者的營運需求）

| ID | 任務 | 決策內容 | 狀態 |
|---|---|---|---|
| H-23 | OpenAI 每日免費額度監督與暫停機制 | ☑ **2026-08-29 完成** — 使用者帳號有 data-sharing 每日免費額度，**分兩個獨立池**：標準池 1M tok/日（gpt-5.4/5.2/5.1/5、gpt-4.1、gpt-4o、o1、o3）／小模型池 10M tok/日（上述的 mini/nano + o3-mini/o4-mini）。方針：**盡量用免費的，超過觸發暫停再決定要不要花錢**。<br>`utils/openai_quota.py` 新增：①`classify_model()` 模型分池（mini優先於standard，否則`gpt-5-mini`會被誤歸標準池用錯上限）②`log_usage()` 持久化帳本 `code/_catalog/llm_usage.jsonl`（時間/模型/池/用途/tokens，純統計無金鑰可進版控，論文交代成本可直接引用）③`today_totals()` 當日分池彙總 ④`check_free_tier_budget()` **呼叫前**煞車（預留5%餘裕）⑤`--usage` CLI 看今日長條圖+歷史累計。<br>已接進兩個真正呼叫LLM的地方：`cluster_story._call_llm()`、`ops/tools.py` T10。cluster_story 額外做了**部分保存**——原本跑完84對才寫檔、中途爆掉全白跑，現在額度不足會保留已完成部分並在側錄標記`complete:false`（避免下次誤以為跑完）。<br>🔴 **開發中抓到一個會靜默燒錢的bug**：`"gpt-5.6-terra".startswith("gpt-5")` 是True，純前綴比對會把**任何未來版本**（5.6/5.9…）誤判成免費的gpt-5，拿付費額度跑還以為在用免費的。改用`_matches_family()`（前綴後不可接數字或小數點），已加回歸測試。<br>新增測試：selftest T7/T8/T9（分池/帳本跨日/預算煞車）+ `t_ops_t10_free_tier_gate_blocks_paid_model`（驗證付費模型必須在`requests.post`被呼叫**之前**擋下，否則錢已經花了）。修正2個既有T10測試（原用`fake-model`會被新煞車擋住、且會把假用量寫進真實帳本，改用免費名單模型+mock掉log_usage）。<br>🔴 **2026-08-29 code review 抓到並修復**：暫停時印的訊息承諾「額度恢復後可用 `--resume` 從中斷處接續」，但`--resume`參數當時根本不存在——真的暫停後照這句話做只會得到argparse報錯，重跑也只能整批84對從頭來。已補上真正的`--resume`：`build()`讀既有`cluster_story.parquet`、以`(tree_id,cluster_a,cluster_b)`為key跳過已完成的pair，`--limit`在resume情境下改指「本次最多再打幾對新的」；`run()`的側錄新增`n_new_this_run`/`resumed`欄位，`model`欄位改記所有出現過的模型（不再只取第一列，避免resume跨次換模型時漏記）。新增`resume_path`參數讓測試能指向temp檔、不必碰production路徑。新增2項測試（`t_cluster_story_resume_skips_completed_pairs`用主鍵唯一性驗證不會重打已完成的pair、`t_cluster_story_resume_missing_file_falls_back_to_fresh`驗證檔案不存在時優雅退回全新執行）。全套件 81/83 通過（2個失敗仍是已知的cluster_story隔離，非回歸）| ☑ |
| **H-24** | 模型選擇 | ☑ **2026-08-30 完成，全部收尾動作都做完** — 使用者定案改用免費名單內的模型：`t10_model=gpt-5-mini`（轉譯任務，用便宜款）、`cluster_story_model=gpt-5`（v9核心賣點的因果推理任務，用完整版），沿用原本terra/sol「便宜款/強款」分工邏輯，`config.ini`已改（gitignored，不進版控）。<br>**四個收尾動作全部完成**：①config.ini改好②`--dry-run`確認prompt無誤，不花錢③**3~5對小樣本實測抗幻覺**：用XM_normal全部3對（含1對關鍵的「低互補」）測gpt-5，四道鐵則逐條核對全過，尤其「低互補必須明講不會帶來分散效果」這條，模型輸出幾乎逐字符合系統prompt要求④**確認OK後跑cluster_story全量**：`--resume`接續剩下36對（TW15+US21），**39對全部完成**（H-03改群數後總對數從84降到39），自動化掃描31對「低」互補的complement_note，**100%都有明確揭露「不會帶來分散」**，無一遺漏。<br>意外發現：gpt-5回覆長度比舊模型sol多92%（每對~3567 tok vs 1856 tok），`EST_TOKENS_PER_CALL`煞車估計值已同步更新（2000→4000）。今日標準池用量137,381/1,000,000 tokens(13.74%)，成本完全在免費額度內。<br>**測試套件回到89/89全過**（`t_cluster_story_report_runs`/`t_cluster_story_sidecar_records_real_model`這兩個原本因缺資料而失敗的測試也轉綠，無殘留隔離）。cluster_story的舊隔離資料`_stale_pre_H03/`同步解除標記，見H-04條目 | ☑ |

### 第四階段 · 免費午餐的量化與驗證（核心）

| ID | 任務 | 決策內容 | 狀態 |
|---|---|---|---|
| H-09 | 有效獨立賭注數 | ☑ **2026-08-29 完成** — 新增 `hrp.effective_number_of_bets()`（Meucci 2009/López de Prado 2016 的PCA熵版ENB，對相關矩陣特徵值分布取熵、取exp；合成資料驗證方向：corr=I時ENB=N、全部完美相關時ENB=1、且隨平均相關單調遞減）+ `research/effective_bets.py`。<br>**實測結果（三棵normal樹，重建完整N×N相關矩陣後算特徵分解，XM約191秒）**：<br>TW N=6,679 → ENB=**3.27**（平均每個真正獨立的訊號背後藏了2,046個高度重複的策略）；US N=8,360 → ENB=**4.36**（藏1,916個）；XM N=15,040 → ENB=**5.77**（藏2,605個）。<br>🔴 **意外但誠實的發現**：HRP現有分群數（TW k=6／US k=7）**超過**了各自的ENB理論上限（k/ENB=183.8%／160.4%）——代表H-03用輪廓係數選出的群數，比「理論上這個宇宙真正有的獨立訊號數」還多；群代表彼此的獨立性（ENB(k個群代表)/k）只有32.7%(TW)／21.3%(US)／59.7%(XM)，代表現有k個群即使已經分開，彼此仍有相當殘餘冗餘，不是k個互相正交的賭注。這跟「多樣性假象」的論文主軸完全吻合，是很有力的量化證據，但也代表**不能簡單宣稱「HRP找到了k個獨立群」**，論文措辭要精確。契約已註冊`EFFECTIVE_BETS`，測試81→84（含真實資料邊界檢查：ENB不可能超過N、群代表ENB不可能超過k）| ☑ |
| H-10 | 群內代表策略挑選規則 | ☑ **2026-08-29 完成** — 新增 `research/cluster_representatives.py`：品質分數＝Calmar比率（CAGR/｜MDD｜，對應老師原話「績效去除以MDD」）＋貪婪多樣性選擇（依品質高到低納入，但新代表對已選代表的相關係數不可超過**該群自己的avg_intra_corr**，門檻用群自己的水準而非武斷全域常數；門檻太嚴時退而求其次用品質單獨補位，沿用`output_a._pick_diversified`同樣的backfill設計）。`co_fail_regimes`附在輸出表當警示欄位，不做篩選（H-15）。<br>`m`（每群代表數）是自由參數，先跑m=3與m=5兩組驗證穩健性：**m=3時16群有14群、m=5時16群全部**，多樣性選擇的代表彼此平均相關都低於純品質前m名（m=3平均降幅+0.113、m=5平均降幅+0.120），**證實了老師講的陷阱是真實存在的**——例如TW群5（75成員）純品質選法彼此相關高達0.95（幾乎是同一策略的複製品），多樣性選擇後降到0.83；US群1純品質0.96降到0.66。0/16（m=3）、1/16（m=5）群動用backfill，代表大部分群的多樣性門檻都能被滿足，不是空談。<br>契約已註冊`CLUSTER_REPRESENTATIVES`，新增真實資料測試驗證「多樣性選擇不可能比naive選法更集中」這條不變量。<br>⚠️ **`m`本身的正式數字尚未拍板**——這裡示範的3/5是說明用，真正的資金配置數字要等H-12的四組對照實驗才有依據 | ☑ |
| H-11 | in-sample／out-of-sample 切分點（HRP分群結構層） | ☑ **2026-08-29 定案（v2，取代原提案）** — **老師的關鍵指引：切在投組配置層，不是策略參數層**——策略池（F×C×V定義）完全不動。<br>🔴 **原提案已撤銷**：原本用「OOS要涵蓋COVID崩盤/2022熊市」當選窗標準，這是**先看歷史再回頭挑窗口**，等於用上帝視角選OOS，跟H-17「論文不可以假設有上帝在」的原則自相矛盾，使用者2026-08-29點出台美股切不同年份「這樣公平嗎」，查證後確認原提案理由站不住腳。<br>**v2定案（使用者定案）：兩市場OOS用同一段絕對日曆時間，不管各自窗口多長**——TW/XM共同窗2007-2025 → IS 2007-01~2018-12(12年/144月，63.2%)／OOS **2019-01~2025-12**(7年/84月，36.8%)；US共同窗2002-2025 → IS 2002-01~2018-12(17年/204月，70.8%)／OOS **2019-01~2025-12**(7年/84月，29.2%)。兩市場IS:OOS比例不同是窗口長度天生不同的自然結果，不是選擇性挑出來的；OOS恰好仍涵蓋2022熊市（但這是結果不是選窗標準）。做法不變：只用IS月份跑相關矩陣/分群/權重(全部凍結)，OOS月份只驗證不重新分群。**只有normal樹做IS/OOS，crisis樹樣本太小(17-26月)不切**（見H-16）。<br>**實作完成（2026-08-29）**：新增 `research/stage3_hrp_isoos.py`，**獨立於`stage3_hrp.py`主線**——新增 `paths.STAGE3_ISOOS`（`_frozen/stage3_isoos/`）獨立目錄、新增3張獨立schema（`CLUSTER_ASSIGN_ISOOS`/`CLUSTER_META_ISOOS`/`ISOOS_CORR_COMPARISON`，tree_id用`_IS`後綴命名空間跟主線`TREE_IDS`區隔），完全不寫入`_frozen/stage3/`、不共用檔名/MANIFEST。<br>流程：①只用IS窗月份跑3棵normal樹完整HRP（策略宇宙跟主線六棵樹相同，只是月份範圍改變）②群定義在此凍結③用同一批群、改算OOS窗(2019-01~2025-12)月份的群代表序列相關④比較IS/OOS的相關係數與互補程度判定是否一致。<br>已用真實資料跑完（279秒），並用**檔案系統證據**驗證兩邊真正獨立（測試檢查主線`_frozen/stage3/`的mtime/內容完全未變、MANIFEST不含任何`_IS`痕跡），滿足使用者「資料要分好，不要搞混」的要求。測試88題全數新增後86/88通過（2個已知cluster_story隔離非回歸）。<br>🔴 **實測結果（重要，見下方彙總）**：TW/US的群間互補程度判定OOS大幅比IS不穩定，XM(跨市場)穩定——這是直接回答H-13問題的第一手數據，詳見對話紀錄 | ☑ **拍板＋實作完成** |
| H-12 | 四組對照實驗 | ☑ **2026-08-30 完成** — 新增 `research/four_group_control.py`。四組挑選依據**只用IS窗資訊**：A_hrp沿用H-10規則(m=5/群)；D_top_cagr純IS期間CAGR前N名(N=k群×5)；B_all=全宇宙；C_random=200次獨立抽樣(seed=42)取均值±標準差。四組portfolio皆等權(S-04)，IS/OOS各自評估CAGR/MDD/Sharpe/ENB。真實資料6秒跑完，契約通過，測試87/89（2個已知cluster_story隔離非回歸）。<br>🔴 **結果分兩層，不能只看老師的假設有沒有成立**：<br>①**核心問題（30多支vs狂灑）結論清楚且一致**：A_hrp（30/35/15檔）在三個市場OOS CAGR都明顯贏過B_all/C_random（TW 19.6%vs17.6%／US 32.5%vs25.3%／XM 28.3%vs22.4%），回答「不一樣，精選30多支確實比狂灑好」。<br>②**次要假設（純CAGR選股OOS會雪崩）只在TW成立，US/XM是相反方向**：TW的D_top_cagr確實衰退最多(IS→OOS掉10.7pp，比A_hrp的6.6pp更多，符合過擬合假設)；但**US跟XM的D_top_cagr在OOS反而比IS更好**(US -3.5pp即OOS更高、XM -4.2pp)，且**D組OOS CAGR大幅超越A_hrp**(US 53.2%vs32.5%、XM 51.8%vs28.3%)。<br>🔴 **2026-08-30 查明原因並修復診斷缺口**：查了D組實際選出的策略，發現**不是分散的贏家，是集中賭注**——XM的D組15檔**全部100%集中在單一HRP群**、US的D組35檔54%集中在單一群。這代表US/XM「D組OOS更好」的表面現象，其實是「剛好押中一個在2019-2025表現超好的因子/群」，是**單一次歷史實現的運氣**，不是「純挑CAGR是穩健策略」的證據——這其實反過來印證了H-10當初的警告，只是這次賭對了。<br>**新增E_top_calmar組**（純Calmar前N名、不設多樣性限制）把「品質指標(CAGR vs Calmar)」和「多樣性限制」兩個因素拆開：D→E(只換指標)、E→A(只加多樣性限制)。**關鍵證據：XM的E組(純Calmar)一樣100%集中在同一群**——代表問題根源是「沒有多樣性限制」本身，跟用CAGR還是Calmar選標的無關；US則兩個因素都有貢獻(D→E貢獻-15.1pp、E→A貢獻-5.6pp)。<br>**新增`n_clusters_covered`/`max_cluster_share`兩個集中度診斷欄位**（永久寫進schema跟輸出），以後不需要再手動debug就能直接看出任何一組是不是集中賭注。<br>**這是需要誠實揭露的結果，不是失敗**：核心結論（精選30多支贏過狂灑）在三市場都成立且穩健；「歷史贏家過擬合」只在TW成立，US/XM的反例已經定位到根因是集中度而非市場本身的因子持續力，但單一段OOS(84個月)得出的結論仍需谨慎，不宜直接寫成強因果論述。新增測試鎖住A組多樣性覆蓋率>=2群、集中度診斷欄位邊界正確，套件87/89通過（2個已知cluster_story隔離非回歸）。 | ☑ |
| **H-25** | 互補性的分群粒度效應（2026-09-01 新增） | ☑ **2026-09-01 完成** — 使用者提問：「為什麼這次分群沒有任何高互補，是策略真的沒有，還是改了分群方法後判定門檻也要跟著改？」查證後兩者皆非，**是分群粒度的聚合效應**。新增 `research/complementarity_granularity.py` + `contracts.COMPLEMENTARITY_GRANULARITY` schema。<br>**證據鏈三層**：①舊k=8的5對高互補**全部來自同一個群**，而那個群是305檔/全台股/99.7%用v1的小群（＝現行TW_normal群1，CAGR中位0.1659、MDD−0.5911、小型股0.285、群內相關0.794，數字完全對得上）；k=3後這305檔被併進6,679檔的全台股群。②直接實測同一批資料同一段月份：XM群1(6,679檔)對美股群2/3是0.484/0.502，但TW細群1(305檔)是**0.392/0.408**、TW細群5(75檔)是**0.325/0.353**——高互補的關係還在資料裡。群代表＝成員簡單平均，平均愈多檔特異成分抵消愈徹底，代表序列愈趨近該市場大盤。③粒度降到L3（XM 453群、成員中位23檔）：**跨市場49,496對有80.5%是高互補、同市場52,882對只有1.1%**。<br>🔴 **排除「小群雜訊」替代解釋**：同市場配對是天然對照組（群大小/月份數同量級）。再加做min_members>=20的穩健性掃描：跨市場仍有**62.6%**高互補、同市場只有**0.3%**、**美股內部L3 12,720對高互補0對**。<br>**定案：`COMPLEMENTARITY_CUTS` 維持 {高:0.5, 中:0.8} 不改**——門檻在L3層級把跨市場/同市場切得極乾淨(80.5% vs 1.1%)，證明門檻是對的；若為了讓L1出現高互補而放寬到0.55，同市場的TW群2×群5(0.539)也會被判成高互補，反而抹掉「分散來源是市場邊界」的核心結論，那是為了結論好看而調參數。<br>**論文敘事要分兩層**：免費午餐存在於「小而特化的群×另一個市場」，不在「整個台股×整個美股」——直接接上H-10代表策略挑選的實務意義。新增1項測試（鎖住粒度效應存在、同市場對照組有效、min>=20仍成立、門檻未被改動），套件 **106/106** 全過 | ☑ |
| H-13 | 互補性穩定度 KPI | ☑ **2026-08-30 查證定案：已由 H-11 的實作完整滿足，不需另外開發** — 老師指定的KPI原話：「in sample都很低，可是out sample會不會還是很低」，要求的三個步驟（IS期間算一次群間相關(分群當下)／OOS期間用同一批群的定義重算一次／比較兩者是否同樣維持低相關）**逐項核對，`stage3_hrp_isoos.py`跟`isoos_corr_comparison.parquet`三項都已產出**：`corr_is`直接來自IS建樹當下的`cluster_meta_and_corr`（分群當下算的）、`corr_oos`是同一批凍結成員在OOS月份重算、`complementarity_stable`欄位就是「兩者是否同樣維持低相關」的逐對判定，彙總結果（TW 53.3%／US 81.0%／XM 100%一致率）已寫進`H09_H10_H11_免費午餐量化與驗證.md` §3.6。H-13與H-11不是兩件平行的工作，是**同一份產出的兩個編號**，H-13到此定案，不需要另外的程式或跑法 | ☑ |

### 第五階段 · 危機分析降級（已定案，工作量小，可插隊先做）

| ID | 任務 | 決策內容 | 狀態 |
|---|---|---|---|
| H-14 | 危機樹保留但降級，不投入IS/OOS | ☑ **2026-08-27 完成** — `stage3_hrp.py` docstring 補上明確決策記錄：crisis樹（17-26個月）繼續保留、`co_fail_regimes`繼續產出，用途限定論文描述性揭露；**刻意不做IS/OOS**（樣本量已經不足以支撐一次穩健估計，切了兩段都不可靠）；明確註記這條規則不適用於未來的H-11（normal樹IS/OOS）| ☑ |
| H-15 | T1 危機格的 method 改回 filter | ☑ **2026-08-27 完成** — `ops/tools.py` 三處（保守/積極/全天候×危機）`method` 從 `"cluster_diversify"` 改成預設 `"filter"`，維持既有更嚴格門檻（MDD最嚴／credibility高／regime_fit危機抗跌，這些門檻算自`strategy_map`欄位不吃crisis樹，不受降級影響）；全天候×危機原本完全靠cluster_diversify、無其他條件，改為沿用base條件（stability_grade高原+跨regime白名單）。**沒改stage2a的危機標籤本身**（跌幅≥30%是客觀價格事實，跟拿它做決策是兩件事）。`output_a.py` 模組docstring同步更新反映新路徑；`_pick_cluster_diversified_crisis()` 函式與`K_CRISIS_GROUPS`常數保留但註明非預設路徑，供之後想對照「crisis樹選法 vs 一般選法」時用。已用`py_compile`確認兩個檔案語法正確 | ☑ |
| H-16 | D-6：危機期免費午餐消失的誠實呈現 | ☑ **2026-08-27 查證完成，已滿足不需新增** — 檢查既有 `complementarity_sensitivity.py` 的輸出文字，發現本來就是「觀察性發現＋明確樣本量說明＋不宣稱強因果」的寫法（例：「可能是危機窗僅17~26個月的估計雜訊，也可能是真實的危機期反同步」「需要另外校準，不能沿用同一套數字」），跟H-16要求的框架已經一致，不需另外產生新文件 | ☑ |

### 第六階段 · 總經／動態窗格（🆕 2026-08-29 升格：不再是「不急的保留項」，是論文第四章主體）

> **狀態變更**：原本標「老師明確表示不急，先保留」，是因為當時 H-08（群的身份）還沒做，
> 總經無從預測起。**2026-08-29 故事線定案走方向C（見第10節）之後，這一階段升格為論文應用章的主體**——
> 「總經狀態 → 該用哪一群」正是方向C 的核心。H-17~H-21 的「⏸ 暫緩」改為「☐ 待做，排在 H-08 之後」，
> 順序邏輯不變（仍要等 H-08 定義出群的身份），但**優先級不再是最低**。
> H-22（HRL名詞）維持暫緩，那是名詞確認不是開發。

| ID | 任務 | 決策內容 | 狀態 |
|---|---|---|---|
| H-17 | 上帝視角基準化 | 🔄 **2026-09-01 重新拆解為三條獨立軸線**（使用者2026-08-31指出：regime跟總經是兩個不同的東西，不能合併成一個上帝視角，兩個都要算、都要留著當參考——原本的分析把兩者混為一談，已修正）。詳細討論見對話紀錄，摘要：<br>①**軸線一·Regime（stage2a牛熊切割，價格驅動）**——使用者2026-09-01定案：「牛熊盤整切法原本就正確」，**只有「危機」這個標籤是上帝視角的範疇**（要等跌幅完整走完才能確認）。☑ **2026-09-01 查證完成，確認已由H-14/H-15/H-16滿足，不需新開發**：①確認`stage3_hrp._load_crisis_months()`直接讀`regime_table_{market}.parquet`的`label=="危機"`，是同一個zigzag事後回推的標籤，跟H-14-16處理的是同一件事②全域搜尋確認危機樹/危機標籤目前**沒有任何存活的決策驅動用途**——`ops/tools.py`的T1三處危機格method都已是H-15改的"filter"，`output_a._pick_cluster_diversified_crisis()`雖然程式碼還在，但呼叫路徑`method=="cluster_diversify"`在現行預設設定下不會被觸發（保留給日後想做對照實驗用，非現行邏輯的一部分）③`ops/tools.py`的T6查詢工具仍會回報危機窗相關係數，但只是**描述性回傳**（附confidence等級），不驅動任何選擇邏輯，跟H-17要求的「保留當參考、不當決策依據」完全一致。測試套件103/103確認無影響。<br>②**軸線二·總經（stage2b clock_cell，指標驅動）**——開發時查證發現一個先前沒被點出的真實方法論缺口：`stage2b_macro.py`的z-score平均/標準差跟四格邊界是**拿2000-2025全樣本算一次凍結**的，程式註解自己寫「防look-ahead」但只防住IS/OOS參數不一致，沒防住「早期月份的z-score偷看了後期資料」這件事。**使用者2026-09-01定案：改用5年滾動窗**（每月z-score只用過去5年資料算，不用全樣本）。<br>☑ **2026-09-01 完成** — 新增 `research/macro_rolling_window.py` + `contracts.MACRO_CLOCK_COMPARISON` schema。滾動窗版本獨立存放於`_frozen/stage2/macro_rolling/`，不覆寫主線`macro_history_{market}.parquet`。嚴格要求滿60個月才分類（`min_periods=60`），前59個月無法分類（312個月中194個月可比較）。<br>🔴🔴 **實測結果，比預期嚴重很多**：TW **47.9%**（93/194月）、US **51.5%**（100/194月）的可比較月份，clock_cell分類在凍結版跟滾動窗版之間**不一樣**——不是少數邊界情況，是**接近一半**。開發時用合成資料先驗證滾動窗z-score真的沒有偷看未來（前後段統計性質劇烈不同的合成序列，前段z-score不受後段影響），排除是程式邏輯錯誤；真實資料的高差異率反映的是clock_cell分類本身對標準化基準相當敏感。<br>**下游影響（尚待使用者決定，見第10.5節新增項）**：`macro_performance.parquet`（階段4）、`cluster_macro_conditional.parquet`（S-02）、`macro_state_snapshot()`（S-02/S-05/S-07 全部呼叫過）目前都是建立在**凍結版**clock_cell之上——若要採用滾動窗版當唯一正式版本，S-02起已經做完、跑過真實LLM的東西全部要重建。測試套件105/105通過（含1項合成資料no-lookahead驗證、1項真實資料比對邏輯驗證）| ☑ 完成，**但下游影響需要決定，見10.5節** |<br>③**軸線三·決策品質（事後諸葛選群 vs 我們的方法）**——見H-18條目 | 🔄 ①**☑已驗證，滿足**②待開發③見H-18 |
| H-18 | 無未來資訊對照組 | 🔄 **2026-09-01 使用者定案：②③都先跑跑看、先做做看，不再等老師確認**（原本③想先跟老師討論，已改為直接動工）：<br>①軸線一：不需新開發（若H-17①確認由H-14-16滿足）<br>②軸線二：5年滾動窗版本 vs 現有全樣本凍結版本，量化clock_cell分類差多少——**待開發**<br>③軸線三：把S-05的A_rule/B_llm改成真正walk-forward⋯——❌ **2026-09-02 取消**：這是為「總經→選群」的決策品質服務的，方向C已終止。<br>🔄 **但 walk-forward 這件事本身被老師升格為畢業必考題**，改成「五組對照 × anchored 多窗次」，不涉及 LLM 決策層，見 **H-26（第11.2節）** | ②☑完成（保留為方法論貢獻）<br>③❌取消→轉 H-26 |
| H-19 | 時間顆粒度實驗 | ☑ **2026-09-01 使用者定案：季度、半年兩個都測**（採納Claude建議，當成H-21多維度矩陣的一部分）。<br>2026-08-31 查證：實測`clock_cell`切換頻率——TW/US逐月平均只維持4-5個月；**意外發現半年取樣並沒有比季度取樣穩定**（TW季度36%換手→半年49%換手，US季度56%→半年59%，皆不減反增，因regime自然存活時間比半年還短），這個發現本身可以在論文提一句，說明顆粒度是先看資料才選的、不是憑感覺。<br>H-18③（軸線三）的walk-forward迴圈開發時要同時支援季度跟半年兩種頻率。<br>🔄 **2026-09-02 用途改變**：原本服務「總經→選群」的決策頻率（已終止），現改為**服務 H-26 anchored walk-forward 的窗口移動設計**——老師指定的窗次是 2~3 年一移（2015/2018/2021），比季/半年粗很多，季/半年的顆粒度實驗在新路線下不是必要項 | ☑ 拍板，🔄 用途改變見第11節 |
| H-20 | 總經因子可泛化性 | ❌ **2026-09-02 老師裁示取消**——方向C整條終止（「不會有答案，不會有好答案」），見第11節 | ❌ 取消 |
| H-21 | 多維度實驗空間彙整 | 🔄 **2026-09-02 維度改變**。老師原話仍成立（「幾個維度各切割成幾個空間，每一個空間去看 in/out sample 表現，這樣就是你的論文」），但**維度從「時間顆粒度 × 總經因子」換成「walk-forward 窗次（H-26）× 精選比例（H-27）」**——總經那個維度已取消。見第11.2節 | 🔄 改由 H-26×H-27 構成 |
| H-22 | 「HRL」名詞澄清（開放問題，非開發項目） | 使用者判斷：老師從頭到尾講的是HRL、可能是記錯名詞（非ASR誤聽），用途應該在後段決策階段（agent/總經選群那塊）。**先保留，等進到H-17~H-21階段再處理**；建議下次開會直接跟老師確認 | ⏸ 暫緩 |

### 依賴關係

```
✅ H-01 → H-02（判定不需執行）→ H-03 → H-06 → H-07 → H-08 ✅ → S-01 ✅ → S-02 ✅ → S-05 ✅ → S-07 ✅
                                                                                  → H-17~H-21（部分解鎖，見下）
✅ H-09 → H-10 → H-11 → H-12 ✅
              → H-13（✅ 已由H-11實作滿足）
H-14/H-15/H-16 不依賴以上任何項目，已完成
H-22（HRL名詞）留待進到H-17~H-21階段再處理
S-06（Prompt風格）⏸ 暫緩，理由見S-06條目——學長的成長/價值分類套不上我們統計分出來
    的HRP群（73%本身就是混合型），且現階段無回測手段可評估優劣。
```

**2026-08-31 現況**：H 系列（H-01~H-24）與 S-01/S-02/S-04/S-05/S-07 全部完成。
**S-06 已定案暫緩、不照學長原案做**（理由見S-06條目：候選群是統計分群非投資哲學
分群，成長/價值標籤套不上；且現階段沒有回測手段可評估哪個風格比較好）。**S-07的
關鍵發現**：三個測試情境裡A_rule（純規則基準）100%都被LLM決策包含，代表LLM決策
穩定錨定在客觀條件式績效排名上，不穩定的只是要不要多加邊際群——這是方向C可信度
與方向D抗幻覺的正面實證。**S 系列（S-01~S-08，S-06暫緩除外）全部完成**，方法論
文件見 `S08_抗幻覺架構方法論.md`。
**H-17~H-21（總經→選群本體）現在只是「部分」解鎖**：S-05 明確保留了範圍邊界——
`decision_layer_arms.py` 的三個決策函式只做了靜態單一快照比較，沒有做無前視偏誤
的歷史回測，因為那正是 H-17（上帝視角基準化）／H-18（無未來資訊對照組）要處理
的、目前仍未拍板的問題，決策頻率也要等 H-19（時間顆粒度）拍板。H-17~H-21要真正
動工，這三項還是得先跟老師/自己拍板。

---

## 10. 論文故事線定案（2026-08-29）與 S 系列開發項

> **背景**：2026-08-26 老師會議提出「量化＋解釋性兩大重點」「總經→選群」；同場老師點名參考
> 學長 **郭鎧菘《結合量化交易與多代理人決策之金融交易框架初探》**（指導教授許智誠，100頁，
> PDF 已加 `.gitignore` 不進版控，未公開發表不可散布）。Claude 讀完後提出 A/B/C/D 四個方向，
> **使用者 2026-08-29 決策：主軸走 C，A 先行打底，D 當輔助貢獻，B 不做**。

### 10.1 四個方向與取捨結論

| 方向 | 內容 | 決策 |
|---|---|---|
| **A** | 純量化 + LLM 僅做解釋（HRP診斷多樣性假象 → 有效賭注數 → 免費午餐只在跨市場 → 選代表 vs 灑全部 → OOS驗證） | ✅ **先做，當地基**。量化部分已完成大半（H-01~H-07），風險最低，是方向C 的必要前置 |
| **B** | 複製學長路線（LLM 直接選標的） | ❌ **不做**。①學長已用實證做過「LLM選股有用」，重做貢獻度有限 ②**我們的 LLM 沒有資訊優勢**——學長的 LLM 讀得到新聞/財報等量化模型看不到的非結構化資訊，我們若讓 LLM 在群之間選，它看到的只有我們自己算給它的量化數字，等於把數字重讀一遍，加值有限且是幻覺溫床 |
| **C** | 總經 → 選群（接續學長論文的「未來研究」第3點：動態Prompt配置機制） | ✅ **主軸**。①老師 8-26 講的「2」（總經）＋用 agent 正是這塊 ②是學長明確列為未來工作的方向，接續性強、差異化明確 ③**總經環境是我們版本的「非結構化資訊」**，LLM 在這裡才有真正的推理空間（總經狀態 × 群特性的配對），不是重讀數字 |
| **D** | 方法論貢獻：抗幻覺架構（程式判決不可推翻 + schema 限制 + 實證稽核） | ✅ **輔助貢獻**，貫穿三、四章。跟學長完全差異化（他沒做抗幻覺的系統性驗證），呼應老師說的「方法論可泛化」。單獨撐不起一章，不獨立成章 |

### 10.2 定案章節骨架

```
第一章 · 問題    上萬個策略的多樣性假象
第二章 · 量化    HRP診斷 → 免費午餐只在跨市場 → 有效賭注數        ← 方向A（H-01~H-07/H-09 已完成大半）
第三章 · 解釋    群的身份是什麼（H-06/H-07/H-08）                 ← 方向A + D
第四章 · 應用    總經 → 選群 → 投組（權重用等權，見 S-04）         ← 方向C（H-17~H-21 + S系列）
第五章 · 驗證    IS/OOS 切在投組配置層 + 四組對照（H-11~H-13）     ← 老師指定
方法論貢獻       抗幻覺架構，貫穿三、四章                          ← 方向D（S-08）
```

### 10.3 從學長論文直接沿用的結論（省掉的工作，需在論文引用出處）

| 學長的實證結論 | 對我們的影響 |
|---|---|
| 🔴 「**多代理的主要貢獻較偏向標的篩選，而非資金權重配置**」「平均權重配置在多數情境下能取得相對穩健的績效，顯示當多代理完成標的篩選後，簡單配置反而能降低模型權重判斷誤差」 | **直接回答老師問的「投組配置」**。老師原話問「這每一群裡面我到底要用績效最好的咧？還是績效去除以MVD咧？」——答案是**選誰才是重點，權重用等權**。H-10 範圍因此收斂為「只解決選代表」，權重不再自己另做一輪驗證（見 S-04） |
| Prompt 風格與候選池特徵不匹配會嚴重反效果（價值型 prompt 用在成長導向池，年化 **-13.85%**，純量化是 +29.63%） | Prompt 風格必須當**實驗變數**處理，不能隨便寫一版就用（見 S-06） |
| Critic（批判者）消融實驗結果**不穩定**，「尚無法被視為穩定提升績效的關鍵模組」 | 我們若加批判者角色，不能預設它一定有用；且這是個誠實揭露負面結果的範例，值得比照 |
| 用 **LLM 訓練資料截止日** 決定驗證期起點（GPT-5／GPT-5-mini 為 2024-09-30），Validation 整段放在截止日之後以避免資料洩漏 | 我們的 H-11 目前只考慮「涵蓋危機」，**沒考慮 LLM 洩漏**。走方向C 後這個維度必須納入（見 S-03） |
| 他用的模型是 **GPT-5 與 GPT-5-mini** | 這兩個**都在我們的免費額度名單內**，對 H-24 模型選擇是正面訊號 |
| 三個研究限制：未納入產業龍頭比較／All-in 無資金水位控管／回測期間未涵蓋完整多空循環 | 第三點我們有優勢（TW 2007-2025、US 2002-2025，涵蓋 GFC＋COVID＋2022），可在論文中當差異點說明 |

### 10.4 S 系列 · 方向C／D 帶出的新開發項

> ❌ **2026-09-02：方向 C 已終止**（見第 11.1 節）。下表 S-01/S-02/S-05/S-07 的產物**全部保留**，
> 但**不再是論文第四章主體**，改為**第二階段 AI Agent（H-29/H-30）的基礎建設**：
> S-02 的資訊源設計、S-03 的「不給日期」、S-05 的三組對照、S-07 的重複執行穩定度、
> S-08 的抗幻覺三鐵則，在 Agent 每季選股的場景下可以直接沿用。
> **S-08（抗幻覺架構）仍是獨立的方法論貢獻，不受方向 C 終止影響。**

| ID | 任務 | 內容 | 狀態 |
|---|---|---|---|
| S-01 | 群→總經的介面定義 | ☑ **2026-08-31 完成** — 新增 `research/cluster_macro_interface.py` + `contracts.CLUSTER_MACRO_INTERFACE` schema，純程式組裝（H-06+H-08合併），無LLM。<br>🔴 **開發中發現的關鍵問題**：查了 `cluster_identity.py` 的prompt結構，四個LLM輸出欄位（identity_label/mechanism_note/performance_pattern/caveat）是**同一次呼叫、共用含真實年份的完整上下文**產生的——即使實測16群裡只有performance_pattern/caveat明確外洩年份、identity_label/mechanism_note沒有，**那也只是這次運氣好，不是架構保證**，跟本專案一路「不給LLM有機會誤用的資訊」的原則不符。<br>**定案做法**：介面表**只收identity_label**（風險最低的短句）；mechanism_note/performance_pattern/caveat**一律不收**，S-02若要更豐富的LLM文字脈絡須另外設計「輸入從頭就不含年份」的專屬prompt，不可重用cluster_identity既有輸出。其餘欄位來自`CLUSTER_PROFILE_QUANT`，**排除window_start_year/window_end_year/best_year/worst_year四個帶日曆年份的欄位**（對應報酬幅度best_year_ret/worst_year_ret保留，純數值無日曆錨點）。<br>16群全部組裝成功，0筆merge缺漏。新增測試兩道防線：①schema層級直接檢查四個排除欄位不存在②內容層級**用每一群自己真實的年份數字精確比對**identity_label有沒有洩漏（開發時發現用通用年份正則會把策略數量如1909/2047誤判成年份，改用精確比對避免假陽性）。套件回到94/94全過 | ☑ |
| S-02 | 決策層資訊源設計 | ☑ **2026-08-31 完成** — 新增 `research/macro_decision_input.py` + `contracts.CLUSTER_MACRO_CONDITIONAL` schema。<br>🔴 **核心決策（要不要附歷史績效）：只附「條件式」績效，不附「無條件」績效**——`cluster_macro_interface`（S-01）的CAGR_median等是「群整體好不好」的無條件統計，直接給決策層等於誘導「總是選CAGR最高的群」，是H-10/H-12已證實的陷阱在總經層的翻版（H-12實測：無多樣性限制的純品質排序在US/XM會塌縮成集中賭注）。新增`cluster_macro_conditional.parquet`——把階段4已算好的`macro_performance`（策略×投資時鐘四格）用H-06同一套「群代表=成員簡單平均」邏輯彙整到群層級，決策層看到的是「群X在『過熱』時條件式平均報酬多少」，不是「群X整體CAGR多少」。16群×4格＝64列，真實資料驗證：TW/US多數群在「衰退」格條件式報酬最高、「過熱」格接近0甚至為負，是有意義的regime差異，非隨機雜訊。<br>**總經狀態表述**：`macro_state_snapshot(market, month)`——只回傳z-score(growth/inflation/rate_level/rate_direction，用平滑版_s3)+clock_cell分類，**不回傳month本身**，`month`只是查表參數（供未來逐月walk-forward測試用），落實S-03。<br>**群特性給到多細**：`group_decision_context(tree_id, cluster_id)`——identity_label(H-08)+純結構欄位(市場/因子/估值濾網構成、群內平均相關，來自S-01)+四格條件式績效，明確排除S-01介面表裡的無條件績效欄位。<br>🔴 **開發中抓到並修復一個真實bug**：`group_decision_context`原本直接把pandas row的numpy純量(int64/float64)塞進payload，`json.dumps()`不加`default=str`會直接`TypeError`——這正是`cluster_story`/`cluster_identity`組prompt時用的標準呼叫方式，若不修，S-05/H-17~H-21真的組prompt時才會炸開。已修正為逐值轉原生Python型別。<br>新增3項測試（契約+64列數量、月份絕不出現在回傳值裡、無條件績效欄位絕不出現+json.dumps()可直接序列化的回歸鎖定），套件回到97/97全過 | ☑ |
| S-03 | OOS 切點納入 LLM 資料截止日 | ☑ **2026-08-29 使用者定案：走②**——不靠時間隔離（凱鬆能把驗證期切在模型訓練截止日之後，但我們2026年當下在跑的決策層，訓練資料範圍極可能已經涵蓋我們全部歷史資料窗2002~2025，切不出一段「模型沒讀過」的乾淨期間，①在我們這裡不可行）。改用**「讓LLM沒有機會用到記憶，即使它有記憶」**：餵給決策層的prompt**不能包含日期、不能包含任何能讓LLM認出「這是哪一段歷史」的線索**，只給匿名化的總經數值＋群的量化側寫（無日期、無策略/公司名稱）。這跟`cluster_story.py`/T10既有的抗幻覺三鐵則（只給程式判決+數字、不給額外資訊、結構化輸出）是同一種設計精神的延伸，多加一條「也不能給日期」。<br>**尚待開發**：S-02（決策層資訊源設計）要把這條規則具體落實到prompt結構裡——是S-02的硬性約束條件，不是獨立工作 | ☑ |
| S-04 | 權重配置定案為等權 | ☑ **2026-08-29 完成，且回頭套用到既有 `output_a.py`**——2026-08-29 code review 發現既有模組（H-15訂的，早於本決策）預設用HRP權重、只在共同月數不足時才退回等權，跟本決策矛盾。使用者確認回頭修：`build_one()` 改為`beats_market`一律以**等權**組合判定；HRP權重仍照算（T9），結果記進`portfolio_ret_hrp`/`portfolio_mdd_hrp`/`free_lunch_mdd_gain_hrp`/`beats_market_hrp`對照欄位，不影響任何篩選或判定，供論文寫「等權 vs 風險平價」的對照。已重跑：131/216可評估、**80/131(61.1%)贏大盤**（equal-weight判定，對照修改前的HRP-primary版本133/216/60.2%——兩次口徑不同不可直接比，這次equal起算已是新基準）；HRP對照組80/131格中有2格（US/全天候）equal與HRP判定不同，其餘一致。測試套件回到79/81（同既有2個cluster_story隔離，非回歸）| ☑ |
| S-05 | 決策層對照組設計 | ☑ **2026-08-31 完成（範圍：三組對照函式+靜態快照比較，非完整回測，見下）** — 新增 `research/decision_layer_arms.py`：**A_rule**（查`cluster_macro_conditional`該clock_cell下avg_ret_median最高的群，純規則無LLM）／**B_llm**（讀S-02的`macro_state_snapshot`+全部候選群`group_decision_context`，LLM選群，四道鐵則含「只能引用提供數字」「看不到日期不可假裝知道是哪一年」「優先看條件式績效」「差異不明顯要在caveat明講」）／**C_all**（全群等權，不看總經狀態）。<br>🔴 **範圍邊界（刻意不做，且說明原因）**：`cluster_macro_conditional`是全樣本算的，若拿它回測過去某月「該選哪群」等於用了未來資訊，這正是H-17/H-18（上帝視角基準化/無未來資訊對照組）要處理的、**目前仍未拍板**的問題；決策頻率也要等H-19拍板。故本模組**只做靜態單一快照比較**（給定當下總經狀態，看三組會選什麼），不假裝做了無前視偏誤的歷史回測——H-19/H-17/H-18拍板後可直接重用這三個`*_decision()`函式接上真正的walk-forward迴圈。<br>**品質驗證**：dry-run確認prompt無日期外洩，2組真實測試（XM_normal/復甦、US_normal/過熱）人工核對rationale/caveat——LLM正確做條件式比較排名、明確引用被排除群的具體數字說明原因、差距不大時誠實承認「帶有一定主觀權重偏好」，無幻覺、無捏造總經事件。今日用量19,167 tokens(1.92%)，成本可忽略。<br>新增4項測試（A_rule對帳真實資料驗證確為最高值、C_all回傳全群且穩定、B_llm mock契約+prompt日期外洩檢查、compare_snapshot dry-run結構完整性），套件回到101/101全過 | ☑ |
| S-06 | Prompt 風格當實驗變數 | ⏸ **2026-08-31 使用者定案：暫緩，不照學長原案做** — 學長測的是「用不同投資人格prompt，挑選本身已有明確風格傾向的個股」，配對錯了風格才會出現他那個「年化-13.85%」的反效果，這個實驗設計成立的前提是候選池本身能清楚分成成長/價值。**我們的群不滿足這個前提**：H-06已查出15,810個策略裡73%是「混合型」，HRP群是統計分群不是投資哲學分群，硬套成長/價值標籤是自己編劇本、不是資料本身的性質，測出來的結果沒有代表性。<br>**第二個理由（獨立於第一個，兩者都成立）**：就算有自然的變因可測，現在也沒有評估方法——S-05已保留範圍邊界（只做靜態快照，無回測），H-17/H-18/H-19（上帝視角/無未來資訊對照組/時間顆粒度）都還沒拍板，沒有「哪個prompt風格決策品質比較好」的驗證手段，多測幾種風格只是多幾份看不出優劣的結果。<br>**替代方案（若未來想恢復類似角度）**：不套用成長/價值這種外加標籤，改測我們資料裡真實存在的維度——例如「資訊呈現方式」（只給數字 vs 數字+要求詳細論述），且待H-17~H-19有進展、有辦法評估好壞後再做 | ⏸ 暫緩 |
| S-07 | 重複執行穩定度 | ☑ **2026-08-31 完成** — 新增 `research/decision_repeatability.py` + `contracts.DECISION_REPEATABILITY` schema。同一個(tree_id,market,month)快照重複呼叫`decision_layer_arms.llm_decision()` 5次（每次都是獨立真實API呼叫，非快取重播），量化：完全一致比例(眾數佔比)、平均pairwise Jaccard相似度、穩定核心(每次都選中的群)、不穩定邊緣(部分次數選中)、規則基準被LLM包含的比例。<br>三個情境涵蓋三棵樹×三種clock_cell（復甦/過熱/停滯性通膨）：<br>①XM_normal/TW/復甦：**100%完全一致**，5次全部選[1,3]<br>②US_normal/US/過熱：80%一致，穩定核心[1,7]、邊緣是群2（第5次額外選了它）<br>③TW_normal/TW/停滯性通膨：60%一致，穩定核心[5,6]、邊緣是群4（3/5次額外選了它）<br>🔴 **關鍵發現：三個情境的`rule_in_llm_rate`全部是100%**——A_rule（純規則基準的最佳群）在全部15次LLM呼叫裡沒有一次被漏掉，代表LLM決策**穩定錨定在客觀條件式績效排名上**，不穩定的只是要不要多加一個邊際群，不是整個決策亂跳。這是支持方向C可信度、也是方向D抗幻覺的正面實證。<br>🔴 **開發中抓到並修復2個真實bug**：①`unstable_fringe`欄位在完全穩定的情境下是空字串，CSV往返讀寫後變NaN，觸發schema的nullable檢查失敗——跟H-10的`co_fail_peers`是同一類問題，已加`nullable=True`修正②`stable_core`/`unstable_fringe`欄位若剛好每列都是空值或單一數字（如"4"、"2"），pandas的CSV型別推斷會把整欄誤判成float64而非字串——已在讀取處明確指定`dtype=str`修正，並在生產模組寫入處加註解提醒未來重讀這份CSV的人。<br>今日累計標準池用量90,587/1,000,000 tokens(9.06%)，成本可忽略。新增2項測試（mock手算五項統計量、真實資料契約+內部一致性交叉驗證），套件回到103/103全過 | ☑ |
| S-08 | 抗幻覺架構論文化 | ☑ **2026-08-31 完成** — 產出 `_analysis_outputs_robustness/S08_抗幻覺架構方法論.md`。整理型文件，無新開發。<br>核心內容：①三道鐵則（程式判決不可推翻/結構化輸出/只能引用提供數字）+ 一個總原則「架構排除、不靠信任」（貫穿S-01/S-02/S-03的判斷準則）②四個LLM節點盤點：T10（⚠️架構已備但0筆真實呼叫，尚未上線）、cluster_story（39對，31對低互補100%明確揭露）、cluster_identity（16群，總經推論字眼0命中）、decision_layer_arms（S-07實測rule_in_llm_rate 100%，決策穩定錨定客觀數字）③附帶列出開發過程中測試方法論抓到的4個真實bug當工程紀律佐證④**誠實列出已知限制**：T10無真實稽核、稽核靠自動化關鍵字掃描+抽樣人工核對非逐字全文審閱、決策層只測3情境15次、S-06暫緩未測風格穩健性、模型換過一次現有稽核只涵蓋gpt-5。<br>用當下最新資料重跑稽核數字（39對，取代舊的84對群數改變前的過期數字） | ☑ |

### 10.5 🔴 尚未決定的細節（拍板前不能往下做的）

| 項目 | 卡在哪 | 關聯 |
|---|---|---|
| ~~IS/OOS 切點~~ | ✅ 2026-08-29 拍板＋**實作完成**（`stage3_hrp_isoos.py`，見H-11）。TW/US群間相關IS/OOS判定不穩定、XM穩定——這個結果本身要不要另外拉出來當H-13的正式產出，還是留在H-11底下，可以再討論 | H-11 |
| ~~LLM 模型~~ | ✅ 2026-08-29 拍板：T10用`gpt-5-mini`、cluster_story用`gpt-5`，`config.ini`已改。**尚待做**：`--dry-run`確認prompt＋3~5對小樣本實測抗幻覺，確認OK才能跑cluster_story全量 | H-24 |
| ~~決策頻率~~ | ✅ 2026-09-01 拍板：季度、半年都測，見H-19 | H-19 |
| **決策層的產出型態** | LLM 是「直接選群」還是「給排序建議、程式做最終選擇」？後者較安全（符合方向D 的程式判決不可推翻原則），但也較弱。**未定** | S-02 / S-05 |
| **總經因子清單** | W-02b 的抓取規格已交給 collector，**資料還沒到**。因子清單定了才知道決策層輸入長什麼樣 | W-02b / S-02 |
| **群內選幾個代表（m的正式數字）** | H-10 的**挑選規則**已完成（貪婪多樣性選擇），但 `m`（每群選幾支）本身還沒拍板——目前只示範m=3/m=5兩組驗證規則穩健，正式數字要等H-12的四組對照實驗跑出來才有依據 | H-10 / H-12 |
| **XM 樹（k=3）在方向C 的角色** | XM 只有 3 群且研判是市場邊界主導，**要不要納入決策層還是只當跨市場分散的證據**，未定 | H-03 / S-01 |
| ~~要不要把S-02起的決策層全部改建在滾動窗clock_cell上~~ | ✅ **2026-09-01 使用者定案並同日完成重跑**。理由：實測3個真實測試過的情境裡2個分類不一樣，最近24個月US只有1個月一致、TW只有5個月一致，且有系統性方向（frozen在近期系統性偏判「過熱」）——不是可忽略的邊界誤差。**重跑結果見下方「H-18②下游重跑計畫」**，105/105測試通過 | H-18② / S-02 / S-05 / S-07 |

### H-18②下游重跑計畫（2026-09-01 登記，同日執行完成 ✅）

**範圍確認**：H-06/H-07/H-08/S-01（`cluster_profile_quant`/`cluster_identity`/`cluster_macro_interface`）**不受影響、不需重跑**——這些都是用日曆年份的定量特徵，不吃`clock_cell`。只有S-02起、跟投資時鐘四格有關的鏈路需要重建。

| 步驟 | 要做什麼 | 估計時間 | 實際結果 |
|---|---|---|---|
| ① 改程式碼 | `stage4_strategy_map.py`的`compute_macro_performance()`改讀滾動窗版`macro_history_rolling_{market}.parquet`，不是凍結版；`macro_decision_input.py`的`macro_state_snapshot()`同步改指向滾動窗檔案 | 幾分鐘 | ✅ 完成，`freeze.verify_inputs`目標同步從`STAGE2/macro`改成`STAGE2/macro_rolling` |
| ② 全量重跑stage4 | `strategy_map.parquet`/`regime_performance.parquet`/`macro_performance.parquet`一起重算 | 估計1~5分鐘 | ✅ **實測11.7秒**。`macro_best_cell`分布明顯改變：US從原本偏「過熱」變成98.2%落在「復甦」（8518/8682），跟H-18②發現的「frozen在近期系統性偏判過熱」方向一致；TW四格分布相對均勻（1582~2184區間） |
| ③ output_a.py快速重跑 | 不消費`macro_best_cell`，選兵結果預期不變，重跑讓DD-08雜湊鏈跟上游對得起來 | 數秒 | ✅ 完成，216列scenario_table，各市場/風險偏好的贏大盤比例跟修正前同量級（未受影響，符合預期） |
| ④ macro_decision_input.py重算 | `cluster_macro_conditional.parquet`用新的`macro_performance.parquet`重新彙整 | 數秒 | ✅ 完成，64列（TW6+US7+XM3群×4格） |
| ⑤ S-05品質驗證重跑 | 3個情境（TW_normal/TW/2025-12、US_normal/US/2025-12、TW_normal/TW/2025-05）重新確認LLM決策品質 | 1~3分鐘 | ✅ 完成，3情境A_rule皆被LLM選擇涵蓋（A⊆B皆True）。**TW_normal/TW/2025-05這個情境的clock_cell在滾動窗版下變成「衰退」**（frozen版本是別的分類），驗證了H-18②發現的分類差異是真實存在、且已在新流程下正確反映 |
| ⑥ S-07穩定度重跑 | 3情境×5次＝15次LLM呼叫 | 15~20分鐘 | ✅ 完成。XM_normal/TW/復甦與TW_normal/TW/衰退兩情境100%完全一致（Jaccard 1.000）；US_normal/US/復甦 60%一致（Jaccard 0.650，跟修正前同量級的隨機性），三情境rule_in_llm_rate皆100% |
| ⑦ 測試套件+契約驗證 | 確認全部通過 | 數秒~1分鐘 | ✅ **105/105通過** |

**總實際耗時：遠低於預估的20~30分鐘**（②只花11.7秒，非估計的1~5分鐘；⑥的15次LLM呼叫是實際大宗）。全部產物已用修正後的滾動窗clock_cell重建，S-02起的決策層鏈路（`cluster_macro_conditional`／`macro_state_snapshot`／S-05／S-07）皆已在無look-ahead的總經訊號上驗證過。

### 10.6 依賴關係（併入 H 系列）

```
方向A（地基）  H-01~H-07 ✅ → H-09 → H-10 ┐
                                 H-11 ─────┼→ H-12 / H-13
                                           │
方向C（主軸）  H-08 → S-01 → S-02 ┬→ S-05 ─┘
                                  ├→ S-06
                                  └→ S-07
               H-17~H-21（總經本體）依賴 S-01/S-02 定義的介面
               S-03 必須與 H-11 / H-24 同時拍板

方向D（輔助）  S-08 依賴 S-07 的稽核數據 + cluster_story 針對新群數重跑
               S-04 無依賴，是一個引用學長結論的決策記錄，可立即定案
```

> ⚠️ **上面這張依賴圖的「方向C」整條已於 2026-09-02 終止，見第 11 節。**

---

## 11. 🔴 2026-09-02 老師會議 · 畢業路線定案

> **來源**：`9-2咪挺.pdf`（TurboScribe 轉錄，**在老師開始統合意見處被 30 分鐘上限截斷**）
> ＋ `逐字稿 (1).docx`（完整版 9,561 字，精度較低但涵蓋結尾）。
> **兩份交叉比對後整理，使用者 2026-09-03 確認無誤。**
> ⚠️ 逐字稿有大量轉錄雜訊（TIC=P_IC／NDD·mdb=MDD／emb·ecm=ENB／火速三千=Russell 3000／
> 路文=論文／凱中·臺中=凱菘），引用原話時已還原，但仍以語意為準。

### 11.1 🔴 最重大變更：方向 C（總經 → 選群）確定終止

使用者當場問「那這樣子的話總經還會用進來嗎？」，老師回答：

> 「因為看起來都**跟總經沒關係**啊⋯你不覺得嗎？」
> 「**所以就不要做了**⋯我剛才就講就不要做了⋯**就是不會有答案，不會有好答案啊**」

**判斷依據**（老師在看美股七群逐年賺賠時說的）：
> 「美股就是更明顯可以看得出來他們都是⋯**基本上都是 beta 股**⋯所以原則上我們上次講說
> 去用總經去看什麼時候該用什麼，**這看起來這個沒什麼用**」

老師同時說明了為什麼還是要先把這些跑出來：
> 「就是**為什麼要先做這些東西出來，就是我們要去看啊**」——即：這是必要的排除性證據，不是白做。

**受影響條目**：

| 條目 | 新狀態 |
|---|---|
| H-17 / H-18② | ☑ 已完成，**保留為方法論貢獻**（滾動窗修掉真實 look-ahead 缺口），不再服務總經選群 |
| H-18③ | 🔄 **不再是「決策品質對照」，改為 11.2① 的 anchored walk-forward** |
| H-19（決策頻率季/半年） | 🔄 原本服務總經選群，**改為服務 walk-forward 的窗口移動設計** |
| **H-20（總經因子可泛化性）** | ❌ **取消** |
| H-21（多維度矩陣） | 🔄 **維度改變**：原「時間顆粒度 × 總經因子」→ 新「**walk-forward 窗次 × 精選比例**」 |
| S-01 / S-02 / S-05 / S-07 / S-08 | 🔶 **產物全部保留**，不再是論文第四章主體；**轉為第二階段 AI Agent 的基礎建設**（資訊源設計、S-03 不給日期、抗幻覺三鐵則、重複執行穩定度都可直接沿用） |
| S-06 | ⏸ 維持暫緩（原因不變） |
| 論文第四章（原＝總經應用） | 🔄 **需重新定位**，見 11.5 |

### 11.2 🎯 畢業必考題（老師：「跑完這個就可以了」，估一兩禮拜）

> 「你今天這樣的答案，**唯一還需要跑的**就是⋯」

| ID | 任務 | 老師原話與規格 | 狀態 |
|---|---|---|---|
| **H-26** | **Anchored walk-forward（多次窗口移動）** | 🔴 最重要。老師：「**你這只有移動一次**嘛。我如果能夠移動了兩三次，然後每次的答案都偏向[HRP]一次，**那 HRP 就的確比較好**」「**我如果 out-sample 多幾次，我的 HRP 精選都會[贏]**⋯那答案就出來了」<br>**明確指定 anchored（擴張窗），不是 rolling**——老師：「你這個應該是跑 **Anchored** 就可以了」（並確認使用者沒聽過這個名詞）。<br>老師給的窗設計：<br>`第1次 IS 2007-2015 → OOS 2015-2018`<br>`第2次 IS 2007-2018 → OOS 2018-2021`（IS 起點不動，把用過的 OOS 併進 IS）<br>`第3次 IS 2007-2021 → OOS 2021-2024`<br>**目的**：補掉目前「單一段 84 個月 OOS」這個最大弱點——一次 OOS 贏不算數，要多次都贏。<br>⚠️ 五組對照（A_hrp/B_all/C_random/D_top_cagr/E_top_calmar）都要在每一個窗次重跑 | ☐ |
| **H-27** | **精選比例掃描**（取代原本的大盤門檻提案） | 🔴 老師**當場收回**了先前提的「贏大盤 0.75 倍 vs 贏大盤 5%」門檻實驗：「**我覺得你那個不用，我剛才講那個收回**」。<br>改成：「**你的精選是抽 5% 而已？抽前 5% 還是抽前 3% 還是前 1% 還是前 10%？**」「**這個有時候你越挑精兵反而是越爛**」「因為你精選的它是**一個需要研究的對象的參數**」<br>→ 把選取比例做成掃描，看 OOS 表現怎麼變。**同時解決了 H-10 懸而未決的 `m`（每群選幾支）**——`m` 不再需要拍板一個數字，它本身就是實驗變數。<br>與 H-26 交叉＝H-21 的新矩陣 | ☐ |
| **H-28** | **HRP 為什麼會贏——質性解釋** | 老師：「**HRP 為什麼會[贏]，這個你要有一定的解釋**啊⋯因為你**有量上面的解釋，可是你沒有一個偏質性上**、為什麼他會贏，**你要說個理由出來**啊」「感覺第一個是這個啦」<br>→ 不是再跑實驗，是要一套**機制上的說法**：為什麼「按互補性精選」會勝過「挑歷史贏家」與「全部灑」。<br>💡 手上已有的素材：H-12 查出 D/E 組是**集中賭注**（XM 100%、US 54% 集中單一群）、H-09 的 ENB、H-25 的粒度效應、H-10 的「純品質選法選出相關 0.974 的複製品」 | ☐ |

### 11.3 💼 第二階段 · 就業加分題（做完必考題再啟動）

> 「你要不要**先把這個先跑完**，然後我們再來看怎麼樣⋯如果覺得 OK 的，我們就來把論文寫一寫。
> **那你如果還想做別的更有趣的**⋯」
> 「這個等於說是你的**就業加分題**⋯前面那個是你的**畢業必考題**」

| ID | 任務 | 內容 | 狀態 |
|---|---|---|---|
| **H-29** | AI Agent 每季選股 | 老師：「變成說是**每月每季去把那個名單拿出來**⋯甚至你的 IS/OOS 變成是每月每季，然後**這個每月每季怎麼挑，稍微用 AI 的 Agent 去做**」「每次季報完了以後我就把它重跑一遍，那我在選的時候**有沒有什麼樣的 prompt 可以去選會讓他更優**——就是有點玩**凱菘那個把戲**」<br>**現實理由**（老師原話）：「因為你現在等於說你**做一個論文都沒有 AI，找工作不太有利**啊」<br>🔶 **S 系列的既有產物在這裡全部可以重用**（S-02 資訊源設計、S-03 不給日期、S-05 三組對照、S-07 重複穩定度、S-08 抗幻覺架構） | ☐ 待第一階段完成 |
| **H-30** | Agent 的 IS/OOS 資料安排 | 老師給的具體設計：「用**前幾季的財報**⋯去年台灣第三季、今年四月、今年第二季⋯**你至少有三次的季報可以用**」「這樣子你就有**一個 in-sample 兩個 out-sample**」「你就**抓後期這三年，因為這三年的新聞通常 AI 裡面都還有**」<br>→ 跟學長凱菘處理 LLM 資料洩漏是同一套思路，老師建議可參考他的 prompt 做法 | ☐ 待第一階段完成 |

### 11.4 ✅ 老師的其他裁示（不需開發，但要記錄）

| 議題 | 老師的裁示 |
|---|---|
| **倖存者偏誤（不重跑）** | 老師主動提起：「你這樣唯一會被人家 K 的就是我們沒有去換 Russell 3000⋯**我是覺得不要換了**，因為我們**沒有要寫期刊論文**⋯**因為我們對所有人都一樣**，你的 A 組 B 組 C 組都是一樣玩⋯**所以這是公平的**」<br>→ 不重跑，但論文限制章節要寫。⚠️ 2026-09-02 另外查出**台股也有同樣問題**（1,809 家裡 1,776 家至今仍在市，26 年只有 33 家曾停止交易），文件目前只標了美股，須補上 |
| HRP 是主流正解 | 「**HRP 是正解**⋯目前大家都是用 HRP。還好那時候跟你講 HRP，不然跑出來應該會有點難看」 |
| 輪廓係數方法 | 「你用的那個**輪廓係數這個是合理的**」；排除作弊解（最大群 67.6%）也認可：「太多了，所以沒有意義」 |
| alpha 門檻 | 確認「贏過或等於大盤才進來」，老師：「這樣看起來合理」 |
| 細粒度高互補（H-25） | 「**哦那很好啊那很好**」 |
| IS/OOS 切 2018 | 「**你會去找 2018 年是很聰明的，因為那個是關鍵的一年**」（老師認為台股在 2018 後因美中關係整個變了，這解釋了台股 IS/OOS 一致率只有 53.3%） |
| 跨市場數字 | IS CAGR 30.51% / OOS 28.25%、MDD IS 16.3 / OOS 22.2 → 「**這倒是很漂亮**」 |
| MDD 是唯一代價 | 「我們這個用因子分析基本上就是不套策略⋯我只有換股而已，**我只有換股的話我的代價就是我的 MDD 一定會比較慘**」 |
| 💡 **ENB 的正確用法** | 老師：「你現在是**人海戰術全部都上**⋯等於說我們就是**中央大學這樣去硬搞一堆**。但如果同樣的策略用在**資管系或管理學院**⋯或**每個學院都抓**，這個時候你的這一招**就會很有用**」<br>接著給了做法：「**根據不同的 F** 的話，應該這個效果會不錯⋯**你只要找到好的**，因為你有能夠去分辨什麼是**有效的 F**⋯有效的 F 再去套這些，然後**有些套 C 有些不套 C**，這樣做出來的會蠻漂亮的」<br>→ 可併進 H-28 的質性解釋，或當 H-21 矩陣的第三個維度 |
| 老師對使用者做法的評價 | 「我就可以很快跟你講用什麼 approach 就會出現可畢業的好答案⋯但**我自己的做法會偏向你的做法**⋯因為你讀碩士也是想要增加你做事的技巧跟能力」 |
| 📌 小提醒 | ①輪廓係數要補**平均值**（這次只列了最大最小）②年度報酬**單位要統一**（有時用 1/2、有時用 %） |

### 11.5 論文章節骨架的調整（待定案）

原骨架的第四章是總經應用，現在必須換掉：

```
第一章 · 問題    上萬個策略的多樣性假象                      （不變）
第二章 · 量化    HRP 診斷 → 免費午餐只在跨市場 → ENB          （不變）
第三章 · 解釋    群的身份是什麼 + H-25 粒度效應               （不變）
第四章 · ???     ← 原本是「總經→選群」，已終止，待重新定位
第五章 · 驗證    Anchored walk-forward 多窗次 + 五組對照      （H-26/H-27 大幅強化）
方法論貢獻       抗幻覺架構（S-08）+ 滾動窗修掉 look-ahead（H-18②）
```

**建議方向（尚未跟老師確認）**：把 H-28（HRP 為什麼會贏的質性解釋）擴充成第四章，
把「集中賭注 vs 分散賭注」的機制講透，用 H-09 ENB／H-10 代表挑選／H-12 集中度診斷／
H-25 粒度效應四組證據串起來。這樣第四章從「應用」變成「機制解釋」，
而且素材全部已經有了，不需要新實驗。**下次開會要跟老師確認這個定位。**

### 11.6 新的執行順序

```
🎯 第一階段（畢業必考題，老師估一兩禮拜）
   H-26 anchored walk-forward（2~3 個窗次 × 五組對照）
        ↓ 產出即為 H-21 新矩陣的一個維度
   H-27 精選比例掃描（前 1%/3%/5%/10%）
        ↓ 兩者交叉 = H-21 多維度實驗矩陣
   H-28 HRP 為什麼會贏（質性解釋，不需新實驗，整理既有證據）
        ↓
   → 論文可以開始寫

💼 第二階段（就業加分題，第一階段完成後再啟動）
   H-29 AI Agent 每季選股（重用 S-01~S-08 的既有基礎）
   H-30 Agent 的 IS/OOS 資料安排（三次季報 = 1 IS + 2 OOS）
```

---

## 6. 建議執行順序（2026-08-25 查證後更新）

```
✅ 研究部程式開發已完成：
   D-01/D-02/D-03/D-04、W-01~W-16（全部，含新增的 W-15 cluster_story、
   W-16 code review 修復的manifest共用bug）、
   R-01~R-09、B-01~B-05/B-07、N-01/N-03/N-05、台股strict/relaxed/all三變體
   （研究部主鏈 階段0→1→2a/2b/2c→3→4→output_a、資料品質防線、
    兩個LLM點（T10 return_story／W-15 cluster_story）皆已完成並用真實資料驗證）
             ↓
⚠️ 狀態不明（非我方程式，需外部確認）
   N-02（美股NO_HISTORY 9檔是否已整段刪除——collector端動作，本repo無ticker清單可驗，
        須向 collector 確認。⚠️ 本節先前誤列為「已完成」，2026-08-25 訂正）
             ↓
🔸 剩下的都不是「研究部程式開發」
   D-05（v9記錄本次資料異常事件，需你補一段敘述我不能自己編）｜
   B-06（跟老師報告數字不用更正這件事，溝通動作看你要不要講）｜
   N-04（GDP版本對齊限制，屬論文limitations寫作，非開發）｜
   N-06（總經資料源要不要換FRED，屬取捨非必要）｜
   OpenAI Admin key（主動查額度用，見 utils/openai_quota.py；反應式偵測已可用不受影響）｜
   美股strict/relaxed變體（Phase2仍是8/13舊資料，屬階段-1候選池、在研究部上游，尚未重跑）
```

> 🔴 **「研究部完成」≠「專案完成」——實戰部幾乎還沒開始**（2026-08-25 查證 `code/ops/`）：
> 目前只有**工具層** `ops/tools.py`（T1~T13）完成。**Agent0／Agent1／Agent2 的本體、
> LangGraph 編排、Agent間資料傳遞格式，repo 裡完全沒有對應程式檔**（已用檔名搜尋確認
> 無任何 agent／langgraph 相關檔案），對應 `實戰部架構_v8.md` §9「尚未展開」那一節。
> 具體影響：**T10 寫好了但目前沒有任何東西會呼叫它**——因為觸發它的 Agent1 還不存在，
> 它是備好待接的零件，不是已上線的功能。

理由：研究部這條線的程式已經沒有阻塞，剩下的不是文件敘述、就是溝通決策、或屬於實戰部與上游候選池的工作，彼此沒有相依關係，可依你的重心自由排序。

> 🆕 **2026-08-27**：以上是第一輪主鏈的收尾狀態。**下一輪開發（老師8-26意見的HRP精修）見第9節 H系列**，
> 從 H-01（alpha出賽關卡）開始，H-14/H-15/H-16（危機降級）可插隊優先做（範圍小、已定案、不依賴其他項目）。

---

## 7. 進度更新方式

- 完成一項就把狀態欄改成 `☑`，並在該列補完成日期
- 阻塞解除時把 🔒 改成 ☐ 並註記解除日
- B 系列每填一個新值，同步更新 `系統設計文件_v1.md` 對應的 DD 條目
- 新發現的落差／問題追加到 N 系列，不要散落在對話裡

---

*建立於 2026-08-20。R-04 是本階段最重要的決策點：18 分鐘決定 20 小時。*
