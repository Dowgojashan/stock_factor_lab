# 開發待辦追蹤

> **用途**：資料修復等待期的工作分配與進度追蹤。**這是活文件，做完就更新狀態欄。**
> **建立**：2026-08-20　**最後更新**：2026-08-20（D-01、D-02、W-01、W-02a ☑；W-02b 因無總經資料阻塞；新增 N-04~N-06）
> **背景**：價格資料異常確認需修復（collector 端執行中），期間研究部開發不停擺。
> 相關文件：`系統設計文件_v1.md`（SDD）｜`_analysis_outputs_dataquality/重跑必要性評估.md`

## 狀態圖例

`☐ 未開始`　`◐ 進行中`　`☑ 完成`　`🔒 被阻塞`　`⏸ 暫緩`

---

## 0. 目前的阻塞源（總覽）

| 阻塞源 | 負責 | 狀態 | 解除後影響 |
|---|---|---|---|
| 台股接縫修復（186 檔）| collector | ◐ 進行中 | 解除 B-01~B-06、啟動 R 系列 |
| 美股 26 檔修復（需先解除凍結）| collector | ☐ 待下游確認解凍 | 同上 |
| 9 個因子重算 | collector | ☐ 等資料修完 | 同上 |
| 🆕 **總經資料尚未收集** | collector | ☐ **新發現 2026-08-20** | 解除 W-02b、階段2b 全部 |

> 🔴 **2026-08-20 新發現**：實查資料庫 12 張表，**沒有任何總經指標**——`singleindicator` 的 25 項全是財報科目（EPS/netDebt/ebitda…）、`factor` 的 26 項全是個股因子。階段2b 需要的 GDP／CPI／利率／景氣訊號**一項都沒有**。
> 契約（`contracts.MACRO_RAW`）與處理鏈（`stage2b_macro.py`）已完成並通過合成資料驗證，**資料一交付即可建置**。抓取規格見第 8 節。

**在此期間：W 系列與 D 系列全部可做，B 系列一律不得定案。**

---

## 1. W 系列 · 可立即進行（不受資料修復影響）

| ID | 任務 | 為何不受影響 | 預估 | 驗收條件 | 狀態 |
|---|---|---|---|---|---|
| **W-01** | **階段2b：查證各總經指標的發布滯後量** | 總經資料與股價完全無關 | 1–2 天 | ☑ **2026-08-20 完成** — `code/research/macro_spec.py`（單一事實來源，10 個指標 × 官方來源連結 + `available_period()` 可得性計算）＋ 匯出 `_frozen/stage2/macro/lag_spec.json`；4 項測試鎖住邏輯（含季頻邊界與「不得晚於決策月」鐵則）。<br>⚠️ 查出 **vintage 限制**（見 N-04）與 **TW GDP 待確認項**（見 N-05）| ☑ |
| **W-02a** | 階段2b：資料契約 + 處理鏈實作 | 邏輯與資料內容無關 | 1 天 | ☑ **2026-08-20 完成** — `contracts.MACRO_RAW`／`MACRO_HISTORY` 契約；`stage2b_macro.py` 完成滯後對齊→月頻攤平→z-score 凍結→3月平滑→投資時鐘四格；`--selftest` 用合成資料驗證，**內建前視稽核 312 個月 × 2 市場、違規 0 筆** | ☑ |
| **W-02b** | 階段2b：正式建置 macro_history | — | 0.5 天 | 🔒 **阻塞：資料庫中沒有任何總經資料**（見第 0 節新增阻塞源）。契約與程式已就緒，資料一到即可跑 | 🔒 |
| **W-03** | 階段3：HRP 演算法本體（相關→距離→linkage→準對角化→遞迴二分）| 演算法與資料內容無關 | 2–3 天 | 用合成資料單元測試通過；PSD 檢查、三角不等式檢查可運作 | ☐ |
| **W-04** | 階段3：**乾跑驗證**（用現有髒資料）| 目的是驗證機制跑得動，不是做決定 | 1 天 | 量出：記憶體峰值（預估 1.6GB）、`single` vs `ward` 樹形、變體報酬序列去重率 | ☐ |
| **W-05** | 實戰部工具層 T1–T13 | 相依 schema 非資料值；用降級版 `cluster_id`（`f_combo` 代理）| 3–5 天 | 13 個工具皆可呼叫並回傳結構化結果 + 信心標註 | ☐ |
| **W-06** | `test_matrix_completeness.py`（12 格矩陣完整性測試）| 純 schema 檢查 | 0.5 天 | 12 格逐格拆條件，缺欄位即 fail 並列出缺哪個 | ☐ |
| **W-07** | `stage1_marks.py` 程式碼（分位計算 + 等級判定 + 兩把刀）| 門檻是分位數參數化的，程式不因資料變而改 | 1 天 | 可執行；門檻值以參數傳入、不寫死。⚠️ 兩把刀依 D-01/D-02 更正後的定義：低 `effective_n` ＋ **高** `rotation_score`（切點須 P95+）、**`empty_ratio`** 過高 | ☐ **阻塞已解除 2026-08-20** |
| **W-08** | 資料品質防線常設化 | 新需求，與修復平行 | 1 天 | `stage1_scan` 產出 `data_glitch` 標記欄；`diagnose_price_anomalies` 納入常規驗收 | ☐ |

> ⚠️ **W-04 的紀律**：乾跑結果**不得用來定案** linkage 方法與 L3 群數（見 B-03）。只記錄「機制可運作」與「效能實測值」。

---

## 2. D 系列 · 文件修正（現在就能做）

| ID | 項目 | 嚴重性 | 說明 | 狀態 |
|---|---|---|---|---|
| **D-01** | 🔴 **`rotation_score` 方向矛盾** | **高 · 實質隱患** | ☑ **2026-08-20 完成** — 研究部 v9 自相矛盾：定義「分數低＝有輪動」、淘汰規則「低 EffN ＋**不**輪動＝淘汰」（不輪動＝分數**高**），但尾端硬篩表寫「低 EffN ＋**低** rotation_score」——**方向相反**。SDD 照抄跟著錯。**不修會砍錯策略且不報錯。** 已修：研究部 v9 尾端硬篩表（加 v9.2 更正區塊）、SDD DD-09（改寫並加實測分布）、SDD 階段1 規格、SDD 3.3 欄位表、GateC v1.2。<br>**另發現分位框架在此欄位會退化**：62.4% 的值恰為 0，切點須用 P95+，已一併寫入三份文件 | ☑ |
| **D-02** | `coverage_ratio` 定義失效 | 中 | ☑ **2026-08-20 完成** — 新增 SDD **DD-12** 完整記錄（含 `backtest.py:344` 權威定義、無 C 條件策略的決定性反證、市場常數證據）；回填研究部 v9 C-4／欄位表／需求路徑／待資料清單、GateC C-4（v1.2）| ☑ |
| **D-03** | 落差清單加第 6 項 | 中 | 價格資料異常應正式列入 SDD 附錄 A，與前五項並列（唯一導致重跑者）| ☐ |
| **D-04** | SDD 新增「資料品質防線」章節 | 中 | 記錄 `max_daily_ret`/`min_daily_ret`/`ann_vol_monthly` 診斷欄位的用途與判準，以及還原價下**絕對價位不可當判準**的教訓 | ☐ |
| **D-05** | 研究部 v9 記錄本次事件 | 中 | 資料異常的發現、collector 查證結論、重跑決議，寫進 v9.2 修訂區塊 | ☐ |

---

## 3. B 系列 · 🔒 資料修復前不得定案

> **這一節是為了防止「用髒資料做的決定被誤當定案」。每一項都已經有初步答案，但那些答案是在污染資料上算的。**

| ID | 項目 | 現有（暫定）值 | 為何必須重新推導 | 狀態 |
|---|---|---|---|---|
| **B-01** | `contracts.py` 的 `EXPECTED_*` 驗收常數 | 總列數 14,078／TW 7,162・US 6,916／`F2_empty` 384・652／F組合 219・188／v0v1 拆分 | 重跑 Phase 1–4 後**候選池組成會變**，這些寫死的驗收數字全部失效 | 🔒 |
| **B-02** | DD-03 HRP 共同窗 | 台 2007-01(228月)／美 2002-01(288月)／XM 2007-01 | **決策邏輯（per-tree 窗）不變，數字要重算**——策略池變了，`hist_start` 分布要重測 | 🔒 |
| **B-03** | DD-06 L3 目標群數 | 錨定 407（= TW 219 + US 188 獨立 F 組合）| 獨立 F 組合數會變 | 🔒 |
| **B-04** | 階段3 正式執行 | — | HRP 樹會被假報酬污染；且 **winsorize 無效**（對月報酬做排序類處理時是單調轉換）| 🔒 |
| **B-05** | strategy_map 凍結 | — | 上游全部要重跑 | 🔒 |
| **B-06** | 向老師更正美股數字 | ~~68.9% → 55.5%~~ **已作廢** | 該數字含 71% 誤判；用正確清單重算為 68.94% → **68.94%（不變）**。**修完資料重算後再決定要不要更正** | 🔒 |
| **B-07** | 階段1 兩把刀的門檻值 | 待資料 | 分位數框架不變，切點要用乾淨資料重定 | 🔒 |

---

## 4. R 系列 · 資料修復完成後的重跑順序

> **關鍵設計：R-04 是決策點，只花 18 分鐘就能決定要不要花 20 小時。**

| 順序 | ID | 動作 | 實測耗時 | 產出／判準 | 狀態 |
|---|---|---|---|---|---|
| 1 | R-01 | collector 修復價格資料（台股接縫 + 美股 26 檔）| — | 全序列對獨立源比值無階梯 | ☐ |
| 2 | R-02 | 重算受影響的 9 個因子 | 1.5–2 h | — | ☐ |
| 3 | R-03 | ⚠️ **重算 `universe_benchmark`** | 待估 | **見第 5 節 N-01——基準本身可能被污染，是新增項目** | ☐ |
| 4 | **R-04** | **只重跑 Phase 1** | **8.7 分/市場 → 台美 18 分** | **決策點**：primary 清單有沒有變 | ☐ |
| 5a | R-05 | 若 primary 清單**有變** → 重跑 Phase 2–4 | Ph2 42.5分／Ph3 5.3h／Ph4 3.9h，台美共約 **20 h**（可過夜）| 新候選池 | ☐ |
| 5b | R-06 | 若 primary 清單**沒變** → 只重跑受影響策略 | 遠低於 20h | 同上 | ☐ |
| 6 | R-07 | 重跑階段0 `candidate_index` | 0.5 秒 | 契約驗證通過 | ☐ |
| 7 | R-08 | 重跑階段1（mktcap + scan）| mktcap 6 分／scan 6 分 | 4 份產物落地 | ☐ |
| 8 | R-09 | **更新 B 系列全部常數與決策** | 0.5 天 | B-01~B-07 逐項填入新值並解除 🔒 | ☐ |

**我的估算（供 R-04 對照，非定案）**：台股 4 個因子判定會變（ROE primary→邊際、ROIC 邊際→淘汰、OCF_E 與 FCF_P 升級）、美股 3 個（EPS primary→邊際、FCF_OI 與 NETDEBT_EBITDA 升級）。**這是近似計算，ρ 變動約 0.10 剛好落在 0.85 門檻附近，估計誤差可能影響判定——所以要實跑驗證。**

---

## 5. N 系列 · 新發現待確認

| ID | 發現 | 說明 | 建議 | 狀態 |
|---|---|---|---|---|
| **N-01** | ⚠️ **自建宇宙基準本身可能被污染** | 台股基準 8.67% 是「同宇宙、等權、含股利」＝等權持有 1,775 檔，**那 186 檔異常股就在裡面**。粗算 2024-01-02：`77 檔 × (1/1775) × 400% ≈ +17%` 單日。三個群聚日加總，基準可能被高估**接近 1pp**。<br>**嚴重性**：Phase 2／Phase 3 的所有門檻都是「贏過基準」→ 基準灌水 → 門檻墊高 → **篩掉了本該過關的策略**（方向是過度嚴格，非放水）| 列入 R-03，與因子一起重算 | ☐ |
| **N-02** | 美股 NO_HISTORY 9 檔的影響被低估 | 那 9 檔是 ticker 重用，**整段早期歷史都是別家公司**，不只是跳動那一天。目前的影響估算只扣掉單日，屬保守低估 | 修復時整段刪除（collector 已規劃）| ☐ |
| **N-04** | ⚠️ **總經資料只能「時點對齊」，做不到「版本對齊」** | 我們能確保只用「該時點已發布」的期間資料，但拿不到「該時點當下看到的原始數值」。GDP／失業率事後會被修正，而 TEJ／FMP 存的是**修正後的最新值**；美國有 ALFRED 保存歷史版本，台灣無對應服務。<br>→ 消除了「用到未來期間」的偏誤，殘留「用到未來修正值」的偏誤（影響小得多，但存在）| **寫進論文 limitations**；CPI／利率幾乎不修正故影響最小，GDP 與景氣領先指標最需註明 | ☐ |
| **N-05** | TW GDP 存的是概估數還是初步統計數？ | 主計總處分兩階段發布：**概估統計**季末後約 1 個月、**初步統計**季末後約 2 個月（國民所得統計評審會）。目前 `macro_spec` 暫定 `lag_months=1`（概估）。**若 TEJ 存的是初步統計數，實際可得時點應為 2 個月** | 請 collector 確認 TEJ 的 GDP 欄位對應哪一階段 | ☐ |
| **N-03** | collector 驗證的限制 | 判定只針對「每檔最大的那次跳動」，不排除同檔其他日期還有較小接縫（TNXP、RCAT、QXO、BMNR 事件數 2–80）| 等 R-01/R-02 做完，用 `diagnose_price_anomalies` 重掃看殘留 | ☐ |

| **N-06** | 🆕 **建議總經資料改用免費官方源，不吃 FMP 額度** | 美股用 **FRED API**（免費、涵蓋 BEA/BLS/Fed/Treasury 全部所需序列）；台股用主計總處／國發會／央行公開統計。<br>⭐ **額外好處**：FRED 的 **ALFRED** 提供 vintage（歷史版本）→ **可望解決 N-04 對美股的限制**，讓美股做到真正的 point-in-time | 見第 8 節規格 | ☐ |

---

## 8. 給 collector 的總經資料抓取規格（W-02b 的前置）

**交付格式**：`_frozen/stage2/macro/macro_raw.parquet`，long 格式，欄位依 `contracts.MACRO_RAW`：

| 欄位 | 說明 |
|---|---|
| `market` | TW / US |
| `indicator_key` | `growth`／`growth_alt`／`inflation`／`rate_level`／`rate_direction` |
| `period` | 資料所描述的期間，`YYYY-MM` 或 `YYYYQn` |
| `value` | 數值 |
| `release_date` | ⭐ **該筆的實際發布日**（有就填）——有這欄就能做真 point-in-time，不必倚賴推估滯後量 |

**期間範圍**：1999-01 起（z-score 用 2000-01~2025-12，前一年供平滑暖機）

**美股 · FRED（免費，無額度限制）**

| `indicator_key` | FRED series | 說明 |
|---|---|---|
| `growth` | `A191RL1Q225SBEA` | 實質 GDP 年化季增率（季）|
| `growth_alt` | `UNRATE` | 失業率（月）|
| `inflation` | `CPIAUCSL` | CPI（月）→ 需自行算年增率 |
| `rate_level` | `FEDFUNDS` 或 `DGS10` | 聯邦基金利率／10Y 殖利率 |
| `rate_direction` | `T10Y2Y` | 10Y−2Y 利差，**FRED 直接提供，不必自己相減** |

> ⭐ 若能一併用 **ALFRED**（`realtime_start`/`realtime_end` 參數）取 vintage，請填 `release_date`，
> 這會讓美股從「時點對齊」升級為「版本對齊」，直接改善 N-04。

**台股 · 官方公開統計**

| `indicator_key` | 指標 | 來源 |
|---|---|---|
| `growth` | 實質 GDP 年增率 | 主計總處 國民所得統計資料庫 |
| `growth_alt` | 工業生產指數年增率 | 經濟部統計處 |
| `inflation` | CPI 年增率 | 主計總處 物價統計資料庫 |
| `rate_level` | 央行重貼現率 | 中央銀行 |
| `rate_direction` | 景氣對策信號綜合判斷分數 | 國發會 |

**❓ 需 collector 回答**：TEJ／主計總處的 GDP 欄位存的是**概估數**（季末+1月）還是**初步統計數**（季末+2月）？這決定 `macro_spec` 的 `lag_months` 該設 1 還是 2（見 N-05）。

**風險軸（第 5 欄）**：v9 定案**留白**，不需交付。

---

## 6. 建議執行順序（等待期）

```
第一優先  D-01 ☑  W-01 ☑（已完成）
             ↓
W-02a ☑（2b 契約與處理鏈）／W-02b 🔒 等資料
→ 現在   W-03 + W-04（HRP 演算法與乾跑）
             ↓
第三優先  D-02~D-05（文件回填）｜ W-06（矩陣測試）｜ W-08（品質防線）
             ↓
有餘裕    W-05（實戰部工具層，最花時間但可切分）
```

理由：D-01 最快且是實質隱患；W-01 是唯一「不能靠增加投入加速」的外部相依，越早開始越好；W-03/W-04 最需要 debug 時間。

---

## 7. 進度更新方式

- 完成一項就把狀態欄改成 `☑`，並在該列補完成日期
- 阻塞解除時把 🔒 改成 ☐ 並註記解除日
- B 系列每填一個新值，同步更新 `系統設計文件_v1.md` 對應的 DD 條目
- 新發現的落差／問題追加到 N 系列，不要散落在對話裡

---

*建立於 2026-08-20。R-04 是本階段最重要的決策點：18 分鐘決定 20 小時。*
