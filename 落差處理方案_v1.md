# 架構↔實驗落差 · 處理方案評估 v1

> **背景**：架構文件（研究部 v9 / 實戰部 v8 / GateC v1）與實驗產物是**平行推進**的——架構在規劃時，Phase 1–4 實驗同時在跑，中間又碰上台美資料收集的限制。兩邊沒有即時同步，於是產生了以下落差。這不是任何一邊做錯，是流程使然，現在補上即可。
> **本文件目的**：把每個落差講清楚（含全量實測數字），各給至少三個方案並比較，最後給建議。**確認後回填進 SDD 與架構文件。**
> **量測日**：2026-08-20　**量測範圍**：全部 14,078 個候選策略（非抽樣）

---

## 摘要

| # | 落差 | 嚴重性 | 建議方案 | 成本 |
|---|---|---|---|---|
| 1 | `sharpe_ann`/`daily_sharpe`/`artifacts_dir` 不在候選 CSV | 🟡 中 | A：階段0 join `stats.parquet` + 對帳斷言 | 半天 |
| 2 | 候選產物散在 L3/L4 兩個 job 目錄 | 🟡 中 | A：規則解析 + 物化成欄位 + 存在性斷言 | 併入落差1 |
| 3 | **台股歷史起始期參差 7 年** | 🔴 高 | B：**每棵樹各自共同窗**（台 2007-01 / 美 2002-01）| 設計決定，零額外工時 |
| 4 | 月頻/日頻描述矛盾 | 🟢 低 | A：月頻為 HRP 權威源（**已實測兩者完全一致**）| 零 |
| 5 | 🆕 **台美策略字串碰撞 1,381 個** | 🔴 高 | A：複合主鍵 `market::strategy` | 契約層，零額外工時 |

> ⚠️ **本文件修正了 SDD v1 的 DD-03**：我先前基於 150 個抽樣建議「P95 分位窗（方案 C）」，全量實測後證明**該方案不划算**，已改為方案 B。詳見落差3。

---

# 落差 1 · `sharpe_ann` / `daily_sharpe` / `artifacts_dir` 不在候選 CSV

## 1.1 問題描述

研究部 v9 第五部分的 strategy_map 欄位表，把這三個欄位標記為「**階段 −1 既有**」，意思是候選 CSV 拿來就有。實查：

```
候選 CSV 實際欄位（8 欄）：
strategy, F組合, C, CAGR, max_drawdown, win_ratio, 持股數, V
```

三個欄位**都不在**。`artifacts_dir` 更是整個 repo 裡都不存在的欄位。

## 1.2 為什麼會發生

Phase 1–4 的分析腳本（`phase4_analyze.py` 等）產 CSV 的目的是「給人看實驗結果」，只輸出決策需要的欄位；而 v9 欄位表是照 `master_index.parquet`（舊的 10 批海選產物，27 欄）的結構寫的。研究部起點在 v9 從 `master_index` 改成候選 CSV 時，欄位表**沒有跟著改**。

## 1.3 實測：資料其實都在，而且對得上

| 檢查項 | 台股 | 美股 |
|---|---|---|
| `stats.parquet` join 命中率 | **7,162 / 7,162 = 100.00%** | **6,916 / 6,916 = 100.00%** |
| `sharpe_ann` 非空率 | 100.00% | 100.00% |
| `daily_sharpe` 非空率 | 100.00% | 100.00% |
| `avg_drawdown` 非空率 | 100.00% | 100.00% |
| **CAGR 對帳（CSV vs stats）** | **max diff = 9.71e-17** | **max diff = 9.71e-17** |

CAGR 差異在 float64 精度極限（1e-17），**證明 CSV 與 `stats.parquet` 是同一次計算的兩個輸出**，不是兩份獨立算的數字。這是採用方案 A 的關鍵依據。

## 1.4 三個方案

### 方案 A：階段0 join `stats.parquet` 補齊（建議）

```python
job = f"{market}_L3_openSec_M" if V == "v0" else f"{market}_L4_openSec_M"
stats = pd.read_parquet(ART / job / "stats.parquet")
idx = idx.merge(stats[["strategy","sharpe_ann","daily_sharpe","avg_drawdown"]], on="strategy")
assert (idx.CAGR - stats_CAGR).abs().max() < 1e-9   # 對帳斷言
```

| 優點 | 缺點 |
|---|---|
| 零重算，不動任何既有程式 | 多一個檔案相依（`stats.parquet` 必須與 CSV 同步）|
| 實測 100% 命中，無遺漏 | 相依是隱性的，需寫進契約才不會被忘記 |
| CAGR 對帳可**程式驗證同源**，不是憑信任 | |
| 半天可完成 | |

### 方案 B：改 `phase4_analyze.py`，重出含完整欄位的 CSV

| 優點 | 缺點 |
|---|---|
| 單一事實來源，下游最乾淨 | **動到已向老師報告過的產物**（8/19 已驗收）|
| 沒有 join 相依 | 要重跑 Phase 4 分析層 |
| | 重出的 CSV 若有任何數字差異，**先前報告的數字全部要重新對帳** |
| | 違反「不動既有 pipeline」原則，需你先確認 |

### 方案 C：階段1 掃描器順手重算 sharpe

| 優點 | 缺點 |
|---|---|
| 不相依 `stats.parquet` | 🔴 **直接違反 v9 已定案**：「採用學姊論文的 `daily_sharpe` 計算，視為正確、直接使用、**不當 bug、不重算**」|
| 掃描器本來就要讀日報酬 | 重算要自訂年化假設 → 產生**第二套 sharpe 數字**，與已報告的不一致 |
| | 論文要多解釋「為什麼有兩個 sharpe」，純粹是自找麻煩 |

## 1.5 建議

**採方案 A，並強制加上對帳斷言。**

理由：
1. 資料已存在且實測 100% 命中，沒有理由重算或重跑。
2. CAGR 對帳到 1e-17 提供了「同源」的**程式化證據**——方案 A 唯一的弱點（隱性相依）因此可以被自動檢查掉：只要 join 後 CAGR/MDD/win_ratio 三欄有任一列對不上就中止，`stats.parquet` 與 CSV 不同步這件事就不可能靜默發生。
3. 方案 B 的成本（重跑 + 重新對帳已報告數字）遠大於收益；方案 C 違反已定案。

**影響範圍**：只影響階段0 規格，下游無感。

---

# 落差 2 · 候選產物散在 L3 / L4 兩個 job 目錄

## 2.1 問題描述

三份架構文件都假設候選策略的回測產物在單一位置。實際上：

| job 目錄 | 目錄數 | 內容 |
|---|---|---|
| `TW_L3_openSec_M` | 4,599 | **全部 `__v0`** |
| `TW_L4_openSec_M` | 4,599 | **全部 `__v1`** |
| `US_L3_openSec_M` | 3,948 | 全部 `__v0` |
| `US_L4_openSec_M` | 3,948 | 全部 `__v1` |

候選池 = 兩者聯集中通過門檻者（台 4,495 v0 + 2,667 v1 = 7,162）。

## 2.2 為什麼會發生

Phase 4 的設計是「v0/v1 逐一配對比較」——v0 的回測在 Phase 3 就跑完了，Phase 4 只需要補跑 v1 再兩兩比較。這是**正確且省算力的設計**，但它的副作用是產物按「哪個 Phase 跑的」而非「屬於哪個候選池」分家。架構文件寫的時候還沒有這個實作細節。

## 2.3 實測

對全部 14,078 個候選逐一檢查對應目錄是否存在 → **缺失 0 個**。原料完整。

## 2.4 三個方案

### 方案 A：規則解析 + 物化成 `artifacts_dir` 欄位（建議）

階段0 依 `V` 值算出路徑，寫成 `candidate_index` 的一個欄位，並逐列 assert 目錄存在。

| 優點 | 缺點 |
|---|---|
| **磁碟零成本** | 規則本身是隱性知識（v0→L3 / v1→L4）|
| 下游只讀欄位，**完全不需要知道這條規則** | 若未來 job 命名改變，規則會失效 |
| 存在性斷言可偵測 `results_artifacts/` 被搬動 | （上述兩點都被「物化成欄位 + 斷言」化解）|

### 方案 B：建 symlink / junction 統一目錄

在 `_frozen/artifacts/{market}/{strategy}` 建連結指向真實位置。

| 優點 | 缺點 |
|---|---|
| 下游只認一個根目錄，路徑邏輯完全消失 | Windows junction 需權限（**RDP 連校內電腦，權限已知會被擋**）|
| | 不可攜：換機器 / 備份還原全部斷掉 |
| | 老師要親自跑程式（M6），他的環境不會有這些連結 |

### 方案 C：物理複製整併成單一目錄

| 優點 | 缺點 |
|---|---|
| 概念最單純 | **+19.8 GB 磁碟**（`results_artifacts` 已 58.5 GB）|
| 可攜性佳 | 製造第二份真實來源 → 不同步風險 |
| | 複製 14,078 × 4 個小檔，耗時且易中斷 |

## 2.5 建議

**採方案 A。** 理由：磁碟零成本、可驗證，而且它把「v0 在 L3、v1 在 L4」這個知識**從約定變成資料**——下游讀 `artifacts_dir` 欄位就好，不需要任何人記得這條規則。方案 B 在本專案的環境（RDP + 老師要換機器跑）是明確的壞選擇；方案 C 的 20 GB 代價換不到對等的好處。

**影響範圍**：與落差 1 同屬階段0，一起實作。

---

# 落差 3 · 🔴 台股歷史起始期參差（最高風險項）

## 3.1 問題描述

研究部 v9 階段3 寫：「報酬矩陣：**14,078 × 312 個月（2000-01 ~ 2025-12）**」。此假設不成立。

**全量實測（14,078 個策略全掃，非抽樣）**：

| 市場 | 最早起始 | 中位起始 | P95 起始 | 最晚起始 | 月數 min / 中位 / max |
|---|---|---|---|---|---|
| **台股** | 2000-06 | **2003-04** | 2006-09 | **2007-05** | 224 / 273 / 307 |
| 美股 | 2000-02 | 2000-12 | 2001-11 | 2002-03 | 286 / 301 / 311 |

台股起始年分布：
```
2000:     3        2004:   735
2001:   380        2005: 1,169
2002: 3,079        2006:   628
2003: 1,166        2007:     2
```

**台股跨度 7 年**（2000-06 ~ 2007-05），美股僅 2 年。所有策略的結束期都是 2025-12（一致）。

## 3.2 為什麼會發生

兩個原因疊加：

1. **台股財報資料洞**（已知問題，已記錄）：台股 2000–2004 的財報資料幾乎全空、約 90% 是假 0。不同因子的資料可得性不同，因子有值的時間點不同 → 策略的第一次選股時點就不同。
2. **F1×F2×C 的交集效應**：策略要等 F1、F2、C 三個構面**全部**有資料才能開始。三個構面各自的起始期不同，交集自然被推遲，且推遲幅度因組合而異。

這是資料收集的客觀限制，不是回測程式的 bug。

## 3.3 為什麼這件事嚴重（不只是少幾年資料）

HRP 的數學基礎是：相關矩陣 → 距離 `d(i,j)=sqrt(0.5×(1−ρ(i,j)))` → 階層聚類。

如果每一對策略用**各自的重疊窗**算相關（pairwise-complete）：

1. 相關矩陣**不保證半正定（PSD）** —— 因為它不是任何單一資料集的真實相關矩陣，而是一堆不同樣本的拼貼；
2. PSD 不成立 → 距離 `d` **可能違反三角不等式**；
3. `scipy.cluster.hierarchy.linkage` 假設輸入是合法的距離度量。餵給它一個違反三角不等式的矩陣，**它不會報錯，會照樣給你一棵樹**——一棵在數學上沒有意義的樹。

這是最危險的失敗模式：**不會爆炸，只會安靜地給出錯的答案**。而整個階段3、實戰部的 cluster 配額、群間互補、HRP 配權全部建立在這棵樹上。

## 3.4 實測：共同窗的 trade-off

| 窗起點 | 台股保留 | 台股保留率 | 美股保留 | 美股保留率 | 可用月數 |
|---|---|---|---|---|---|
| 2002-01 | 383 | 5.3% | 6,915 | 100.0% | 288 |
| 2003-01 | 3,462 | 48.3% | 6,916 | 100% | 276 |
| 2004-01 | 4,628 | 64.6% | 6,916 | 100% | 264 |
| 2005-01 | 5,363 | 74.9% | 6,916 | 100% | 252 |
| 2006-01 | 6,532 | 91.2% | 6,916 | 100% | 240 |
| **2007-01** | **7,160** | **99.97%** | 6,916 | 100% | **228** |
| 2008-01 | 7,162 | 100% | 6,916 | 100% | 216 |

台美合併樹（XM）：2007-01 → **14,076 / 14,078 = 99.99%**，228 個月。

**關鍵觀察**：台股從 2006-01 移到 2007-01，只多犧牲 12 個月，卻把保留率從 91.2% 拉到 99.97%（多留 628 個策略）。這是一個非常划算的轉折點。

## 3.5 四個方案

### 方案 A：全池單一共同窗 2007-01

所有樹都用 2007-01 ~ 2025-12（228 個月）。

| 優點 | 缺點 |
|---|---|
| PSD 保證 | **美股白白陪葬 7 年**——美股本來 2002-01 就能 100% 保留（288 月）|
| 保留 99.99%（僅犧牲 2 個策略）| 美股樹的統計效力被台股的資料問題綁架 |
| 一個窗，論文最好解釋 | |
| 實作最簡單 | |

### 方案 B：每棵樹各自的共同窗（建議）

| 樹 | 窗 | 月數 | 保留 |
|---|---|---|---|
| 台↔台 | 2007-01 ~ 2025-12 | 228 | 7,160 / 7,162 |
| 美↔美 | **2002-01** ~ 2025-12 | **288** | 6,915 / 6,916 |
| 台↔美（XM）| 2007-01 ~ 2025-12 | 228 | 14,076 / 14,078 |

| 優點 | 缺點 |
|---|---|
| 每棵樹各自 PSD 保證 | 三棵樹的窗不同 → **群間相關數值不可直接跨樹比較** |
| 美股樹多 60 個月（**+26% 樣本**）| 論文方法論要多解釋一句 |
| 台股樹與 XM 樹保留率仍達 99.97%+ | |
| 符合 v9 定案「三組平權、各自獨立有用」的定位 | |

### 方案 C：分位窗 + 排除長尾（原 SDD DD-03 的建議，**已被數據否決**）

取市場內 P95 分位（台股 2006-09）為窗，歷史不足者排除。

| 優點 | 缺點 |
|---|---|
| 理論上比全池窗多留一些月份 | 🔴 **實測後不划算**：2006-09 vs 2007-01 只多 4 個月，卻要犧牲約 9%（~630 個）台股策略 |
| | 老師明確要「留一個軍團讓 AI 選」——**砍策略比砍月份代價高得多** |
| | 多一個 `excluded_from_tree` 欄位與相應說明，複雜度增加卻沒換到東西 |

> 我在 SDD v1 建議此方案時只有 150 個抽樣，抽樣沒抓到 2007-05 那條長尾、也低估了 2006 年的密度。全量數據出來後這個方案應該撤銷。

### 方案 D：pairwise-complete（僅列為對照，不可用）

| 優點 | 缺點 |
|---|---|
| 資料用好用滿 | 🔴 見 3.3：非 PSD、距離違反三角不等式、**樹在數學上無意義且不會報錯** |

## 3.6 建議

**採方案 B（每棵樹各自共同窗），並加做方案 E 的穩健性檢定。**

**核心理由 —— 被捨棄的台股 2000–2006 本來就是不可信的那一段：**

台股 2000–2004 財報幾乎全空、約 90% 是假 0，這是已知且已記錄的資料問題；先前做過的乾淨期間穩健性檢定就是用 2005–2025 重驗的，結論是主要 primary 判定不變。也就是說，**把台股 HRP 窗設在 2007-01 不是「損失了資料」，而是「排除了已知不可信的資料」**——資料品質反而是提升的。這一點讓方案 B 的代價從「犧牲」變成「淨化」，論文也好交代。

**其餘理由**：
1. **危機樣本沒有損失**：228 個月 = 2007-01 ~ 2025-12，涵蓋 2008 金融海嘯、2015、2018、2020 疫情崩、2022 熊市——所有關鍵危機段全在，危機樹的樣本不受影響。這是最重要的一點，因為「危機時策略共同崩潰」是論文支柱之一。
2. **美股沒有理由陪葬**：per-tree 窗讓美股樹保住 288 個月（24 年），比方案 A 多 26% 樣本。
3. **跨樹不可比是可接受的**：v9 已定案三組樹是「三種平行的互補性工具」，本來就不是拿來互比數值的。Agent1 查的是「這棵樹裡哪些群互補」，不是「台股樹的 0.3 跟美股樹的 0.3 誰比較大」。

**🆕 方案 E（配套穩健性檢定，非替代方案）**：主分析用方案 B，另外用 2003-01 窗（台股保留 48.3%）跑一次台股樹，比較兩個窗下的**群結構一致性（ARI）**。若一致性高 → 證明群結構不是窗選擇的產物；若低 → 這件事本身要寫進 limitations。成本很低（階段3 多跑一次），但論文防禦力提升很多。

**待你確認**：方案 B 的三個窗（台 2007-01 / 美 2002-01 / XM 2007-01）。

**影響範圍**：階段3 全部；`returns_meta.parquet` 需記錄每策略的 `hist_start`；strategy_map 需記錄每棵樹用的窗（進 MANIFEST）；論文方法論需說明。

---

# 落差 4 · 月頻 / 日頻描述矛盾

## 4.1 問題描述

研究部 v9 內部自相矛盾：

- 階段3 正文：「報酬矩陣…用**月頻**，與階段 2b 對齊」
- 改動 4 補充 A：「要算常態相關+危機相關，**必須先把每個策略的每日報酬存下來**，才能按牛熊 cycle 切分段算」

到底哪個是待辦、哪個是規格，接手者無從判斷。

## 4.2 為什麼會發生

補充 A 寫在較早的版本，當時還不確定回測產物存了什麼；階段3 正文是 v9 改成 HRP 時重寫的。兩段沒有互相對照。

## 4.3 實測：兩者都在，而且完全一致

| 檔案 | 頻率 | 大小 | 全庫 |
|---|---|---|---|
| `return_table.parquet` | 月（年×12 矩陣）| 12 KB | **169 MB** |
| `stock_data.parquet` | 日（淨值序列）| 155 KB | 2.2 GB |

**一致性驗證**（台美 × L3/L4 各取一策略，把日淨值複利成月報酬，與 `return_table` 逐月比對）：

| 樣本 | 共同月數 | max\|diff\| | >1e-6 的月數 |
|---|---|---|---|
| TW_L3 | 262 | 2.220e-16 | 0 |
| US_L3 | 303 | 4.441e-16 | 0 |
| TW_L4 | 262 | 2.220e-16 | 0 |
| US_L4 | 302 | 2.220e-16 | 0 |

差異在 float64 精度極限。**`return_table` 就是 `stock_data` 的月度彙總，沒有任何獨立資訊**——等於系統已經幫我們把月報酬預算好了。

（附帶驗證：日頻起點 2004-03-31 → 月表首個非零月 2004-04，完全吻合。這證明用「首個非零月」偵測策略起始期是正確的，落差3 的全量掃描方法因此站得住。）

## 4.4 三個方案

### 方案 A：月頻（`return_table`）為 HRP 權威源，日頻只供需要日資料的欄位（建議）

| 優點 | 缺點 |
|---|---|
| I/O **169 MB vs 2.2 GB（13 倍差距）** | 無實質缺點——已證明兩者數值完全相同 |
| 與階段 2b 月頻總經天然對齊 | |
| 已預算好，不需重算 | |

### 方案 B：一律從日頻 `stock_data` 重算月報酬

| 優點 | 缺點 |
|---|---|
| 單一來源，不依賴 `return_table` 的正確性 | 讀 2.2 GB 而非 169 MB，**13 倍 I/O** |
| | 已實測結果完全相同 → **純浪費**|

### 方案 C：HRP 直接用日頻算相關

| 優點 | 缺點 |
|---|---|
| 樣本數 ×21 | 日頻相關被非同步交易、換股日效應污染，相關係數失真 |
| | 與階段 2b 月頻總經對應**不對齊** |
| | 14,078 × ~4,700 日的矩陣，記憶體暴增 |
| | 老師明確說「相關有很多種，不用弄太細，跑一個 Pearson 即可」 |

## 4.5 建議

**採方案 A。** 並且在架構文件明確註記：**v9 改動 4 補充 A 的「必須先把每日報酬存下來」這個待辦，實際上早已滿足**（`stock_data.parquet` 一直都在，且 `return_table.parquet` 連月度彙總都做好了），**不需要任何額外工作**。這條待辦應該從文件裡標記為已完成，否則接手者會以為還有一件事沒做。

**加做（成本極低）**：階段3 完成後，抽 200 個策略用日頻算一次相關，比對群結構是否翻盤。當作一句話的穩健性註記。

**影響範圍**：階段1 掃描器規格（確定讀哪些檔）、階段3 輸入。

---

# 落差 5 · 🆕 台美策略字串碰撞（量測時發現的新問題）

## 5.1 問題描述

**這不在原本的四個落差裡，是我做落差 1 的主鍵檢查時發現的。**

台股與美股的候選池中，**有 1,381 個完全相同的策略字串**。例如：

```
PS_qb0of3__OCF_E_qb2of3__C18_FCF_P_DYN_qmax8__v0
P_IC_qb0of3__NETDEBT_EBITDA_qb1of3__C5_ROE_DYN_yoy__v0
EV_S_qb0of3__ACCRUAL_qb0of3__C18_FCF_P_DYN_qmax8__v0
```

（市場**內部**則無重複：台股 0 個、美股 0 個。）

## 5.2 為什麼會發生

台美用的是**同一套因子命名規則與同一組 C 條件**（這是刻意的設計，讓兩個市場可比）。策略字串只編碼「F1桶 × F2桶 × C × V」，**不編碼市場**。兩個市場的候選池各自獨立產生，自然會出現相同組合都入選的情況——1,381 個碰撞正是「台美有共通的有效因子組合」的表現，本身是好消息。

## 5.3 為什麼嚴重

研究部 v9 階段 0 要「**合併台美兩份 CSV → 統一索引**」。合併後若以 `strategy` 為主鍵：

- `merge` 會產生**笛卡兒積**：1,381 個字串各自配對出 2 列 → 憑空多出 1,381 列，或
- `set_index` / `join` 會**靜默取錯**：台股策略拿到美股的 `effective_n`、美股策略被分到台股的 cluster

而且**不會有任何錯誤訊息**。錯誤會一路傳到 strategy_map、傳到 Agent1 的快篩、傳到最終建議卡。1,381 / 14,078 ≈ **9.8% 的資料會錯**，而從結果上幾乎看不出來。

## 5.4 三個方案

### 方案 A：複合主鍵 `strategy_uid = market + "::" + strategy`（建議）

| 優點 | 缺點 |
|---|---|
| 簡單、**人眼可讀**（debug 時一眼看出是哪個市場）| 字串較長 |
| 排序穩定，台美自然分組 | 所有下游都要改用它 |
| 與現有 `market` 欄位不衝突，兩者並存 | |

### 方案 B：雜湊代理鍵 `sha1(market + strategy)[:12]`

| 優點 | 缺點 |
|---|---|
| 定長、檔名友善 | **完全不可讀**，debug 極痛苦 |
| | 需要維護一張對照表，多一個出錯點 |
| | 論文附錄列策略時要反查 |

### 方案 C：不合併，台美全程分開，只在 XM 樹時臨時加前綴

| 優點 | 缺點 |
|---|---|
| 階段0 改動最小 | XM 合併樹、產出A（20年對照表）、實戰部跨市場挑兵**都需要合併視角**，遲早要面對 |
| | 「臨時加前綴」分散在多個地方 → 更容易漏掉某一處，風險反而比統一處理高 |
| | 快篩要跨市場撈時，Agent1 拿到的清單沒有唯一鍵 |

## 5.5 建議

**採方案 A**，並在 `contracts.py` 對每一份產物加 `assert df.strategy_uid.is_unique`。

理由：這類「靜默錯 join」的 bug 是最貴的——它不會讓程式崩潰，只會讓結論悄悄變錯，而且愈晚發現愈難回頭查。用一個可讀的複合鍵 + 一行斷言就能徹底杜絕，投入產出比極高。方案 B 的可讀性損失在碩論情境（要在附錄列策略、要跟老師討論具體策略）代價太大。

**影響範圍**：全管線契約層。**現在改是零成本，等階段1 跑完再改要重跑。**

---

# 建議的處理順序

| 順序 | 落差 | 為何排這裡 |
|---|---|---|
| 1 | **#5 主鍵** | 契約層決定，影響所有後續產物。**現在改零成本，晚改要重跑** |
| 2 | **#3 共同窗** | 最高風險、決定階段3 可行性；已有全量數據，可立刻定案 |
| 3 | #1 + #2 | 同屬階段0，一起實作（半天）|
| 4 | #4 | 只是文件註記 + 規格確認，零工時 |

---

# 需要你拍板的四件事

| # | 決定 | 我的建議 | 若選別的會怎樣 |
|---|---|---|---|
| 1 | 補 sharpe 的方式 | **join `stats.parquet` + 對帳斷言** | 選重出 CSV 要重跑 Phase 4 並重新對帳已報告數字 |
| 2 | 產物路徑處理 | **規則解析物化成欄位** | 選 symlink 在 RDP/換機器會斷；選複製要 +19.8 GB |
| 3 | **HRP 共同窗** | **每棵樹各自窗：台 2007-01(228月) / 美 2002-01(288月) / XM 2007-01(228月)** + 2003-01 窗做穩健性對照 | 選單一窗美股白損 26% 樣本；選 pairwise 樹不可信 |
| 4 | 主鍵 | **`market::strategy` 複合鍵** | 不改的話 9.8% 資料會靜默錯配 |

確認後我會把這四項回填進 SDD v1（含撤銷 DD-03 原方案），並同步修正三份架構文件的對應段落，然後開始 M0。

---

*落差處理方案 v1 · 2026-08-20 · 全量實測基礎（14,078 策略全掃，非抽樣）*
